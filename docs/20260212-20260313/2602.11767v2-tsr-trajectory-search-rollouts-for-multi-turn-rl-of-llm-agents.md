---
title: "TSR: Trajectory-Search Rollouts for Multi-Turn RL of LLM Agents"
title_zh: TSR：用于大语言模型智能体多轮强化学习的轨迹搜索展开
authors: "Aladin Djuhera, Swanand Ravindra Kadhe, Farhan Ahmed, Heiko Ludwig, Holger Boche"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.11767v2"
tags: ["query:slm-rl"]
score: 9.0
evidence: 多轮强化学习智能体的轨迹搜索回溯
tldr: "针对大语言模型智能体在多轮强化学习中面临的奖励稀疏和环境随机性挑战，本文提出 TSR（轨迹搜索回滚）方法。该方法将推理时的搜索策略（如 Best-of-N、束搜索）引入训练阶段的回滚过程，通过轻量级树搜索生成高质量轨迹。TSR 与优化算法无关，能显著提升训练稳定性和模型性能，在 Sokoban 等任务上实现高达 15% 的性能提升。"
selection_source: fresh_fetch
motivation: 传统多轮强化学习在处理稀疏奖励和随机环境时，简单的轨迹采样容易导致训练不稳定和模式崩溃。
method: TSR 在训练回滚阶段采用轻量级树搜索（如 Best-of-N 或浅层前瞻），利用任务反馈选择高质量动作来构建训练轨迹。
result: "在 Sokoban、FrozenLake 和 WebShop 等任务中，TSR 配合 PPO 或 GRPO 实现了最高 15% 的性能提升，且学习过程更加稳定。"
conclusion: 将搜索机制从推理端前置到训练回滚阶段，是增强多轮智能体学习能力的一种简单、通用且有效的机制。
---

## 摘要
大语言模型（LLM）的进展正推动着一种转变，即利用强化学习（RL）通过跨任务的迭代、多轮交互来训练智能体。然而，多轮强化学习仍然面临挑战，因为奖励通常是稀疏或延迟的，且环境可能具有随机性。在这种情况下，朴素的轨迹采样可能会阻碍利用并导致模式崩溃。我们提出了 TSR（Trajectory-Search Rollouts，轨迹搜索展开），这是一种训练时方法，它重新利用了测试时缩放（test-time scaling）的思想，以改进每轮展开（rollout）的生成。TSR 执行轻量级的树状搜索，通过利用任务特定的反馈在每一轮选择高分动作，从而构建高质量的轨迹。这在保持底层优化目标不变的情况下，提高了展开质量并稳定了学习过程，使得 TSR 与优化器无关。我们使用 best-of-N、束搜索（beam search）和浅层前瞻搜索（shallow lookahead search）实例化了 TSR，并将其与 PPO 和 GRPO 结合，在 Sokoban、FrozenLake 和 WebShop 任务上实现了高达 15% 的性能提升和更稳定的学习，且仅增加了一次性的训练计算量。通过将搜索从推理阶段转移到训练的展开阶段，TSR 为更强的多轮智能体学习提供了一种简单且通用的机制，与现有框架和拒绝采样风格的选择方法互补。

## 速览
**TLDR**：针对大语言模型智能体在多轮强化学习中面临的奖励稀疏和环境随机性挑战，本文提出 TSR（轨迹搜索回滚）方法。该方法将推理时的搜索策略（如 Best-of-N、束搜索）引入训练阶段的回滚过程，通过轻量级树搜索生成高质量轨迹。TSR 与优化算法无关，能显著提升训练稳定性和模型性能，在 Sokoban 等任务上实现高达 15% 的性能提升。 \
**Motivation**：传统多轮强化学习在处理稀疏奖励和随机环境时，简单的轨迹采样容易导致训练不稳定和模式崩溃。 \
**Method**：TSR 在训练回滚阶段采用轻量级树搜索（如 Best-of-N 或浅层前瞻），利用任务反馈选择高质量动作来构建训练轨迹。 \
**Result**：在 Sokoban、FrozenLake 和 WebShop 等任务中，TSR 配合 PPO 或 GRPO 实现了最高 15% 的性能提升，且学习过程更加稳定。 \
**Conclusion**：将搜索机制从推理端前置到训练回滚阶段，是增强多轮智能体学习能力的一种简单、通用且有效的机制。

---

## Abstract
Advances in large language models (LLMs) are driving a shift toward using reinforcement learning (RL) to train agents from iterative, multi-turn interactions across tasks. However, multi-turn RL remains challenging as rewards are often sparse or delayed, and environments can be stochastic. In this regime, naive trajectory sampling can hinder exploitation and induce mode collapse. We propose TSR (Trajectory-Search Rollouts), a training-time approach that repurposes test-time scaling ideas for improved per-turn rollout generation. TSR performs lightweight tree-style search to construct high-quality trajectories by selecting high-scoring actions at each turn using task-specific feedback. This improves rollout quality and stabilizes learning while leaving the underlying optimization objective unchanged, making TSR optimizer-agnostic. We instantiate TSR with best-of-N, beam, and shallow lookahead search, and pair it with PPO and GRPO, achieving up to 15% performance gains and more stable learning on Sokoban, FrozenLake, and WebShop tasks at a one-time increase in training compute. By moving search from inference time to the rollout stage of training, TSR provides a simple and general mechanism for stronger multi-turn agent learning, complementary to existing frameworks and rejection-sampling-style selection methods.

---

## 论文详细总结（自动生成）

### 论文总结：TSR（轨迹搜索展开）用于大语言模型智能体多轮强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向交互式智能体演进，多轮强化学习（Multi-turn RL）成为训练的关键。然而，多轮 RL 面临三大挑战：
*   **奖励稀疏或延迟**：动作序列长，仅在末尾有奖励，导致优势估计（Advantage Estimation）方差极大。
*   **不可逆陷阱**：早期的一个错误动作（如推箱子到死角）可能导致整条轨迹报废，浪费训练算力。
*   **训练不稳定性**：朴素的随机采样容易导致“模式崩溃”或“回声陷阱（Echo Trap）”，即模型性能在训练中突然大幅下降。
**研究动机**：借鉴推理时缩放（Test-time Scaling）的思想，探讨如果在训练的“展开（Rollout）”阶段投入更多计算量进行搜索，能否提升训练的质量和稳定性。

#### 2. 方法论：TSR（Trajectory-Search Rollouts）
TSR 的核心思想是将**树搜索机制**从推理阶段提前到训练的轨迹生成阶段。
*   **核心流程**：在每一轮交互时，不再只采样一个动作，而是采样多个候选动作，利用评分函数（Scoring Function）进行筛选，构建高质量轨迹后再喂给优化器（如 PPO 或 GRPO）。
*   **关键技术细节**：
    *   **三种搜索变体**：
        1.  **Best-of-N**：独立采样 N 条完整轨迹，保留得分最高的一条。
        2.  **束搜索（Beam Search）**：每轮维护 B 个最优前缀，通过分支因子 M 扩展并保留高分路径。
        3.  **浅层前瞻（Shallow Lookahead）**：在当前步模拟未来 D 步的结果来决定当前动作。
    *   **评分函数 $S(\cdot)$**：在确定性任务中使用环境奖励；在稀疏奖励任务（如 FrozenLake）中使用启发式距离评分；在延迟奖励任务（如 WebShop）中使用进度/语义对齐评分。
    *   **优化器无关性**：TSR 仅改变数据生成方式，不修改 PPO/GRPO 的损失函数，具有极强的通用性。

#### 3. 实验设计
*   **场景与数据集**：
    *   **Sokoban（推箱子）**：确定性逻辑推理任务。
    *   **FrozenLake（冰湖）**：随机性导航任务，奖励极度稀疏。
    *   **WebShop**：长程网页购物任务，奖励延迟。
*   **Benchmark 与对比方法**：
    *   基于 **RAGEN** 框架。
    *   对比基准：**Instance Filtering**（实例过滤，仅在任务层面筛选，不在动作层面搜索）。
    *   模型：Qwen2.5-0.5B 和 Qwen2.5-3B。
*   **评估指标**：成功率、训练稳定性（梯度范数）、推理效率（响应长度和交互轮数）。

#### 4. 资源与算力
*   **硬件配置**：使用了 **8 张 NVIDIA A100 GPU**。
*   **软件框架**：使用 Ray 进行分布式执行，vLLM 加速采样，FSDP（完全分片数据并行）进行训练。
*   **训练时长**：论文附录提供了详细的墙钟时间分析。例如，在 WebShop 任务上，TSR（束搜索）训练约需 15 小时，而基准方法约需 6 小时。虽然单步耗时增加，但 TSR 收敛所需的步数更少。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同性质的环境（确定性、随机性、长程），2 种模型规模，以及 3 种搜索策略。
*   **消融实验**：对搜索预算（束宽 B、采样数 M、深度 D）进行了详细的缩放分析。
*   **公平性**：所有方法在过滤后提供给优化器的轨迹数量一致，确保了对比的客观性。实验结果通过 3 次运行的均值和标准差展示，具有统计说服力。

#### 6. 主要结论与发现
*   **性能显著提升**：TSR 在所有任务上均优于基准，WebShop 任务提升高达 15%。
*   **小模型逆袭**：经过 TSR 训练的 **0.5B 模型**在特定任务上的表现超过了 **GPT-4o** 和 **Qwen2.5-72B**。
*   **训练更稳定**：TSR 有效消除了梯度尖峰，防止了模式崩溃。
*   **推理更高效**：虽然训练时增加了计算量，但训练出的模型在推理时更“果断”，交互轮数和回复长度显著减少。

#### 7. 优点
*   **模块化与通用性**：不改变底层 RL 算法，易于集成到现有框架。
*   **解决稀疏奖励痛点**：通过每轮搜索，即使在环境奖励缺失时也能通过代理评分引导模型学习。
*   **计算量可调**：通过搜索参数（N, B, M）灵活平衡训练成本与模型质量。

#### 8. 不足与局限
*   **依赖评分函数**：在完全没有环境反馈或无法定义代理评分的任务中，TSR 的效果会受限。
*   **训练初期开销**：单次迭代的计算成本高于朴素采样，虽然收敛快，但对即时算力资源有更高要求。
*   **代理评分偏差**：如果代理评分函数设计不当，可能会引导模型学习到错误的启发式策略。

（完）
