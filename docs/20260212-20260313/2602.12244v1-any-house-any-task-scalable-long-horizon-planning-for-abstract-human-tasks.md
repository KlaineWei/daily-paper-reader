---
title: "Any House Any Task: Scalable Long-Horizon Planning for Abstract Human Tasks"
title_zh: Any House Any Task：面向抽象人类任务的可扩展长程规划
authors: "Zhihong Liu, Yang Li, Rengming Huang, Cewu Lu, Panpan Cai"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.12244v1"
tags: ["query:slm-rl"]
score: 8.0
evidence: 机器人可扩展的长程规划
tldr: 本研究针对大规模家居环境中机器人任务规划的扩展性难题，提出了Any House Any Task (AHAT) 框架。该框架利用大语言模型将模糊的人类指令和场景图映射为PDDL定义的接地子目标，并结合符号推理生成长程最优规划。为提升模型对复杂意图的分解能力，引入了TGPO强化学习算法，通过外部修正优化推理轨迹。实验证明，AHAT在处理简短指令但需复杂执行的任务上显著优于现有SOTA方法，有效解决了长程规划中的歧义与规模化挑战。
selection_source: fresh_fetch
motivation: 现有的机器人任务规划方法在面对大规模环境、长程规划及模糊指令时，性能往往会随环境复杂度的增加而迅速下降。
method: 提出AHAT框架，通过训练LLM将指令转化为PDDL子目标并结合符号推理，并利用集成外部修正的TGPO强化学习算法优化推理过程。
result: 在具有简短指令但需复杂执行步骤的家居任务中，AHAT的性能显著超越了目前的提示词工程、传统规划及学习方法。
conclusion: 结合LLM的语义理解能力与符号推理的严谨性，并通过针对性强化学习优化，是实现大规模长程机器人任务规划的有效途径。
---

## 摘要
开放世界中基于语言的任务规划对于在大规模家庭环境中运行的机器人至关重要。尽管最近许多工作尝试通过提示（prompting）或训练利用大语言模型（LLMs）来解决这一问题，但可扩展性仍然是一个关键挑战。随着环境规模、规划长度、指令模糊性和约束复杂性的增加，性能往往会迅速下降。在这项工作中，我们提出了 Any House Any Task (AHAT)，这是一种针对模糊人类指令下的大型环境长程规划而优化的家庭任务规划器。其核心在于，AHAT 利用经过训练的 LLM 将任务指令和文本场景图映射为在规划领域定义语言（PDDL）中定义的落地子目标（grounded subgoals）。随后，通过显式符号推理求解这些子目标，以生成可行且最优的长程规划。为了增强模型分解复杂且模糊意图的能力，我们引入了 TGPO，这是一种新型强化学习算法，它将中间推理轨迹的外部修正集成到群体相对策略优化（GRPO）中。实验表明，AHAT 在性能上显著优于最先进的提示、规划和学习方法，特别是在以指令简短但需要复杂执行计划为特征的人类风格家庭任务中。

## 速览
**TLDR**：本研究针对大规模家居环境中机器人任务规划的扩展性难题，提出了Any House Any Task (AHAT) 框架。该框架利用大语言模型将模糊的人类指令和场景图映射为PDDL定义的接地子目标，并结合符号推理生成长程最优规划。为提升模型对复杂意图的分解能力，引入了TGPO强化学习算法，通过外部修正优化推理轨迹。实验证明，AHAT在处理简短指令但需复杂执行的任务上显著优于现有SOTA方法，有效解决了长程规划中的歧义与规模化挑战。 \
**Motivation**：现有的机器人任务规划方法在面对大规模环境、长程规划及模糊指令时，性能往往会随复杂度的增加而迅速下降。 \
**Method**：提出AHAT框架，通过训练LLM将指令转化为PDDL子目标并结合符号推理，并利用集成外部修正的TGPO强化学习算法优化推理过程。 \
**Result**：在具有简短指令但需复杂执行的真实感家居任务中，AHAT的性能显著超越了当前的提示词工程、规划及学习方法。 \
**Conclusion**：结合LLM的语义理解能力与符号推理的严谨性，并通过针对性强化学习优化，是实现大规模长程机器人任务规划的有效途径。

---

## Abstract
Open world language conditioned task planning is crucial for robots operating in large-scale household environments. While many recent works attempt to address this problem using Large Language Models (LLMs) via prompting or training, a key challenge remains scalability. Performance often degrades rapidly with increasing environment size, plan length, instruction ambiguity, and constraint complexity. In this work, we propose Any House Any Task (AHAT), a household task planner optimized for long-horizon planning in large environments given ambiguous human instructions. At its core, AHAT utilizes an LLM trained to map task instructions and textual scene graphs into grounded subgoals defined in the Planning Domain Definition Language (PDDL). These subgoals are subsequently solved to generate feasible and optimal long-horizon plans through explicit symbolic reasoning. To enhance the model's ability to decompose complex and ambiguous intentions, we introduce TGPO, a novel reinforcement learning algorithm that integrates external correction of intermediate reasoning traces into Group Relative Policy Optimization (GRPO). Experiments demonstrate that AHAT achieves significant performance gains over state-of-the-art prompting, planning, and learning methods, particularly in human-style household tasks characterized by brief instructions but requiring complex execution plans.

---

## 论文详细总结（自动生成）

这是一份关于论文《Any House Any Task: Scalable Long-Horizon Planning for Abstract Human Tasks》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大规模家居环境下的机器人长程任务规划**。
*   **研究动机**：现有的基于大语言模型（LLM）的规划方法（如提示词工程或端到端训练）在面对真实世界挑战时存在严重的“不可扩展性”。当环境规模增大、规划步数变长、人类指令变得抽象模糊、或者物理约束变复杂时，模型性能会迅速崩溃。
*   **核心挑战**：如何让机器人在拥有数百个物体的复杂房屋中，理解类似“为节日聚会打扫房间”这种不包含具体步骤的抽象指令，并生成可行且最优的动作序列。

### 2. 方法论：核心思想与技术细节
AHAT 采用了一种**“神经+符号”混合架构**，并结合了改进的强化学习算法。
*   **核心思想**：将高层的“意图理解与任务分解”交给 LLM，将底层的“约束满足与路径搜索”交给符号规划器（PDDL Planner）。
*   **规划流程**：
    1.  **任务分解**：LLM 接收指令和文本场景图，生成自然语言形式的“推理轨迹”（Decomposition Trace）。
    2.  **子目标接地**：LLM 将分解后的步骤转化为 PDDL 格式的子目标（Subgoals）及相关物体。
    3.  **符号求解**：使用现成的 PDDL 规划器对每个子目标进行顺序求解，最终拼接成完整的长程计划。
*   **关键技术：TGPO 算法**：
    *   在群体相对策略优化（GRPO）的基础上，引入了**轨迹引导（Trace-Guided）**机制。
    *   **外部修正**：当规划失败时，利用一个更强的辅助模型（如 GPT-5）修正中间的推理轨迹（即任务分解步骤）。
    *   **约束采样**：在强化学习的 Rollout 阶段，强制模型遵循修正后的轨迹（Token-level constrained sampling），但让模型自主生成对应的 PDDL 子目标。这解决了强化学习在复杂推理任务中“信度分配”难的问题。

### 3. 实验设计
*   **数据集与场景**：
    *   **AHAT Benchmark**：基于 HSSD 和 Gibson 物理场景构建，包含 308 个房屋场景图和 1.6k 个用户画像（Persona），合成了 50k 个训练任务。
    *   **测试维度**：分为 Easy（显式指令）、Complex（长程复杂约束）、Abstract（高层抽象意图）三个子集。
*   **对比 Benchmark**：
    *   **PARTNR**：多智能体协作与推理基准。
    *   **Behavior-1K**：包含 1000 种日常活动的具身智能基准。
    *   **Human Tasks**：通过众包收集的真实人类简短指令。
*   **对比方法**：
    *   通用模型：GPT-5, Gemini-3.0-Pro。
    *   提示词规划器：SayPlan, DELTA。
    *   学习类方法：SFT（监督微调）, GRPO, Reinforce++。

### 4. 资源与算力
*   **模型基础**：实验基于 **Qwen-2.5-7B** 作为基础模型进行微调。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了训练包含 3 个 Epoch 的 SFT 预热，随后进行 TGPO 强化学习迭代。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估，包括在自建基准上的数百个任务测试，以及在两个主流公开基准上的验证。
*   **消融实验**：设计了针对“子目标规划 vs 端到端规划”以及“TGPO vs 其他 RL 算法”的对比实验。
*   **可扩展性测试**：专门针对场景规模（30-180 节点）、规划长度（1-31+ 动作）、指令抽象度、约束复杂度进行了四维度的压力测试。
*   **评价**：实验设计非常**充分且客观**。通过引入人类真实指令和第三方公开基准，有效避免了自建数据集的偏差风险。

### 6. 主要结论与发现
*   **性能领先**：AHAT 在所有测试集上均显著优于基准方法。在最难的“抽象任务”中，AHAT 的成功率比最强基准高出约 68.9%。
*   **鲁棒的可扩展性**：随着环境规模和规划长度增加，AHAT 的性能下降极其缓慢（仅约 16%），而其他方法（如 SayPlan 或直接用 GPT-5）则会出现断崖式下跌。
*   **推理的重要性**：实验证明，通过 TGPO 优化推理轨迹，比单纯增加训练数据或使用基础 RL 能更有效地提升模型处理模糊指令的能力。

### 7. 优点（亮点）
*   **解耦设计**：巧妙地将 LLM 的常识推理能力与 PDDL 的逻辑严谨性结合，避免了 LLM 在处理长程逻辑约束时的“幻觉”。
*   **数据合成创新**：引入“用户画像”生成指令，使合成数据更贴近真实人类的多样化表达（如不同职业、文化背景对家务的要求不同）。
*   **TGPO 算法**：通过外部修正引导 RL 训练，为解决复杂逻辑任务的强化学习提供了一种高效的新思路。

### 8. 不足与局限
*   **模态限制**：目前主要依赖文本形式的场景图（Textual Scene Graph），这在处理视觉细节（如物体的精确位姿、遮挡关系）时可能存在信息损失。
*   **闭域假设**：依赖预定义的 PDDL 领域定义，对于完全未见过的动作类型或物理交互，系统的泛化能力受限。
*   **环境假设**：实验假设环境是全观测且静态的，未充分考虑感知噪声、动态障碍物或在线重规划的挑战。

（完）
