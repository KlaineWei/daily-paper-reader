---
title: "PhGPO: Pheromone-Guided Policy Optimization for Long-Horizon Tool Planning"
title_zh: PhGPO：面向长程工具规划的信息素引导策略优化
authors: "Yu Li, Guangfeng Cai, Shengtian Yang, Han Luo, Shuo Han, Xu He, Dong Li, Lei Feng"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13691v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 智能体长程工具规划的策略优化
tldr: 针对大语言模型在长程多步工具规划中面临的搜索空间爆炸问题，本文提出了一种受蚁群算法启发的“信息素引导策略优化”（PhGPO）方法。该方法通过从历史成功轨迹中学习工具转移模式（即信息素），并利用其引导策略优化，使模型能够复用历史经验，从而显著提升了长程工具规划的效率和成功率。
selection_source: fresh_fetch
motivation: 长程工具规划面临组合爆炸挑战，且历史成功路径中的转移模式未能在后续训练中得到充分复用。
method: 提出PhGPO，通过学习历史成功轨迹中的转移模式作为“信息素”，并将其作为显式引导来优化策略。
result: 实验结果表明，PhGPO在处理长程工具规划任务时表现出显著的有效性。
conclusion: PhGPO通过引入可复用的转移模式引导，有效解决了复杂工具调用场景下的策略优化难题。
---

## 摘要
大语言模型（LLM）智能体的最新进展展示了通过工具使用执行复杂任务的强大能力。然而，长程多步工具规划具有挑战性，因为探索空间面临组合爆炸。在这种情况下，即使找到了正确的工具使用路径，通常也仅被视为当前训练的即时奖励，无法为后续训练提供任何可重用的信息。在本文中，我们认为历史成功的轨迹包含可重用的工具转换模式，这些模式可以在整个训练过程中被利用。受蚁群算法的启发（其中历史成功的路径可以通过信息素来反映），我们提出了信息素引导的策略优化（PhGPO），它从历史轨迹中学习基于轨迹的转换模式（即信息素），并利用学习到的信息素来引导策略优化。这种学习到的信息素提供了明确且可重用的引导，将策略优化引向历史成功的工具转换，从而改进了长程工具规划。全面的实验结果证明了我们提出的 PhGPO 的有效性。

## Abstract
Recent advancements in Large Language Model (LLM) agents have demonstrated strong capabilities in executing complex tasks through tool use. However, long-horizon multi-step tool planning is challenging, because the exploration space suffers from a combinatorial explosion. In this scenario, even when a correct tool-use path is found, it is usually considered an immediate reward for current training, which would not provide any reusable information for subsequent training. In this paper, we argue that historically successful trajectories contain reusable tool-transition patterns, which can be leveraged throughout the whole training process. Inspired by ant colony optimization where historically successful paths can be reflected by the pheromone, we propose Pheromone-Guided Policy Optimization (PhGPO), which learns a trajectory-based transition pattern (i.e., pheromone) from historical trajectories and then uses the learned pheromone to guide policy optimization. This learned pheromone provides explicit and reusable guidance that steers policy optimization toward historically successful tool transitions, thereby improving long-horizon tool planning. Comprehensive experimental results demonstrate the effectiveness of our proposed PhGPO.

---

## 论文详细总结（自动生成）

以下是对论文《PhGPO: Pheromone-Guided Policy Optimization for Long-Horizon Tool Planning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）智能体在执行复杂任务时，往往需要调用长达 10-20 步的工具序列。在这种长程规划中，工具选择面临**组合爆炸**的挑战。
*   **研究动机**：现有的强化学习（RL）方法通常将成功的工具调用路径隐式地吸收进模型参数中，缺乏对成功转移模式（Transition Patterns）的显式记录和复用。当成功轨迹稀少时，RL 的探索效率极低。
*   **核心思想**：受蚁群算法（ACO）启发，论文提出将历史成功的工具转移模式提炼为**“信息素”（Pheromone）**，作为显式的转移先验知识，引导策略优化过程，从而提高长程规划的成功率和样本效率。

### 2. 方法论：核心思想与关键技术
PhGPO 将工具规划建模为在工具转移图上的导航任务，核心步骤包括：
*   **工具转移图构建**：基于模型上下文协议（MCP）构建有向图，节点代表工具，边代表工具间的转移。同时构建“参数调用集”以处理具体的工具参数模式。
*   **信息素机制（显式先验）**：
    *   **任务无关信息素**：记录全局范围内频繁且成功的工具转移，通过“沉积”（成功后增加）和“挥发”（随时间衰减）进行更新。
    *   **任务相关信息素**：利用任务描述的嵌入向量（Embedding）从记忆库中检索相似任务的成功经验，提供针对当前上下文的引导。
    *   **信息素融合**：结合上述两者，生成最终的转移先验概率。
*   **渐进式训练流水线**：
    1.  **监督预热**：通过下个工具预测任务进行初步微调，建立基础能力。
    2.  **信息素引导的 RL**：使用 **GRPO（群组相对策略优化）** 作为后端。在采样阶段，将模型输出的概率与信息素先验相结合，增加探索到高潜力路径的概率。
    3.  **课程学习**：训练过程中逐渐减少“教师强制”（Oracle Guidance），增加任务长度，最终过渡到完全由信息素引导的自主规划。

### 3. 实验设计
*   **数据集/场景**：
    *   **Toolathlon**：包含数百个工具的复杂 MCP 环境。
    *   **TRAJECT-Bench**：侧重于生产级 API 的轨迹感知基准。
    *   **TOUCAN**：大规模合成的工具智能体数据集。
*   **对比方法**：
    *   **提示与规划类**：ReAct, Plan-and-Solve, Beyond ReAct。
    *   **检索增强类（RAG）**：Gorilla, ToolReAGt, Tool Graph Retriever。
    *   **强化学习类**：ToRL, StepTool, TreeRL。
    *   **图导航类**：ToolNet, GAP, NaviAgent, GTool。
*   **评估指标**：匹配率（Match Ratio，轨迹相似度）和下个工具准确率（Next-tool Accuracy）。

### 4. 资源与算力
*   **模型底座**：使用了 Qwen2.5-7B 和 Llama3.1-8B。
*   **微调技术**：采用 LoRA（r=64, α=128）。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到了训练参数（如 Batch size 隐含在群组采样 M=5 中，Epochs 为 25 等），属于典型的中等规模 LLM 微调实验。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流长程工具基准上进行了测试，涵盖了从 3 步到 13 步以上的平均轨迹长度。
*   **消融实验**：非常充分。验证了信息素引导、混合课程学习、挥发机制、任务相关性等组件的贡献。
*   **鲁棒性测试**：进行了超参数敏感性分析（针对 8 个关键参数）以及优化器不可知性测试（验证了 PhGPO 在 GRPO、PPO、RLOO 下均有效）。
*   **客观性**：通过可视化信息素矩阵的演变过程，直观展示了模型如何从随机探索转向聚焦于“黄金路径”，实验设计逻辑严密。

### 6. 主要结论与发现
*   **性能领先**：PhGPO 在所有基准测试中均显著优于现有方法，尤其在长程任务中，相比基准 RL 方法提升幅度更大。
*   **显式先验的价值**：显式的转移先验（信息素）比隐式的参数更新更能有效解决稀疏奖励下的探索难题。
*   **长度缩放效应**：随着轨迹长度增加，PhGPO 带来的相对增益（Relative Gain）显著扩大，证明了其在处理极长序列时的优越性。
*   **收敛速度**：信息素引导显著加快了发现第一条高质量路径的速度（路径发现效率提升约 20%-25%）。

### 7. 优点（亮点）
*   **跨学科借鉴**：成功将经典的蚁群算法思想引入 LLM 智能体规划，解决了 RL 在大动作空间中的探索效率问题。
*   **显式记忆管理**：通过信息素的沉积与挥发，实现了一种动态更新、可解释且可复用的智能体“经验存储”机制。
*   **训练策略稳健**：渐进式训练（预热 -> 引导 -> 自主）有效缓解了 RL 训练初期的不稳定性。

### 8. 不足与局限
*   **依赖参考轨迹**：信息素的初始积累和质量评估在很大程度上依赖于基准测试提供的“参考轨迹”（Golden Paths），在完全无监督或无参考的环境下表现未知。
*   **计算开销**：虽然提升了采样效率，但维护工具转移图和进行任务相关性检索会增加额外的系统复杂度和内存开销。
*   **模拟环境局限**：实验主要在模拟器（Simulator）中进行，未充分讨论在真实世界 API 调用中面临的延迟、成本及动态变化的工具文档问题。

（完）
