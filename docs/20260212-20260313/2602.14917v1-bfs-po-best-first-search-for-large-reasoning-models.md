---
title: "BFS-PO: Best-First Search for Large Reasoning Models"
title_zh: BFS-PO：大推理模型的最佳优先搜索
authors: "Fiorenzo Parascandolo, Wenhui Tan, Enver Sangineto, Ruihua Song, Rita Cucchiara"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.14917v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 专注于效率的强化学习算法，旨在减少推理模型中的过度思考
tldr: 针对大推理模型（LRM）在强化学习过程中容易出现的“过度思考”和输出冗长问题，本文提出了 BFS-PO 算法。该方法引入最佳优先搜索（Best-First Search）探索策略，通过基于最大熵节点的重溯机制寻找最短正确答案。实验证明，BFS-PO 在提升模型推理准确率的同时，能显著缩短推理链长度，降低计算成本，实现了性能与效率的双重提升。
selection_source: fresh_fetch
motivation: 现有的强化学习算法常导致大推理模型产生冗长且低效的推理链，即“过度思考”现象，增加了计算开销。
method: 提出 BFS-PO 算法，利用最佳优先搜索和最大熵节点回溯机制，在训练中引导模型探索并学习更短的正确路径。
result: 在多个基准测试中，BFS-PO 成功提高了模型的推理准确性，并显著减少了生成内容的长度。
conclusion: BFS-PO 证明了通过优化搜索策略可以有效缓解大模型的过度思考问题，同时提升推理性能和生成效率。
---

## 摘要
大推理模型（LRMs），如 OpenAI o1 和 DeepSeek-R1，通过长推理链在推理任务中表现出了卓越的性能。然而，这也导致了计算成本的显著增加以及冗余输出的产生，这种现象被称为“过度思考”（overthinking）。强化学习（RL）算法（如 GRPO/DAPO）往往会加剧这种过度思考的倾向。在本文中，我们提出了 BFS-PO，这是一种利用最佳优先搜索（Best-First Search）探索策略来缓解该问题的强化学习算法。具体而言，BFS-PO 利用基于最大熵节点的回溯机制来寻找最短的正确答案。通过在训练过程中生成逐渐变短的回复，BFS-PO 学会了产生简洁的推理链。通过在不同的基准测试和基础 LRM 上进行实验，我们证明了 BFS-PO 能够同时提高 LRM 的准确性并缩短其回答长度。

## Abstract
Large Reasoning Models (LRMs) such as OpenAI o1 and DeepSeek-R1 have shown excellent performance in reasoning tasks using long reasoning chains. However, this has also led to a significant increase of computational costs and the generation of verbose output, a phenomenon known as overthinking. The tendency to overthinking is often exacerbated by Reinforcement Learning (RL) algorithms such as GRPO/DAPO. In this paper, we propose BFS-PO, an RL algorithm which alleviates this problem using a Best-First Search exploration strategy. Specifically, BFS-PO looks for the shortest correct answer using a backtracking mechanism based on maximum entropy nodes. By generating progressively shorter responses during training, BFS-PO learns to produce concise reasoning chains. Using different benchmarks and base LRMs, we show that BFS-PO can simultaneously increase the LRM accuracy and shorten its answers.

---

## 论文详细总结（自动生成）

以下是对论文《BFS-PO: Best-First Search for Large Reasoning Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大推理模型（LRM，如 OpenAI o1、DeepSeek-R1）虽然通过长思维链（CoT）提升了准确率，但普遍存在“过度思考”（Overthinking）现象，即生成包含大量冗余、错误或仅为了文本连贯性的无用 Token，导致计算成本高昂。
*   **核心挑战**：现有的强化学习（RL）算法（如 GRPO/DAPO）在处理负奖励时，由于长度归一化机制，往往会诱导模型生成更长的回复。而现有的缩短 CoT 的方法（如 SFT 或剪枝）通常以牺牲准确率为代价。
*   **整体含义**：本文旨在开发一种 RL 算法，在不降低（甚至提高）推理准确率的前提下，引导模型自主发现并学习更简洁的推理路径。

### 2. 论文提出的方法论：BFS-PO
*   **核心思想**：将 RL 的探索过程从“独立并行采样”转变为“基于搜索树的启发式探索”。通过**最佳优先搜索（Best-First Search）**策略，不断在当前已发现的“最短正确路径”基础上进行回溯和重新探索。
*   **关键技术细节**：
    1.  **树构建与选择**：在每轮迭代中，从搜索树中选择当前长度最短且结果正确的完整路径作为“最佳路径”。
    2.  **最大熵回溯（Backtracking）**：利用 Token 的生成熵（Entropy）作为不确定性的度量。选择最佳路径中熵值最高的 Token 作为回溯点，因为高熵节点通常代表模型做出关键逻辑决策的“分叉路口”。
    3.  **树扩展**：从回溯点开始，利用旧策略模型采样 $G$ 个新的分支，并加入搜索树。
    4.  **分支优势函数（Branch Advantage）**：扩展了 GRPO 的优势计算方法。对于树中的每个节点，将其奖励与其同级分支的平均奖励进行比较，计算局部优势，从而提供更精准、更密集的监督信号。
*   **算法流程**：初始化树 -> 迭代 $K$ 次（选择最短正确路径 -> 找最大熵节点 -> 扩展新分支） -> 计算分支优势 -> 执行策略优化。

### 3. 实验设计
*   **数据集**：
    *   **数学推理**：GSM8K, MATH-500。
    *   **综合科学**：MMLU-STEM。
    *   **竞赛级/域外测试**：AIME’25, MINERVA-MATH（用于验证泛化性）。
*   **基准模型（Base LRMs）**：Qwen2.5-3B/7B-Instruct, Llama-3.1-8B-Instruct。
*   **对比方法**：
    *   Zero-shot（基准）。
    *   SFT（监督微调）。
    *   TokenSkip（基于 GPT-4 评分的 CoT 压缩方法）。
    *   DAPO（带长度惩罚的先进 RL 算法）。
    *   Tree of Thoughts（基于外部模型评估的搜索方法）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 SXM4 GPU (64GB)**。
*   **训练时长**：
    *   在 GSM8K 上训练约 **12 小时**。
    *   在 MATH-500 上训练约 **24 小时**。
*   **效率提升**：由于搜索树结构允许复用父节点的 KV Cache，BFS-PO 的训练速度比 DAPO 快约 **1.37 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构/规模的模型，在 5 个主流基准测试上进行了验证。
*   **消融实验**：非常充分。作者对比了：
    *   迭代次数 $K$ 的影响。
    *   有无 BFS 策略（对比随机回溯）。
    *   分支优势函数 vs. 全局优势函数。
    *   有无长度惩罚（$R_l$）。
*   **客观性**：实验不仅关注准确率和长度，还引入了 **AES（准确率效率得分）** 这一综合指标，确保对比的公平性。同时进行了跨领域的域外（Out-of-domain）测试，验证了方法的鲁棒性。

### 6. 论文的主要结论与发现
*   **双重提升**：BFS-PO 能够**同时**提高模型的推理准确率并显著缩短 CoT 长度。这打破了以往“简洁必失准”的僵局。
*   **搜索优于采样**：基于最大熵的回溯机制比 GRPO 的独立采样更能有效探索逻辑空间。
*   **泛化能力强**：在 MATH-500 上训练的模型在 AIME’25 等更难的域外任务上表现优于 DAPO，准确率提升最高达 4 个百分点以上。
*   **自发简洁性**：模型在训练过程中学会了省略冗余的单位说明和重复的计算步骤，生成更干练的数学表达。

### 7. 优点（亮点）
*   **无需 PRM**：与传统的树搜索（如 Tree of Thoughts）不同，BFS-PO 不需要额外的过程奖励模型（PRM）或外部 LLM 来评估中间步骤，仅依靠最终结果验证和 Token 熵，降低了实现门槛。
*   **训练效率高**：通过树状结构的 KV Cache 复用，实现了比传统 RL 更快的训练速度。
*   **逻辑驱动**：利用熵寻找决策点的做法具有很强的统计学和认知科学解释力。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 3B 到 8B 的中小型模型上，尚未在 70B 或更大规模的模型上验证其扩展性（Scaling Law）。
*   **回溯深度**：虽然使用了 $K$ 次迭代，但对于极复杂的长程推理任务，简单的最大熵回溯是否足以覆盖所有关键逻辑错误仍有待商榷。
*   **计算开销**：尽管比 DAPO 快，但 RL 训练本身（尤其是涉及多次采样的树构建）依然比单纯的 SFT 消耗更多资源。

（完）
