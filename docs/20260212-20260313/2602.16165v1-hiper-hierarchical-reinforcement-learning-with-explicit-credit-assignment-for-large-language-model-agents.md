---
title: "HiPER: Hierarchical Reinforcement Learning with Explicit Credit Assignment for Large Language Model Agents"
title_zh: HiPER：具有显式信用分配的大语言模型智能体分层强化学习
authors: "Jiangweizhi Peng, Yuanxin Liu, Ruida Zhou, Charles Fleming, Zhaoran Wang, Alfredo Garcia, Mingyi Hong"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16165v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 用于大模型智能体长程任务的分层规划执行强化学习框架
tldr: 针对大语言模型智能体在长程、稀疏奖励任务中信用分配困难的问题，本文提出HiPER框架。该框架采用分层强化学习，将策略分解为生成子目标的高层规划器和执行动作的底层执行器。通过引入分层优势估计（HAE）技术，在规划和执行两个层面显式分配信用，显著降低了梯度估计方差。实验表明，HiPER在ALFWorld和WebShop等基准测试中刷新了SOTA记录，尤其在复杂多步任务中表现优异。
selection_source: fresh_fetch
motivation: 传统的扁平化强化学习策略在处理长程、稀疏奖励任务时，难以在漫长的动作序列中进行有效的信用分配。
method: 提出HiPER框架，将策略解耦为高层规划与底层执行，并利用分层优势估计（HAE）在两个层级上分别进行信用分配。
result: "在ALFWorld和WebShop基准测试中，HiPER配合Qwen2.5-7B模型分别达到了97.4%和83.3%的成功率，显著超越了现有方法。"
conclusion: 显式的分层分解和层级化信用分配是提升多轮对话LLM智能体在复杂长程任务中训练效率和性能的关键。
---

## 摘要
将大语言模型（LLM）训练为用于多轮决策的交互式智能体仍然具有挑战性，特别是在具有稀疏和延迟奖励的长程任务中，智能体必须在接收到有意义的反馈之前执行长序列的动作。大多数现有的强化学习（RL）方法将 LLM 智能体建模为在单一时间尺度上运行的扁平策略，每轮选择一个动作。在稀疏奖励设置下，这种扁平策略必须在没有显式时间抽象的情况下在整个轨迹中传播信用，这通常会导致优化不稳定和信用分配效率低下。我们提出了 HiPER，一种新型的分层“计划-执行”强化学习框架，它显式地将高层规划与底层执行分离。HiPER 将策略分解为提出子目标的高层规划器和在多个动作步骤中执行这些子目标的底层执行器。为了使优化与此结构保持一致，我们引入了一项名为分层优势估计（HAE）的关键技术，该技术在规划和执行两个层面仔细分配信用。通过聚合每个子目标执行期间的回报并协调两个层面的更新，HAE 提供了一个无偏梯度估计器，并且与扁平的广义优势估计（GAE）相比，可证明地降低了方差。实验表明，HiPER 在具有挑战性的交互式基准测试中实现了最先进的性能，在使用 Qwen2.5-7B-Instruct 时，在 ALFWorld 上达到了 97.4% 的成功率，在 WebShop 上达到了 83.3%（比之前的最佳方法分别提高了 6.6% 和 8.3%），在需要多个依赖子任务的长程任务中增益尤为显著。这些结果强调了显式分层分解对于多轮 LLM 智能体可扩展强化学习训练的重要性。

## 速览
**TLDR**：针对大语言模型智能体在长程任务中面临的稀疏奖励和信用分配难题，本文提出HiPER框架。该框架采用分层强化学习结构，将策略分解为生成子目标的高层规划器和执行具体动作的底层执行器。通过引入分层优势估计（HAE）技术，在规划和执行两个层面进行显式信用分配，有效降低了梯度估计方差。实验表明，HiPER在ALFWorld和WebShop等基准测试中显著提升了性能，尤其在复杂长程任务中表现优异。 \
**Motivation**：现有的扁平化强化学习方法在处理具有稀疏奖励的长程任务时，难以在缺乏时间抽象的情况下进行有效的信用分配。 \
**Method**：提出HiPER框架，将策略分解为规划器和执行器，并利用分层优势估计（HAE）在两个层级上分别进行信用分配和优化。 \
**Result**：在ALFWorld和WebShop基准测试中分别达到97.4%和83.3%的成功率，显著超越了现有的SOTA方法。 \
**Conclusion**：显式的分层分解和针对性的信用分配对于提升多轮对话LLM智能体在复杂长程任务中的训练效率和性能至关重要。

---

## Abstract
Training LLMs as interactive agents for multi-turn decision-making remains challenging, particularly in long-horizon tasks with sparse and delayed rewards, where agents must execute extended sequences of actions before receiving meaningful feedback. Most existing reinforcement learning (RL) approaches model LLM agents as flat policies operating at a single time scale, selecting one action at each turn. In sparse-reward settings, such flat policies must propagate credit across the entire trajectory without explicit temporal abstraction, which often leads to unstable optimization and inefficient credit assignment.   We propose HiPER, a novel Hierarchical Plan-Execute RL framework that explicitly separates high-level planning from low-level execution. HiPER factorizes the policy into a high-level planner that proposes subgoals and a low-level executor that carries them out over multiple action steps. To align optimization with this structure, we introduce a key technique called hierarchical advantage estimation (HAE), which carefully assigns credit at both the planning and execution levels. By aggregating returns over the execution of each subgoal and coordinating updates across the two levels, HAE provides an unbiased gradient estimator and provably reduces variance compared to flat generalized advantage estimation.   Empirically, HiPER achieves state-of-the-art performance on challenging interactive benchmarks, reaching 97.4\% success on ALFWorld and 83.3\% on WebShop with Qwen2.5-7B-Instruct (+6.6\% and +8.3\% over the best prior method), with especially large gains on long-horizon tasks requiring multiple dependent subtasks. These results highlight the importance of explicit hierarchical decomposition for scalable RL training of multi-turn LLM agents.

---

## 论文详细总结（自动生成）

以下是对论文《HiPER: Hierarchical Reinforcement Learning with Explicit Credit Assignment for Large Language Model Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体强化学习（RL）方法大多采用“扁平化（Flat）”策略，即在单一时间尺度上逐轮选择动作。在长程（Long-horizon）且奖励稀疏的任务中，这种方式难以在漫长的动作序列中进行有效的信用分配（Credit Assignment），导致优化不稳定、效率低下。
*   **研究动机**：作者观察到成功完成长程任务的智能体轨迹往往具有隐含的分层结构（即由多个子目标组成的片段）。因此，提出将这种隐含结构显式化，通过分层架构来提升智能体在复杂环境中的决策能力。

### 2. 方法论：HiPER 框架
HiPER（Hierarchical Plan-Execute RL）的核心思想是将策略分解为高层规划和底层执行。
*   **Plan-Execute 接口**：通过结构化系统提示词，要求 LLM 在每一步输出三个部分：
    1.  `<switch>`：决定是保持（KEEP）当前子目标还是切换（SWITCH）新目标。
    2.  `<subgoal>`：当前的子目标描述。
    3.  `<action>`：具体的环境交互动作。
*   **分层优势估计（HAE）**：这是本文的关键技术，用于解决不同时间尺度决策的信用分配问题：
    *   **底层（执行）优势**：在子目标片段内，计算动作层面的 TD 残差，并将其末端引导至下一个边界的高层价值函数，实现层级间的耦合。
    *   **高层（规划）优势**：将每个子目标片段视为一个“宏步（Macro-step）”，根据片段内的累积奖励计算子目标选择的优势。
    *   **切换优势**：通过比较“切换到新目标”与“继续当前目标”的预期价值差，优化切换决策。
*   **算法流程**：采用 Actor-Critic 架构（基于 PPO），使用共享骨干网络但具有双头输出的 Critic 来同时估计高层和底层价值。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：一个基于文本的具身家庭环境，包含 6 类任务（如：寻找并冷却杯子），需要复杂的逻辑推理和多步操作。
    *   **WebShop**：模拟电子商务网站交互，要求智能体根据用户需求搜索并购买特定商品。
*   **对比方法**：
    *   基础模型（Qwen2.5-1.5B/7B-Instruct）。
    *   标准 RL 方法：PPO、RLOO、GRPO。
    *   最先进的智能体 RL 方法：GiGPO（Group-in-Group Policy Optimization）。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen2.5-1.5B 和 7B 两个版本。
*   **训练配置**：训练时长为 150 个 epoch；每个 rollout 包含 128 个独立环境。
*   **算力开销**：文中未明确给出具体的 GPU 型号和总机时，但提到 HiPER 相比扁平化 PPO 仅增加了约 **0.8% 的峰值显存开销**（得益于共享 Critic 骨干网络），证明了该方法在资源占用上的高效性。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准测试上进行了全面评估，每个实验均运行了 **3 个随机种子**并取平均值。
*   **消融实验**：
    *   验证了 Plan-Execute 提示词对性能的影响。
    *   测试了不同规模的 Critic 模型对结果的影响（发现较小的 Critic 也能保持高性能）。
    *   对超参数 `cKEEP`（鼓励保持目标的惩罚项）进行了敏感性分析。
*   **充分性评价**：实验设计较为充分，涵盖了从 1.5B 到 7B 的模型跨度，且对比了最新的 SOTA 方法，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能突破**：HiPER 在 ALFWorld 上达到 97.4% 的成功率，在 WebShop 上达到 83.3%，显著刷新了 SOTA 记录。
*   **长程任务优势**：在需要多个依赖子任务的复杂类别（如 Pick2、Look）中，HiPER 的提升最为显著。
*   **训练效率**：相比 PPO/GRPO，HiPER 实现了约 **2.8 倍的样本效率提升**，且训练曲线更加平滑稳定。
*   **自发规划**：即使没有外部子目标监督，智能体也能通过强化学习自发产生有意义的、可解释的子目标序列。

### 7. 优点
*   **理论完备**：证明了 HAE 是无偏的梯度估计器，且相比扁平化 GAE 能显著降低方差。
*   **显式解耦**：将复杂的长程决策分解为可管理的短程子目标，符合人类解决问题的认知模式。
*   **可解释性**：通过显式的 `<subgoal>` 输出，开发者可以清晰地观察智能体的规划逻辑。
*   **低开销**：在不显著增加算力负担的情况下，大幅提升了训练效果。

### 8. 不足与局限
*   **Token 消耗**：由于每一步都要输出 `<switch>` 和 `<subgoal>` 块，推理时的 Token 消耗量比扁平化策略略高。
*   **任务依赖性**：虽然在文本环境中表现优异，但在动作空间极度连续或缺乏自然语言描述能力的底层控制任务中，该框架的适用性有待验证。
*   **提示词敏感性**：分层结构的有效性可能在一定程度上依赖于系统提示词的设计质量。

（完）
