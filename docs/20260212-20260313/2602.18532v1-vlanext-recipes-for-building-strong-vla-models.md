---
title: "VLANeXt: Recipes for Building Strong VLA Models"
title_zh: VLANeXt：构建强力 VLA 模型的方案
authors: "Xiao-Ming Wu, Bin Fan, Kang Liao, Jian-Jian Jiang, Runze Yang, Yihang Luo, Zhonghua Wu, Wei-Shi Zheng, Chen Change Loy"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18532v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型设计的系统研究
tldr: 针对视觉-语言-动作模型（VLA）领域训练协议和评估设置不统一的问题，本文在统一框架下系统地重新审视了VLA的设计空间。通过对基础组件、感知要素和动作建模三个维度的深入研究，总结出12项构建强力VLA模型的关键发现。基于这些发现，作者提出了VLANeXt模型，在LIBERO基准测试和现实世界实验中均表现出色，并发布了统一的开源代码库以促进社区研究。
selection_source: fresh_fetch
motivation: 当前VLA研究领域因训练协议和评估标准的不一致，导致难以识别影响模型性能的核心设计因素。
method: 从基础组件、感知要素和动作建模三个维度系统剖析VLA设计空间，并提炼出12条构建强力模型的关键准则。
result: VLANeXt模型在LIBERO和LIBERO-plus基准测试中刷新了SOTA记录，并在真实世界实验中证明了其强大的泛化性能。
conclusion: 本研究通过系统化的实验确立了VLA模型的高效构建方案，并为社区提供了统一且易用的研究基准与代码库。
---

## 摘要
随着大型基础模型的兴起，视觉-语言-动作模型（VLA）应运而生，利用强大的视觉和语言理解能力进行通用策略学习。然而，目前的 VLA 领域仍然处于碎片化和探索阶段。尽管许多团队已经提出了各自的 VLA 模型，但训练协议和评估设置的不一致使得难以确定哪些设计选择真正起作用。为了给这个不断发展的领域带来结构化指导，我们在统一的框架和评估设置下重新审视了 VLA 的设计空间。从类似于 RT-2 和 OpenVLA 的简单 VLA 基线出发，我们从三个维度系统地剖析了设计选择：基础组件、感知要素和动作建模视角。通过这项研究，我们提炼出 12 项关键发现，共同构成了构建强力 VLA 模型的实用方案。这一探索的成果是一个简单而有效的模型——VLANeXt。VLANeXt 在 LIBERO 和 LIBERO-plus 基准测试中优于之前的最先进方法，并在现实世界实验中展示了强大的泛化能力。我们将发布一个统一且易于使用的代码库，作为社区复现我们的发现、探索设计空间并在共享基础上构建新 VLA 变体的通用平台。

## Abstract
Following the rise of large foundation models, Vision-Language-Action models (VLAs) emerged, leveraging strong visual and language understanding for general-purpose policy learning. Yet, the current VLA landscape remains fragmented and exploratory. Although many groups have proposed their own VLA models, inconsistencies in training protocols and evaluation settings make it difficult to identify which design choices truly matter. To bring structure to this evolving space, we reexamine the VLA design space under a unified framework and evaluation setup. Starting from a simple VLA baseline similar to RT-2 and OpenVLA, we systematically dissect design choices along three dimensions: foundational components, perception essentials, and action modelling perspectives. From this study, we distill 12 key findings that together form a practical recipe for building strong VLA models. The outcome of this exploration is a simple yet effective model, VLANeXt. VLANeXt outperforms prior state-of-the-art methods on the LIBERO and LIBERO-plus benchmarks and demonstrates strong generalization in real-world experiments. We will release a unified, easy-to-use codebase that serves as a common platform for the community to reproduce our findings, explore the design space, and build new VLA variants on top of a shared foundation.

---

## 论文详细总结（自动生成）

这篇论文《VLANeXt: Recipes for Building Strong VLA Models》对视觉-语言-动作（VLA）模型的设计空间进行了系统性的探索，旨在为构建高性能机器人控制模型提供一套标准化的“方案”。

### 1. 核心问题与整体含义
*   **研究动机**：尽管 VLA 模型（如 RT-2, OpenVLA）在通用机器人控制方面展现了巨大潜力，但当前研究领域处于“碎片化”状态。不同团队采用不同的训练协议、模型架构和评估标准，导致难以辨别哪些设计选择（如骨干网络、感知输入、损失函数）是真正提升性能的关键。
*   **核心目标**：在统一的框架和评估协议下，系统剖析 VLA 的设计空间，提炼出构建强力模型的 12 项关键准则，并据此开发出高性能模型 **VLANeXt**。

### 2. 方法论：核心思想与技术细节
作者从基础组件、感知要素、动作建模三个维度进行了逐步演进的消融研究：
*   **基础组件 (Foundational Components)**：
    *   **策略模块设计**：放弃简单的 Token 复用，采用更深层的专用策略头（MetaQuery 风格），并使用 16 个可学习的 Meta Query。
    *   **VLM-Policy 连接**：提出“软连接”（Soft Connection），在 VLM 和策略模块之间插入可学习的 Query 作为潜在缓冲区，优于松散耦合或层间紧密耦合。
    *   **动作分块 (Action Chunking)**：采用长度为 8 的动作块，以提供更连贯的动作序列预测。
    *   **学习目标**：对比了分类、回归、扩散和流匹配（Flow Matching）。最终选择流匹配，因其在处理多峰分布时表现稳健且优于分类。
*   **感知要素 (Perception Essentials)**：
    *   **多视角融合**：结合第三人称视角和手眼（Wrist）相机视角，显著提升空间感知。
    *   **本体感受 (Proprioception)**：将机器人的状态信息输入到 VLM 端（而非仅输入策略头）效果最好，有助于多模态特征的深度融合。
    *   **时间历史**：实验发现加入过往帧历史反而可能引入噪声，VLANeXt 最终倾向于使用当前帧。
*   **动作建模视角 (Action Modeling)**：
    *   **频域辅助损失**：引入时间序列预测的思想，通过离散余弦变换（DCT）在频域计算预测动作与真实动作的 MSE 损失，以极低成本提升了动作的结构化预测能力。

### 3. 实验设计
*   **数据集与基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，用于测试基础任务能力。
    *   **LIBERO-plus**：在 LIBERO 基础上引入了光照、背景、相机位姿、语言指令变体等扰动，专门测试模型的**鲁棒性与泛化性**。
    *   **真实世界实验**：包括单臂任务（清理桌面、开抽屉放物体）和双臂任务（抬筐、双臂清理桌面）。
*   **对比方法**：对比了 Diffusion Policy, Octo, OpenVLA, $\pi_0$, $\pi_0$-Fast, UniVLA, OpenVLA-OFT 等多种 SOTA 模型。

### 4. 资源与算力
*   **训练细节**：模型在 LIBERO 数据集上训练 10,000 步，Batch Size 为 256。
*   **学习率**：3B 以下模型为 $1 \times 10^{-4}$，更大模型为 $5 \times 10^{-5}$。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100/H100）和数量，但提到“世界建模”辅助任务会使训练时间增加 3 倍，因此在最终方案中将其剔除以保证效率。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了极其详尽的消融实验（Table 1 涵盖了 20 余种变体），从基线逐步叠加改进点，形成了一条清晰的性能提升轨迹。
*   **客观性**：所有消融实验均在统一的 LIBERO 空间套件（Spatial Suite）上进行，并在性能饱和后引入更难的 LIBERO-plus 进行区分，实验设计科学、公平。
*   **跨领域验证**：不仅在仿真环境中测试，还跨越了单臂到双臂（Aloha 系统）的真实硬件验证，证明了方案的普适性。

### 6. 主要结论与发现
1.  **强力 VLM 是基础**：更强的视觉语言骨干（如 Qwen3-VL）能显著提升 VLA 性能。
2.  **软连接优于硬连接**：通过 Query 缓冲区的层级交互能更好地传递表征。
3.  **本体感受位置至关重要**：在 VLM 端注入本体感受信息比在策略头注入更有效。
4.  **频域建模有效**：将动作预测视为时间序列预测并引入频域损失，能以极小开销提升精度。
5.  **VLANeXt 性能卓越**：在 LIBERO-plus 上的平均成功率比之前的 SOTA（OpenVLA-OFT）高出约 10%。

### 7. 优点
*   **系统性强**：不仅给出了一个模型，更给出了一套经过验证的“设计手册”，对后续研究极具参考价值。
*   **效率与性能平衡**：在提升性能的同时，考虑了训练成本（如舍弃高成本的世界建模）。
*   **开源贡献**：承诺发布统一的代码库，有助于解决 VLA 领域复现难、标准不一的问题。

### 8. 不足与局限
*   **时间历史的结论**：发现增加时间历史帧无效，这与部分机器人研究（强调时序上下文）的直觉相悖，可能受限于 LIBERO 任务的特性，在更复杂的长程推理任务中可能仍需重新评估。
*   **算力细节缺失**：未公布具体训练时长和硬件资源消耗，不便于开发者评估复现成本。
*   **数据依赖**：虽然模型架构强大，但其泛化能力仍高度依赖于预训练数据（如 DROID）的质量和多样性。

（完）
