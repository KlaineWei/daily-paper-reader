---
title: "ReSyn: Autonomously Scaling Synthetic Environments for Reasoning Models"
title_zh: ReSyn：为推理模型自主扩展合成环境
authors: "Andre He, Nathaniel Weir, Kaj Bostrom, Allen Nie, Darion Cassel, Sam Bayless, Huzefa Rangwala"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20117v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 在合成环境上进行强化学习训练以提升 7B 模型的推理能力
tldr: "本研究针对推理语言模型训练中手工环境难以扩展的问题，提出了 ReSyn 框架。ReSyn 能够自主生成包含实例生成器和验证器的多样化推理环境，涵盖约束满足、算法谜题和空间推理等任务。通过在 ReSyn 生成的环境中进行强化学习，Qwen2.5-7B 模型在多个推理和数学基准测试中取得显著进步，特别是在挑战性的 BBEH 基准上实现了 27% 的相对提升。该研究证明了大规模合成推理环境是提升模型推理能力的有效途径。"
selection_source: fresh_fetch
motivation: 现有的强化学习训练方法受限于手工设计的推理环境，难以实现大规模扩展和多样化任务覆盖。
method: 提出 ReSyn 流水线，通过自主生成包含实例生成器和验证器的推理环境，实现合成数据的规模化生产。
result: "实验表明，基于 ReSyn 训练的模型在推理和数学基准上均有提升，其中 BBEH 基准的相对性能提高了 27%。"
conclusion: 大规模生成带有验证机制的推理环境并增加任务多样性，是增强语言模型推理能力的有效手段。
---

## 摘要
具有可验证奖励的强化学习（RLVR）通过利用验证器的监督，已成为训练推理语言模型（RLM）的一种极具前景的方法。尽管对于许多任务而言，实现验证器比标注解决方案更容易，但现有的合成数据生成方法在很大程度上仍以解决方案为中心，而基于验证器的方法则依赖于少数手工构建的程序化环境。在这项工作中，我们通过引入 ReSyn 来扩展 RLVR。ReSyn 是一个能够生成配备实例生成器和验证器的多样化推理环境的流水线，涵盖了约束满足、算法谜题和空间推理等任务。使用 ReSyn 数据进行强化学习训练的 Qwen2.5-7B-Instruct 模型在推理基准测试和域外数学基准测试中均取得了持续提升，其中在具有挑战性的 BBEH 基准测试上实现了 27% 的相对提升。消融实验表明，基于验证器的监督和增加的任务多样性都做出了显著贡献，这为大规模生成推理环境可以增强 RLM 的推理能力提供了经验证据。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has emerged as a promising approach for training reasoning language models (RLMs) by leveraging supervision from verifiers. Although verifier implementation is easier than solution annotation for many tasks, existing synthetic data generation methods remain largely solution-centric, while verifier-based methods rely on a few hand-crafted procedural environments. In this work, we scale RLVR by introducing ReSyn, a pipeline that generates diverse reasoning environments equipped with instance generators and verifiers, covering tasks such as constraint satisfaction, algorithmic puzzles, and spatial reasoning. A Qwen2.5-7B-Instruct model trained with RL on ReSyn data achieves consistent gains across reasoning benchmarks and out-of-domain math benchmarks, including a 27\% relative improvement on the challenging BBEH benchmark. Ablations show that verifier-based supervision and increased task diversity both contribute significantly, providing empirical evidence that generating reasoning environments at scale can enhance reasoning abilities in RLMs

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ReSyn** 的框架，旨在通过自主生成多样化的合成推理环境，解决强化学习训练推理模型时面临的任务多样性不足和人工标注成本高的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：近期如 OpenAI-o1 和 DeepSeek-R1 的成功证明了强化学习（RL）能显著提升模型的推理能力。然而，这种训练高度依赖“可验证的奖励”（Verifiable Rewards），目前主要集中在数学和代码领域。
*   **核心挑战**：现有的合成数据生成方法要么是“以解决方案为中心”（容易产生幻觉），要么依赖于少数人工设计的程序化环境（如数独、逻辑谜题），这限制了任务的多样性和模型泛化能力的提升。
*   **核心问题**：如何自动、大规模地生成既具有挑战性又具备可靠验证机制的推理任务环境？

### 2. 方法论
ReSyn 的核心思想是**利用大模型生成“环境代码”而非单纯的“问答对”**。
*   **环境定义**：每个环境 $E$ 包含三个核心 Python 函数：
    1.  **实例生成器 ($\rho_0$)**：根据难度等级随机生成问题参数（如一个随机图或约束条件）。
    2.  **观察函数 ($O$)**：将参数转化为自然语言问题。
    3.  **奖励/验证函数 ($R$)**：利用编程逻辑（如动态规划、图算法）验证模型给出的答案是否正确。
*   **流水线流程**：
    1.  **关键词提取**：从现有基准测试中提取推理关键词（如“拓扑排序”、“约束满足”）。
    2.  **任务合成**：提示 LLM 为每个关键词编写上述三个 Python 函数。
    3.  **双阶段过滤**：首先由“LLM 裁判”检查代码逻辑和验证机制；其次进行**难度校准**，剔除那些难度不随参数增加而变化的任务。
    4.  **强化学习**：使用生成的 $(Q, V)$ 对（问题与验证器），通过 DAPO（一种开源 RL 算法）对模型进行训练。

### 3. 实验设计
*   **基础模型**：Qwen2.5-7B-Instruct。
*   **对比基准 (Benchmarks)**：
    *   **推理类**：BBH (Big-Bench Hard), BBEH (Extra Hard)。
    *   **数学类**：GSM8K, AIME 2024。
*   **对比方法**：
    *   **Instruct 基座模型**：未经 ReSyn 训练的版本。
    *   **SynLogic**：之前最先进的、基于 35 个手工设计逻辑任务的合成数据训练方法。
*   **消融实验**：对比了“基于验证器的 RL”、“基于代码生成答案的 RL”以及“基于纯文本答案的 RL”；同时研究了“任务数量”与“每个任务的实例数量”对性能的影响。

### 4. 资源与算力
*   **训练细节**：模型训练了 400 个更新步数（Update Steps）。
*   **软件框架**：使用了开源的 `verl` 强化学习框架和 `DAPO` 训练配方。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和总耗时，但提到使用了 Ray 进行分布式训练，且训练规模在 7B 参数模型的可控范围内。

### 5. 实验数量与充分性
*   **实验规模**：最终生成了 418 个独特的推理环境，包含 1.6 万个训练实例。
*   **充分性**：实验设计较为全面。不仅在通用推理基准上进行了测试，还跨领域测试了数学能力。
*   **客观性**：通过统计显著性检验（如 Wald 检验用于难度校准，Bootstrap 用于 BBEH 任务对比）来确保结果的可靠性，并纠正了基准测试中常见的答案提取错误，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能显著提升**：ReSyn 训练的模型在 BBH 上相对提升 14%，在极具挑战性的 BBEH 上提升 27%。
*   **跨领域泛化**：尽管训练数据不包含数学题，但在 GSM8K 和 AIME 上的表现也有显著增长，证明了逻辑推理能力的通用性。
*   **生成器-验证器间隙**：实验证明，让 LLM 编写验证逻辑比直接生成正确答案更有效，因为验证通常比求解更容易且更准确。
*   **多样性至上**：消融实验显示，**增加任务的种类（多样性）**比单纯增加同一任务下的题目数量对提升推理能力更有帮助。

### 7. 优点
*   **自主性与可扩展性**：摆脱了人工设计环境的瓶颈，能够自主发现和构建新的推理模式。
*   **高可靠性**：通过代码验证器提供 0/1 奖励，避免了传统合成数据中的标签噪声问题。
*   **难度可控**：引入了自动化的难度校准机制，确保训练数据具有合理的梯度。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B 参数模型，尚未验证该方法在超大规模模型（如 70B+）上的边际效应。
*   **代码质量依赖**：虽然有过滤机制，但如果 LLM 生成的验证代码存在隐蔽 Bug，可能会误导 RL 过程。
*   **领域局限**：目前主要适用于可以被程序化验证的符号、逻辑和算法推理，对于主观性强或缺乏明确真值的推理任务（如伦理判断）尚不适用。

（完）
