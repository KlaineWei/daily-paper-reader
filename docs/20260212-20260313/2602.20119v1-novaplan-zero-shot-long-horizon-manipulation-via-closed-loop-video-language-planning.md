---
title: "NovaPlan: Zero-Shot Long-Horizon Manipulation via Closed-Loop Video Language Planning"
title_zh: NovaPlan：通过闭环视频语言规划实现零样本长程操作
authors: "Jiahui Fu, Junyu Nan, Lingfeng Sun, Hongyu Li, Jianing Qian, Jennifer L. Barry, Kris Kitani, George Konidaris"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20119v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 用于长程机器人操作的分层VLM与视频规划
tldr: NovaPlan是一个分层框架，旨在解决长程机器人操作任务。它结合了闭环视觉语言模型（VLM）规划与视频生成技术，通过将任务分解为子目标并从生成的视频中提取物体关键点和人体手势作为运动先验，实现了无需演示或训练的零样本复杂任务执行与自主错误恢复。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型和视频生成模型在处理长程任务时，往往缺乏实际执行所需的物理接地能力。
method: 采用分层架构，高层利用VLM进行任务分解与闭环监控，底层通过从生成视频中提取物体关键点和手势先验来指导机器人动作。
result: 在多个长程任务和FMB基准测试中，NovaPlan展示了在无预训练情况下的复杂装配能力和灵活的错误恢复行为。
conclusion: 该研究证明了结合闭环语义规划与视频运动先验是实现零样本、高鲁棒性机器人长程操作的有效途径。
---

## 摘要
解决长程任务需要机器人将高层语义推理与底层物理交互相结合。虽然视觉语言模型 (VLM) 和视频生成模型可以分解任务并想象结果，但它们往往缺乏现实世界执行所需的物理接地。我们提出了 NovaPlan，这是一个分层框架，它将闭环 VLM 和视频规划与具有几何接地的机器人执行相统一，用于零样本长程操作。在高层，VLM 规划器将任务分解为子目标，并在闭环中监控机器人执行，使系统能够通过自主重新规划从单步失败中恢复。为了计算底层机器人动作，我们从生成的视频中提取并利用任务相关的物体关键点和人手姿态作为运动学先验，并采用切换机制选择较优者作为机器人动作的参考，即使在严重遮挡或深度不准确的情况下也能保持稳定执行。我们在三个长程任务和功能性操作基准 (FMB) 上证明了 NovaPlan 的有效性。结果表明，NovaPlan 可以在没有任何先验演示或训练的情况下执行复杂的组装任务，并表现出灵巧的错误恢复行为。项目主页：https://nova-plan.github.io/

## Abstract
Solving long-horizon tasks requires robots to integrate high-level semantic reasoning with low-level physical interaction. While vision-language models (VLMs) and video generation models can decompose tasks and imagine outcomes, they often lack the physical grounding necessary for real-world execution. We introduce NovaPlan, a hierarchical framework that unifies closed-loop VLM and video planning with geometrically grounded robot execution for zero-shot long-horizon manipulation. At the high level, a VLM planner decomposes tasks into sub-goals and monitors robot execution in a closed loop, enabling the system to recover from single-step failures through autonomous re-planning. To compute low-level robot actions, we extract and utilize both task-relevant object keypoints and human hand poses as kinematic priors from the generated videos, and employ a switching mechanism to choose the better one as a reference for robot actions, maintaining stable execution even under heavy occlusion or depth inaccuracy. We demonstrate the effectiveness of NovaPlan on three long-horizon tasks and the Functional Manipulation Benchmark (FMB). Our results show that NovaPlan can perform complex assembly tasks and exhibit dexterous error recovery behaviors without any prior demonstrations or training. Project page: https://nova-plan.github.io/

---

## 论文详细总结（自动生成）

这篇论文介绍了 **NovaPlan**，一个旨在实现机器人**零样本（Zero-Shot）长程操作**的分层框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在没有任何任务特定演示或训练的情况下，让机器人完成复杂的长程操作任务（如多层积木堆叠、颜色分类、搜索隐藏物体及精密组装）。
*   **研究动机**：
    *   **语义与物理的断层**：现有的视觉语言模型（VLM）擅长高层推理但缺乏底层物理感知；视频生成模型能“想象”过程但难以转化为精确的机器人控制指令。
    *   **具身鸿沟（Embodiment Gap）**：直接从视频预测动作往往因机器人形态差异导致执行不准。
    *   **长程不稳定性**：开环执行容易累积误差，且视频模型在长序列中易产生幻觉或物理违和。

### 2. 方法论：核心思想与关键技术
NovaPlan 采用**分层闭环架构**，将高层语义规划与底层几何接地执行相结合：
*   **高层：闭环视频语言规划**
    *   **任务分解**：VLM（如 GPT-5.2）将宏观指令分解为子目标序列。
    *   **视频推演（Rollout）**：利用视频生成模型（Wan 2.2, Veo 3.1）为每个子目标生成多个候选视频。
    *   **验证与筛选**：VLM 根据目标一致性、物理规律、运动方向和最终结果四个指标对视频进行评分，选择最优路径。
    *   **闭环监控**：每步执行后，VLM 充当“评论家”，对比初始态、当前态和目标态，若失败则触发自主恢复程序。
*   **底层：混合流（Hybrid Flow）执行**
    *   **物体流（Object Flow）**：通过 3D 关键点跟踪提取物体 6-DoF 轨迹。
    *   **手部流（Hand Flow）**：当物体被遮挡或旋转剧烈时，自动切换到提取视频中“人手”的姿态（使用 HaMeR 模型）作为运动学先验。
    *   **几何校准**：通过 RANSAC 算法和双锚点校准，将生成的视频深度和手部姿态映射到现实世界的度量尺度，补偿投影漂移。
*   **非抓取式恢复（Non-prehensile Recovery）**：针对精密任务，系统能生成“手指拨动/戳（Poke）”等非抓取动作来修正微小偏差。

### 3. 实验设计
*   **实验场景**：
    1.  **四层积木堆叠**：垂直堆叠四块积木，考察空间推理。
    2.  **颜色分类**：将积木放入匹配颜色的容器，其中黄色块需精密对齐以防卡住。
    3.  **隐藏物体搜索**：在抽屉中寻找物体并放置，考察部分可观测下的探索能力。
    4.  **功能性操作基准 (FMB)**：高精度组装任务，涉及不规则形状和毫米级公差。
*   **对比方法（Baselines）**：
    *   **NovaFlow**：纯物体中心的视频驱动方法。
    *   **$\pi_{0.5}$**：先进的视觉-语言-动作（VLA）模型。
    *   **MOKA**：基于 VLM 视觉提示的规划方法。
*   **Benchmark**：在长程任务中，为基准方法提供“先验规划（Oracle Plan）”以确保公平对比其执行能力。

### 4. 资源与算力
*   **硬件环境**：
    *   **感知与生成**：NVIDIA **H100** GPU（负责视频生成、深度估计、3D 跟踪）。
    *   **手部流提取**：NVIDIA **RTX A6000** 工作站。
*   **运行效率**：处理一段 41 帧的 720P 视频，端到端耗时约 **40 秒**（包含视频生成 30s、深度估计 3s、跟踪 3.5s 等）。

### 5. 实验数量与充分性
*   **实验规模**：每个长程任务进行 10 次试验，涵盖不同的初始布局。
*   **充分性**：实验设计较为全面，不仅对比了不同架构（VLA vs. VLM-based），还通过消融实验验证了“手部流”切换的必要性。在 FMB 这种极具挑战性的基准上进行了测试，展示了系统在极端情况下的恢复能力。实验过程客观，明确指出了基准方法在长程逻辑上的短板。

### 6. 主要结论与发现
*   **闭环优于开环**：NovaPlan 的验证-恢复机制使其在长程任务中的成功率远高于开环方法。
*   **手部先验的价值**：引入人手姿态作为参考，有效解决了物体被遮挡时的跟踪失效问题，显著提升了执行稳定性。
*   **零样本潜力**：系统无需任何机器人演示，仅靠互联网规模的视觉/视频基础模型，即可发现如“非抓取式拨动”等复杂行为。
*   **性能领先**：在四层堆叠任务中，NovaPlan 成功率达 70%，而 NovaFlow 仅为 30%，VLA 模型则无法完成。

### 7. 优点（亮点）
*   **混合跟踪机制**：动态切换物体流和手部流，增强了对遮挡和复杂旋转的鲁棒性。
*   **自主错误恢复**：利用 VLM 识别失败模式并生成针对性的修正动作（如戳、拨），而非简单的重置。
*   **几何接地**：提出了一套完整的校准流程，解决了视频生成中常见的尺度缩放和投影扭曲问题。

### 8. 不足与局限
*   **实时性欠佳**：单步决策需 40 秒，无法处理需要快速反应的动态任务。
*   **依赖基础模型质量**：如果视频生成模型产生严重的物理幻觉（如物体凭空消失），系统仍会失效。
*   **单视角局限**：目前主要依赖单目相机，在处理深度信息极其敏感或严重遮挡的复杂组装时，精度仍有提升空间。
*   **计算成本高**：依赖 H100 等顶级算力，部署成本较高。

（完）
