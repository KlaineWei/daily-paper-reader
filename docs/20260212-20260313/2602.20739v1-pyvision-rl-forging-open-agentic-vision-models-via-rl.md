---
title: "PyVision-RL: Forging Open Agentic Vision Models via RL"
title_zh: PyVision-RL：通过强化学习打造开放式智能体视觉模型
authors: "Shitian Zhao, Shaoheng Lin, Ming Li, Haoquan Zhang, Wenshuo Peng, Kaipeng Zhang, Chen Wei"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20739v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 强化学习提升多模态智能体的持续交互能力
tldr: 针对多模态智能体在强化学习中易出现的“交互崩溃”问题（即模型倾向于减少工具使用和多轮推理），本文提出 PyVision-RL 框架。该框架通过过采样-过滤-排序策略和累积工具奖励，稳定了开源模型的训练并鼓励多轮交互。此外，针对视频推理引入了按需上下文构建技术，通过选择性采样关键帧显著降低了视觉 Token 使用量，在提升性能的同时优化了效率。
selection_source: fresh_fetch
motivation: 旨在解决多模态智能体在强化学习中因交互崩溃导致的工具调用减少和多轮推理能力退化问题。
method: 采用过采样-过滤-排序的 Rollout 策略与累积工具奖励机制，并结合按需采样的视频上下文构建技术。
result: 实验表明 PyVision-RL 在图像和视频理解任务上均取得优异表现，并大幅提升了视觉 Token 的利用效率。
conclusion: 维持持续的交互能力和实现按需视觉处理是开发高效、可扩展多模态智能体模型的关键路径。
---

## 摘要
智能体多模态模型的强化学习通常面临交互崩溃（interaction collapse）的问题，即模型会学习减少工具使用和多轮推理，从而限制了智能体行为的优势。我们提出了 PyVision-RL，这是一个针对权重开放多模态模型的强化学习框架，旨在稳定训练并维持交互。我们的方法结合了“过采样-过滤-排序”（oversampling-filtering-ranking）的展开策略与累积工具奖励，以防止崩溃并鼓励多轮工具使用。利用统一的训练流水线，我们开发了用于图像和视频理解的 PyVision-Image 和 PyVision-Video。在视频推理方面，PyVision-Video 采用了按需上下文构建，在推理过程中选择性地采样与任务相关的帧，从而显著减少视觉 token 的使用。实验结果显示了强大的性能和更高的效率，证明了持续交互和按需视觉处理对于构建可扩展的多模态智能体至关重要。

## Abstract
Reinforcement learning for agentic multimodal models often suffers from interaction collapse, where models learn to reduce tool usage and multi-turn reasoning, limiting the benefits of agentic behavior. We introduce PyVision-RL, a reinforcement learning framework for open-weight multimodal models that stabilizes training and sustains interaction. Our approach combines an oversampling-filtering-ranking rollout strategy with an accumulative tool reward to prevent collapse and encourage multi-turn tool use. Using a unified training pipeline, we develop PyVision-Image and PyVision-Video for image and video understanding. For video reasoning, PyVision-Video employs on-demand context construction, selectively sampling task-relevant frames during reasoning to significantly reduce visual token usage. Experiments show strong performance and improved efficiency, demonstrating that sustained interaction and on-demand visual processing are critical for scalable multimodal agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《PyVision-RL: Forging Open Agentic Vision Models via RL》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：尽管大型多模态模型（MLLM）在推理能力上有所提升，但在通过强化学习（RL）训练智能体（Agentic）能力时，普遍面临**“交互崩溃”（Interaction Collapse）**问题。模型在训练过程中会逐渐学会减少工具使用和多轮推理，退化为简单的单跳回答，导致无法充分发挥智能体框架的优势。
*   **核心问题**：如何在开源多模态模型中稳定地进行强化学习，以诱导模型产生持续的、多轮的工具使用行为，并提高处理高分辨率图像和长视频的效率？

### 2. 方法论：PyVision-RL 框架
*   **核心思想**：将 Python 视为原始工具（Dynamic Tooling），通过改进 RL 的采样策略和奖励机制，鼓励模型进行主动的视觉探索。
*   **关键技术细节**：
    *   **智能体脚手架（Agentic Scaffold）**：模型交替生成自然语言推理和 Python 代码块。环境执行代码并返回结果（文本或图像），模型据此继续推理直至给出最终答案。
    *   **过采样-过滤-排序（Oversampling-Filtering-Ranking）策略**：
        1.  **过采样**：生成大量 Rollout 轨迹。
        2.  **过滤**：剔除执行错误、超时或无奖励方差（全对或全错）的样本组。
        3.  **排序**：根据奖励的标准差对样本组进行排序，优先选择难度适中、学习信号强的样本进行训练。
    *   **累积工具奖励（Accumulative Tool Reward）**：
        公式：$R = R_{acc} + 0.1 \cdot n_{tc} \cdot 1\{R_{acc}=1\}$
        其中 $R_{acc}$ 是答案准确性奖励，$n_{tc}$ 是工具调用次数。只有在答案正确时才奖励工具使用，防止模型产生无效的冗余调用。
    *   **按需上下文构建（On-demand Context Construction）**：针对视频任务，视频不直接输入模型，而是仅加载到 Python 运行时。模型通过代码动态采样关键帧并绘图，极大降低了视觉 Token 的消耗。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像理解**：V*、HRBench（视觉搜索）；DynaMath、MathVerse、MathVision、WeMath（多模态数学推理）；TIR-Bench（智能体工具使用）。
    *   **视频理解**：VSI-Bench（空间推理）。
*   **对比方法**：
    *   **静态工具类**：Pixel-Reasoner, Mini-o3, DeepEyes。
    *   **动态工具类**：Thyme, CodeV, CodeDance, DeepEyes-v2。
    *   **基准模型**：Qwen2.5-VL-7B。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   **RL 阶段**：训练 700 个步数（steps）。
    *   **超参数**：过采样 Batch Size 为 32，训练 Batch Size 为 16，Group Size 为 8。
    *   **SFT 阶段**：在单节点上针对图像（7K 样本）和视频（44K 样本）分别进行了一个 epoch 的冷启动训练。

### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个主流图像基准和 1 个视频基准上进行了测试，涵盖了感知、推理和智能体操作等多个维度。
*   **消融实验**：非常充分。作者分别验证了最大轮数限制（Max Turn Budget）、累积工具奖励、标准差排序策略以及 GRPO 算法中优势函数归一化项的影响。
*   **客观性**：通过对比不同 FPS 下的 Token 消耗与准确率曲线，客观展示了该方法在效率上的巨大优势。

### 6. 主要结论与发现
*   **性能领先**：PyVision-Image 在视觉搜索任务上显著超过基线，如在 V* 上比 DeepEyes-v2 高出 6.9%。
*   **效率突破**：PyVision-Video 在 VSI-Bench 上以平均 **5K** 视觉 Token 的代价达到了 44% 的准确率，而 Qwen2.5-VL-7B 需要 **45K** Token 且准确率仅为 38%。
*   **RL 稳定性**：证明了通过标准差排序和移除优势函数归一化，可以显著稳定多模态智能体的 RL 训练过程。

### 7. 优点与亮点
*   **动态视频采样**：打破了传统的均匀采样模式，让模型“按需取帧”，解决了长视频处理的 Token 爆炸问题。
*   **解决交互崩溃**：通过奖励机制的设计，成功让模型学会了“多思考、多用工具”而不是走捷径。
*   **开源贡献**：提供了权重开放的模型和完整的 RL 训练流水线，对开源社区具有重要价值。

### 8. 不足与局限
*   **安全风险**：由于模型以 Python 为原始工具，在实际部署中存在执行恶意代码或误删文件的风险，需要严格的沙箱环境。
*   **推理延迟**：虽然节省了视觉 Token，但多轮交互（Reasoning-Action-Observation）会显著增加端到端的推理时间。
*   **数据依赖**：SFT 阶段仍高度依赖 GPT-4 等闭源模型生成的合成数据进行冷启动。

（完）
