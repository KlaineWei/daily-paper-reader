---
title: Reinforcement-aware Knowledge Distillation for LLM Reasoning
title_zh: 面向大语言模型推理的强化感知知识蒸馏
authors: "Zhaoyang Zhang, Shuli Jiang, Yantao Shen, Yuting Zhang, Dhananjay Ram, Shuo Yang, Zhuowen Tu, Wei Xia, Stefano Soatto"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22495v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 通过强化学习感知方法将推理能力蒸馏至小模型
tldr: 针对大语言模型推理能力提升带来的高推理成本问题，本文提出了一种强化学习感知蒸馏方法（RLAD）。该方法通过信任区域比率蒸馏（TRRD）替代传统的KL散度正则项，将教师模型的指导与学生模型的强化学习过程动态结合，解决了分布不匹配和目标冲突问题。在逻辑推理和数学任务中，RLAD显著优于离线蒸馏和传统的在线蒸馏方法，有效提升了小模型的推理性能。
selection_source: fresh_fetch
motivation: 传统的知识蒸馏在结合强化学习时，存在教师监督与学生分布不匹配以及KL正则项与奖励最大化目标相互干扰的问题。
method: 提出RLAD框架，核心是TRRD机制，它将教师模型与旧策略混合作为锚点，通过似然比目标实现优势感知的选择性模仿。
result: 在逻辑推理和数学基准测试中，RLAD的表现优于离线蒸馏、标准GRPO以及基于KL散度的在线蒸馏方法。
conclusion: RLAD通过在强化学习中动态平衡探索、利用与模仿，为高效提升小规模推理模型的性能提供了新路径。
---

## 摘要
强化学习（RL）后训练近期在长思维链推理大语言模型（LLM）中取得了重大进展，但此类模型的高昂推理成本促使人们将其蒸馏到更小的学生模型中。大多数现有的知识蒸馏（KD）方法是为监督微调（SFT）设计的，依赖于固定的教师轨迹或基于教师-学生 Kullback-Leibler (KL) 散度的正则化。当与强化学习结合时，这些方法通常面临分布不匹配和目标干扰的问题：教师监督可能与学生不断演化的采样分布（rollout distribution）不一致，且 KL 正则化项可能与奖励最大化目标产生竞争，需要精细的损失平衡。为了解决这些问题，我们提出了强化感知蒸馏（RLAD），它在强化学习过程中执行选择性模仿——仅在能够改进当前策略更新时才引导学生向教师学习。我们的核心组件——置信域比例蒸馏（TRRD），将教师-学生 KL 正则化项替换为一种锚定于教师与旧策略混合分布的 PPO/GRPO 式似然比目标，从而在学生采样上实现优势感知且受置信域限制的蒸馏，并自然地平衡了探索、利用和模仿。在多种逻辑推理和数学基准测试中，RLAD 的表现始终优于离线蒸馏、标准 GRPO 以及基于 KL 的在线教师-学生知识蒸馏。

## Abstract
Reinforcement learning (RL) post-training has recently driven major gains in long chain-of-thought reasoning large language models (LLMs), but the high inference cost of such models motivates distillation into smaller students. Most existing knowledge distillation (KD) methods are designed for supervised fine-tuning (SFT), relying on fixed teacher traces or teacher-student Kullback-Leibler (KL) divergence-based regularization. When combined with RL, these approaches often suffer from distribution mismatch and objective interference: teacher supervision may not align with the student's evolving rollout distribution, and the KL regularizer can compete with reward maximization and require careful loss balancing. To address these issues, we propose RL-aware distillation (RLAD), which performs selective imitation during RL -- guiding the student toward the teacher only when it improves the current policy update. Our core component, Trust Region Ratio Distillation (TRRD), replaces the teacher-student KL regularizer with a PPO/GRPO-style likelihood-ratio objective anchored to a teacher--old-policy mixture, yielding advantage-aware, trust-region-bounded distillation on student rollouts and naturally balancing exploration, exploitation, and imitation. Across diverse logic reasoning and math benchmarks, RLAD consistently outperforms offline distillation, standard GRPO, and KL-based on-policy teacher-student knowledge distillation.

---

## 论文详细总结（自动生成）

这篇论文由亚马逊 AWS Agentic AI 团队提出，旨在解决将大型推理模型（如 DeepSeek-R1）的推理能力蒸馏至小型模型时，强化学习（RL）与知识蒸馏（KD）结合不佳的问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：长思维链（CoT）推理模型虽然强大，但推理成本极高。将这些能力蒸馏到小模型（学生）是主流方案。
*   **核心问题**：传统的蒸馏方法（如离线 SFT 蒸馏或在线 KL 散度正则化）在 RL 阶段存在两个主要缺陷：
    1.  **分布不匹配**：教师模型的静态分布可能与学生模型在 RL 过程中不断演化的采样分布不一致。
    2.  **目标干扰**：传统的 KL 正则项往往与奖励（Reward）最大化目标产生冲突，导致训练不稳定，且需要繁琐的超参数调优来平衡两者。
*   **研究动机**：开发一种“强化感知”的蒸馏框架，让学生模型仅在有助于提升奖励的情况下才模仿教师，实现选择性模仿。

### 2. 方法论：RL-Aware Distillation (RLAD)
*   **核心思想**：将教师模型的指导直接嵌入到 RL 的策略更新目标中，而不是作为一个独立的惩罚项。
*   **关键技术：信任区域比率蒸馏 (TRRD)**：
    *   **混合锚点**：TRRD 不再单纯对比“新旧策略”，而是引入一个由“教师策略”和“旧学生策略”按比例 $\alpha$ 混合而成的“混合锚点分布”。
    *   **似然比目标**：将 GRPO/PPO 中的重要性采样比率替换为 TRRD 比率。这意味着学生模型的更新被限制在一个以“教师+旧策略”为中心的信任区域内。
    *   **优势感知（Advantage-aware）**：通过将教师信号置于优势权重（Advantage）之下，TRRD 实现了：
        *   当教师偏好的动作具有高奖励时，允许更大的步长更新。
        *   当教师偏好的动作与奖励信号冲突时，通过信任区域限制这种错误的模仿。
*   **算法流程**：在 GRPO 框架下，计算每个 Token 的 TRRD 比率，应用 PPO 式的裁剪（Clipping），并结合优势函数进行策略梯度更新。

### 3. 实验设计
*   **场景与数据集**：
    *   **逻辑推理**：使用 K&K Logistics 数据集，包含从易到难（PPL2 到 PPL8）的多个子集。
    *   **复杂数学推理**：使用 Skywork-OR1 训练集，在 AIME24/25、AMC23/24、MATH500 等基准测试上评估。
*   **模型配置**：
    *   **学生模型**：Qwen3-0.6B/1.7B/8B，Qwen2.5-1.5B-DS。
    *   **教师模型**：Qwen3-8B/32B。
*   **对比方法（Benchmarks）**：
    *   **GRPO**：纯强化学习，无教师指导。
    *   **KDRL**：在 GRPO 基础上增加显式的教师-学生 KL 正则项。
    *   **Offline SFT**：在教师生成的轨迹上进行监督微调。

### 4. 资源与算力
*   **逻辑推理实验**：使用 **8 台 NVIDIA H200 GPU**。
*   **数学推理实验**：使用 **64 台 NVIDIA H200 GPU**。
*   **训练细节**：数学任务训练最多 5 个 Epoch，采用动态微批处理（Dynamic Micro-batching）技术。文中提到，由于教师模型仅用于计算 Logits 而不生成轨迹，相比标准 GRPO 仅增加了约 **12%** 的训练延迟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.5B 到 8B 不同量级的学生模型，以及从逻辑到数学的不同领域，实验设计较为全面。
*   **消融实验**：对混合系数 $\alpha$ 进行了详细消融（从 0.1 到 0.9），证明了 RLAD 对该参数在一定范围内具有鲁棒性。
*   **客观性**：通过 Pass@1 和 Pass@32 等多维度指标评估，并展示了训练过程中的奖励曲线和验证集准确率曲线，体现了实验的透明度和公平性。

### 6. 主要结论与发现
*   **性能领先**：RLAD 在所有基准测试中均优于 GRPO 和 KDRL。在逻辑推理 PPL8（最难子集）上，RLAD 将 0.6B 模型的准确率从 0.63 提升至 0.83。
*   **训练稳定性**：相比于 KDRL 经常出现的震荡，RLAD 的训练曲线更加平滑，收敛速度更快。
*   **奖励驱动 vs 纯模仿**：发现 KDRL 往往在 Pass@32（多样性）上提升明显，但在 Pass@1（准确性）上表现一般；而 RLAD 在 Pass@1 上提升显著，说明其更有效地结合了奖励优化。

### 7. 优点（亮点）
*   **优雅的数学统一**：将蒸馏目标与 RL 的信任区域（Trust Region）有机结合，避免了多目标优化时的超参数“拉锯战”。
*   **选择性模仿**：解决了“教师并不总是对的”或“教师在学生分布外不可靠”的问题，通过优势函数动态调节教师的影响力。
*   **效率高**：在引入强大教师指导的同时，保持了较低的计算开销增加。

### 8. 不足与局限
*   **Logits 依赖**：该方法依赖于获取教师模型的 Token 级 Logits，这要求教师模型必须是开源的或能够访问其概率输出，无法直接应用于闭源 API 教师（如 GPT-4）。
*   **模型容量限制**：在简单任务（如 MATH500）上提升较小，说明当小模型能力接近饱和时，算法改进的边际效应递减。
*   **长文本挑战**：虽然支持长上下文，但在极长文本下的 Logits 计算和存储仍可能面临显存压力。

（完）
