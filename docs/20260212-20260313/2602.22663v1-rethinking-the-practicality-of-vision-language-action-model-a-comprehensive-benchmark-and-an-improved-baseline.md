---
title: "Rethinking the Practicality of Vision-language-action Model: A Comprehensive Benchmark and An Improved Baseline"
title_zh: 重新思考视觉-语言-动作模型的实用性：一个全面的基准测试和改进的基准模型
authors: "Wenxuan Song, Jiayi Chen, Xiaoquan Sun, Huashuo Lei, Yikai Qin, Wei Zhao, Pengxiang Ding, Han Zhao, Tongxin Wang, Pengxu Hou, Zhide Zhong, Haodong Yan, Donglin Wang, Jun Ma, Haoang Li"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22663v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: VLA模型的基准测试与改进基线
tldr: 针对现有视觉-语言-动作（VLA）模型参数量大、预训练成本高及跨具身适用性差的问题，本文提出了CEBench基准测试，涵盖仿真与真实世界的多种具身场景。同时，推出了轻量化模型LLaVA-VLA，通过多视图感知、本体感受标记化和动作分块技术，结合两阶段训练范式，实现了在消费级GPU上的高效部署，并统一了导航与操作任务，显著提升了VLA的实用性。
selection_source: fresh_fetch
motivation: 现有VLA模型因参数规模巨大、预训练需求极高且难以适配多样化机器人硬件，严重限制了其在实际场景中的广泛应用。
method: 提出了包含仿真与真实数据的CEBench基准，并开发了集成多重视图、本体感受和动作分块的轻量化LLaVA-VLA模型，采用两阶段训练以降低成本。
result: 实验证明LLaVA-VLA具有极强的泛化性，在多具身任务中表现优异，并成功实现了首个端到端的真实世界移动操作演示。
conclusion: 通过建立全面的基准和改进的轻量化基准模型，本研究为开发更具实用性、可部署在消费级硬件上的通用机器人智能体提供了新路径。
---

## 摘要
视觉-语言-动作（VLA）模型已成为一种通用的机器人智能体。然而，现有的 VLA 模型受限于过大的参数规模、高昂的预训练需求以及对多样化具身（embodiments）的适用性有限。为了提高 VLA 的实用性，我们提出了一个全面的基准测试和一个改进的基准模型。首先，我们提出了 CEBench，这是一个涵盖仿真和真实世界中多样化具身的新基准，并考虑了领域随机化。我们收集了 1.44 万条仿真轨迹和 1600 条真实世界专家策划的轨迹，以支持在 CEBench 上的训练。其次，以 CEBench 作为测试平台，我们研究了 VLA 实用性的三个关键方面，并提出了一些核心发现。基于这些发现，我们推出了 LLaVA-VLA，这是一种轻量级且功能强大的 VLA，旨在消费级 GPU 上进行实际部署。在架构上，它集成了一个紧凑的 VLM 主干网络，并结合了多视图感知、本体感受标记化（proprioceptive tokenization）和动作分块（action chunking）。为了消除对昂贵预训练的依赖，LLaVA-VLA 采用了包括后训练（post-training）和微调在内的两阶段训练范式。此外，LLaVA-VLA 扩展了动作空间，以统一导航和操作。跨具身的实验证明了 LLaVA-VLA 的泛化能力和多功能性，而真实世界的移动操作实验使其成为首个用于移动操作的端到端 VLA 模型。论文被接收后，我们将开源所有数据集、代码和检查点，以促进可复现性和未来的研究。

## Abstract
Vision-Language-Action (VLA) models have emerged as a generalist robotic agent. However, existing VLAs are hindered by excessive parameter scales, prohibitive pre-training requirements, and limited applicability to diverse embodiments. To improve the practicality of VLAs, we propose a comprehensive benchmark and an improved baseline. First, we propose CEBench, a new benchmark spanning diverse embodiments in both simulation and the real world with consideration of domain randomization. We collect 14.4k simulated trajectories and 1.6k real-world expert-curated trajectories to support training on CEBench. Second, using CEBench as our testbed, we study three critical aspects of VLAs' practicality and offer several key findings. Informed by these findings, we introduce LLaVA-VLA, a lightweight yet powerful VLA designed for practical deployment on consumer-grade GPUs. Architecturally, it integrates a compact VLM backbone with multi-view perception, proprioceptive tokenization, and action chunking. To eliminate reliance on costly pre-training, LLaVA-VLA adopts a two-stage training paradigm including post-training and fine-tuning. Furthermore, LLaVA-VLA extends the action space to unify navigation and manipulation. Experiments across embodiments demonstrate the capabilities of generalization and versatility of LLaVA-VLA , while real-world mobile manipulation experiments establish it as the first end-to-end VLA model for mobile manipulation. We will open-source all datasets, codes, and checkpoints upon acceptance to foster reproducibility and future research.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking the Practicality of Vision-language-action Model: A Comprehensive Benchmark and An Improved Baseline》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管视觉-语言-动作（VLA）模型在机器人控制领域展现了巨大潜力，但现有模型面临三大实用性障碍：
    1.  **参数规模巨大**：动辄数十亿（7B+）的参数量使得模型难以部署在移动平台或消费级 GPU 上。
    2.  **预训练成本高昂**：依赖大规模机器人数据集进行预训练，消耗极大的计算资源。
    3.  **具身适用性受限**：大多局限于固定底座的机械臂操作，缺乏对移动操作（Mobile Manipulation）等跨具身场景的支持。
*   **核心目标**：通过构建全面的基准测试（CEBench）和开发轻量化基准模型（LLaVA-VLA），探索如何使 VLA 模型在实际部署中更高效、更通用且更易于训练。

### 2. 论文提出的方法论
*   **核心思想**：利用轻量级视觉语言模型（VLM）作为主干，通过优化感知输入、动作表示和训练策略，在不进行大规模预训练的情况下实现高性能。
*   **关键技术细节**：
    *   **LLaVA-VLA 架构**：基于 **LLaVA-OneVision-0.5B** 紧凑主干。
    *   **多视图感知**：将第三人称全局视野与第一人称手眼视野图像进行垂直拼接，作为单一输入，既保留了空间信息又减少了 Token 冗余。
    *   **本体感受标记化（Proprioception Tokenizer）**：将机器人的状态（本体感受）转化为 Token 序列，而非简单的 MLP 投影，以便更好地利用 VLM 的语言建模能力。
    *   **动作分块（Action Chunking）**：一次性预测未来 5 步动作，增强规划的连贯性和执行的稳定性。
    *   **混合动作空间**：设计了“方向 Token + 数值 Token”的组合，通过特殊的 `<Navigation>` 引导，使模型能无缝切换导航（前进、转弯、停止）与操作任务。
*   **训练流程**：采用**两阶段训练范式**：
    1.  **后训练（Post-training）**：在多任务数据集上进行初步对齐。
    2.  **微调（Fine-tuning）**：在特定任务的高质量数据上进行精调。

### 3. 实验设计
*   **基准测试（CEBench）**：
    *   **CALVIN**：用于评估单臂长程指令遵循和视觉泛化。
    *   **RoboTwin (仿真)**：包含 36 个任务、1.44万条轨迹，重点测试双臂协作和领域随机化（DR）。
    *   **真实世界**：使用 Cobot-Magic 移动双臂机器人，包含 8 个固定底座任务和 2 个移动操作任务（共 1.6k 轨迹）。
*   **对比方法**：
    *   **大型 VLA**：OpenVLA (7B), RoboFlamingo (3B)。
    *   **轻量级/生成式方法**：ACT, Diffusion Policy, TinyVLA, RDT, 3D-VLA 等。

### 4. 资源与算力
*   **算力配置**：
    *   **后训练阶段**：使用 **8 台 NVIDIA H100 GPU**。
    *   **微调阶段**：仅需 **1 台 NVIDIA 4090 GPU**（消费级显卡）。
*   **训练时长**：在 CALVIN 任务上，后训练一个 Epoch 仅需约 **6 小时**。
*   **部署友好性**：模型参数仅 0.5B，可在消费级硬件上实现实时推理。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在仿真环境（CALVIN 和 RoboTwin）中进行了数千次评估循环。
    *   在真实世界中，针对 6 个双臂任务和 2 个移动操作任务进行了详细对比。
    *   **消融实验**：针对模型规模、多视图融合方式、本体感受编码、动作分块大小、动作空间离散化等进行了 8 组深入的对比实验（Findings 1-8）。
*   **客观性与公平性**：实验涵盖了“可见场景”和“领域随机化（DR）”场景，对比了不同参数量级的模型，并使用了官方标准指标（如成功率、平均完成长度），评估较为全面客观。

### 6. 论文的主要结论与发现
1.  **规模非唯一因素**：0.5B 的 LLaVA-VLA 在多个指标上达到或超过了 7B 的 OpenVLA。
2.  **感知至关重要**：多视图拼接和本体感受标记化显著提升了空间推理能力。
3.  **无需大规模预训练**：通过高质量的领域内多任务后训练，小模型也能建立有效的视觉-动作映射。
4.  **动作空间设计**：离散化的动作 Token 结合动作分块，在保持自回归特性的同时，性能不输连续扩散头。
5.  **移动操作突破**：LLaVA-VLA 是首个能以端到端方式处理移动操作任务（导航+操作）的 VLA 模型。

### 7. 优点
*   **极高的实用性**：将 VLA 的门槛从昂贵的服务器集群降低到了单块 4090 显卡。
*   **开源贡献**：承诺开源所有数据集、代码和模型权重，对社区友好。
*   **架构简洁高效**：通过简单的图像拼接和 Token 优化解决了复杂的感知问题，避免了过度设计的复杂模块。
*   **跨具身统一**：成功统一了导航与操作的动作空间，扩展了 VLA 的应用边界。

### 8. 不足与局限
*   **真实世界样本量**：虽然有 1.6k 轨迹，但相比仿真数据仍显不足，可能限制了模型在极端复杂现实环境下的鲁棒性。
*   **动作分块固定**：动作分块大小（Chunk Size=5）是静态设置的，对于不同节奏的任务（如极快或极慢的动作）可能不是最优。
*   **长程任务挑战**：虽然在 CALVIN 上表现优异，但在更复杂的真实世界长序列任务中，模型仍可能面临复合误差问题。

（完）
