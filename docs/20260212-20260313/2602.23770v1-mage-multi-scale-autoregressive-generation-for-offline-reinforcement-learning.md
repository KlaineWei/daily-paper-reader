---
title: "MAGE: Multi-scale Autoregressive Generation for Offline Reinforcement Learning"
title_zh: MAGE：面向离线强化学习的多尺度自回归生成
authors: "Chenxing Lin, Xinhui Gao, Haipeng Zhang, Xinran Li, Haitao Wang, Songzhu Mei, Chenglu Wen, Weiquan Liu, Siqi Shen, Cheng Wang"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23770v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于长程离线强化学习的分层生成
tldr: 针对离线强化学习中长时程、稀疏奖励任务的挑战，本文提出MAGE框架。该方法利用多尺度自回归生成技术，通过条件引导的多尺度自动编码器学习分层轨迹表示，并结合多尺度Transformer实现从粗到细的轨迹生成。MAGE有效捕捉了轨迹的多分辨率时间依赖性，在5个基准测试中表现优于15种基线算法，显著提升了长时程任务中的轨迹生成质量与可控性。
selection_source: fresh_fetch
motivation: 现有的生成式离线强化学习方法在处理长时程稀疏奖励任务时，往往忽略了轨迹中固有的多尺度时间结构，导致性能受限。
method: 提出MAGE框架，采用条件引导的多尺度自动编码器和Transformer，实现从粗粒度到细粒度的轨迹分层自回归生成。
result: 在五个离线强化学习基准测试中，MAGE在与15种基线算法的对比中展现出卓越性能，能生成连贯且可控的轨迹。
conclusion: MAGE通过整合多尺度轨迹建模与条件引导，有效解决了长时程离线强化学习中的复杂轨迹建模难题。
---

## 摘要
生成模型因其建模复杂轨迹分布的能力，在离线强化学习（RL）中受到了广泛关注。然而，现有的基于生成的方法在处理具有稀疏奖励特征的长时程任务时仍然面临挑战。一些分层生成方法被开发出来以缓解这一问题，它们通过一种策略将原始问题分解为短时程子问题，并利用另一种策略生成详细动作。尽管这些方法有效，但它们往往忽略了轨迹中固有的多尺度时间结构，导致性能欠佳。为了克服这些局限性，我们提出了 MAGE，一种基于多尺度自回归生成的离线强化学习方法。MAGE 结合了一个条件引导的多尺度自编码器来学习分层轨迹表示，以及一个多尺度 Transformer，从粗到细的时间尺度自回归地生成轨迹表示。MAGE 有效地捕捉了轨迹在多个分辨率下的时间依赖性。此外，采用了一个条件引导的解码器来对短期行为进行精确控制。在五个离线强化学习基准测试上与 15 种基准算法进行的广泛实验表明，MAGE 成功地将多尺度轨迹建模与条件引导相结合，在长时程稀疏奖励设置下生成了连贯且可控的轨迹。

## Abstract
Generative models have gained significant traction in offline reinforcement learning (RL) due to their ability to model complex trajectory distributions. However, existing generation-based approaches still struggle with long-horizon tasks characterized by sparse rewards. Some hierarchical generation methods have been developed to mitigate this issue by decomposing the original problem into shorter-horizon subproblems using one policy and generating detailed actions with another. While effective, these methods often overlook the multi-scale temporal structure inherent in trajectories, resulting in suboptimal performance. To overcome these limitations, we propose MAGE, a Multi-scale Autoregressive GEneration-based offline RL method. MAGE incorporates a condition-guided multi-scale autoencoder to learn hierarchical trajectory representations, along with a multi-scale transformer that autoregressively generates trajectory representations from coarse to fine temporal scales. MAGE effectively captures temporal dependencies of trajectories at multiple resolutions. Additionally, a condition-guided decoder is employed to exert precise control over short-term behaviors. Extensive experiments on five offline RL benchmarks against fifteen baseline algorithms show that MAGE successfully integrates multi-scale trajectory modeling with conditional guidance, generating coherent and controllable trajectories in long-horizon sparse-reward settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAGE: Multi-scale Autoregressive Generation for Offline Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
离线强化学习（Offline RL）旨在从固定数据集中学习策略，但在处理**长时程（Long-horizon）**且**奖励稀疏（Sparse-reward）**的任务时表现不佳。
*   **现有挑战：** 
    *   基于Transformer的方法（如Decision Transformer）由于单向自回归特性，缺乏对全局上下文的双向理解。
    *   基于扩散模型的方法（如Decision Diffuser）存在局部生成偏差，生成的轨迹局部合理但全局不连贯。
    *   现有的分层方法通常采用固定的两层结构，难以捕捉轨迹中复杂的多尺度时间抽象。
*   **研究动机：** 提出一种能够同时建模全局轨迹轮廓和局部微观细节的生成框架，以应对复杂任务中的时间依赖性。

### 2. 方法论
MAGE 提出了一个**从粗到细（Coarse-to-fine）**的自回归生成框架，核心包含两个模块：
*   **多尺度轨迹自动编码器 (MTAE)：**
    *   利用 VQ-VAE 架构将轨迹（状态 $s$ 和待获得奖励 $R$ 的序列）编码为分层的离散 Token 映射。
    *   通过预定义的尺度表（Scale Schedule），将轨迹分解为从粗粒度（捕捉长期依赖）到细粒度（捕捉短期细节）的多个 Token 层级。
*   **多尺度条件引导 Transformer：**
    *   **自回归生成：** 按照尺度顺序生成 Token 映射，每一层级的生成都以之前更粗层级的 Token 以及初始状态 $s_0$、目标奖励 $R_0$ 为条件。
    *   **条件引导细化：** 在解码器中引入参数高效的 **Adapter（适配器）** 模块，通过均方误差损失（$L_{cond}$）强制生成的轨迹起点与实际 $s_0$ 严格对齐，解决量化过程中的信息损失问题。
*   **动作执行：** 采用**潜空间逆动力学模型（Latent Inverse Dynamics）**，直接从生成的多尺度潜表示中预测要执行的动作 $a$。

### 3. 实验设计
*   **数据集/场景：** 涵盖了 5 个主流离线 RL 基准测试：
    1.  **Adroit：** 高维、精细的手部操控任务（极度稀疏奖励）。
    2.  **Franka Kitchen：** 顺序多任务操控。
    3.  **AntMaze：** 长时程迷宫导航。
    4.  **Maze2D & Multi2D：** 二维路径规划。
    5.  **Gym Locomotion：** 经典的 MuJoCo 运动任务（稠密奖励）。
*   **对比基线（15 种）：**
    *   **非生成式：** BC, CQL, IQL, MPPI。
    *   **生成式：** DT, TT, TAP, Diffuser, Decision Diffuser (DD), RGG, Diffusion-QL。
    *   **分层生成式：** ADT, HDMI, HD, CARP。

### 4. 资源与算力
*   **硬件：** 使用了 NVIDIA GeForce **RTX 4090** GPU 和 Intel Xeon Gold 6348 CPU (2.60GHz)。
*   **训练细节：** 论文未明确给出总训练时长，但提到 MAGE 的**推理速度**极快：比分层扩散模型（HD）快约 50 倍，比 Decision Diffuser 快约 80 倍，单步推理仅需 27ms，满足 20Hz 的实时控制需求。

### 5. 实验数量与充分性
*   **实验规模：** 在 5 个基准测试的数十个子任务上进行了评估。
*   **重复性：** 每个实验使用 5 个随机种子，Adroit/Kitchen 每个种子测试 20 次，迷宫任务每个种子测试 100 次。
*   **消融实验：** 非常充分。分别验证了：
    1.  时间尺度数量 $K$ 的影响。
    2.  不同轨迹建模方案（如只建模状态、动作或两者结合）的优劣。
    3.  RTG（待获得奖励）引导的重要性。
    4.  Adapter 模块和条件损失 $L_{cond}$ 的必要性。
*   **客观性：** 对比了从传统 RL 到最新分层扩散模型的所有主流方法，实验设计较为公正。

### 6. 主要结论与发现
*   **性能卓越：** MAGE 在长时程稀疏奖励任务（如 AntMaze Large, Adroit Human/Cloned）中显著优于所有基线。
*   **多尺度优势：** 实验证明增加时间尺度（如 $K=8$）能显著提升性能，证明了分层建模时间抽象的有效性。
*   **建模选择：** 发现建模 $(R, s)$ 序列比包含动作 $a$ 的序列效果更好，因为动作序列通常包含更多高频噪声。
*   **效率：** 在保持高成功率的同时，推理效率远超基于扩散的方法，具备实际应用潜力。

### 7. 优点（亮点）
*   **创新的生成范式：** 将视觉领域的 VAR（多尺度自回归）思想引入 RL，解决了扩散模型推理慢和 Transformer 全局感官弱的问题。
*   **端到端可微细化：** 通过 Gumbel-Softmax 和 Adapter 实现了离散 Token 生成过程中的条件约束优化。
*   **通用性：** 不仅在稀疏奖励任务中领先，在稠密奖励的运动任务中也保持了极强的竞争力。

### 8. 不足与局限
*   **极端场景挑战：** 在 OGBench 等极端长时程任务中，虽然表现优于基线，但绝对成功率仍有提升空间。
*   **分布偏移：** 论文提到在处理超出分布（OOD）的情景时，如何更好地管理分布偏移仍是一个待解决的挑战。
*   **灵活性权衡：** 粗尺度下的全局规划虽然保证了连贯性，但也可能在一定程度上限制了细尺度在应对突发环境变化时的灵活性。

（完）
