---
title: "HiMAC: Hierarchical Macro-Micro Learning for Long-Horizon LLM Agents"
title_zh: HiMAC：面向长程 LLM 智能体的分层宏观-微观学习
authors: "Hongbo Jin, Rongpeng Zhu, Jiayu Ding, Wenhao Zhang, Ge Li"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.00977v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 用于智能体长程决策的分层强化学习
tldr: 针对大语言模型智能体在长程任务中因扁平化策略导致的规划效率低和误差累积问题，本文提出了HiMAC框架。该框架将决策过程显式分解为宏观蓝图规划和微观目标条件执行，并引入无评论家的层次化策略优化范式及迭代协同进化策略。实验证明，HiMAC在多个复杂环境下显著提升了长程任务的成功率和样本效率，强调了结构化层次对提升智能体能力的关键作用。
selection_source: fresh_fetch
motivation: 现有的扁平化自回归策略在处理长程任务时存在推理效率低下和严重的误差传播问题。
method: 提出一种层次化强化学习框架，通过宏观规划生成蓝图并由微观执行器完成目标，结合无评论家的层次化策略优化与协同进化训练。
result: 在ALFWorld、WebShop和Sokoban等基准测试中，HiMAC均优于现有的提示词方法和强化学习基线，达到了SOTA性能。
conclusion: 引入结构化的层次体系而非单纯增加模型规模，是实现稳健的长程智能体决策的关键。
---

## 摘要
大语言模型 (LLM) 智能体近期在交互式决策中展现出强大能力，但在需要结构化规划和可靠执行的长程任务中仍存在根本性局限。现有方法主要依赖于扁平的自回归策略，即高层推理和底层动作在单个 token 序列中生成，导致在长轨迹上的探索效率低下且误差传播严重。在这项工作中，我们提出了 HiMAC，一种分层智能体强化学习 (RL) 框架，它将长程决策显式分解为宏观层面的规划和微观层面的执行。HiMAC 将推理建模为结构化的蓝图生成过程，随后进行目标条件化的动作执行，从而在基于 LLM 的智能体中实现稳健的长程规划。为了高效训练该分层结构，我们引入了一种无评论家 (critic-free) 的分层策略优化范式，通过分层相对优势估计将基于组的强化学习扩展到双层结构。此外，我们提出了一种迭代协同进化训练策略，在规划器探索和执行器适配之间交替进行，从而缓解分层学习中固有的非平稳性。在 ALFWorld、WebShop 和 Sokoban 上的广泛实验表明，HiMAC 始终优于强大的提示词和强化学习基准，在文本和视觉具身环境（visually grounded environments）中均实现了最先进的性能，并显著提高了样本效率。我们的结果表明，引入结构化分层，而非仅仅增加模型规模，是实现稳健的长程智能体智能的关键因素。

## 速览
**TLDR**：针对大语言模型智能体在长程任务中因扁平化生成导致的错误累积问题，本文提出HiMAC框架。该框架将决策分解为宏观规划（蓝图生成）与微观执行（目标导向动作），并引入无评论器的层次化策略优化与迭代协同进化策略。实验证明，HiMAC在多个复杂环境下显著提升了长程规划能力和采样效率，证明了结构化层次对提升智能体性能的关键作用。 \
**Motivation**：现有的扁平化自回归策略在长程任务中容易产生严重的错误传播和低效探索。 \
**Method**：提出一种将宏观规划与微观执行解耦的层次化强化学习框架，并采用无评论器的层次化策略优化及迭代协同进化训练策略。 \
**Result**：在ALFWorld、WebShop和Sokoban等基准测试中，HiMAC在性能和采样效率上均显著优于现有的提示词方法和强化学习基线。 \
**Conclusion**：引入结构化的层次体系而非单纯增加模型规模，是实现稳健长程智能体决策的关键。

---

## Abstract
Large language model (LLM) agents have recently demonstrated strong capabilities in interactive decision-making, yet they remain fundamentally limited in long-horizon tasks that require structured planning and reliable execution. Existing approaches predominantly rely on flat autoregressive policies, where high-level reasoning and low-level actions are generated within a single token sequence, leading to inefficient exploration and severe error propagation over extended trajectories. In this work, we propose HiMAC, a hierarchical agentic RL framework that explicitly decomposes long-horizon decision-making into macro-level planning and micro-level execution. HiMAC models reasoning as a structured blueprint generation process followed by goal-conditioned action execution, enabling robust long-horizon planning within LLM-based agents. To train this hierarchy efficiently, we introduce a critic-free hierarchical policy optimization paradigm that extends group-based reinforcement learning to bi-level structures through hierarchical relative advantage estimation. Furthermore, we propose an iterative co-evolution training strategy that alternates between planner exploration and executor adaptation, mitigating the non-stationarity inherent in hierarchical learning. Extensive experiments on ALFWorld, WebShop, and Sokoban demonstrate that HiMAC consistently outperforms strong prompting and reinforcement learning baselines, achieving state-of-the-art performance and substantially improved sample efficiency across both text-based and visually grounded environments. Our results show that introducing structured hierarchy, rather than increasing model scale alone, is a key factor for enabling robust long-horizon agentic intelligence.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HiMAC** 的新型分层强化学习框架，旨在解决大语言模型（LLM）智能体在处理长程（Long-Horizon）任务时的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体大多采用“扁平化（Flat）”的自回归策略，即在同一个 token 序列中混合高层推理和底层动作。
*   **研究动机**：在长程任务中，扁平化架构面临三大挑战：
    1.  **探索复杂度呈指数级增长**：在巨大的动作空间中进行盲目搜索。
    2.  **信用分配延迟**：难以确定长序列中哪个步骤导致了最终的成败。
    3.  **语义漂移（Context Drift）**：随着轨迹增长，智能体容易丢失全局目标。
*   **整体含义**：论文认为，要实现稳健的长程智能，结构化的层次归纳偏置（Inductive Bias）比单纯增加模型规模更重要。

### 2. 方法论：HiMAC 框架
HiMAC 将决策过程分解为**宏观规划（Macro-level Planning）**和**微观执行（Micro-level Execution）**：
*   **核心思想**：
    *   **宏观策略（Planner）**：根据任务指令生成一个“结构化蓝图（Blueprint）”，即一系列自然语言子目标。
    *   **微观策略（Executor）**：针对每个子目标生成具体的原子动作，直到输出特殊的 `<sub_done>` 标记切换到下一个子目标。
*   **关键技术细节**：
    *   **无评论家分层策略优化（Critic-Free HPO）**：扩展了 GRPO（组相对策略优化）算法。通过在宏观层（蓝图组）和微观层（动作组）分别计算组内相对优势（Relative Advantage），无需训练复杂的价值网络（Value Network/Critic），降低了训练方差。
    *   **迭代协同进化训练（Iterative Co-Evolution）**：
        *   **阶段 A（宏观探索）**：固定执行器，更新规划器，使其学习生成可行的蓝图。
        *   **阶段 B（微观适配）**：固定高置信度蓝图，更新执行器，提升其完成子目标的能力。
    *   这种交替训练模式将不稳定的双层优化问题转化为一系列平稳的单层更新。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **ALFWorld**：具身决策任务，涉及家庭环境中的多步推理。
    2.  **WebShop**：长程网页导航，在嘈杂的高维观察中进行购物决策。
    3.  **Sokoban（推箱子）**：视觉空间规划任务，测试精确的序列推理。
*   **对比基线**：
    *   **提示词方法**：ReAct, Reflexion。
    *   **强化学习方法**：PPO, RLOO, GRPO, 以及针对智能体优化的 GiGPO。
    *   **模型底座**：Qwen2.5-Instruct (1.5B/7B), Qwen2.5-VL (3B/7B), Qwen3-VL (8B)。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了在 ALFWorld 中使用了 128 个并行环境进行采样，设置了最大 Prompt 长度（1024-2048）和响应长度（512），这暗示了实验需要相当规模的计算集群支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了文本和视觉两种模态，使用了从 1.5B 到 8B 不同规模的多个模型底座。
*   **充分性**：
    *   **多维度评估**：不仅对比了成功率，还分析了得分（Score）和样本效率。
    *   **消融实验**：非常详尽，分别验证了分层结构、协同进化策略、`<sub_done>` 机制以及蓝图选择策略的有效性。
    *   **客观性**：所有实验均基于 3 个随机种子取平均值，并对比了闭源模型（如 GPT-4o, Gemini-2.5-Pro）的表现，具有较高的参考价值。

### 6. 主要结论与发现
*   **性能领先**：HiMAC 在所有基准测试中均达到 SOTA。在 WebShop 任务中，1.5B 规模的 HiMAC 成功率比最强 RL 基线高出 **16%**。
*   **结构胜过规模**：1.5B 规模的 HiMAC 在某些任务上甚至超过了参数量大得多的扁平化模型或闭源模型。
*   **样本效率**：HiMAC 达到相同成功率所需的训练迭代次数显著少于扁平化策略。
*   **涌现行为**：定性分析显示，经过训练的宏观策略自发产生了“自我验证”行为（如在完成任务后主动检查库存）。

### 7. 优点
*   **架构优雅**：通过显式的宏观-微观分离，有效遏制了长程任务中的误差传播。
*   **训练稳定**：无评论家的设计避开了在稀疏奖励、高维语义空间中训练价值网络的难题。
*   **可解释性强**：生成的自然语言蓝图让智能体的规划过程对人类透明、可理解。

### 8. 不足与局限
*   **分层深度固定**：目前仅支持两层架构（宏观-微观），对于极度复杂的超长程任务，可能需要更多层级的抽象。
*   **推理开销**：生成蓝图会带来额外的 token 消耗和推理延迟。
*   **任务依赖性**：该方法假设任务可以被合理地分解为子目标，对于某些难以模块化的模糊任务，效果可能受限。
*   **环境反馈要求**：微观执行器仍依赖环境的实时观察，若环境反馈极度匮乏，蓝图的引导作用可能会打折扣。

（完）
