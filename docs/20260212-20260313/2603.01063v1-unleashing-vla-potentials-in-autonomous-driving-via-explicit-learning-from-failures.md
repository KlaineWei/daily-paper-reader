---
title: Unleashing VLA Potentials in Autonomous Driving via Explicit Learning from Failures
title_zh: 通过显式学习失败经验释放自动驾驶中 VLA 的潜力
authors: "Yuechen Luo, Qimao Chen, Fang Li, Shaoqing Xu, Jaxin Liu, Ziying Song, Zhi-xin Yang, Fuxi Wen"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01063v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 长尾驾驶场景中VLA的强化学习优化
tldr: 针对自动驾驶中视觉-语言-动作（VLA）模型在强化学习阶段因奖励信号稀疏而陷入性能瓶颈的问题，本文提出了ELF-VLA框架。该框架通过引入结构化诊断反馈，将模糊的标量奖励转化为详细的失败原因报告，并利用反馈引导的精炼过程生成高质量样本注入训练，从而显著提升了模型在长尾场景下的规划能力，在NAVSIM基准测试中达到SOTA水平。
selection_source: fresh_fetch
motivation: 传统的强化学习在自动驾驶长尾场景中因奖励信号过于稀疏且缺乏具体失败原因，导致模型难以通过盲目探索实现性能突破。
method: 提出了ELF-VLA框架，通过生成结构化的诊断报告识别失败根源，并利用反馈引导的精炼技术将修正后的高奖励样本反馈至强化学习训练中。
result: 在公开的NAVSIM基准测试中，该方法在PDMS、EPDMS评分及高层规划准确率上均取得了当前最先进的性能表现。
conclusion: 通过显式地从失败中学习并引入结构化反馈，可以有效释放VLA模型在复杂自动驾驶任务中的潜在性能。
---

## 摘要
用于自动驾驶的视觉-语言-动作（VLA）模型在强化学习（RL）优化过程中经常遇到性能瓶颈。这种停滞源于受限于先前监督微调（SFT）的探索能力，导致在长尾场景中持续失败。在这些关键情况下，所有探索的动作都产生零值的驾驶得分。这种信息稀疏的奖励信号预示着失败，但未能识别其根本原因——是由于规划错误、推理缺陷还是轨迹执行不力。为了解决这一局限性，我们提出了通过显式学习失败经验的 VLA（ELF-VLA），这是一个通过结构化诊断反馈增强强化学习的框架。我们的方法不再依赖模糊的标量奖励，而是生成详细且可解释的报告，以识别具体的失败模式。随后，VLA 策略利用这种显式反馈来生成反馈引导的优化。通过将这些经过修正的高奖励样本注入回强化学习训练批次中，我们的方法提供了有针对性的梯度，使策略能够解决无引导探索无法处理的关键场景。大量实验表明，我们的方法释放了 VLA 模型的潜在能力，在公开的 NAVSIM 基准测试中，在综合 PDMS、EPDMS 得分和高层规划准确率方面均达到了最先进（SOTA）的性能。

## Abstract
Vision-Language-Action (VLA) models for autonomous driving often hit a performance plateau during Reinforcement Learning (RL) optimization. This stagnation arises from exploration capabilities constrained by previous Supervised Fine-Tuning (SFT), leading to persistent failures in long-tail scenarios. In these critical situations, all explored actions yield a zero-value driving score. This information-sparse reward signals a failure, yet fails to identify its root cause -- whether it is due to incorrect planning, flawed reasoning, or poor trajectory execution. To address this limitation, we propose VLA with Explicit Learning from Failures (ELF-VLA), a framework that augments RL with structured diagnostic feedback. Instead of relying on a vague scalar reward, our method produces detailed, interpretable reports that identify the specific failure mode. The VLA policy then leverages this explicit feedback to generate a Feedback-Guided Refinement. By injecting these corrected, high-reward samples back into the RL training batch, our approach provides a targeted gradient, which enables the policy to solve critical scenarios that unguided exploration cannot. Extensive experiments demonstrate that our method unlocks the latent capabilities of VLA models, achieving state-of-the-art (SOTA) performance on the public NAVSIM benchmark for overall PDMS, EPDMS score and high-level planning accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unleashing VLA Potentials in Autonomous Driving via Explicit Learning from Failures》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是自动驾驶中**视觉-语言-动作（VLA）模型**在强化学习（RL）优化阶段遇到的**性能瓶颈（Performance Plateau）**。
*   **研究动机**：传统的 VLA 模型在经过监督微调（SFT）后，其探索能力受限于 SFT 数据集的分布。在面对复杂、长尾的挑战性场景（如无保护左转、紧急避让）时，模型的所有探索尝试往往都会失败，导致获得“全零”的稀疏奖励信号。
*   **核心痛点**：单一的标量奖励（如 PDMS 分数）无法告诉模型失败的根本原因——是高层规划（Think）错了，还是底层轨迹执行（Act）出了问题。这种信息匮乏使得模型难以在关键场景中实现突破。

### 2. 方法论：ELF-VLA 框架
论文提出了 **ELF-VLA（Explicit Learning from Failures）**，其核心思想是将“失败诊断”与“策略修正”相结合。

*   **两阶段 SFT**：
    1.  **认知阶段**：在海量驾驶问答数据集上预训练，提升模型对驾驶语义的理解。
    2.  **精炼阶段**：在混合数据集（基础输入 + 带有反馈的输入）上微调，使模型学会根据反馈修正轨迹。
*   **结构化诊断反馈（Teacher 模型）**：
    当 Student 模型（VLA 策略）在 RL 探索中失败时，引入一个强大的 Teacher 模型（如 Qwen3-VL-32B）。Teacher 会对比错误轨迹与真值（GT），生成包含元动作分析、思维过程分析、安全/效率失败分析及具体修正建议的**结构化报告**。
*   **反馈引导的精炼与重注入**：
    Student 模型利用该报告生成修正后的高奖励轨迹。这些修正样本被重新注入到 **GRPO（Group Relative Policy Optimization）** 的训练批次中，为模型提供明确的梯度引导。
*   **关键技术细节**：
    *   **难样本筛选（Curation）**：通过 SFT 模型预采样，剔除已掌握的简单场景，集中算力攻克低分、高方差的困难场景。
    *   **策略塑造（Policy Shaping）**：引入函数 $f(x) = \frac{x}{x+\gamma}$，对低概率但高价值的修正样本赋予更高权重，防止训练崩溃。

### 3. 实验设计
*   **数据集与 Benchmark**：主要在 **NAVSIM v1** 和 **NAVSIM v2**（基于 OpenScene 的规划导向基准）上进行评估。
*   **评价指标**：
    *   **PDMS / EPDMS**：综合考虑碰撞、道路合规性、效率和舒适度的闭环规划得分。
    *   **高层规划准确率**：评估纵向速度和横向路径选择的准确性。
*   **对比方法**：包括传统的模块化方法（UniAD）、端到端方法（TransFuser, DiffusionDrive）以及最新的 VLA 基准（AutoVLA, DriveVLA）。

### 4. 资源与算力
论文明确列出了实验配置：
*   **硬件**：使用了 **32 张 NVIDIA H20 GPU**。
*   **训练时长**：
    *   第一阶段（预训练）：16 张 GPU，约 2 天。
    *   第二阶段（SFT）：16 张 GPU，约 8 小时。
    *   第三阶段（RL）：32 张 GPU，约 18 小时。
*   **模型规模**：Student 模型基于 **InternVL3-8B**，Teacher 模型使用 **Qwen3-VL-32B**。

### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **SOTA 对比**：在 NAVSIM v1/v2 上均取得了当前最高分。
*   **消融实验**：
    *   对比了不同反馈策略（GT-GRPO, Rule-GRPO vs. ELF-VLA）。
    *   分析了数据筛选策略的影响。
    *   探讨了反馈触发阈值 $s$、精炼样本数量 $k$ 以及 Policy Shaping 的有效性。
*   **客观性**：通过“全失败率（Total-Failure Ratio）”分析，直观展示了模型在解决长尾场景上的进步，实验设计逻辑严密，对比基准公平。

### 6. 主要结论与发现
*   **突破瓶颈**：显式学习失败原因能有效解决 RL 阶段的奖励稀疏问题，使模型在长尾场景下的 PDMS 显著提升。
*   **反馈质量至关重要**：结构化的文本反馈比简单的规则反馈或直接注入 GT 轨迹效果更好，因为前者更符合 VLA 模型的推理逻辑且分布偏移更小。
*   **高层规划增强**：ELF-VLA 不仅提升了底层轨迹精度，其高层规划准确率（80.3%）也远超 72B 规模的通用大模型。

### 7. 优点（亮点）
*   **可解释性强**：将失败诊断转化为人类可读的结构化报告，使自动驾驶系统的优化过程不再是黑盒。
*   **训练效率高**：通过难样本筛选和反馈注入，避免了在简单场景上的无效探索。
*   **算法创新**：结合了 Policy Shaping 解决了离策（Off-policy）引导下的训练稳定性问题。

### 8. 不足与局限
*   **Teacher 依赖**：Student 模型的上限在很大程度上受限于 Teacher 模型的诊断能力。
*   **仿真环境限制**：实验主要在 NAVSIM 这种**非反应式（Non-reactive）**仿真环境中完成，尚未在具有动态交互的实时闭环环境中充分验证。
*   **推理开销**：虽然训练阶段使用了 Teacher，但模型在推理时生成 CoT 仍有一定延迟（文中提到加速后为 0.1s），对于极高频率的实时控制可能仍有挑战。

（完）
