---
title: Recursive Models for Long-Horizon Reasoning
title_zh: 递归模型用于长程推理
authors: "Chenxiao Yang, Nathan Srebro, Zhiyuan Li"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02112v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于长程推理和子任务分解的递归模型
tldr: 针对现代语言模型在长程推理中受限于有限上下文的问题，本文提出了一种递归模型框架。该方法允许模型在隔离的上下文中递归调用自身来解决子任务。理论证明，递归分解能将子任务所需的活动上下文需求呈指数级降低，优于传统的摘要等上下文管理方法。实验表明，采用递归推理的3B参数模型在布尔可满足性（SAT）任务上显著超越了顶尖的大语言模型。
selection_source: fresh_fetch
motivation: 现代语言模型的有限上下文窗口限制了其处理复杂长程推理任务的能力。
method: 提出递归模型框架，允许模型在独立的上下文中递归调用自身以分解并解决子任务。
result: 理论证明递归分解可指数级降低上下文需求，且3B规模的递归模型在SAT任务上表现优于前沿大模型。
conclusion: 递归是实现长程推理的核心原则，能使模型在受限上下文中高效处理复杂的组合搜索问题。
---

## 摘要
现代语言模型在有限的上下文内进行推理，这一固有约束构成了长程推理的根本障碍。我们将递归视为克服这一障碍的核心原则，并提出了递归模型作为一种极简实现，其中模型可以在隔离的上下文中递归地调用自身来解决子任务。我们证明了任何可计算问题都允许递归分解，其中每个子任务所需的活动上下文仅为标准自回归模型的指数级缩小；这严格优于任何局限于单一序列的上下文管理方法（如摘要）。我们进一步将该框架推广到具有任意上下文处理和控制流的现代智能体系统，并证明递归模型可以在这一更广泛的类别中实现最优能力。在实验上，我们训练了一个 3B 模型进行递归推理，并在布尔可满足性（Boolean satisfiability）任务上进行了评估，这是一项需要长程组合搜索的任务，实验结果表明该模型显著优于前沿大语言模型（LLMs）。

## Abstract
Modern language models reason within bounded context, an inherent constraint that poses a fundamental barrier to long-horizon reasoning. We identify recursion as a core principle for overcoming this barrier, and propose recursive models as a minimal realization, where the model can recursively invoke itself to solve subtasks in isolated contexts. We prove that any computable problem admits a recursive decomposition in which each subtask requires only exponentially smaller active context than standard autoregressive models; this strictly surpasses any context management approach confined to a single sequence, such as summarization. We further generalize our framework to modern agentic systems with arbitrary context processing and control flows, and prove that recursive models can achieve optimal power within this broader class. Experimentally, we train a 3B model to reason recursively and evaluate on Boolean satisfiability, a task requiring long-horizon combinatorial search, where it significantly outperforms frontier LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Recursive Models for Long-Horizon Reasoning》（递归模型用于长程推理）的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现代大语言模型（LLMs）受限于**有限的上下文窗口（Bounded Context Window）**。在处理需要极长推理链条的任务（如复杂的组合搜索、数学证明）时，模型无法在单次前向传播或单一序列中保留所有必要信息。
*   **核心问题**：现有的上下文管理方法（如摘要压缩、外部存储检索）是否足以应对长程推理？是否存在一种更底层的机制能从根本上突破上下文限制？
*   **整体含义**：本文将“递归”识别为克服上下文障碍的核心原则，并证明了递归模型在计算能力上指数级优于传统的自回归模型和单上下文管理模型（如摘要法）。

### 2. 方法论
*   **核心思想**：提出**递归模型（Recursive Model, RCM）**。它由一个基础生成器（LLM）和一个简单的上下文管理器组成，通过维护一个**上下文栈（Context Stack）**来实现任务分解。
*   **关键技术细节**：
    *   **最小工具集**：引入 `call`（调用）和 `return`（返回）两个特殊标记。
    *   **Call 机制**：当模型生成 `call` 块时，提取子任务描述并推入栈顶，创建一个全新的、隔离的上下文。
    *   **Return 机制**：子任务完成后，生成 `return` 块，将结果传回父上下文，并彻底丢弃子任务的中间推理过程，从而释放空间。
    *   **空间分离**：区分**全局空间（Global Space）**（所有挂起上下文的总和，可存储在磁盘/内存）和**局部空间（Local Space）**（当前活跃的上下文窗口，必须在 GPU 显存中）。
*   **算法流程**：模型在当前上下文生成推理；遇到子任务则发起递归调用；子任务返回结果后，模型继续在父上下文中利用该结果进行下一步推理。

### 3. 实验设计
*   **数据集/场景**：**布尔可满足性问题（SAT）**。这是一个典型的 NP 完全问题，天然适合通过回溯搜索（递归）来解决。
*   **Benchmark**：使用 SATBench，将其转换为自然语言形式的逻辑谜题。
*   **对比方法（Baselines）**：
    *   前沿大模型：GPT-4o, LLaMA3.3-70B, Qwen3-235B。
    *   推理增强模型：DeepSeek-Distill-14B。
*   **评估指标**：在简单（Easy）、中等（Medium）、困难（Hard）三个难度等级上的准确率，以及推理过程中的上下文效率。

### 4. 资源与算力
*   **硬件配置**：使用了 **2 张 NVIDIA H200 GPU**。
*   **训练细节**：
    *   基础模型：Qwen2.5-3B-Instruct。
    *   训练时长：约 **8 小时**。
    *   训练参数：10 个 Epoch，学习率 1e-5，AdamW 优化器。
*   **推理效率**：递归模型通过将 KV 缓存移出/移入显存，显著降低了单步推理的 FLOPs 消耗。

### 5. 实验数量与充分性
*   **实验规模**：
    *   训练集：仅使用 15 个变量以内的简单和中等难度实例进行微调。
    *   测试集：每个难度等级随机抽取 100 个实例。
*   **充分性评价**：
    *   **理论充分性**：论文用了大量篇幅进行计算复杂度证明（如证明 RCM 属于 $TIME(2^{O(S(n))})$ 复杂度类），这为实验提供了坚实的理论支撑。
    *   **实验客观性**：通过 3B 小模型挑战 235B 大模型的对比实验，有力证明了递归机制的优越性。
    *   **局限性**：实验目前主要集中在 SAT 这一种任务上，虽然 SAT 具有代表性，但在更多样化的长程推理任务（如长文本代码生成、多步规划）上的泛化性仍有待进一步验证。

### 6. 主要结论与发现
*   **指数级增益**：递归分解能将任务所需的活动上下文需求从指数级降低为线性级。
*   **能力边界**：证明了任何可计算问题都存在递归分解形式。递归模型的能力上限远高于任何单上下文模型（如摘要法）。
*   **以小博大**：经过递归微调的 3B 模型在 SAT 任务上显著超越了 GPT-4o 和 Qwen3-235B，尤其是在需要深度搜索的复杂实例上。
*   **泛化能力**：在简单/中等任务上训练的模型，能够泛化到更难的（Hard）任务，显示出递归逻辑的通用性。

### 7. 优点
*   **理论深度**：不仅提出了方法，还从计算复杂度和图灵机理论角度给出了严密的证明，填补了智能体系统形式化分析的空白。
*   **架构极简**：不需要修改 Transformer 架构，仅通过增加 `call/return` 逻辑和微调即可实现。
*   **推理高效**：通过隔离上下文，避免了长序列带来的注意力计算开销爆炸。

### 8. 不足与局限
*   **任务覆盖面**：目前仅在 SAT 任务上验证，对于非结构化或搜索路径不明确的推理任务（如创意写作、开放式问答），递归分解的难度较大。
*   **错误累积风险**：虽然隔离上下文减少了干扰，但如果底层子任务返回了错误结果，父任务往往难以纠正，可能导致整条推理链崩溃。
*   **推理延迟**：深度递归涉及多次顺序调用，虽然节省了显存，但可能增加端到端的推理时间（Latency）。

（完）
