---
title: "HiMAP-Travel: Hierarchical Multi-Agent Planning for Long-Horizon Constrained Travel"
title_zh: HiMAP-Travel：面向长程约束旅行规划的分层多智能体规划
authors: "The Viet Bui, Wenjun Li, Yong Liu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04750v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 使用GRPO策略训练的针对长程任务的分层多智能体规划
tldr: 针对长程旅行规划中LLM智能体难以处理硬约束（如预算、多样性）且效率低下的问题，本文提出HiMAP-Travel分层多智能体框架。该框架将规划分解为战略协调与并行执行，通过协调器分配资源、执行器独立规划，并结合事务监控与讨价还价协议确保全局约束一致性。实验显示，该方法在TravelPlanner基准上显著超越现有模型，并大幅降低了推理延迟。
selection_source: fresh_fetch
motivation: 传统的顺序LLM智能体在长程规划中容易随上下文增长而偏离全局约束，且难以兼顾效率与硬性限制。
method: 提出分层架构，由协调器进行资源分配，多个执行器并行规划，并利用事务监控和讨价还价协议处理约束冲突。
result: "在TravelPlanner测试中达到52.65%的通过率，优于DeepTravel等基准，且在多轮场景下实现2.5倍的延迟降低。"
conclusion: 分层并行规划结合硬约束监控机制，能有效提升LLM在复杂长程任务中的规划成功率与执行效率。
---

## 摘要
序列化大语言模型（LLM）智能体在处理带有预算和多样性要求等硬约束的长程规划任务时表现不佳。随着规划的推进和上下文的增长，这些智能体会偏离全局约束。我们提出了 HiMAP-Travel，这是一个分层多智能体框架，将规划分解为策略协调和并行的天级执行。协调器（Coordinator）负责跨天分配资源，而天执行器（Day Executors）则独立并行地进行规划。三个关键机制实现了这一点：一个在并行智能体间强制执行预算和唯一性约束的事务监控器（transactional monitor）；一个允许智能体拒绝不可行子目标并触发重新规划的讨价还价协议（bargaining protocol）；以及一个通过角色调节（role conditioning）驱动所有智能体、并使用 GRPO 训练的单一策略。在 TravelPlanner 上，搭载 Qwen3-8B 的 HiMAP-Travel 实现了 52.78% 的验证集和 52.65% 的测试集最终通过率（FPR）。在使用相同模型、训练和工具的受控对比中，它比序列化 DeepTravel 基准高出 8.67 个百分点。它还分别超过了 ATLAS 17.65 个百分点和 MTP 10.0 个百分点。在 FlexTravelBench 多轮场景中，它实现了 44.34%（2 轮）和 37.42%（3 轮）的 FPR，同时通过并行化将延迟降低了 2.5 倍。

## Abstract
Sequential LLM agents fail on long-horizon planning with hard constraints like budgets and diversity requirements. As planning progresses and context grows, these agents drift from global constraints. We propose HiMAP-Travel, a hierarchical multi-agent framework that splits planning into strategic coordination and parallel day-level execution. A Coordinator allocates resources across days, while Day Executors plan independently in parallel. Three key mechanisms enable this: a transactional monitor enforcing budget and uniqueness constraints across parallel agents, a bargaining protocol allowing agents to reject infeasible sub-goals and trigger re-planning, and a single policy trained with GRPO that powers all agents through role conditioning. On TravelPlanner, HiMAP-Travel with Qwen3-8B achieves 52.78% validation and 52.65% test Final Pass Rate (FPR). In a controlled comparison with identical model, training, and tools, it outperforms the sequential DeepTravel baseline by +8.67~pp. It also surpasses ATLAS by +17.65~pp and MTP by +10.0~pp. On FlexTravelBench multi-turn scenarios, it achieves 44.34% (2-turn) and 37.42% (3-turn) FPR while reducing latency 2.5x through parallelization.

---

## 论文详细总结（自动生成）

### 论文总结：HiMAP-Travel 分层多智能体长程约束规划框架

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）智能体在处理**长程（Long-Horizon）且带有硬约束（如预算、多样性、时间一致性）**的规划任务时的脆弱性进行了研究。
*   **约束漂移（Constraint Drift）**：作者发现，传统的顺序执行智能体（如 ReAct）随着规划步数的增加，推理轨迹和工具输出会不断堆叠，导致上下文过长，模型对初始全局约束（如总预算）的注意力被稀释，从而在规划后期频繁违规。
*   **效率瓶颈**：现有的“验证-修正”机制（如 ATLAS）通常在生成完整计划后才检查约束，导致在规划早期出错时会浪费大量计算资源，且延迟随规划长度呈超线性增长。

#### 2. 方法论：核心思想与关键技术
HiMAP-Travel 提出了一种分层解耦的架构，将规划任务从“生成后修复”转变为“构建即正确”。

*   **分层架构**：
    *   **协调器（Coordinator）**：负责战略层。将全局查询分解为天级别的子目标（Sub-goals），分配预算暗示（Budget Hints）和城市角色。
    *   **执行器（Executors）**：负责战术层。多个执行器并行工作，每个执行器只负责一天的具体规划，确保上下文独立，防止“上下文腐烂”。
*   **关键机制**：
    *   **同步全局状态（Σ）**：引入一个外部的事务性监控器，通过全局互斥锁管理共享资源（如已用预算、已选景点去重、交通模式锁定）。执行器在提交动作前必须通过 `CHECK` 和 `COMMIT` 操作，确保并行执行时不冲突。
    *   **协作讨价还价协议（Bargaining Protocol）**：如果执行器发现分配的子目标不可行（如预算不足），会返回结构化的反馈信号，触发协调器重新分配资源。
    *   **统一角色调节策略**：使用单一的策略网络 $\pi_\theta$，通过系统提示词（System Prompt）切换角色。
*   **算法训练**：采用 **GRPO（Group Relative Policy Optimization）** 算法进行端到端强化学习训练。为了解决多智能体训练的内存压力，提出了 **FIFO 共享采样池机制**，在角色分区达到组大小后立即更新，降低了内存占用。

#### 3. 实验设计
*   **数据集与基准**：
    *   **TravelPlanner**：包含 1225 个查询，涉及 13 项硬约束和常识约束。
    *   **FlexTravelBench**：测试多轮对话下的约束适应能力（约束随轮次逐步揭示）。
*   **对比方法**：
    *   **顺序 RL 基准**：DeepTravel（使用相同的 Qwen3 内核和 GRPO 训练）。
    *   **多智能体框架**：ATLAS（基于 Gemini-2.5-Pro）、MTP（基于 GPT-4）。
    *   **基础模式**：ReAct、Reflexion。
*   **评估指标**：最终通过率（FPR）、交付率（Delivery Rate）、常识/硬约束通过率、延迟（Latency）。

#### 4. 资源与算力
*   **模型内核**：Qwen3-4B-Instruct 和 Qwen3-8B。
*   **训练细节**：在 TravelPlanner 的 45 个训练查询上训练 100 个 epoch。
*   **算力环境**：附录提到使用 8 卡 GPU 节点进行实验。
*   **超参数**：学习率 $5 \times 10^{-6}$，Batch Size 32，GRPO 组大小 $G=4$。
*   **推理效率**：在 7 天行程规划中，HiMAP-Travel 将延迟从 190 秒降低至 72 秒（约 2.6 倍加速）。

#### 5. 实验数量与充分性
*   **实验规模**：在 1000 个测试用例上进行了大规模验证，并提供了 4 个随机种子的均值和标准差。
*   **消融实验**：非常充分。分别验证了“同步监控器”、“协调器”、“讨价还价协议”和“并行化”对 FPR 的贡献，证明了每个组件的必要性。
*   **公平性**：作者特别强调了“受控对比”，即在相同的模型内核、工具集和训练算法下对比顺序与分层架构，客观地证明了架构改进带来的增益。

#### 6. 主要结论与发现
*   **性能领先**：HiMAP-Travel (8B) 在 TravelPlanner 测试集上达到 **52.65% FPR**，远超 ATLAS (35.0%) 和 DeepTravel (43.98%)。
*   **方差大幅降低**：相比顺序模型，HiMAP-Travel 的跨种子方差降低了 **93%**，表现出极高的训练稳定性。
*   **解决约束漂移**：实验证明，HiMAP-Travel 在行程后期的预算达标率（Day 5: 91%）远高于顺序模型（Day 5: 42%）。
*   **多轮适应性**：在 FlexTravelBench 上，即使在 3 轮约束增加的情况下，依然保持了较强的鲁棒性。

#### 7. 优点
*   **架构创新**：通过事务性监控器解决了并行智能体间的资源竞争问题，将“悲观”的后验验证转变为“乐观”的前验约束。
*   **效率卓越**：利用并行执行大幅缩短了长程规划的墙钟时间。
*   **训练高效**：证明了在极小规模数据集（45 条）上通过 GRPO 和分层架构也能训练出极强泛化能力的规划智能体。

#### 8. 不足与局限
*   **数据质量依赖**：分析发现部分失败源于数据库元数据错误（如 $0 酒店），模型目前还不能完全识别并绕过这些异常数据。
*   **复杂约束挑战**：在“房间规则（Room Rule）”和“房间类型”等细粒度约束上的表现仍有提升空间（通过率约 30%-40%）。
*   **回滚假设**：讨价还价协议假设预订操作是可无代价回滚的，这在现实世界的某些即时支付场景中可能不成立。

（完）
