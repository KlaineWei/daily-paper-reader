---
title: "$\\textbf{Re}^{2}$: Unlocking LLM Reasoning via Reinforcement Learning with Re-solving"
title_zh: Re²：通过重解强化学习解锁大语言模型推理能力
authors: "Pinzheng Wang, Shuli Xu, Juntao Li, Yu Luo, Dong Li, Jianye Hao, Min Zhang"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07197v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 基于可验证奖励的强化学习用于推理
tldr: "针对大语言模型在强化学习后仍存在低质量推理路径和“过度思考”的问题，本文提出 Re²（带重解的强化学习）。该方法通过纯强化学习训练模型在发现推理方向错误时主动放弃并重新开始，而非盲目推导。实验表明，Re² 将模型的“重试”行为比例从 0.5% 提升至 30% 以上，在相同计算预算下显著优于标准 RLVR，并展现出更强的测试时性能扩展性。"
selection_source: fresh_fetch
motivation: 现有的强化学习推理模型在初始思路错误时往往会陷入低效的无效推导，导致推理质量和效率下降。
method: 提出 Re² 方法，利用纯强化学习训练模型识别无效路径并灵活选择“重新开始”解题，而非强行得出结论。
result: "该方法成功将模型的重解行为比例提升至 30% 以上，在同等训练预算下显著提升了推理准确率和测试时性能。"
conclusion: Re² 证明了赋予模型“推倒重来”的能力是提升复杂推理任务效率和准确性的有效途径。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 通过增加测试时计算量，在增强大语言模型 (LLMs) 的推理性能方面展现出巨大潜力。然而，即使经过广泛的 RLVR 训练，此类模型在其思维链 (CoT) 中仍倾向于生成不必要且低质量的步骤，导致低效的过度思考和较低的回答质量。我们研究表明，当 CoT 的初始方向或质量不佳时，模型往往无法得出正确答案，即使其生成的 token 数量是初始 CoT 初始化良好时的数倍。为此，我们引入了重解强化学习 (Re²)，使 LLM 能够灵活地放弃无成效的推理路径，并在必要时重新启动解题过程，而不是始终执着于给出一个最终答案。Re² 采用纯强化学习，无需任何初步的监督微调，成功地将原始模型中仅 0.5% 的罕见“重做”行为提升至 30% 以上。在相同的训练计算预算下，这相比标准 RLVR 带来了显著的性能提升，并且随着样本数量的增加，在测试时性能上也表现出显著的改进。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has shown promise in enhancing the reasoning performance of large language models (LLMs) by increasing test-time compute. However, even after extensive RLVR training, such models still tend to generate unnecessary and low-quality steps in their chain-of-thought (CoT), leading to inefficient overthinking and lower answer quality. We show that when the initial direction or quality of the CoT is suboptimal, the model often fails to reach the correct answer, even after generating several times more tokens than when the initial CoT is well-initialized. To this end, we introduce Reinforcement Learning with Re-solving (Re$^2$), in which LLMs learn to flexibly abandon unproductive reasoning paths and restart the solution process when necessary, rather than always committing to a final answer. Re$^2$ applies pure reinforcement learning without any preliminary supervised fine-tuning, successfully amplifying the rare redo behavior in vanilla models from only 0.5% to over 30%. This leads to substantial performance gains over standard RLVR under the same training compute budget, and also demonstrates notable improvements in test-time performance as the number of samples increases.

---

## 论文详细总结（自动生成）

以下是对论文《$\text{Re}^2$: Unlocking LLM Reasoning via Reinforcement Learning with Re-solving》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的强化学习推理模型（如 DeepSeek-R1 风格的 RLVR）在面对复杂问题时，往往会陷入“过度思考”或“无效思考”。研究发现，如果模型在推理链（CoT）的早期阶段方向错误，即使后续生成数倍的 token，也极难修正错误。
*   **研究动机**：在人类解题时，发现思路不通会选择“推倒重来”。作者希望赋予 LLM 这种“灵活放弃并重启”的能力，打破传统 RLVR 强迫模型在单一路径上走到底的局限，从而提升推理效率和最终准确率。

### 2. 论文提出的方法论：Re² (Reinforcement Learning with Re-solving)
*   **核心思想**：通过纯强化学习（无需 SFT），训练模型在推理过程中自主决定是“继续推导得出答案”还是“放弃当前路径重新开始（Redo）”。
*   **关键技术细节**：
    *   **前缀组生成（Prefix Group Generation）**：在训练时，为每个问题采样多个响应并随机截断，形成 $n$ 个不同的推理前缀。针对每个前缀，模型再生成 $m$ 个后续推导。
    *   **三路奖励策略（Reward Strategy）**：
        1.  **正确答案**：奖励为 1。
        2.  **错误答案**：奖励为 0。
        3.  **选择重解（Resolve）**：奖励设为“从头开始解题的期望成功率”。该成功率通过同一批次中其他组（out-of-group）的平均准确率动态估算。
    *   **算法流程**：基于 DAPO 框架进行组内优势（Advantage）计算。如果当前路径质量低，选择“重解”获得的期望奖励会高于强行给出错误答案的奖励（0），从而引导模型学习在迷茫时重启。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：AIME 2024、AIME 2025（最新发布，无数据泄露风险）、AMC 2023、GSM8K。
    *   **科学推理**：GPQA-Diamond（研究生水平的硬核科学问题）。
*   **对比方法（Baselines）**：
    *   **DAPO**：目前主流的开源 RLVR 强化学习方法。
    *   **原始模型**：未经 RL 训练的 Base 或 Instruct 模型。
    *   **Majority Voting**：作为测试时扩展（Test-time scaling）的基准对比。
*   **实验模型**：涵盖 Qwen2.5 (7B/14B)、Llama-3.2-3B 以及推理专用模型 DeepSeek-R1-Distill-Llama-8B。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100-PCIE-40GB GPU**。
*   **训练开销**：
    *   Re² 采用两阶段生成（先生成前缀，再生成后缀），其 Rollout（采样）阶段的时间比标准 DAPO 增加约 **11%**。
    *   例如，Qwen2.5-14B 的训练在上述硬件上持续了约 25 小时。
*   **超参数**：Batch Size 为 32，学习率 $1 \times 10^{-6}$，最大序列长度 8192。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同规模和类型的模型上进行了验证，覆盖了从 3B 到 14B 的参数区间。
*   **充分性与公平性**：
    *   **同预算对比**：在对比 Re² 和 DAPO 时，严格控制了训练过程中生成的 token 总数一致。
    *   **多维度分析**：不仅对比了最终准确率，还分析了训练动力学（奖励变化）、推理长度演变、以及不同难度题目下的“重解”触发率。
    *   **测试时扩展性**：通过采样 1 到 1024 个样本，绘制了完整的性能随计算量增长的曲线，证明了 Re² 在高算力投入下的上限更高。

### 6. 论文的主要结论与发现
*   **重解行为的激活**：通过纯 RL，模型自发的“重解”行为比例从原始模型的 **0.5% 飙升至 30% 以上**。
*   **性能提升**：在所有基准测试中，Re² 均显著优于标准 RLVR。在 AIME 2025 等高难度任务上，准确率提升尤为明显（例如 Qwen2.5-7B-it 从 8.6% 提升至 21.2%）。
*   **更理性的推理**：Re² 减少了模型在错误路径上“瞎编”的倾向。模型变得更“诚实”：有把握时直接输出，思路混乱时选择重启。
*   **Test-time Scaling 优势**：在增加采样次数时，Re² 的性能增长曲线比传统多数投票（Majority Voting）更陡峭，展现了更好的计算效率。

### 7. 优点：方法或实验设计上的亮点
*   **无需 SFT 数据**：完全依赖强化学习自发涌现重解行为，降低了对高质量人工标注数据的依赖。
*   **动态奖励建模**：将“重解”的奖励与模型当前的实时能力（Batch 内表现）挂钩，这比固定惩罚或奖励更符合模型训练的动态过程。
*   **解决“路径依赖”**：从根本上缓解了 CoT 推理中“一步错步步错”的顽疾，提升了长链推理的鲁棒性。

### 8. 不足与局限
*   **推理效率可控性**：目前无法在推理阶段精确控制模型触发“重解”的频率，可能导致某些简单题目也出现不必要的重启，增加推理延迟。
*   **任务覆盖面**：实验主要集中在数学和科学推理（有明确答案的问题），对于创意写作、开放式对话等缺乏客观验证奖励（Verifiable Rewards）的场景，该方法的适用性尚未验证。
*   **多轮重解限制**：虽然理论支持多次重解，但过多的重解轮数会显著消耗上下文窗口和推理成本。

（完）
