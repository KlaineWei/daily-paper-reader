---
title: "Scale-Plan: Scalable Language-Enabled Task Planning for Heterogeneous Multi-Robot Teams"
title_zh: Scale-Plan：面向异构多机器人团队的可扩展语言赋能任务规划
authors: "Piyush Gupta, Sangjae Bae, Jiachen Li, David Isele"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08814v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 多机器人团队的可扩展语言使能任务规划
tldr: 针对异构多机器人系统在长程任务规划中面临的感知信息冗余和LLM幻觉问题，本文提出了Scale-Plan框架。该框架结合PDDL领域规范与LLM推理，通过构建动作图并进行结构化搜索，自动提取与任务相关的最小动作和对象子集，从而生成紧凑的问题表示。实验表明，该方法在复杂多智能体任务中显著提升了规划的扩展性和可靠性，并引入了MAT2-THOR基准进行验证。
selection_source: fresh_fetch
motivation: 传统符号规划器依赖人工建模且扩展性差，而现有LLM方法在处理包含大量无关对象的复杂环境时容易产生幻觉且缺乏环境对齐。
method: 提出一种可扩展的LLM辅助框架，利用动作图引导的结构化搜索来识别任务相关的最小对象和动作子集，从而简化PDDL问题表示。
result: 在多机器人基准测试MAT2-THOR中，Scale-Plan在规划效率和成功率上均显著优于纯LLM及现有的混合LLM-PDDL基准方法。
conclusion: 该研究证明了通过预先过滤无关信息并结合结构化搜索与语言模型，可以有效解决异构多机器人系统在大规模环境下的长程规划难题。
---

## 摘要
异构多机器人系统的长程任务规划对于在现实环境中部署协作团队至关重要；然而，由于感知信息量巨大，且其中大部分与任务目标无关并加重了规划负担，这仍然是一项挑战。传统的符号规划器依赖于人工构建的问题规范，限制了可扩展性和适应性，而最近基于大语言模型（LLM）的方法在物体丰富的场景中经常面临幻觉和弱接地（即生成的计划与实际环境物体及约束之间的对齐较差）的问题。我们提出了 Scale-Plan，这是一个可扩展的 LLM 辅助框架，能够从自然语言指令中生成紧凑且与任务相关的问题表示。给定 PDDL 领域规范，Scale-Plan 构建一个捕获领域结构的动作图，并利用浅层 LLM 推理来引导结构化图搜索，从而识别相关动作和物体的最小子集。通过在规划前过滤无关信息，Scale-Plan 实现了高效的任务分解、分配和长程计划生成。我们在复杂的多智能体任务上评估了该方法，并引入了 MAT2-THOR，这是一个基于 AI2-THOR 构建的经过清理的基准测试，用于对多机器人规划系统进行可靠评估。Scale-Plan 在所有指标上均优于纯 LLM 和混合 LLM-PDDL 基准，提升了系统的可扩展性和可靠性。

## Abstract
Long-horizon task planning for heterogeneous multi-robot systems is essential for deploying collaborative teams in real-world environments; yet, it remains challenging due to the large volume of perceptual information, much of which is irrelevant to task objectives and burdens planning. Traditional symbolic planners rely on manually constructed problem specifications, limiting scalability and adaptability, while recent large language model (LLM)-based approaches often suffer from hallucinations and weak grounding-i.e., poor alignment between generated plans and actual environmental objects and constraints-in object-rich settings. We present Scale-Plan, a scalable LLM-assisted framework that generates compact, task-relevant problem representations from natural language instructions. Given a PDDL domain specification, Scale-Plan constructs an action graph capturing domain structure and uses shallow LLM reasoning to guide a structured graph search that identifies a minimal subset of relevant actions and objects. By filtering irrelevant information prior to planning, Scale-Plan enables efficient decomposition, allocation, and long-horizon plan generation. We evaluate our approach on complex multi-agent tasks and introduce MAT2-THOR, a cleaned benchmark built on AI2-THOR for reliable evaluation of multi-robot planning systems. Scale-Plan outperforms pure LLM and hybrid LLM-PDDL baselines across all metrics, improving scalability and reliability.

---

## 论文详细总结（自动生成）

### 论文总结：Scale-Plan —— 面向异构多机器人团队的可扩展语言赋能任务规划

#### 1. 核心问题与整体含义
该论文关注的是**异构多机器人系统在复杂环境下的长程任务规划**。
*   **研究动机**：在现实场景（如家庭、仓库）中，环境包含海量的感知信息，但大部分物体与当前任务无关。传统的符号规划器（如 PDDL）依赖人工建模，扩展性差；而直接使用大语言模型（LLM）进行规划时，过多的无关信息会导致模型产生“幻觉”、超出上下文长度限制，并导致生成的计划与物理环境约束脱节（弱接地）。
*   **核心目标**：通过过滤无关环境信息，生成紧凑的任务表示，从而提升多机器人协作规划的效率、可靠性和可扩展性。

#### 2. 方法论
Scale-Plan 框架结合了符号逻辑的严谨性和 LLM 的推理能力，其核心流程如下：
*   **动作图构建（Action Graph Construction）**：
    *   **离线阶段**：从 PDDL 领域规范中提取动作模式（Action Schemas）。
    *   **构图规则**：若动作 A 的效果满足动作 B 的前置条件，则建立有向边。分为**严格规则**（完全满足前置条件）和**宽松规则**（部分满足且为保持连通性所必需），从而捕捉领域内的逻辑依赖。
*   **任务相关环境过滤（Task-Relevant Filtering）**：
    *   **在线阶段**：LLM 首先根据自然语言指令提出候选动作和物体。
    *   **图搜索**：以候选动作为终点，在动作图中进行反向深度优先搜索（DFS），识别出满足逻辑链条所需的最小动作和物体子集。
*   **结构化规划流水线**：
    *   **任务分解与分配**：利用过滤后的精简信息，LLM 将总任务分解为子任务，并根据机器人能力（异构性）进行分配。
    *   **计划集成与代码化**：将各机器人的子计划整合，处理并行与时序依赖，最后通过“Plan-to-Code”模块转化为 AI2-THOR 模拟器可执行的指令。
*   **无需中间 PDDL 问题文件**：与现有方法不同，Scale-Plan 不显式生成易出错的 PDDL 问题文件，而是直接从过滤后的表示中合成执行序列。

#### 3. 实验设计
*   **实验场景**：基于 **AI2-THOR** 物理模拟器。
*   **基准测试（Benchmark）**：引入了 **MAT2-THOR**。这是作者对原始 MAT-THOR 数据集进行清理后的版本，修正了错误的地面真值（Ground Truth）、去除了冗余任务，并增加了 `num_contains` 等约束参数。
*   **任务分类**：包含 49 个任务，分为简单（Simple）、复杂（Complex）和模糊（Vague）三类。
*   **对比方法**：
    1.  **LLM as a Planner**：直接用 LLM 生成计划。
    2.  **LLM+P**：LLM 生成 PDDL 问题文件 + 外部规划器。
    3.  **LaMMA-P (PDDL-only)**：模块化 PDDL 规划流水线。
    4.  **LaMMA-P (LLM-corrected)**：在 PDDL 规划基础上增加 LLM 纠错。

#### 4. 资源与算力
*   **模型使用**：论文明确提到使用了 **GPT-5.2**（注：这可能是基于论文发表时间设定的未来版本或特定 API 引用）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要依赖 LLM 推理和图搜索算法，不涉及大规模模型训练，算力消耗主要集中在 API 调用和模拟器运行上。

#### 5. 实验数量与充分性
*   **实验规模**：在 49 个标准任务上进行了测试，涵盖了不同难度梯度。
*   **消融实验**：设计了多组消融实验，包括“无过滤+联合规划”、“无过滤+结构化规划”以及“LLM 浅层过滤”，验证了动作图过滤机制的必要性。
*   **客观性与公平性**：所有方法在相同的环境设置和执行协议下运行。通过引入 TCR（任务完成率）、GCR（目标召回率）和 ER（可执行率）三个指标，从成功、进度和鲁棒性三个维度进行了全面评估，实验设计较为客观。

#### 6. 主要结论与发现
*   **性能领先**：Scale-Plan 在所有指标上均优于基准方法。在总任务完成率（TCR）上比最强的基准（LaMMA-P 纠错版）高出 **25%**。
*   **复杂任务优势明显**：在复杂任务中，Scale-Plan 的 TCR 提升了 **35%**，证明了环境过滤在长程规划中的关键作用。
*   **效率权衡**：虽然 Scale-Plan 的规划成功率高，但由于涉及多次 LLM 顺序调用，其规划时间（PT）高于简单的 LLM 直接规划方法，存在“质量换时间”的权衡。

#### 7. 优点
*   **创新的过滤机制**：利用 PDDL 逻辑构建动作图，系统性地减少了 LLM 的搜索空间，有效缓解了幻觉问题。
*   **异构支持**：能够显式考虑不同机器人的技能子集，实现高效的任务分配。
*   **基准贡献**：发布的 MAT2-THOR 修正了现有数据集的诸多缺陷，为多机器人规划研究提供了更可靠的评估工具。

#### 8. 不足与局限
*   **接地（Grounding）风险**：虽然减少了无关物体，但仍缺乏显式的物理接地（如精确的坐标对齐），可能导致机器人导航到物体的“预测位置”而非“真实位置”。
*   **常识推理缺失**：在失败案例分析中，模型有时会忽略“放东西前先开门”等物理常识（Affordance），说明仅靠 PDDL 逻辑和 LLM 仍有覆盖不到的语义约束。
*   **实时性挑战**：多次 LLM 推理导致规划延迟较高，可能不适用于对实时响应要求极高的动态环境。

（完）
