---
title: "Good Reasoning Makes Good Demonstrations: Implicit Reasoning Quality Supervision via In-Context Reinforcement Learning"
title_zh: 良好的推理造就良好的示例：通过上下文强化学习实现隐式推理质量监督
authors: "Tiehua Mei, Minxuan Lv, Leiyu Pan, Zhenpeng Su, Hongru Hou, Hengrui Chen, Ao Xu, Deqing Yang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09803v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 带有可验证奖励的上下文强化学习
tldr: 本研究针对强化学习（RLVR）在提升大模型推理能力时无法区分正确答案背后推理质量的问题，提出了一种名为 In-Context RLVR 的新方法。该方法基于“高质量推理是更好的示例”这一观察，利用模型自身的上下文学习能力来衡量推理过程的“证据增益”，从而作为质量监督信号。通过贝叶斯分析证明，该方法能隐式地为高质量推理分配更高权重，在无需外部评估器的情况下，显著提升了数学推理任务的准确率和逻辑质量。
selection_source: fresh_fetch
motivation: 传统的强化学习方法（RLVR）对所有得到正确答案的推理路径一视同仁，导致模型可能学到逻辑错误但结果正确的“歪打正着”式推理。
method: 引入 In-Context RLVR 框架，通过计算推理路径作为示例时的“证据增益”来衡量其质量，并据此隐式重权化奖励信号。
result: 实验表明，该方法在多个数学基准测试中不仅提高了模型的解题准确率，还显著改善了推理过程的逻辑严密性。
conclusion: 利用模型自身的上下文学习能力作为推理质量的评估指标，是一种高效且无需额外标注成本的强化学习优化方案。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 提升了大语言模型的推理能力，但它对所有正确的解法一视同仁，这可能会强化那些因偶然获得正确答案的错误推理路径。我们观察到，更好的推理是更好的老师：高质量的解法比低质量的解法能作为更有效的示例。我们将这种教学能力称为“示例效用” (Demonstration Utility)，并展示了策略模型自身的上下文学习能力提供了一种衡量该效用的有效方法，从而产生了一种被称为“证据增益” (Evidence Gain) 的质量信号。为了在训练过程中利用这一信号，我们引入了 In-Context RLVR。通过贝叶斯分析，我们证明了该目标函数通过证据增益隐式地对奖励进行重新加权，为高质量的推理路径分配更高的权重，为低质量的路径分配更低的权重，且无需昂贵的计算或外部评估器。在数学基准测试上的实验表明，与标准 RLVR 相比，该方法在准确率和推理质量上均有提升。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) improves reasoning in large language models but treats all correct solutions equally, potentially reinforcing flawed traces that get correct answers by chance. We observe that better reasoning are better teachers: high-quality solutions serve as more effective demonstrations than low-quality ones. We term this teaching ability Demonstration Utility, and show that the policy model's own in-context learning ability provides an efficient way to measure it, yielding a quality signal termed Evidence Gain. To employ this signal during training, we introduce In-Context RLVR. By Bayesian analysis, we show that this objective implicitly reweights rewards by Evidence Gain, assigning higher weights to high-quality traces and lower weights to low-quality ones, without requiring costly computation or external evaluators. Experiments on mathematical benchmarks show improvements in both accuracy and reasoning quality over standard RLVR.

---

## 论文详细总结（自动生成）

这是一份关于论文《Good Reasoning Makes Good Demonstrations: Implicit Reasoning Quality Supervision via In-Context Reinforcement Learning》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：带有可验证奖励的强化学习（RLVR，如 GRPO）已成为提升大语言模型（LLM）推理能力的有效手段。然而，RLVR 仅依赖最终答案的正确性（结果监督），导致模型可能强化那些“逻辑错误但答案正确”的推理路径（即“歪打正着”）。
*   **核心问题**：如何在不依赖昂贵的人工过程标注（PRM）或额外的外部奖励模型的情况下，自动识别并鼓励高质量的推理过程？
*   **研究动机**：作者观察到“高质量的推理是更好的老师”。如果一段推理过程作为示例（Demonstration）能更有效地帮助模型解决其他问题，那么这段推理的质量就更高。

### 2. 方法论
*   **核心思想**：利用模型自身的**上下文学习（In-Context Learning, ICL）**能力作为衡量推理质量的“尺子”。
*   **关键技术细节**：
    *   **证据增益（Evidence Gain, $\Delta$）**：定义为模型在给定候选推理路径作为示例后，生成验证集中参考答案的对数似然（Log-Likelihood）的平均增益。公式表达为：$\Delta = E[\log \pi(e_{ans} | q, r, e_q) - \log \pi(e_{ans} | e_q)]$。
    *   **In-Context RLVR**：这是该论文提出的训练框架。在强化学习的采样（Rollout）阶段，不直接输入问题 $q$，而是在 $q$ 之前随机拼接一个来自验证集的高质量示例。
    *   **隐式重权化机制**：通过贝叶斯分析证明，在“带示例”的条件下进行 RL 训练，其目标函数等价于在“无示例”的原始目标上，根据“证据增益”对奖励进行隐式加权。这意味着高质量的推理路径会自动获得更大的梯度信号。

### 3. 实验设计
*   **模型与基座**：使用了 DeepSeek-R1-Distill-Qwen 的 1.5B 和 7B 版本。
*   **数据集**：训练集采用 KlearReasoner-MathSub-30K（约 3 万条数学推理题）。
*   **Benchmark（基准测试）**：AIME24、AIME25、HMMT25、MATH500、AMC23 和 OlympiadBench。
*   **对比方法**：
    *   标准 RL 算法：GRPO、DAPO。
    *   改进型 RL 算法：CISPO、GSPO、CE-GPPO。
    *   所有评估均在**零样本（Zero-shot）**模式下进行，以确保公平性。

### 4. 资源与算力
*   **算力配置**：
    *   **1.5B 模型**：使用了 32 张 GPU。
    *   **7B 模型**：使用了 128 张 GPU。
*   **训练效率**：论文明确指出，In-Context RLVR 相比于基准方法（DAPO）的训练耗时增加**不到 5%**。
*   **对比开销**：文中提到，如果显式计算 1.2 万个样本在 100 个示例下的证据增益，在 H800 GPU 上需要约 80 小时，而该方法通过隐式训练避开了这一巨大开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 的模型参数量，验证了方法的扩展性。
*   **消融实验**：
    *   验证了示例质量的影响（对比了使用 R1 生成的高质量示例与 V3.1 生成的普通示例）。
    *   分析了证据增益与推理质量（逻辑严密性、清晰度等 8 个维度）的相关性。
*   **客观性**：引入了 DeepSeek-V3.2 作为自动评估器，并聘请 4 名人类专家进行盲测，证明了自动评估与人类判断的高度一致性（相关系数 $\rho \approx 0.74$）。

### 6. 主要结论与发现
*   **性能提升**：IC-DAPO 在所有数学基准测试中均优于强基准 DAPO，平均准确率提升约 2.5 个百分点，在 AIME 等高难度竞赛题上提升尤为显著（最高达 5.8 分）。
*   **质量改善**：实验证明，该方法不仅提高了得分，还显著提升了推理过程的逻辑严密性和清晰度。
*   **信号稳定性**：证据增益作为一个质量信号，在整个强化学习训练过程中与推理质量保持了稳定的正相关性（$\rho \approx 0.4$）。

### 7. 优点
*   **极低开销**：无需显式计算复杂的奖励值，仅通过改变输入分布（拼接示例）就实现了质量监督。
*   **无需外部评估器**：利用模型自身的 ICL 能力实现自监督，避免了 PRM 昂贵的标注成本。
*   **理论优雅**：通过贝叶斯推导将 In-Context 训练与奖励重权化联系起来，为方法提供了坚实的数学基础。

### 8. 不足与局限
*   **领域局限性**：实验目前集中在数学推理领域，在 STEM 其他学科或通用逻辑推理任务上的表现尚待验证。
*   **种子依赖**：构建初始的示例集（Validation Set）仍需要一个极强的模型（如 DeepSeek-R1）来提供高质量的参考路径。
*   **相关性上限**：虽然证据增益与质量正相关，但相关系数（0.4 左右）说明它并不能完全替代精细的人工过程审计，仍存在一定的噪声。

（完）
