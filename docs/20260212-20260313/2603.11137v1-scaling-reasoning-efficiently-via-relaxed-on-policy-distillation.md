---
title: Scaling Reasoning Efficiently via Relaxed On-Policy Distillation
title_zh: 通过松弛同策略蒸馏高效扩展推理能力
authors: "Jongwoo Ko, Sara Abdali, Young Jin Kim, Tianyi Chen, Pashmina Cameron"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11137v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 将推理能力转移到受限容量模型的在线策略蒸馏
tldr: 本研究针对在线蒸馏在提升小模型推理能力时存在的不稳定和负迁移问题，提出了REOPOLD框架。该框架将蒸馏视为策略优化过程，通过混合奖励裁剪、基于熵的动态采样及探索到精炼的训练策略，放宽了传统蒸馏的严格模仿限制。实验表明，REOPOLD在数学、视觉和工具使用任务中表现优异，显著提升了样本效率，使7B模型在视觉推理上达到了32B教师模型的水平，并实现了显著的推理加速。
selection_source: fresh_fetch
motivation: 传统的在线蒸馏在将推理能力转移至受限模型时，容易因严格的模仿约束导致训练不稳定和负迁移。
method: 提出REOPOLD框架，利用混合奖励裁剪、基于熵的动态采样和探索到精炼的策略来优化蒸馏过程。
result: 该方法在多项任务中实现了6.7至12倍的样本效率提升，并使7B学生模型在视觉推理上媲美32B教师模型。
conclusion: 通过放宽在线蒸馏的模仿限制，可以更高效、稳定地将复杂推理能力迁移至轻量化模型中。
---

## 摘要
同策略蒸馏对于将推理能力迁移至容量受限的模型至关重要，但仍容易出现不稳定和负迁移。我们从理论和实证两方面证明，同策略蒸馏可以被解释为一种策略优化形式，其中教师与学生模型的对数似然比充当了 token 级奖励。基于这一见解，我们提出了 REOPOLD（松弛同策略蒸馏）框架，该框架通过放宽标准同策略蒸馏的严格模仿约束来稳定优化过程。具体而言，REOPOLD 通过基于混合的奖励裁剪、基于熵的 token 级动态采样以及统一的“从探索到精炼”训练策略，适度且有选择地利用来自教师的奖励。实证结果表明，在数学、视觉和智能体工具使用推理任务中，REOPOLD 凭借训练时卓越的样本效率和推理时增强的测试时间扩展能力超越了基线方法。具体而言，REOPOLD 的表现优于近期的强化学习方法，实现了 6.7 至 12 倍的样本效率提升，并使 7B 参数的学生模型在视觉推理任务中达到了 32B 参数教师模型的水平，同时实现了约 3.32 倍的推理加速。

## Abstract
On-policy distillation is pivotal for transferring reasoning capabilities to capacity-constrained models, yet remains prone to instability and negative transfer. We show that on-policy distillation can be interpreted, both theoretically and empirically, as a form of policy optimization, where the teacher-student log-likelihood ratio acts as a token reward. From this insight, we introduce REOPOLD (Relaxed On-Policy Distillation) a framework that stabilizes optimization by relaxing the strict imitation constraints of standard on-policy distillation. Specifically, REOPOLD temperately and selectively leverages rewards from the teacher through mixture-based reward clipping, entropy-based token-level dynamic sampling, and a unified exploration-to-refinement training strategy. Empirically, REOPOLD surpasses its baselines with superior sample efficiency during training and enhanced test-time scaling at inference, across mathematical, visual, and agentic tool-use reasoning tasks. Specifically, REOPOLD outperforms recent RL approaches achieving 6.7~12x greater sample efficiency and enables a 7B student to match a 32B teacher in visual reasoning with a ~3.32x inference speedup.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scaling Reasoning Efficiently via Relaxed On-Policy Distillation》（通过松弛同策略蒸馏高效扩展推理能力）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**如何高效且稳定地将大型语言模型（LLM）的复杂推理能力迁移到参数量较小的模型（SLM）中。**

*   **背景**：虽然强化学习（RL）在提升模型推理能力（如 OpenAI o1, DeepSeek-R1）上效果显著，但小模型由于表达能力有限，直接进行 RL 往往面临优化困难和奖励稀疏的问题。
*   **动机**：现有的“同策略蒸馏”（On-policy Distillation）虽然比 RL 更高效，但存在**训练不稳定**、**负迁移**（学生模型性能反而下降）以及**熵塌陷**（模型过早失去多样性）等缺陷。
*   **核心意义**：论文提出 REOPOLD 框架，通过将蒸馏重新解释为一种“策略优化”过程，放宽了对教师模型的严格模仿约束，从而实现了更稳定、更具样本效率的推理能力迁移。

### 2. 方法论：REOPOLD 框架
REOPOLD 的核心思想是**“松弛”模仿压力**，将教师信号视为一种 token 级的奖励，并对其进行动态调节。

*   **理论等价性**：论文证明了在应用 `stop-gradient`（停止梯度）操作后，同策略蒸馏的目标函数在数学上等价于策略梯度优化，其中教师与学生模型的对数似然比（Log-likelihood Ratio）即为奖励信号。
*   **关键技术细节**：
    1.  **混合奖励裁剪（Mixture-Based Reward Clipping）**：针对教师概率极低时产生的极端负奖励（重尾分布），利用混合分布理论推导出一个理论下界进行裁剪，防止梯度爆炸。
    2.  **基于熵的 token 级动态采样**：观察到低熵 token（确定性高的词）往往不含推理信息且奖励接近零。REOPOLD 仅对高熵 token（代表关键推理分支）计算梯度，过滤掉无意义的噪声，提升样本效率。
    3.  **探索到精炼的多阶段训练**：
        *   **探索阶段**：屏蔽强烈的负反馈，类似于监督微调（SFT），鼓励模型探索多种可能的正确路径。
        *   **精炼阶段**：引入负反馈和熵引导采样，类似于 RL，对推理路径进行精细化判别和巩固。

### 3. 实验设计
论文在三个关键推理领域进行了广泛验证：
*   **数学推理**：
    *   **数据集**：AIME-24/25, AMC-23, MATH-500, Minerva Math, Olympiad Bench。
    *   **基线**：SFT, 原始 RKL 蒸馏, GRPO (强化学习), ProRL, DeepScaleR。
*   **视觉推理**：
    *   **数据集**：Geometry3K, MathVerse, MathVision, MathVista, HallusionBench。
    *   **基线**：Qwen2.5-VL 原始模型, PAPO, NoisyRollout。
*   **智能体工具使用**：
    *   **场景**：视觉工具调用（如缩放、选帧）。
    *   **数据集**：Pixel-Reasoner, V-Star, InfoVQA, TallyQA。

### 4. 资源与算力
论文详细列出了实验所用的硬件和时间成本：
*   **硬件**：单节点 8 × NVIDIA H100 80GB GPU。
*   **训练时长**：
    *   **数学推理**：1.5B 模型约 200 GPU 小时；7B 模型约 312 GPU 小时。
    *   **视觉推理**：3B 模型约 20 GPU 小时；7B 模型约 24 GPU 小时。
    *   **智能体任务**：3B 模型约 120 GPU 小时。
*   **分析**：论文指出，虽然需要教师模型参与，但主要的计算瓶颈在于学生模型的生成过程（占 77% 以上），教师模型的推理开销相对较小（8%-22%）。

### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多规模验证**：涵盖了 1.5B、3B、7B 等不同参数规模的学生模型。
*   **多教师对比**：使用了 7B、32B 等不同规模的教师模型，验证了方法的鲁棒性。
*   **消融实验**：分别验证了 `stop-gradient`、奖励裁剪、动态采样和多阶段训练对最终性能的贡献。
*   **测试时间扩展（Test-time Scaling）**：不仅对比了 Pass@1，还展示了在增加推理步数/样本数（Pass@K, Maj@K）时，REOPOLD 展现出比基线更优的扩展曲线。

### 6. 主要结论与发现
*   **样本效率极高**：在数学任务上，REOPOLD 达到相同精度所需的样本量仅为近期 RL 方法（如 ProRL, DeepScaleR）的 **1/6.7 至 1/12**。
*   **性能跨越**：通过 REOPOLD 蒸馏的 7B 学生模型在视觉推理任务上能**媲美 32B 的教师模型**，且推理速度提升约 3.32 倍。
*   **稳定性**：解决了传统蒸馏在 7B 等稍大模型上容易出现的训练崩溃问题，实现了持续的性能增长。
*   **推理加速**：小模型通过蒸馏获得了更简洁高效的推理链，显著降低了测试时的延迟。

### 7. 优点
*   **理论与实证结合**：将蒸馏问题转化为 RL 问题，为优化提供了清晰的数学解释。
*   **即插即用**：不需要复杂的奖励函数设计（Reward Engineering），直接利用教师模型的对数似然作为信号。
*   **解决负迁移**：通过“松弛”约束，允许学生模型在保持多样性的同时学习教师的逻辑，而非死板地模仿 token。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文做了敏感性分析，但奖励裁剪系数 $\lambda$ 和熵采样比例 $\beta$ 仍可能需要针对不同任务进行微调。
*   **教师模型依赖**：尽管开销可控，但训练过程仍需一个高性能的教师模型实时提供推理信号，这在某些极端受限的环境下可能仍显沉重。
*   **长链推理的局限**：虽然在数学和视觉上表现优异，但在极长链条的通用逻辑推理中，是否会出现累积误差仍有待进一步在大规模长文本任务上验证。

（完）
