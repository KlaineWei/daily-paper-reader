---
title: "Learning From Failures: Efficient Reinforcement Learning Control with Episodic Memory"
title_zh: 从失败中学习：基于情景记忆的高效强化学习控制
authors: Chenyang Miao
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07110v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于机器人强化学习长程探索的情境记忆
tldr: 针对强化学习在复杂动力学环境下容易因频繁失败（如碰撞、跌倒）导致训练效率低下的问题，本文提出了一种名为FEMA的失败情节记忆警示技术。该方法通过情节记忆模块显式存储失败经验，并在交互中检索相似失败案例以预警，防止机器人重复陷入不稳定状态。实验表明，FEMA能显著提升MuJoCo任务的采样效率，并在真实双足机器人任务中验证了其有效性。
selection_source: fresh_fetch
motivation: 强化学习早期训练常被短时、低回报的失败轨迹占据，阻碍了长时探索和模型收敛。
method: 提出FEMA技术，利用情节记忆存储失败经验并实时检索，引导策略避开不稳定状态以追求长时价值。
result: "在MuJoCo任务上将多种经典RL算法的采样效率提升了33.11%，并成功应用于真实双足机器人。"
conclusion: 通过显式学习和避开失败经验，FEMA能有效解决复杂控制任务中的探索难题，显著增强强化学习的训练效率。
---

## 摘要
强化学习在机器人学习领域取得了显著成功。然而，在具有挑战性的探索和复杂的接触动力学环境下，训练早期阶段经常被碰撞和跌倒等过早终止的情况所主导。结果，学习过程被短时界、低回报的轨迹所淹没，这阻碍了收敛并限制了长时界的探索。为了缓解这一问题，我们提出了一种名为“失败情景记忆警示”（Failure Episodic Memory Alert，简称 FEMA）的技术。FEMA 通过情景记忆模块显式地存储短时界的失败经验。在交互过程中，它会检索相似的失败经验，防止机器人反复陷入不稳定状态，从而引导策略走向具有更大长期价值的长时界轨迹。FEMA 可以轻松地与无模型强化学习算法结合，在多种经典强化学习算法的 MuJoCo 任务中实现了 33.11% 的显著样本效率提升。此外，将 FEMA 集成到并行化的 PPO 训练流水线中，证明了其在真实世界双足机器人任务上的有效性。

## Abstract
Reinforcement learning has achieved remarkable success in robot learning. However, under challenging exploration and contact-rich dynamics, early-stage training is frequently dominated by premature terminations such as collisions and falls. As a result, learning is overwhelmed by short-horizon, low-return trajectories, which hinder convergence and limit long-horizon exploration. To alleviate this issue, we propose a technique called Failure Episodic Memory Alert (FEMA). FEMA explicitly stores short-horizon failure experiences through an episodic memory module. During interactions, it retrieves similar failure experiences and prevents the robot from recurrently relapsing into unstable states, guiding the policy toward long-horizon trajectories with greater long-term value. FEMA can be combined easily with model-free reinforcement learning algorithms, and yields a substantial sample-efficiency improvement of 33.11% on MuJoCo tasks across several classical RL algorithms. Furthermore, integrating FEMA into a parallelized PPO training pipeline demonstrates its effectiveness on a real-world bipedal robot task.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning From Failures: Efficient Reinforcement Learning Control with Episodic Memory》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在机器人强化学习（RL）的早期训练阶段，由于高维动作空间和复杂的接触动力学，智能体（Agent）极易发生碰撞、跌倒等“过早终止”现象。
*   **核心痛点**：这些频繁的失败导致训练数据被大量短时界（Short-horizon）、低回报的轨迹淹没，阻碍了策略向长时界（Long-horizon）探索的收敛，造成采样效率极低。
*   **整体含义**：论文提出，失败轨迹并非无用，而是包含了重要的时空结构信息。通过显式记录并利用这些“痛苦的教训”，可以预警并引导智能体避开危险区域，从而提高学习效率。

### 2. 方法论：FEMA 技术框架
论文提出了 **FEMA（Failure Episodic Memory Alert，失败情景记忆警示）**，主要包含两个核心模块：

*   **失败情景记忆构建**：
    *   **数据存储**：当发生碰撞或跌倒导致任务终止时，记录最后 $K$ 个时间步的轨迹，计算其蒙特卡洛回报（MC-return）。
    *   **联合嵌入空间**：使用状态编码器 $f(s)$ 和动作编码器 $g(a)$ 将高维状态和连续动作映射到潜空间，并通过联合编码器生成语义表示 $\phi(s, a)$。
    *   **风险评估**：设计一个风险头（Risk Head），以负的归一化蒙特卡洛回报作为监督信号进行训练，从而估计当前“状态-动作对”的危险程度。
*   **风险感知动作选择机制**：
    *   **候选采样**：在交互时，从当前策略分布中采样 $N$ 个候选动作。
    *   **记忆检索**：在记忆库中检索与当前状态语义相似的过去失败案例。
    *   **评分与决策**：综合考虑候选动作与失败经验的距离（越远越好）以及风险头的预测值（越低越好）进行评分。最终选择得分最高的动作执行，从而主动避开潜在的失败状态。

### 3. 实验设计
*   **仿真场景**：使用了 MuJoCo 物理引擎中的四个经典基准任务：Humanoid（人形机器人）、Walker2d、Hopper 和 Ant。
*   **真实世界任务**：六自由度双足机器人爬楼梯任务（台阶高度 10cm）。
*   **对比基准（Benchmarks）**：
    *   **基础算法**：PPO、SAC、CrossQ。
    *   **对比方法**：Vanilla RL（原始算法）以及经典的情景控制方法 EMAC。
*   **评估指标**：平均回报、采样效率（达到特定性能所需的步数）、平均情节长度。

### 4. 资源与算力
*   **仿真训练**：在 MuJoCo 任务中使用了标准的训练流程。
*   **并行训练**：在真实机器人任务中，集成了 Isaac Gym 平台，采用了 **4096 个并行环境** 进行大规模并行 PPO 训练。
*   **具体硬件**：论文**未明确指出**具体的 GPU 型号、数量或具体的总训练时长（小时/天），但提到了并行训练的迭代次数（如 4500 次迭代）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个 MuJoCo 任务上分别结合了 3 种不同的 RL 算法进行验证。
    *   每个实验均采用了 **5 个随机种子** 以确保结果的统计显著性。
    *   进行了详细的**消融实验**，分析了相似性阈值 $\epsilon$、候选动作数 $N$、记忆更新频率 $M$ 以及 Top-O 检索机制对性能的影响。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到复杂的仿真任务，并跨越了多种 RL 范式（On-policy 和 Off-policy），最后通过真机实验验证了实用性，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **效率提升**：FEMA 在 MuJoCo 任务上将多种 RL 算法的平均采样效率提升了 **33.11%**。
*   **性能增强**：在 Humanoid 等复杂任务中，FEMA 不仅加快了收敛，还帮助算法达到了更高的最终平均回报（例如 PPO+FEMA 显著优于 Vanilla PPO）。
*   **早期引导作用**：实验证明 FEMA 能在训练初期显著增加平均情节长度，帮助智能体跳出“频繁失败”的陷阱，收集更高质量的长程数据。
*   **真机可行性**：FEMA 成功引导双足机器人在 7 秒内完成爬楼梯任务，而原始 PPO 算法在相同训练量下无法稳定完成。

### 7. 优点与亮点
*   **即插即用**：作为一种辅助模块，FEMA 可以轻松集成到现有的各种无模型 RL 算法中，无需大幅修改核心架构。
*   **变废为宝**：不同于以往关注“成功经验”的情景记忆，FEMA 专门挖掘“失败经验”的价值，这在机器人学习初期尤为高效。
*   **语义匹配**：引入学习到的联合嵌入空间进行检索，比传统的随机投影或原始空间距离度量更具语义准确性。

### 8. 不足与局限
*   **超参数敏感性**：消融实验显示，相似性阈值 $\epsilon$ 和更新频率 $M$ 对性能影响较大，不同任务可能需要精细调优。
*   **任务限制**：该方法主要针对有明确“过早终止”条件（如跌倒、碰撞）的任务，对于没有明确失败定义的连续任务可能效果有限。
*   **计算开销**：虽然论文提到了高效检索，但在超大规模记忆库下，实时采样 $N$ 个动作并进行检索对比可能会带来额外的推理延迟，文中对此计算开销的讨论较少。

（完）
