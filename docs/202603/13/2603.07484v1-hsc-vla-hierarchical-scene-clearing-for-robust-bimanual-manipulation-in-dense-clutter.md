---
title: "HSC-VLA: Hierarchical Scene-Clearing for Robust Bimanual Manipulation in Dense Clutter"
title_zh: HSC-VLA：用于密集杂乱环境下鲁棒双臂操作的分层场景清理框架
authors: "Zhen Liu, Xinyu Ning, Zhe Hu, XinXin Xie, Yitong Liu, Zhongzhu Pu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07484v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 用于长程操作任务的分层 VLA 框架
tldr: 针对视觉语言动作模型（VLA）在密集杂乱环境中因无关干扰物导致注意力分散和性能下降的问题，本文提出了HSC-VLA分层框架。该框架将高层视觉语义推理与低层运动执行解耦，通过“大脑”模块生成任务相关的场景掩码来过滤干扰，并利用基于扩散策略的“小脑”模块进行双臂操作。实验证明，该方法在复杂超市货架场景下的成功率显著优于端到端基准模型，展现了极强的鲁棒性和长程任务处理能力。
selection_source: fresh_fetch
motivation: 传统VLA模型在密集杂乱环境中难以区分任务相关目标与干扰物，导致长程操作任务的指令遵循能力大幅下降。
method: 提出一种分层架构，通过高层模块生成场景掩码实现视觉清理，并结合低层扩散策略进行基于掩码过滤的双臂操作。
result: "在密集杂乱场景下，HSC-VLA实现了86.7%的综合成功率，比表现最好的端到端基准模型π0-Full FT高出52.4%。"
conclusion: 通过显式的场景清理抽象和分层解耦，HSC-VLA有效解决了复杂环境下的表示瓶颈，显著提升了双臂机器人操作的鲁棒性。
---

## 摘要
现代视觉-语言-动作（VLA）模型在高度密集的动作环境中经常面临严重的指令遵循失败，其中与任务无关的视觉杂乱会分散注意力、破坏定位，并在复杂的长程场景中显著降低性能。为了克服单体式端到端架构的表示瓶颈，我们提出了 HSC-VLA，这是一个分层框架，通过显式的场景清理抽象，将高层视觉语义推理与低层高频感觉运动执行解耦。HSC-VLA 采用一个高层“大脑”（Brain）来分解长程任务，并生成特定于任务的场景掩码，在抑制干扰物的同时保留与任务相关的几何信息。过滤后的观测结果随后被传递给低层“小脑”（Cerebellum），这是一个基于扩散的策略，仅使用掩码过滤后的视觉和本体感受来执行双臂操作。在密集杂乱的超市货架上进行的广泛实验表明，HSC-VLA 在高密度杂乱环境下实现了 86.7% 的综合成功率，比表现最好的单体基线模型（π0-Full FT，34.3%）高出 52.4%。HSC-VLA 还表现出强大的长程性能，在杂物分类上达到 72%，在补货任务上达到 66%，证明了在复杂杂乱操作中强大的鲁棒性和有效的故障恢复能力。

## Abstract
Modern Vision--Language--Action models often suffer from critical instruction-following failures in high-density manipulation environments, where task-irrelevant visual clutter dilutes attention, corrupts grounding, and substantially degrades performance in complex long-horizon scenarios. To overcome the representation bottleneck of monolithic end-to-end architectures, we propose HSC-VLA, a hierarchical framework that decouples high-level visual-semantic reasoning from low-level, high-frequency sensorimotor execution through an explicit scene-clearing abstraction. HSC-VLA employs a high-level Brain to decompose long-horizon tasks and to generate task-specific scene masks that preserve task-relevant geometry while suppressing distractors. The filtered observations are then passed to a low-level Cerebellum, a diffusion-based policy that performs bimanual manipulation using only mask-filtered vision and proprioception. Extensive experiments in densely cluttered supermarket shelves demonstrate that HSC-VLA achieves 86.7\% aggregate success under high-density clutter, surpassing the best monolithic baseline ($π_0$-Full FT at 34.3\%) by 52.4\%. HSC-VLA also exhibits strong long-horizon performance, reaching 72\% on clutter sorting and 66\% on restocking, demonstrating strong robustness and effective failure recovery in complex cluttered manipulation.

---

## 论文详细总结（自动生成）

### HSC-VLA：用于密集杂乱环境下鲁棒双臂操作的分层场景清理框架

#### 1. 核心问题与研究动机
*   **核心问题**：现有的单体式（Monolithic）视觉-语言-动作（VLA）模型在密集杂乱环境（如超市货架）中表现不佳。
*   **研究背景**：在物体高度堆叠、遮挡严重的场景下，无关的视觉干扰会分散模型的注意力（Attention Dilution），导致目标定位失效。此外，单体架构难以平衡高层语义推理与低层高频运动控制，在长程任务（Long-horizon tasks）中容易出现因果混淆和错误累积。

#### 2. 方法论
HSC-VLA 提出了一个分层架构，通过“场景清理”抽象层将感知与动作解耦：
*   **高层“大脑”（Brain）**：采用预训练的视觉语言模型（如 Qwen-v1-235B），负责将长程指令分解为子目标序列，并识别场景中的干扰物，生成任务无关区域的边界框（Bounding Boxes）。
*   **几何感知过滤**：利用分割模型（SAM/Cutie）将边界框转化为像素级掩码（Mask）。通过公式 $\hat{I}_t = I_t \odot (1 - Q_t)$ 过滤原始图像，仅保留与当前任务相关的几何结构，抑制背景噪声。
*   **低层“小脑”（Cerebellum）**：一个基于扩散策略（Diffusion Policy）的执行器。它接收过滤后的图像、机器人本体感受状态和子目标嵌入，生成平滑的动作块（Action Chunk）。
*   **感知-动作一致性协议**：在离线训练和在线推理中使用完全相同的掩码过滤逻辑，确保模型在部署时不会因视觉分布偏移而失效。
*   **验证与重规划**：引入闭环验证机制，在每个动作块执行后检查子目标是否完成，若失败则触发重试或重新规划。

#### 3. 实验设计
*   **实验场景**：
    *   **真实世界**：使用 Inspire-Omni O1 双臂机器人，模拟超市货架环境（每层 5-20 个 SKU），涵盖高低两种杂乱密度。
    *   **仿真环境**：在 RoboTwin 2.0 物理仿真平台上进行原子技能基准测试。
*   **任务类型**：抓取（Grasp）、放置（Place）、双臂协同（Bimanual）、杂物分类（Sorting）及补货（Restocking）。
*   **对比基准（Baselines）**：包括 ACT、Diffusion Policy (DP)、DP3、RDT-1B 以及不同微调模式下的 $\pi_0$（LoRA 和 Full FT）。

#### 4. 资源与算力
*   **训练细节**：模型训练了 50,000 个梯度步（Steps），Batch Size 为 32。使用 AdamW 优化器，学习率从 $5 \times 10^{-5}$ 线性预热后余弦退火至 $2.5 \times 10^{-6}$。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量，但提到高层规划器使用了 235B 参数规模的大模型（推理阶段冻结）。

#### 5. 实验数量与充分性
*   **数据规模**：通过动觉教导采集了 2,100 条专家轨迹，涵盖单臂和双臂操作。
*   **实验组数**：
    *   在不同密度下进行了数百次真实机器人测试（SR@300, SR@200 等）。
    *   进行了针对“场景清理策略”的消融实验（无掩码 vs 静态掩码 vs 动态清理）。
    *   针对长程任务（步骤 $\ge 3$ 或 $5$）进行了专门的鲁棒性评估。
*   **充分性评价**：实验设计较为全面，涵盖了从基础技能到复杂长程任务的演进，且对比了当前最前沿的 VLA 模型，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能飞跃**：在高密度杂乱场景下，HSC-VLA 的综合成功率达到 **86.7%**，而表现最好的单体基线 $\pi_0$ 仅为 34.3%，提升了 **52.4%**。
*   **长程鲁棒性**：在补货任务中，HSC-VLA 成功率达 66%，远超基线的 14%，证明了分层架构在减少错误累积方面的有效性。
*   **动态清理的必要性**：消融实验显示，动态更新掩码对于处理物体移动后的长程任务至关重要，静态掩码在复杂任务中表现极差。

#### 7. 优点
*   **解耦设计**：通过显式的场景清理解决了 VLA 模型在复杂视觉环境下的表示瓶颈。
*   **感知一致性**：训练与推理阶段视觉预处理的高度一致，显著增强了模型的零样本鲁棒性。
*   **双臂协同**：在需要高精度配合的双臂任务中表现出极高的稳定性（高密度下成功率达 97%）。

#### 8. 不足与局限
*   **依赖性风险**：系统性能高度依赖于高层 VLM 对干扰物定位的准确性以及分割模型的质量，一旦掩码初始化失败，后续执行将受阻。
*   **计算延迟**：动态掩码更新和高层 VLM 推理引入了额外的计算开销，可能限制机器人在极高频交互场景下的表现。
*   **技能库限制**：目前的低层策略受限于预定义的工具库（Tool Library），对于需要全新物理交互模式的任务，泛化能力可能受限。

（完）
