---
title: "NS-VLA: Towards Neuro-Symbolic Vision-Language-Action Models"
title_zh: NS-VLA：迈向神经符号视觉-语言-动作模型
authors: "Ziyue Zhu, Shangyang Wu, Shuai Zhao, Zhiqiu Zhao, Shengjie Li, Yi Wang, Fang Li, Haoran Luo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09542v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 通过在线强化学习优化的神经符号 VLA 模型
tldr: 针对视觉-语言-动作（VLA）模型在数据依赖、架构复杂及探索受限等方面的挑战，本文提出了NS-VLA框架。该框架结合了神经符号学习与在线强化学习，通过符号编码器提取结构化原语，利用符号求解器进行高效动作序列化，并借助强化学习优化生成过程。实验证明，NS-VLA在单样本训练、抗干扰能力及零样本泛化方面均优于现有方法，显著提升了数据效率和探索空间。
selection_source: fresh_fetch
motivation: 旨在解决现有VLA模型在可重用原语学习、大规模数据依赖以及探索能力不足等方面存在的局限性。
method: 提出一种结合符号编码器、符号求解器与在线强化学习的神经符号框架，实现结构化特征提取与高效动作规划。
result: 在机器人操作基准测试中，该方法在单样本学习、数据扰动环境及零样本泛化任务上均表现出超越前人的性能。
conclusion: NS-VLA通过神经符号与强化学习的结合，为构建高效、泛化能力强且具备自主探索能力的机器人控制模型提供了新路径。
---

## 摘要
视觉-语言-动作（VLA）模型旨在将指令与视觉上下文相结合，并为机器人操纵生成动作序列。尽管近期取得了进展，但 VLA 模型在学习相关且可重用的原语、减少对大规模数据和复杂架构的依赖，以及实现超越演示的探索方面仍面临挑战。为了应对这些挑战，我们提出了一种通过在线强化学习（RL）实现的创新神经符号视觉-语言-动作（NS-VLA）框架。该框架引入了一个符号编码器来嵌入视觉和语言特征并提取结构化原语，利用符号求解器进行高效的数据动作排序，并利用在线强化学习通过广泛的探索来优化生成。在机器人操纵基准测试上的实验表明，NS-VLA 在单样本训练和数据扰动设置下均优于以往的方法，同时展现出卓越的零样本泛化能力、高数据效率和扩展的探索空间。我们的代码已开源。

## Abstract
Vision-Language-Action (VLA) models are formulated to ground instructions in visual context and generate action sequences for robotic manipulation. Despite recent progress, VLA models still face challenges in learning related and reusable primitives, reducing reliance on large-scale data and complex architectures, and enabling exploration beyond demonstrations. To address these challenges, we propose a novel Neuro-Symbolic Vision-Language-Action (NS-VLA) framework via online reinforcement learning (RL). It introduces a symbolic encoder to embedding vision and language features and extract structured primitives, utilizes a symbolic solver for data-efficient action sequencing, and leverages online RL to optimize generation via expansive exploration. Experiments on robotic manipulation benchmarks demonstrate that NS-VLA outperforms previous methods in both one-shot training and data-perturbed settings, while simultaneously exhibiting superior zero-shot generalizability, high data efficiency and expanded exploration space. Our code is available.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **NS-VLA** 的新型神经符号视觉-语言-动作模型，旨在通过结合符号推理与在线强化学习，解决现有 VLA 模型在数据效率、泛化能力和环境探索方面的局限性。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA）主要依赖端到端的深度学习架构，面临三大挑战：
    1.  **缺乏结构化意识**：难以捕捉机器人操作中可重用的“原语”（Primitives）逻辑，导致泛化性差。
    2.  **数据依赖性强**：需要海量专家演示数据，且模型架构极其复杂。
    3.  **探索能力受限**：大多基于离线模仿学习，限制了机器人在动态环境中超越演示轨迹的自主探索能力。
*   **核心目标**：构建一个既具备神经感知能力，又具备符号逻辑推理能力的框架，通过在线强化学习（RL）提升机器人的操作效率和鲁棒性。

### 2. 方法论：核心思想与关键技术
NS-VLA 框架由三个紧密耦合的部分组成：
*   **神经符号编码与嵌入（Neuro-Symbolic Encoding）**：
    *   利用预训练 VLM（如 Qwen3-VL）提取视觉-语言特征。
    *   **计划生成**：将自然语言指令转化为结构化的原语序列（如 `pick` -> `place_on`）。
    *   **符号分类器**：预测当前正在执行的原语，并引入“单调约束指针”，确保任务按逻辑顺序推进，防止状态切换时的闪烁。
*   **符号求解器（Symbolic Solver）**：
    *   **视觉 Token 稀疏化**：基于当前原语作为查询（Query），通过 Top-K 注意力机制过滤掉无关的背景视觉信息，仅保留关键目标特征。
    *   **动作生成器**：使用轻量化 Causal Transformer 接收稀疏视觉特征和符号状态，生成连续的动作块（Action Chunks）。
*   **在线强化学习优化（Online RL Optimization）**：
    *   **JGRPO 算法**：改进自 DeepSeek 的 GRPO 算法，引入 KL 散度正则化，防止在线优化时偏离行为克隆（BC）的参考策略。
    *   **原语分段奖励设计**：结合任务成功奖励、原语完成里程碑奖励以及基于潜在空间原型的进度塑造奖励（Progress Shaping）。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：用于基础多任务操作评估。
    *   **LIBERO-Plus**：引入光照、纹理、布局等 7 维扰动，测试鲁棒性。
    *   **CALVIN**：测试长程任务（Long-horizon）和零样本（Zero-shot）泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 **OpenVLA**、**$\pi_0$**、**UniVLA**、**VLA-Adapter**、**ACT**、**DP** 等 10 余种主流 VLA 和模仿学习模型。
    *   设置了 **1-shot（每个任务仅 1 条演示）** 和 **全量数据** 两种训练模式。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **模型规模**：提到了使用了不同参数量的 Qwen3-VL 作为骨干网络（2B、4B、8B），并指出 NS-VLA 在 2B 规模下即可达到极高性能。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   三大基准数据集的主实验。
    *   针对数据效率的 1-shot 专项实验。
    *   针对环境扰动的鲁棒性分析。
    *   跨数据集（CALVIN ABC -> D）的零样本泛化实验。
    *   **消融实验**：分别验证了原语分类器、视觉提取器、动作生成器和 RL 模块的必要性。
    *   **探索空间分析**：可视化对比了在线 RL 与扩散模型、流匹配方法的动作覆盖范围。
*   **充分性评价**：实验设计非常全面且客观，通过多维度对比证明了神经符号架构在低数据量下的绝对优势。

### 6. 主要结论与发现
*   **卓越的数据效率**：在 1-shot 设置下，NS-VLA 的性能下降远小于基准模型，证明符号先验提供了强大的归纳偏置。
*   **极强的鲁棒性**：在面对视觉和空间扰动时，NS-VLA 保持了最高的成功率，说明其捕捉的是任务的本质关联而非背景噪声。
*   **更广的探索空间**：在线 RL 使得模型能够探索出比专家演示更优或更稳健的轨迹分布。
*   **零样本能力**：在 CALVIN 任务中，NS-VLA 在 5 连任务成功率上显著超越了现有 SOTA 模型。

### 7. 优点与亮点
*   **架构创新**：成功将神经符号（Neuro-Symbolic）思想引入 VLA，解决了端到端模型“黑盒”且难以解释的问题。
*   **计算高效**：通过视觉 Token 稀疏化和动作块生成，显著降低了推理时的计算冗余。
*   **训练稳定**：提出的 JGRPO 算法结合 KL 锚定，解决了在线强化学习在稀疏奖励环境下容易崩溃的问题。

### 8. 不足与局限
*   **原语定义依赖**：目前仍需要预定义原语库（如 pick, place），对于完全未知的复杂动作可能需要手动扩展原语。
*   **Sim-to-Real 验证缺失**：实验全部在仿真环境中完成，尚未在真实物理机器人上进行大规模验证。
*   **算力细节不明**：缺乏具体的训练资源消耗数据，难以评估其在实际部署时的算力成本。

（完）
