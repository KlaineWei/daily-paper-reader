---
title: "Ada-RS: Adaptive Rejection Sampling for Selective Thinking"
title_zh: Ada-RS：用于选择性思考的自适应拒绝采样
authors: "Yirou Ge, Yixi Li, Alec Chiu, Shivani Shekhar, Zijie Pan, Avinash Thangali, Yun-Shiuan Chuang, Chaitanya Kulkarni, Uma Kona, Linsey Pang, Prakhar Mehrotra"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19519v1"
tags: ["query:slm-rl"]
score: 8.0
evidence: 针对8B模型的自适应拒绝采样以实现高效推理
tldr: "本研究针对大语言模型在处理简单请求时因过度使用思维链（CoT）而导致 Token 浪费的问题，提出了 Ada-RS（自适应拒绝采样）框架。该框架通过自适应长度惩罚奖励对多个采样结果评分，并利用随机拒绝采样筛选出高效且准确的样本，用于 DPO 或 DAPO 等下游优化。实验表明，在工具调用场景下，Ada-RS 在保持准确率的同时，最高可减少 80% 的 Token 输出和 95% 的冗余思考，显著优化了推理效率与性能的平衡。"
selection_source: carryover_cache
motivation: 解决大语言模型在简单任务中盲目使用思维链导致的计算资源浪费和推理延迟问题。
method: 引入一种与算法无关的自适应拒绝采样框架，通过长度惩罚奖励机制筛选出兼顾简洁与准确的训练样本。
result: 在 Qwen3-8B 模型上的测试显示，该方法在维持工具调用准确率的前提下，大幅降低了 Token 消耗和思考频率。
conclusion: 证明了通过优化训练信号的选择，可以有效实现大语言模型的“选择性思考”，从而提升其在实际部署中的效率。
---

## 摘要
大语言模型（LLMs）正越来越多地部署在对成本和延迟敏感的场景中。虽然思维链（CoT）能提升推理能力，但在处理简单请求时会浪费 Token。我们研究了使用工具的 LLMs 的选择性思考，并引入了自适应拒绝采样（Ada-RS），这是一种与算法无关的样本过滤框架，用于学习选择性且高效的推理。对于每个给定的上下文，Ada-RS 使用自适应长度惩罚奖励对多个采样补全进行评分，然后应用随机拒绝采样，仅保留高奖励候选样本（或偏好对）用于下游优化。我们展示了 Ada-RS 如何接入偏好对（如 DPO）或分组策略优化策略（如 DAPO）。在一个面向工具调用的合成电子商务基准测试中，使用带有 LoRA 的 Qwen3-8B，Ada-RS 通过减少高达 80% 的平均输出 Token 和高达 95% 的思考率，在保持或提高工具调用准确率的同时，改善了标准算法的准确率-效率前沿。这些结果表明，训练信号选择是延迟敏感型部署中实现高效推理的有力杠杆。

## Abstract
Large language models (LLMs) are increasingly being deployed in cost and latency-sensitive settings. While chain-of-thought improves reasoning, it can waste tokens on simple requests. We study selective thinking for tool-using LLMs and introduce Adaptive Rejection Sampling (Ada-RS), an algorithm-agnostic sample filtering framework for learning selective and efficient reasoning. For each given context, Ada-RS scores multiple sampled completions with an adaptive length-penalized reward then applies stochastic rejection sampling to retain only high-reward candidates (or preference pairs) for downstream optimization. We demonstrate how Ada-RS plugs into both preference pair (e.g. DPO) or grouped policy optimization strategies (e.g. DAPO). Using Qwen3-8B with LoRA on a synthetic tool call-oriented e-commerce benchmark, Ada-RS improves the accuracy-efficiency frontier over standard algorithms by reducing average output tokens by up to 80% and reducing thinking rate by up to 95% while maintaining or improving tool call accuracy. These results highlight that training-signal selection is a powerful lever for efficient reasoning in latency-sensitive deployments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Ada-RS: Adaptive Rejection Sampling for Selective Thinking》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在处理复杂任务时通常依赖思维链（CoT）推理，但在实际部署中，CoT 会产生大量的 Token 消耗和推理延迟。对于许多简单或常规的请求（如闲聊、简单查询），强制性的推理过程不仅浪费计算资源，还会降低用户体验。
论文的核心问题是：**如何让模型学会“选择性思考”**，即在保证准确率的前提下，仅在处理复杂任务时进行推理，而在简单任务中直接给出答案。研究背景聚焦于对成本和延迟极度敏感的工业级应用场景（如电子商务助手）。

### 2. 论文提出的方法论：Ada-RS
论文提出了一种名为 **Ada-RS（自适应拒绝采样）** 的框架，其核心思想是通过优化“训练信号的选择”来引导模型实现高效推理。

*   **核心思想**：在训练阶段，对模型生成的多个候选样本进行筛选，优先保留那些“既准确又简洁”的样本，并根据任务难度动态调整对冗余思考的惩罚力度。
*   **关键技术细节**：
    *   **自适应长度惩罚（ALP）奖励**：定义奖励函数 $r = \text{正确性} - \alpha \cdot s_K(x) \cdot |t|$。其中 $s_K(x)$ 是当前模型对该问题的在线解决率估计。如果问题很容易（解决率高），则加大对推理长度 $|t|$ 的惩罚；如果问题很难，则减小惩罚，允许模型进行长推理。
    *   **拒绝采样机制**：
        *   **成对拒绝采样（用于 DPO）**：在候选对中，根据奖励差值概率性地选择样本构建偏好对。
        *   **组内拒绝采样（用于 DAPO）**：在候选组中，根据样本奖励相对于组内均值的标准分进行筛选。
    *   **算法流程**：Ada-RS 作为一个插件，可以集成到 **DPO（离线偏好优化）** 或 **DAPO（在线分组策略优化）** 中。它先采样，再通过 ALP 评分，最后通过拒绝采样过滤出高质量信号进行参数更新。

### 3. 实验设计
*   **数据集/场景**：使用了一个合成的、面向工具调用的电子商务数据集。该数据集模拟了多轮对话和多步工具调用（如产品搜索、账户查询、交易查询），任务分布参考了 $\tau^2$-Bench 零售基准。
*   **Benchmark**：包含 121 个任务、8 种用户角色，训练集约 1.5 万次工具调用，评估集约 2500 次工具调用。
*   **对比方法**：
    *   **NFT (No-Fine-Tuning)**：基础模型在“始终思考”和“禁止思考”提示词下的表现。
    *   **SFT (监督微调)**：在混合了思考和非思考数据的语料上微调。
    *   **标准 DPO/DAPO**：不含 Ada-RS 机制的偏好优化。
    *   **消融版本**：测试了 NLL（负对数似然）辅助损失、不同惩罚系数 $\alpha$ 和采样温度 $\beta_{rs}$ 的影响。

### 4. 资源与算力
*   **模型基础**：使用 **Qwen3-8B** 作为基础推理模型。
*   **微调技术**：采用 **LoRA** 适配器（参数设置：$r=32, \alpha=32$）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。文中提到了优化器使用 Adam，学习率在 $5 \times 10^{-6}$ 到 $5 \times 10^{-4}$ 之间，并使用了 0.03 的预热比例。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的对比，包括准确率、输出 Token 长度和思考率（Thinking Rate）。
*   **消融实验**：针对 $\beta_{rs}$（采样选择性）和 $\alpha$（长度惩罚强度）进行了详细的超参数扫参，并验证了 NLL 损失在稳定训练中的必要性。
*   **客观性与公平性**：实验涵盖了离线（DPO）和在线（DAPO）两种主流强化学习范式，证明了 Ada-RS 的通用性。虽然仅在一个特定领域（电商）进行，但实验设计逻辑严密，对比基准全面。

### 6. 主要结论与发现
*   **效率大幅提升**：相比于标准算法，Ada-RS 将平均输出 Token 减少了约 **70%-80%**，思考频率降低了高达 **95%**。
*   **性能保持**：在大幅缩减推理长度的同时，工具调用的准确率保持不变甚至有所提升，成功实现了准确率与效率的帕累托优化。
*   **训练信号的重要性**：实验证明，仅仅依靠奖励函数设计是不够的，通过拒绝采样过滤掉“不具信息量”或“过度冗余”的训练样本是实现选择性思考的关键。
*   **稳定性**：引入 NLL 辅助损失对于防止模型在优化效率时出现性能崩溃（退化为从不思考）至关重要。

### 7. 优点
*   **算法无关性**：Ada-RS 作为一个样本过滤框架，可以轻松插入现有的各种偏好优化算法中。
*   **自适应性**：通过在线估计任务难度来动态调整惩罚，避免了“一刀切”导致的简单任务太啰嗦或复杂任务思考不足的问题。
*   **工业实用性**：直接针对延迟和成本这两个大模型落地的核心痛点，具有很强的实战参考价值。

### 8. 不足与局限
*   **领域局限性**：实验仅在合成的电子商务工具调用场景下完成，尚未在更广泛的通用推理任务（如数学、代码）或更大规模的模型（如 70B+）上验证。
*   **评估指标单一**：主要关注单步工具调用的准确率，未充分评估多轮对话中用户最终目标的达成率（Goal Completion）。
*   **数据依赖**：效果高度依赖于初始采样候选集的质量，如果教师模型或初始策略无法生成高质量的“简洁且正确”的样本，拒绝采样的效果会受限。

（完）
