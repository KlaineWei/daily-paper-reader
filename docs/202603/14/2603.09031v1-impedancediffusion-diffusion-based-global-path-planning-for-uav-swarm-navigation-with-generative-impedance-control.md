---
title: "ImpedanceDiffusion: Diffusion-Based Global Path Planning for UAV Swarm Navigation with Generative Impedance Control"
title_zh: ImpedanceDiffusion：基于扩散模型的无人机集群导航全局路径规划与生成式阻抗控制
authors: "Faryal Batool, Yasheerah Yaqoot, Muhammad Ahsan Mustafa, Roohan Ahmed Khan, Aleksey Fedoseev, Dzmitry Tsetserukou"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09031v1"
tags: ["query:slm-rl"]
score: 8.0
evidence: 结合VLM的无人机长程路径规划分层框架
tldr: 针对复杂室内环境下的无人机集群导航，本文提出 ImpedanceDiffusion 框架。该框架结合了基于图像条件的扩散模型进行全局路径规划、人工势场法（APF）进行轨迹跟踪，以及利用 VLM-RAG 进行语义感知的变阻抗控制。通过扩散模型直接从 RGB 图像生成路径，并根据障碍物属性动态调整阻抗参数，实现了在动态复杂环境中的高效、安全避障与集群协同，并在真实无人机平台上验证了其鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决复杂室内环境下无人机集群导航中长时程规划、反应式避障与自适应顺应性难以兼顾的问题。
method: 提出一种分层框架，利用扩散模型从图像生成全局路径，结合 APF 跟踪层和基于 VLM-RAG 的语义变阻抗控制。
result: "在 20 种实验配置下实现了 100% 的轨迹生成率和 92% 的导航成功率，并成功完成零样本从仿真到现实的部署。"
conclusion: 该框架通过结合生成式规划与语义感知控制，为复杂环境下的无人机集群提供了可靠且具备自适应能力的导航方案。
---

## 摘要
在杂乱的室内环境中实现安全的集群导航需要长时程规划、反应式避障和自适应顺应性。我们提出了 ImpedanceDiffusion，这是一个分层框架，它利用以图像为条件的基于扩散模型的全局路径规划，并结合人工势场（APF）跟踪和语义感知的变阻抗控制，用于无人机集群。该扩散模型直接从 RGB 图像生成几何全局轨迹，无需显式构建地图。这些轨迹由基于 APF 的反应层进行跟踪，同时 VLM-RAG 模块以 90% 的检索准确率执行语义障碍物分类，以便在执行过程中针对混合障碍物环境调整阻抗参数。本文评估了两种扩散规划器：(i) 使用单次推理的顶视图长时程规划器，以及 (ii) 通过两阶段推理流水线部署的第一人称视角（FPV）短时程规划器。两种规划器在 20 种静态和动态实验配置中均实现了 100% 的轨迹生成率，并通过分层 APF-阻抗控制栈在 Crazyflie 2.1 无人机上完成了零样本（zero-shot）从仿真到现实（sim-to-real）的部署验证。顶视图规划器产生的轨迹更平滑，在硬障碍物附近的跟踪速度较为保守（1.0-1.2 m/s），在软障碍物附近为 0.6-1.0 m/s。相比之下，FPV 规划器生成的轨迹具有更大的局部间隙和通常更高的速度，在硬障碍物附近达到 1.4-2.0 m/s，在软障碍物附近最高可达 1.6 m/s。在 20 种实验配置（共 100 次运行）中，该框架实现了 92% 的成功率，同时保持了稳定的基于阻抗的编队控制，振荡有界且无飞行碰撞，证明了在杂乱室内环境中可靠且自适应的集群导航能力。

## Abstract
Safe swarm navigation in cluttered indoor environment requires long-horizon planning, reactive obstacle avoidance, and adaptive compliance. We propose ImpedanceDiffusion, a hierarchical framework that leverages image-conditioned diffusion-based global path planning with Artificial Potential Field (APF) tracking and semantic-aware variable impedance control for aerial drone swarms.   The diffusion model generates geometric global trajectories directly from RGB images without explicit map construction. These trajectories are tracked by an APF-based reactive layer, while a VLM-RAG module performs semantic obstacle classification with 90% retrieval accuracy to adapt impedance parameters for mixed obstacle environments during execution.   Two diffusion planners are evaluated: (i) a top-view long-horizon planner using single-pass inference and (ii) a first-person-view (FPV) short-horizon planner deployed via a two-stage inference pipeline. Both planners achieve a 100% trajectory generation rate across twenty static and dynamic experimental configurations and are validated via zero-shot sim-to-real deployment on Crazyflie 2.1 drones through the hierarchical APF-impedance control stack. The top-view planner produces smoother trajectories that yield conservative tracking speeds of 1.0-1.2 m/s near hard obstacles and 0.6-1.0 m/s near soft obstacles. In contrast, the FPV planner generates trajectories with greater local clearance and typically higher speeds, reaching 1.4-2.0 m/s near hard obstacles and up to 1.6 m/s near soft obstacles. Across 20 experimental configurations (100 total runs), the framework achieved a 92% success rate while maintaining stable impedance-based formation control with bounded oscillations and no in-flight collisions, demonstrating reliable and adaptive swarm navigation in cluttered indoor environments.

---

## 论文详细总结（自动生成）

### ImpedanceDiffusion：基于扩散模型的无人机集群导航与生成式阻抗控制论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在杂乱的室内环境（如仓库、实验室）中，无人机（UAV）集群导航面临长时程规划、实时避障和物理交互顺应性的多重挑战。传统导航流水线高度依赖显式的几何地图（如占据网格地图）和复杂的感知融合，在传感器受限或部分可观测的环境下容易失效。
**核心问题**：能否直接从单张 RGB 图像推断出安全的全局飞行轨迹，并根据障碍物的语义属性（如人是“软”的，柱子是“硬”的）动态调整集群的交互行为？
**研究意义**：该研究旨在结合生成式 AI（扩散模型、VLM）与经典控制理论（APF、阻抗控制），实现一种无需显式建图、具备语义感知能力的自适应集群导航框架。

#### 2. 方法论：核心思想与关键技术
该框架采用**分层控制架构**，主要包含四个核心模块：
*   **扩散模型全局规划器**：
    *   **核心思想**：利用条件 UNet 扩散模型，将 RGB 图像、起点和终点作为输入，通过迭代去噪生成像素空间的轨迹掩码。
    *   **技术细节**：采用平方余弦噪声调度（Squared-cosine schedule）进行前向加噪，通过 DDPM 后验进行反向去噪。损失函数结合了轨迹重建损失（$L_{path}$）和端点准确性损失（$L_{endpoint}$）。
*   **VLM-RAG 语义推理**：
    *   使用视觉语言模型（Molmo-7B-O）识别图像中的障碍物类别。
    *   通过检索增强生成（RAG）技术，从预构建的向量数据库（FAISS）中检索经过实验验证的阻抗参数（质量 $m$、刚度 $k$、阻尼 $d$）。
*   **人工势场（APF）局部跟踪**：
    *   作为反应层，负责跟踪扩散模型生成的全局路径，并提供实时的排斥力以应对局部障碍物。
*   **生成式变阻抗控制**：
    *   **无人机-无人机编队**：通过虚拟质量-弹簧-阻尼系统维持集群构型。
    *   **无人机-障碍物交互**：根据 VLM 识别的类型调整参数。例如，靠近人类（软障碍物）时降低刚度、增加阻尼和偏转距离，以实现更具顺应性的避障。

#### 3. 实验设计
*   **数据集**：
    *   **P1（顶视图规划器）**：在 ProcTHOR 环境中生成 10,000 条 A* 轨迹进行训练。
    *   **P2（FPV 规划器）**：在模拟室内环境（VLN-Go2-Matterport）中生成 13,000 条轨迹。
*   **实验场景**：涵盖 20 种配置，包括静态/动态障碍物、硬障碍物（圆柱、椅子、门）和软障碍物（人类）的混合环境。
*   **对比方法**：对比了两种不同视觉模态训练的扩散规划器：
    1.  **Diffusion Planner 1 (P1)**：基于顶视图，单次推理生成长程路径。
    2.  **Diffusion Planner 2 (P2)**：基于第一人称视角（FPV），采用两阶段推理生成路径。
*   **硬件平台**：使用 Crazyflie 2.1 微型无人机集群进行实地验证。

#### 4. 资源与算力
*   **计算平台**：配备 NVIDIA RTX 4090 GPU (24GB VRAM) 和 Intel Core i9-13900K 处理器的计算工作站。
*   **模型优化**：为了在显存限制下运行，VLM 采用了 Molmo-7B-O 的 4-bit 量化版本（BnB 4-bit）。
*   **训练细节**：模型在仿真环境中训练 30 个 epoch，每个模型使用 100 个扩散步长。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 20 种实验配置，每种配置重复 5 次，总计 **100 次飞行运行**。
*   **充分性评价**：实验设计较为充分，涵盖了从仿真到现实的零样本迁移（Zero-shot Sim-to-Real）。通过对比路径长度、碰撞率、目标误差和总转角等量化指标，客观地评估了两种规划器的优劣。同时，引入了动态人类障碍物，增强了实验的复杂度和现实意义。

#### 6. 主要结论与发现
*   **成功率**：框架实现了 **92% 的导航成功率**，失败主要源于硬件通信而非算法本身。
*   **生成能力**：两种扩散规划器在所有场景中均实现了 **100% 的轨迹生成率**。
*   **语义感知**：VLM-RAG 模块的障碍物分类与参数检索准确率达到 **90%**。
*   **规划器差异**：
    *   **P1（顶视图）**：生成的路径更平滑（转角更小），推理速度更快（1.4-2.5s），但在障碍物附近的间隙较小。
    *   **P2（FPV）**：虽然推理较慢（2.5-3.4s），但具备更好的局部避障间隙（碰撞率更低，0.246 vs 0.348）。
*   **阻抗表现**：成功实现了根据障碍物类型动态切换阻抗，软障碍物诱发了更大的偏转和更低的速度，确保了交互安全性。

#### 7. 优点（亮点）
*   **无需地图**：摆脱了对显式几何地图和复杂 SLAM 系统的依赖，直接从视觉生成路径。
*   **语义与物理结合**：首次将 VLM 的语义推理与底层的阻抗控制参数直接耦合，使集群具备了“理解”环境并调整物理顺应性的能力。
*   **鲁棒性**：通过 APF 补偿了扩散模型生成路径的微小几何误差，确保了实机飞行的安全性。

#### 8. 不足与局限
*   **计算延迟**：扩散模型和 VLM 的推理目前依赖于高性能工作站，尚未实现机载实时计算，这限制了其在完全去中心化任务中的应用。
*   **离散切换**：目前的阻抗调节是基于类别的离散切换，而非连续的策略学习，在极度复杂的混合场景下可能不够平滑。
*   **环境光照依赖**：VLM 和扩散模型均基于 RGB 图像，在极端光照或低对比度环境下的鲁棒性有待进一步验证。

（完）
