---
title: "ResWM: Residual-Action World Model for Visual RL"
title_zh: ResWM：面向视觉强化学习的残差动作世界模型
authors: "Jseen Zhang, Gabriel Adineera, Jinzhou Tan, Jinoh Kim"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11110v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于稳定长程视觉控制的基于模型的强化学习框架
tldr: 本研究针对视觉强化学习中绝对动作建模导致优化不稳定和控制效率低的问题，提出了残差动作世界模型（ResWM）。该模型将控制变量从绝对动作转变为相对于前一步的增量调整，并引入观测差异编码器来捕捉相邻帧的变化。ResWM 在 Dreamer 框架基础上实现了更平滑的探索和更可靠的规划。实验表明，该方法在 DeepMind Control Suite 上显著提升了采样效率和控制平滑度，为机器人系统的实际部署提供了更稳定、节能的动作轨迹。
selection_source: fresh_fetch
motivation: 传统的基于绝对动作的世界模型在处理连续控制任务时，容易导致优化不稳定、控制震荡且搜索空间过大。
method: 提出 ResWM 框架，通过残差动作建模和观测差异编码器，将控制和动力学学习聚焦于状态与动作的增量变化。
result: 在 DeepMind Control Suite 上的实验证明，ResWM 在采样效率、最终回报和动作平滑度方面均优于 Dreamer 和 TD-MPC 等强基准。
conclusion: 残差动作建模是提升视觉强化学习算法在机器人领域实用性和稳定性的简单且强大的原则。
---

## 摘要
从原始视觉观测中学习预测性世界模型是强化学习（RL）中的一个核心挑战，特别是在机器人学和连续控制领域。传统的基于模型的强化学习框架直接将未来预测建立在绝对动作（absolute actions）之上，这导致优化过程不稳定：最优动作分布依赖于具体任务且先验未知，往往会导致振荡或低效的控制。为解决这一问题，我们引入了残差动作世界模型（ResWM），这是一个将控制变量从绝对动作重新表述为残差动作（即相对于前一步的增量调整）的新框架。这一设计符合现实世界控制中固有的平滑性，缩小了有效搜索空间，并稳定了长时程规划。为了进一步增强表示能力，我们提出了一个观测差异编码器（Observation Difference Encoder），显式地对相邻帧之间的变化进行建模，从而产生与残差动作自然耦合的紧凑潜空间动力学。ResWM 以极小的修改且无需额外超参数的方式集成到 Dreamer 风格的潜空间动力学模型中。想象回滚（imagination rollouts）和策略优化均在残差动作空间中进行，从而实现了更平滑的探索、更低的控制方差以及更可靠的规划。在 DeepMind Control Suite 上的实验结果表明，ResWM 在样本效率、渐近收益和控制平滑度方面取得了持续的提升，显著超越了 Dreamer 和 TD-MPC 等强基准模型。除了性能提升外，ResWM 还能产生更稳定且更节能的动作轨迹，这一特性对于部署在现实环境中的机器人系统至关重要。这些发现表明，残差动作建模为弥合强化学习算法进展与机器人实际需求之间的鸿沟提供了一个简单而强大的原则。

## Abstract
Learning predictive world models from raw visual observations is a central challenge in reinforcement learning (RL), especially for robotics and continuous control. Conventional model-based RL frameworks directly condition future predictions on absolute actions, which makes optimization unstable: the optimal action distributions are task-dependent, unknown a priori, and often lead to oscillatory or inefficient control. To address this, we introduce the Residual-Action World Model (ResWM), a new framework that reformulates the control variable from absolute actions to residual actions -- incremental adjustments relative to the previous step. This design aligns with the inherent smoothness of real-world control, reduces the effective search space, and stabilizes long-horizon planning. To further strengthen the representation, we propose an Observation Difference Encoder that explicitly models the changes between adjacent frames, yielding compact latent dynamics that are naturally coupled with residual actions. ResWM is integrated into a Dreamer-style latent dynamics model with minimal modifications and no extra hyperparameters. Both imagination rollouts and policy optimization are conducted in the residual-action space, enabling smoother exploration, lower control variance, and more reliable planning. Empirical results on the DeepMind Control Suite demonstrate that ResWM achieves consistent improvements in sample efficiency, asymptotic returns, and control smoothness, significantly surpassing strong baselines such as Dreamer and TD-MPC. Beyond performance, ResWM produces more stable and energy-efficient action trajectories, a property critical for robotic systems deployed in real-world environments. These findings suggest that residual action modeling provides a simple yet powerful principle for bridging algorithmic advances in RL with the practical requirements of robotics.

---

## 论文详细总结（自动生成）

这是一份关于论文《ResWM: Residual-Action World Model for Visual RL》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在基于模型的强化学习（MBRL）中，智能体通过视觉观测学习“世界模型”来进行规划。
*   **核心问题**：传统的框架（如 Dreamer）通常将未来状态预测建立在**绝对动作**（Absolute Actions）之上。这种方式忽略了物理控制中的时间连续性，导致：
    1.  **优化不稳定**：最优动作分布随任务剧烈变化，搜索空间巨大。
    2.  **控制震荡**：容易产生高频 chattering（动作抖动），不仅降低规划效率，还会对真实机器人硬件造成机械损伤和能源浪费。
*   **研究动机**：利用物理控制中固有的“平滑性”先验，将控制变量从“绝对值”转变为“增量值”，以缩小搜索空间并稳定长程规划。

### 2. 论文提出的方法论
ResWM 框架主要由以下三个核心组件构成：
*   **残差动作策略（Residual-Action Policy）**：
    *   **核心思想**：策略网络不再直接输出 $a_t$，而是输出相对于前一步动作的增量 $\delta a_t$。
    *   **公式表述**：$a_t = \text{tanh}(a_{t-1} + \delta a_t)$，其中 $\delta a_t \sim \pi_\theta(\cdot | z_t, a_{t-1})$。
    *   **作用**：将全局搜索转化为局部精细化调整，强制引入了时间平滑性先验。
*   **观测差异编码器（Observation Difference Encoder, ODL）**：
    *   **技术细节**：不只是编码单帧图像，而是显式计算相邻帧的差异：$z_t = \text{LN}(\text{FC}(f(o_t) - f'(o_{t-1})))$。
    *   **作用**：过滤静态背景冗余，提取与动态变化（如物体移动、关节摆动）直接相关的特征，使潜空间表示与残差动作在因果上更匹配。
*   **残差驱动的潜空间动力学**：
    *   将残差动作 $\delta a_t$ 直接作为循环状态空间模型（RSSM）的输入：$s_{t+1} \sim g_\phi(s_{t+1} | s_t, \delta a_t)$。
*   **正则化机制**：
    *   引入 KL 散度惩罚，使 $\delta a_t$ 趋向于零均值高斯分布，鼓励智能体以最小的动作改变量完成任务。

### 3. 实验设计
*   **数据集/场景**：
    *   **DeepMind Control Suite (DMControl)**：包含 6 个标准任务（如 Cheetah Run, Walker Walk）和 5 个困难任务（如 Quadruped Walk, Hopper Hop）。
    *   **Atari 10 Benchmark**：用于验证模型在离散动作空间和高维像素输入下的泛化能力。
*   **Benchmark 与对比方法**：
    *   **强基准**：DreamerV3, TD-MPC。
    *   **最新方法**：ResAct (AAAI'25), TACO (NeurIPS'23), MaDi (AAMAS'24), DeepRAD 等。
    *   **兼容性测试**：将 ResWM 模块集成到 pixel SAC、DeepMDP、RAD 等不同类型的算法中观察提升。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及总训练时长。
*   **实现细节**：提到使用了 Adam 优化器，且 ResWM 可以在不增加额外超参数的情况下无缝集成到现有的 Dreamer 风格架构中。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 DMControl 标准任务上进行了 100K 和 500K 步的对比。
    *   在困难任务上进行了 500K 和 1M 步的对比。
    *   在 Atari 10 个不同类型的游戏上进行了测试。
    *   **消融实验**：对比了“无残差策略”、“无 ODL 编码器”和“无正则化”三种变体，验证了各组件的贡献度。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了最终得分，还通过注意力图（Attention Maps）可视化了 ODL 的工作原理，并展示了动作轨迹的平滑度曲线。跨越连续控制和离散游戏的测试证明了其广泛的适用性。

### 6. 论文的主要结论与发现
*   **性能领先**：ResWM 在采样效率和渐近性能上均达到 SOTA。在 DMControl 困难任务上，平均得分显著高于 ResAct 和 TACO。
*   **平滑性与能效**：ResWM 生成的动作轨迹更平滑，有效减少了高频震荡，这对于现实世界中机器人的节能和寿命延长具有重要意义。
*   **感知聚焦**：可视化表明，ODL 引导模型将注意力集中在关键关节和末端执行器上，而非弥散在整个物体轮廓上。
*   **组件优先级**：消融实验证明贡献度排序为：**残差策略 > ODL > 正则化**。

### 7. 优点
*   **物理直觉强**：将控制理论中的增量控制思想引入深度 RL，符合生物运动控制逻辑。
*   **即插即用**：不需要复杂的超参数调优，对现有世界模型架构改动极小。
*   **鲁棒性高**：在部分可观测和复杂动力学任务（困难任务）中表现出比绝对动作模型更强的稳定性。

### 8. 不足与局限
*   **反应延迟风险**：由于动作被限制为增量更新，在需要“瞬时极大跨度动作切换”的极端场景下（如突发碰撞规避），智能体可能反应较慢。
*   **计算开销**：ODL 需要同时处理 $o_t$ 和 $o_{t-1}$，虽然在潜空间想象阶段开销忽略不计，但在与环境交互的编码阶段会略微增加内存和推理延迟。
*   **实验环境局限**：虽然在 DMControl 和 Atari 表现优异，但尚未在具有极高随机性或真实物理噪声的户外机器人场景中进行验证。

（完）
