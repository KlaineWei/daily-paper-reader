---
title: "DySL-VLA: Efficient Vision-Language-Action Model Inference via Dynamic-Static Layer-Skipping for Robot Manipulation"
title_zh: DySL-VLA：通过动静态层跳过实现机器人操纵的高效视觉-语言-动作模型推理
authors: "Zebin Yang, Yijiahao Qi, Tong Xie, Bo Yu, Shaoshan Liu, Meng Li"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22896v1"
tags: ["query:slm-rl"]
score: 8.0
evidence: 通过动态层跳减实现高效VLA推理
tldr: 针对视觉-语言-动作（VLA）模型在机器人任务中计算开销大的问题，本文提出DySL-VLA框架。该框架基于动作重要性差异，通过动态跳过非关键层加速推理。它将模型层分为信息层和增量层，并引入先验-后验跳过引导机制及两阶段知识蒸馏算法。实验表明，该方法在保持精度的同时显著提升了推理速度并大幅减少了参数量。
selection_source: carryover_cache
motivation: VLA模型计算成本高，难以满足机器人实时操作的需求，且不同动作步骤对精度的要求存在差异。
method: 提出一种动态-静态层跳过机制，将层分为必选和可选，并利用先验-后验引导及知识蒸馏进行高效训练。
result: 在Calvin数据集上，相比基准模型实现了3.75倍的加速，且可训练参数量减少了85.7倍。
conclusion: DySL-VLA通过动态调整计算资源分配，有效平衡了VLA模型的推理效率与操作精度。
---

## 摘要
视觉-语言-动作 (VLA) 模型通过融合语言模型的推理能力与视觉模型的 3D 理解能力，在机器人操纵等任务中取得了显著成功。然而，其高昂的计算成本仍然是需要实时性能的现实应用中的主要障碍。我们观察到，任务中的动作具有不同程度的重要性：关键步骤需要高精度，而较不重要的步骤可以容忍更大的方差。基于这一洞察，我们提出了 DySL-VLA，这是一种通过根据每个动作的重要性动态跳过 VLA 层来解决计算成本问题的新型框架。DySL-VLA 将其层分为两类：始终执行的信息层 (informative layers) 和可以有选择地跳过的增量层 (incremental layers)。为了在不牺牲准确性的情况下智能地跳过层，我们发明了一种先验-后验跳过引导机制 (prior-post skipping guidance mechanism) 来确定何时启动层跳过。我们还提出了一种跳过感知的两阶段知识蒸馏算法，以高效地将标准 VLA 训练为 DySL-VLA。实验表明，在 Calvin 数据集上，DySL-VLA 的成功长度比 Deer-VLA 提高了 2.1%，同时可训练参数减少了 85.7 倍，并且在等精度下相对于 RoboFlamingo 基准实现了 3.75 倍的加速。我们的代码可在 https://github.com/PKU-SEC-Lab/DYSL_VLA 获取。

## Abstract
Vision-Language-Action (VLA) models have shown remarkable success in robotic tasks like manipulation by fusing a language model's reasoning with a vision model's 3D understanding. However, their high computational cost remains a major obstacle for real-world applications that require real-time performance. We observe that the actions within a task have varying levels of importance: critical steps demand high precision, while less important ones can tolerate more variance. Leveraging this insight, we propose DySL-VLA, a novel framework that addresses computational cost by dynamically skipping VLA layers based on each action's importance. DySL-VLA categorizes its layers into two types: informative layers, which are consistently executed, and incremental layers, which can be selectively skipped. To intelligently skip layers without sacrificing accuracy, we invent a prior-post skipping guidance mechanism to determine when to initiate layer-skipping. We also propose a skip-aware two-stage knowledge distillation algorithm to efficiently train a standard VLA into a DySL-VLA. Our experiments indicate that DySL-VLA achieves 2.1% improvement in success length over Deer-VLA on the Calvin dataset, while simultaneously reducing trainable parameters by a factor of 85.7 and providing a 3.75x speedup relative to the RoboFlamingo baseline at iso-accuracy. Our code is available on https://github.com/PKU-SEC-Lab/DYSL_VLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《DySL-VLA: Efficient Vision-Language-Action Model Inference via Dynamic-Static Layer-Skipping for Robot Manipulation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然推理能力强，但由于其庞大的 LLM 主干网络，计算开销巨大，推理频率通常仅为 1-5 Hz，难以满足机器人实时物理交互（通常需要 20-50+ Hz）的需求。
*   **研究动机**：
    *   **动作重要性不均**：在机器人任务中，并非所有动作都同等重要。例如，抓取或释放物体的瞬间对精度要求极高，而预备性的移动过程则可以容忍一定的偏差。
    *   **模型层贡献不均**：观察发现，VLA 模型中某些“信息层”对激活分布改变显著，而其他层贡献较小。
    *   **现有方法缺陷**：现有的剪枝、量化或早期退出（Early-exit）方法往往对所有动作一视同仁，或者在早期退出时丢失了深层的重要信息。

### 2. 方法论
DySL-VLA 提出了一种**动静态层跳过（Dynamic-Static Layer-Skipping）**框架，核心技术包括：
*   **动静态层划分**：将 VLA 层分为“静态信息层”（始终执行，保留核心 3D 理解和推理能力）和“动态增量层”（根据动作重要性选择性跳过）。
*   **先验-后验跳过引导（Prior-Post Skipping Guidance）**：
    *   **先验预测**：定义“轨迹连续性”指标（基于前 $k$ 步动作的差异）。当连续性高时（简单移动），允许跳过更多层；当连续性降低（精细操作）时，前移“允许跳过点”，强制执行更多层。
    *   **后验验证**：若检测到连续性突然大幅下降，系统会重新进行一次无跳过的完整推理，以确保关键动作的绝对精度。
*   **跳过感知的两阶段知识蒸馏**：
    *   **第一阶段**：仅训练轻量级适配器（Adapters），使其能够总结并拟合被跳过层的激活分布。
    *   **第二阶段**：联合训练跳过控制器（Controllers）和适配器，引入任务损失和归一化损失，在保证精度的前提下最大化跳过率。
    *   **优势**：无需微调庞大的 LLM 主干，仅训练极少量的适配器参数。

### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：用于评估长程操纵任务的成功长度（0-5）。
    *   **LIBERO**：包含 Spatial、Object、Goal 和 Long 四个子集，评估任务成功率（SR）。
*   **基准模型（Backbones）**：RoboFlamingo (3B/9B) 和 OpenVLA-oft (7B)。
*   **对比方法**：
    *   全量模型：RoboFlamingo, OpenVLA。
    *   加速方法：DeeR-VLA（早期退出）、FlexiDepth（自适应跳层）、SparseVLM（剪枝）等。
    *   传统方法：HULC, SPIL。

### 4. 资源与算力
*   **训练资源**：
    *   在 CALVIN 任务上使用 **RTX 4090**。
    *   在 LIBERO 任务上使用 **NVIDIA A6000**。
*   **训练效率**：
    *   DySL-VLA 的训练时长约为 **7 GPU 小时**，而对比方法 DeeR-VLA 需要 **112 GPU 小时**。
    *   可训练参数量从 1.2B 降至 14M（CALVIN）或从 7.1B 降至 226M（LIBERO），参数量减少了 **31.4x 至 85.7x**。
*   **部署平台**：在边缘计算平台 **Jetson Orin** 上进行了实测，验证了实时性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流机器人操纵基准数据集，进行了跨数据集（D→D, ABC→D）的泛化性测试。
*   **消融实验**：非常充分。分别验证了先验预测、后验验证、两阶段蒸馏、静态层比例、轨迹长度 $k$ 等因素对性能的影响。
*   **客观性**：通过等精度下的延迟对比（Iso-accuracy latency）和成功率双重指标衡量，并对比了随机跳过（Random Skip）等基准，证明了策略的有效性。

### 6. 主要结论与发现
*   **性能提升**：在 CALVIN 数据集上，DySL-VLA 比 DeeR-VLA 成功长度提高 2.1%，且训练成本大幅降低。
*   **显著加速**：相对于 RoboFlamingo 基准，实现了 **3.75倍** 的推理加速；在 Jetson Orin 上，配合 Action Chunking 技术，控制频率可达 **23.2 Hz**，满足实时要求。
*   **精度保持**：通过保留关键的静态层和后验验证机制，DySL-VLA 在大幅加速的同时，几乎没有损失原始 VLA 模型的任务成功率。

### 7. 优点
*   **高效训练**：采用适配器微调而非全量微调，极大地降低了算力门槛。
*   **动态感知**：巧妙地利用“动作连续性”作为动作重要性的代理指标，无需复杂的额外标注。
*   **部署友好**：不依赖特定的硬件算子（如稀疏卷积或特殊量化内核），具有良好的通用性。
*   **鲁棒性**：后验验证机制为关键动作提供了“安全网”，避免了动态跳层带来的灾难性失败。

### 8. 不足与局限
*   **连续性假设**：该方法高度依赖“动作连续性”与“动作重要性”的正相关关系。在某些特殊任务中，如果重要动作本身也是平滑连续的，该启发式策略可能失效。
*   **适配器开销**：虽然适配器很轻量，但在极端追求低延迟的场景下，频繁调用控制器和适配器仍会产生一定的串行开销。
*   **任务覆盖**：实验主要集中在桌面操纵任务，对于移动导航或更复杂的全身协调任务，其动静态层的划分策略可能需要重新调整。

（完）
