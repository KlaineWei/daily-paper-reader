---
title: "Reasoning as Gradient: Scaling MLE Agents Beyond Tree Search"
title_zh: 推理即梯度：将 MLE 智能体扩展至树搜索之外
authors: "Yifei Zhang, Xu Yang, Xiao Yang, Bowen Xian, Qizheng Li, Shikai Fang, Jingyuan Li, Jian Wang, Mingrui Xu, Weiqing Liu, Jiang Bian"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01692v1"
tags: ["query:slm-rl"]
score: 8.0
evidence: 智能体的多步推理与基于梯度的优化
tldr: "本研究针对机器学习工程（MLE）智能体效率低下的问题，提出了一种名为GOME的新型框架。传统智能体依赖树搜索等无梯度优化，而GOME将LLM的结构化推理类比为梯度计算，引入动量机制和分布式优化思想。实验表明，GOME在MLE-Bench上取得了35.1%的SOTA成绩。研究揭示了模型推理能力与优化策略的关系：随着模型变强，基于梯度的定向更新比穷举搜索更具优势，为未来高性能智能体设计提供了新方向。"
selection_source: carryover_cache
motivation: 随着LLM推理能力的提升，传统的树搜索等无梯度优化方法在处理复杂的机器学习工程任务时，效率已逐渐低于基于定向更新的优化方式。
method: 提出GOME框架，将结构化诊断推理映射为梯度，将成功经验转化为动量，并利用多轨迹执行实现类似分布式优化的过程。
result: "在MLE-Bench基准测试中，GOME在受限的计算预算下实现了35.1%的奖牌率，刷新了当前的最先进水平。"
conclusion: 研究证明了随着模型推理能力的增强，基于梯度的优化范式将比传统的树搜索更具扩展性和效率优势。
---

## 摘要
基于大语言模型（LLM）的机器学习工程（MLE）智能体主要依赖于树搜索，这是一种利用标量验证分数对候选方案进行排序的无梯度优化形式。随着 LLM 推理能力的提升，与定向更新相比，穷举枚举变得越来越低效，这类似于准确的梯度如何使高效下降优于随机搜索。我们引入了 Gome，一个实现基于梯度优化的 MLE 智能体。Gome 将结构化诊断推理映射为梯度计算，将成功记忆映射为动量，并将多轨迹执行映射为分布式优化。在隔离架构效应与外部知识的闭环协议下，Gome 在单张 V100 GPU 且受限的 12 小时预算内，在 MLE-Bench 上实现了 35.1% 的最先进（SOTA）奖牌率。跨 10 个模型的扩展实验揭示了一个关键的交叉点：对于较弱的模型，树搜索通过穷举探索来补偿不可靠的推理，从而保持优势；随着推理能力的增强，基于梯度的优化逐渐胜出，且在尖端模型上的差距进一步扩大。鉴于面向推理的 LLM 的快速发展，这使得基于梯度的优化成为一种日益有利的范式。我们发布了代码库和 GPT-5 轨迹。

## Abstract
LLM-based agents for machine learning engineering (MLE) predominantly rely on tree search, a form of gradient-free optimization that uses scalar validation scores to rank candidates. As LLM reasoning capabilities improve, exhaustive enumeration becomes increasingly inefficient compared to directed updates, analogous to how accurate gradients enable efficient descent over random search. We introduce \textsc{Gome}, an MLE agent that operationalizes gradient-based optimization. \textsc{Gome} maps structured diagnostic reasoning to gradient computation, success memory to momentum, and multi-trace execution to distributed optimization. Under a closed-world protocol that isolates architectural effects from external knowledge, \textsc{Gome} achieves a state-of-the-art 35.1\% any-medal rate on MLE-Bench with a restricted 12-hour budget on a single V100 GPU. Scaling experiments across 10 models reveal a critical crossover: with weaker models, tree search retains advantages by compensating for unreliable reasoning through exhaustive exploration; as reasoning capability strengthens, gradient-based optimization progressively outperforms, with the gap widening at frontier-tier models. Given the rapid advancement of reasoning-oriented LLMs, this positions gradient-based optimization as an increasingly favorable paradigm. We release our codebase and GPT-5 traces.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reasoning as Gradient: Scaling MLE Agents Beyond Tree Search》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的机器学习工程（MLE）智能体主要依赖“树搜索”（无梯度优化），通过验证分数（标量）来筛选候选方案。这种方式在 LLM 推理能力较弱时有效，但在面对具备强大推理能力的模型（如 GPT-5, o3, R1）时，忽略了执行反馈中的丰富诊断信息，导致搜索效率低下。
*   **研究动机**：作者认为 MLE 任务具有“可修复性”和“代码空间连续性”。与其像随机搜索一样不断尝试新分支，不如利用 LLM 的推理能力从错误日志和训练动态中提取“改进方向”。
*   **整体含义**：论文提出将推理视为“梯度”，将 MLE 智能体的优化过程从“无梯度的穷举搜索”转向“基于梯度的定向更新”。

### 2. 方法论：Gome 框架
Gome（Gradient-based Optimization for Machine Learning Engineering）将经典优化器的组件映射到智能体架构中：
*   **推理即梯度（Reasoning as Gradient）**：通过结构化推理分析执行反馈（日志、错误、代码差异），生成具体的改进假设，决定“如何更新”而非仅仅“选哪个”。
*   **成功记忆即动量（Success Memory as Momentum）**：建立全局共享的成功经验库，存储已验证的改进假设及其反馈，引导后续更新向证明有效的方向加速。
*   **多轨迹执行即分布式优化（Multi-trace Optimization）**：并行维护 $N$ 条优化轨迹，各轨迹独立演化但通过共享内存交换知识，并利用“强制多样化”初始化来覆盖搜索空间。
*   **分层验证（Hierarchical Validation）**：包含格式检查、评估对齐（检测数据泄露和过拟合）以及综合分析三个阶段，确保只有真正的改进才被接受。
*   **鲁棒实现**：采用小数据调试循环、多随机种子选择和自适应时间管理。

### 3. 实验设计
*   **基准测试**：主要使用 **MLE-Bench**（包含 75 个 Kaggle 竞赛任务）和其精简版 MLE-Bench-Lite。
*   **对比方法**：
    *   **闭环智能体**：MLAB, OpenHands, AIDE, AIRA, ML-Master。
    *   **消融变体**：Gome-MCTS（将优化策略替换为蒙特卡洛树搜索的受控变体）。
*   **评估协议**：采用“闭环协议”（Closed-world），即禁止外部知识检索，仅依靠任务材料和执行反馈，以隔离架构设计对性能的影响。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA V100 GPU**，12 vCPUs，220GB RAM。
*   **时间预算**：每个任务限制在 **12 小时**（部分扩展实验为 24 小时）。
*   **模型支持**：跨越 10 个模型，包括 GPT-4o-mini, DeepSeek-R1, o3 以及 **GPT-5**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 MLE-Bench 全部 75 个任务，每个任务运行 3 个独立随机种子并取平均值（Mean ± SEM）。
*   **消融实验**：针对结构化推理、成功记忆、多轨迹优化分别做了消融，证明了各组件的必要性。
*   **扩展性分析**：测试了从“效率型”到“前沿型”共 10 种不同推理能力的模型，揭示了性能随模型能力提升的演变规律。
*   **真实场景验证**：在实时 Kaggle 竞赛（Store Sales）中进行了实测，证明了其在非公开数据集上的泛化能力。
*   **评价**：实验设计严谨，通过控制变量（如 Gome-MCTS）公平地对比了优化范式的差异，数据量充足且具有统计学意义。

### 6. 主要结论与发现
*   **SOTA 性能**：Gome 在 GPT-5 驱动下实现了 **35.1%** 的奖牌率，刷新了闭环协议下的最高纪录。
*   **关键交叉点（Crossover）**：
    *   在弱模型上，树搜索占优，因为它能通过穷举弥补推理的不可靠。
    *   在强模型上，基于梯度的优化显著胜出，且差距随模型推理能力的增强而扩大。
*   **效率优势**：相比 AIRA 等方法，Gome 在更弱的硬件（V100 vs H200）和更短的时间内取得了更好的结果。
*   **过拟合检测**：Gome 的分层验证能有效识别“验证集分数上升但测试集下降”的伪改进，检测率达 66.7%。

### 7. 优点与亮点
*   **范式创新**：首次在 MLE 领域系统性地将推理形式化为梯度信号，为智能体设计提供了坚实的理论类比。
*   **扩展性强**：实验清晰地展示了该方法如何随 LLM 推理能力的进化而获得更大的收益。
*   **实用性高**：在受限的算力和时间内表现卓越，且代码库和 GPT-5 的执行轨迹已开源。

### 8. 不足与局限
*   **推理依赖性**：在推理能力较弱的模型上表现不佳，容易产生“梯度幻觉”（错误的诊断反馈）。
*   **闭环限制**：虽然闭环协议有利于学术对比，但在实际应用中，缺乏外部知识检索（如查阅最新论文或 API 文档）会限制其处理极高难度任务的能力。
*   **缺乏数学证明**：目前的“文本梯度”仍是功能性类比，缺乏像连续空间梯度下降那样的形式化收敛保证。
*   **成本问题**：使用 GPT-5 或 o3 等前沿模型进行多次迭代，API 调用成本依然较高。

（完）
