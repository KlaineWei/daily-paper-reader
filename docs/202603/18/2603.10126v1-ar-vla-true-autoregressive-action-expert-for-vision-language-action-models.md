---
title: "AR-VLA: True Autoregressive Action Expert for Vision-Language-Action Models"
title_zh: AR-VLA：视觉-语言-动作模型的真实自回归动作专家
authors: "Yutong Hu, Jan-Nico Zaech, Nikolay Nikolov, Yuanqi Yao, Sombit Dey, Giuliano Albanese, Renaud Detry, Luc Van Gool, Danda Paudel"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10126v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 具有长效记忆的VLA模型自回归动作生成
tldr: 本研究针对现有视觉-语言-动作（VLA）模型在生成动作时频繁重置上下文、缺乏时空一致性的问题，提出了AR-VLA。该模型引入了一个独立的自回归动作专家，通过长效记忆维持历史信息，将动作生成为连续的因果序列。通过重锚定机制解决感知延迟，实现了快速控制与慢速推理的解耦。实验证明，AR-VLA在保持高成功率的同时，显著提升了动作的平滑度和历史感知能力，为机器人策略训练提供了新范式。
selection_source: fresh_fetch
motivation: 现有VLA模型通常在每次观察时重置上下文，导致动作生成缺乏时空一致性，且难以应对感知与控制之间的频率不匹配。
method: 提出一种自回归动作专家模型，利用长效记忆维持历史上下文，并结合重锚定机制来处理训练和推理过程中的感知延迟。
result: 在仿真和真实机器人实验中，AR-VLA 表现出优异的历史感知能力，生成的动作轨迹更平滑，且成功率达到或超过了现有最先进的反应式VLA模型。
conclusion: AR-VLA 提供了一个可扩展且具备上下文感知能力的动作生成框架，为构建高效、稳健的机器人策略奠定了结构基础。
---

## 摘要
我们提出了一种独立的自回归（AR）动作专家（Action Expert），它在以可刷新的视觉-语言前缀为条件的同时，将动作生成为连续的因果序列。与现有的视觉-语言-动作（VLA）模型和扩散策略不同，后者在每次新观测时都会重置时间上下文并以反应式方式预测动作，而我们的动作专家通过长效记忆维护其自身的历史信息，具有内在的上下文感知能力。这种结构解决了快速控制与慢速推理之间的频率不匹配问题，实现了运动语法（kinematic syntax）的高效独立预训练以及与重型感知主干网络的模块化集成，从而自然地确保了跨帧生成的动作在时空上的一致性。为了同步这些异步的混合 V-L-A 模态，我们利用了一种重锚定（re-anchoring）机制，在训练和推理过程中从数学上考虑了感知的陈旧性（staleness）。在模拟和真实机器人操作任务上的实验表明，所提出的方法可以有效地替代专用和通用策略中传统的基于块（chunk-based）的动作头。AR-VLA 表现出卓越的历史感知能力和显著更平滑的动作轨迹，同时保持或超过了最先进的反应式 VLA 的任务成功率。总之，我们的工作引入了一种可扩展的、上下文感知的动作生成方案，为训练有效的机器人策略提供了稳健的结构基础。

## Abstract
We propose a standalone autoregressive (AR) Action Expert that generates actions as a continuous causal sequence while conditioning on refreshable vision-language prefixes. In contrast to existing Vision-Language-Action (VLA) models and diffusion policies that reset temporal context with each new observation and predict actions reactively, our Action Expert maintains its own history through a long-lived memory and is inherently context-aware. This structure addresses the frequency mismatch between fast control and slow reasoning, enabling efficient independent pretraining of kinematic syntax and modular integration with heavy perception backbones, naturally ensuring spatio-temporally consistent action generation across frames. To synchronize these asynchronous hybrid V-L-A modalities, we utilize a re-anchoring mechanism that mathematically accounts for perception staleness during both training and inference. Experiments on simulated and real-robot manipulation tasks demonstrate that the proposed method can effectively replace traditional chunk-based action heads for both specialist and generalist policies. AR-VLA exhibits superior history awareness and substantially smoother action trajectories while maintaining or exceeding the task success rates of state-of-the-art reactive VLAs. Overall, our work introduces a scalable, context-aware action generation schema that provides a robust structural foundation for training effective robotic policies.

---

## 论文详细总结（自动生成）

### AR-VLA：视觉-语言-动作模型的真实自回归动作专家 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
当前的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然在推理时生成 Token，但在时间维度上通常是“反应式”的。它们大多采用**动作分块（Action Chunking）**策略，即根据当前观察预测一簇动作，但在下一个感知周期会重置上下文。这种做法导致了“马尔可夫健忘症”：模型忽略了动作的连续性和历史动量，导致控制不平滑、难以处理长程任务，且无法解决“慢速感知（VLM推理）”与“快速控制（机器人频率）”之间的频率不匹配问题。本论文旨在构建一个真正的**自回归动作专家**，让机器人像大语言模型处理文字一样，基于持续的动作流历史来预测下一步。

#### 2. 方法论：核心思想与关键技术
AR-VLA 的核心是将动作生成建模为连续的因果序列，并与感知主干解耦。
*   **自回归动作专家（AR Action Expert）**：一个独立的 Transformer 解码器，维护长效记忆。它不随感知更新而重置，而是将历史动作和状态视为 Token 流。
*   **混合键值缓存（Hybrid KV Cache, HKV）**：
    *   **动作流（Proprioceptive Stream）**：采用滚动 FIFO 机制，存储长期的机器人状态和动作历史。
    *   **视觉语言流（VL Stream）**：存储来自 VLM 的语义特征，作为可刷新的前缀，当新图像处理完后异步更新。
*   **动态时间重锚定（Dynamic Temporal Re-anchoring, DTR）**：利用旋转位置编码（RoPE）的位移不变性，将视觉特征锚定在捕获时的时间戳上。这使得模型能从数学上理解感知的“陈旧性”（Staleness），解决了训练（短序列）与推理（长序列）之间的分布偏移。
*   **两阶段训练协议**：
    1.  **动作预训练**：仅在动作轨迹数据上训练，学习运动语法（如关节限制、物理因果）。
    2.  **多模态对齐**：引入 VLM 特征，通过随机历史掩码（Stochastic History Masking）防止模型过度依赖历史而忽略视觉指令。

#### 3. 实验设计
论文在通用策略和专用策略两个维度进行了广泛评估：
*   **通用策略场景**：在 **BridgeV2** 数据集上预训练，使用 **PaliGemma-3B** 作为视觉主干。
    *   **Benchmark**：SimplerEnv 仿真环境（WidowX 机器人任务）及真实世界 WidowX 机器人。
    *   **对比方法**：OpenVLA, Octo, SpatialVLA, CogACT, 以及作者复现的 Pi-0-Fast 和 Pi-0.5。
*   **专用策略场景**：
    *   **任务**：PushT（2D推动）、ALOHA（双臂方块传输、插拔）。
    *   **对比方法**：ACT（动作分块 Transformer）、Diffusion Policy（扩散策略）。
*   **历史感知专项测试**：设计了 **PushT2**（仿真）和 **Stack3**（真实），这些任务在中间阶段目标会变得不可见，必须依赖动作历史记忆才能成功。

#### 4. 资源与算力
*   **算力细节**：论文提到第一阶段（动作预训练）非常高效，在**单张 NVIDIA A6000 GPU** 上运行 20,000 步仅需约 **2 小时**。
*   **模型规模**：通用模型采用 3B VLM + 300M 动作专家；专用模型采用 ResNet-18 + 轻量级 Transformer 解码器。
*   **训练步数**：通用策略第二阶段对齐训练了 30,000 步。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真与真实世界、单臂与双臂、通用与专用任务，实验设计非常全面。
*   **消融实验**：针对预训练必要性、RoPE 锚定策略、历史掩码率、上下文长度（1 到 40 步）进行了详细消融，验证了每个模块的贡献。
*   **客观性**：通过在真实世界中先进行“茄子入槽”基准校准，确保不同模型在相同的相机位姿下公平竞争，体现了实验的严谨性。

#### 6. 主要结论与发现
*   **性能领先**：AR-VLA 在 SimplerEnv 中平均成功率达 61.5%，显著超过 CogACT（52.1%）和 Pi-0 系列。
*   **轨迹平滑**：相比反应式模型，AR-VLA 生成的动作 Jerk（加加速度）更小，运动更符合运动学一致性。
*   **历史感知**：在目标被遮挡或需记忆已完成步骤的任务中，AR-VLA 表现出极强的鲁棒性，而反应式模型会陷入循环震荡。
*   **异步优势**：通过解耦，动作专家能以高频率（约 30ms/步）稳定运行，不受 VLM 慢速推理（约 70ms/帧）的阻塞。

#### 7. 优点
*   **结构优雅**：成功将“系统1（快速直觉控制）”与“系统2（慢速语义推理）”在架构上解耦。
*   **时空一致性**：通过自回归机制自然解决了动作抖动问题，无需复杂的后处理平滑。
*   **训练高效**：动作语法的独立预训练大幅缩短了多模态对齐的时间。
*   **数学严密**：DTR 机制巧妙利用 RoPE 解决了异步系统中的时间对齐难题。

#### 8. 不足与局限
*   **误差累积风险**：作为自回归模型，一旦产生偏离分布（OOD）的动作，该错误会被存入 KV Cache，可能导致后续动作产生复合误差（Compounding Errors）。
*   **知识隔离限制**：为了保护 VLM 语义能力，采用了梯度隔离，这可能限制了视觉特征针对特定动作任务进行深度微调的潜力。
*   **应用范围**：目前主要集中在机械臂操纵，尚未在足式机器人或更复杂的动态平衡任务中验证。

（完）
