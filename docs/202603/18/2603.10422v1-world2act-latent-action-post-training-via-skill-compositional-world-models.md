---
title: "World2Act: Latent Action Post-Training via Skill-Compositional World Models"
title_zh: World2Act：通过技能组合世界模型进行潜空间动作后训练
authors: "An Dinh Vuong, Tuan Van Vo, Abdullah Sohail, Haoran Ding, Liang Ma, Xiaodan Liang, Anqing Duan, Ivan Laptev, Ian Reid"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10422v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 利用世界模型和类强化学习对齐进行VLA策略后训练
tldr: World2Act是一个针对视觉-语言-动作（VLA）策略的后训练框架，旨在解决世界模型在像素空间监督下易受伪影影响的问题。它通过对比匹配将动作与世界模型的潜在动力学特征对齐，并引入基于LLM的技能分解流水线，构建了支持长时程任务的技能组合式世界模型。该方法在RoboCasa和LIBERO基准测试中达到SOTA，并显著提升了真实世界的泛化能力。
selection_source: fresh_fetch
motivation: 现有的基于世界模型的后训练方法依赖像素级监督，容易受到生成伪影的影响，且难以处理变长的机器人执行任务。
method: 提出World2Act框架，利用对比匹配将VLA动作与世界模型的潜在特征对齐，并结合LLM驱动的技能分解流水线实现长时程视频生成。
result: "在RoboCasa和LIBERO上取得SOTA性能，并在真实世界任务中实现了6.7%的性能提升。"
conclusion: 通过在潜在空间进行动作对齐并结合技能组合，World2Act有效增强了具身智能体在复杂环境下的鲁棒性和泛化性。
---

## 摘要
世界模型（WMs）已成为后训练视觉-语言-动作（VLA）策略的一种极具前景的方法，旨在提高其在环境变化下的鲁棒性和泛化能力。然而，大多数基于世界模型的后训练方法依赖于像素空间监督，这使得策略对像素级伪影以及不完美世界模型推演（rollouts）产生的幻觉非常敏感。我们提出了 World2Act，这是一个后训练框架，它使用对比匹配目标将 VLA 动作直接与世界模型视频动力学潜变量对齐，从而减少了对像素的依赖。后训练性能与推演质量紧密相关，但当前的世界模型在生成任意长度视频方面面临困难，因为它们大多在固定长度的片段上进行训练，而机器人执行任务的持续时间差异很大。为了解决这个问题，我们提出了一种基于大语言模型（LLM）的自动技能分解流水线，将高层指令分割为底层提示词。我们的流水线生成了 RoboCasa-Skill 和 LIBERO-Skill，支持在不同任务跨度下保持时间一致性的技能组合世界模型。实验表明，将 World2Act 应用于 GR00T-N1.6 和 Cosmos Policy 等 VLA 模型，在 RoboCasa 和 LIBERO 上取得了最先进的结果，并将真实世界性能提升了 6.7%，增强了具身智能体的泛化能力。

## Abstract
World Models (WMs) have emerged as a promising approach for post-training Vision-Language-Action (VLA) policies to improve robustness and generalization under environmental changes. However, most WM-based post-training methods rely on pixel-space supervision, making policies sensitive to pixel-level artifacts and hallucination from imperfect WM rollouts. We introduce World2Act, a post-training framework that aligns VLA actions directly with WM video-dynamics latents using a contrastive matching objective, reducing dependence on pixels. Post-training performance is tied to rollout quality, yet current WMs struggle with arbitrary-length video generation as they are mostly trained on fixed-length clips while robotic execution durations vary widely. To address this, we propose an automatic LLM-based skill-decomposition pipeline that segments high-level instructions into low-level prompts. Our pipeline produces RoboCasa-Skill and LIBERO-Skill, supporting skill-compositional WMs that remain temporally consistent across diverse task horizons. Empirically, applying World2Act to VLAs like GR00T-N1.6 and Cosmos Policy achieves state-of-the-art results on RoboCasa and LIBERO, and improves real-world performance by 6.7%, enhancing embodied agent generalization.

---

## 论文详细总结（自动生成）

### World2Act：通过技能组合世界模型进行潜空间动作后训练

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）策略在面对环境变化或未见过的接触条件时泛化能力较弱。虽然世界模型（WM）可以提供动力学先验，但目前的后训练（Post-training）方法大多依赖**像素空间（Pixel-space）**的监督（如通过逆动力学模型推断动作或像素级奖励）。
*   **研究动机**：
    *   **像素敏感性**：世界模型生成的视频在长时程任务中容易产生幻觉和伪影，像素级的误差会干扰策略学习。
    *   **生成长度瓶颈**：视频扩散模型通常在固定长度片段上训练，而机器人任务时长跨度大，导致长时程生成不稳定。
    *   **效率需求**：直接微调大规模 VLA 参数效率低且易导致灾难性遗忘。

#### 2. 方法论
World2Act 提出了一个两阶段的后训练框架，核心在于**潜空间对齐**和**技能分解**：
*   **技能组合世界模型流水线**：
    *   **自动分解**：利用 LLM（如 DeepSeek）将高层指令分解为原子技能描述，并根据夹持器状态变化自动分割专家演示视频。
    *   **数据集构建**：发布了 `RoboCasa-Skill` 和 `LIBERO-Skill` 数据集，通过缩短视频片段长度并使其分布更均匀，降低了生成难度。
    *   **自回归生成**：推理时，LLM 生成技能序列，世界模型逐个生成短视频片段并拼接，确保长时程任务的时间一致性。
*   **World2Act 后训练（两阶段）**：
    *   **阶段 1：潜空间对齐**。训练视频适配器（Video Adapter）和动作适配器（Action Adapter），通过**对比学习（InfoNCE Loss）**将世界模型的视频潜变量与机器人动作特征对齐，建立共享潜空间。
    *   **阶段 2：残差策略训练**。冻结基础 VLA 模型，引入轻量级的**残差策略（Residual Policy）**。在在线推演中，利用对齐的潜空间作为引导，使 VLA 输出的动作在潜空间内向世界模型的动力学先验靠拢。

#### 3. 实验设计
*   **数据集/场景**：
    *   **RoboCasa**：复杂的厨房操作任务。
    *   **LIBERO**：包含空间、物体、目标和长时程四个维度的基准测试。
    *   **真实世界**：使用 Franka Research 3 机械臂进行杯子放置、碗抓取和关抽屉任务。
*   **Benchmark 与对比方法**：
    *   **基础 VLA**：GR00T-N1.5/N1.6, Cosmos Policy, π0, π0.5, OpenVLA 等。
    *   **后训练基准**：DreamGen（像素级监督）, VLA-RFT（强化学习微调）, Ctrl-World。

#### 4. 资源与算力
*   **世界模型训练**：使用 8 台 **AMD Instinct MI210 (48GB)** GPU 进行微调。
*   **VLA 评估与推理**：在单张 **NVIDIA RTX 4090 (24GB)** 上完成。
*   **训练时长**：残差策略训练约需 **6.8 小时**，比传统的 LoRA 微调（约 15 小时）快 2.25 倍，且推理速度保持在 250Hz 以上（针对 GR00T-N1.6）。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流仿真器上进行了大规模测试，每个任务运行 5 个随机种子，每个种子 50 次试验。
*   **消融实验**：涵盖了适配器架构对比（残差 vs LoRA）、损失函数对比（单向 vs 双向 InfoNCE）、世界模型底座对比（Base-WM vs Skill-WM）以及数据规模缩放实验。
*   **充分性评价**：实验设计非常全面，不仅验证了性能提升，还深入探讨了潜空间余弦相似度与任务成功率的正相关性，并展示了跨任务的零样本泛化能力，实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **潜空间对齐优于像素对齐**：World2Act 在 RoboCasa 上显著超过了依赖像素监督的 DreamGen，证明了潜空间特征对伪影更具鲁棒性。
*   **技能分解是关键**：通过将长任务分解为原子技能，世界模型的物理对齐度（PA）和指令遵循度（IF）大幅提升。
*   **性能提升**：在 RoboCasa 上将 GR00T-N1.6 的成功率从 70.1% 提升至 72.6%；在真实世界任务中平均提升了 **6.7%** 的成功率。
*   **高效性**：残差策略在保持 VLA 原始知识的同时，以极低的计算开销实现了动力学先验的注入。

#### 7. 优点
*   **创新性**：首次提出将 VLA 动作与世界模型的视频潜变量直接进行对比对齐，摆脱了对高质量像素生成的过度依赖。
*   **数据驱动的分解**：引入了自动化的 LLM 技能分解流水线，解决了长时程机器人任务生成的难题。
*   **实用性**：残差策略设计精巧，训练速度快，推理延迟低，非常适合部署在真实机器人硬件上。

#### 8. 不足与局限
*   **底座敏感性**：实验发现 World2Act 对不同 VLA 底座的提升幅度不一（如对 Cosmos Policy 的提升较小），可能受限于底座本身的联合表示能力。
*   **真实世界差距**：虽然有提升，但真实世界的绝对成功率仍然有限，表明世界模型与真实物理动力学之间仍存在领域鸿沟（Domain Gap）。
*   **依赖 LLM 质量**：技能分解的准确性高度依赖于 LLM 的推理能力，在极少数非预置交互场景下可能失效。

（完）
