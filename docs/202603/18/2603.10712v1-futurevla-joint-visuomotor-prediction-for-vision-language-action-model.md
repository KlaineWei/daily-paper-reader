---
title: "FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model"
title_zh: FutureVLA：视觉-语言-动作模型的联合视觉运动预测
authors: "Xiaoxu Xu, Hao Li, Jinhui Ye, Yilun Chen, Jia Zeng, Xinyi Chen, Linning Xu, Dahua Lin, Weixin Li, Jiangmiao Pang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10712v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: VLA模型的联合视觉运动预测
tldr: 针对具身智能体中视觉感知与动作执行耦合不足的问题，本文提出FutureVLA。该模型通过创新的联合视觉-动作预测架构，在预训练阶段利用异构数据集和门控机制实现视觉状态与动作建模的结构化解耦，使模型能捕捉连续物理动力学。在后期训练中，通过潜空间嵌入对齐策略，使多种VLA模型在不改变推理架构的情况下内化时间先验，显著提升了机器人的操作性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有VLA模型在联合建模视觉与动作预测时，容易受无关视觉细节干扰或缺乏时间连续性，难以有效捕捉环境约束下的动态动作意图。
method: 提出一种联合视觉-动作预测架构，利用门控机制解耦视觉状态与动作流，并通过潜空间嵌入对齐策略将物理先验整合至下游模型。
result: 实验结果表明，FutureVLA在多种机器人操纵任务中均能显著且一致地提升现有VLA框架的预测精度和执行效果。
conclusion: FutureVLA通过解耦视觉与动作信息并强化物理动力学建模，为提升具身智能体的时间预测能力提供了一种高效且通用的解决方案。
---

## 摘要
预测性预见对于智能具身智能体至关重要。由于机器人的运动执行本质上受其对环境几何视觉感知的约束，有效地预测未来需要捕捉这种紧密耦合的视觉运动相互作用。虽然最近的视觉-语言-动作（VLA）模型尝试引入未来引导，但它们在处理这种联合建模方面仍面临困难。现有的显式方法将模型容量分散到了与任务无关的视觉细节上，而依赖稀疏帧对的隐式方法则破坏了时间连续性。由于过度依赖视觉重建，这些方法变得以视觉为主导，将静态场景上下文与动态动作意图纠缠在一起。我们认为，有效的联合视觉运动预测建模既需要时间连续性，也需要视觉条件的监督解耦。为此，我们提出了 FutureVLA，其特点是采用了一种新型的联合视觉运动预测架构。FutureVLA 旨在通过先解耦视觉和运动信息，然后联合编码广义物理先验来提取联合视觉运动嵌入。具体而言，在预训练阶段，我们利用异构操作数据集并引入联合视觉运动门控机制，从结构上将视觉状态保持与时间动作建模分离。它允许运动流专注于连续的物理动力学，同时显式地查询视觉标记以获取环境约束，从而产生具有高度泛化能力的联合视觉运动嵌入。随后，在后训练阶段，我们采用潜嵌入对齐策略，使各种下游 VLA 模型能够在不修改其推理架构的情况下内化这些时间先验。大量的实验表明，FutureVLA 持续改进了 VLA 框架。

## Abstract
Predictive foresight is important to intelligent embodied agents. Since the motor execution of a robot is intrinsically constrained by its visual perception of environmental geometry, effectively anticipating the future requires capturing this tightly coupled visuomotor interplay. While recent vision-language-action models attempt to incorporate future guidance, they struggle with this joint modeling. Existing explicit methods divert capacity to task-irrelevant visual details, whereas implicit methods relying on sparse frame pairs disrupt temporal continuity. By heavily relying on visual reconstruction, these methods become visually dominated, entangling static scene context with dynamic action intent. We argue that effective joint visuomotor predictive modeling requires both temporal continuity and visually-conditioned supervision decoupling. To this end, we propose FutureVLA, featuring a novel Joint Visuomotor Predictive Architecture. FutureVLA is designed to extract joint visuomotor embeddings by first decoupling visual and motor information, and then jointly encoding generalized physical priors. Specifically, in the pretraining stage, we leverage heterogeneous manipulation datasets and introduce a Joint Visuomotor Gating mechanism to structurally separate visual state preservation from temporal action modeling. It allows the motor stream to focus on continuous physical dynamics while explicitly querying visual tokens for environmental constraints, yielding highly generalizable joint visuomotor embeddings. Subsequently, in the post-training stage, we employ a latent embeddings alignment strategy, enabling diverse downstream VLA models to internalize these temporal priors without modifying their inference architectures. Extensive experiments demonstrate that FutureVLA consistently improves VLA frameworks.

---

## 论文详细总结（自动生成）

以下是对论文《FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在预测未来状态以指导当前动作时，存在两个主要缺陷：
    1.  **视觉主导纠缠（Visual Dominance）**：显式预测未来帧的方法会将大量模型容量浪费在与任务无关的背景细节上，导致动作意图与静态场景混淆。
    2.  **时间不连续性（Temporal Discontinuity）**：隐式方法通常依赖稀疏的帧对（如仅起始帧和结束帧），破坏了动作执行所需的连续物理动力学。
*   **研究动机**：有效的联合视觉运动预测需要**时间连续性**和**视觉条件的监督解耦**。机器人动作执行必须受环境几何约束，但动作逻辑应独立于视觉渲染。

### 2. 方法论：FutureVLA 架构
FutureVLA 采用两阶段训练范式：
*   **阶段一：联合视觉运动预训练（Joint Visuomotor Pretraining）**
    *   **视觉标记化**：使用预训练且冻结的 **3D-VAE**（来自 WAN 模型）将连续的 17 帧视频剪辑压缩为时间标记，保留细粒度的运动结构。
    *   **联合视觉运动门控（JVG）**：将时间标记解耦为**视觉流**（负责保存初始环境状态）和**运动流**（负责物理动力学建模）。
    *   **解耦监督**：视觉流通过重建第一帧图像来学习静态约束；运动流通过预测动作块（Action Chunk）来学习动态意图。
    *   **门控交叉注意力**：运动流通过交叉注意力机制显式查询视觉流中的空间特征（Affordance），确保动作符合物理环境。
*   **阶段二：VLA 后训练与潜嵌入对齐**
    *   **知识迁移**：将预训练得到的联合视觉运动嵌入作为“语义锚点”。
    *   **潜嵌入对齐**：在训练下游 VLA（如基于 Qwen3-VL）时，通过 MSE 损失将 VLA 的中间表示与预训练的未来感知嵌入对齐。
    *   **推理优势**：这种对齐策略使模型在推理时仅需当前单帧输入，即可内化未来时间先验，不增加推理开销。

### 3. 实验设计
*   **数据集**：使用了包含 1560 万帧的混合数据集，涵盖 OXE、LIBERO 和 Bridge 等异构操作数据。
*   **Benchmark**：
    *   **仿真环境**：SimplerEnv（Google Robot 和 WidowX 机器人）、LIBERO（Object, Spatial, Goal, Long 四个子集）、LIBERO-Plus（鲁棒性测试）。
    *   **真实世界**：使用 Franka 机器人执行四类任务：制作汉堡、插花、勺子舀豆子、擦白板。
*   **对比方法**：包括原始 VLA 基准（OpenVLA, $\pi_0$, GR00T-N1.5）、显式未来引导（WorldVLA, DreamVLA）和隐式未来引导（UniVLA, Villa-X, LAPA）。

### 4. 资源与算力
*   **算力投入**：使用了 **16 张 NVIDIA A100 GPU**。
*   **训练时长**：预训练阶段耗时约 **3 天**。
*   **模型规格**：后训练阶段采用了 Qwen3-VL-4B-Instruct 作为视觉语言模型（VLM）主干。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括跨机器人实体的仿真测试、长程任务评估、以及 7 种不同环境扰动下的鲁棒性分析。
*   **消融实验**：针对 JVPM 引导的有效性、JVG 门控机制的作用、时间采样密度（2/5/9/17 帧对比）、时间跨度长度、以及重建目标（首帧 vs 末帧）等进行了深入消融。
*   **客观性**：通过在统一架构下重新实现对比方法（如 WorldVLA-style, Villa-X-style），消除了主干网络差异带来的偏差，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：FutureVLA 在 SimplerEnv 上比无引导基准平均提升 **11.4%**，在真实机器人操作中比强大的 $\pi_0$ 模型高出 **26.7%**。
*   **解耦的重要性**：实验证明，将视觉重建与动作建模分离能显著减少视觉噪声干扰，提升动作预测的物理一致性。
*   **连续性优势**：连续多帧建模（17 帧）的效果远优于稀疏采样（2 帧或 5 帧），证明了时间连续性对捕捉复杂动力学至关重要。

### 7. 优点（亮点）
*   **结构化解耦**：创新的 JVG 机制解决了视觉信息过载导致的动作建模失效问题。
*   **推理零成本**：通过潜空间对齐，在不改变推理架构和输入要求的前提下，赋予了模型“预见未来”的能力。
*   **强泛化与鲁棒性**：在 LIBERO-Plus 的各种扰动（光照、背景、相机视角变化）下表现出极强的稳定性。

### 8. 不足与局限
*   **传感器局限**：目前主要依赖视觉信息，对于擦白板等接触密集型任务，缺乏触觉或力矩反馈的整合。
*   **视频编码约束**：受限于 3D-VAE 的架构，输入帧数必须符合 $4N+1$ 的格式，限制了时间窗口选择的灵活性。
*   **开环风险**：虽然引入了未来预测，但在极长程任务中，若对齐不够完美，仍可能存在复合误差导致的执行偏差。

（完）
