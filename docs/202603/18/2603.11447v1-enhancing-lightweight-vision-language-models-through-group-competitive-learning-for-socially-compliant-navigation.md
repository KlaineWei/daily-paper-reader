---
title: Enhancing Lightweight Vision Language Models through Group Competitive Learning for Socially Compliant Navigation
title_zh: 通过群体竞争学习增强轻量级视觉语言模型，以实现符合社会规范的导航
authors: "Xinyu Zhang, Atsushi Konno, Toshihiko Yamasaki, Ling Xiao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11447v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于机器人导航的轻量级视觉语言模型
tldr: 针对社交机器人导航中轻量级视觉语言模型（VLM）推理能力不足的问题，本文提出了一种名为“群体竞争学习（GCL）”的策略。该策略通过引入群体竞争目标（GCO）和非对称群体优化（AGO），在保持推理效率的同时显著提升了轻量级模型的决策性能。实验表明，经过GCL增强的3B模型在性能上超越了传统的8B模型，为实时机器人部署提供了高效且精准的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决大型视觉语言模型计算开销大而轻量级模型在复杂社交导航场景中推理能力较弱的矛盾。
method: 提出了群体竞争学习（GCL）策略，结合群体竞争目标（GCO）和非对称群体优化（AGO）来挖掘轻量级模型的性能极限。
result: "实验显示GCL使3B规模的模型在F1分数上提升了40%，并成功超越了参数量更大的8B基准模型。"
conclusion: GCL策略有效平衡了模型的推理精度与计算效率，是实现社交合规导航实时部署的有效途径。
---

## 摘要
社交机器人导航需要场景语义与人类社会规范的深度融合。扩大视觉语言模型（VLM）的规模通常能提升符合社会规范导航的推理与决策能力。然而，模型尺寸的增加会带来巨大的计算开销，限制了其在实时机器人部署中的适用性。相反，轻量级 VLM 虽然能实现高效推理，但在复杂的社会环境中往往表现出较弱的推理和决策性能。同时实现强大的推理能力和高效性仍然是一个亟待解决的挑战。为了弥补这一差距，我们提出了群体竞争学习（GCL），这是一种旨在增强轻量级 VLM 能力的策略。我们的策略引入了群体竞争目标（GCO）以协调全局语义与分布正则化，并结合非对称群体优化（AGO）来探索模型性能的上限。在社交导航基准测试上的实证评估表明，GCL 显著提升了 VLM 的性能。具体而言，GCL 使 Qwen2.5-VL-3B 学习者模型和引导模型 Qwen3-VL-4B 分别达到了 0.968 和 0.914 的 F1 分数，相比原始监督微调（SFT）分别提升了 40% 和 12%。值得注意的是，在原始 SFT 下，3B 模型最初落后于 8B 模型（F1 分数：0.692 对比 0.755）。然而，通过 GCL，3B 模型的表现超过了 8B 基准模型（提升了 28%）。这些结果表明，GCL 为在实际部署中同时实现高精度和计算效率提供了一种有效的解决方案。

## Abstract
Social robot navigation requires a sophisticated integration of scene semantics and human social norms. Scaling up Vision Language Models (VLMs) generally improves reasoning and decision-making capabilities for socially compliant navigation. However, increased model size incurs substantial computational overhead, limiting suitability for real-time robotic deployment. Conversely, lightweight VLMs enable efficient inference but often exhibit weaker reasoning and decision-making performance in socially complex environments. Achieving both strong reasoning ability and efficiency remains an open challenge. To bridge this gap, we propose Group Competitive Learning (GCL), a strategy designed to amplify the capabilities of lightweight VLMs. Our strategy introduces the Group Competitive Objective (GCO) to harmonize global semantics with distributional regularization, alongside Asymmetric Group Optimization (AGO) to explore the upper limits of model performance. Empirical evaluations on social navigation benchmarks demonstrate that GCL significantly elevates VLM performance. Specifically, GCL enables the Qwen2.5-VL-3B learner model and guide Qwen3-VL-4B to achieve an F1 score of 0.968 and 0.914, representing 40\% and 12\% improvement over vanilla supervised fine-tuning (SFT). Notably, under vanilla SFT, the 3B model initially trails the 8B model (F1: 0.692 vs. 0.755). However, through the GCL, the 3B model outperforms (28\%) the 8B baseline model. These results suggest that GCL provides an effective solution for achieving both high accuracy and computational efficiency in real-world deployment.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**群体竞争学习（Group Competitive Learning, GCL）**的训练策略，旨在提升轻量级视觉语言模型（VLM）在社交机器人导航任务中的推理与决策能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证机器人实时部署所需的**计算效率**（轻量级模型）的同时，获得足以应对复杂社交环境的**语义推理与决策能力**（通常仅大模型具备）。
*   **研究动机**：
    *   大型 VLM 推理能力强但计算开销巨大，难以在嵌入式机器人平台上实时运行。
    *   轻量级 VLM 推理速度快，但在理解人类社会规范（如保持距离、避让、预测意图）方面表现较弱。
    *   现有的知识蒸馏（KD）方法多侧重于视觉特征对齐或简单的 Token 分布匹配，缺乏全局语义一致性与决策逻辑的深度协调。

### 2. 方法论：群体竞争学习（GCL）
GCL 的核心思想是构建一个动态的、双流协同的训练框架，让两个具有不同架构或参数规模的模型（学习者 Learner 和 引导者 Guide）通过良性竞争互相增强。

*   **群体竞争目标（GCO）**：由三部分损失函数组成：
    1.  **监督损失 ($L_{sup}$)**：标准的自回归语言建模损失，确保模型遵循指令。
    2.  **全局语义损失 ($L_{GSL}$)**：采用 InfoNCE 风格的对比学习，通过注意力池化将变长隐藏状态聚合为全局语义向量，强制不同模型在特征空间实现高维语义对齐。
    3.  **分布正则化损失 ($L_{DRL}$)**：利用 Jensen-Shannon (JS) 散度约束两个模型的 Token 输出概率分布，通过非目标 Token 的“软知识”传递隐式经验。
*   **非对称群体优化（AGO）**：
    1.  **基于性能的角色分配（学习率）**：表现较好的模型作为 Guide，分配较低学习率以保持预训练知识的稳定性；表现较差的作为 Learner，分配较高学习率以加速对齐。
    2.  **基于容量的熵控制（温度系数）**：大参数模型采用低温度（$\tau=2.0$）以保持判别边界清晰；小参数模型采用高温度（$\tau=3.0$）以平滑分布，增强捕捉“暗知识”的灵活性。

### 3. 实验设计
*   **数据集**：
    *   **SNEI**：包含 265 个场景，1,625 个图像-文本对。
    *   **MUSON**：更复杂的城市环境数据集，包含 640 个场景，4,000 个图像-文本对。
*   **评估指标**：Action-F1（动作准确率）、Perception-cos（感知语义相似度）、Reasoning-cos（推理逻辑相似度）。
*   **对比基准（Benchmark）**：主要对比对象是**原始监督微调（Vanilla SFT）**。
*   **对比模型**：涵盖了 Qwen2.5-VL（3B, 7B）和 Qwen3-VL（2B, 4B, 8B）等不同代际和规模的模型，共组合了 12 组竞争对。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA RTX 8000 GPU** 组成的集群。
*   **技术框架**：采用了 **DeepSpeed ZeRO-3** 显存优化技术。
*   **训练细节**：学习率采用余弦退火调度，训练 10 个 Epoch，Warmup 比例为 0.1。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   **12 组模型组合**：涵盖了“小带大”、“大带小”以及“同规模互带”等多种模式。
    *   **消融实验**：针对 $L_{GSL}$ 和 $L_{DRL}$ 的权重进行了敏感度分析。
    *   **超参数分析**：深入探讨了非对称学习率比例（$r$）和非对称温度梯度对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。通过在两个独立数据集（SNEI 和 MUSON）上验证，证明了方法的泛化性；通过数学推导证明了非对称温度设计的梯度动力学原理，增强了理论说服力。

### 6. 主要结论与发现
*   **性能飞跃**：GCL 显著提升了轻量级模型的表现。Qwen2.5-VL-3B 在 GCL 作用下，Action-F1 从 0.692 提升至 0.968（**+40%**）。
*   **以小博大**：经过 GCL 训练的 **3B 模型性能超越了 8B 的基准模型**（F1 分数领先 28%），证明了该策略能有效挖掘小模型的潜力。
*   **非对称优势**：实验证实，非对称的学习率和温度设置是 GCL 成功的关键，能够有效防止训练崩溃并实现跨容量模型的协同进化。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了算法，还给出了严谨的梯度推导，解释了为什么非对称设计有效。
*   **架构无关性**：GCL 可以无缝集成到各种异构 VLM 架构中，具有很强的通用性。
*   **效率极高**：为资源受限的机器人提供了一种“低成本、高回报”的方案，使得 2B-3B 级别的模型也能胜任复杂的社交导航任务。

### 8. 不足与局限
*   **物理部署缺失**：论文主要集中在导航栈的高层规划（Planning Layer），尚未在真实的物理机器人平台上进行闭环测试（虽然作者解释了规划与控制是解耦的）。
*   **复杂动态场景挑战**：在处理具有强反射表面或细微人机交互（如推门、眼神交流）的场景时，模型仍存在误判风险。
*   **时序推理不足**：目前的输入主要是单帧图像或简短上下文，对于长时序的社交意图预测仍有提升空间。

（完）
