---
title: "Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning"
title_zh: 简单方案行之有效：视觉-语言-动作模型是结合强化学习的天然持续学习者
authors: "Jiaheng Hu, Jay Shim, Chen Tang, Yoonchang Sung, Bo Liu, Peter Stone, Roberto Martin-Martin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11653v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 利用参数高效的LoRA对VLA模型进行持续强化学习
tldr: 本研究探讨了视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现。尽管传统观点认为顺序微调会导致灾难性遗忘，但作者发现，结合低秩自适应（LoRA）的简单顺序微调在大型预训练模型上表现极佳。通过在多个基准测试上的实验，证明了该方法在保持高塑性的同时几乎没有遗忘，且具备强大的零样本泛化能力，为构建自进化具身智能体提供了高效简洁的方案。
selection_source: fresh_fetch
motivation: 旨在解决具身智能体在不断演化的开放环境中如何通过持续强化学习实现自我改进，并挑战顺序微调必然导致灾难性遗忘的传统认知。
method: 采用基于低秩自适应（LoRA）的简单顺序微调策略，在大型预训练 VLA 模型上进行在线强化学习。
result: 实验表明，该简单方案在多个挑战性基准测试中表现出极高的学习效率和极低的遗忘率，性能优于许多复杂的持续学习算法。
conclusion: 大型预训练模型、参数高效微调与在线强化学习的协同作用重塑了稳定性与塑性的权衡，使顺序微调成为 VLA 持续学习的强大且高效的方法。
---

## 摘要
针对视觉-语言-动作（VLA）模型的持续强化学习（CRL）是实现能够在开放式、演化环境中进行适应的自我改进具身智能体的一个极具前景的方向。然而，持续学习的传统观点认为，朴素的顺序微调（Seq. FT）会导致灾难性遗忘，因此需要复杂的 CRL 策略。在这项工作中，我们退后一步，对三种模型和五个具有挑战性的终身强化学习基准上的大型预训练 VLA 的 CRL 进行了系统研究。我们发现，与既有观点相反，结合低秩自适应（LoRA）的简单顺序微调表现异常强劲：它实现了高可塑性，几乎没有表现出遗忘，并保持了强大的零样本泛化能力，经常优于更复杂的 CRL 方法。通过详细分析，我们表明这种鲁棒性源于大型预训练模型、参数高效自适应和同策略（on-policy）强化学习之间的协同作用。这些组件共同重塑了稳定性-可塑性权衡，使持续适应既稳定又具可扩展性。我们的结果将顺序微调定位为 VLA 持续强化学习的一种强大方法，并为大模型时代的终身学习提供了新的见解。代码可在 github.com/UT-Austin-RobIn/continual-vla-rl 获取。

## Abstract
Continual Reinforcement Learning (CRL) for Vision-Language-Action (VLA) models is a promising direction toward self-improving embodied agents that can adapt in openended, evolving environments. However, conventional wisdom from continual learning suggests that naive Sequential Fine-Tuning (Seq. FT) leads to catastrophic forgetting, necessitating complex CRL strategies. In this work, we take a step back and conduct a systematic study of CRL for large pretrained VLAs across three models and five challenging lifelong RL benchmarks. We find that, contrary to established belief, simple Seq. FT with low-rank adaptation (LoRA) is remarkably strong: it achieves high plasticity, exhibits little to no forgetting, and retains strong zero-shot generalization, frequently outperforming more sophisticated CRL methods. Through detailed analysis, we show that this robustness arises from a synergy between the large pretrained model, parameter-efficient adaptation, and on-policy RL. Together, these components reshape the stability-plasticity trade-off, making continual adaptation both stable and scalable. Our results position Sequential Fine-Tuning as a powerful method for continual RL with VLAs and provide new insights into lifelong learning in the large model era. Code is available at github.com/UT-Austin-RobIn/continual-vla-rl.

---

## 论文详细总结（自动生成）

这篇论文对视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现进行了系统性研究，挑战了“顺序微调必然导致灾难性遗忘”的传统认知。以下是详细总结：

### 1. 核心问题与整体含义
*   **研究动机**：具身智能体需要在不断变化的环境中通过持续强化学习（CRL）实现自我改进。传统持续学习理论认为，简单的顺序微调（Seq. FT）会导致严重的“灾难性遗忘”，因此学术界开发了大量复杂的算法（如正则化、重放、参数隔离）来缓解这一问题。
*   **核心问题**：在大模型时代，拥有强大预训练基础的 VLA 模型在结合参数高效微调（PEFT）和强化学习（RL）时，是否依然遵循传统的遗忘规律？
*   **整体含义**：研究发现，大型预训练 VLA + LoRA + 同策略（On-policy）RL 的组合产生了一种天然的协同效应，使得最简单的顺序微调方案在几乎不产生遗忘的情况下，表现优于大多数复杂的 CRL 算法。

### 2. 方法论
*   **核心思想**：采用“简单方案（Simple Recipe）”，即直接对新任务进行顺序微调，不使用任何显式的遗忘防止机制。
*   **关键技术细节**：
    *   **基础模型**：使用大型预训练 VLA（如 OpenVLA, Pi-0），利用其强大的零样本泛化能力。
    *   **参数高效自适应**：采用 **LoRA（低秩自适应）**，秩（Rank）通常设为 32。LoRA 将更新限制在低秩子空间，保护了原始预训练权重。
    *   **强化学习算法**：使用 **GRPO（群体相对策略优化）**。这是一种稳定的同策略算法，通过群体内部比较来估计优势函数，无需额外的价值网络。
    *   **协同机制分析**：论文指出稳定性源于三个维度的结合：**容量**（大模型的零空间广阔）、**约束**（LoRA 限制了更新几何形状）和**目标**（同策略 RL 隐式地限制了 KL 散度漂移）。

### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：包括 Libero-Object（物体）、Libero-Spatial（空间关系）、Libero-Long（长程任务）。
    *   **RoboCasa**：复杂的厨房环境任务。
    *   **ManiSkill**：基于 SAPIEN 物理引擎的操纵任务。
*   **对比方法**：
    *   **基准**：顺序微调（Seq. FT）、多任务训练（Oracle 上界）。
    *   **CRL 算法**：EWC（正则化）、Expert Replay（专家重放）、Dark Experience Replay（DER，逻辑重放）、Dynamic Weight Expansion（DWE，参数隔离）。
    *   **大模型适配方法**：SLCA（层级学习率）、RETAIN（权重合并）。
*   **评估指标**：平均成功率（AVG）、负向后向迁移（NBT，即遗忘率）、前向迁移（FWT）、零样本成功率（ZS）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号和总训练时长。
*   **规模线索**：提到模型参数量高达 70 亿（7B），训练批次大小（Global Batch Size）为 8192，每个任务训练约 10240 个回合（Episodes）。这暗示了实验需要大规模的计算集群（如 A100 或 H100 集群）以及高效的分布式训练框架（如论文提到的 RLinf）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同的 VLA 架构（OpenVLA-OFT, Pi-0, OpenVLA），5 个主流基准测试。
*   **充分性**：
    *   每组实验均使用 3 个独立随机种子取平均值。
    *   进行了详尽的**鲁棒性测试**：包括相机视角扰动、光照变化、机器人初始位置变化以及任务顺序重排。
    *   进行了**消融实验**：分别移除 RL（改用 SFT）、移除大模型（改用小模型）、移除 LoRA（全参数微调），证明了三者缺一不可。
*   **评价**：实验设计非常全面且客观，通过多维度的对比有力支撑了结论。

### 6 主要结论与发现
*   **顺序微调极其强大**：在 VLA 持续学习中，Seq. FT + LoRA 的遗忘率极低（通常 < 2%），且可塑性极高。
*   **优于复杂算法**：许多传统的 CRL 算法（如 EWC）因为过度约束参数更新，反而损害了模型学习新任务的能力（可塑性损失）。
*   **零样本能力保持**：顺序微调不仅能学会新任务，还能维持甚至增强模型对未见任务的零样本泛化能力。
*   **训练差距可弥补**：Seq. FT 与多任务 Oracle 之间的微小差距可以通过增加训练步数来消除，证明其并未陷入糟糕的局部最优。

### 7. 优点
*   **简洁性**：提出了一种无需复杂内存管理或正则化项的 CRL 方案，极具实用价值。
*   **见解深刻**：通过 Fisher 信息矩阵和权重秩分析，从数学和几何角度解释了为什么大模型配合 LoRA 不易遗忘。
*   **开源贡献**：提供了完整的代码库和基准实现，有助于社区推动具身智能的终身学习研究。

### 8. 不足与局限
*   **依赖预训练**：该结论建立在模型已有强大预训练基础的前提下，对于从零开始训练（From Scratch）的持续学习未必适用。
*   **任务识别假设**：实验中任务指令是显式给出的（语言输入），未探讨在任务边界模糊或指令缺失情况下的表现。
*   **环境多样性限制**：虽然涵盖了多个仿真环境，但尚未在真实的物理机器人上进行大规模持续学习验证（Sim-to-Real 挑战）。
*   **奖励函数**：主要基于稀疏奖励（Sparse Reward），在密集奖励或其他复杂奖励函数下的表现有待进一步观察。

（完）
