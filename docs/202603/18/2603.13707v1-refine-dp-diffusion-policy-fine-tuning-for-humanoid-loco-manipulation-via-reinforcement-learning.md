---
title: "REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning"
title_zh: REFINE-DP：通过强化学习进行类人机器人移动-操作协同的扩散策略微调
authors: "Zhaoyuan Gu, Yipu Chen, Zimeng Chai, Alfred Cueva, Thong Nguyen, Yifan Wu, Huishu Xue, Minji Kim, Isaac Legene, Fukang Liu, Matthew Kim, Ayan Barula, Yongxin Chen, Ye Zhao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13707v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 通过强化学习实现类人机器人长程任务的分层框架
tldr: 针对人形机器人移动操作中高层规划与底层控制解耦导致的分布偏移和任务失败问题，本文提出 REFINE-DP 框架。该框架通过强化学习（PPO）同时微调预训练的扩散策略（DP）高层规划器和底层控制器，使两者协同优化。实验证明该方法在门禁穿越和长程物体搬运任务中表现优异，显著提升了任务成功率和泛化能力，并能有效处理分布外场景。
selection_source: fresh_fetch
motivation: 旨在解决人形机器人中扩散策略规划器与底层控制器不匹配导致的指令跟踪差、分布偏移及任务失败等挑战。
method: 提出一种分层框架，利用基于 PPO 的扩散策略梯度同时微调高层 DP 规划器和底层 RL 控制器，实现两者的联合优化。
result: "在仿真中实现了超过 90% 的任务成功率，并成功在真实世界动态环境中完成了平滑的自主任务执行。"
conclusion: 研究证明强化学习微调是提升人形机器人移动操作可靠性、克服离线预训练数据局限性的关键手段。
---

## 摘要
类人机器人的移动-操作协同（loco-manipulation）需要在复杂的机器人-环境动力学和长时程任务下，将协调的高层运动规划与稳定的底层全身执行相结合。虽然扩散策略（DPs）在从演示中学习方面展现出潜力，但在类人机器人上部署它们面临着严峻挑战：离线训练的运动规划器与底层控制器是解耦的，这会导致指令跟踪效果差、分布偏移累积以及任务失败。对于高维类人系统而言，通过扩展演示数据规模的常规方法成本极高。为了应对这一挑战，我们提出了 REFINE-DP（扩散策略的强化学习微调），这是一个联合优化 DP 高层规划器和基于 RL 的底层移动-操作控制器的分层框架。DP 通过基于 PPO 的扩散策略梯度进行微调以提高任务成功率，同时控制器同步更新以精确跟踪规划器不断演化的指令分布，从而减少降低运动质量的分布不匹配问题。我们在执行移动-操作任务（包括穿过门和长时程物体搬运）的类人机器人上验证了 REFINE-DP。REFINE-DP 在仿真中实现了超过 90% 的成功率，即使是在预训练数据中未见的分布外（OOD）情况下也是如此，并能在现实世界的动态环境中实现平滑的自主任务执行。我们提出的方法显著优于预训练的 DP 基准，并证明了 RL 微调是实现可靠类人机器人移动-操作协同的关键。https://refine-dp.github.io/REFINE-DP/

## Abstract
Humanoid loco-manipulation requires coordinated high-level motion plans with stable, low-level whole-body execution under complex robot-environment dynamics and long-horizon tasks. While diffusion policies (DPs) show promise for learning from demonstrations, deploying them on humanoids poses critical challenges: the motion planner trained offline is decoupled from the low-level controller, leading to poor command tracking, compounding distribution shift, and task failures. The common approach of scaling demonstration data is prohibitively expensive for high-dimensional humanoid systems. To address this challenge, we present REFINE-DP (REinforcement learning FINE-tuning of Diffusion Policy), a hierarchical framework that jointly optimizes a DP high-level planner and an RL-based low-level loco-manipulation controller. The DP is fine-tuned via a PPO-based diffusion policy gradient to improve task success rate, while the controller is simultaneously updated to accurately track the planner's evolving command distribution, reducing the distributional mismatch that degrades motion quality. We validate REFINE-DP on a humanoid robot performing loco-manipulation tasks, including door traversal and long-horizon object transport. REFINE-DP achieves an over $90\%$ success rate in simulation, even in out-of-distribution cases not seen in the pre-trained data, and enables smooth autonomous task execution in real-world dynamic environments. Our proposed method substantially outperforms pre-trained DP baselines and demonstrates that RL fine-tuning is key to reliable humanoid loco-manipulation. https://refine-dp.github.io/REFINE-DP/

---

## 论文详细总结（自动生成）

以下是对论文《REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心挑战**：类人机器人的“移动-操作协同”（Loco-manipulation）涉及高维全身控制和长时程任务。现有的扩散策略（Diffusion Policy, DP）虽然能从演示中学习复杂行为，但在类人机器人上部署时存在**规划器与底层控制器脱节**的问题。
*   **研究动机**：
    *   **分布偏移（Distribution Shift）**：离线训练的 DP 在实际执行时会产生累积误差，导致任务失败。
    *   **数据成本**：对于高维类人系统，通过大规模采集演示数据来覆盖所有场景（Scaling）成本极其昂贵。
    *   **控制不匹配**：底层控制器往往无法完美跟踪高层规划器生成的动态指令，导致运动抖动或精度下降。
*   **目标**：提出一种分层框架，通过强化学习（RL）微调来解决分布偏移，并实现规划与控制的联合优化。

### 2. 方法论
*   **核心思想**：采用“高层规划+底层控制”的分层架构，并利用基于 PPO 的扩散策略梯度（DPPO）同时微调高层 DP 和底层 RL 控制器。
*   **关键技术细节**：
    *   **分层框架**：高层 DP 输出低维指令（基座速度、手部 6D 姿态）；底层 RL 控制器负责全身动力学执行（足端放置、手臂跟踪）。
    *   **DPPO 微调**：将扩散过程的去噪步骤嵌入到马尔可夫决策过程（MDP）中，使不可微的扩散采样过程变得可优化，从而利用环境奖励直接更新 DP 参数。
    *   **联合优化（Joint Optimization）**：这是本文的核心创新。在微调 DP 的同时，也更新底层控制器的参数。这使得控制器能够“适应”规划器不断演变的指令分布，从而减少跟踪误差并提升运动平滑度。
    *   **数据增强**：结合 VR 遥操作（50 条）和启发式规划器生成的数据（扩展至 1000 条）进行预训练。

### 3. 实验设计
*   **实验场景**：
    1.  **物体抓取**：基础移动与操纵。
    2.  **长程搬运**：跨越 40 秒的长时程拾取与放置任务。
    3.  **门禁穿越**：自主开门并走过去（类人机器人领域的高难任务）。
    4.  **台阶辅助检索**：在非平整地面（台阶）上移动并获取物体。
*   **基准方法（Benchmarks）**：
    *   **DiT Planner**：基于 Transformer 的预训练扩散策略（不微调）。
    *   **MLP / MLP-FT**：基于 MLP 的确定性策略及其微调版本。
    *   **Residual RL**：在冻结的 DP 之上学习残差补偿的基准方法。
*   **硬件平台**：Booster T1 类人机器人（29 自由度）。

### 4. 资源与算力
*   **计算硬件**：
    *   **策略计算机**：AMD 7945HX CPU + NVIDIA RTX 4060 GPU。
    *   **推理加速**：使用 NVIDIA TensorRT 对 DP 进行加速，使其运行在 10Hz。
*   **训练细节**：底层控制器运行频率为 50Hz。文中未明确给出总的训练时长（GPU-hours），但提到联合优化相比单独微调 DP，达到 90% 成功率所需的迭代次数减少了一半（从 40 次降至 20 次）。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 类任务，每类任务在仿真中进行了 100 次独立测试。
*   **充分性**：
    *   **消融实验**：对比了不同骨干网络（Transformer vs MLP）和不同微调策略（联合优化 vs 仅微调规划器）。
    *   **分布外（OOD）测试**：专门设计了课程学习（Curriculum）来测试机器人在未见过的初始位置和随机化环境下的表现。
    *   **真实验证**：在真实物理环境下验证了开门和物体搬运，证明了算法的 Sim-to-Real 迁移能力。
*   **评价**：实验设计较为全面，涵盖了成功率、跟踪精度、运动平滑度和数据效率等多个维度，对比实验具有说服力。

### 6. 主要结论与发现
*   **成功率大幅提升**：REFINE-DP 将任务成功率从预训练的 50-70% 提升至 **90% 以上**。
*   **联合优化的必要性**：联合优化不仅提高了成功率，还使手部位置误差显著降低，姿态误差降低了 **50%**，且运动更加平滑（速度波动更小）。
*   **极高的数据效率**：使用 **50 条** 演示数据进行微调后的效果，优于使用 **1000 条** 数据仅进行预训练的效果。
*   **自主纠错能力**：在真实实验中，微调后的策略展现出重试机制（如开门没抓准时会自动调整脚步重新尝试），而预训练策略往往会卡死。

### 7. 优点
*   **闭环优化**：通过联合优化解决了分层架构中常见的“规划与控制不匹配”问题。
*   **鲁棒性强**：能够处理分布外（OOD）场景，减少了对海量专家数据的依赖。
*   **实用性高**：在真实人形机器人上实现了长时程、多阶段的复杂任务，且具备一定的动态环境适应能力。

### 8. 不足与局限
*   **感知局限**：目前主要依赖运动捕捉系统（MoCap）获取物体状态，尚未完全过渡到纯视觉（Vision-based）闭环。
*   **Sim-to-Real 差距**：虽然仿真成功率极高，但真实环境受限于传感器噪声和地面摩擦，成功率（50-70%）仍有提升空间。
*   **任务复杂度**：虽然涵盖了开门等任务，但尚未涉及更复杂的全身接触动力学（如攀爬或双手协同操作重物）。

（完）
