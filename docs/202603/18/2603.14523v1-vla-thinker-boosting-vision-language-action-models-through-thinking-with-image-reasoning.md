---
title: "VLA-Thinker: Boosting Vision-Language-Action Models through Thinking-with-Image Reasoning"
title_zh: VLA-Thinker：通过以图思考推理增强视觉-语言-动作模型
authors: "Chaoyang Wang, Wenrui Bao, Sicheng Gao, Bingxin Xu, Yu Tian, Yogesh S. Rawat, Yunhao Ge, Yuzhang Shang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14523v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 基于GRPO强化学习的VLA模型，用于长程任务推理
tldr: 本研究针对具身智能中的视觉-语言-动作（VLA）模型，提出了VLA-Thinker框架。该框架引入了“以图思考”推理机制，将感知建模为可动态调用的推理动作，解决了传统模型将视觉输入视为静态背景的局限。通过SFT冷启动和GRPO强化学习两阶段训练，模型在LIBERO和RoboTwin 2.0基准测试中表现卓越，显著提升了复杂长程任务的成功率。
selection_source: fresh_fetch
motivation: 现有VLA模型多依赖静态视觉输入的文本推理，限制了模型在长程任务中主动感知环境和消除歧义的能力。
method: 提出一种将感知作为动态推理动作的框架，结合视觉思维链SFT冷启动与GRPO强化学习来优化推理-动作轨迹。
result: "在LIBERO基准测试中实现了97.5%的成功率，并在RoboTwin 2.0等长程机器人任务中取得了显著的性能增益。"
conclusion: 通过将感知整合为动态推理过程并利用强化学习对齐，VLA-Thinker显著增强了具身智能在复杂环境下的操作性能。
---

## 摘要
视觉-语言-动作（VLA）模型在具身智能方面展现出了巨大的潜力，但现有的大多数方法依赖于基于文本的思维链推理，其中视觉输入被视为静态上下文。这限制了模型在长程任务中主动重新观察环境并消除歧义的能力。我们提出了 VLA-Thinker，这是一个“以图思考”推理框架，它将感知建模为一种可动态调用的推理动作。为了训练该系统，我们引入了一个两阶段训练流水线，包括：(1) 使用精选视觉思维链数据的 SFT 冷启动阶段，以激活结构化推理和工具使用行为；(2) 基于 GRPO 的强化学习，使完整的推理-动作轨迹与任务级成功保持一致。在 LIBERO 和 RoboTwin 2.0 基准测试上的广泛实验表明，VLA-Thinker 显著提升了操作性能，在 LIBERO 上实现了 97.5% 的成功率，并在长程机器人任务中取得了显著提升。项目与代码：https://cywang735.github.io/VLA-Thinker/。

## Abstract
Vision-Language-Action (VLA) models have shown promising capabilities for embodied intelligence, but most existing approaches rely on text-based chain-of-thought reasoning where visual inputs are treated as static context. This limits the ability of the model to actively revisit the environment and resolve ambiguities during long-horizon tasks. We propose VLA-Thinker, a thinking-with-image reasoning framework that models perception as a dynamically invocable reasoning action. To train such a system, we introduce a two-stage training pipeline consisting of (1) an SFT cold-start phase with curated visual Chain-of-Thought data to activate structured reasoning and tool-use behaviors, and (2) GRPO-based reinforcement learning to align complete reasoning-action trajectories with task-level success. Extensive experiments on LIBERO and RoboTwin 2.0 benchmarks demonstrate that VLA-Thinker significantly improves manipulation performance, achieving 97.5% success rate on LIBERO and strong gains across long-horizon robotic tasks. Project and Codes: https://cywang735.github.io/VLA-Thinker/ .

---

## 论文详细总结（自动生成）

以下是对论文《VLA-Thinker: Boosting Vision-Language-Action Models through Thinking-with-Image Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在引入思维链（CoT）推理时，大多局限于“以文本为中心”的范式。在这种范式下，视觉观察被一次性编码为静态上下文，而推理仅在语言空间展开。
*   **研究动机**：静态视觉编码限制了模型在长程操作任务中主动重新审视环境、消除视觉歧义或从中间错误中恢复的能力。人类在操作时是动态、迭代且主动感知的，论文旨在赋予 VLA 模型这种“边思考边观察”的能力。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **VLA-Thinker** 框架，将视觉感知建模为推理过程中可显式、动态调用的“推理动作”（Reasoning Action）。模型不再是被动接收图像，而是通过工具调用主动获取任务相关的视觉信息。
*   **关键技术细节**：
    *   **以图思考推理（Thinking-with-Image）**：引入 `ZOOM-IN`（放大）工具。模型在生成动作前，可以产生多步推理，其中包含对特定区域（Bounding Box）的视觉查询请求，系统返回局部高清子图并更新推理上下文。
    *   **两阶段训练流水线**：
        1.  **SFT 冷启动阶段**：利用 Qwen3-VL-30B 合成高质量的具身 CoT 数据（包含推理步骤和工具调用格式），解决现有数据集缺乏推理轨迹的问题，激活模型的结构化推理和工具使用能力。
        2.  **GRPO 强化学习阶段**：采用群体相对策略优化（Group Relative Policy Optimization, GRPO）。通过采样多条推理-动作轨迹，利用最终的任务成功信号（Sparse Reward）和格式奖励，对整个轨迹进行因果对齐，优化模型“何时调用工具”和“如何推理”。
*   **算法流程**：输入指令和图像 -> 模型生成 `<think>` 文本 -> 决定是否调用 `<tool>` -> 获取新视觉证据 -> 继续推理 -> 最终输出环境动作 $A$。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程）四个子集，共计 130 个任务。
    *   **RoboTwin 2.0**：具有强领域随机化的双臂协作模拟基准，涵盖从短程（约 120 步）到超长程（约 550 步）的 12 个代表性任务。
*   **对比方法**：
    *   **基准模型**：OpenVLA, OpenVLA-OFT。
    *   **先进模型**：π0, π0-FAST, RDT, DeepThinkVLA, CoT-VLA, UniVLA 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：完整的两阶段训练过程大约耗时 **3 天**。
*   **模型规格**：基于 Llama2-7B 后干网络，初始化自 OpenVLA-OFT 权重。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 上对每个任务进行了 50 次随机初始条件的评估。
    *   在 RoboTwin 2.0 上对每个任务进行了 100 次随机初始条件的评估。
    *   进行了消融实验，分别验证了仅 SFT、仅 GRPO 以及完整 VLA-Thinker 的表现。
*   **充分性与客观性**：实验覆盖了单臂与双臂、简单与复杂长程任务，对比了当前最前沿的多个 VLA 模型。通过分层评估（按任务步数分类）展示了模型在不同难度下的稳定性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能突破**：VLA-Thinker 在 LIBERO 基准测试中达到了 **97.5%** 的平均成功率，刷新了 SOTA 记录，比基准 OpenVLA-OFT 提升了 6.5%。
*   **长程任务优势**：在 RoboTwin 2.0 的长程和超长程任务中，VLA-Thinker 的领先优势扩大，证明了动态感知能有效抑制长推理链中的误差累积。
*   **推理效率优化**：RL 训练曲线显示，随着训练进行，模型学会了更选择性地调用工具，减少了冗余推理，响应长度逐渐缩短。

### 7. 优点（亮点）
*   **范式创新**：打破了“感知-推理-动作”的线性顺序，实现了感知与推理的交织（Interleaved），使感知变得“主动”。
*   **训练稳定性**：通过 SFT 冷启动为强化学习提供了良好的先验，解决了在稀疏奖励下直接训练复杂推理轨迹难以收敛的问题。
*   **测试时缩放（Test-time Scaling）**：通过增加推理步数和感知次数换取更高的任务成功率，展示了具身智能领域的 Scaling Law 潜力。

### 8. 不足与局限
*   **工具多样性有限**：目前仅验证了 `ZOOM-IN` 这一种视觉工具，虽然证明了框架可行，但应对更复杂环境可能需要更多元化的感知工具（如分割、深度查询等）。
*   **推理开销**：由于采用了自回归推理，推理时间比纯反应式模型增加了约 19%，在对实时性要求极高的场景下可能存在挑战。
*   **幻觉风险**：作为基于多模态大语言模型（MLLM）的系统，仍可能继承大模型的幻觉问题，导致在推理中引用错误的物体属性。

（完）
