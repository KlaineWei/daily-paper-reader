---
title: "AnoleVLA: Lightweight Vision-Language-Action Model with Deep State Space Models for Mobile Manipulation"
title_zh: AnoleVLA：用于移动操作的基于深度状态空间模型的轻量级视觉-语言-动作模型
authors: "Yusuke Takagi, Motonari Kambara, Daichi Yashima, Koki Seno, Kento Tokura, Komei Sugiura"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15046v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 针对资源受限环境的使用状态空间模型的轻量级VLA
tldr: "针对语言引导的机器人操作任务，为解决标准Transformer架构在资源受限环境下计算成本高的问题，本文提出了AnoleVLA。该模型采用深层状态空间模型（SSM）处理多模态序列，实现了轻量化和高效推理。实验表明，AnoleVLA在真实场景中的成功率比大型VLA高出21%，且推理速度快约三倍，显著提升了移动操作的效率。"
selection_source: fresh_fetch
motivation: 传统的基于Transformer的视觉-语言-动作模型（VLA）计算开销巨大，难以在资源受限的移动机器人平台上实时部署。
method: 提出AnoleVLA模型，利用深层状态空间模型（SSM）替代Transformer来高效处理视觉、文本和动作序列。
result: 在真实世界评估中，AnoleVLA的成功率比代表性大型VLA高出21个百分点，且推理速度提升了约三倍。
conclusion: AnoleVLA证明了通过状态空间模型构建轻量化VLA的可行性，为高效、实时的机器人多模态任务提供了新方案。
---

## 摘要
在本研究中，我们探讨了语言引导的机器人操作问题，即机器人需要根据视觉观察和自然语言指令来操作各种物体。这一任务对于在人类环境中运行的服务机器人至关重要，且要求具备安全性、效率和任务级的通用性。尽管视觉-语言-动作模型（VLA）在该任务中展现出强大的性能，但由于标准 Transformer 骨干网络的计算开销，它们在资源受限环境中的部署仍然面临挑战。为了克服这一局限性，我们提出了 AnoleVLA，这是一种轻量级的 VLA，它采用深度状态空间模型来高效处理多模态序列。该模型利用其轻量且快速的序列状态建模能力来处理视觉和文本输入，从而使机器人能够高效地生成运动轨迹。我们在仿真和物理实验中对所提方法进行了评估。值得注意的是，在真实世界的评估中，AnoleVLA 的任务成功率比代表性的大规模 VLA 高出 21 个百分点，同时推理速度提升了约三倍。

## Abstract
In this study, we address the problem of language-guided robotic manipulation, where a robot is required to manipulate a wide range of objects based on visual observations and natural language instructions. This task is essential for service robots that operate in human environments, and requires safety, efficiency, and task-level generality. Although Vision-Language-Action models (VLAs) have demonstrated strong performance for this task, their deployment in resource-constrained environments remains challenging because of the computational cost of standard transformer backbones. To overcome this limitation, we propose AnoleVLA, a lightweight VLA that uses a deep state space model to process multimodal sequences efficiently. The model leverages its lightweight and fast sequential state modeling to process visual and textual inputs, which allows the robot to generate trajectories efficiently. We evaluated the proposed method in both simulation and physical experiments. Notably, in real-world evaluations, AnoleVLA outperformed a representative large-scale VLA by 21 points for the task success rate while achieving an inference speed approximately three times faster.

---

## 论文详细总结（自动生成）

以下是对论文《AnoleVLA: Lightweight Vision-Language-Action Model with Deep State Space Models for Mobile Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对**语言引导的机器人操作**任务，旨在解决现有视觉-语言-动作模型（VLA）在移动机器人上部署困难的问题。
*   **研究动机**：传统的 VLA（如 OpenVLA、RT-2）通常依赖 Transformer 架构，其自注意力机制的计算复杂度随序列长度呈平方级增长（$O(L^2)$）。这导致在处理包含多模态（视觉、语言、状态）的长序列时，推理延迟高且显存占用大，难以满足移动机器人实时控制的需求。
*   **核心目标**：开发一种轻量级、高速度且具备任务通用性的 VLA 模型，使其能在资源受限的硬件平台上高效运行。

### 2. 方法论
论文提出了 **AnoleVLA**，其核心思想是利用**深度状态空间模型（Deep SSM/Mamba）**替代 Transformer 作为骨干网络。
*   **核心架构**：
    *   **输入嵌入层**：将机器人当前状态（Proprioception）、状态增量（Delta State）、视觉图像（通过预训练的 SigLIP 2 编码）和自然语言指令拼接成多模态序列。
    *   **Mamba 骨干网络**：采用 Mamba 块（包含选择性 SSM 层、SiLU 门控单元等），利用其线性计算复杂度（$O(L)$）实现高效的序列建模。
    *   **动作头**：直接从骨干网络的最后一个隐藏状态通过线性投影生成连续的动作块（Action Chunk），无需离散化分词。
*   **关键技术：两阶段训练策略**：
    *   **第一阶段**：使用速度损失（Velocity Loss，$L_1$ 范数）监督预测动作与真实轨迹的差异。
    *   **第二阶段**：引入加速度损失（Acceleration Loss），监督动作序列的时间差分，以提升机器人执行动作的平滑度和稳定性。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：使用 Meta-World 基准测试，包含 50 种不同的物体操作任务（如开门、移动物体等）。
    *   **物理实验**：使用丰田 HSR 移动机器人，在模拟家庭环境（WRS2020RS 布局）中执行移动、抓取、开关抽屉、按压泵头等任务。
*   **对比基准（Benchmarks）**：
    *   **大型模型**：$\pi_{0.5}$ (3.0B 参数)。
    *   **同规模轻量级模型**：SmolVLA (0.45B)、VLA-Adapter (0.5B)、TinyVLA (0.42B)。
*   **评估指标**：任务成功率（Success Rate）和推理速度（Inference Speed）。

### 4. 资源与算力
*   **硬件配置**：使用单张 **NVIDIA GeForce RTX 4090 GPU (24GB)** 和 Intel Core i9-14900F 处理器。
*   **训练时长**：总训练时间约为 **20 小时**（两阶段各 200k 步，共 400k 步）。
*   **模型规模**：参数量约为 **0.47B**（约 4.7 亿），计算量为 91 GFLOPs。
*   **推理性能**：推理速度为 **216 ms/chunk**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真：使用了 Meta-World 的 2500 个情节（Episodes）进行训练。
    *   实机：收集了 250 个情节（每类任务 50 个），并在实机上进行了 100 次测试（每类任务 20 次）。
*   **消融实验**：针对“加速度损失”进行了对比实验，证明了两阶段训练对提升成功率（提升约 4.7%）的有效性。
*   **充分性评价**：实验覆盖了从仿真到实机的迁移，对比了多种主流轻量级 VLA，并进行了详细的错误分析（归纳为位置识别错误、抓取点预测错误等），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能卓越**：在 Meta-World 仿真中，AnoleVLA 的平均成功率（67.85%）显著优于所有对比模型（包括 3B 规模的 $\pi_{0.5}$）。
*   **实机优势**：在真实世界测试中，AnoleVLA 的成功率比 $\pi_{0.5}$ 高出 **21 个百分点**，且推理速度快了约 **3 倍**。
*   **平滑性**：加速度损失的引入有效减少了机器人动作的抖动，使其在接触密集型任务（如开抽屉）中表现更佳。
*   **样本效率**：在仅有少量实机演示数据（每项任务 50 条）的情况下，模型表现出极强的适应能力。

### 7. 优点
*   **高效性**：利用 Mamba 的线性复杂度解决了 Transformer 在长序列下的性能瓶颈，非常适合实时机器人控制。
*   **轻量化**：在不到 0.5B 的参数规模下实现了超越 3B 模型的性能，降低了部署门槛。
*   **控制质量**：通过两阶段训练策略关注轨迹的二阶导数（加速度），提升了物理执行的平滑度。

### 8. 不足与局限
*   **空间推理能力有限**：错误分析显示，最主要的失败原因是“位置识别错误”，表明模型在精确提取物体空间坐标方面仍有提升空间。
*   **数据多样性**：实机实验的数据集规模相对较小（250 个情节），且仅在单一机器人平台（HSR）上验证，跨平台泛化性有待进一步研究。
*   **任务复杂度**：虽然涵盖了多种任务，但主要集中在短时域的操作，对于需要极长时序推理的复杂任务表现尚不明确。

（完）
