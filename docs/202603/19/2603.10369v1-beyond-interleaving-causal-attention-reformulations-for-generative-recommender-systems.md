---
title: "Beyond Interleaving: Causal Attention Reformulations for Generative Recommender Systems"
title_zh: 超越交织：生成式推荐系统的因果注意力重构
authors: Hailing Cheng
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10369v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 生成式推荐系统的因果注意力重构
tldr: "本研究针对生成式推荐系统中“项目-动作”交替序列导致的计算效率低下和信号干扰问题，提出了基于因果注意力重构的新范式。通过引入AttnLFA和AttnMVP两种架构，将序列长度缩减50%，并显式建模项目与动作间的因果依赖。实验证明，该方法在提升推荐准确度的同时，显著降低了训练耗时，为构建高效且可扩展的生成式排序系统提供了重要参考。"
selection_source: fresh_fetch
motivation: 传统的交替序列建模方式导致序列长度翻倍、计算开销呈平方级增长，且难以有效捕捉项目与动作间的因果关系。
method: "提出了AttnLFA和AttnMVP两种新架构，通过注意力机制显式编码项目与动作的依赖关系，从而将序列复杂度降低了50%。"
result: "实验结果显示，新架构在提升推荐性能（如降低评估损失和NE）的同时，分别减少了23%和12%的训练时间。"
conclusion: 显式建模项目与动作的因果关系比传统的交替序列建模更具效率和表现力，是生成式推荐系统设计的更优范式。
---

## 摘要
生成式推荐系统 (GR) 越来越多地通过交织项目 (item) 和动作 (action) 标记，将用户行为建模为序列生成任务。尽管这种方式行之有效，但该公式引入了显著的结构和计算效率低下问题：它使序列长度翻倍，产生了平方级的开销，并依赖隐式注意力来恢复项目与其相关动作之间的因果关系。此外，交织异构标记迫使 Transformer 解耦语义不兼容的信号，导致注意力噪声增加并降低了表示效率。在这项工作中，我们提出了一种原则性的生成式推荐重构方法，使序列建模与底层的因果结构和注意力理论保持一致。我们证明了当前的交织机制实际上是相似度加权动作池化的一种低效代理。为解决这一问题，我们引入了两种新型架构，通过消除交织依赖关系将序列复杂度降低了 50%：基于注意力的动作后期融合 (AttnLFA) 和基于注意力的混合值池化 (AttnMVP)。这些模型显式地编码了 $i_n \rightarrow a_n$ 的因果依赖关系，同时保留了基于 Transformer 的序列建模的表达能力。我们在来自某大型社交网络的规模化产品推荐数据上评估了我们的框架。实验结果表明，AttnLFA 和 AttnMVP 一致优于交织基准模型，评估损失分别改善了 0.29% 和 0.80%，并在归一化熵 (NE) 方面取得了显著提升。至关重要的是，这些性能提升的同时，训练时间分别减少了 23% 和 12%。我们的研究结果表明，显式建模项目-动作因果关系为可扩展且高效的生成式排序提供了一种卓越的设计范式。

## Abstract
Generative Recommender Systems (GR) increasingly model user behavior as a sequence generation task by interleaving item and action tokens. While effective, this formulation introduces significant structural and computational inefficiencies: it doubles sequence length, incurs quadratic overhead, and relies on implicit attention to recover the causal relationship between an item and its associated action. Furthermore, interleaving heterogeneous tokens forces the Transformer to disentangle semantically incompatible signals, leading to increased attention noise and reduced representation efficiency.In this work, we propose a principled reformulation of generative recommendation that aligns sequence modeling with underlying causal structures and attention theory. We demonstrate that current interleaving mechanisms act as inefficient proxies for similarity-weighted action pooling. To address this, we introduce two novel architectures that eliminate interleaved dependencies to reduce sequence complexity by 50%: Attention-based Late Fusion for Actions (AttnLFA) and Attention-based Mixed Value Pooling (AttnMVP). These models explicitly encode the $i_n \rightarrow a_n$ causal dependency while preserving the expressive power of Transformer-based sequence modeling.We evaluate our framework on large-scale product recommendation data from a major social network. Experimental results show that AttnLFA and AttnMVP consistently outperform interleaved baselines, achieving evaluation loss improvements of 0.29% and 0.80%, and significant gains in Normalized Entropy (NE). Crucially, these performance gains are accompanied by training time reductions of 23% and 12%, respectively. Our findings suggest that explicitly modeling item-action causality provides a superior design paradigm for scalable and efficient generative ranking.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Interleaving: Causal Attention Reformulations for Generative Recommender Systems》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前的生成式推荐系统（如 Meta 的 HSTU）通常采用“项目-动作”交替（Interleaving）的序列建模方式（即 $[i_0, a_0, i_1, a_1, \dots]$），将推荐问题转化为类似 LLM 的 Token 生成任务。
*   **核心问题**：
    *   **计算低效**：交替序列使序列长度翻倍，由于 Transformer 的平方复杂度，计算和内存开销增加约 4 倍。
    *   **语义异构噪声**：项目（视频/帖子）与动作（点击/点赞）属于完全不同的语义空间。强制将它们混在一起会让模型难以解耦信号，产生注意力噪声。
    *   **因果关系模糊**：标准注意力机制是对称的，难以显式捕捉“特定项目 $i_n$ 诱发特定动作 $a_n$”这一核心因果逻辑，往往依赖位置编码来隐式学习。

### 2. 方法论
论文提出了两种重构架构，旨在将序列长度缩减 50%，并显式建模因果依赖：
*   **核心思想**：将推荐建模为“以项目为条件的动作池化”任务，即利用项目相似度来加权聚合历史动作。
*   **AttnLFA（动作后期融合注意力）**：
    *   项目序列独立通过 Transformer 编码。
    *   最后一层将项目表示作为 Query 和 Key，将动作嵌入作为 Value 进行注意力池化。
    *   **关键技术**：采用**查询偏移机制（Query-shifting）**，通过将 Query 序列向左偏移一位并配合 FlashAttention 的因果掩码，严格确保 $i_n$ 只能看到 $\{a_0, \dots, a_{n-1}\}$，防止标签泄露。
*   **AttnMVP（混合值池化注意力）**：
    *   **早期融合**：在 Transformer 的每一层中，将项目表示与动作嵌入相加作为混合 Value（$V = H + \lambda A$）。
    *   **渐进学习**：通过多层堆叠，使项目表示从纯内容语义演变为“用户偏好感知的语义”（例如从“狗”演变为“用户喜欢的狗”）。
*   **AttnDHN（双螺旋网络，探索性）**：对称的双流架构，同时更新项目和动作表示，但目前在稳定性上存在挑战。

### 3. 实验设计
*   **数据集/场景**：来自大型专业社交网络（LinkedIn）的规模化产品推荐日志，包含过去 12 个月的用户交互，序列长度高达 1024。
*   **基准模型（Benchmark）**：标准的交替式生成推荐器（Interleaved Ranker）。
*   **评估指标**：多任务二元交叉熵损失（BCE Loss）、归一化熵（NE）。
*   **预测任务**：长停留时间（Long Dwell）、贡献行为（Contribution，如转发/评论）、点赞（Like）等 12 个任务。

### 4. 资源与算力
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 A100 或 H100）或具体集群规模。
*   **效率提升**：明确提到了训练耗时的减少。AttnLFA 相比基准减少了 **22.8%** 的训练时间，AttnMVP 减少了 **12.3%**。
*   **实现细节**：使用了 FlashAttention 内核以优化吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：在工业级大规模数据集上进行，具有较强的说服力。
*   **消融实验**：对比了 AttnMVP 与去掉后期融合池化的版本（AttnMVP-LFA），验证了早期融合是性能提升的主因。
*   **客观性**：所有模型使用相同的超参数、嵌入层、Transformer 层数（12层）和旋转位置编码（RoPE），确保了对比的公平性。

### 6. 主要结论与发现
*   **性能超越**：AttnMVP 在评估损失上比基准提升了 **0.8%**，NE 指标在各项任务中均有显著改善。
*   **因果建模的优越性**：显式建模 $i_n \rightarrow a_n$ 的因果关系比简单的 Token 交替更有效。
*   **效率增益**：通过将序列长度减半，显著降低了计算开销，证明了该架构在生产环境中的可扩展性。
*   **早期融合更佳**：AttnMVP 优于 AttnLFA，说明在表示学习的早期阶段引入动作信号有助于捕捉更深层的用户偏好。

### 7. 优点
*   **理论与实践结合**：从因果结构和信息论角度重新审视了推荐序列建模，而非盲目套用 NLP 的建模方式。
*   **计算友好**：通过查询偏移机制完美适配 FlashAttention，在提升精度的同时大幅降低了工业界关注的训练成本。
*   **解决噪声问题**：通过解耦异构标记，减少了由于位置邻近导致的虚假注意力关联。

### 8. 不足与局限
*   **数据单一性**：实验仅在 LinkedIn 这一特定社交网络数据上完成，尚未在电商（如 Amazon）或短视频（如 TikTok）等其他公开数据集上验证泛化性。
*   **双流架构不成熟**：提出的 AttnDHN 架构在训练稳定性上表现不佳，需要减小学习率才能收敛，限制了其当前的应用。
*   **语义空间差异**：动作空间（通常几十个）与项目空间（海量）的极度不对称，仍是限制更复杂双向交互建模的瓶颈。

（完）
