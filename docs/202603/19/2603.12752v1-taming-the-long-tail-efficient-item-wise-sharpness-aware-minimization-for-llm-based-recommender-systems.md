---
title: "Taming the Long Tail: Efficient Item-wise Sharpness-Aware Minimization for LLM-based Recommender Systems"
title_zh: 驯服长尾：针对基于大语言模型推荐系统的高效逐项目锐度感知最小化
authors: "Jiaming Zhang, Yuyuan Li, Xiaohua Feng, Li Zhang, Longfei Li, Jun Zhou, Chaochao Chen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12752v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 基于大模型的序列推荐与长尾问题
tldr: 针对基于大语言模型的推荐系统（LRS）面临的长尾问题，本文揭示了其受预训练先验和推荐数据分布的双重影响。为此，提出了一种高效的项级锐度感知最小化（EISAM）优化框架。该框架通过自适应调节不同物品的损失平面平滑度，在保持计算效率的同时显著提升了长尾物品的推荐性能，并提供了理论泛化界限证明。
selection_source: fresh_fetch
motivation: 现有的LRS在处理由预训练语料和推荐数据集共同导致的双重长尾分布时表现不佳，尤其是尾部物品性能受限。
method: 提出EISAM框架，通过引入高效的项级惩罚项来捕捉细粒度的物品锐度，从而实现对损失平面的自适应正则化。
result: 在三个真实数据集上的实验表明，EISAM在不损害整体推荐质量的前提下，显著提升了长尾物品的预测准确率。
conclusion: 本研究为LRS的长尾问题提供了首个系统性的解决方案，并通过理论与实验验证了项级锐度优化在提升泛化能力方面的有效性。
---

## 摘要
基于大语言模型的推荐系统（LRSs）最近通过直接采用大语言模型（LLMs）作为骨干网络，成为了序列推荐中的一种新范式。虽然 LRSs 展示了强大的知识利用和指令遵循能力，但尚未在长期存在的长尾问题下得到系统研究。在本文中，我们进行了一项实证研究，揭示了 LRSs 面临两种截然不同的长尾类型：i) 先验长尾，隐式地继承自预训练语料库；ii) 数据长尾，源于偏斜的推荐数据集。我们的分析表明，两者都导致了头部和尾部项目之间的性能差异，且两个头部的交集表现出更强的头部效应。尽管如此，LRSs 的整体性能分布，特别是在尾部，仍然由数据长尾主导。为了应对这一挑战，我们提出了高效逐项目锐度感知最小化（EISAM），这是一种新型优化框架，通过在项目级别自适应地正则化损失景观来提高尾部项目的性能。EISAM 引入了一种高效的惩罚设计，在保持 LLMs 计算可扩展性的同时，捕捉细粒度的项目特定锐度。此外，我们推导了 EISAM 的泛化界。我们的理论分析表明，在我们的逐项目正则化下，该界限以更快的速度下降，为其有效性提供了理论支持。在三个真实世界数据集上的广泛实验表明，EISAM 在保持整体质量的同时显著提升了尾部项目的推荐性能，为 LRSs 中的长尾问题建立了第一个系统性解决方案。

## Abstract
Large Language Model-based Recommender Systems (LRSs) have recently emerged as a new paradigm in sequential recommendation by directly adopting LLMs as backbones. While LRSs demonstrate strong knowledge utilization and instruction-following abilities, they have not been systematically studied under the long-standing long-tail problem. In this paper, we conduct an empirical study and reveal that LRSs face two distinct types of long-tail: i) prior long-tail, inherited implicitly from pretraining corpora, and ii) data long-tail, originating from skewed recommendation datasets. Our analysis shows that both contribute to the performance disparity between head and tail items, with the intersection of the two heads exhibiting an even stronger head effect. Nevertheless, the overall performance distribution in LRSs, especially on the tail, remains dominated by the data long-tail. To address this challenge, we propose Efficient Item-wise Sharpness-Aware Minimization (EISAM), a novel optimization framework that improves tail-item performance by adaptively regularizing the loss landscape at the item level. EISAM introduces an efficient penalty design that captures fine-grained item-specific sharpness while maintaining computational scalability for LLMs. In addition, we derive a generalization bound for EISAM. Our theoretical analysis shows that the bound decreases at a faster rate under our item-wise regularization, offering theoretical support for its effectiveness. Extensive experiments on three real-world datasets demonstrate that EISAM significantly boosts tail-item recommendation performance while preserving overall quality, establishing the first systematic solution to the long-tail problem in LRSs.

---

## 论文详细总结（自动生成）

这篇论文针对基于大语言模型的推荐系统（LRS）中的长尾问题进行了深入研究，并提出了一种高效的优化框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然基于大语言模型（LLM）的推荐系统（LRS）在知识利用和指令遵循方面表现出色，但其在**长尾分布**下的表现尚未得到系统研究。
*   **研究动机**：作者发现 LRS 面临双重长尾挑战：
    1.  **先验长尾（Prior Long-tail）**：源自 LLM 预训练语料库的隐式偏差。
    2.  **数据长尾（Data Long-tail）**：源自推荐系统训练集中严重的交互偏斜。
*   **实证发现**：数据长尾是主导因素。尽管 LLM 的先验知识能增强头部性能，但一旦物品处于数据分布的尾部，其推荐准确率会急剧下降。

### 2. 方法论：核心思想与技术细节
论文提出了 **EISAM（Efficient Item-wise Sharpness-Aware Minimization）** 框架：
*   **核心思想**：通过在**项目级别（Item-wise）**自适应地调节损失平面的平滑度（Sharpness），提升模型对长尾物品的泛化能力。
*   **关键技术**：
    *   **项级锐度正则化**：定义了每个物品的损失锐度，并引入频率感知权重函数 $f(q_i)$（如指数形式），赋予低频（尾部）物品更高的正则化权重。
    *   **高效优化流程**：
        1.  **扰动构建**：利用一阶泰勒展开近似损失变化，计算使加权损失最大的参数扰动 $\epsilon$。
        2.  **梯度近似**：为了避免计算高阶 Hessian 矩阵，采用类似 SAM 的简化处理，将扰动视为常量进行反向传播。
        3.  **三步反向传播**：每个迭代步包含三次反向传播（计算扰动、计算原始梯度、计算扰动后的梯度），在保持细粒度控制的同时兼顾了计算效率。
*   **理论支持**：推导了 EISAM 的泛化界限，证明项级正则化能使平衡测试集上的泛化误差以更快的速度下降。

### 3. 实验设计
*   **数据集**：使用了三个真实世界数据集：**ML-1M**（电影）、**Steam**（游戏）、**ADM**（亚马逊数字音乐）。
*   **骨干模型（Backbones）**：采用了 **BIGRec** 和 **TALLRec**，均基于 **Llama2-7B**。
*   **对比方法（Benchmarks）**：
    *   原始 LRS 模型。
    *   **RW (Re-weighting)**：传统的样本重采样/加权。
    *   **SAM**：标准的全局锐度感知最小化。
    *   **GroupSAM**：组级锐度感知最小化（针对公平性设计的变体）。
*   **评估指标**：NDCG@10 和 HR@10，分别针对整体、头部和尾部物品进行统计。

### 4. 资源与算力
*   **微调技术**：使用了 **LoRA** 策略（$r=8, \alpha=16$）以降低显存占用。
*   **训练参数**：Adam 优化器，学习率 $5 \times 10^{-4}$，Batch Size 为 64。
*   **算力说明**：文中**未明确提及具体的 GPU 型号和数量**。但提供了训练时长对比：在 ML-1M 数据集上，EISAM 每轮（epoch）耗时约 3652 秒，比标准 SAM 仅多出约 14% 的开销，远低于 GroupSAM（约 4 倍开销）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集 × 2 个骨干模型 × 4 种对比方法，实验设置较为全面。
*   **充分性**：
    *   **消融与敏感性分析**：详细讨论了超参数 $\lambda$（正则化强度）和 $\gamma$（权重衰减系数）的影响。
    *   **可视化分析**：通过 3D 和 2D 损失平面图直观展示了 EISAM 如何使尾部物品的损失景观变得更平滑。
    *   **客观性**：实验对比了整体性能与长尾性能，证明了该方法在提升尾部的同时没有牺牲头部精度。

### 6. 主要结论与发现
*   **性能提升**：EISAM 在尾部物品上的 NDCG@10 平均提升了 **8.90%**，整体性能也有显著增长。
*   **平滑度与泛化**：实验证实，损失平面的平滑度与长尾物品的泛化能力呈正相关，EISAM 成功压制了尾部物品的锐度。
*   **效率优势**：相比于粗粒度的组级优化（GroupSAM），EISAM 在提供更细粒度控制的同时，计算开销极低，适合大规模 LRS 应用。

### 7. 优点
*   **首创性**：首次系统性地区分并研究了 LRS 中的先验长尾与数据长尾。
*   **理论与实践结合**：不仅提出了算法，还给出了严谨的泛化界限证明。
*   **高效性**：巧妙避开了 Hessian 运算，解决了 LLM 参数量巨大导致难以进行细粒度锐度优化的痛点。

### 8. 不足与局限
*   **模型覆盖**：实验主要基于 Llama2-7B，尚未验证在更大规模（如 70B+）或其他架构（如 MoE）模型上的表现。
*   **权重函数依赖**：虽然测试了三种权重函数，但最佳权重函数（指数型）的参数 $\gamma$ 仍需针对不同数据集进行手动调优。
*   **用户侧长尾**：研究重点在于物品侧（Item-side）的长尾，未涉及用户侧（User-side）活跃度差异带来的长尾挑战。

（完）
