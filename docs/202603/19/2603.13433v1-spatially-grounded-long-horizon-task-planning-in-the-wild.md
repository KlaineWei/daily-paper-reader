---
title: Spatially Grounded Long-Horizon Task Planning in the Wild
title_zh: 真实场景下的空间定位长程任务规划
authors: "Sehun Jung, HyunJee Song, Dong-Hee Kim, Reuben Tan, Jianfeng Gao, Yong Jae Lee, Donghyun Kim"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13433v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 机器人使用VLM进行空间落地的长程任务规划
tldr: 本研究针对视觉语言模型在机器人长程任务规划中缺乏空间接地的问题，提出了新基准GroundedPlanBench，旨在同时评估子动作规划及其具体的空间操作位置。此外，开发了V2GP框架，通过机器人视频演示自动生成数据以增强模型的空间感知规划能力。实验证明，V2GP显著提升了模型在复杂环境下的动作执行准确性，为实现空间可执行的机器人规划提供了新路径。
selection_source: fresh_fetch
motivation: 现有视觉语言模型在长程任务规划中缺乏对具体操作空间位置的评估，限制了其在真实世界中的应用。
method: 提出了GroundedPlanBench基准测试和V2GP自动化数据生成框架，利用机器人视频演示来增强空间感知的动作规划。
result: 实验表明当前VLM在空间接地规划方面存在瓶颈，而V2GP能显著提升模型在动作规划和空间定位上的表现。
conclusion: 该研究通过引入空间接地评估和自动化训练框架，有效推动了机器人实现可执行的空间动作规划。
---

## 摘要
机器人操作的最新进展越来越多地利用视觉语言模型（VLM）进行高层推理，例如将任务指令分解为以自然语言表达的序列动作计划，以指导下游的底层电机执行。然而，目前的基准测试并未评估这些计划是否在空间上可执行，特别是在指定机器人执行计划时应进行交互的精确空间位置方面，这限制了对真实世界操作能力的评估。为了填补这一空白，我们定义了一项名为“定位规划”（grounded planning）的新任务，并推出了 GroundedPlanBench，这是一个专门为真实场景下空间定位长程动作规划而策划的新基准。GroundedPlanBench 联合评估分层子动作规划和空间动作定位（在何处执行动作），从而能够系统地评估生成的子动作对于机器人操作而言是否在空间上可执行。我们进一步引入了“视频到空间定位规划”（Video-to-Spatially Grounded Planning, V2GP），这是一个利用真实机器人视频演示来改进空间定位长程规划的自动化数据生成框架。我们的评估表明，空间定位的长程规划仍然是当前 VLM 的一个主要瓶颈。我们的结果表明，V2GP 为提高动作规划和空间定位性能提供了一种极具前景的方法，这一点在我们的基准测试以及真实世界机器人操作实验中得到了验证，推动了空间可执行规划的进展。

## Abstract
Recent advances in robot manipulation increasingly leverage Vision-Language Models (VLMs) for high-level reasoning, such as decomposing task instructions into sequential action plans expressed in natural language that guide downstream low-level motor execution. However, current benchmarks do not assess whether these plans are spatially executable, particularly in specifying the exact spatial locations where the robot should interact to execute the plan, limiting evaluation of real-world manipulation capability. To bridge this gap, we define a novel task of grounded planning and introduce GroundedPlanBench, a newly curated benchmark for spatially grounded long-horizon action planning in the wild. GroundedPlanBench jointly evaluates hierarchical sub-action planning and spatial action grounding (where to act), enabling systematic assessment of whether generated sub-actions are spatially executable for robot manipulation. We further introduce Video-to-Spatially Grounded Planning (V2GP), an automated data generation framework that leverages real-world robot video demonstrations to improve spatially grounded long-horizon planning. Our evaluations reveal that spatially grounded long-horizon planning remains a major bottleneck for current VLMs. Our results demonstrate that V2GP provides a promising approach for improving both action planning and spatial grounding performance, validated on our benchmark as well as through real-world robot manipulation experiments, advancing progress toward spatially actionable planning.

---

## 论文详细总结（自动生成）

以下是对论文《Spatially Grounded Long-Horizon Task Planning in the Wild》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉语言模型（VLM）虽然擅长将高层指令分解为自然语言动作序列，但这些计划往往缺乏**空间接地（Spatial Grounding）**，即无法明确指出机器人应在何处进行交互。这导致生成的计划在物理空间上存在歧义或幻觉，难以直接指导底层电机执行。
*   **研究背景**：目前的基准测试多侧重于抽象规划（在模拟器中）或单步空间推理（如VQA），缺乏对真实世界、长程（Long-Horizon）且具备空间坐标定位的规划能力的综合评估。

### 2. 方法论：V2GP 框架
论文提出了 **V2GP（Video-to-Spatially Grounded Planning）**，这是一个自动化数据生成框架，旨在从机器人视频演示中提取结构化的空间定位规划数据。其核心流程包括：
1.  **时序子动作分解**：利用机器人夹持器（Gripper）的状态信号（开/合），将连续视频切割成独立的操纵片段。
2.  **交互物体识别**：调用 VLM 分析每个片段，生成被操纵物体的详细视觉描述和语义类别，解决原始指令中的表述模糊问题。
3.  **动作空间接地**：使用 **SAM3** 模型，结合前一步生成的物体描述，自动提取目标物体的 2D 边界框（用于抓取、打开、关闭）和放置终点坐标（点）。
4.  **空间定位任务规划集成**：将语义物体、空间坐标和时序动作整合为结构化样本。同时利用大模型（如 Gemini）将显式指令改写为隐式/抽象指令（如“收拾桌子”），增强模型的推理能力。
5.  **过滤与精炼**：通过物体追踪一致性检查和指令修正，剔除演示失败或检测错误的噪声数据。

### 3. 实验设计
*   **基准测试（GroundedPlanBench）**：
    *   **数据来源**：基于 DROID 真实世界机器人数据集，涵盖 308 个场景。
    *   **规模**：包含 1000 个评估回合，分为短程（1-4步）、中程（5-8步）和长程（9-26步）。
    *   **指令类型**：分为显式（详细步骤）和隐式（抽象目标）。
*   **对比方法**：
    *   **端到端模型**：Gemini-3-Flash, Gemini-2.5-Flash, Qwen3-VL, InternVL3.5。
    *   **解耦方法**：将高层规划（GPT-5.2/Qwen3）与专门的接地模型（SAM3, Embodied-R1）组合。
*   **评估指标**：动作召回率（ARR）和任务成功率（TSR，要求所有步骤的动作类型和空间定位均正确）。

### 4. 资源与算力
*   **模型微调**：论文使用了 **LoRA** 技术对 **Qwen3-VL**（4B 和 32B 版本）进行微调。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的基准测试对比（涵盖 4 种主流闭源/开源模型及其变体），并针对不同任务长度和指令类型进行了细分评估。
*   **消融与对比**：对比了“端到端生成”与“规划+接地解耦”两种范式，验证了 V2GP 带来的性能提升。
*   **真实世界验证**：在 Franka Research 3 机器人上进行了 10 项真实任务测试，证明了生成计划的可执行性。
*   **评价**：实验设计较为全面，涵盖了从仿真评估到真实硬件验证的完整闭环，对比维度客观。

### 6. 主要结论与发现
*   **空间接地是瓶颈**：即使是强大的 VLM，在多步规划中准确进行空间定位（“在何处操作”）依然面临巨大挑战。
*   **长程任务难度剧增**：随着步骤增加，所有模型的成功率均显著下降，长程规划仍是主要瓶颈。
*   **V2GP 的有效性**：通过 V2GP 训练后的 Qwen3-VL-4B 在某些指标上甚至超过了未微调的 32B 模型，显著提升了空间定位的准确性和规划的连贯性。
*   **隐式指令挑战**：模型在处理抽象指令时表现远逊于显式指令，说明其意图理解能力仍有待提高。

### 7. 优点
*   **任务定义新颖**：首次明确定义了“定位规划”任务，并构建了相应的真实世界基准。
*   **自动化程度高**：V2GP 框架能够高效利用现有的机器人演示视频，无需大规模人工标注即可生成高质量训练数据。
*   **软硬件结合**：不仅有算法指标，还通过真实机器人实验验证了方案的落地潜力。

### 8. 不足与局限
*   **噪声敏感性**：子动作分解高度依赖夹持器信号，若信号存在噪声或延迟，会导致分解错误。
*   **幻觉风险**：数据生成过程中依赖的基础模型（如 SAM3 或 VLM 标注器）若产生幻觉，会引入错误标签。
*   **长程数据稀缺**：尽管有自动化框架，但高质量、超长程的真实机器人演示数据在原始数据集中依然相对较少。
*   **动态环境限制**：目前的规划主要基于初始观察图像，对于执行过程中环境发生动态变化的应对能力未做深入探讨。

（完）
