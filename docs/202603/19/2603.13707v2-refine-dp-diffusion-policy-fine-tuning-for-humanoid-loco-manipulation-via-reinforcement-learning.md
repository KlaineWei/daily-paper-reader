---
title: "REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning"
title_zh: REFINE-DP：通过强化学习进行类人机器人移动操作的扩散策略微调
authors: "Zhaoyuan Gu, Yipu Chen, Zimeng Chai, Alfred Cueva, Thong Nguyen, Yifan Wu, Huishu Xue, Minji Kim, Isaac Legene, Fukang Liu, Matthew Kim, Ayan Barula, Yongxin Chen, Ye Zhao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13707v2"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于类人机器人运动操作和长程任务的分层框架
tldr: 针对人形机器人移动操作中高层规划与底层控制解耦导致的分布偏移和任务失败问题，本文提出REFINE-DP框架。该框架通过强化学习（PPO）微调扩散策略（DP）作为高层规划器，并同步优化底层控制器以精准跟踪动态指令。实验证明，该方法在门禁通行和长程搬运任务中表现优异，显著提升了模拟和现实环境中的任务成功率及鲁棒性，解决了高质量演示数据稀缺的挑战。
selection_source: fresh_fetch
motivation: 离线训练的扩散策略与底层控制器之间存在解耦导致的分布偏移，且人形机器人高质量演示数据获取成本极高。
method: 提出一种分层框架，利用基于PPO的扩散策略梯度微调高层规划器，并同步更新底层控制器以适应演进的指令分布。
result: "在模拟环境中实现了超过90%的任务成功率，并成功处理了分布外场景，同时在真实动态环境中实现了平滑的自主任务执行。"
conclusion: 强化学习微调是实现可靠的人形机器人移动操作的关键，能有效解决扩散策略在复杂动态系统中的部署难题。
---

## 摘要
类人机器人的移动操作（loco-manipulation）需要在复杂的机器人-环境动力学和长程任务下，将协调的高层运动规划与稳定的底层全身执行相结合。虽然扩散策略（DPs）在从演示中学习方面展现出潜力，但在类人机器人上部署它们面临着严峻挑战：离线训练的运动规划器与底层控制器是解耦的，导致指令跟踪效果差、分布偏移累积以及任务失败。对于高维类人系统而言，扩展演示数据的常规方法成本极高。为了应对这一挑战，我们提出了 REFINE-DP（扩散策略的强化学习微调），这是一个联合优化 DP 高层规划器和基于强化学习（RL）的底层移动操作控制器的分层框架。DP 通过基于 PPO 的扩散策略梯度进行微调以提高任务成功率，同时控制器同步更新以精确跟踪规划器不断演变的指令分布，从而减少降低运动质量的分布失配。我们在执行移动操作任务（包括穿过门和长程物体搬运）的类人机器人上验证了 REFINE-DP。REFINE-DP 在仿真中实现了超过 90% 的成功率，即使是在预训练数据中未见的分布外情况下也是如此，并能在现实世界的动态环境中实现平滑的自主任务执行。我们提出的方法显著优于预训练的 DP 基准，并证明了 RL 微调是实现可靠类人机器人移动操作的关键。https://refine-dp.github.io/REFINE-DP/

## Abstract
Humanoid loco-manipulation requires coordinated high-level motion plans with stable, low-level whole-body execution under complex robot-environment dynamics and long-horizon tasks. While diffusion policies (DPs) show promise for learning from demonstrations, deploying them on humanoids poses critical challenges: the motion planner trained offline is decoupled from the low-level controller, leading to poor command tracking, compounding distribution shift, and task failures. The common approach of scaling demonstration data is prohibitively expensive for high-dimensional humanoid systems. To address this challenge, we present REFINE-DP (REinforcement learning FINE-tuning of Diffusion Policy), a hierarchical framework that jointly optimizes a DP high-level planner and an RL-based low-level loco-manipulation controller. The DP is fine-tuned via a PPO-based diffusion policy gradient to improve task success rate, while the controller is simultaneously updated to accurately track the planner's evolving command distribution, reducing the distributional mismatch that degrades motion quality. We validate REFINE-DP on a humanoid robot performing loco-manipulation tasks, including door traversal and long-horizon object transport. REFINE-DP achieves an over $90\%$ success rate in simulation, even in out-of-distribution cases not seen in the pre-trained data, and enables smooth autonomous task execution in real-world dynamic environments. Our proposed method substantially outperforms pre-trained DP baselines and demonstrates that RL fine-tuning is key to reliable humanoid loco-manipulation. https://refine-dp.github.io/REFINE-DP/

---

## 论文详细总结（自动生成）

以下是对论文《REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
类人机器人的**移动操作（Loco-manipulation）**要求高层运动规划与底层全身控制高度协同。目前主流的**扩散策略（Diffusion Policy, DP）**虽然能学习复杂的专家行为，但在类人机器人上部署面临三大挑战：
*   **解耦导致的分布偏移**：离线训练的规划器与底层控制器之间存在鸿沟，导致指令跟踪差，误差随时间累积。
*   **数据成本极高**：在高维类人系统上通过大规模采集演示数据来覆盖所有场景极其昂贵。
*   **动态适应性差**：纯模仿学习难以处理未见过的环境扰动或分布外（OOD）场景。
**研究意义**：本文提出 REFINE-DP 框架，旨在通过强化学习（RL）微调来弥补离线模仿学习的不足，实现高效、鲁棒的类人机器人自主操作。

### 2. 方法论：核心思想与关键技术
REFINE-DP 采用**分层架构**并引入**联合优化**机制，具体分为三个阶段：
*   **分层控制架构**：
    *   **高层规划器（DP）**：输出低维、物理可解释的指令（基座速度、手部位姿）。
    *   **底层控制器（RL）**：基于 PPO 训练的全身控制器，负责将高层指令转化为关节力矩，确保行走稳定性和操作精度。
*   **核心技术：联合优化（Joint Optimization）**：
    *   **DP 微调**：利用 **DPPO（Diffusion Policy Policy Optimization）** 算法，将扩散过程的去噪步骤嵌入马尔可夫决策过程（MDP），通过环境反馈的稀疏奖励直接更新 DP 参数。
    *   **同步更新**：在微调 DP 的同时，底层 RL 控制器也进行更新。这解决了“指令分布漂移”问题，使控制器能够适应 DP 在微调过程中产生的演变指令，从而提高跟踪精度。
*   **算法流程**：1) 采集少量专家演示；2) 离线预训练 DP；3) 在仿真环境中通过 RL 联合微调 DP 和底层控制器。

### 3. 实验设计
*   **实验场景**：
    1.  **物体抓取（Task 1）**：基础任务。
    2.  **长程搬运（Task 2）**：持续约 40 秒的复杂序列任务。
    3.  **开门与穿行（Task 3）**：涉及复杂的接触动力学。
    4.  **台阶辅助抓取（Task 4）**：非平整地面上的移动操作。
*   **对比基准（Benchmarks）**：
    *   **DiT/MLP Planner**：纯离线预训练的 Transformer 或 MLP 架构。
    *   **MLP-FT**：对 MLP 架构进行 RL 微调。
    *   **Residual RL**：在冻结的 DP 之上学习残差策略。
    *   **DiT-FT (Ours)**：仅微调 DP。
    *   **Joint Optimized (Ours)**：本文提出的联合微调方案。

### 4. 资源与算力
*   **硬件平台**：Booster T1 类人机器人（29 自由度）。
*   **计算设备**：
    *   **策略计算机**：AMD 7945HX CPU + NVIDIA RTX 4060 GPU。
    *   **推理优化**：使用 NVIDIA TensorRT 加速，DP 运行频率 10Hz，底层 RL 运行频率 50Hz。
*   **仿真环境**：使用 NVIDIA IsaacLab。
*   **说明**：文中未明确给出具体的总训练时长或使用的 GPU 集群规模，但提到联合优化比单独微调 DP 节省了约一半的迭代次数（20 vs 40 次迭代达到 90% 成功率）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 类典型任务，每类任务均进行了 100 次以上的仿真测试。
*   **消融实验**：对比了不同 Backbone（Transformer vs MLP）、不同微调策略（残差 vs 直接微调）以及有无联合优化的效果。
*   **数据效率实验**：测试了在不同演示数据量（50 到 1000 条）下的性能表现。
*   **分布外（OOD）实验**：通过课程学习验证了模型在超出预训练范围的随机初始位置下的鲁棒性。
*   **充分性评价**：实验设计较为全面，不仅关注成功率，还深入分析了运动平滑度、跟踪误差和执行效率，且包含真机验证，具有较高的说服力。

### 6. 主要结论与发现
*   **微调是关键**：RL 微调能将成功率从 50-70% 提升至 **90% 以上**，且能处理预训练数据中未见的 OOD 场景。
*   **联合优化更优**：同时优化高层和底层不仅提升了成功率，还显著降低了末端执行器的跟踪误差（位置误差降低，方向误差降低达 50%），使动作更平滑。
*   **数据效率极高**：使用 REFINE-DP，仅需 **50 条**演示数据微调后的效果，即可超过纯离线训练使用 **1000 条**数据的表现。
*   **执行效率提升**：微调后的策略减少了犹豫动作，任务完成速度平均提升了 15%。

### 7. 优点
*   **架构优雅**：分层框架降低了 DP 的动作空间维度，使其更易于训练和部署。
*   **解决核心痛点**：通过联合优化解决了模仿学习中常见的分布偏移和指令不匹配问题。
*   **实战导向**：在真机上实现了开门、搬运等长程任务，展示了极强的鲁棒性和重试能力（如开门失败后自动调整位置重试）。

### 8. 不足与局限
*   **感知依赖**：目前的 DP 规划器依赖于**状态输入（State-based）**，即需要动捕系统（MoCap）提供物体和机器人的精确位姿，尚未完全实现基于视觉（Vision-based）的端到端闭环。
*   **真机性能下降**：受限于传感器噪声和物理摩擦差异，真机成功率（50-70%）较仿真（90%+）仍有明显差距。
*   **任务范围**：虽然涵盖了多种任务，但主要集中在准静态或中速操作，对于极高动态或高爆发力的动作（如跳跃抓取）尚未验证。

（完）
