---
title: Iterative Semantic Reasoning from Individual to Group Interests for Generative Recommendation with LLMs
title_zh: 基于大语言模型从个人到群体兴趣迭代语义推理的生成式推荐
authors: "Xiaofei Zhu, Jinfei Chen, Feiyang Yuan, Zhou Yang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13934v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 基于大模型的生成式推荐迭代语义推理
tldr: 针对推荐系统中用户兴趣理解不足的问题，本文提出了一种基于大语言模型的迭代语义推理框架（ISRF）。该框架通过LLM在显式个体兴趣与隐式群体兴趣之间建立桥梁，利用多步双向推理构建语义交互图，并结合相似用户图进行群体兴趣建模。通过迭代优化策略，实现个体与群体兴趣的相互增强，在多个数据集上显著提升了生成式推荐的性能。
selection_source: fresh_fetch
motivation: 现有的生成式推荐方法往往仅侧重于语义整合，缺乏从显式个体兴趣到隐式群体兴趣的深度语义推理能力。
method: 提出ISRF框架，利用LLM进行多步双向推理构建语义图，并采用迭代批量优化策略实现个体与群体兴趣的协同演进。
result: 在Sports、Beauty和Toys等数据集上的广泛实验证明，ISRF在各项指标上均优于目前最先进的基准模型。
conclusion: 该研究表明，通过迭代推理机制整合个体与群体语义信息，可以更全面地捕捉用户兴趣并显著提升生成式推荐的准确性。
---

## 摘要
推荐系统旨在从历史行为中学习用户兴趣并交付相关物品。近期的方法利用大语言模型（LLMs）构建并整合用户和物品的语义表示，以捕捉用户兴趣。然而，用户行为理论表明，真正理解用户兴趣不仅需要语义整合，还需要从显式个人兴趣到隐式群体兴趣的语义推理。为此，我们提出了一种用于生成式推荐的迭代语义推理框架（ISRF）。ISRF 利用 LLMs 通过三个步骤桥接显式个人兴趣和隐式群体兴趣。首先，我们对物品属性进行多步双向推理，以推断语义物品特征，并构建捕捉用户显式兴趣的语义交互图。其次，我们基于语义物品特征生成语义用户特征，并构建基于相似度的用户图，以推断相似用户群体的隐式兴趣。第三，我们采用迭代批优化策略，其中个人显式兴趣直接指导群体隐式兴趣的细化，而群体隐式兴趣则间接增强个人建模。这一迭代过程确保了连贯且渐进的兴趣推理，从而实现更准确、更全面的用户兴趣学习。在 Sports、Beauty 和 Toys 数据集上的广泛实验表明，ISRF 优于最先进的基准模型。代码可在 https://github.com/htired/ISRF 获取。

## Abstract
Recommendation systems aim to learn user interests from historical behaviors and deliver relevant items. Recent methods leverage large language models (LLMs) to construct and integrate semantic representations of users and items for capturing user interests. However, user behavior theories suggest that truly understanding user interests requires not only semantic integration but also semantic reasoning from explicit individual interests to implicit group interests. To this end, we propose an Iterative Semantic Reasoning Framework (ISRF) for generative recommendation. ISRF leverages LLMs to bridge explicit individual interests and implicit group interests in three steps. First, we perform multi-step bidirectional reasoning over item attributes to infer semantic item features and build a semantic interaction graph capturing users' explicit interests. Second, we generate semantic user features based on the semantic item features and construct a similarity-based user graph to infer the implicit interests of similar user groups. Third, we adopt an iterative batch optimization strategy, where individual explicit interests directly guide the refinement of group implicit interests, while group implicit interests indirectly enhance individual modeling. This iterative process ensures consistent and progressive interest reasoning, enabling more accurate and comprehensive user interest learning. Extensive experiments on the Sports, Beauty, and Toys datasets demonstrate that ISRF outperforms state-of-the-art baselines. The code is available at https://github.com/htired/ISRF.

---

## 论文详细总结（自动生成）

### 论文总结：基于大语言模型从个人到群体兴趣迭代语义推理的生成式推荐 (ISRF)

#### 1. 核心问题与整体含义（研究动机和背景）
传统的推荐系统主要依赖物品 ID，缺乏语义理解。虽然近期研究开始引入大语言模型（LLMs）来增强语义表示，但大多局限于“语义整合”（即简单地将文本向量化），而忽略了用户行为理论中的**语义推理**过程。
论文指出，理解用户兴趣需要从**显式的个体兴趣**（如：买了防滑篮球鞋）推理出**隐式的群体兴趣**（如：这类用户通常也关注透气性和长效耐用性）。该研究旨在通过 LLM 的推理能力，桥接个体与群体、显式与隐式之间的鸿沟，实现更精准的生成式推荐。

#### 2. 方法论：核心思想与关键技术
论文提出了 **ISRF（Iterative Semantic Reasoning Framework）**，包含三个核心模块：
*   **个体兴趣推理 (IIR)**：
    *   利用 **CoT（思维链）** 引导 LLM 对物品属性进行双向推理：先推断“哪些用户会喜欢”，再推断“哪些用户会不喜欢”，最后融合生成综合语义特征。
    *   使用 PCA 降维并配合 **Adapter（适配器）** 将 LLM 的高维语义空间映射到推荐任务空间。
    *   通过 **LightGCN** 在用户-物品交互图上捕捉显式的个体偏好。
*   **群体兴趣推理 (GIR)**：
    *   基于 LLM 生成的用户兴趣描述计算用户间的余弦相似度，构建**用户语义相似度图**。
    *   再次利用 LightGCN 在该图上聚合邻居信息，从而挖掘具有相似行为模式的群体隐式兴趣。
*   **迭代细化 (IR)**：
    *   采用迭代批量优化策略：在直接推荐任务中，以群体兴趣作为“教师”信号，通过对比蒸馏损失指导个体建模；在序列推荐任务中，通过最大化互信息来增强个体表示的表达力。

#### 3. 实验设计
*   **数据集**：采用了 Amazon 平台上的三个公开基准数据集：**Sports & Outdoors**（运动户外）、**Beauty**（美妆）、**Toys & Games**（玩具）。
*   **任务场景**：涵盖了**序列推荐 (Sequential Recommendation)** 和 **直接推荐 (Direct Recommendation)** 两种主流生成式推荐任务。
*   **对比方法 (Baselines)**：共对比了 14 种方法，包括：
    *   传统/深度方法：Caser, GRU4Rec, SASRec, BERT4Rec 等。
    *   GNN 方法：LightGCN, NCL, XSimGCL。
    *   LLM 相关方法：P5, POD, RSL, 以及当前最强的 **ELMRec**。

#### 4. 资源与算力
*   **模型骨干**：语义推理和特征提取使用 **DeepSeek-R1-14B**；推荐任务的生成式骨干模型使用 **T5-small**。
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4090 (24GB VRAM)**。
*   **训练效率**：
    *   训练时长：根据数据集大小，每轮（epoch）耗时约 5 到 15 分钟。
    *   显存占用：约 21GB 至 24GB 之间。
    *   推理时长：直接推荐任务约 10-20 分钟，序列推荐任务约 7-13 分钟。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括总体性能对比、消融实验（验证 IIR、GIR 和 IR 模块的有效性）、语义变体分析（正向 vs 负向推理的影响）、超参数敏感性分析（GCN 层数 $L'$ 和相似邻居数 $K$）以及案例分析。
*   **客观性与公平性**：实验采用了 5 次试验的 t-检验来确保统计显著性；对比了从传统到最前沿的各类模型，覆盖面广；数据集处理遵循了前人研究的标准协议，具有较好的客观性。

#### 6. 主要结论与发现
*   **性能领先**：ISRF 在所有数据集和任务上均显著优于 SOTA 模型（如 ELMRec）。在直接推荐任务中，性能提升尤为显著（最高达 24.57%）。
*   **推理的重要性**：消融实验证明，引入 LLM 的双向语义推理（正向偏好+负向厌恶）比单一的语义嵌入更有效。
*   **群体效应**：通过相似用户图捕捉的隐式兴趣能有效弥补个体历史行为的稀疏性，案例分析证实 ISRF 能发现用户未曾表现但潜在感兴趣的类别。

#### 7. 优点（亮点）
*   **视角新颖**：将推荐过程建模为从“个体显式”到“群体隐式”的迭代推理过程，符合消费者行为学理论。
*   **架构融合**：巧妙地结合了 LLM 的深层语义推理能力与 GNN 的结构化拓扑建模能力。
*   **双向建模**：不仅考虑用户喜欢什么，还通过 LLM 推理用户可能讨厌什么，增强了表示的判别性。

#### 8. 不足与局限
*   **计算开销**：虽然训练时间在可接受范围内，但构建用户相似度图的复杂度为 $O(n^2)$，在超大规模用户场景下可能面临扩展性挑战。
*   **离线依赖**：语义推理部分（DeepSeek-R1）似乎是预先离线生成的，这限制了系统对新物品或实时兴趣演变的即时响应能力。
*   **数据集局限**：实验仅限于 Amazon 的三个子类目，尚未在更具挑战性的短视频或新闻推荐等极度稀疏/高动态场景下验证。

（完）
