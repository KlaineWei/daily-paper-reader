---
title: Bringing Model Editing to Generative Recommendation in Cold-Start Scenarios
title_zh: 将模型编辑引入冷启动场景下的生成式推荐
authors: "Chenglei Shen, Teng Shi, Weijie Yu, Xiao Zhang, Jun Xu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14259v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 冷启动场景下生成式推荐的模型编辑
tldr: "生成式推荐（GR）在冷启动场景下表现极差，且传统重训练方法效率低、更新慢。本文受NLP模型编辑启发，提出GenRecEdit框架，通过显式建模序列上下文与Token生成关系、迭代式Token级编辑以及一对一触发机制，实现了在不重训练的情况下高效注入新项目知识。实验证明，该方法在显著提升冷启动推荐性能的同时，仅需重训练时间的9.5%，且不损害原有推荐质量。"
selection_source: fresh_fetch
motivation: 针对生成式推荐在冷启动场景下准确率骤降且传统重训练方法成本高、更新延迟的问题，探索如何将模型编辑技术引入推荐系统。
method: 提出GenRecEdit框架，采用迭代式Token级编辑注入多Token项目表示，并引入一对一触发机制以减少推理时的编辑干扰。
result: "实验显示GenRecEdit在大幅提升冷启动项目推荐效果的同时，仅消耗约9.5%的重训练时间，并保持了模型原有的推荐性能。"
conclusion: 本研究证明了模型编辑在解决生成式推荐冷启动问题上的高效性与可行性，为推荐系统的快速知识更新提供了新思路。
---

## 摘要
生成式推荐（GR）在端到端生成范式的序列推荐中展现出巨大潜力。然而，现有的 GR 模型面临严重的冷启动崩溃问题：它们在冷启动物品上的推荐准确率可能会降至接近零。目前的解决方案通常依赖于利用冷启动交互进行重新训练，但这受到反馈稀疏、计算成本高和更新延迟的限制，阻碍了其在快速演变的推荐目录中的实际应用。受自然语言处理（NLP）中模型编辑的启发（该技术能够实现向大语言模型中进行免训练的知识注入），我们探索了如何将这一范式引入生成式推荐。然而，这面临两个关键挑战：首先，GR 缺乏自然语言中常见的显式主客体绑定，使得针对性编辑变得困难；其次，GR 不具备稳定的 token 共现模式，导致多 token 物品表示的注入变得不可靠。为了应对这些挑战，我们提出了 GenRecEdit，这是一个专为生成式推荐量身定制的模型编辑框架。GenRecEdit 显式建模了完整序列上下文与下一个 token 生成之间的关系，采用迭代的 token 级编辑来注入多 token 物品表示，并引入了一对一触发机制以减少推理过程中多个编辑之间的干扰。在多个数据集上的广泛实验表明，GenRecEdit 在保持模型原始推荐质量的同时，显著提高了冷启动物品的推荐性能。此外，它仅需重新训练所需时间的约 9.5% 即可实现这些提升，从而实现了更高效、更频繁的模型更新。

## Abstract
Generative recommendation (GR) has shown strong potential for sequential recommendation in an end-to-end generation paradigm. However, existing GR models suffer from severe cold-start collapse: their recommendation accuracy on cold-start items can drop to near zero. Current solutions typically rely on retraining with cold-start interactions, which is hindered by sparse feedback, high computational cost, and delayed updates, limiting practical utility in rapidly evolving recommendation catalogs. Inspired by model editing in NLP, which enables training-free knowledge injection into large language models, we explore how to bring this paradigm to generative recommendation. This, however, faces two key challenges: GR lacks the explicit subject-object binding common in natural language, making targeted edits difficult; and GR does not exhibit stable token co-occurrence patterns, making the injection of multi-token item representations unreliable. To address these challenges, we propose GenRecEdit, a model editing framework tailored for generative recommendation. GenRecEdit explicitly models the relationship between the full sequence context and next-token generation, adopts iterative token-level editing to inject multi-token item representations, and introduces a one-to-one trigger mechanism to reduce interference among multiple edits during inference. Extensive experiments on multiple datasets show that GenRecEdit substantially improves recommendation performance on cold-start items while preserving the model's original recommendation quality. Moreover, it achieves these gains using only about 9.5% of the training time required for retraining, enabling more efficient and frequent model updates.

---

## 论文详细总结（自动生成）

以下是对论文《Bringing Model Editing to Generative Recommendation in Cold-Start Scenarios》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：生成式推荐（Generative Recommendation, GR）通过将物品转化为语义ID（Semantic IDs, SIDs）并利用语言模型架构进行自回归生成，展现了强大的序列推荐能力。
*   **核心问题**：GR模型在面对**冷启动物品**（训练集中未出现的物品）时会出现“冷启动崩溃（Cold-start collapse）”现象，即推荐准确率几乎降为零。
*   **研究动机**：现有的解决办法（如重新训练或增量微调）存在反馈稀疏、计算成本高、更新延迟等问题。本文旨在引入NLP领域的“模型编辑”技术，实现无需重新训练即可快速、高效地将新物品知识注入模型。

### 2. 论文提出的方法论：GenRecEdit
GenRecEdit是首个专为生成式推荐设计的模型编辑框架，其核心思想是将冷启动物品的SID模式视为可编辑的知识。
*   **位置相关知识准备（Position-Wise Knowledge Preparation）**：
    *   由于冷启动物品缺乏交互历史，通过计算物品元数据（Meta-info）的嵌入相似度，检索相似的热启动物品，并将其交互历史作为冷启动物品的“伪历史（Pseudo histories）”。
    *   将推荐任务拆解为“上下文 -> 下一个Token”的映射，构建位置相关的编辑请求 $\langle s_p, o_p \rangle$。
*   **定位-编辑框架（Locate-Then-Edit Framework）**：
    *   **层定位**：训练一个线性探测分类器（Probing Classifier），识别哪些层最能区分新旧知识，从而选定编辑层。
    *   **参数更新**：针对选定的FFN层，通过最小二乘法求解闭式解（Closed-form update），在注入新知识的同时，利用协方差矩阵约束来保留原始知识（即维持对热启动物品的推荐能力）。
*   **一对一触发机制（One-One Triggering Policy）**：
    *   在推理阶段，根据当前解码的Token位置，仅激活对应的编辑层。这种“一对一”的门控机制避免了多个位置编辑之间的相互干扰，确保了多Token生成的稳定性。

### 3. 实验设计
*   **数据集**：使用了 Amazon 2023 Review 数据集中的三个子类：Video Games（视频游戏）、Software（软件）、Cell Phones and Accessories（手机及配件）。
*   **Benchmark与对比方法**：
    *   **传统ID类**：SASRec, BERT4Rec。
    *   **语义ID生成类**：VQ-Rec, TIGER, LC-Rec。
    *   **冷启动专项类**：Retraining（全量重训）、Finetuning（微调）、SpecGR（基于检索的推测生成）。
*   **评估指标**：NDCG@K, Recall@K, 以及 IID Ratio（衡量生成结果中属于目标分布的比例）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的GPU型号和数量。
*   **训练/更新时长**：论文强调了效率优势，指出 GenRecEdit 的模型更新时间仅为**重训练（Retraining）时间的 9.5%**。
*   **模型规格**：基于 T5 架构，解码器层数为 6 层，使用 gated-silu 激活函数。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同规模和稀疏度的数据集上进行了测试。
*   **实验维度**：
    *   **整体性能对比**：验证了在全集和冷启动子集上的优越性。
    *   **消融实验**：分别验证了位置化准备、分类器定位、一对一触发机制的必要性。
    *   **敏感性分析**：探讨了伪知识的质量、数量以及权衡参数 $\lambda$ 对结果的影响。
*   **充分性评价**：实验设计较为全面，不仅关注了冷启动的提升，还客观评估了对热启动物品性能的副作用（遗忘程度），实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **冷启动崩溃的本质**：GR模型其实能预测出冷启动物品的第一个Token（粗分类正确），但在后续Token生成时会由于偏向已见模式而失败。
*   **模型编辑的有效性**：GenRecEdit 能够显著提升冷启动物品的推荐精度，且在 Software 等数据集上表现出比微调（Finetuning）更好的知识保留能力。
*   **效率优势**：模型编辑提供了一种近乎实时的更新手段，极大地降低了维护推荐系统实时性的计算开销。

### 7. 优点（亮点）
*   **范式创新**：首次将模型编辑引入生成式推荐领域，解决了推荐系统特有的“无固定主客体结构”和“Token共现不稳定”的编辑难题。
*   **设计精巧**：提出的“一对一触发机制”非常契合自回归生成的特性，有效解决了多步生成中的编辑冲突。
*   **实用性强**：9.5% 的时间成本使其在需要频繁更新物品库的实际业务场景（如新闻、短视频）中极具吸引力。

### 8. 不足与局限
*   **伪历史依赖**：冷启动性能高度依赖于“伪历史”构造的质量。实验显示，使用真实历史（Upper Bound）与使用伪历史之间仍存在较大的性能差距（Quality Gap）。
*   **SID模式限制**：该方法目前主要针对基于语义ID（SID）的生成式模型，对于其他类型的推荐架构（如纯判别式或基于文本生成的模型）的适用性尚未探讨。
*   **长序列挑战**：虽然解决了 4 个 Token 的 SID 生成，但对于更长的语义编码或更复杂的生成任务，编辑的稳定性和层定位的准确性可能会下降。

（完）
