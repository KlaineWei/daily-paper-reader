---
title: "CORAL: COntextual Reasoning And Local Planning in A Hierarchical VLM Framework for Underwater Monitoring"
title_zh: CORAL：用于水下监测的分层视觉语言模型（VLM）框架中的上下文推理与局部规划
authors: "Zhenqi Wu, Yuanjie Lu, Xuesu Xiao, Xiaomin Lin"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14786v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于机器人局部规划的分层 VLM 框架
tldr: 针对水下牡蛎礁监测任务，本文提出CORAL框架，旨在解决现有自主水下航行器（AUV）缺乏语义理解以及端到端视觉语言模型（VLM）推理慢、易碰撞和误差累积的问题。该框架采用分层架构，将VLM的高层语义推理与基于动力学的底层避障规划解耦，并引入几何验证模块。实验表明，CORAL显著提升了覆盖率，消除了碰撞，并大幅减少了VLM调用次数，为复杂水下环境的智能监测提供了新方案。
selection_source: fresh_fetch
motivation: 现有的水下机器人导航缺乏语义理解，而端到端VLM系统存在推理延迟高、忽略动力学约束及误差累积等局限。
method: 提出一种分层框架，利用VLM进行高层路径点引导，结合动力学规划器实现底层避障，并通过几何验证模块触发重新规划。
result: "与现有技术相比，CORAL将覆盖率提升了17.85%，实现了零碰撞，并减少了57%的VLM推理调用。"
conclusion: CORAL通过解耦语义推理与局部规划，为复杂水下环境中的智能语义监测提供了一种高效且安全的解决方案。
---

## 摘要
牡蛎礁是维持生物多样性、过滤水质和保护海岸线的关键生态系统物种，但其在全球范围内持续减少。修复这些生态系统需要定期进行水下监测以评估礁石健康状况，而由人类潜水员执行此类任务不仅成本高昂、具有危险性，且范围有限。自主水下航行器（AUV）提供了一种极具前景的替代方案，但现有的 AUV 依赖于基于几何的导航，无法解释场景语义。近期的视觉语言模型（VLM）为智能探索提供了语义推理能力，但现有的 VLM 驱动系统采用端到端范式，引入了三个关键局限性。首先，这些系统要求 VLM 生成每一个导航决策，导致频繁等待推理结果。其次，VLM 无法对机器人动力学建模，导致在复杂环境中发生碰撞。第三，有限的自我修正能力使得微小偏差累积成巨大的路径误差。为解决这些局限性，我们提出了 CORAL，这是一个将高层语义推理与底层反应式控制解耦的框架。VLM 通过选择航路点提供高层探索指导，而基于动力学的规划器则负责底层的无碰撞执行。几何验证模块负责验证航路点，并在需要时触发重新规划。与之前的最先进技术相比，CORAL 将覆盖率提高了 14.28 个百分点（相对提高 17.85%），碰撞率降低了 100%，且 VLM 调用次数减少了 57%。

## Abstract
Oyster reefs are critical ecosystem species that sustain biodiversity, filter water, and protect coastlines, yet they continue to decline globally. Restoring these ecosystems requires regular underwater monitoring to assess reef health, a task that remains costly, hazardous, and limited when performed by human divers. Autonomous underwater vehicles (AUVs) offer a promising alternative, but existing AUVs rely on geometry-based navigation that cannot interpret scene semantics. Recent vision-language models (VLMs) enable semantic reasoning for intelligent exploration, but existing VLM-driven systems adopt an end-to-end paradigm, introducing three key limitations. First, these systems require the VLM to generate every navigation decision, forcing frequent waits for inference. Second, VLMs cannot model robot dynamics, causing collisions in cluttered environments. Third, limited self-correction allows small deviations to accumulate into large path errors. To address these limitations, we propose CORAL, a framework that decouples high-level semantic reasoning from low-level reactive control. The VLM provides high-level exploration guidance by selecting waypoints, while a dynamics-based planner handles low-level collision-free execution. A geometric verification module validates waypoints and triggers replanning when needed. Compared with the previous state-of-the-art, CORAL improves coverage by 14.28% percentage points, or 17.85% relatively, reduces collisions by 100%, and requires 57% fewer VLM calls.

---

## 论文详细总结（自动生成）

### 论文总结：CORAL —— 用于水下监测的分层 VLM 框架

#### 1. 核心问题与研究动机
*   **背景**：牡蛎礁对生态系统至关重要，但全球范围内正在急剧减少。传统的人工潜水监测成本高、风险大且范围有限。
*   **核心问题**：现有的自主水下航行器（AUV）导航存在两大极端：
    1.  **传统几何导航**：仅依赖障碍物地图，缺乏语义理解，无法识别并优先探索具有生态价值的区域（如牡蛎簇）。
    2.  **端到端 VLM 导航**：虽然具备语义推理能力，但推理延迟高（需频繁等待）、缺乏动力学感知（易碰撞）、且缺乏自我修正机制（误差易累积）。
*   **研究目标**：提出一种分层框架，将高层语义推理与底层动力学控制解耦，实现高效、安全且具有语义感知能力的水下监测。

#### 2. 方法论
CORAL 框架由感知、高层规划、几何验证和底层控制四个核心模块组成：
*   **感知与质心链提取**：
    *   将深度图和语义分割图融合进持久的 2D 占据网格地图。
    *   利用连通域分析提取牡蛎簇质心，并通过贪婪算法将其组织成有序的“质心链”（Centroid Chain），将连续的探索空间离散化为可供 VLM 选择的候选目标。
*   **分层规划架构**：
    *   **高层 VLM 规划器**：采用双模式切换。在有明确簇时进行“质心选择”；在连续脊状结构时进行“自由航路点预测”。
    *   **智能触发机制（Smart Trigger）**：仅在机器人接近目标、被困或规划失败时调用 VLM，避免了每一步都进行推理的开销。
*   **几何验证模块**：
    *   对 VLM 生成的航路点进行形式化检查，包括“后退检测”和“趋势偏差检测”。若验证失败，则向 VLM 提供结构化反馈并要求重新规划。
*   **底层 MDDP 控制器**：
    *   采用递减动力学规划（MDDP），在靠近机器人的区域使用高保真动力学模型和密集碰撞检查，在远端简化模型。
    *   通过 MPPI 算法生成符合水下航行器非线性动力学约束且无碰撞的轨迹。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：基于 Blender 设计了 10 个具有不同拓扑结构的复杂环境（L型、S型、K型分支、E型多叉、O型环路），障碍物密度高于现有基准。
    *   **现实环境**：在 12 英尺直径的实验池中部署 BlueROV2，使用真实的牡蛎壳和浮球障碍物进行验证。
*   **对比基准（Baselines）**：
    *   **DREAM**：现有的最先进端到端 VLM 导航方法。
    *   **CORAL w/o Low-Level Planner**：保留高层模块但使用简单的 PD 控制器，以验证动力学规划的重要性。
*   **消融实验**：分别移除自我验证模块、质心选择模式，或使用纯规则启发式方法进行对比。

#### 4. 资源与算力
*   **硬件设备**：
    *   **感知与地图更新**：在一台配备 **NVIDIA RTX 5090** 显卡的 PC 上实时运行。
    *   **VLM 推理**：通过云端 API 调用（文中提到使用了 GPT 5.2，中值延迟约 10 秒）。
    *   **机器人平台**：BlueROV2 Heavy，配备前视摄像头、IMU 和声纳。
*   **训练时长**：由于该框架主要基于预训练的 VLM 和在线规划，不涉及大规模的端到端训练过程。

#### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同拓扑的仿真环境中进行了详尽测试，并进行了实机部署。
*   **充分性评价**：实验设计较为充分。通过不同形状的拓扑结构（如 K 型分支测试决策能力，E 型测试回溯能力）全面评估了系统的鲁棒性。消融实验清晰地展示了每个模块（验证、质心链、动力学规划）对最终性能的贡献。

#### 6. 主要结论与发现
*   **性能提升**：相比 DREAM，CORAL 的覆盖率提升了 **17.85%**（达到 94.28%）。
*   **安全性**：实现了 **0 碰撞**，而基准方法在复杂环境中存在多次碰撞。
*   **效率**：VLM 调用次数减少了 **57%**，显著降低了计算成本和推理等待时间。
*   **可靠性**：几何验证模块有效纠正了 VLM 的空间推理错误，避免了机器人向后行驶或偏离主航道。

#### 7. 优点
*   **解耦设计**：成功解决了 VLM 推理慢与机器人实时控制需求之间的矛盾。
*   **动力学感知**：引入 MDDP 确保了生成的路径在物理上是可执行的，特别是在受限的水下空间。
*   **空间约束**：通过将 VLM 的输出限制在“质心链”上，有效缓解了 VLM 幻觉和空间推理能力弱的问题。

#### 8. 不足与局限
*   **全局回环问题**：在复杂的分支拓扑中，VLM 有时会选择局部正确但全局会导致重复探索的路径，缺乏长期的全局拓扑记忆。
*   **感知依赖**：系统高度依赖语义分割和深度估计的准确性，在极度浑浊的水下环境（文中未深入测试）可能会失效。
*   **通信依赖**：高层规划依赖云端 VLM API，这在深海或远程离线任务中可能面临通信瓶颈。

（完）
