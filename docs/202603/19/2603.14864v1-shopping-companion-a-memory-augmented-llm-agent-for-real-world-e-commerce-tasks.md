---
title: "Shopping Companion: A Memory-Augmented LLM Agent for Real-World E-Commerce Tasks"
title_zh: Shopping Companion：一种用于真实世界电子商务任务的记忆增强型大语言模型智能体
authors: "Zijian Yu, Kejun Xiao, Huaipeng Zhao, Tao Luo, Xiaoyi Zeng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14864v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于电子商务推荐和购物任务的记忆增强LLM智能体
tldr: 本研究针对电商领域中LLM智能体难以捕捉长期用户偏好的问题，提出了Shopping Companion框架。该框架通过统一内存检索与购物辅助，并结合双重奖励强化学习策略，解决了现有系统偏好识别与任务执行脱节的挑战。同时，研究发布了一个包含120万真实商品、侧重长期记忆的新基准测试。实验证明，该轻量级模型在偏好捕捉和任务成功率上优于GPT-4等强基线模型，展现了其在复杂电商场景下的优越性。
selection_source: fresh_fetch
motivation: 现有电商LLM智能体缺乏评估长期偏好的基准，且偏好识别与购物辅助功能相互分离，导致无法进行端到端优化。
method: 提出了一个集成内存检索、购物辅助及用户干预的统一框架，并采用带有工具级奖励的双重奖励强化学习策略进行训练。
result: "实验显示即使是顶尖模型在复杂基准上的成功率也低于70%，而Shopping Companion在偏好捕捉和任务表现上均显著优于强基线。"
conclusion: 统一的设计架构和针对性的强化学习策略能有效提升LLM智能体在处理真实世界复杂电商任务时的性能。
---

## 摘要
在电子商务领域，大语言模型（LLM）智能体在推荐、预算编制和组合优惠等购物任务中展现出巨大潜力，其中从长期对话中准确捕捉用户偏好至关重要。然而，两个挑战阻碍了这一潜力的实现：（1）缺乏用于评估长期偏好感知购物任务的基准测试；（2）由于现有设计将偏好识别和购物辅助视为独立组件，导致缺乏端到端优化。在本文中，我们引入了一个具有长期记忆设置的新型基准，涵盖了超过 120 万个真实世界产品的两项购物任务，并提出了 Shopping Companion，这是一个统一的框架，在支持用户干预的同时，共同解决记忆检索和购物辅助问题。为了训练这些能力，我们开发了一种带有工具级奖励的双重奖励强化学习策略，以处理多轮交互中固有的稀疏且不连续的奖励。实验结果表明，即使是最先进的模型（如 GPT-5）在我们的基准测试中的成功率也低于 70%，这突显了该领域的重大挑战。值得注意的是，我们使用 Shopping Companion 训练的轻量级 LLM 始终优于强大的基准模型，实现了更好的偏好捕捉和任务性能，验证了我们统一设计的有效性。

## Abstract
In e-commerce, LLM agents show promise for shopping tasks such as recommendations, budgeting, and bundle deals, where accurately capturing user preferences from long-term conversations is critical. However, two challenges hinder realizing this potential: (1) the absence of benchmarks for evaluating long-term preference-aware shopping tasks, and (2) the lack of end-to-end optimization due to existing designs that treat preference identification and shopping assistance as separate components. In this paper, we introduce a novel benchmark with a long-term memory setup, spanning two shopping tasks over 1.2 million real-world products, and propose Shopping Companion, a unified framework that jointly tackles memory retrieval and shopping assistance while supporting user intervention. To train such capabilities, we develop a dual-reward reinforcement learning strategy with tool-wise rewards to handle the sparse and discontinuous rewards inherent in multi-turn interactions. Experimental results demonstrate that even state-of-the-art models (such as GPT-5) achieve success rates under 70% on our benchmark, highlighting the significant challenges in this domain. Notably, our lightweight LLM, trained with Shopping Companion, consistently outperforms strong baselines, achieving better preference capture and task performance, which validates the effectiveness of our unified design.

---

## 论文详细总结（自动生成）

以下是对论文《Shopping Companion: A Memory-Augmented LLM Agent for Real-World E-Commerce Tasks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在电子商务领域，LLM 智能体（Agent）被寄予厚望，用于处理推荐、预算管理和组合优惠等复杂任务。然而，现有技术面临两大瓶颈：
*   **缺乏长期偏好评估基准**：现有的电商基准（如 WebShop）多关注单次会话，忽略了用户在长期对话中隐含的偏好（如品牌偏好、尺码历史、审美风格）。
*   **系统设计脱节**：现有的设计通常将“偏好识别”和“购物辅助”视为独立模块，缺乏端到端的联合优化，导致智能体在处理复杂约束时表现不佳。
**整体含义**：本文旨在通过构建一个包含长期记忆（LTM）和真实购物场景的新基准，并提出一个统一的强化学习框架，提升智能体在真实电商环境中的实用性。

### 2. 方法论：核心思想与关键技术
论文提出了 **Shopping Companion** 框架，其核心思想是将偏好提取与任务执行整合进一个闭环系统，并引入用户干预机制。

*   **两阶段智能体架构**：
    *   **阶段 1（偏好识别）**：调用内存工具（`mem_search`, `mem_view` 等）检索历史对话，提取隐含偏好，并呈现给用户确认。
    *   **阶段 2（购物辅助）**：基于确认的偏好，调用商品工具（`product_search`, `product_view`）在 120 万件商品的数据库中检索并验证约束，最终给出建议。
*   **双重奖励强化学习策略（Dual-Reward RL）**：
    *   使用 **GRPO（群组相对策略优化）** 算法。
    *   **阶段奖励（Stage Reward）**：分别针对偏好提取的准确性和最终购物建议的成功率设置轨迹级奖励。
    *   **工具级奖励（Tool-wise Reward）**：为解决多轮交互中奖励稀疏的问题，对每一次工具调用（如搜索是否召回了目标商品）给予即时反馈，优化信用分配。
    *   **格式奖励（Format Reward）**：确保模型遵循特定的思考（Think）和工具调用（Tool Call）协议。

### 3. 实验设计
*   **数据集与基准**：构建了一个包含 **120 万个真实商品** 的沙盒环境。基准测试包含 1000 条指令（500 条单品推荐，500 条组合优惠），每条指令配有 15-50 轮包含隐含偏好的历史对话。
*   **任务类型**：
    1.  **单品推荐**：基于长期偏好寻找特定商品。
    2.  **组合优惠（Add-on Deals）**：在预算和优惠券约束下寻找多件匹配商品。
*   **对比方法**：
    *   **闭源模型**：GPT-5, GPT-4.1, GPT-4o, Qwen3-Max。
    *   **开源模型**：Qwen3 系列（4B, 30B, 80B）。
    *   **消融变体**：SFT 模型、仅带双重奖励的 RL 模型、带工具级奖励的 RL 模型。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H20 GPU**。
*   **软件框架**：使用 **LLaMA-Factory** 进行微调（SFT），使用 **VeRL** 框架进行强化学习（RL）。
*   **训练细节**：
    *   SFT 阶段：3 个 Epoch，学习率 5e-5。
    *   RL 阶段：2.6 个 Epoch，学习率 1e-6，每个样本生成 8 个 Rollout，最大输出长度 32,768 tokens。

### 5. 实验数量与充分性
*   **实验规模**：测试集包含 200 个查询（对应 400 个阶段实例）。
*   **充分性**：
    *   进行了详尽的**消融实验**，验证了两阶段策略、不同奖励函数设计对性能的影响。
    *   引入了**用户模拟器**，测试了不同干预粒度（无提示、低提示、高提示）下的表现。
    *   **客观性**：采用了 GPT-5 作为评测器（LLM-as-Judge），并通过人工校验确认其与人类专家的一致性超过 90%。实验覆盖了从轻量级（4B）到超大规模模型的对比，结果较为公正。

### 6. 主要结论与发现
*   **任务艰巨性**：即使是 GPT-5，在复杂的组合优惠任务中成功率也仅为 54%，平均成功率低于 70%。
*   **Shopping Companion 的优越性**：经过 RL 训练的 4B 轻量级模型在单品推荐上的成功率（84%）超过了 GPT-5（75%），在组合优惠任务上也表现出色。
*   **工具级奖励的作用**：引入工具级奖励显著提升了模型调用工具的效率，减少了冗余轮次，并缩短了响应长度。
*   **用户干预的价值**：实验证明，即便是简单的用户修正也能显著提升最终任务的成功率。

### 7. 优点与亮点
*   **端到端优化**：首次将长期记忆检索与下游购物任务通过 RL 进行联合训练，而非简单的 Pipeline 堆叠。
*   **细粒度监督**：提出的工具级奖励有效解决了长程 Agent 任务中的信用分配难题。
*   **真实感强**：基准测试基于百万级真实商品数据，且模拟了“草里寻针”式的偏好提取场景，具有极高的实用参考价值。

### 8. 不足与局限
*   **复杂约束处理仍有空间**：在涉及预算和多件商品组合的复杂逻辑中，所有模型的表现依然显著低于单品任务，说明组合优化仍是瓶颈。
*   **工具奖励的通用性**：工具级奖励的设计高度依赖于特定的电商工具集，迁移到其他领域（如医疗或法律）可能需要大量的领域知识来重新设计奖励函数。
*   **隐私风险**：长期记忆涉及用户历史数据，论文虽提到隐私保护，但在实际部署中如何平衡个性化与数据脱敏仍是挑战。

（完）
