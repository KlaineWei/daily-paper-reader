---
title: "ReFORM: Review-aggregated Profile Generation via LLM with Multi-Factor Attention for Restaurant Recommendation"
title_zh: ReFORM：基于大语言模型与多因素注意力的评论聚合画像生成餐厅推荐框架
authors: "Moonsoo Park, Seulbeen Je, Donghyeon Park"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16236v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 使用大语言模型进行餐厅推荐和画像生成
tldr: 针对现有推荐系统过度依赖LLM内部知识而忽视评论中多维决策因素的问题，本文提出ReFORM框架。该框架利用LLM从评论中提取特定因素的用户和物品画像，并结合多因素注意力机制（MFA）捕捉用户决策中的关键偏好。在餐厅推荐数据集上的实验证明，ReFORM在性能和鲁棒性上均优于现有基准模型，为个性化推荐提供了更深层的洞察。
selection_source: fresh_fetch
motivation: 现有研究主要依赖LLM对物品标题的内部知识，未能充分挖掘评论中蕴含的多种用户决策影响因素。
method: 提出ReFORM框架，利用LLM从评论中生成多维度的用户和物品画像，并通过多因素注意力机制识别用户决策中的核心因素。
result: 在两个不同规模的餐厅数据集上的实验结果显示，该方法在推荐性能上显著优于当前最先进的基准模型。
conclusion: 通过整合LLM生成的细粒度画像与注意力机制，ReFORM有效提升了推荐系统的鲁棒性并增强了对个性化偏好的理解。
---

## 摘要
在推荐系统中，大语言模型（LLMs）与图卷积网络相结合，因其能生成描述性摘要以提高推荐鲁棒性而受到广泛关注。然而，现有的基于 LLM 增强的推荐研究主要依赖于 LLM 对物品标题的内部知识，而忽略了影响用户决策的各种因素的重要性。尽管评论中蕴含着反映每个用户各种决策因素的丰富信息，但很少有研究主动利用这些见解进行推荐。为了解决这些局限性，我们提出了 ReFORM：一种基于大语言模型与多因素注意力机制的评论聚合画像生成推荐框架。具体而言，我们首先利用 LLM 从评论中生成特定因素的用户和物品画像，以捕捉用户对物品的偏好以及用户对物品的评价。随后，我们提出了一种多因素注意力机制，以突出每个用户决策过程中最具影响力的因素。在本文中，我们在两个不同规模的餐厅数据集上进行了实验，证明了该框架的鲁棒性以及优于现有基准模型的性能。此外，深入分析验证了所提模块的有效性，并为个性化来源提供了见解。我们的源代码和数据集可在 https://github.com/m0onsoo/ReFORM 获取。

## Abstract
In recommender systems, large language models (LLMs) have gained popularity for generating descriptive summarization to improve recommendation robustness, along with Graph Convolution Networks. However, existing LLM-enhanced recommendation studies mainly rely on the internal knowledge of LLMs about item titles while neglecting the importance of various factors influencing users' decisions. Although information reflecting various decision factors of each user is abundant in reviews, few studies have actively exploited such insights for recommendation. To address these limitations, we propose a ReFORM: Review-aggregated Profile Generation via LLM with Multi-FactOr Attentive RecoMmendation framework. Specifically, we first generate factor-specific user and item profiles from reviews using LLM to capture a user's preference by items and an item's evaluation by users. Then, we propose a Multi-Factor Attention to highlight the most influential factors in each user's decision-making process. In this paper, we conduct experiments on two restaurant datasets of varying scales, demonstrating its robustness and superior performance over state-of-the-art baselines. Furthermore, in-depth analyses validate the effectiveness of the proposed modules and provide insights into the sources of personalization. Our source code and datasets are available at https://github.com/m0onsoo/ReFORM.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ReFORM** 的新型餐厅推荐框架，旨在通过大语言模型（LLM）深度挖掘用户评论中的多维决策因素，以提升推荐的个性化程度和鲁棒性。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 增强型推荐系统主要依赖 LLM 对物品标题或元数据的内部知识，忽视了用户评论中蕴含的丰富决策因素（如餐厅氛围、等候时间、同伴类型等）。
*   **研究动机**：用户决策通常受多种细粒度因素驱动，而这些因素在隐式反馈（点击/购买）中无法体现。论文旨在利用 LLM 的文本理解能力，从非结构化的评论中提取结构化的“因素画像”，并动态衡量这些因素对不同用户的重要性。

### 2. 方法论
ReFORM 框架主要由三个核心组件构成：
*   **评论聚合画像生成 (RPG)**：
    *   利用 GPT-4o mini 结合特定领域的 Prompt，从聚合的用户/物品评论中提取 7 个维度的画像：**菜系类型、口味、氛围、价格、时间、等候情况、同伴**。
    *   用户画像捕捉其一贯偏好，物品画像捕捉其显著特征。
*   **多因素注意力机制 (MFA)**：
    *   **画像编码**：使用 BERT 将提取的文本因素转化为向量。
    *   **交叉注意力**：将用户画像作为 Query，物品画像作为 Key 和 Value。通过计算注意力权重，识别哪些因素在特定决策中起主导作用。
    *   **多键聚合**：为了防止单次决策的随机性偏差，引入了多键（n-key）最大池化策略，增强特征表示的稳定性。
*   **图表示学习与对齐**：
    *   使用 **LightGCN** 作为骨干网络提取高阶协同过滤信号。
    *   将 GCN 学习到的结构嵌入与 MFA 生成的语义嵌入进行拼接，通过 BPR 损失函数进行端到端训练。

### 3. 实验设计
*   **数据集**：使用了两个公开的餐厅数据集：**Yelp**（大规模）和 **Google Restaurants (GR)**（小规模）。
*   **基准方法 (Benchmarks)**：对比了四类共 7 种模型：
    1.  **传统 GCN**：GCCF, LightGCN。
    2.  **自监督 GCN**：SGL, SimGCL。
    3.  **基于内容的 GCN**：MMGCN, GRCN。
    4.  **LLM 增强型 GCN**：RLMRec（当前最先进的基准）。
*   **评价指标**：Recall@K 和 NDCG@K (K=10, 20)。

### 4. 资源与算力
*   **LLM 使用**：画像生成阶段使用了 **GPT-4o mini**。
*   **训练细节**：使用了 Adam 优化器，学习率为 1e-3，Batch Size 为 4096。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同规模的数据集上进行了测试，结果取 5 次随机种子的平均值，并进行了显著性检验（p-value）。
*   **消融实验**：
    *   验证了 MFA 模块的必要性（对比 MLP 和平均池化）。
    *   **因素消融**：逐一屏蔽 7 个因素，分析各因素对性能的贡献。
    *   **噪声注入实验**：通过向 RPG 阶段注入无关评论，验证了真实评论画像对模型性能的关键作用。
*   **充分性评价**：实验设计较为全面，不仅验证了最终效果，还深入探讨了超参数（如注意力键的数量 $n$）和数据质量对模型的影响，逻辑严密且客观。

### 6. 主要结论与发现
*   **性能领先**：ReFORM 在所有指标上均显著优于基准模型。在 Yelp 上 Recall@20 比 LightGCN 提升了 51.3%，比 RLMRec 提升了 9.2%。
*   **因素重要性**：**菜系类型 (Cuisine type)** 和 **口味 (Flavor)** 是影响餐厅推荐最重要的因素；而等候时间和时间段的影响相对较小。
*   **鲁棒性**：即使在数据稀疏的 Google Restaurants 数据集上，ReFORM 依然表现稳健，证明了引入外部语义信息的有效性。

### 7. 优点
*   **细粒度建模**：不同于以往将文本视为整体的做法，ReFORM 将其拆解为多个决策因素，更符合人类真实的决策逻辑。
*   **动态权重**：MFA 机制允许模型为不同用户识别不同的核心关注点（例如：有人看重价格，有人看重氛围）。
*   **可解释性**：通过因素消融实验，可以直观地观察到哪些特征对推荐结果贡献最大。

### 8. 不足与局限
*   **领域局限性**：目前仅在餐厅领域验证，虽然提到的 7 个因素具有代表性，但迁移到电商或电影领域需要重新设计领域相关的因素和 Prompt。
*   **成本与延迟**：依赖 LLM 生成画像会产生额外的 API 调用成本和预处理时间，对于实时性要求极高的系统可能存在挑战。
*   **数据依赖**：噪声注入实验表明，该模型高度依赖高质量的用户评论，对于缺乏评论的新用户或新物品（冷启动问题）可能效果受限。

（完）
