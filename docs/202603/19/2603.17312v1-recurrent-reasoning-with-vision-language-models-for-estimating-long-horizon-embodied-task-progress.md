---
title: Recurrent Reasoning with Vision-Language Models for Estimating Long-Horizon Embodied Task Progress
title_zh: 基于视觉语言模型的循环推理用于估计长程具身任务进度
authors: "Yuelin Zhang, Sijie Cheng, Chen Li, Zongzhao Li, Yuxin Huang, Yang Liu, Wenbing Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17312v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 利用 VLM 进行循环推理以处理长程具身任务进度
tldr: 针对具身智能体在长程任务中进度估计难、计算开销大的问题，本文提出了递归推理视觉语言模型（R2VLM）。该模型通过递归处理局部视频片段，并利用不断演进的思想链（CoT）维护全局上下文，记录任务分解与完成状态。在ALFRED和Ego4D数据集上的实验表明，R2VLM在进度估计及下游策略学习等任务中达到了SOTA水平，实现了高效且精准的长程任务推理。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理长视频时计算成本高昂，且往往忽视了对复杂任务逻辑推理能力的利用。
method: 提出一种递归推理框架，通过迭代处理视频片段并维护一个记录任务分解与状态的动态思想链来捕捉全局上下文。
result: 在多个基准测试和下游应用中，该模型显著提升了进度估计的准确性并刷新了长程任务感知的性能记录。
conclusion: R2VLM证明了结合递归机制与显式推理链可以有效解决长程具身任务的进度感知难题，具有极强的泛化能力。
---

## 摘要
准确估计任务进度对于具身智能体规划和执行长程、多步任务至关重要。尽管已取得显著进展，现有的基于视觉语言模型（VLM）的方法主要利用其视频理解能力，而忽略了其复杂的推理潜力。此外，使用 VLM 处理长视频轨迹在实际部署中计算成本过高。为了应对这些挑战，我们提出了循环推理视觉语言模型（R²VLM）。我们的模型采用循环推理框架，迭代地处理局部视频片段，并通过不断演进的思维链（CoT）维持全局上下文。该思维链显式记录了任务分解、关键步骤及其完成状态，使模型能够对复杂的时序依赖关系进行推理。这种设计在保留核心推理能力的同时，避免了处理长视频的高昂成本。我们在来自 ALFRED 和 Ego4D 的大规模自动生成数据集上训练了 R²VLM。在进度估计及下游应用（包括进度增强的策略学习、强化学习的奖励建模和主动辅助）上的广泛实验表明，R²VLM 具有强大的性能和泛化能力，在长程任务进度估计方面达到了新的 SOTA。模型和基准测试已在 Hugging Face 公开发布。

## Abstract
Accurately estimating task progress is critical for embodied agents to plan and execute long-horizon, multi-step tasks. Despite promising advances, existing Vision-Language Models (VLMs) based methods primarily leverage their video understanding capabilities, while neglecting their complex reasoning potential. Furthermore, processing long video trajectories with VLMs is computationally prohibitive for real-world deployment. To address these challenges, we propose the Recurrent Reasoning Vision-Language Model ($\text{R}^2$VLM). Our model features a recurrent reasoning framework that processes local video snippets iteratively, maintaining a global context through an evolving Chain of Thought (CoT). This CoT explicitly records task decomposition, key steps, and their completion status, enabling the model to reason about complex temporal dependencies. This design avoids the high cost of processing long videos while preserving essential reasoning capabilities. We train $\text{R}^2$VLM on large-scale, automatically generated datasets from ALFRED and Ego4D. Extensive experiments on progress estimation and downstream applications, including progress-enhanced policy learning, reward modeling for reinforcement learning, and proactive assistance, demonstrate that $\text{R}^2$VLM achieves strong performance and generalization, achieving a new state-of-the-art in long-horizon task progress estimation. The models and benchmarks are publicly available at \href{https://huggingface.co/collections/zhangyuelin/r2vlm}{huggingface}.

---

## 论文详细总结（自动生成）

以下是对论文《Recurrent Reasoning with Vision-Language Models for Estimating Long-Horizon Embodied Task Progress》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：具身智能体（Embodied Agents）在执行长程（Long-horizon）、多步骤任务时，准确估计任务进度对规划和决策至关重要。
*   **现有挑战**：
    1.  **推理不足**：现有基于视觉语言模型（VLM）的方法多侧重于视频理解，忽视了任务步骤间的逻辑推理。
    2.  **计算开销**：直接处理长视频轨迹会导致极高的计算成本和延迟，难以在真实场景部署。
*   **核心目标**：开发一种既能利用 VLM 推理能力，又能高效处理长视频流的进度估计模型。

### 2. 方法论
*   **核心思想**：提出 **R²VLM（Recurrent Reasoning VLM）** 框架。该模型不一次性处理全量视频，而是通过**递归推理**的方式，迭代处理局部视频片段，并利用动态演进的**思想链（CoT）**作为“全局记忆”。
*   **关键技术细节**：
    *   **递归推理框架**：输入为“当前视频片段 + 上一轮生成的 CoT”。模型输出“更新后的 CoT + 当前进度百分比”。
    *   **CoT 设计**：显式记录任务分解、已完成步骤、待完成步骤及完成状态，确保时序逻辑的一致性。
    *   **两阶段训练**：
        1.  **SFT（监督微调）**：在自动生成的包含视频片段、CoT 和进度标签的数据集上训练。
        2.  **RL（强化学习）**：使用 PPO 算法进一步优化。
    *   **奖励函数设计**：组合了格式奖励（Rfmt）、分桶奖励（Rbin，粗粒度）、平均绝对误差奖励（Rmae，细粒度）、**改进奖励（Rimp，鼓励比上一轮更准）**和完成奖励（Rfin）。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFRED**：模拟环境下的具身任务数据集。
    *   **Ego4D**：真实世界的第一视角视频数据集。
*   **Benchmark**：基于上述数据集构建了包含 11,499 条 ALFRED 轨迹和 13,965 条 Ego4D 轨迹的大规模增量视频交互数据集。
*   **对比方法**：
    *   **闭源 API 模型**：GPT-5 (预估)、Gemini-2.5-Pro、Qwen3-VL-Plus。
    *   **开源模型**：MiniCPM-V-2.6、InternVL3、Qwen2.5-VL (7B/72B)。
    *   **专用方法**：LIV、ROVER、GVL。

### 4. 资源与算力
*   **硬件环境**：使用 **8 张 GPU** 进行全参数微调（具体型号未标明，但提到使用了 DeepSpeed ZeRO-2/3 优化）。
*   **模型底座**：基于 Qwen2.5-VL-7B-Instruct。
*   **训练时长**：SFT 阶段在两个数据集上分别训练了 8 个 Epoch（约 15k-16k 步）；RL 阶段训练了 1.5 到 4 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个大型数据集，总计超过 25 万个对话元组（Snippet-CoT-Progress）。
*   **实验维度**：
    *   **主实验**：在 4 个核心指标（$p_{mae}$, $\Delta p_{mae}$, $bin$, $acc$）上全面对比。
    *   **消融实验**：验证了“视频片段 vs. 全量视频”的效率差异，以及各项奖励组件的贡献。
    *   **下游应用**：在策略学习、强化学习奖励建模、主动辅助三个实际场景中验证了泛化性。
*   **评价**：实验设计客观、公平，通过跨域测试（Cross-domain）验证了模型并非简单的模式记忆，而是习得了推理能力。

### 6. 主要结论与发现
*   **性能领先**：R²VLM（7B）在所有指标上均显著优于包括 72B 参数量在内的所有基线模型。
*   **效率优势**：与处理全量视频的方法相比，R²VLM 的推理时间不随视频长度增加而增长，且避免了长序列带来的注意力分散和幻觉问题。
*   **RL 的必要性**：在 CoT 基础上引入 RL 训练，能显著提升模型在复杂长程任务中的自我纠错能力和预测稳定性。

### 7. 优点
*   **创新的递归机制**：通过 CoT 传递上下文，巧妙解决了长视频处理的计算瓶颈。
*   **高解释性**：CoT 提供了清晰的推理路径，用户可以直观看到模型是如何分解任务并判断进度的。
*   **强大的下游赋能**：模型生成的进度信号可直接转化为强化学习的稠密奖励（Dense Reward），显著提升了具身策略训练的效率。

### 8. 不足与局限
*   **真实场景挑战**：在 Ego4D（真实世界）上的表现虽为 SOTA，但仍明显低于 ALFRED（模拟环境），说明环境噪声和人类行为的多样性仍是挑战。
*   **数据依赖**：模型高度依赖于高质量 CoT 数据的自动生成质量，若初始任务分解出错，可能会影响后续递归推理。
*   **采样频率限制**：目前采用固定的视频片段长度（2s/4s），对于极短时间内发生的关键动作可能存在捕捉不敏感的风险。

（完）
