---
title: "VLM2Rec: Resolving Modality Collapse in Vision-Language Model Embedders for Multimodal Sequential Recommendation"
title_zh: VLM2Rec：解决多模态序列推荐中视觉-语言模型嵌入器的模态崩溃问题
authors: "Junyoung Kim, Woojoo Kim, Jaehyung Lim, Dongha Kim, Hwanjo Yu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17450v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 视觉语言模型用于多模态序列推荐
tldr: 针对多模态序列推荐中预训练编码器语义容量有限且难以融合协同过滤信号的问题，本文提出VLM2Rec框架，利用视觉语言模型（VLM）作为推荐嵌入器。为解决微调过程中出现的模态崩溃现象，引入弱模态惩罚对比学习和跨模态关系拓扑正则化，平衡模态利用并保持几何一致性，显著提升了推荐的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 传统小规模编码器限制了多模态推荐的语义表达，而直接微调VLM作为嵌入器会导致严重的模态崩溃问题。
method: 提出VLM2Rec框架，通过弱模态惩罚对比学习纠正梯度失衡，并利用跨模态关系拓扑正则化维持模态间的几何一致性。
result: 在多个场景下的实验表明，VLM2Rec在准确性和鲁棒性方面均优于现有的最先进基准模型。
conclusion: VLM2Rec有效解决了VLM在推荐任务中的模态崩溃问题，证明了高容量多模态模型在序列推荐中的巨大潜力。
---

## 摘要
多模态环境下的序列推荐（SR）通常依赖于小型且冻结的预训练编码器，这限制了语义容量，并阻碍了协同过滤（CF）信号充分融入物品表示。受大语言模型（LLMs）作为高容量嵌入器取得成功的启发，我们研究了将视觉-语言模型（VLMs）作为感知协同过滤信号的多模态编码器用于序列推荐。然而，我们发现标准的对比监督微调（SFT）在使视觉-语言模型适应嵌入生成并注入协同过滤信号时，会放大其固有的模态崩溃。在这种状态下，优化过程由单一模态主导，而另一模态则发生退化，最终损害了推荐准确性。为解决这一问题，我们提出了 VLM2Rec，这是一个基于视觉-语言模型嵌入器的多模态序列推荐框架，旨在确保模态利用的平衡。具体而言，我们引入了弱模态惩罚对比学习以纠正优化过程中的梯度不平衡，并引入了跨模态关系拓扑正则化以保持模态间的几何一致性。大量实验表明，VLM2Rec 在多种场景下的准确性和鲁棒性均持续优于最先进的基准模型。

## Abstract
Sequential Recommendation (SR) in multimodal settings typically relies on small frozen pretrained encoders, which limits semantic capacity and prevents Collaborative Filtering (CF) signals from being fully integrated into item representations. Inspired by the recent success of Large Language Models (LLMs) as high-capacity embedders, we investigate the use of Vision-Language Models (VLMs) as CF-aware multimodal encoders for SR. However, we find that standard contrastive supervised fine-tuning (SFT), which adapts VLMs for embedding generation and injects CF signals, can amplify its inherent modality collapse. In this state, optimization is dominated by a single modality while the other degrades, ultimately undermining recommendation accuracy. To address this, we propose VLM2Rec, a VLM embedder-based framework for multimodal sequential recommendation designed to ensure balanced modality utilization. Specifically, we introduce Weak-modality Penalized Contrastive Learning to rectify gradient imbalance during optimization and Cross-Modal Relational Topology Regularization to preserve geometric consistency between modalities. Extensive experiments demonstrate that VLM2Rec consistently outperforms state-of-the-art baselines in both accuracy and robustness across diverse scenarios.

---

## 论文详细总结（自动生成）

这篇论文由浦项科技大学（POSTECH）的研究团队撰写，旨在解决多模态序列推荐（MSR）中视觉-语言模型（VLM）作为嵌入器时出现的“模态崩溃”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：传统的序列推荐（SR）主要依赖物品ID，面临数据稀疏和冷启动问题。引入多模态（文本、图像）信息已成为主流，但现有方法多使用小型且冻结的预训练编码器（如CLIP、BERT），导致语义容量有限，且无法有效吸收协同过滤（CF）信号。
*   **核心问题**：受大语言模型（LLM）作为嵌入器成功的启发，研究者尝试将视觉-语言模型（VLM）用于序列推荐。然而，研究发现直接进行对比监督微调（SFT）会引发**“SFT悖论”**：微调过程会放大VLM固有的**模态崩溃（Modality Collapse）**。模型会走捷径，过度依赖易学习的模态（通常是文本），导致弱模态（图像）的梯度信号被忽略，甚至在融合空间中产生负迁移，损害推荐精度。

### 2. 方法论：VLM2Rec 框架
VLM2Rec 旨在通过目标函数层面的干预，在不增加复杂架构的前提下实现模态平衡。
*   **序列级编码**：不同于以往仅编码单个物品的方法，VLM2Rec 将整个交互历史作为序列输入 VLM，利用其多图推理能力捕捉动态行为模式。
*   **外部融合策略**：采用简单的元素级加和（Summation）融合文本和图像表示，避免引入额外的训练参数。
*   **弱模态惩罚对比学习 ($L_{WPCL}$)**：
    *   **用户自适应模态间隙估计**：动态计算每个用户在文本和图像模态下的判别边距（Margin），识别出当前的强、弱模态。
    *   **间隙引导的动态惩罚**：引入惩罚权重 $w_{u, pen}$。当模态间隙增大时，加大对弱模态负样本的推送力度，强制模型在弱模态空间中学习更具判别力的特征。
*   **跨模态关系拓扑正则化 ($L_{CRTR}$)**：
    *   为了防止 $L_{WPCL}$ 过度推开负样本导致几何畸变，该模块通过 KL 散度对齐文本和图像空间的相似度分布（即拓扑结构），确保模态间的语义一致性，同时保留各自的特征。

### 3. 实验设计
*   **数据集**：使用了 Amazon 的四个领域数据集：Toys、Beauty、Clothing、Sports。
*   **评估任务**：
    *   **任务 1（直接推荐）**：直接利用 VLM 生成的序列和物品嵌入进行向量相似度检索。
    *   **任务 2（下游模型初始化）**：将生成的嵌入作为传统 SR 模型（如 GRU4Rec, SASRec）的初始化参数。
*   **对比方法（Baselines）**：
    *   ID 类：GRU4Rec, SASRec。
    *   文本类：BERT, LLM2Rec, LLMEmb, SLIM。
    *   多模态类：CLIP, NoteLLM-2, VLM Prompt。

### 4. 资源与算力
*   **硬件设备**：主要在单张 **NVIDIA RTX 3090** 上运行（针对 3B 规模模型）；对于全参数微调或更大规模模型（如 32B），使用了单张 **A100 80GB**。
*   **训练细节**：使用 LoRA 适配器进行微调，训练 3 个 epoch，学习率为 1e-5，batch size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上进行了两类核心任务的对比，涵盖了从 2B 到 32B 不同参数规模的 VLM 家族（Qwen2.5-VL, InternVL3, Llava 1.5）。
*   **消融实验**：详细验证了 $L_{WPCL}$、惩罚权重 $w_{pen}$、停止梯度操作（Stop-gradient）以及 $L_{CRTR}$ 的必要性。
*   **鲁棒性分析**：包括冷启动分析、跨域推荐实验、超参数敏感性测试以及少样本（Few-shot）效率分析。
*   **评价**：实验设计非常全面且客观，通过梯度动力学和几何指标（Alignment & Uniformity）深入剖析了模态崩溃的原理，论证具有说服力。

### 6. 主要结论与发现
*   **SFT 悖论验证**：实验证实标准 SFT 会导致图像模态的判别力（Separability）降至 1 以下，甚至不如未微调的原始状态。
*   **VLM2Rec 的优越性**：在所有数据集和任务中，VLM2Rec 均显著优于 SOTA 方法，尤其在任务 1 中提升幅度巨大（最高达 32%）。
*   **冷启动与泛化**：由于平衡了多模态语义，VLM2Rec 在处理新物品和跨领域迁移时表现出极强的鲁棒性。
*   **效率提升**：仅需 5-6% 的训练数据（K=1024），VLM2Rec 就能达到或超过全量训练的其他基准模型。

### 7. 优点与亮点
*   **揭示了新问题**：首次在推荐系统领域深入探讨并量化了 VLM 微调中的模态崩溃现象。
*   **即插即用**：该框架不改变 VLM 架构，生成的嵌入可直接用于现有的各种 SR 模型，具有很强的通用性。
*   **理论与实验结合**：不仅给出了性能提升，还通过几何分析（拓扑对齐）解释了为什么该方法有效。

### 8. 不足与局限
*   **计算成本**：尽管有少样本训练方案，但 VLM 处理图像序列的推理开销仍远高于传统的小型编码器，在大规模实时推荐场景中可能存在延迟挑战。
*   **模态局限**：目前仅关注文本和图像，未涉及视频、音频或更复杂的属性信息。
*   **长序列挑战**：受限于 VLM 的上下文窗口和计算复杂度，实验中最大序列长度设为 10，对于超长行为序列的处理能力尚待验证。

（完）
