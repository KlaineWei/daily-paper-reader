---
title: "KineVLA: Towards Kinematics-Aware Vision-Language-Action Models with Bi-Level Action Decomposition"
title_zh: KineVLA：通过双层动作分解构建运动学感知的视觉-语言-动作模型
authors: "Gaoge Han, Zhengqing Gao, Ziwen Li, Jiaxin Huang, Shaoli Huang, Fakhri Karray, Mingming Gong, Tongliang Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17524v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 具有双层分解的运动学感知视觉语言动作模型
tldr: 本文针对现有视觉-语言-动作（VLA）模型在处理精细运动学指令方面的不足，提出了KineVLA框架。该框架通过双层动作分解和推理Token，将任务目标的不变性与执行轨迹的运动学可变性显式解耦。研究团队还构建了包含丰富运动学属性的仿真与真实世界数据集。实验证明，KineVLA在LIBERO和Realman-75机器人平台上均实现了更精确、可控且泛化性更强的操控表现。
selection_source: fresh_fetch
motivation: 现有VLA模型对指令中包含的精细运动学属性（如方向、轨迹和姿态）建模不足，限制了复杂操控任务的精确执行。
method: 提出一种双层动作表示方法，利用推理Token作为中间变量，将高层目标指令与底层的运动学细节进行解耦与对齐。
result: 在仿真和真实机器人实验中，KineVLA在运动学敏感的任务上表现优于强基准模型，展现出更高的操控精度和可控性。
conclusion: 通过引入运动学感知的双层架构，KineVLA有效提升了机器人对复杂、细粒度语言指令的理解与执行能力。
---

## 摘要
在本文中，我们引入了一项全新的富含运动学信息的视觉-语言-动作（VLA）任务。在该任务中，语言指令在关键时刻密集地编码了从开始到完成的各种运动学属性（如方向、轨迹、朝向和相对位移），这与现有的仅粗略或部分捕捉运动学的动作指令不同，从而支持细粒度和个性化的操作。在这种设定下，任务目标保持不变，而执行轨迹必须适应指令级的运动学规范。为了应对这一挑战，我们提出了 KineVLA，这是一个视觉-语言-动作框架，通过双层动作表示和双层推理标记，显式地将目标层级的不变性与运动学层级的变异性解耦，这些标记作为显式的、受监督的中间变量来对齐语言和动作。为了支持这一任务，我们构建了跨越仿真和真实机器人平台的运动学感知 VLA 数据集，其特点是具有指令级的运动学变化和双层标注。在 LIBERO 和 Realman-75 机器人上进行的大量实验表明，KineVLA 在运动学敏感的基准测试中始终优于强大的 VLA 基线模型，实现了更精确、可控且具泛化性的操作行为。

## Abstract
In this paper, we introduce a novel kinematics-rich vision-language-action (VLA) task, in which language commands densely encode diverse kinematic attributes (such as direction, trajectory, orientation, and relative displacement) from initiation through completion, at key moments, unlike existing action instructions that capture kinematics only coarsely or partially, thereby supporting fine-grained and personalized manipulation. In this setting, where task goals remain invariant while execution trajectories must adapt to instruction-level kinematic specifications. To address this challenge, we propose KineVLA, a vision-language-action framework that explicitly decouples goal-level invariance from kinematics-level variability through a bi-level action representation and bi-level reasoning tokens to serve as explicit, supervised intermediate variables that align language and action. To support this task, we construct the kinematics-aware VLA datasets spanning both simulation and real-world robotic platforms, featuring instruction-level kinematic variations and bi-level annotations. Extensive experiments on LIBERO and a Realman-75 robot demonstrate that KineVLA consistently outperforms strong VLA baselines on kinematics-sensitive benchmarks, achieving more precise, controllable, and generalizable manipulation behaviors.

---

## 论文详细总结（自动生成）

### KineVLA：通过双层动作分解构建运动学感知的视觉-语言-动作模型总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$）主要关注“目标导向”的任务，即模型能理解“做什么”（如抓取瓶子），但往往忽略了“如何做”的精细运动学细节（如以什么角度抓取、沿什么轨迹移动、瓶子标签朝向何方）。
*   **核心问题**：在现实交互中，语义相同的目标（Goal Invariant）往往伴随着不同的运动学规范（Kinematics Variant）。现有模型由于缺乏对细粒度运动学指令的敏感性，无法满足用户个性化、精确的操作需求。
*   **整体含义**：本文旨在填补这一空白，提出了一种能够感知并执行复杂运动学指令的新型 VLA 框架。

#### 2. 方法论
KineVLA 的核心思想是通过**双层分解**将任务目标的不变性与运动学执行的变异性显式解耦。
*   **双层动作表示（Bi-Level Action Representation）**：
    *   采用残差向量量化变分自编码器（RVQ-VAE）。
    *   **目标层（Goal-level）**：捕捉低频的任务结构和语义目标。
    *   **运动学层（Kinematics-level）**：捕捉高频的运动修正、接触细节和精细参数。
    *   **训练策略**：两阶段训练。第一阶段在多任务数据上预训练目标层码本；第二阶段冻结目标层，在运动学数据集上训练精细层码本。
*   **双层推理标记（Bi-Level Reasoning Tokens）**：
    *   引入类似思维链（CoT）的生成范式。模型不仅生成动作，还生成两层文本推理：一层描述粗粒度目标，另一层解析细粒度运动学约束（如轨迹形状、接触时机、目标部位）。
*   **互信息正则化（Mutual Information Regularization）**：
    *   为了确保推理文本与动作执行的一致性，引入了互信息优化（基于 InfoNCE 损失），拉近配对的推理嵌入与动作嵌入，减少执行漂移。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO-Goal-Relabeled**：对标准 LIBERO 任务进行重标注，加入精细运动学描述。
    *   **Kine-LIBERO**：新构建的大规模仿真数据集（266K 帧），包含物体部位交互、空间关系和运动约束。
    *   **Kine-Realman**：真实世界数据集（49K 帧），使用 Realman-75 机械臂收集。
*   **Benchmark 与指标**：评估“目标成功率（Goal Success）”和“运动学成功率（Kinematics Success）”。
*   **对比方法**：
    *   **OpenVLA**：基于分箱（Binning）的强基准模型。
    *   **$\pi_{0.5}$**：基于扩散（Diffusion）的最新模型。
    *   **VQ-VLA**：采用单层向量量化动作标记的模型。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   RVQ-VAE 训练：使用 1 张 NVIDIA A100 GPU。
    *   KineVLA 微调：使用 **4 张 NVIDIA A100 GPU**。
*   **训练时长/参数**：
    *   微调过程进行了 50,000 个梯度步（50K steps）。
    *   Batch Size 为 64。
    *   动作分块长度（Action Chunk）$K=5$。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境（LIBERO 平台）和真实机器人（Realman-75）场景。
*   **消融实验**：系统地验证了双层表示（Bi-Rep）、双层推理（Bi-Rea）以及互信息优化（MI）的各自贡献。
*   **充分性与公平性**：对比了不同架构（自回归 vs 扩散）和不同标记化方案。通过“干预实验（Intervention）”验证了推理标记对动作生成的因果影响，实验设计较为严谨、客观。

#### 6. 主要结论与发现
*   **运动学敏感性**：在保持目标成功率的同时，KineVLA 在运动学成功率上显著优于所有基线模型，证明了显式解耦的必要性。
*   **推理的作用**：双层推理标记不仅提高了可解释性，还作为中间变量有效对齐了语言指令与底层控制。
*   **效率**：尽管增加了推理步骤，但 KineVLA 的推理延迟（96ms）与单层 VQ 模型（84ms）相当，远优于 OpenVLA（237ms）。

#### 7. 优点
*   **任务定义新颖**：首次明确提出了“目标不变、运动学可变”的 VLA 任务设定。
*   **架构优雅**：通过双层 RVQ-VAE 巧妙地处理了动作空间中宏观语义与微观精度的矛盾。
*   **实用性强**：提供了包含 37.8 万帧图像的精细化标注数据集，对社区具有重要价值。

#### 8. 不足与局限
*   **场景局限**：目前的实验主要集中在桌面操作（Tabletop Manipulation），尚未扩展到移动操作或更复杂的全身协调任务。
*   **坐标系依赖**：运动学指令依赖于第三人称相机坐标系（左、右、前、后），在多相机或动态视角下的鲁棒性有待验证。
*   **应用限制**：虽然模型支持精细指令，但对于未在数据集中覆盖的极端运动学约束，其泛化能力可能受限。

（完）
