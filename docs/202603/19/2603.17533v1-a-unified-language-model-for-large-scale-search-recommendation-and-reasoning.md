---
title: "A Unified Language Model for Large Scale Search, Recommendation, and Reasoning"
title_zh: 面向大规模搜索、推荐与推理的统一语言模型
authors: "Marco De Nadai, Edoardo D'Amico, Max Lefarov, Alexandre Tamborrino, Divita Vohra, Mark VanMiddlesworth, Shawn Lin, Jacqueline Wood, Jan Stypka, Eliza Klyce, Keshi Dai, Timothy Christopher Heath, Martin D. Gould, Yves Raimond, Sandeep Ghael, Tony Jebara, Andreas Damianou, Vladan Radosavljevic, Paul N. Bennett, Mounia Lalmas, Praveen Chandar"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17533v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 用于大规模推荐和检索的统一语言模型
tldr: 本研究针对大规模异构目录下的搜索、推荐与推理任务，提出了统一的语言模型框架 NEO。NEO 将预训练的解码器模型转化为无需外部工具、基于目录生成的模型，通过将项目表示为结构化标识符（SIDs）并与自然语言交织，实现了受限解码下的有效生成。该模型在千万级项目规模上表现优异，不仅超越了特定任务的基准模型，还展示了跨任务迁移能力，为构建语言可控的通用发现模型提供了实践路径。
selection_source: fresh_fetch
motivation: 现有的 LLM 在处理大规模异构目录的推荐和检索时，面临文本生成歧义、系统架构复杂及难以端到端优化等挑战。
method: 提出 NEO 框架，通过将项目表示为 SIDs 并引入受限解码技术，使模型能在单一序列中交织处理自然语言与特定类型的项目标识符。
result: 在包含超过 1000 万个项目的真实世界数据集上，NEO 在搜索、推荐和用户理解等任务中均优于强力基准模型。
conclusion: NEO 证明了通过将实体表示为独立模态并进行指令微调，可以将大规模发现能力整合进单一的、语言可控的生成式模型中。
---

## 摘要
大语言模型（LLMs）正越来越多地应用于推荐、检索和推理，但在大型异构目录上部署一个能够共同支持这些行为的单一端到端模型仍然具有挑战性。此类系统必须生成对真实物品的明确引用，处理多种实体类型，并在严格的延迟和可靠性约束下运行，而这些要求仅靠纯文本生成很难满足。虽然工具增强型推荐系统解决了部分问题，但它们引入了编排复杂性并限制了端到端优化。我们将这一场景视为一个更广泛研究问题的实例：如何使 LLM 以完全自包含的方式，对多领域实体、用户和语言进行联合推理。为此，我们推出了 NEO，这是一个将预训练的仅解码器（decoder-only）LLM 适配为无需工具、基于目录生成的框架。NEO 将物品表示为语义标识符（SIDs），并训练单一模型在共享序列中交织自然语言和有类型的物品标识符。文本提示词控制任务、目标实体类型和输出格式（ID、文本或混合），而受限解码则在不限制自由文本的情况下，保证生成目录中有效的物品。我们将这种受指令调节的可控性称为“语言可操控性”（language-steerability）。我们将 SIDs 视为一种独特的模态，并研究了通过阶段性对齐和指令微调将离散实体表示集成到 LLM 中的设计选择。我们在包含超过 1000 万个物品、跨多种媒体类型和发现任务（包括推荐、搜索和用户理解）的真实世界目录上对 NEO 进行了大规模评估。在离线实验中，NEO 始终优于强大的特定任务基准模型，并表现出跨任务迁移能力，展示了将大规模发现能力整合到单一语言可操控生成模型中的可行路径。

## Abstract
LLMs are increasingly applied to recommendation, retrieval, and reasoning, yet deploying a single end-to-end model that can jointly support these behaviors over large, heterogeneous catalogs remains challenging. Such systems must generate unambiguous references to real items, handle multiple entity types, and operate under strict latency and reliability constraints requirements that are difficult to satisfy with text-only generation. While tool-augmented recommender systems address parts of this problem, they introduce orchestration complexity and limit end-to-end optimization. We view this setting as an instance of a broader research problem: how to adapt LLMs to reason jointly over multiple-domain entities, users, and language in a fully self-contained manner. To this end, we introduce NEO, a framework that adapts a pre-trained decoder-only LLM into a tool-free, catalog-grounded generator. NEO represents items as SIDs and trains a single model to interleave natural language and typed item identifiers within a shared sequence. Text prompts control the task, target entity type, and output format (IDs, text, or mixed), while constrained decoding guarantees catalog-valid item generation without restricting free-form text. We refer to this instruction-conditioned controllability as language-steerability. We treat SIDs as a distinct modality and study design choices for integrating discrete entity representations into LLMs via staged alignment and instruction tuning. We evaluate NEO at scale on a real-world catalog of over 10M items across multiple media types and discovery tasks, including recommendation, search, and user understanding. In offline experiments, NEO consistently outperforms strong task-specific baselines and exhibits cross-task transfer, demonstrating a practical path toward consolidating large-scale discovery capabilities into a single language-steerable generative model.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **NEO** 的统一语言模型框架，旨在解决大规模异构目录下的搜索、推荐和推理任务。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在超大规模（千万级）、异构（包含播客、有声书、艺人等多种类型）的目录中，部署一个既能理解自然语言指令，又能精准引用真实物品，且无需外部工具（Tool-free）的端到端生成式模型？
*   **研究背景**：现有的 LLM 推荐系统要么依赖纯文本（易产生歧义且不稳定），要么依赖外部工具（增加系统复杂度和延迟），或者使用原子 ID（难以扩展且缺乏语义）。
*   **整体含义**：NEO 将推荐和搜索视为一种“语言操控”任务，通过将物品标识符（IDs）视为一种新的模态，实现了在单一模型中交织处理自然语言与物品引用的能力。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将物品表示为**语义标识符（Semantic IDs, SIDs）**，并采用分阶段对齐的策略，将这些离散的物品标识符集成到预训练 LLM 的词表和语义空间中。
*   **关键技术细节**：
    *   **语义标识符 (SIDs)**：利用残差 K-means（Residual K-means）对物品的内容嵌入（Embedding）进行分层量化，生成短序列（如 4 个 Token）。这种结构保证了语义相近的物品在 ID 空间上也相近。
    *   **三阶段训练流程**：
        1.  **语义基础 (Stage 1)**：构建基于内容的 SIDs。
        2.  **领域对齐 (Stage 2)**：冻结 LLM 主干，仅训练 SID 相关的嵌入层和输出头。通过“SID $\leftrightarrow$ 文本描述”的双向预测任务，让模型理解 SID 的含义。
        3.  **能力诱导 (Stage 3)**：全参数微调（或 LoRA），进行多任务指令微调（推荐、检索、解释生成、用户画像分析）。
    *   **受限解码 (Constrained Decoding)**：在推理时，利用前缀树（Trie）强制模型在生成物品 ID 时只能输出目录中真实存在的 ID 组合，确保 100% 的物品落地率。

### 3. 实验设计
*   **数据集与场景**：使用来自 Spotify 的真实世界大规模数据集，包含超过 **1000 万个物品**和 **1500 万名用户**。物品类型涵盖播客单集（Episodes）、节目（Shows）、有声书（Audiobooks）和艺人（Artists）。
*   **Benchmark（基准模型）**：
    *   **推荐任务**：对比了生产环境中的成熟基准，包括基于图神经网络（GNN）的双塔模型。
    *   **检索任务**：对比了基于历史搜索日志训练的深度检索系统（Dense Retrieval）。
*   **对比方法**：除了生产基准，还进行了大量消融实验，对比了原子 ID、LSH 量化、无对齐阶段训练、以及持续预训练（CPT）等方案。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 Nvidia H100 GPU**。
*   **软件框架**：基于 PyTorch 和 Ray 进行分布式训练，采用了序列打包（Sequence Packing）技术以提高训练效率。
*   **模型主干**：主要实验基于 **Qwen3-0.6B**，并使用 **Llama 3.2 1B** 验证了框架的通用性。

### 5. 实验数量与充分性
*   **实验规模**：在千万级物品规模上进行测试，这在学术论文中较为罕见，具有极高的工业参考价值。
*   **消融实验**：做了非常详尽的消融研究，涵盖了 ID 构造方式、对齐策略的影响、语言能力保留（MMLU 测试）等。
*   **充分性评价**：实验设计非常客观且充分。作者不仅关注了推荐准确率（HR, NDCG），还引入了 **LLM-as-a-Judge**（使用 GPT-4o-mini）来评估生成的推荐理由（Recsplanation）和用户理解的质量。

### 6. 主要结论与发现
*   **性能超越**：NEO 在推荐和检索任务上均显著优于经过多年优化的生产级基准模型。
*   **语义 ID 的必要性**：相比随机分配的原子 ID，具有语义结构的 SIDs 能显著提升模型的泛化能力。
*   **对齐的重要性**：分阶段对齐（Stage 2）是关键，它能在不破坏 LLM 原始语言能力（MMLU 分数几乎不下降）的前提下，完成物品模态的引入。
*   **跨任务迁移**：多任务联合训练表现出正向迁移效应，即同时学习推荐和搜索任务能提升各自的效果。

### 7. 优点
*   **端到端统一性**：将搜索、推荐和解释生成整合进一个模型，简化了系统架构。
*   **语言可操控性**：用户可以通过自然语言指令灵活控制输出（例如：“推荐一个搞笑的播客”或“解释为什么推荐这个艺人”）。
*   **工业级可扩展性**：证明了生成式推荐模型可以扩展到千万级物品规模，并保持较低的推理延迟。

### 8. 不足与局限
*   **冷启动挑战**：虽然 SIDs 基于内容，但在完全没有历史行为的极度冷启动场景下，NEO 的表现仍略逊于使用了专门侧边特征（Side Features）的基准模型。
*   **ID 冲突处理**：当多个物品被映射到同一个 SID 桶时，目前采用基于流行度的简单消歧，这可能引入一定的流行度偏差。
*   **模型规模限制**：实验主要集中在 1B 以下的小参数模型，虽然效果好，但更大规模模型（如 7B+）在这一框架下的成本和收益尚未深入探讨。

（完）
