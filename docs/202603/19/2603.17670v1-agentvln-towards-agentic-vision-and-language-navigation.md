---
title: "AgentVLN: Towards Agentic Vision-and-Language Navigation"
title_zh: AgentVLN：迈向智能体视觉语言导航
authors: "Zihao Xin, Wentong Li, Yixuan Jiang, Ziyuan Huang, Bin Wang, Piji Li, Jianke Zhu, Jie Qin, Shengjun Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17670v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 适用于边缘计算平台的VLM-as-Brain具身导航框架
tldr: 本研究提出AgentVLN，一个针对视觉语言导航（VLN）的高效具身智能框架。针对现有系统在空间感知和2D-3D表示不匹配方面的局限，该框架采用VLM作为大脑，解耦语义推理与感知规划。通过跨空间表示映射、主动探索策略及查询驱动感知思维链（QD-PCoT），AgentVLN显著提升了长程导航性能，并支持边缘计算部署。
selection_source: fresh_fetch
motivation: 现有VLN系统受限于空间感知能力不足、2D-3D表示不匹配以及单目尺度歧义，难以处理复杂长程导航任务。
method: 提出VLM-as-Brain范式，结合跨空间表示映射、主动探索策略及QD-PCoT机制，并构建了AgentVLN-Instruct指令微调数据集。
result: 实验表明AgentVLN在长程VLN基准测试中持续超越现有SOTA方法，且具备轻量化部署能力。
conclusion: AgentVLN为下一代具身导航模型提供了一个实用且高效的范式，有效解决了复杂环境下的空间理解与推理难题。
---

## 摘要
视觉语言导航（VLN）要求具身智能体在未知环境中将复杂的自然语言指令转化为长程导航任务。尽管视觉语言模型（VLM）提供了强大的二维语义理解能力，但目前的 VLN 系统仍受限于有限的空间感知、2D-3D 表示不匹配以及单目尺度歧义。在本文中，我们提出了 AgentVLN，这是一种新颖且高效的具身导航框架，可部署在边缘计算平台上。我们将 VLN 建模为部分可观测半马尔可夫决策过程（POSMDP），并引入了“VLM 作为大脑”的范式，通过即插即用的技能库将高层语义推理与感知和规划解耦。为了解决多级表示的不一致性，我们设计了一种跨空间表示映射，将感知层的三维拓扑路标投影到图像平面，为 VLM 生成像素对齐的视觉提示。基于这一桥梁，我们集成了上下文感知的自我修正和主动探索策略，以从遮挡中恢复并抑制长轨迹上的误差累积。为了进一步解决非结构化环境中指令的空间歧义，我们提出了一种查询驱动的感知思维链（QD-PCoT）方案，使智能体具备主动寻求几何深度信息的元认知能力。最后，我们构建了 AgentVLN-Instruct，这是一个大规模指令微调数据集，具有基于目标可见性的动态阶段路由。大量实验表明，AgentVLN 在长程 VLN 基准测试中始终优于先前的最先进（SOTA）方法，为下一代具身导航模型的轻量化部署提供了一种实用的范式。代码：https://github.com/Allenxinn/AgentVLN。

## Abstract
Vision-and-Language Navigation (VLN) requires an embodied agent to ground complex natural-language instructions into long-horizon navigation in unseen environments. While Vision-Language Models (VLMs) offer strong 2D semantic understanding, current VLN systems remain constrained by limited spatial perception, 2D-3D representation mismatch, and monocular scale ambiguity. In this paper, we propose AgentVLN, a novel and efficient embodied navigation framework that can be deployed on edge computing platforms. We formulate VLN as a Partially Observable Semi-Markov Decision Process (POSMDP) and introduce a VLM-as-Brain paradigm that decouples high-level semantic reasoning from perception and planning via a plug-and-play skill library. To resolve multi-level representation inconsistency, we design a cross-space representation mapping that projects perception-layer 3D topological waypoints into the image plane, yielding pixel-aligned visual prompts for the VLM. Building on this bridge, we integrate a context-aware self-correction and active exploration strategy to recover from occlusions and suppress error accumulation over long trajectories. To further address the spatial ambiguity of instructions in unstructured environments, we propose a Query-Driven Perceptual Chain-of-Thought (QD-PCoT) scheme, enabling the agent with the metacognitive ability to actively seek geometric depth information. Finally, we construct AgentVLN-Instruct, a large-scale instruction-tuning dataset with dynamic stage routing conditioned on target visibility. Extensive experiments show that AgentVLN consistently outperforms prior state-of-the-art methods (SOTA) on long-horizon VLN benchmarks, offering a practical paradigm for lightweight deployment of next-generation embodied navigation models. Code: https://github.com/Allenxinn/AgentVLN.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentVLN: Towards Agentic Vision-and-Language Navigation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
视觉语言导航（VLN）要求智能体根据自然语言指令在未知的3D物理环境中导航。尽管现有的视觉语言模型（VLM）在2D图像理解上表现出色，但在具身导航任务中面临三大挑战：
*   **空间感知能力不足**：预训练VLM缺乏对3D几何和度量尺度的直观理解。
*   **2D-3D表示不匹配**：高层语义推理（2D图像空间）与底层路径规划（3D物理空间）之间存在断层。
*   **单目尺度歧义**：仅靠RGB图像难以准确判断目标的深度和空间方位，导致定位失败。
*   **部署困难**：现有SOTA模型通常参数量巨大（≥7B），依赖云端推理，难以满足边缘设备（如机器人）的实时控制需求。

### 2. 方法论：核心思想与关键技术
论文提出了 **AgentVLN** 框架，核心思想是将导航建模为**部分可观测半马尔可夫决策过程（POSMDP）**，并采用“**VLM作为大脑**”的范式。

*   **VLM-as-Brain 架构**：将高层语义推理与底层技能执行解耦。VLM作为中央控制器，通过调用“感知类技能”（如构建局部地图）和“规划类技能”（如执行多步移动）来完成任务。
*   **跨空间表示映射（Cross-Space Mapping）**：
    *   通过相机内参和位姿，将感知层计算出的3D拓扑路标（Waypoints）反向投影到2D图像平面。
    *   生成**像素对齐的视觉提示**（Visual Prompts），使VLM能直接在视觉空间内选择路径，消除了2D语义与3D结构间的鸿沟。
*   **查询驱动感知思维链（QD-PCoT）**：
    *   当指令存在空间歧义时，智能体不会盲目预测，而是主动生成自然语言查询（如“椅子离我多远？”）。
    *   调用感知技能获取深度/几何信息，并将其作为文本反馈纳入上下文，通过多轮推理输出精确的目标像素坐标。
*   **上下文感知自我修正与主动探索**：在遇到遮挡或偏离轨迹时，利用历史上下文生成细粒度的原子动作（前、左、右）进行航向校正。

### 3. 实验设计
*   **数据集**：在 **R2R-CE**（连续环境下的房间到房间导航）和 **RxR-CE**（多语言、长程导航）两个主流基准上进行评估。
*   **Benchmark 指标**：成功率（SR）、路径长度加权成功率（SPL）、导航误差（NE）、Oracle成功率（OS）等。
*   **对比方法**：涵盖了单系统架构（如 NaVid, StreamVLN, JanusVLN）和双系统架构（如 InternVLA-N1, DualVLN, EfficientVLN）等近20种SOTA模型。
*   **真实世界验证**：在 Unitree Go2 四足机器人上部署，测试了室内外多种复杂场景。

### 4. 资源与算力
*   **底座模型**：采用轻量级的 **Qwen2.5-VL-3B**。
*   **硬件资源**：训练在拥有 **32张 NVIDIA A100 GPU** 的计算集群上完成。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 128，学习率 2e-5，采用余弦退火策略。
*   **推理部署**：由于模型仅 3B 参数且无需额外的深度估计模块，支持在 **Jetson 边缘计算板卡**上进行本地实时推理。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个大型数据集上进行了全面对比，并构建了包含动态路由机制的 **AgentVLN-Instruct** 指令微调数据集。
*   **消融实验**：针对“VLM-as-Brain”范式、跨空间映射、自我修正策略（CDFG）和 QD-PCoT 进行了逐项剥离实验，验证了各模块的增益。
*   **上下文长度分析**：探讨了不同时间窗口（帧数）对导航性能的影响，确定了 8 帧为最优平衡点。
*   **客观性**：实验对比了相同参数量级及更大参数量级的模型，结果显示 AgentVLN 在更小的参数规模下实现了更高的 SPL，证明了其效率和路径规划的优越性。

### 6. 主要结论与发现
*   **性能超越**：AgentVLN-3B 在 R2R-CE 上的 SR 达到 67.2%，SPL 达到 64.7%，显著超过了参数量更大的 InternVLA-N1-8.3B（SR 58.2%, SPL 54.0%）。
*   **效率优势**：相比于引入额外深度模块的 EfficientVLN，AgentVLN 在不增加参数负担的情况下，SR 提升了约 3%。
*   **解耦的有效性**：将 3D 几何信息转化为 2D 视觉提示，极大地降低了 VLM 理解物理空间的难度。
*   **元认知能力**：QD-PCoT 机制证明了主动询问缺失信息比被动接受多模态输入更能有效解决空间歧义。

### 7. 优点（亮点）
*   **轻量且强大**：通过架构创新而非堆砌参数，实现了在边缘设备上运行 SOTA 性能的导航模型。
*   **解决 2D-3D 鸿沟**：跨空间映射机制非常巧妙，将复杂的 3D 几何问题简化为 VLM 擅长的 2D 视觉匹配问题。
*   **主动交互推理**：QD-PCoT 引入了类似人类的“先询问再行动”逻辑，增强了模型在模糊指令下的鲁棒性。
*   **即插即用**：技能库设计使得模型易于扩展到不同的传感器（如 LiDAR）或机器人平台。

### 8. 不足与局限
*   **对感知技能的依赖**：模型的表现高度依赖于底层感知技能（如 SLAM 或深度图获取）的准确性，若底层传感器失效，高层推理也将受阻。
*   **动态环境挑战**：虽然在真实世界做了测试，但论文主要侧重于静态或半静态环境的导航，对于行人密集等高度动态环境的避障和交互讨论较少。
*   **训练数据偏差**：AgentVLN-Instruct 主要基于 Habitat 模拟器生成，尽管加入了噪声，但在极端复杂的野外环境中的泛化能力仍有待进一步验证。

（完）
