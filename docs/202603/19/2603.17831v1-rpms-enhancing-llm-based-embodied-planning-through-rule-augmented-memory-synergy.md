---
title: "RPMS: Enhancing LLM-Based Embodied Planning through Rule-Augmented Memory Synergy"
title_zh: RPMS：通过规则增强的记忆协同提升基于大语言模型的具身规划
authors: "Zhenhang Yuan, Shenghai Yuan, Lihua Xie"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17831v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 通过规则增强记忆提升闭环环境下的LLM具身规划
tldr: 针对具身智能体在复杂环境中因动作前提约束严苛和状态漂移导致的规划失败问题，本文提出RPMS架构。该架构通过结构化规则检索确保动作可行性，利用轻量级信念状态过滤情节记忆，并采用规则优先的仲裁机制解决冲突。实验表明，RPMS在ALFWorld和ScienceWorld基准测试中显著提升了不同规模LLM的成功率，证明了规则与记忆协同作用的有效性。
selection_source: fresh_fetch
motivation: LLM在具身任务中常因无法满足动作前提条件及状态感知漂移，陷入无效动作生成的恶性循环。
method: 提出RPMS架构，结合结构化规则检索、基于信念状态的记忆门控以及规则优先的冲突仲裁机制。
result: "在ALFWorld任务中，Llama 3.1 8B和Claude 4.5的成功率分别提升了23.9%和11.9%，且在ScienceWorld上也表现出良好的泛化性。"
conclusion: 规则检索是提升性能的关键因素，而情节记忆需在当前状态过滤和显式规则约束下才能发挥稳定的正面作用。
---

## 摘要
LLM 智能体在封闭世界具身环境中经常失败，因为动作必须满足严格的前置条件（如位置、物品栏和容器状态），且失败反馈稀疏。我们识别出两种结构耦合的失败模式：(P1) 无效动作生成和 (P2) 状态漂移，两者在退化循环中相互放大。我们提出了 RPMS，这是一种冲突管理的架构，它通过结构化规则检索强制执行动作的可行性，通过轻量级信念状态过滤记忆的适用性，并通过规则优先的仲裁机制解决两个来源之间的冲突。在 ALFWorld（134 个未见任务）上，RPMS 使用 Llama 3.1 8B 实现了 59.7% 的单次尝试成功率（比基线提高 23.9 个百分点），使用 Claude Sonnet 4.5 实现了 98.5%（提高 11.9 个百分点）；在 8B 模型的提升中，仅规则检索就贡献了 +14.9 个百分点（具有统计学显著性），使其成为主导因素。一个关键发现是情境记忆是有条件有用的：在没有接地的情况下使用时，它会损害某些任务类型的性能，但一旦经过当前状态过滤并受到显式动作规则的约束，它就会变成稳定的净增益。将 RPMS 适配到使用 GPT-4 的 ScienceWorld，在所有消融条件下均获得了一致的提升（平均得分 54.0，而 ReAct 基线为 44.9），提供了核心机制在结构迥异的环境中依然有效的迁移证据。

## Abstract
LLM agents often fail in closed-world embodied environments because actions must satisfy strict preconditions -- such as location, inventory, and container states -- and failure feedback is sparse. We identify two structurally coupled failure modes: (P1) invalid action generation and (P2) state drift, each amplifying the other in a degenerative cycle. We present RPMS, a conflict-managed architecture that enforces action feasibility via structured rule retrieval, gates memory applicability via a lightweight belief state, and resolves conflicts between the two sources via rules-first arbitration. On ALFWorld (134 unseen tasks), RPMS achieves 59.7% single-trial success with Llama 3.1 8B (+23.9 pp over baseline) and 98.5% with Claude Sonnet 4.5 (+11.9 pp); of the 8B gain, rule retrieval alone contributes +14.9 pp (statistically significant), making it the dominant factor. A key finding is that episodic memory is conditionally useful: it harms performance on some task types when used without grounding, but becomes a stable net positive once filtered by current state and constrained by explicit action rules. Adapting RPMS to ScienceWorld with GPT-4 yields consistent gains across all ablation conditions (avg. score 54.0 vs. 44.9 for the ReAct baseline), providing transfer evidence that the core mechanisms hold across structurally distinct environments.

---

## 论文详细总结（自动生成）

以下是对论文《RPMS: Enhancing LLM-Based Embodied Planning through Rule-Augmented Memory Synergy》的结构化深入总结：

### 1. 核心问题与整体含义
论文针对大语言模型（LLM）在**封闭世界具身环境**（如 ALFWorld）中规划失败的根本原因进行了深入分析。研究发现，LLM 智能体常因无法满足严苛的动作前置条件（如位置、物品栏状态）以及环境反馈稀疏（仅提示“Nothing happens”）而陷入困境。
论文识别出两种耦合的失败模式：
*   **(P1) 无效动作生成**：生成的动作在语言上合理但在物理环境中不可行。
*   **(P2) 状态漂移**：智能体对环境状态的认知与真实状态脱节。
这两者形成恶性循环，导致规划彻底失效。RPMS 的核心目标是通过**规则增强**与**记忆协同**，确保动作的可执行性与状态的一致性。

### 2. 方法论：RPMS 架构
RPMS 采用了一种冲突管理的架构，主要包含以下四个关键组件：
*   **轻量级信念状态追踪 (Belief State Tracking)**：不依赖 LLM，而是通过确定性的模式匹配更新智能体的位置、手部状态、容器开关状态等关键变量，生成“状态签名”。
*   **分层规则手册 (Hierarchical Rule Manual)**：将知识分为三层：
    1.  **通用规则 (Universal)**：环境无关的搜索策略和失败恢复逻辑。
    2.  **领域规则 (Domain)**：针对特定任务类型（如加热、清洁）的标准流程。
    3.  **环境规则 (Environment)**：具体的动作语义和前置条件（如加热动作是原子性的，物体保持在手中）。
*   **状态一致性过滤的情节记忆 (Episodic Memory with SCF)**：存储过去的成功片段、失败教训和验证过的模式。在检索时，通过“状态一致性过滤（SCF）”剔除与当前手部状态或位置不匹配的记忆，防止引入误导性建议。
*   **规则优先仲裁 (Rules-First Arbitration)**：当检索到的记忆与显式规则冲突时，强制执行规则（硬过滤）或添加警告标签（软标注），确保物理约束高于经验建议。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：包含 134 个未见过的（Out-of-distribution）测试任务，涵盖 6 类具身任务。
    *   **ScienceWorld**：用于验证跨环境泛化性，包含 26 类科学实验任务，共 241 个测试回合。
*   **Benchmark 与对比方法**：
    *   核心基线是 **ReAct**（推理+行动）。
    *   对比了不同规模的模型：Llama 3.1 8B/70B、Claude Sonnet 4.5 以及 GPT-4。
    *   进行了 **2×2 因子消融实验**：对比“无规则无记忆（基线）”、“仅规则”、“仅记忆”和“全功能 RPMS”。

### 4. 资源与算力
*   **模型推理**：Llama 3.1 8B 使用 DeepInfra 服务，GPT-4 和 Claude 通过 API 调用。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法是推理侧增强（Prompt-only），不涉及大规模模型微调，其主要的计算开销在于 Prompt 长度的增加（每步额外增加约 800-1200 tokens）。

### 5. 实验数量与充分性
*   **实验规模**：在 ALFWorld 上测试了 134 个任务，在 ScienceWorld 上测试了 241 个回合，样本量足以支撑统计分析。
*   **充分性与公平性**：
    *   采用了 **McNemar 检验**进行统计显著性分析。
    *   所有消融实验均在相同的模型、Prompt 模板和解码参数下进行，确保了对比的公平性。
    *   通过跨环境（ALFWorld 到 ScienceWorld）的迁移实验，验证了架构的普适性而非过拟合单一环境。

### 6. 主要结论与发现
*   **性能飞跃**：在 Llama 3.1 8B 上，RPMS 将成功率从 35.8% 提升至 59.7%（+23.9%）；在 Claude 4.5 上达到了 98.5%。
*   **规则是主导因素**：在 8B 模型的提升中，规则检索贡献了 14.9 个百分点，是解决 P1 问题的关键。
*   **记忆的“有条件有用性”**：单纯增加记忆在某些任务（如 Look 类型）中反而会降低性能（从 55.6% 降至 11.1%），因为不匹配的经验会干扰决策。只有在规则约束和状态过滤下，记忆才能转化为稳定的正向增益。
*   **效率提升**：RPMS 显著减少了完成任务所需的平均步数，并能更早地识别错误而非耗尽步数上限。

### 7. 优点
*   **结构化解决耦合问题**：明确区分并处理了动作可行性（规则）与经验适用性（记忆过滤）之间的冲突。
*   **无需训练**：作为一种推理时模块化方案，易于部署且能显著提升小参数模型（如 8B）的能力。
*   **鲁棒的仲裁机制**：通过“规则优先”解决了 RAG 系统中常见的知识冲突问题。
*   **跨领域迁移能力**：在结构迥异的 ScienceWorld 中依然有效，证明了分层规则设计的合理性。

### 8. 不足与局限
*   **依赖先验经验**：情节记忆模块仍需要预先收集交互轨迹，无法在完全零样本的情况下生成经验。
*   **规则编写成本**：虽然通用规则可迁移，但第三层（环境特定规则）仍需人工编写或针对新环境进行适配。
*   **过滤粒度**：状态一致性过滤目前主要依赖手部占用等简单指标，对于更复杂的前置条件匹配可能仍显粗糙。
*   **仲裁层级**：目前的仲裁是在条目级别进行的，无法对记忆条目内部的细微冲突进行更精细的处理。

（完）
