---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于机器人策略学习的世界-动作模型
tldr: 针对现有世界-动作模型（WAM）推理开销大且视觉与动作表示耦合的问题，本文提出 GigaWorld-Policy。该模型采用以动作为中心的设计，通过因果关系解耦动作预测与视频生成，使推理时可跳过视频生成以提高效率。在多项机器人任务中，该模型比领先基准快9倍，且成功率显著提升，展现了高效且精准的机器人策略学习能力。
selection_source: fresh_fetch
motivation: 现有世界-动作模型推理速度慢且动作预测过度依赖视频生成质量，限制了其实际部署。
method: 提出一种以动作为中心的架构，通过因果设计解耦动作与视频生成，并利用大规模机器人数据集进行预训练。
result: "在真实机器人平台上，推理速度比Motus快9倍，任务成功率提升7%，在RoboTwin 2.0上性能提升95%。"
conclusion: GigaWorld-Policy 证明了通过解耦视觉动力学与动作预测，可以在保持高性能的同时大幅提升机器人策略的推理效率。
---

## 摘要
从预训练视频生成骨干网络初始化的世界-动作模型（WAM）在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模通常会使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们推出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习二维像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练表述为两个耦合的组件：模型在当前观测的条件下预测未来动作序列，并同时在预测动作和相同观测的条件下生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束促使产生符合物理规律的动作。通过防止未来视频标记（tokens）影响动作标记的因果设计，显式的未来视频生成在推理阶段是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化的大规模机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其适配为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

这是一份关于论文《GigaWorld-Policy: An Efficient Action-Centered World–Action Model》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的机器人策略模型主要分为两类：**VLA（视觉-语言-动作）模型**受限于监督信号稀疏，难以学习物理一致性；**WAM（世界-动作模型）**虽然通过预测未来视频引入了密集监督，但面临推理延迟高（必须生成视频）和动作预测过度依赖视频质量（误差累积）的问题。
*   **研究动机**：开发一种既能利用世界模型提供的密集物理先验，又能像传统 VLA 一样实现低延迟、高鲁棒性推理的“以动作为中心”的世界-动作模型。

### 2. 论文提出的方法论
*   **核心思想**：提出 **GigaWorld-Policy**，通过因果掩码（Causal Masking）设计，在训练时将未来视觉动力学作为辅助推理信号和密集监督源，但在推理时使视频生成变为“可选”步骤。
*   **关键技术细节**：
    *   **架构设计**：基于 5B 参数的扩散 Transformer（Wan 2.2），将多视角图像、机器人状态、动作序列和文本指令统一 Token 化。
    *   **因果自注意力掩码**：强制要求动作 Token 仅能关注当前观测和状态，而未来视频 Token 可以关注动作。这种设计实现了动作预测与视频生成的解耦。
    *   **训练目标**：采用**流匹配（Flow Matching）**算法，同时优化动作预测损失（$\mathcal{L}_{action}$）和未来视频预测损失（$\mathcal{L}_{video}$）。
    *   **课程学习管线**：
        1.  **通用视频预训练**：继承大规模网络视频知识。
        2.  **具身数据预训练**：利用约 10,000 小时的机器人和人类抓取视频（如 Ego4D, Open X-Embodiment）注入物理和交互先验。
        3.  **目标任务微调**：在特定机器人的轨迹数据上进行指令对齐。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：RoboTwin 2.0，包含 50 多个具身智能操纵任务，涉及清理、放置、堆叠等。
    *   **真实世界**：使用 AgileX PiPER 6自由度机械臂，执行“清理桌面”、“扫描二维码”、“扫垃圾”、“叠碗”四个复杂任务。
*   **Benchmark 与对比方法**：
    *   **VLA 类**：$\pi$ 0.5、GigaBrain-0、X-VLA。
    *   **WAM 类**：Motus、Cosmos-Policy。
*   **评估指标**：成功率（SR）和单步推理延迟（Latency）。

### 4. 资源与算力
*   **训练算力**：论文提到在公共数据集上预训练消耗了 **6000 GPU 小时**。
*   **推理算力**：推理性能测试基于 **NVIDIA A100 GPU**。
*   **模型规模**：骨干网络拥有 **5B（50亿）参数**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 50 多个任务，每个任务在清洁和随机场景下分别测试 100 次。
    *   真实世界实验中，每个任务进行 20 次尝试，评估标准细化到动作阶段（如抓取成功计 0.5 分）。
*   **消融实验**：针对预训练配置、未来帧预测数量（$\Delta$ 间隔）、因果掩码的作用、数据效率（Data Efficiency）等进行了详细消融。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实的迁移，并对比了当前最前沿的同类模型，客观地展示了速度与成功率的权衡。

### 6. 主要结论与发现
*   **高效性**：在 A100 上推理延迟仅为 **0.36s**，比领先的 WAM 模型 Motus 快 **9 倍**。
*   **高性能**：在真实世界任务中，成功率比 Motus 高出 **7%**，比 $\pi$ 0.5 高出 **14%**。
*   **数据效率**：GigaWorld-Policy 仅需 **10%** 的训练数据即可达到 VLA 基准模型使用 100% 数据时的性能。
*   **视觉辅助作用**：实验证明，即使推理时不生成视频，训练阶段的视频预测任务也能显著提升动作预测的准确性（成功率从 0.65 提升至 0.83）。

### 7. 优点
*   **解耦创新**：通过因果掩码巧妙解决了 WAM 模型推理慢的痛点，实现了“训练时有世界模型监督，推理时无视频生成负担”。
*   **预训练策略**：三阶段课程学习充分利用了互联网视频、人类演示和机器人数据，增强了模型的泛化能力。
*   **实用性强**：在保持高成功率的同时达到了接近实时的控制频率，更符合工业部署需求。

### 8. 不足与局限
*   **长程任务依赖**：虽然推理时视频可选，但对于极度复杂的长程规划任务，如果不进行视频回放（Rollout），模型是否还能保持高成功率尚存疑问。
*   **算力门槛**：5B 参数量对于嵌入式机器人控制器仍有较大压力，论文未讨论模型量化或蒸馏至更小端侧模型的效果。
*   **多视角融合**：目前采用简单的图像拼接方式处理多视角，可能未充分挖掘视角间的几何关联。

（完）
