---
title: "Action Draft and Verify: A Self-Verifying Framework for Vision-Language-Action Model"
title_zh: Action Draft and Verify：一种视觉-语言-动作模型的自验证框架
authors: "Chen Zhao, Zhuoran Wang, Haoyang Li, Shifeng Bao, Guanlin Li, Youhe Feng, Yang Li, Jie Tang, Jing Zhang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18091v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 视觉-语言-动作模型的自验证框架
tldr: 针对具身智能任务中扩散模型生成动作的高精度与自回归模型泛化性之间的权衡，本文提出ADV框架。该框架利用扩散动作专家生成多个候选动作块，并由视觉语言模型（VLM）通过单次前向传播的困惑度指标进行评分和筛选。这种“先草拟后验证”的机制结合了两种范式的优势，显著提升了机器人在复杂环境下的成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 旨在结合扩散模型的高精度动作生成能力与自回归模型在分布外环境中的强泛化性。
method: 提出ADV框架，由扩散专家生成候选动作，再利用VLM的困惑度指标在单次推理中对候选动作进行评分和重排序。
result: "在模拟环境和真实世界中，该方法相比纯扩散基准分别提升了4.3%和19.7%的任务成功率。"
conclusion: ADV框架通过自验证机制有效融合了不同动作生成范式的优点，为提升VLA模型的性能和可靠性提供了新思路。
---

## 摘要
视觉-语言-动作（VLA）模型近期在具身任务中展现出强大的性能。现代 VLA 通常采用扩散动作专家来高效生成高精度的连续动作块，而自回归生成在底层控制方面可能较慢且精度较低。然而，自回归范式仍能提供互补的先验知识，从而提高在分布外环境中的鲁棒性和泛化能力。为了结合这两种范式的优势，我们提出了 Action-Draft-and-Verify (ADV)：由扩散动作专家草拟多个候选动作块，并由视觉语言模型（VLM）通过类困惑度（perplexity-style）指标在单次前向传播中对所有候选动作进行评分，从而选出一个动作。在相同的骨干网络、训练数据和动作块长度下，ADV 在模拟环境和真实世界中的成功率分别比基于扩散的基准模型提高了 4.3 个百分点和 19.7 个百分点，且仅增加了单次 VLM 重排序的开销。

## Abstract
Vision-Language-Action (VLA) models have recently demonstrated strong performance across embodied tasks. Modern VLAs commonly employ diffusion action experts to efficiently generate high-precision continuous action chunks, while auto-regressive generation can be slower and less accurate at low-level control. Yet auto-regressive paradigms still provide complementary priors that can improve robustness and generalization in out-of-distribution environments. To leverage both paradigms, we propose Action-Draft-and-Verify (ADV): diffusion action expert drafts multiple candidate action chunks, and the VLM selects one by scoring all candidates in a single forward pass with a perplexity-style metric. Under matched backbones, training data, and action-chunk length, ADV improves success rate by +4.3 points in simulation and +19.7 points in real-world over diffusion-based baseline, with a single-pass VLM reranking overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Action Draft and Verify: A Self-Verifying Framework for Vision-Language-Action Model》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型主要分为两类：**扩散模型（Diffusion）**擅长生成高精度、连续的动作块，但在分布外（OOD）环境下容易失效且缺乏纠错能力；**自回归模型（Auto-regressive）**虽然底层控制精度较低，但能更好地利用预训练语言模型的先验知识，展现出更强的鲁棒性和泛化性。
*   **核心问题**：如何结合扩散模型的高精度生成能力与自回归模型的强泛化/验证能力，以提升机器人在复杂及未知环境下的任务成功率？

### 2. 方法论：Action Draft and Verify (ADV)
ADV 框架将推理过程分为“草拟”和“验证”两个阶段：
*   **草拟阶段（Draft）**：利用一个轻量级的扩散动作专家（Diffusion Action Expert），基于 VLM 提取的特征，通过注入不同的噪声并行生成 $M$ 个候选动作轨迹块（Action Chunks）。
*   **验证阶段（Verify）**：
    *   **Textual FAST 编码**：为了让 VLM 能够理解动作，提出 Textual FAST 技术。它先利用 FAST 算法压缩动作，再将其渲染为文本字符串，最后使用 VLM 原生的 Tokenizer 进行编码。这避免了引入低频的动作专用 Token，更好地对齐了 VLM 的预训练分布。
    *   **评分与筛选**：VLM 对所有候选动作块进行单次并行前向传播，计算每个候选块的**类困惑度（Perplexity-style）得分**（即长度归一化的负对数似然 NLL）。
    *   **决策**：选择得分最低（即 VLM 认为最符合逻辑/先验）的动作块作为最终执行指令。
*   **训练目标**：采用联合训练（Co-training）策略，同时优化自回归损失（$L_{VLM}$）和扩散流匹配损失（$L_{dif}$）。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟环境**：LIBERO（包含 Spatial, Object, Goal, Long 四类任务）和 RoboTwin 2.0（包含 Easy 和 Hard/OOD 模式）。
    *   **真实世界**：设计了推方块、清理桌面、指令取放、精准挂杯子四个任务，并包含对“未见过的指令和物体”的泛化测试。
*   **对比基准（Baselines）**：
    *   **Model-FAST**：纯自回归生成的 VLA。
    *   **Model-Diffusion**：纯扩散模型生成的 VLA。
    *   **SOTA 模型**：包括 $\pi_{0.5}$、OpenVLA、Octo、HybridVLA 等。
*   **评估指标**：成功率（Success Rate）、平均成功步数（越低越好）、恢复尝试次数（越高代表纠错能力越强）、抓取前碰撞率。

### 4. 资源与算力
*   **模型规模**：动作专家模型约为 500M 参数（16层 Transformer）。VLM 骨干网络使用了 Qwen2.5-VL-3B 和 InternVL3.5-2B。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。但在推理效率方面提到，ADV 在 Qwen2.5-VL-3B 上可达到约 53.3Hz 的控制频率（32步动作块），证明了其轻量化特性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了数千次模拟器测试和大量的真机实验（真机数据集包含 2000 条轨迹，50 多种物体）。
*   **消融实验**：非常充分。研究者分析了 K-th 排名动作的影响、噪声鲁棒性、不同 Tokenization 方式（Action Bins, FAST, VLA-0）的对比，以及在诱导错误状态下的恢复能力。
*   **公平性**：在对比实验中，确保了骨干网络、训练数据和动作块长度完全一致，体现了客观性。

### 6. 主要结论与发现
*   **性能提升**：ADV 在模拟环境中比纯扩散基准提升了 4.3%，在真实世界中提升了 **19.7%**。
*   **验证机制的本质**：VLM 验证器的作用主要是**“过滤掉极差的动作”**（如导致碰撞或无意义的动作），而非必须精准选出全局最优动作。实验显示选择前 3 名的动作成功率相近，但选到第 4 名后成功率大幅下降。
*   **OOD 鲁棒性**：在分布外环境下，ADV 显著增加了机器人的恢复尝试次数，并降低了因抖动导致的无效碰撞。
*   **Token 策略**：Textual FAST 证明了将动作转化为“文本感”的表征对于 VLM 评分的可靠性至关重要。

### 7. 优点
*   **即插即用**：作为一个推理框架，它可以应用于多种现有的“VLM + 扩散专家”结构的 VLA 模型（如 $\pi_{0.5}$）。
*   **兼顾精度与逻辑**：解决了扩散模型在复杂逻辑判断上的短板，利用 VLM 的常识来纠正物理执行器的偏差。
*   **高效性**：通过单次并行评分代替了逐 Token 的自回归生成，推理开销相对较小。

### 8. 不足与局限
*   **依赖候选质量**：如果扩散专家生成的 $M$ 个候选动作全部失效，验证阶段也无法“无中生有”创造出正确的动作。
*   **推理延迟**：虽然是单次前向，但相比纯扩散模型仍增加了计算量，对于实时性要求极高的极高速任务可能存在挑战。
*   **训练成本**：需要联合训练自回归和扩散两个分支，相比单一范式增加了训练复杂度。

（完）
