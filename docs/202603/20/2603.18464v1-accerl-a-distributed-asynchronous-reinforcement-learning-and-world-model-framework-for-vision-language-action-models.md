---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 用于VLA模型的分布式强化学习和世界模型框架
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习训练中面临的计算效率瓶颈和数据获取难题。
method: 采用完全异步且解耦的架构物理隔离核心环节，并引入即插即用的世界模型生成虚拟经验。
result: 在LIBERO基准上取得SOTA性能，实现吞吐量的超线性扩展及卓越的硬件利用率。
conclusion: AcceRL证明了异步解耦架构与世界模型结合能显著提升大规模VLA模型的训练效率与稳定性。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

### AcceRL：面向视觉-语言-动作（VLA）模型的分布式异步强化学习与世界模型框架总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：视觉-语言-动作（VLA）模型在具身智能领域展现了巨大潜力，但其强化学习（RL）微调面临严峻挑战。
*   **核心问题**：
    *   **系统瓶颈**：传统同步RL框架存在“长尾效应”，即高能效GPU必须等待慢速物理模拟器完成采样，导致硬件利用率低下。
    *   **数据饥渴**：大规模VLA模型（如7B参数）需要海量交互数据，而物理模拟器的运行速度和推理延迟限制了数据获取效率。
*   **整体含义**：本文提出了AcceRL框架，通过物理隔离训练、推理和采样，并引入可训练的世界模型（World Model），旨在解决大规模具身智能模型训练慢、效率低的问题。

#### 2. 论文提出的方法论
*   **核心思想**：采用**完全异步且解耦**的架构，将系统分为训练池、推理池和采样池，消除同步障碍。
*   **关键技术细节**：
    *   **宏观异步（训练与采样解耦）**：训练器（Trainer）与采样器（Rollout Worker）独立运行。采样器持续向分布式重放池发送轨迹，训练器利用ZeRO优化技术并行更新参数，并通过NCCL广播同步权重。
    *   **微观异步（交互与推理解耦）**：采用“推理即服务”（Inference-as-a-Service）模式。采样器仅负责环境交互，推理请求发送至中心化推理池，通过动态窗口机制实现高效批处理（Batching）。
    *   **世界模型增强（AcceRL-WM）**：集成基于扩散模型（Diffusion-based）的观测模型和奖励模型。智能体可以在“想象”中进行虚拟采样，绕过物理模拟器的速度限制。
    *   **算法优化**：
        *   **GIPO算法**：使用高斯重要性采样策略优化，替代标准PPO的硬截断，缓解异步带来的策略滞后问题。
        *   **词表瘦身（Vocabulary Slimming）**：裁剪语言模型头，仅保留动作Token，降低显存占用并提升吞吐量。
        *   **Token级优化**：在Token级别计算重要性采样比率，增强数值稳定性。

#### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 具身智能基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **对比方法**：
    *   **OpenVLA-OFT**：基于正交微调的监督学习基准。
    *   **RLinf-VLA**：一种高效的VLA强化学习框架。
    *   **SimpleVLA-RL**：采用同步训练方案的VLA强化学习方法。

#### 4. 资源与算力
*   **硬件型号**：实验使用了 **NVIDIA H200** GPU。
*   **算力规模**：
    *   在吞吐量测试中，扩展至 **7个训练GPU** 和 **64个采样Worker**。
    *   训练器在7卡H200上达到了 **104.22 SPS**（每秒样本数）的超线性扩展性能。
*   **训练细节**：模型基于 OpenVLA（Llama-2-7B 骨干网络）。

#### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **性能对比**：在LIBERO的四个任务类别上进行了全面的成功率对比。
    *   **扩展性实验**：测试了采样器数量和训练器GPU数量对吞吐量的影响。
    *   **消融实验**：验证了“价值重计算”（Value Re-computation）、GIPO算法相对于标准PPO的优越性。
    *   **效率验证**：对比了有无世界模型下的样本效率。
*   **充分性评价**：实验设计较为充分，涵盖了系统性能（吞吐量）、算法性能（成功率）和样本效率（世界模型）三个维度，对比基准均为该领域最新的SOTA方法，结果具有客观性。

#### 6. 论文的主要结论与发现
*   **性能领先**：AcceRL在LIBERO所有任务中均达到SOTA，尤其在长程任务（LIBERO-Long）中成功率达99.1%，远超监督学习基准（90.7%）。
*   **超线性扩展**：得益于ZeRO优化和异步架构，系统吞吐量随GPU增加呈超线性增长，硬件利用率保持在94%以上。
*   **效率飞跃**：引入世界模型后，在线样本效率提升了 **200倍（20,000%）**，仅需1万次真实环境交互即可实现性能突破。
*   **稳定性增强**：GIPO算法和价值重计算机制有效解决了异步架构中的策略滞后和数据陈旧问题。

#### 7. 优点
*   **架构创新**：首次在分布式异步RL流水线中集成了即插即用的可训练世界模型。
*   **系统优化深入**：通过推理池化、词表裁剪和数据预取等手段，极大地压榨了硬件性能。
*   **算法稳健**：针对异步环境下的策略偏移问题，提出了针对性的数学修正方案（如GIPO）。

#### 8. 不足与局限
*   **模型支持范围**：目前主要针对VLA模型，尚未支持超大规模语言模型（LLM）的全量对齐训练。
*   **世界模型开销**：虽然减少了环境交互，但训练和运行高质量的扩散世界模型本身也需要额外的计算资源。
*   **应用限制**：实验主要在模拟环境（MuJoCo）中进行，在真实物理世界中的泛化能力和实时性表现仍需进一步验证。

（完）
