---
title: "RewardFlow: Topology-Aware Reward Propagation on State Graphs for Agentic RL with Large Language Models"
title_zh: RewardFlow：大语言模型智能体强化学习中基于状态图的拓扑感知奖励传播
authors: "Xiao Feng, Bo Han, Zhanke Zhou, Jiaqi Fan, Jiangchao Yao, Ka Ho Li, Dahai Yu, Michael Kwok-Po Ng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18859v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 利用状态级奖励增强LLM智能体推理能力的强化学习
tldr: 针对大语言模型在智能体推理任务中面临的奖励稀疏和过程奖励模型成本高的问题，本文提出 RewardFlow。该方法通过构建状态图并利用拓扑感知传播机制，从推理轨迹中自动估算状态级奖励。实验表明，RewardFlow 作为稠密奖励能显著提升强化学习的性能、鲁棒性和训练效率，在四个基准测试中均优于现有基准。
selection_source: fresh_fetch
motivation: 强化学习在提升大语言模型推理能力时面临终端奖励稀疏的问题，而训练专门的过程奖励模型成本过高且难以扩展。
method: 提出一种轻量级的 RewardFlow 方法，通过构建状态图并利用拓扑感知传播分析各状态对成功的贡献，从而生成客观的状态级奖励。
result: 在四个智能体推理基准测试中，RewardFlow 在性能、鲁棒性和训练效率方面均显著优于之前的强化学习基准方法。
conclusion: RewardFlow 为智能体强化学习提供了一种轻量且高效的状态级奖励估算方案，有效解决了奖励稀疏性带来的优化难题。
---

## 摘要
强化学习（RL）在增强大语言模型（LLMs）与外部环境交互的智能体推理能力方面具有巨大潜力。然而，终端奖励固有的稀疏性阻碍了细粒度的状态级优化。尽管过程奖励建模（process reward modeling）提供了一种有前景的替代方案，但训练专门的奖励模型通常涉及巨大的计算成本和扩展难题。为了应对这些挑战，我们提出了 RewardFlow，这是一种专为智能体推理任务量身定制的轻量级状态级奖励估计方法。RewardFlow 通过构建状态图，利用了推理轨迹中状态的内在拓扑结构。这使得能够分析各状态对成功的贡献，随后通过拓扑感知图传播来量化贡献并产生客观的状态级奖励。当作为密集奖励集成到强化学习优化中时，RewardFlow 在四个智能体推理基准测试中显著优于先前的强化学习基准，展示了卓越的性能、鲁棒性和训练效率。RewardFlow 的实现已在 https://github.com/tmlr-group/RewardFlow 公开。

## Abstract
Reinforcement learning (RL) holds significant promise for enhancing the agentic reasoning capabilities of large language models (LLMs) with external environments. However, the inherent sparsity of terminal rewards hinders fine-grained, state-level optimization. Although process reward modeling offers a promising alternative, training dedicated reward models often entails substantial computational costs and scaling difficulties. To address these challenges, we introduce RewardFlow, a lightweight method for estimating state-level rewards tailored to agentic reasoning tasks. RewardFlow leverages the intrinsic topological structure of states within reasoning trajectories by constructing state graphs. This enables an analysis of state-wise contributions to success, followed by topology-aware graph propagation to quantify contributions and yield objective, state-level rewards. When integrated as dense rewards for RL optimization, RewardFlow substantially outperforms prior RL baselines across four agentic reasoning benchmarks, demonstrating superior performance, robustness, and training efficiency. The implementation of RewardFlow is publicly available at https://github.com/tmlr-group/RewardFlow.

---

## 论文详细总结（自动生成）

### 论文总结：RewardFlow —— 基于状态图拓扑感知的智能体强化学习奖励传播

#### 1. 核心问题与整体含义（研究动机和背景）
在利用强化学习（RL）提升大语言模型（LLM）智能体推理能力时，普遍面临**奖励稀疏性**问题：环境通常只在任务结束时提供一个终端奖励（成功或失败），而中间众多的推理步骤缺乏反馈。这导致“信用分配”（Credit Assignment）极其困难，模型难以识别哪些中间动作真正促成了成功。虽然现有的过程奖励建模（PRM）能提供步骤级指导，但通常需要昂贵的人工标注或训练额外的奖励模型。

**RewardFlow 的核心动机**是：无需训练额外的奖励模型，直接利用推理轨迹中状态之间的**内在拓扑结构**（即状态与成功目标的距离和连接性）来客观地估算中间状态的奖励。

---

#### 2. 方法论：核心思想与关键技术
RewardFlow 提出了一种轻量级的框架，通过构建状态图并进行奖励传播来生成密集奖励：

*   **状态图构建（State Graph Construction）**：
    *   将从环境中采样得到的多个轨迹进行聚合。
    *   **状态归一化**：通过嵌入向量或特征增强，将语义等效的状态合并为同一个图节点。
    *   **噪声剪枝**：识别并移除 LLM 生成的无效动作（如幻觉动作），确保图结构的真实性。
*   **拓扑感知奖励传播（Graph Propagation）**：
    *   从成功的终端节点开始，利用**逆向广度优先搜索（BFS）**向后传播奖励。
    *   **公式化表示**：状态 $s$ 的奖励 $R(s) = \gamma^{d(s)}$，其中 $d(s)$ 是该状态到最近成功节点的短程跳数，$\gamma \in (0, 1]$ 是衰减因子。这意味着离成功越近的状态奖励越高。
*   **动作级奖励映射（Reverse Back）**：
    *   将图中的状态奖励映射回原始轨迹。
    *   定义动作奖励为状态势能差：$\tilde{r}(s_t, a_t) = R(s_{t+1}) - R(s_t)$。
*   **协同优势估计与策略更新**：
    *   结合**动作级优势**（局部对比）和**轨迹级优势**（全局成败），使用 PPO 风格的剪切代理目标函数更新策略模型。

---

#### 3. 实验设计
*   **数据集/场景**：
    1.  **ALFWorld**：文本交互式家庭任务环境。
    2.  **WebShop**：大规模在线购物交互任务。
    3.  **Sokoban（推箱子）**：视觉空间规划任务（测试多模态能力）。
    4.  **DeepResearch**：基于搜索引擎的知识问答任务。
*   **基准方法（Benchmarks）**：对比了 RLOO、GRPO、GiGPO（专门针对智能体的 RL 算法）以及 Search-R1 等强基准。
*   **实验模型**：采用了 Qwen2.5 系列（1.5B, 3B, 7B）以及 Qwen2.5-VL（视觉版本）。

---

#### 4. 资源与算力
论文明确列出了硬件配置：
*   **GPU 型号**：NVIDIA A100 和 H20。
*   **算力分配**：
    *   训练 1.5B 和 3B 模型：使用 **2 张** GPU。
    *   训练 7B 模型：使用 **4 张** GPU。
*   **训练时长**：虽然未给出总小时数，但论文强调 RewardFlow 的图构建和传播开销极低（每步仅需约 1.5-2.4 秒），相比于模型推理和梯度更新，其计算开销几乎可以忽略不计。

---

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同模态和性质的基准上进行了测试，涵盖了从 1.5B 到 7B 的多个模型参数量。
*   **消融实验**：详细测试了状态归一化、无效动作过滤、采样数量（Rollouts）对性能的影响。
*   **鲁棒性测试**：进行了分布外（OOD）任务评估，验证了模型在未见场景下的泛化能力。
*   **客观性**：实验设计对比了当前最先进的组采样 RL 算法（如 GRPO），并展示了在不同采样预算下的表现，结果具有较强的说服力和公平性。

---

#### 6. 主要结论与发现
*   **性能卓越**：RewardFlow 在所有基准上均显著优于基准方法。在 ALFWorld 上比最强基准提升了 12.3%，在 Sokoban 上提升了 27.7%。
*   **训练效率高**：通过提供密集的、基于拓扑的奖励，模型收敛更快，且不需要昂贵的 PRM 训练成本。
*   **鲁棒性强**：在采样轨迹较少（探索不充分）的情况下，RewardFlow 依然能通过图结构提取有效信号，表现优于传统方法。
*   **泛化能力**：在 OOD 任务中表现稳定，证明其学习到的是通用的推理逻辑而非简单的轨迹记忆。

---

#### 7. 优点
*   **无需额外模型**：摆脱了对人工标注过程奖励或额外奖励模型的依赖，极大地降低了系统复杂度和计算成本。
*   **拓扑感知**：巧妙地将环境的内在逻辑（状态可达性）转化为数学上的图距离，提供了客观的信用分配依据。
*   **通用性**：方法不局限于特定模态，在纯文本和视觉-语言任务中均表现出色。

---

#### 8. 不足与局限
*   **状态表示依赖**：该方法高度依赖于“状态归一化”的质量。如果环境状态极其复杂且难以通过语义嵌入进行有效聚合（例如状态表示非常稀疏或包含大量无关随机信息），状态图的构建质量会下降。
*   **随机性挑战**：在高度随机的环境中（如 DeepResearch），虽然论文采用了聚类策略，但频繁变动的环境反馈仍可能导致图结构碎片化。
*   **推理痕迹缺失**：目前主要基于环境反馈的状态，尚未充分利用模型生成的内部思维链（CoT）作为图节点，这限制了其在更抽象推理任务中的应用。

（完）
