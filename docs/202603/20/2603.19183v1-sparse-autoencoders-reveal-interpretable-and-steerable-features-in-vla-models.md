---
title: Sparse Autoencoders Reveal Interpretable and Steerable Features in VLA Models
title_zh: 稀疏自编码器揭示了 VLA 模型中具有可解释性且可引导的特征
authors: "Aiden Swann, Lachlain McGranahan, Hugo Buurmeijer, Monroe Kennedy, Mac Schwager"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19183v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 视觉-语言-动作（VLA）模型的机械可解释性研究
tldr: 本研究利用机械可解释性技术，通过在视觉-语言-动作（VLA）模型的隐藏层激活上训练稀疏自编码器（SAEs），揭示了其内部表征。研究发现，虽然大部分特征对应于特定演示的记忆，但仍存在可解释且可操控的通用运动原语。通过在LIBERO基准上进行干预实验，证明了这些特征能因果性地影响机器人行为，并揭示了训练数据规模对特征泛化性的影响。
selection_source: fresh_fetch
motivation: 旨在理解VLA模型在机器人任务中泛化性能不稳定的内在机制，并区分模型是学习了通用规律还是仅仅死记硬背。
method: 在VLA模型的隐藏层激活上训练稀疏自编码器（SAEs）以提取稀疏且可解释的特征字典，并提出量化指标区分通用特征与记忆特征。
result: 实验表明，虽然多数特征源于记忆，但通用的运动原语特征可被成功提取并用于跨场景操控机器人行为。
conclusion: 研究提供了VLA模型具备跨任务泛化特征的首个机械论证据，并指出大规模多样化数据和知识隔离技术是提升模型泛化性的关键。
---

## 摘要
视觉-语言-动作（VLA）模型已成为通用机器人操控的一种极具前景的方法。然而，它们的泛化能力并不一致：虽然这些模型在某些设置下表现出色，但经过微调的变体往往在面对新物体、新场景和新指令时失效。我们应用机械可解释性（mechanistic interpretability）技术来更好地理解 VLA 模型的内部工作机制。为了探测内部表示，我们在 VLA 的隐藏层激活上训练了稀疏自编码器（SAE）。SAE 学习一个稀疏字典，其特征作为模型计算的紧凑且可解释的基。我们发现，提取的大多数 SAE 特征对应于特定训练演示中的记忆序列。然而，一些特征对应于可解释、通用且可引导的运动基元（motion primitives）和语义属性，为 VLA 的泛化能力提供了一个充满希望的视角。我们提出了一种指标，根据特征是代表可泛化的可迁移基元还是特定情节的记忆来对其进行分类。我们通过在 LIBERO 基准测试上的引导实验验证了这些发现。我们展示了单个 SAE 特征会因果性地影响机器人行为。引导通用特征会诱发出与其语义含义一致的行为，并可应用于跨任务和跨场景。这项工作提供了第一个机械论证据，证明 VLA 可以学习跨任务和场景的可泛化特征。我们观察到，在小型机器人数据集上进行监督微调会不成比例地放大记忆效应。相比之下，在更大、更多样化的数据集（如 DROID）上训练或使用知识隔离（knowledge insulation）会促进更通用特征的产生。我们提供了一个开源代码库和用户友好的界面，用于激活收集、SAE 训练和特征引导。项目页面位于 http://drvla.github.io。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising approach for general-purpose robot manipulation. However, their generalization is inconsistent: while these models can perform impressively in some settings, fine-tuned variants often fail on novel objects, scenes, and instructions. We apply mechanistic interpretability techniques to better understand the inner workings of VLA models. To probe internal representations, we train Sparse Autoencoders (SAEs) on hidden layer activations of the VLA. SAEs learn a sparse dictionary whose features act as a compact, interpretable basis for the model's computation. We find that the large majority of extracted SAE features correspond to memorized sequences from specific training demonstrations. However, some features correspond to interpretable, general, and steerable motion primitives and semantic properties, offering a promising glimpse toward VLA generalizability. We propose a metric to categorize features according to whether they represent generalizable transferable primitives or episode-specific memorization. We validate these findings through steering experiments on the LIBERO benchmark. We show that individual SAE features causally influence robot behavior. Steering general features induces behaviors consistent with their semantic meaning and can be applied across tasks and scenes. This work provides the first mechanistic evidence that VLAs can learn generalizable features across tasks and scenes. We observe that supervised fine-tuning on small robotics datasets disproportionately amplifies memorization. In contrast, training on larger, more diverse datasets (e.g., DROID) or using knowledge insulation promotes more general features. We provide an open-source codebase and user-friendly interface for activation collection, SAE training, and feature steering. Our project page is located at http://drvla.github.io

---

## 论文详细总结（自动生成）

### 论文总结：Sparse Autoencoders Reveal Interpretable and Steerable Features in VLA Models

#### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$）虽然在机器人操控中表现出潜力，但其泛化性极不稳定。在特定任务微调后，模型往往表现出“死记硬背”（Memorization）而非真正的技能学习，导致在面对新场景或指令时失效。
*   **核心问题**：如何从机械论（Mechanistic）的角度理解 VLA 模型的内部工作机制？模型内部究竟哪些部分在编码通用的运动原语，哪些部分在进行轨迹记忆？
*   **整体含义**：本研究首次将机械可解释性技术（特别是稀疏自编码器 SAE）应用于 VLA 模型，旨在识别并操控模型内部的语义和运动特征，从而区分泛化与记忆。

#### 2. 方法论
*   **核心思想**：利用稀疏自编码器（SAE）将 VLA 模型隐藏层的高维、叠加（Superposed）激活分解为稀疏且具有单一语义（Monosemantic）的特征字典。
*   **关键技术细节**：
    *   **SAE 架构**：采用 TopK 架构配合 AuxK 辅助损失函数，直接控制特征稀疏度并解决“死特征”问题。
    *   **激活收集**：在 $\pi_{0.5}$ 模型的 PaliGemma（视觉语言骨干）和 Action Expert（动作专家）层的残差流中提取激活。
    *   **特征分类指标**：提出四个自动化指标来量化特征的“通用性”：
        1.  **情节覆盖率（Episode Coverage）**：特征在多少比例的训练情节中激活。
        2.  **平均起始计数（Mean Onset Count）**：特征在单个情节中“爆发”激活的次数（通用特征通常具有多次爆发）。
        3.  **平均激活强度（Mean Activation Magnitude）**：特征激活的峰值强度。
        4.  **相对运行长度（Relative Run Length）**：特征激活持续时间占情节总长度的比例（记忆特征通常持续激活）。
    *   **特征引导（Steering）**：在推理时，将特定 SAE 特征的解码向量按比例加回残差流，观察其对机器人闭环行为的因果影响。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：模拟环境基准，包含 40 个桌面操控任务，用于闭环评估。
    *   **DROID**：大规模真实世界机器人数据集，包含 1545 个任务，用于验证特征在多样化场景下的通用性。
*   **对比对象**：
    *   对比了不同模型架构（$\pi_{0.5}$ vs. OpenVLA）。
    *   对比了不同训练策略（标准微调 vs. 知识隔离 KI）。
    *   对比了不同层级（视觉语言层 vs. 动作专家层）。
*   **Benchmark**：主要在 LIBERO-Spatial, Object, Goal 等子集上进行测试。

#### 4. 资源与算力
*   **算力说明**：文中提到使用了斯坦福大学的 **Marlowe 集群**（基于 GPU 的计算设备）。
*   **具体细节**：虽然未明确给出总的 GPU 小时数，但提供了 SAE 训练的超参数：Batch Size 为 4096，训练 100 个 Epoch，学习率为 $1 \times 10^{-4}$。对于 DROID 数据集，处理了约 2000 个情节的激活数据。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 $\pi_{0.5}$ 的 8 个层和 OpenVLA 的 5 个层上训练了 SAE。
    *   手动标注了 120 个特征用于可解释性评估，并训练了逻辑回归分类器。
    *   进行了多种子（Multi-seed）消融实验，验证特征提取的稳定性。
    *   在 LIBERO 环境中进行了多项闭环引导实验（如抓取对齐、物体运输等）。
*   **充分性评价**：实验设计较为全面，涵盖了从离线特征分析到在线因果干预的完整流程。通过对比模拟与真实数据、不同模型深度，验证了结论的普适性，实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **特征可解释性**：约 80% 的采样特征具有明确的人类可理解含义。
*   **记忆 vs. 泛化**：绝大多数特征（90% 以上）属于对特定演示的记忆，但在 PaliGemma 中层存在通用的运动原语（如“预抓取对齐”、“任务完成信号”）。
*   **因果可引导性**：通过引导特定特征，可以精确改变机器人行为（例如，引导“预抓取”特征会让机器人悬停在物体上方而不执行抓取）。
*   **训练影响**：增加微调步数会加剧记忆效应；而使用**知识隔离（KI）**技术或更大规模的数据集（DROID）能显著提升通用特征的比例。

#### 7. 优点
*   **首创性**：这是首个针对 VLA 模型提供跨任务泛化特征机械论证据的工作。
*   **实用工具**：开发并开源了 **Dr. VLA** 工具包，降低了机器人模型可解释性研究的门槛。
*   **量化评估**：提出的通用性指标无需运行昂贵的策略回放即可评估模型泛化风险。

#### 8. 不足与局限
*   **引导的不确定性**：并非所有具有清晰语义的特征都能产生可预测的引导效果，这可能与动作解码器的非线性性质有关。
*   **数据规模限制**：相比 LLM 研究，机器人领域的激活数据量仍较小，可能限制了 SAE 发现更细粒度特征的能力。
*   **硬件验证缺失**：引导实验主要在仿真环境（LIBERO）中完成，尚未在真实机器人硬件上进行大规模验证。
*   **Token 处理**：主要采用均值池化（Mean-pooling）处理 Token，可能丢失了部分空间细粒度信息。

（完）
