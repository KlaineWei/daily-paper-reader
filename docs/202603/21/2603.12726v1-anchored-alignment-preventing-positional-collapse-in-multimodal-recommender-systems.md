---
title: "Anchored Alignment: Preventing Positional Collapse in Multimodal Recommender Systems"
title_zh: 锚定对齐：防止多模态推荐系统中的位置坍缩
authors: "Yonghun Jeong, David Yoon Suk Kang, Yeon-Chang Lee"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12726v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 多模态推荐系统与对齐
tldr: 针对多模态推荐系统中直接对齐导致模态结构模糊和ID主导的问题，本文提出AnchorRec框架。该框架通过在轻量级投影域引入基于锚点的间接对齐机制，实现了对齐过程与表示学习的解耦。实验证明，AnchorRec在保留各模态原生结构的同时，有效避免了位置坍缩，显著提升了推荐准确率和多模态表达的连贯性。
selection_source: fresh_fetch
motivation: 传统的多模态对齐方法容易模糊模态特有结构并加剧ID主导现象，导致表示空间的位置坍缩。
method: 提出一种基于锚点的间接对齐框架AnchorRec，在轻量级投影域内进行对齐以解耦表示学习。
result: 在四个Amazon数据集上的实验表明，该方法在Top-N推荐任务中表现优异，且具有更好的多模态一致性。
conclusion: 通过解耦对齐与表示学习，AnchorRec能在维持模态原生特性的基础上实现高效的跨模态协作。
---

## 摘要
多模态推荐系统 (MMRS) 利用图像、文本和交互信号来丰富物品表示。然而，最近基于对齐的 MMRS 强制执行统一的嵌入空间，往往会模糊特定模态的结构并加剧 ID 主导地位。因此，我们提出了 AnchorRec，这是一个在轻量级投影域中执行间接、基于锚点的对齐的多模态推荐框架。通过将对齐与表示学习解耦，AnchorRec 在保持跨模态一致性并避免位置坍缩的同时，保留了每个模态的原始结构。在四个 Amazon 数据集上的实验表明，AnchorRec 实现了具有竞争力的 Top-N 推荐准确率，而定性分析则证明了其改进的多模态表达能力和连贯性。AnchorRec 的代码库可在 https://github.com/hun9008/AnchorRec 获取。

## Abstract
Multimodal recommender systems (MMRS) leverage images, text, and interaction signals to enrich item representations. However, recent alignment based MMRSs that enforce a unified embedding space often blur modality specific structures and exacerbate ID dominance. Therefore, we propose AnchorRec, a multimodal recommendation framework that performs indirect, anchor based alignment in a lightweight projection domain. By decoupling alignment from representation learning, AnchorRec preserves each modality's native structure while maintaining cross modal consistency and avoiding positional collapse. Experiments on four Amazon datasets show that AnchorRec achieves competitive top N recommendation accuracy, while qualitative analyses demonstrate improved multimodal expressiveness and coherence. The codebase of AnchorRec is available at https://github.com/hun9008/AnchorRec.

---

## 论文详细总结（自动生成）

以下是对论文《Anchored Alignment: Preventing Positional Collapse in Multimodal Recommender Systems》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态推荐系统（MMRS）在进行跨模态对齐时，通常将不同模态（如图像、文本、ID）强制投影到同一个统一的潜空间。这导致了两个主要挑战：
    *   **位置坍缩（Positional Collapse）**：不同模态的嵌入在空间中过于接近，导致模态特有的语义多样性丢失。
    *   **ID 信号主导（ID Dominance）**：由于推荐模型通常以交互驱动的排序损失（如 BPR）为主，最终的物品表示往往被 ID 交互模式统治，掩盖了多模态的语义特征。
*   **研究背景**：早期的 MMRS 仅将多模态视为辅助特征，而近年来的对齐方法（如 AlignRec）虽然增强了一致性，但牺牲了模态的表达能力。

### 2. 方法论
AnchorRec 的核心思想是**“间接对齐”**，即在轻量级的投影域中进行对齐，从而将对齐过程与原始表示学习解耦。
*   **核心组件**：
    1.  **模态编码器**：使用 LightGCN 处理 ID，BERT 处理文本，VGGNet 处理视觉，BEiT3 生成融合的多模态特征。
    2.  **协作细化（Collaborative Refinement）**：通过物品 ID 调制模态特征，并利用物品相似性矩阵进行传播，使模态特征捕获协作信号。
    3.  **锚点投影（Anchor-based Projection）**：这是关键创新。模型不直接在原始空间对齐，而是通过 MLP 将各模态映射到投影域。在该域中，以**融合多模态嵌入（$p_i^{mm}$）作为“锚点”**。
*   **关键损失函数**：
    *   **锚点对齐学习（AAL）**：使用 InfoNCE 损失，在投影域内将 ID、文本和视觉的投影向多模态锚点拉近。
    *   **锚点模态保留（AMP）**：通过惩罚学习到的相似度与预训练特征相似度之间的偏差，保留各模态原始的结构特性。
*   **表示融合**：最终物品表示由各模态嵌入求和，并加入一个来自投影锚点的修正项，以微调语义一致性。

### 3. 实验设计
*   **数据集**：使用了 Amazon 的四个子数据集：Baby、Sports、Office 和 Video Games。
*   **评估指标**：Recall@20 和 NDCG@20。
*   **对比方法（Baselines）**：
    *   **准确率导向型**：VBPR, LATTICE, FREEDOM, LGMRec, SMORE。
    *   **对齐导向型**：BM3, DA-MRS, AlignRec。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到使用了官方的 MMRec 框架进行实现，超参数通过验证集上的网格搜索确定。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个真实数据集上进行了测试，对比了 8 种最先进的基线模型。
*   **实验维度**：
    *   **定量分析**：Top-N 推荐准确率对比。
    *   **定性分析**：通过 t-SNE 可视化展示了位置坍缩的缓解；通过邻域重叠矩阵验证了模态结构的保留。
    *   **案例研究**：展示了检索到的 Top-3 邻居物品，证明了模型在语义和交互信号之间的平衡。
*   **充分性评价**：实验设计较为全面，不仅关注推荐精度，还深入探讨了表示空间的结构特性，论证了其解决“位置坍缩”问题的有效性。

### 6. 主要结论与发现
*   **性能表现**：AnchorRec 在保持竞争力的推荐准确率的同时，显著提升了多模态表示的表达能力。
*   **结构保留**：通过间接对齐，模型成功保留了各模态的原生邻域结构，避免了所有模态嵌入聚集成一个点的现象。
*   **语义连贯性**：相比于 AlignRec，AnchorRec 检索到的相似物品在视觉和文本语义上更加一致，减少了 ID 信号的过度干扰。

### 7. 优点
*   **解耦设计**：通过引入投影域，巧妙地解决了对齐一致性与模态特异性之间的矛盾。
*   **锚点机制**：使用多模态融合特征而非 ID 作为锚点，有效缓解了 ID 主导问题。
*   **可视化证据强**：论文提供了直观的 t-SNE 和邻域分析，有力地支撑了其关于“位置坍缩”的论点。

### 8. 不足与局限
*   **用户端建模较弱**：模型主要针对物品侧进行多模态对齐，用户侧仍主要依赖 ID 嵌入，未充分挖掘用户侧的多模态偏好。
*   **依赖预训练模型**：模态特征提取依赖于固定的预训练编码器，若预训练特征本身存在偏差，可能会影响后续对齐效果。
*   **精度权衡**：在某些数据集上，其绝对推荐准确率略低于专门优化排序的非对齐模型（如 SMORE），这反映了表达能力与预测精度之间仍存在微小的权衡。

（完）
