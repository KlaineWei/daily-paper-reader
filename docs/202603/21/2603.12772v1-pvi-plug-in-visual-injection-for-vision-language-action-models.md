---
title: "PVI: Plug-in Visual Injection for Vision-Language-Action Models"
title_zh: PVI：视觉-语言-动作模型的插件式视觉注入
authors: "Zezhou Zhang, Songxin Zhang, Xiao Xiong, Junjie Zhang, Zejian Xie, Jingyi Xi, Zunyao Mao, Zan Mao, Zhixin Mai, Zhuoyang Song, Jiaxing Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12772v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于VLA模型的轻量级模块，旨在改进时间与几何线索
tldr: 针对视觉语言动作模型（VLA）在处理细粒度几何线索和时序信息方面的不足，本文提出了插件式视觉注入（PVI）。PVI是一个轻量级、与编码器无关的模块，通过零初始化的残差路径将辅助视觉特征注入预训练的动作专家模型中。该方法仅需单阶段微调即可显著提升模型性能，尤其在需要状态跟踪和协调的多阶段任务中表现优异，并在真实机器人实验中验证了其有效性。
selection_source: fresh_fetch
motivation: 现有的VLA模型往往忽略了细粒度的几何特征和时序信息，且现有的注入方法通常需要复杂的架构修改。
method: 提出了一种名为PVI的轻量级模块，利用零初始化残差路径将辅助视觉表示（如时序视频特征）注入到预训练的动作专家中。
result: 实验表明PVI在多项任务中优于基础策略，且时序特征（V-JEPA2）的效果优于静态图像特征（DINOv2）。
conclusion: PVI为增强VLA模型的感知能力提供了一种高效且灵活的方案，特别适用于复杂的长程操作任务。
---

## 摘要
将预训练的视觉语言模型（VLM）与流匹配动作专家相结合的视觉-语言-动作（VLA）架构已成为语言条件操控的一种强大范式。然而，针对语义抽象进行优化且通常以静态视觉观测为条件的 VLM，往往会削弱细粒度的几何线索，并且通常缺乏为动作专家提供的显式时间证据。先前的工作通过注入辅助视觉特征来缓解这一问题，但现有方法要么侧重于静态空间表示，要么需要对架构进行重大修改以适应时间输入，导致时间信息未得到充分探索。我们提出了插件式视觉注入（PVI），这是一种轻量级、与编码器无关的模块，它附加到预训练的动作专家上，并通过零初始化的残差路径注入辅助视觉表示，仅需单阶段微调即可保留预训练行为。使用 PVI，我们在基础策略和一系列具有竞争力的替代注入策略上获得了一致的提升。我们的对照研究表明，时间视频特征（V-JEPA2）优于强大的静态图像特征（DINOv2），在需要状态跟踪和协调的多阶段任务中提升最为显著。在长时程双臂折叠衣服上的真实机器人实验进一步证明了 PVI 在模拟环境之外的实用性。

## Abstract
VLA architectures that pair a pretrained VLM with a flow-matching action expert have emerged as a strong paradigm for language-conditioned manipulation. Yet the VLM, optimized for semantic abstraction and typically conditioned on static visual observations, tends to attenuate fine-grained geometric cues and often lacks explicit temporal evidence for the action expert. Prior work mitigates this by injecting auxiliary visual features, but existing approaches either focus on static spatial representations or require substantial architectural modifications to accommodate temporal inputs, leaving temporal information underexplored. We propose Plug-in Visual Injection (PVI), a lightweight, encoder-agnostic module that attaches to a pretrained action expert and injects auxiliary visual representations via zero-initialized residual pathways, preserving pretrained behavior with only single-stage fine-tuning. Using PVI, we obtain consistent gains over the base policy and a range of competitive alternative injection strategies, and our controlled study shows that temporal video features (V-JEPA2) outperform strong static image features (DINOv2), with the largest gains on multi-phase tasks requiring state tracking and coordination. Real-robot experiments on long-horizon bimanual cloth folding further demonstrate the practicality of PVI beyond simulation.

---

## 论文详细总结（自动生成）

以下是对论文《PVI: Plug-in Visual Injection for Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型通常将预训练的 VLM（视觉语言模型）作为语义编码器，配合扩散/流匹配 Transformer 作为动作专家。然而，VLM 倾向于提取高层语义而忽略了**细粒度的几何线索**（如边缘、对齐、抓取点）和**时序动态信息**（如运动连续性、接触转换）。
*   **研究背景**：现有的改进方法要么在 VLM 内部融合特征（仍受语义压缩限制），要么通过侧支注入静态特征，但往往需要大幅修改架构，且对时序视频信息的利用不足。

### 2. 方法论（PVI：插件式视觉注入）
*   **核心思想**：引入一个轻量级、与编码器无关的插件模块，直接将辅助视觉表示注入到冻结的动作专家（Action Expert）中，而不改变其原始架构或预训练权重。
*   **关键技术细节**：
    *   **双路径设计（Dual-Pathway）**：在冻结的主 DiT（扩散 Transformer）旁创建一个可训练的副本分支（Copy Branch），该分支初始化自预训练权重。
    *   **层级残差注入**：副本分支接收辅助视觉特征（如 V-JEPA2 提取的视频特征），其每一层的隐藏状态通过**零初始化（Zero-initialized）**的线性层映射后，以残差形式加到主 DiT 的对应层中。
    *   **编码器无关性**：PVI 的接口可以接入任何视觉编码器（静态图像 DINOv2 或时序视频 V-JEPA2），通过一个简单的投影层将特征对齐到动作专家的嵌入空间。
    *   **单阶段微调**：训练时冻结 VLM 和主 DiT，仅训练投影层、副本分支和注入层，确保模型从原始预训练行为平滑过渡。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **仿真环境**：使用 **RoboTwin 2.0** 仿真器，涵盖 20 到 50 个不同的双臂操作任务（如调整瓶子、敲击、开关、堆叠等）。
    *   **真实机器人**：在 Airbot 双臂平台上进行长时程（8 个阶段）的**布料折叠**实验，涉及复杂的形变物体处理。
*   **Benchmark 与对比方法**：
    *   **基准模型**：GR00T N1.5。
    *   **对比注入策略**：Concat（输入级拼接）、ControlNet 风格、ReferenceNet 风格、ControlVLA 风格（双重注意力注入）。
    *   **表示研究**：对比了 DINOv2（静态）、V-JEPA2（时序）以及两者的组合。

### 4. 资源与算力
*   **硬件设备**：使用了 **NVIDIA H20Z GPU**。
*   **算力分配**：
    *   单任务对比实验：4 台 H20Z，训练 20k - 60k 步。
    *   多任务及真实机器人实验：8 台 H20Z，训练约 60 个 epoch。
*   **参数量**：PVI 引入了约 0.9B 的可训练参数（在 3B 规模的基座上），但由于冻结了主干网络，实际训练开销与标准微调相当。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 20 个仿真任务上对比了 5 种注入策略。
    *   在 10 个任务上进行了详细的视觉表示（静态 vs 时序）对照实验。
    *   进行了 20 任务和 50 任务的大规模多任务扩展性测试。
    *   包含针对时序上下文长度（帧数）和初始化策略的消融实验。
*   **评价**：实验设计非常充分且客观。通过在相同预训练权重、相同数据和超参数下进行对比，公平地验证了 PVI 架构的优越性。

### 6. 主要结论与发现
*   **注入机制至关重要**：PVI 的层级残差注入显著优于简单的特征拼接（Concat）或其他生成式控制架构，平均成功率从 35.7% 提升至 **59.7%**（+24.0 pp）。
*   **时序信息是关键**：时序视频特征（V-JEPA2）在多阶段协调任务中的表现一致优于强力的静态特征（DINOv2），证明了运动趋势和状态跟踪对闭环操控的互补价值。
*   **良好的扩展性**：PVI 在多任务学习中依然稳健，并能成功迁移到真实世界中处理极具挑战性的柔性物体（布料）长程操作。

### 7. 优点与亮点
*   **微侵入性**：无需修改预训练 VLA 的核心架构，保护了原始的语义理解能力。
*   **稳定性**：零初始化技术确保了训练初期模型行为不发生剧烈偏移。
*   **灵活性**：编码器无关的设计使其成为一个通用的“视觉增强插件”，未来可轻松接入更强的视觉模型。
*   **实战价值**：在双臂布料折叠这种高难度任务上的成功演示，证明了其在复杂工业/家庭场景的应用潜力。

### 8. 不足与局限
*   **推理开销**：虽然训练是轻量级的，但推理时需要运行辅助编码器和副本分支，会增加一定的计算延迟和显存占用。
*   **依赖微调**：PVI 仍属于微调范畴，需要一定量的目标任务演示数据，无法直接实现完全的零样本（Zero-shot）增强。
*   **架构限制**：目前主要针对基于 Transformer（DiT）的动作专家设计，对于其他类型的动作生成器（如 MLP 或 RNN 基底）的适用性尚未探讨。

（完）
