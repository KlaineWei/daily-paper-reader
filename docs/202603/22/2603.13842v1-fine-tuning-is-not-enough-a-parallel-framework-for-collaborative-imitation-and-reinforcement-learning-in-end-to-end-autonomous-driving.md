---
title: "Fine-tuning is Not Enough: A Parallel Framework for Collaborative Imitation and Reinforcement Learning in End-to-end Autonomous Driving"
title_zh: 微调还不够：端到端自动驾驶中协同模仿学习与强化学习的并行框架
authors: "Zhexi Lian, Haoran Wang, Xuerun Yan, Weimeng Lin, Xianhong Zhang, Yongyu Chen, Jia Hu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13842v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于端到端自动驾驶控制的强化学习
tldr: 针对端到端自动驾驶中模仿学习（IL）受限于人类演示质量且强化学习（RL）顺序微调易导致策略漂移的问题，本文提出PaIR-Drive并行框架。该框架通过解耦IL与RL分支实现协同优化，推理时利用RL进一步优化IL生成的轨迹。结合树状轨迹采样器与GRPO算法，PaIR-Drive在NAVSIM基准上显著提升了基准模型的性能，甚至能纠正人类专家的次优行为。
selection_source: fresh_fetch
motivation: 传统的顺序RL微调方法存在策略漂移和性能上限问题，且高度依赖预训练的IL策略。
method: 提出PaIR-Drive并行框架，将IL与RL分为两个冲突无关的训练分支，并引入树状轨迹采样器结合GRPO进行优化。
result: 在NAVSIMv1和v2基准上达到91.2 PDMS和87.9 EPDMS的领先水平，显著优于现有的RL微调方法。
conclusion: PaIR-Drive通过并行协同机制有效突破了IL的性能瓶颈，为端到端自动驾驶提供了一种更高效、更具探索能力的训练范式。
---

## 摘要
端到端自动驾驶通常基于模仿学习（IL）构建，但其性能受限于人类演示数据的质量。为了克服这一局限性，近期的方法通过顺序微调引入了强化学习（RL）。然而，这种范式仍然是次优的：顺序 RL 微调可能会引入策略漂移，并且由于依赖于预训练的 IL 策略，往往会导致性能瓶颈。为了解决这些问题，我们提出了 PaIR-Drive，这是一个用于端到端自动驾驶中协同模仿学习与强化学习的通用并行框架。在训练过程中，PaIR-Drive 将 IL 和 RL 分离为两个具有无冲突训练目标的并行分支，从而实现完全协同的优化。这种设计消除了在应用新的 IL 策略时重新训练 RL 的需求。在推理过程中，RL 利用 IL 策略进一步优化最终规划，使性能超越 IL 的先验知识。此外，我们在 RL 分支中引入了一种树状结构轨迹神经采样器，用于群体相对策略优化（GRPO），从而增强了探索能力。在 NAVSIMv1 和 v2 基准测试上的广泛分析表明，基于 Transfuser 和 DiffusionDrive 这两个 IL 基准模型，PaIR-Drive 实现了 91.2 PDMS 和 87.9 EPDMS 的竞争性性能。PaIR-Drive 始终优于现有的 RL 微调方法，甚至可以纠正人类专家的次优行为。定性结果进一步证实，PaIR-Drive 能够有效地探索并生成高质量的轨迹。

## Abstract
End-to-end autonomous driving is typically built upon imitation learning (IL), yet its performance is constrained by the quality of human demonstrations. To overcome this limitation, recent methods incorporate reinforcement learning (RL) through sequential fine-tuning. However, such a paradigm remains suboptimal: sequential RL fine-tuning can introduce policy drift and often leads to a performance ceiling due to its dependence on the pretrained IL policy. To address these issues, we propose PaIR-Drive, a general Parallel framework for collaborative Imitation and Reinforcement learning in end-to-end autonomous driving. During training, PaIR-Drive separates IL and RL into two parallel branches with conflict-free training objectives, enabling fully collaborative optimization. This design eliminates the need to retrain RL when applying a new IL policy. During inference, RL leverages the IL policy to further optimize the final plan, allowing performance beyond prior knowledge of IL. Furthermore, we introduce a tree-structured trajectory neural sampler to group relative policy optimization (GRPO) in the RL branch, which enhances exploration capability. Extensive analysis on NAVSIMv1 and v2 benchmark demonstrates that PaIR-Drive achieves Competitive performance of 91.2 PDMS and 87.9 EPDMS, building upon Transfuser and DiffusionDrive IL baselines. PaIR-Drive consistently outperforms existing RL fine-tuning methods, and could even correct human experts' suboptimal behaviors. Qualitative results further confirm that PaIR-Drive can effectively explore and generate high-quality trajectories.

---

## 论文详细总结（自动生成）

### 论文总结：PaIR-Drive —— 端到端自动驾驶的并行协同学习框架

#### 1. 核心问题与研究动机
*   **研究背景**：当前的端到端自动驾驶主要依赖模仿学习（IL），即通过模仿人类专家的驾驶行为来学习。
*   **核心问题**：
    1.  **数据质量瓶颈**：IL 的性能上限受限于人类演示数据的质量。人类专家可能存在违章、低效或在简单场景（如直线行驶）占比过高的问题，导致模型难以处理复杂长尾场景。
    2.  **微调范式缺陷**：现有的“IL 预训练 + RL 微调”顺序范式存在**策略漂移**（Policy Drift）和**性能天花板**问题。由于 IL 和 RL 的优化目标可能冲突，顺序微调容易陷入局部最优。
*   **研究动机**：探索如何设计一个统一的框架，使 IL 和 RL 能够协同优化，从而突破人类演示的限制，实现超越专家的驾驶表现。

#### 2. 方法论
PaIR-Drive 提出了一个**并行（Parallel）框架**，将 IL 和 RL 解耦为两个独立的优化分支：
*   **并行架构**：
    *   **IL 分支**：遵循典型的“传感器编码→感知融合→轨迹解码”流程，使用人类专家轨迹作为监督信号（L1 损失）。
    *   **RL 分支**：以 BEV 特征和参考轨迹（训练时为专家轨迹，推理时为 IL 输出）为输入，探索更优路径。
*   **关键技术：树状轨迹神经采样器（Tree-structured Trajectory Neural Sampler）**：
    *   该采样器以递归方式预测不同驾驶意图（如左转、右转、加速、减速）下的轨迹点偏移量（Offsets）。
    *   通过在时间维度上分支，形成树状结构的候选轨迹，相比随机采样，这种设计提高了探索效率和轨迹质量。
*   **优化算法**：采用**群体相对策略优化（GRPO）**。通过在模拟器中评估候选轨迹的奖励（安全性、合规性、效率、舒适度等），利用组内相对优势更新 RL 策略。
*   **推理机制**：推理时，IL 分支生成基础轨迹，RL 分支在其基础上扩展出轨迹树。最后利用一个轻量级的**奖励世界模型（RWM）**对所有候选轨迹评分，选出最优方案。

#### 3. 实验设计
*   **数据集与基准**：使用了大规模真实世界模拟基准 **NAVSIM v1** 和 **NAVSIM v2**。
*   **评价指标**：PDMS（v1）和 EPDMS（v2），综合考量无碰撞、车道合规、进度、舒适度等多个维度。
*   **对比方法**：
    *   **纯 IL 方法**：VADv2, Transfuser, DiffusionDrive, WoTE 等。
    *   **顺序 IL+RL 方法**：Transfuser w/ GRPO, ReCogDrive, DriveDPO, AutoVLA 等。
*   **特殊测试集**：提取了人类表现较差的场景（Human bad v1/v2），专门验证模型纠正人类错误的能力。

#### 4. 资源与算力
*   **硬件设备**：使用了 **4 台 NVIDIA L40 GPU**。
*   **训练配置**：
    *   IL 分支：Batch size 32，训练 50 个 epoch，使用 AdamW 优化器（学习率 1e-4）。
    *   RL 分支：Batch size 16，训练 50 个 epoch，学习率 2e-5（余弦退火）。
*   **算法参数**：GRPO 组大小（Group size）设为 15，剪切范围（Clip range）为 0.2。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准（v1/v2）上进行了全面测试，并针对 Transfuser 和 DiffusionDrive 两种不同的 IL 基座进行了验证。
*   **消融实验**：
    1.  验证了并行框架优于顺序框架。
    2.  测试了树状采样结构对性能的提升。
    3.  分析了 GRPO 组大小对结果的影响。
    4.  验证了奖励世界模型（RWM）并非性能提升的唯一因素。
*   **充分性评价**：实验设计涵盖了性能对比、鲁棒性验证（纠正人类错误）和组件有效性分析，结果客观且具有说服力。

#### 6. 主要结论与发现
*   **性能领先**：在 NAVSIM v1 上达到 91.2 PDMS，在 v2 上达到 87.9 EPDMS，显著优于现有的顺序微调方法。
*   **纠正能力**：PaIR-Drive 能够有效纠正人类专家的次优行为（如闯红灯、逆行），在“人类表现差”的测试集中，EPDMS 提升了 10.8 分。
*   **通用性**：该框架可作为通用工具包，无缝集成到任何基于 IL 的自动驾驶模型中，且无需重新训练 RL 分支。
*   **探索效率**：树状采样结构结合 GRPO 能够生成比纯 IL 更安全、更平滑的轨迹。

#### 7. 优点与亮点
*   **架构创新**：通过并行解耦解决了 IL 和 RL 目标冲突的问题，打破了顺序微调的性能瓶颈。
*   **即插即用**：RL 分支不依赖于特定的 IL 策略，具有极强的灵活性和适配性。
*   **高效探索**：引入树状神经采样器，将离散意图与连续偏移量结合，比传统的随机采样 RL 更具方向性。

#### 8. 不足与局限
*   **推理开销**：推理时需要运行 IL 分支、RL 分支以及 RWM 模型，虽然 RWM 是轻量级的，但整体计算复杂度高于纯 IL 模型。
*   **边际效应**：实验观察到，当基座 IL 模型已经非常强大时，RL 带来的性能提升幅度会有所收窄。
*   **环境依赖**：RL 分支的训练依赖于高质量的模拟器奖励函数，奖励函数的设计（Reward Engineering）对最终效果影响较大。

（完）
