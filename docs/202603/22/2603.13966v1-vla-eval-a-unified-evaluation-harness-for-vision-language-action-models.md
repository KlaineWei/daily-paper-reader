---
title: "vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models"
title_zh: vla-eval：视觉-语言-动作模型的统一评估框架
authors: "Suhwan Choi, Yunsung Lee, Yubeen Park, Chris Dongjoo Kim, Ranjay Krishna, Dieter Fox, Youngjae Yu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13966v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 视觉-语言-动作（VLA）模型的统一评估框架
tldr: 针对视觉-语言-动作（VLA）模型评估中存在的脚本碎片化、依赖冲突和协议不明确等问题，本文推出了开源评估框架 vla-eval。该框架通过 WebSocket 协议和 Docker 隔离技术解耦了模型推理与基准测试，支持 13 个基准测试和 6 种模型。它实现了 47 倍的吞吐量提升，并对现有模型进行了复现性审计，发布了包含 657 项结果的 VLA 排行榜，显著提升了 VLA 领域的评估效率和标准化程度。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型评估缺乏统一标准，各模型库独立维护脚本导致代码冗余、依赖冲突且协议不明确。
method: 提出一种基于 WebSocket 和 Docker 的解耦架构，通过统一接口实现模型推理与基准环境的隔离与并行化执行。
result: 框架支持 13 个基准测试，通过并行化实现 47 倍吞吐量提升，并成功复现了已发表模型的性能，同时揭示了隐藏的评估陷阱。
conclusion: vla-eval 为 VLA 模型提供了标准化、高效且可复现的评估基础设施，有助于推动该领域的公平对比与快速迭代。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用由每个模型仓库独立维护的特定基准测试脚本进行评估，这导致了代码重复、依赖冲突以及协议定义不明确等问题。我们提出了 vla-eval，这是一个开源评估框架，它通过基于 WebSocket msgpack 协议和 Docker 环境隔离，实现了模型推理与基准测试执行的解耦。模型只需实现一个 predict() 方法即可完成集成；基准测试通过四方法接口进行集成；完整的交叉评估矩阵将自动运行。完成一次完整的评估仅需两条命令：vla eval serve 和 vla eval run。该框架支持 13 个仿真基准测试和 6 个模型服务器。通过回合分片（episode sharding）和批处理推理实现的并行评估使吞吐量提升了 47 倍，在大约 18 分钟内即可完成 2000 个 LIBERO 回合。利用该基础设施，我们对一个已发表的 VLA 模型在三个基准测试上进行了复现性审计，发现这三个基准测试均能紧密复现已发表的数值，同时也揭示了一些未记录的需求、模糊的终止语义以及可能在无形中扭曲结果的隐藏归一化统计数据。此外，我们还发布了一个 VLA 排行榜，汇总了 17 个基准测试中的 657 项已发表结果。框架、评估配置以及所有复现结果均已公开。

## Abstract
Vision Language Action VLA models are typically evaluated using per benchmark scripts maintained independently by each model repository, leading to duplicated code, dependency conflicts, and underspecified protocols. We present vla eval, an open source evaluation harness that decouples model inference from benchmark execution through a WebSocket msgpack protocol with Docker based environment isolation. Models integrate once by implementing a single predict() method; benchmarks integrate once via a four method interface; the full cross evaluation matrix works automatically. A complete evaluation requires only two commands: vla eval serve and vla eval run. The framework supports 13 simulation benchmarks and six model servers. Parallel evaluation via episode sharding and batch inference achieves a 47x throughput improvement, completing 2000 LIBERO episodes in about 18 minutes. Using this infrastructure, we conduct a reproducibility audit of a published VLA model across three benchmarks, finding that all three closely reproduce published values while uncovering undocumented requirements ambiguous termination semantics and hidden normalization statistics that can silently distort results. We additionally release a VLA leaderboard aggregating 657 published results across 17 benchmarks. Framework, evaluation configs, and all reproduction results are publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
随着视觉-语言-动作（VLA）模型的快速发展，评估生态系统却陷入了碎片化困境。目前主要存在四大问题：
*   **重复劳动：** 每个模型仓库都独立维护特定基准测试（Benchmark）的脚本，导致代码冗余且易随上游接口更新而失效。
*   **依赖与资产冲突：** 不同基准测试（如 LIBERO、ManiSkill2、CALVIN）对 Python 版本和物理引擎（robosuite, SAPIEN, PyBullet）的要求各异，难以在单一环境中并存。
*   **协议不明确：** 论文常忽略随机种子、回合数、归一化统计数据等关键细节，导致结果难以复现。
*   **评估速度慢：** 串行评估数千个回合耗时极长（如 LIBERO 需 14 小时），限制了迭代效率。

**核心含义：**
本文推出了 **vla-eval**，这是一个受 `lm-evaluation-harness` 启发的统一评估框架，旨在通过解耦模型推理与环境执行，实现 VLA 模型评估的标准化、容器化和并行化。

### 2. 论文提出的方法论
**核心思想：**
采用**客户端-服务器架构**，利用 WebSocket 协议和 msgpack 二进制序列化实现模型与基准测试环境的通信。

**关键技术细节：**
*   **架构解耦：** 模型服务器（Model Server）运行在宿主机，基准测试环境运行在独立的 **Docker 容器**中。这种隔离解决了复杂的依赖冲突。
*   **统一接口：** 
    *   **模型端：** 仅需实现一个 `predict(obs, ctx)` 方法，支持自动动作分块（Action Chunking）和集成策略。
    *   **基准测试端：** 通过 `StepBenchmark` 接口实现 `reset`、`step`、`make_obs` 和 `get_step_result` 四个方法。
*   **并行化策略：** 
    *   **回合分片（Episode Sharding）：** 将评估任务分配到 $N$ 个并行的 Docker 容器中。
    *   **批处理推理（Batch Inference）：** 模型服务器同时处理来自多个环境的观测请求。
*   **声明式配置：** 使用 YAML 文件定义模型和基准测试参数，确保评估过程的可追溯性和一键复现。

### 3. 实验设计
**数据集与场景：**
*   **支持的基准测试：** 涵盖 13 个主流仿真环境，包括 LIBERO (7D 动作空间)、CALVIN (7D)、SimplerEnv (7D)、ManiSkill2 (7D)、RoboCasa (7D) 等，动作空间跨度从 6D 到 14D。
*   **支持的模型：** 集成了 CogACT、OpenVLA、π0、GR00T N1、X-VLA 等 6 种代表性模型。

**对比实验：**
*   **复现性审计：** 使用 DB-CogACT 模型在 LIBERO（2000 回合）、CALVIN（1000 序列）和 SimplerEnv（288 回合）上进行实验，对比“官方发布值”与“框架复现值”。
*   **性能测试：** 对比串行评估与并行评估（不同分片数 $N$ 和批大小 $B$）的吞吐量和总耗时。

### 4. 资源与算力
*   **算力设备：** 吞吐量测试使用了 **NVIDIA H100 GPU** 作为模型服务器。
*   **评估时长：** 
    *   **LIBERO：** 串行需 14 小时，并行化后（50 分片, B=16）缩短至 **18 分钟**。
    *   **CALVIN：** 16 分片下缩短至 33 分钟（16 倍加速）。
    *   **SimplerEnv：** 16 分片下缩短至 8.5 分钟（12 倍加速）。

### 5. 实验数量与充分性
*   **实验规模：** 进行了大规模的复现实验（总计超过 3000 个回合/序列），并对 13 个基准测试进行了适配。
*   **充分性：** 实验不仅验证了性能提升，还深入探讨了导致复现偏差的底层原因（如终止语义和归一化统计），具有很强的实战指导意义。
*   **客观性：** 通过固定种子和版本化 Docker 镜像，排除了环境差异带来的干扰，实验设计客观公平。

### 6. 论文的主要结论与发现
*   **性能飞跃：** 通过回合分片和批处理推理，实现了最高 **47 倍** 的评估吞吐量提升。
*   **复现性陷阱：** 审计发现即使是知名基准测试也存在隐性陷阱：
    *   **模糊的终止语义：** 如 SimplerEnv 中某些标志位仅代表瞬时成功而非回合结束，提前停止会虚高得分。
    *   **隐藏的归一化：** 如 CALVIN 需要特定的训练集统计数据进行观测预处理，若文档未注明则会导致性能大幅下降。
*   **生态现状：** 调查显示 81% 的模型仅在单一基准测试上评估，跨环境对比极度匮乏，凸显了统一框架的必要性。

### 7. 优点
*   **工程化极佳：** 解决了 VLA 领域长期存在的“依赖地狱”问题，极大降低了新模型接入评估的门槛。
*   **高效并行：** 需求/供应匹配（Demand/Supply）的并行化方法论为大规模机器人仿真评估提供了范式。
*   **社区贡献：** 发布了包含 657 项结果的 VLA 排行榜，并提供了标准化的协议定义。

### 8. 不足与局限
*   **场景限制：** 目前仅针对**仿真环境**，未涵盖真实机器人（Real-robot）评估。
*   **指标单一：** 评估主要集中在**任务成功率**，缺乏对运动质量、效率和安全性的深入度量。
*   **排行榜数据源：** 排行榜结果多从论文中提取，而非全部通过框架重新运行验证，可能存在原始论文报告偏差。
*   **模型覆盖：** 虽然支持了 6 种模型，但相对于快速增长的 VLA 领域，仍需持续更新以保持生命力。

（完）
