---
title: Tactile Modality Fusion for Vision-Language-Action Models
title_zh: 视觉-语言-动作模型的触觉模态融合
authors: "Charlotte Morissette, Amin Abyaneh, Wei-Di Chang, Anas Houssaini, David Meger, Hsiu-Chin Lin, Jonathan Tremblay, Gregory Dudek"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14604v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: VLA模型的轻量级触觉模态融合
tldr: 针对视觉语言动作（VLA）模型在接触密集型任务中缺乏力学感知的问题，本文提出 TacFiLM。这是一种轻量级的模态融合方法，通过特征线性调制（FiLM）将预训练的触觉信号融入视觉特征中。相比于复杂的令牌拼接或大规模预训练，该方法采用后训练微调策略，在保持计算效率的同时，显著提升了机器人在插拔等任务中的成功率、操作速度和力稳定性，增强了模型的泛化能力。
selection_source: fresh_fetch
motivation: 视觉感知难以捕捉接触密集型操作中的力学动态，而现有的触觉融合方法往往计算复杂度过高。
method: 提出 TacFiLM，利用特征线性调制（FiLM）将预训练的触觉表示作为条件，对 VLA 模型的中间视觉特征进行融合。
result: 在插拔任务的实验中，该方法在成功率、完成时间及力稳定性方面均表现出显著提升，且具备良好的分布外泛化性。
conclusion: TacFiLM 证明了轻量级触觉融合是增强 VLA 模型处理接触密集型任务能力的有效途径。
---

## 摘要
我们提出了 TacFiLM，这是一种将视触觉信号集成到视觉-语言-动作（VLA）模型中的轻量级模态融合方法。虽然 VLA 模型的最新进展引入了兼具泛化性和语义基础的机器人策略，但这些模型主要依赖于基于视觉的感知。然而，仅靠视觉无法捕捉富接触操作过程中发生的复杂交互动力学，包括接触力、表面摩擦、顺应性和剪切力。虽然最近将触觉信号集成到 VLA 模型中的尝试通常通过令牌拼接（token concatenation）或大规模预训练增加了复杂性，但行为模型沉重的计算需求使得更轻量级的融合策略成为必要。为了应对这些挑战，TacFiLM 提出了一种训练后微调方法，该方法使用特征级线性调制（FiLM）将中间视觉特征以预训练的触觉表示为条件进行调节。在插入任务上的实验结果表明，在分布内和分布外任务中，成功率、直接插入性能、完成时间和力稳定性均有一致的提升。总之，这些结果证明了我们的方法是将触觉信号集成到 VLA 模型中的有效途径，改善了富接触操作行为。

## Abstract
We propose TacFiLM, a lightweight modality-fusion approach that integrates visual-tactile signals into vision-language-action (VLA) models. While recent advances in VLA models have introduced robot policies that are both generalizable and semantically grounded, these models mainly rely on vision-based perception. Vision alone, however, cannot capture the complex interaction dynamics that occur during contact-rich manipulation, including contact forces, surface friction, compliance, and shear. While recent attempts to integrate tactile signals into VLA models often increase complexity through token concatenation or large-scale pretraining, the heavy computational demands of behavioural models necessitate more lightweight fusion strategies. To address these challenges, TacFiLM outlines a post-training finetuning approach that conditions intermediate visual features on pretrained tactile representations using feature-wise linear modulation (FiLM). Experimental results on insertion tasks demonstrate consistent improvements in success rate, direct insertion performance, completion time, and force stability across both in-distribution and out-of-distribution tasks. Together, these results support our method as an effective approach to integrating tactile signals into VLA models, improving contact-rich manipulation behaviours.

---

## 论文详细总结（自动生成）

### 视觉-语言-动作模型的触觉模态融合 (TacFiLM) 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA）主要依赖视觉感知，但在处理“接触密集型”（contact-rich）任务（如精密插拔）时表现不佳。视觉无法捕捉接触力、摩擦力和微米级的位姿偏差，且容易受到遮挡影响。
*   **研究动机**：现有的触觉集成方法通常采用“令牌拼接”（Token Concatenation），这会增加输入序列长度，导致计算开销激增，并可能稀释视觉-语言的先验知识。因此，需要一种**轻量级、高效且不增加推理负担**的触觉融合策略。

#### 2. 论文提出的方法论：TacFiLM
*   **核心思想**：利用**特征级线性调制（FiLM）**技术，将预训练的触觉表示作为条件信号，动态调节 VLA 模型视觉编码器中的中间特征，而不是将其作为额外的输入令牌。
*   **关键技术细节**：
    *   **预训练触觉编码器**：采用 Sparsh 或 T3 等自监督预训练模型，将 DIGIT 触觉传感器的图像转化为固定维度的触觉嵌入（Embedding）。
    *   **FiLM 调制机制**：通过一个轻量级 MLP 将触觉嵌入映射为缩放参数 ($\gamma$) 和偏移参数 ($\beta$)。
    *   **特征融合**：在视觉主干网络（如 SigLIP 和 DINOv2）的 ViT 块中，在多头自注意力层之前应用公式：$FiLM(F) = F \odot (1 + \gamma) + \beta$。这种方式改变了视觉特征的分布，使其具备“触觉感知”能力。
    *   **高效微调**：使用 LoRA（低秩适配）对增强后的 VLA 模型进行后训练微调，仅更新极少数参数，保留了原始模型的语义推理能力。

#### 3. 实验设计
*   **实验场景**：使用 Franka Emika Panda 机器人手臂，配备 DIGIT 触觉传感器，执行多种插拔任务。
*   **任务集**：
    *   **分布内（ID）**：圆形插销（2mm/3mm 间隙）、USB 接口插拔。
    *   **分布外（OOD）**：方形插销、五边形插销（不同间隙）、HDMI 接口插拔。
*   **Benchmark（对比方法）**：
    1.  **OpenVLA-OFT**：仅视觉的基准模型。
    2.  **TactileConcat**：将触觉特征投影为令牌并与视觉/语言令牌拼接的常见融合方法。

#### 4. 资源与算力
*   **训练细节**：所有模型均训练至 80,000 步（80k steps）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长。但考虑到其基于 Llama 2 7B 主干并使用 LoRA 微调，通常需要 A100 或 H100 级别的显卡支持。

#### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 **700 次** 真实机器人测试（Rollouts）。
    *   分布内测试：270 次。
    *   分布外测试：225 次。
    *   消融实验与鲁棒性测试：210 次。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了成功率，还引入了**最大接触力**、**直接插入率**和**完成时间**等安全与效率指标。此外，还针对光照变化和相机帧率下降进行了鲁棒性压力测试，验证了触觉在视觉失效时的补偿作用。

#### 6. 主要结论与发现
*   **性能提升**：TacFiLM 在所有任务中均优于视觉唯一模型和拼接融合模型。在某些任务中，成功率比次优方法高出 **30%**。
*   **操作更安全、更高效**：TacFiLM 显著降低了操作过程中的最大接触力（在某些任务中仅为基准方法的 1/3），并缩短了任务完成时间。
*   **泛化能力强**：在未见过的几何形状（如五边形插销）和更复杂的接口（HDMI）上，TacFiLM 表现出极强的分布外泛化能力。
*   **视觉鲁棒性**：在光照减少 80% 或视频流卡顿时，TacFiLM 仍能保持 100% 的成功率，证明触觉有效弥补了视觉信息的缺失。

#### 7. 优点与亮点
*   **轻量化设计**：FiLM 调制不增加 Transformer 的序列长度，推理延迟极低。
*   **即插即用**：利用现有的预训练触觉编码器（如 Sparsh-DINO），无需从头训练触觉表示。
*   **多维度评估**：不仅关注“能否完成”，还关注“完成得是否优雅”（力稳定性、直接插入率），这在机器人领域至关重要。

#### 8. 不足与局限
*   **任务多样性有限**：实验主要集中在插拔类任务（Insertion），尚未在抓取、折叠或更复杂的双手协作任务中验证。
*   **依赖真实数据**：由于缺乏高精度的视触觉仿真器，所有数据均来自真实世界采集，这限制了其在大规模数据集上的扩展性。
*   **架构特定性**：目前主要针对 OpenVLA 架构进行了优化，在其他 VLA 架构（如 $\pi_0$）上的通用性有待进一步验证。

（完）
