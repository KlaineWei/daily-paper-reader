---
title: "GNNVerifier: Graph-based Verifier for LLM Task Planning"
title_zh: GNNVerifier：基于图的大语言模型任务规划验证器
authors: "Yu Hao, Qiuyu Wang, Cheng Yang, Yawen Li, Zhiqiang Zhang, Chuan Shi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14730v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 自主智能体中基于图的LLM任务规划验证器
tldr: 针对大语言模型在任务规划中容易产生幻觉且难以识别结构性错误的问题，本文提出了GNNVerifier。该方法将规划方案表示为包含丰富属性的有向图，利用图神经网络（GNN）进行结构化评估和细粒度错误定位。通过自动生成的扰动数据进行训练，GNNVerifier能有效指导LLM对错误步骤进行局部修正。实验证明，该方法在多种数据集和模型上显著提升了规划质量。
selection_source: fresh_fetch
motivation: 现有的基于LLM的验证器容易被看似合理的叙述误导，且难以检测步骤间的类型不匹配或依赖断裂等复杂结构性错误。
method: 将规划建模为有向图并利用GNN进行全局合理性评分与局部风险定位，从而引导LLM进行精准的局部编辑和修正。
result: 在多种数据集、骨干LLM和规划器上的广泛实验表明，GNNVerifier在提升任务规划质量方面取得了显著的性能增益。
conclusion: 通过结合图结构分析与神经网络评估，GNNVerifier为增强LLM任务规划的鲁棒性和准确性提供了一种高效且可解释的验证方案。
---

## 摘要
大语言模型（LLM）促进了自主智能体的发展。作为此类智能体的核心组件，任务规划旨在将复杂的自然语言请求分解为具体的、可解决的子任务。由于 LLM 生成的规划经常容易产生幻觉且对长上下文提示敏感，最近的研究引入了规划验证器来识别和纠正潜在缺陷。然而，大多数现有方法仍然依赖 LLM 作为验证器，通过额外的提示进行规划审查或自我反思。基于 LLM 的验证器可能会被看似合理的叙述所误导，并且难以检测由跨步骤结构关系引起的故障，例如类型不匹配、中间体缺失或依赖关系断裂。为了解决这些局限性，我们提出了一种用于 LLM 任务规划的基于图的验证器。具体而言，所提方法包含四个主要组件：首先，我们将规划表示为具有丰富属性的有向图，其中节点表示子任务，边编码执行顺序和依赖约束。其次，图神经网络（GNN）执行结构评估和诊断，生成用于规划采纳的图级合理性评分，以及用于定位错误区域的节点/边级风险评分。第三，我们从真实规划图中构建可控扰动，并自动生成具有细粒度标注的训练数据。最后，在 GNN 验证器反馈的指导下，当图级评分不足时，我们使 LLM 能够进行局部编辑（例如工具替换或插入）以纠正规划。在不同数据集、骨干 LLM 和规划器上的广泛实验表明，我们的 GNNVerifier 在提高规划质量方面取得了显著收益。我们的数据和代码可在 https://github.com/BUPT-GAMMA/GNNVerifier 获取。

## Abstract
Large language models (LLMs) facilitate the development of autonomous agents. As a core component of such agents, task planning aims to decompose complex natural language requests into concrete, solvable sub-tasks. Since LLM-generated plans are frequently prone to hallucinations and sensitive to long-context prom-pts, recent research has introduced plan verifiers to identify and correct potential flaws. However, most existing approaches still rely on an LLM as the verifier via additional prompting for plan review or self-reflection. LLM-based verifiers can be misled by plausible narration and struggle to detect failures caused by structural relations across steps, such as type mismatches, missing intermediates, or broken dependencies. To address these limitations, we propose a graph-based verifier for LLM task planning. Specifically, the proposed method has four major components: Firstly, we represent a plan as a directed graph with enriched attributes, where nodes denote sub-tasks and edges encode execution order and dependency constraints. Secondly, a graph neural network (GNN) then performs structural evaluation and diagnosis, producing a graph-level plausibility score for plan acceptance as well as node/edge-level risk scores to localize erroneous regions. Thirdly, we construct controllable perturbations from ground truth plan graphs, and automatically generate training data with fine-grained annotations. Finally, guided by the feedback from our GNN verifier, we enable an LLM to conduct local edits (e.g., tool replacement or insertion) to correct the plan when the graph-level score is insufficient. Extensive experiments across diverse datasets, backbone LLMs, and planners demonstrate that our GNNVerifier achieves significant gains in improving plan quality. Our data and code is available at https://github.com/BUPT-GAMMA/GNNVerifier.

---

## 论文详细总结（自动生成）

### GNNVerifier: 基于图的大语言模型任务规划验证器论文总结

#### 1. 核心问题与整体含义
论文关注的核心问题是**大语言模型（LLM）在复杂任务规划中的可靠性**。虽然 LLM 能将自然语言请求分解为子任务序列，但常因幻觉或对长上下文敏感而产生看似合理实则不可执行的规划。
*   **研究动机**：现有的验证方法多依赖 LLM 自我反思（Self-reflection），这类方法容易被流畅的叙述误导，且难以识别步骤间的**结构性错误**（如参数类型不匹配、关键中间步骤缺失、逻辑依赖断裂）。
*   **核心目标**：引入图神经网络（GNN）作为外部验证器，利用图结构建模能力来捕捉规划中的深层结构矛盾，并引导 LLM 进行精准修复。

#### 2. 方法论
GNNVerifier 提出了一套结合图建模、神经网络评估与局部修正的框架：
*   **核心思想**：将规划方案转化为一个**属性有向图**（Attributed Plan Graph），节点代表工具调用，边代表执行顺序和依赖。
*   **关键技术细节**：
    1.  **属性图构建**：集成工具语义、步骤文本对齐度、I/O 接口兼容性、以及基于训练数据的工具共现统计特征。
    2.  **GNN 编码器**：使用有向、边感知的 GNN，结合用户请求的嵌入向量，通过多层消息传递生成节点、边及全图的表示。
    3.  **扰动监督学习**：针对缺乏错误标注数据的问题，通过对真实规划图进行“替换工具”、“删除步骤”、“压缩路径”等操作构造硬负样本，自动生成训练信号。
    4.  **两阶段训练**：第一阶段学习全图合理性评分（排序损失）；第二阶段冻结部分参数，学习节点和边的风险定位（分类损失）。
    5.  **验证引导的局部修正**：当全图评分低于阈值时，识别高风险区域，并为 LLM 提供候选工具建议，限制其仅在特定位置进行“替换”或“插入”操作。

#### 3. 实验设计
*   **数据集/场景**：
    *   **TaskBench**：包含 HuggingFace（AI 工作流）、Multimedia（多媒体处理）、DailyLife（日常服务）三个领域。
    *   **UltraTool**：更大规模、更复杂的工具利用基准测试。
*   **对比基准（Baselines）**：
    *   **规划器**：Direct（直接生成）、ReAct（推理-动作交替）、GNN4Plan（图引导规划）。
    *   **验证方法**：Raw（无验证）、Refine（自我精炼）、VeriCoder（基于 LLM 的验证）、VeriPlan（形式化验证+LLM）。
*   **评估指标**：Node-F1（节点准确率）、Link-F1（依赖边准确率）、Accuracy（全图完全匹配准确率）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A800-40G GPU**。
*   **模型细节**：文本编码器使用 E5-large；验证器为 3 层 GNN，隐藏层维度 1024。
*   **训练参数**：学习率 2e-5，Batch Size 512，采用早停机制（Patience=5）。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验：
*   **多模型验证**：在 GPT-4o 和 Qwen3-235B 两个主流大模型上均验证了有效性。
*   **消融实验**：设计了 8 组变体，分别验证了 GNN 结构、两阶段训练、节点/边特征、以及不同粒度反馈信号的必要性。
*   **多维度分析**：包括错误类型分布分析、GNN 嵌入空间的 t-SNE 可视化、超参数敏感性分析以及具体案例研究。
*   **结论**：实验覆盖了不同规模的数据集和不同架构的规划器，样本量充足（训练集 3000，测试集 500），对比公平且客观。

#### 6. 主要结论与发现
*   **性能提升**：GNNVerifier 在全图准确率（Acc）上显著优于所有基线，相对提升最高达 15.96% 以上。
*   **结构敏感性**：单纯增加特征而不使用 GNN（如 MLP 变体）效果甚至不如原始模型，证明了**结构化建模**是识别规划错误的关键。
*   **局部修正优势**：通过风险评分引导 LLM 进行局部编辑，比让 LLM 重新生成整个规划更高效且准确，尤其在修复“工具选错”和“步骤缺失”方面表现突出。

#### 7. 优点
*   **创新性**：首次将 GNN 引入 LLM 规划验证，弥补了 LLM 难以处理复杂图结构的短板。
*   **自动化**：通过扰动算子解决了细粒度错误标注数据匮乏的问题。
*   **可解释性**：节点和边的风险评分提供了直观的诊断信息，使验证过程不再是“黑盒”。
*   **鲁棒性**：在不同 LLM 和不同规划器下均表现出一致的增益。

#### 8. 不足与局限
*   **静态验证限制**：目前的验证基于静态文本和结构分析，尚未整合工具执行后的实时反馈（如 API 返回的错误码）。
*   **依赖预定义图**：验证器的效果部分取决于工具依赖图（Dependency Graph）的质量，若工具库动态变化剧烈，可能需要重新训练或调整。
*   **计算开销**：虽然 GNN 比 LLM 轻量，但在推理链条中增加了一个额外的神经网络模块，会带来一定的系统复杂度和微小的延迟增加。

（完）
