---
title: "HapticVLA: Contact-Rich Manipulation via Vision-Language-Action Model without Inference-Time Tactile Sensing"
title_zh: HapticVLA：无需推理时触觉传感的视觉-语言-动作模型实现丰富接触操作
authors: "Konstantin Gubernatorov, Mikhail Sannikov, Ilya Mikhalchuk, Egor Kuznetsov, Makar Artemov, Ogunwoye Faith Ouwatobi, Marcelino Fernando, Artem Asanov, Ziang Guo, Dzmitry Tsetserukou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15257v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 用于机器人操控的视觉-语言-动作模型及奖励加权学习
tldr: "HapticVLA 提出了一种无需推理时触觉传感器的视觉-语言-动作模型，用于处理接触丰富的操作任务。该方法通过安全感知奖励加权流匹配（SA-RWFM）训练触觉专家，并利用触觉蒸馏（TD）将触觉推理能力迁移至常规 VLA 模型。这使得机器人仅凭视觉和状态输入即可实现具备触觉感知能力的精准操作，在降低硬件成本的同时，在真实实验中取得了 86.7% 的成功率，优于带有直接触觉反馈的基准模型。"
selection_source: fresh_fetch
motivation: 触觉传感器硬件成本高且难以在不同机器人平台间复用，限制了具备触觉感知能力的 VLA 模型的广泛应用。
method: 采用两阶段法：先通过 SA-RWFM 训练包含触觉奖励的动作专家，再通过触觉蒸馏将该专家的触觉推理能力转移到仅依赖视觉和状态输入的 VLA 模型中。
result: "在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，显著优于包括推理时具备直接触觉反馈在内的多种基准模型。"
conclusion: 该研究证明了触觉感知能力可以离线学习并蒸馏到视觉模型中，从而在无需额外触觉硬件的情况下实现高性能的接触丰富型机器人操作。
---

## 摘要
触觉感知是视觉-语言-动作（VLA）架构的一项关键能力，因为它能在丰富接触的任务中实现灵巧且安全的操作。然而，对专用触觉硬件的依赖增加了成本，并降低了在不同机器人平台上的可复现性。我们认为，触觉感知操作可以离线学习，并在推理时无需直接触觉反馈即可部署。为此，我们提出了 HapticVLA，它分为两个紧密耦合的阶段：安全感知奖励加权流匹配（SA-RWFM）和触觉蒸馏（TD）。SA-RWFM 训练了一个流匹配动作专家，该专家结合了预计算的安全感知触觉奖励，对过大的抓取力和次优的抓取轨迹进行惩罚。TD 进一步将这种触觉感知能力转移到常规 VLA 中：我们从 SA-RWFM 教师模型中蒸馏出一个紧凑的触觉 token，并训练一个学生 VLA 从视觉和状态模态中预测该 token，从而在推理时无需车载触觉传感器即可生成触觉感知动作。这种设计在 VLA 中保留了丰富接触的触觉感知推理，同时在部署期间消除了对车载触觉传感器的需求。在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，始终优于基准 VLA，包括那些在推理期间提供直接触觉反馈的版本。

## Abstract
Tactile sensing is a crucial capability for Vision-Language-Action (VLA) architectures, as it enables dexterous and safe manipulation in contact-rich tasks. However, reliance on dedicated tactile hardware increases cost and reduces reproducibility across robotic platforms. We argue that tactile-aware manipulation can be learned offline and deployed without direct haptic feedback at inference. To this end, we present HapticVLA, which proceeds in two tightly coupled stages: Safety-Aware Reward-Weighted Flow Matching (SA-RWFM) and Tactile Distillation (TD). SA-RWFM trains a flow-matching action expert that incorporates precomputed, safety-aware tactile rewards penalizing excessive grasping force and suboptimal grasping trajectories. TD further transfers this tactile-aware capability into a conventional VLA: we distill a compact tactile token from the SA-RWFM teacher and train a student VLA to predict that token from vision and state modalities, enabling tactile-aware action generation at inference without requiring on-board tactile sensors. This design preserves contact-rich tactile-aware reasoning within VLA while removing the need for on-board tactile sensors during deployment. On real-world experiments, HapticVLA achieves a mean success rate of 86.7%, consistently outperforming baseline VLAs - including versions provided with direct tactile feedback during inference.

---

## 论文详细总结（自动生成）

### HapticVLA 论文结构化总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型主要依赖视觉感知，缺乏对力反馈和触觉的理解，导致在处理易碎、柔软或需要精细力度控制的“接触丰富型”任务（如抓取鸡蛋、华夫饼）时表现不佳。
*   **研究动机**：虽然集成触觉传感器能解决上述问题，但硬件成本高、跨平台复用性差。本文旨在研究：**能否在训练阶段利用触觉数据学习安全操作逻辑，但在推理部署阶段完全不依赖触觉传感器？**

#### 2. 方法论：核心思想与关键技术
HapticVLA 采用两阶段训练框架，将触觉知识“固化”到视觉策略中：
*   **阶段一：安全感知奖励加权流匹配（SA-RWFM）**
    *   **触觉奖励计算**：利用训练集中的触觉图（Tactile Maps）计算安全奖励。惩罚项包括：过大的抓取力、压力集中、力不对称以及物体滑动（Slip）。
    *   **SA-RWFM 训练**：在离线 fine-tuning 阶段，根据计算出的奖励对训练样本进行加权。高奖励（操作安全且成功）的样本在流匹配（Flow Matching）损失函数中权重更高。
    *   **锚点正则化（Anchor Regularization）**：引入正则化项约束参数漂移，防止生成式策略在强化学习微调过程中发生模式崩溃。
*   **阶段二：触觉蒸馏（TD）**
    *   **教师-学生架构**：将第一阶段训练的“触觉专家”作为教师模型。
    *   **离线目标生成**：预先计算教师模型生成的动作序列作为“软目标”。
    *   **混合目标训练**：学生模型（仅输入视觉和机器人状态）通过模仿教师模型的动作序列进行训练。采用混合目标函数，结合了地面真值（GT）和教师预测，使学生模型在无触觉输入的情况下也能预测出“具备触觉感知意识”的动作。

#### 3. 实验设计
*   **实验场景与任务**：
    1.  **果酱罐放置**：处理可变形塑料罐。
    2.  **华夫饼放置**：处理极易碎且形状不规则的物体。
    3.  **鸡蛋放置**：极高精度的力度控制，需打开包装盒并移动鸡蛋。
*   **硬件平台**：双臂 LeRobot SO-101 机器人，右臂配备高密度触觉传感器阵列（单指 100 个触点）。
*   **数据集**：
    *   **真实世界数据**：310 个回合（包含成功和故意制造的失败/损坏样本，用于奖励学习）。
    *   **仿真数据**：在 NVIDIA Isaac Sim 中构建数字孪生，生成 1000 个回合以增强鲁棒性。
*   **基准模型（Benchmarks）**：对比了 **SmolVLA**（基础模型）、**X-VLA**（跨具身适配模型）和 **VLA-0**。

#### 4. 资源与算力
*   **推理端**：使用 **NVIDIA Jetson Orin NX (16GB)** 边缘计算设备，证明了模型的高效性。
*   **训练端**：文中未明确给出训练所用的具体 GPU 型号和总时长，但提到模型基于 SmolVLA (0.45B 参数)，属于轻量级 VLA，通常在单台或少量 80GB 显存级 GPU（如 A100/H100）上即可完成微调。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务进行 20 次独立测试，总计 60 次真实世界评估。
*   **消融实验**：针对“有无触觉蒸馏（TD）”和“异步/同步推理”进行了对比实验。
*   **充分性评价**：实验覆盖了从“易变形”到“极易碎”的不同梯度任务，对比了当前主流的轻量级 VLA 模型。虽然 20 次重复实验在机器人领域属于标准配置，但若能增加更多随机初始位置的测试会更具说服力。整体实验设计客观，通过引入失败样本进行奖励加权，体现了公平的对比环境。

#### 6. 主要结论与发现
*   **性能卓越**：HapticVLA 在三项任务中的平均成功率达到 **86.7%**，远超基础模型 SmolVLA（41.7%）。
*   **蒸馏有效性**：通过触觉蒸馏，模型在**完全没有触觉传感器输入**的情况下，表现甚至优于某些在推理时带有直接触觉反馈的变体。
*   **泛化能力**：传统的 VLA 模型（如 X-VLA, VLA-0）在未经触觉加权微调时，在这些高精度接触任务中几乎完全失败（成功率为 0%），证明了触觉意识对精细操作的必要性。

#### 7. 优点
*   **低成本部署**：实现了“训练时有感，推理时无感”，极大降低了机器人大规模部署的硬件门槛。
*   **安全意识**：通过 SA-RWFM 显式引入安全奖励，使模型学会了“温柔”地对待物体，而不仅仅是完成位移。
*   **数字孪生增强**：结合 Isaac Sim 仿真数据提升了模型对物体姿态变化的鲁棒性。

#### 8. 不足与局限
*   **训练依赖性**：虽然推理不需要传感器，但训练阶段仍需高质量的触觉数据和精确的奖励标注，这增加了数据采集的复杂性。
*   **任务局限**：目前仅在三种特定物体上验证，对于更复杂的动态接触任务（如拧瓶盖、使用工具）的泛化能力尚待验证。
*   **时延敏感**：消融实验显示异步推理会导致性能下降，说明模型对视觉-动作的时间对齐要求较高。

（完）
