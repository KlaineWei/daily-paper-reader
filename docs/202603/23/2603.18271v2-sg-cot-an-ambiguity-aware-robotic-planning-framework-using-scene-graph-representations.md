---
title: "SG-CoT: An Ambiguity-Aware Robotic Planning Framework using Scene Graph Representations"
title_zh: SG-CoT：一种基于场景图表示的歧义感知机器人规划框架
authors: "Akshat Rana, Peeyush Agarwal, K. P. S. Rana, Amarjit Malhotra"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18271v2"
tags: ["query:slm-rl"]
score: 9.0
evidence: 使用场景图表示的大语言模型机器人规划器
tldr: 针对大语言模型在机器人规划中面临的歧义性挑战，本文提出SG-CoT框架。该框架通过构建环境的场景图表示，并利用检索函数让LLM迭代查询相关信息，从而将推理过程与实际观测结合。SG-CoT能识别歧义并主动提问澄清，显著提升了机器人在单智能体和多智能体环境下的规划成功率和任务可靠性。
selection_source: fresh_fetch
motivation: 大语言模型作为机器人规划器时，难以有效识别和处理环境及指令中的歧义信息，导致规划可靠性不足。
method: 提出一种两阶段框架，先构建包含物体属性与关系的场景图，再利用检索函数引导LLM迭代查询场景图以消除歧义。
result: "实验显示SG-CoT在提问准确率上提升超10%，且在单智能体和多智能体环境的任务成功率分别至少提升了4%和15%。"
conclusion: 通过将场景图表示与思维链推理相结合，SG-CoT显著增强了机器人在复杂、模糊环境下的规划能力和泛化性。
---

## 摘要
歧义性对作为机器人规划器的大语言模型（LLMs）构成了重大挑战。在本文中，我们提出了场景图思维链（SG-CoT），这是一个两阶段框架，其中 LLM 迭代地查询环境的场景图表示，以检测并澄清歧义。首先，根据输入观测构建环境的结构化场景图表示，捕捉物体、其属性以及与其他物体的关系。其次，LLM 配备了检索函数，用于查询场景图中与给定指令相关的部分。这使 LLM 的推理过程基于观测，从而提高了机器人在歧义情况下的规划可靠性。SG-CoT 还允许 LLM 识别歧义来源，并向用户或其他机器人提出相关的消歧问题。广泛的实验表明，SG-CoT 一贯优于先前的方法，在提问准确率上至少提高了 10%，在单智能体环境中的成功率至少提高了 4%，在多智能体环境中至少提高了 15%，验证了其在更具泛化性的机器人规划中的有效性。

## Abstract
Ambiguity poses a major challenge to large language models (LLMs) used as robotic planners. In this letter, we present Scene Graph-Chain-of-Thought (SG-CoT), a two-stage framework where LLMs iteratively query a scene graph representation of the environment to detect and clarify ambiguities. First, a structured scene graph representation of the environment is constructed from input observations, capturing objects, their attributes, and relationships with other objects. Second, the LLM is equipped with retrieval functions to query portions of the scene graph that are relevant to the provided instruction. This grounds the reasoning process of the LLM in the observation, increasing the reliability of robotic planners under ambiguous situations. SG-CoT also allows the LLM to identify the source of ambiguity and pose a relevant disambiguation question to the user or another robot. Extensive experimentation demonstrates that SG-CoT consistently outperforms prior methods, with a minimum of 10% improvement in question accuracy and a minimum success rate increase of 4% in single-agent and 15% in multi-agent environments, validating its effectiveness for more generalizable robot planning.

---

## 论文详细总结（自动生成）

以下是对论文《SG-CoT: An Ambiguity-Aware Robotic Planning Framework using Scene Graph Representations》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在机器人规划中表现出强大的泛化能力，但面临“歧义性”挑战。歧义可能来自用户指令的不明确（如“给我拿点吃的”），也可能来自环境状态（如环境中有多个相同的杯子或根本没有杯子）。
*   **核心问题**：现有的LLM规划器通常过于自信，且缺乏对环境细粒度属性和关系的理解，难以在复杂或模糊的情况下主动寻求澄清，容易导致错误或不安全的行为。
*   **研究目标**：开发一个能够感知歧义、基于环境实况进行推理，并能主动向用户或协作机器人提问以消除歧义的规划框架。

### 2. 论文提出的方法论：SG-CoT
SG-CoT（Scene Graph-Chain-of-Thought）是一个结合场景图表示与迭代思维链推理的两阶段框架：
*   **第一阶段：场景图生成**：
    *   利用开放集物体检测器（如Grounding DINO）或视觉语言模型（VLM）识别物体。
    *   通过提示词（Prompting）驱动VLM生成结构化的JSON格式场景图，包含节点（物体及其颜色、状态等属性）和边（物体间的空间或语义关系，如“在...左边”、“在...内部”）。
*   **第二阶段：迭代推理与消歧**：
    *   **核心思想**：LLM不直接输出最终动作，而是通过调用API函数（`retrieve_node` 和 `retrieve_edge`）多次查询场景图。
    *   **关键流程**：
        1.  **初始化**：将指令、物体列表和场景图架构输入LLM。
        2.  **循环推理**：LLM生成推理轨迹（Thought），若信息不足则调用API查询场景图特定部分。
        3.  **接地（Grounding）**：查询结果返回并存入对话历史，LLM基于真实观测修正理解，避免幻觉。
        4.  **决策**：当信息足够时，输出动作序列；若发现歧义（如匹配到多个目标或无目标），则生成针对性的澄清问题（如“你是说黄色的块还是蓝色的块？”）。

### 3. 实验设计
*   **实验场景**：
    *   **单智能体环境**：基于SayCan模拟桌面环境（PyBullet），涉及物体抓取与放置。
    *   **多智能体环境**：基于LEMMA基准测试，涉及两个机器人在部分可观测环境下的协作（堆叠与传递任务）。
*   **歧义类型**：
    *   **多样性（Multiplicity）**：环境中存在多个符合描述的物体。
    *   **缺失（Absence）**：环境中不存在目标物体。
    *   **欠指定（Underspecification）**：指令本身模糊。
    *   **部分可观测性**：多机器人任务中，目标物体在当前机器人视野外。
*   **对比方法（Baselines）**：
    *   **CLARA**：基于不确定性估计的方法。
    *   **Inner Monologue**：基于环境反馈的机制。
    *   **ProgPrompt**：基于编程提示的技术（扩展了提问功能）。
*   **评估指标**：成功率（SR）和正确提问率（CQR）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **Gemini-2.5-Flash** 和 **Qwen3-VL-2B-Instruct** 作为核心LLM/VLM引擎。
*   **算力说明**：论文**未明确说明**具体的硬件配置（如GPU型号、数量）及训练/推理时长。由于该方法主要基于提示词工程（Prompting）和API调用，不涉及大规模模型微调，其算力消耗主要集中在模型推理和场景图生成的Token消耗上。

### 5. 实验数量与充分性
*   **实验规模**：在单智能体实验中进行了400次试验（每种歧义类型100次，外加100次无歧义场景）。
*   **消融实验**：设计了三组消融实验，分别验证了“迭代推理”、“检索步数”以及“场景图边信息”对性能的影响。
*   **充分性评价**：实验覆盖了从简单指令到长程任务（Long-horizon），从单机到多机协作，且对比了多种主流基准方法。实验设计较为客观，通过引入CQR指标专门衡量消歧质量，证明了方法的有效性。

### 6. 论文的主要结论与发现
*   **性能提升**：SG-CoT在提问准确率（CQR）上比现有最佳方法提升了至少10%。
*   **成功率增强**：在单智能体任务中成功率提升4%以上，在多智能体协作任务中提升15%以上。
*   **推理优势**：迭代查询机制能有效减少LLM的幻觉，使其能精准定位歧义来源（如区分是“没看到物体”还是“看到太多物体”）。
*   **模型规模效应**：性能随模型能力（如Gemini vs Qwen）的提升而增强。

### 7. 优点
*   **结构化接地**：通过场景图将非结构化的视觉信息转化为可查询的结构化知识，增强了推理的逻辑性。
*   **主动消歧**：不同于被动执行，该框架赋予机器人“不知道就问”的能力，显著提高了人机协作的安全性。
*   **可扩展性**：检索深度随任务复杂度动态调整，不随环境物体总数呈指数级增长。

### 8. 不足与局限
*   **感知依赖**：高度依赖VLM生成场景图的准确性。如果VLM在第一阶段对空间关系产生幻觉，后续推理将受误导。
*   **成本与延迟**：由于采用多轮迭代推理和历史记录拼接，Token消耗量较大，且多次调用API会增加系统的推理延迟。
*   **环境限制**：目前仅在模拟环境（Simulation）中验证，尚未在复杂的真实物理世界（Real-world）中进行测试。
*   **应用范围**：主要针对桌面操作类任务，对于更复杂的具身导航或动态环境的适应性有待验证。

（完）
