---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：面向视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v2"
tags: ["query:slm-rl"]
score: 10.0
evidence: 视觉-语言-动作模型的强化学习和世界模型
tldr: 针对大规模视觉-语言-动作（VLA）模型在强化学习中面临的计算效率低和数据获取难等挑战，本文提出了AcceRL框架。该框架采用完全异步且解耦的设计，通过物理隔离训练、推理和采样过程消除同步瓶颈。此外，AcceRL首次在分布式异步流水线中集成了可训练的世界模型以生成虚拟经验。实验证明，该框架在LIBERO基准上达到了SOTA水平，具有超线性的吞吐量扩展能力和极高的采样效率。
selection_source: fresh_fetch
motivation: 解决大规模VLA模型在强化学习训练过程中存在的计算效率低下和数据获取困难的问题。
method: 提出一种完全异步且解耦的分布式框架，物理隔离训练、推理与采样，并集成可插拔的世界模型生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现超线性吞吐量扩展，并显著提升了复杂控制任务中的采样效率。
conclusion: AcceRL为大规模VLA模型的高效训练提供了强有力的分布式异步框架和世界模型增强方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临着重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，用于生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展和极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。代码已在 https://github.com/distanceLu/AcceRL 公开。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO~\cite{liu2023libero} benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks. Code is publicly available at https://github.com/distanceLu/AcceRL.

---

## 论文详细总结（自动生成）

以下是对论文《AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大规模视觉-语言-动作（VLA）模型在机器人控制领域展现了巨大潜力，但其强化学习（RL）训练面临两大瓶颈：
    1.  **计算效率低下**：传统同步RL框架存在“长尾效应”，GPU常因等待慢速物理模拟器或不同步的采样 worker 而处于闲置状态。
    2.  **数据获取困难**：物理模拟器频率低且VLA模型推理延迟高，导致获取数百万次交互数据的成本极高。
*   **核心目标**：开发一个完全异步、解耦的分布式框架，通过物理隔离训练、推理和采样，并引入“世界模型”实现虚拟经验生成，从而大幅提升VLA模型的训练吞吐量和样本效率。

### 2. 方法论
*   **核心思想**：采用“宏观+微观”双层异步机制，并集成可训练的世界模型（World Model）。
*   **关键技术细节**：
    *   **宏观异步（训练与采样解耦）**：训练器（Trainer）与采样器（Rollout Worker）物理隔离。采样器持续将轨迹存入分布式重放池，训练器并行采样更新，利用 ZeRO 优化技术消除同步屏障。
    *   **微观异步（交互与推理解耦）**：采用“推理即服务（IaaS）”模式。采样 worker 仅负责环境交互，将推理请求发送至中心化推理池，通过动态批处理（Dynamic Batching）最大化 GPU 利用率。
    *   **世界模型增强**：集成基于扩散模型（Diffusion-based）的观测模型 $M_{obs}$ 和奖励模型 $M_{reward}$。智能体可以在“想象”中进行虚拟采样，绕过物理模拟器的速度限制。
    *   **算法优化**：
        *   **策略滞后处理**：引入 GIPO（高斯重要性采样策略优化）算法替代 PPO 的硬截断，并配合“价值重计算”和“全局优势归一化”来稳定异步训练。
        *   **VLA 适配**：实施词表瘦身（仅保留动作 Token）、动作感知注意力池化价值头、以及 Token 级策略优化（解决动作块联合概率导致的数值不稳定）。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 机器人操作基准测试，涵盖 LIBERO-Spatial、LIBERO-Object、LIBERO-Goal 和 LIBERO-Long 四个子集。
*   **Benchmark 与对比方法**：
    *   **监督学习基准**：OpenVLA-OFT。
    *   **强化学习基准**：RLinf-VLA、SimpleVLA-RL。
    *   **消融实验**：对比了有无价值重计算、GIPO vs. PPO、以及有无世界模型的情况。

### 4. 资源与算力
*   **硬件型号**：实验中提到了使用 **NVIDIA H200 GPU**。
*   **算力规模**：
    *   训练器扩展性测试中使用了多达 **7 张 GPU**。
    *   采样端测试了从 1 到 **64 个 Rollout Workers** 的扩展性。
*   **训练时长**：文中未明确给出总训练小时数，但强调了吞吐量（SPS），在 7 张 H200 上达到了 **104.22 SPS**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个复杂的机器人任务类别，进行了吞吐量扩展性实验、样本效率对比实验以及多项算法组件的消融实验。
*   **充分性评价**：实验设计较为充分。不仅验证了任务成功率（算法性能），还深入分析了系统层面的硬件利用率（>94%）和吞吐量扩展曲线。通过对比 SOTA 方法，证明了其在长程任务（LIBERO-Long）中的显著优势。

### 6. 主要结论与发现
*   **性能领先**：AcceRL 在所有 LIBERO 任务中均达到 SOTA，尤其在长程任务中成功率达 99.1%，远超监督学习基准（90.7%）。
*   **超线性扩展**：得益于 ZeRO 优化和解耦设计，训练吞吐量随 GPU 数量增加呈现超线性增长。
*   **效率飞跃**：引入世界模型后，在线样本效率提升了 **200 倍**（20,000%），仅需 10,000 次真实环境交互即可实现性能突破。
*   **稳定性**：GIPO 算法在处理异步数据滞后方面比 PPO 快约 7.5 倍，且收敛更平滑。

### 7. 优点
*   **系统架构创新**：首次将可训练的世界模型无缝集成到分布式异步 RL 流水线中，解决了 VLA 训练的“数据饥渴”问题。
*   **零卸载设计**：通过物理隔离确保模型常驻 GPU，避免了频繁的 CPU-GPU 内存交换，极大提升了硬件饱和度。
*   **数值稳定性**：针对 VLA 的 Token 级优化和 GIPO 算法有效解决了大规模模型在 RL 训练中的梯度爆炸和不收敛问题。

### 8. 不足与局限
*   **应用范围限制**：目前主要针对 VLA 模型和机器人控制任务，尚未验证其在超大规模纯语言模型（LLM）对齐任务中的表现。
*   **世界模型开销**：虽然世界模型减少了环境交互，但基于扩散模型的 $M_{obs}$ 本身计算成本较高，对推理算力有一定要求。
*   **实机验证缺失**：论文实验均在 MuJoCo 模拟器中完成，缺乏在真实物理机器人上的部署数据。

（完）
