---
title: "DALI: LLM-Agent Enhanced Dual-Stream Adaptive Leadership Identification for Group Recommendations"
title_zh: DALI：大语言模型智能体增强的双流自适应领导力识别群组推荐框架
authors: "Boxun Song, Min Gao, Jiawei Cheng"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19909v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于群体推荐的LLM智能体增强框架
tldr: 针对现有群组推荐系统难以区分领导者主导与协作决策模式的问题，本文提出DALI框架。该框架结合了大语言模型的符号推理与神经网络的表示学习，通过动态规则生成模块自主演化识别规则，并利用神经符号聚合机制分别处理领导力识别与协作动态建模。实验证明，DALI在真实旅游数据集上显著提升了推荐准确率，展现了其在复杂决策环境下的自适应能力。
selection_source: fresh_fetch
motivation: 现有群组推荐系统在处理受单一成员主导的决策时，往往无法准确区分领导者主导型与协作型群组，导致偏好建模失真。
method: 提出DALI框架，利用LLM代理动态生成识别规则，并结合神经符号聚合机制，通过符号推理识别领导力，通过注意力机制建模协作动态。
result: 在Mafengwo旅游数据集上的实验结果显示，DALI在推荐准确性指标上显著优于现有的手工规则和神经聚合模型。
conclusion: DALI成功将LLM的推理能力与神经网络的表征能力结合，为复杂现实场景下的群组推荐提供了更具自适应性的解决方案。
---

## 摘要
群组推荐系统在从休闲活动到组织团队建设的各种场景中，在支持集体决策方面发挥着关键作用。现有的群组推荐方法通常采用手工设计的聚合规则（如均值、最小痛苦、加权和）或神经聚合模型（如基于注意力的深度学习框架），但两者在区分领导者主导型群组与协作型群组方面均存在不足，且往往无法准确反映真实的群组偏好，尤其是在单个成员对群组选择产生不成比例的影响时。为了解决这些局限性，我们提出了双流自适应领导力识别（DALI）框架，该框架独特地结合了大语言模型（LLM）的符号推理能力与基于神经网络的表示学习。具体而言，DALI 引入了两项关键创新：一个通过迭代性能反馈自主制定和演化识别规则的动态规则生成模块，以及一个同时采用符号推理来稳健识别领导力群组和基于注意力的神经聚合来准确建模协作群组动态的神经符号聚合机制。在马蜂窝旅游数据集上进行的实验证实，与现有框架相比，DALI 显著提高了推荐准确性，突显了其动态适应复杂现实世界群组决策环境的能力。

## Abstract
Group recommendation systems play a pivotal role in supporting collective decisions across various contexts, from leisure activities to organizational team-building. Existing group recommendation approaches typically use either handcrafted aggregation rules (e.g. mean, least misery, weighted sum) or neural aggregation models (e.g. attention-based deep learning frameworks), yet both fall short in distinguishing leader-dominated from collaborative groups and often misrepresent true group preferences, especially when a single member disproportionately influences group choices. To address these limitations, we propose the Dual-stream Adaptive Leadership Identification (DALI) framework, which uniquely combines the symbolic reasoning capabilities of Large Language Models (LLMs) with neural network-based representation learning. Specifically, DALI introduces two key innovations: a dynamic rule generation module that autonomously formulates and evolves identification rules through iterative performance feedback, and a neuro-symbolic aggregation mechanism that concurrently employs symbolic reasoning to robustly recognize leadership groups and attention-based neural aggregation to accurately model collaborative group dynamics. Experiments conducted on the Mafengwo travel dataset confirm that DALI significantly improves recommendation accuracy compared to existing frameworks, highlighting its capability to dynamically adapt to complex, real-world group decision environments.

---

## 论文详细总结（自动生成）

这篇论文提出了 **DALI**（Dual-stream Adaptive Leadership Identification），这是一个利用大语言模型（LLM）智能体增强的群组推荐框架，旨在解决群组决策中“领导者主导”与“平等协作”模式难以准确识别的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的群组推荐系统（GR）通常假设群组偏好是成员偏好的简单聚合（如均值或注意力加权），忽略了现实中复杂的**权力结构**。
*   **研究动机**：在实际场景（如家庭旅游、团队建设）中，决策往往由一位“领导者”主导，或者由全体成员协作。现有的手工规则（如最小痛苦原则）或纯神经模型（如注意力机制）难以区分这两种模式，且缺乏解释性。
*   **整体含义**：论文试图通过引入 LLM 的推理能力，动态地识别群组中的领导力结构，从而实现更精准、更具解释性的群组偏好建模。

### 2. 方法论：核心思想与技术细节
DALI 采用**神经符号双流架构**，结合了 LLM 的符号推理和神经网络的表示学习：
*   **LLM 智能体架构**：
    *   **角色模块**：定义了“规则治理专家”（负责实时分类）和“规则演化引擎”（负责规则升级）。
    *   **记忆模块**：存储版本化的规则库、案例特征库和性能日志。
    *   **规划模块**：构建“性能-规则反馈循环（PRFL）”，当推荐效果下降时，触发 LLM 生成新规则。
    *   **动作模块**：执行特征提取、规则匹配和规则验证。
*   **双流决策机制**：
    *   **符号流**：LLM 根据成员权重分布特征，匹配逻辑规则（如“若最大权重显著高于均值，则为领导型”），输出可解释的分类。
    *   **神经流**：使用三层门控多层感知机（MLP）捕捉数据中的隐式交互模式。
*   **神经符号混合聚合（NSHA）**：
    *   **三重阈值逻辑**：若双流结果一致则直接采用；若识别为领导型，则仅采用领导者偏好；若识别为协作型，则采用注意力加权聚合；若结果冲突，则根据训练阶段动态融合。
*   **闭环演化**：系统通过“实践-反思-演化”循环，利用训练反馈自主优化识别规则，克服了静态规则的僵化。

### 3. 实验设计
*   **数据集**：
    *   **Mafengwo（马蜂窝）**：真实的旅游社交数据，群组规模较大且动态性强。
    *   **CAMRa2011**：电影评分数据，主要由家庭用户组成，群组规模较小且稳定。
*   **Benchmark 与对比方法**：
    *   **基础模型**：AGREE, GroupIM, HCR, CubeRec, DisRec。
    *   **对比框架**：将 DALI 与最新的领导力感知框架 **LARGE** 进行对比。
*   **评价指标**：HR@5/10, NDCG@5/10。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9-14900K CPU，64GB RAM，**NVIDIA GeForce RTX 4090 GPU (24GB VRAM)**。
*   **软件环境**：Ubuntu 22.04, Python 3.10, PyTorch 1.13。
*   **训练细节**：论文未明确说明总训练时长，但提到了使用 Adam 优化器，学习率为 0.001，Batch Size 为 32，训练 50 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同性质的数据集上进行了全量对比实验。
    *   **消融实验**：分别移除了神经流、符号流和动态演化机制，验证了各组件的必要性。
    *   **超参数敏感性分析**：测试了不同 Batch Size 对规则生成效率的影响。
    *   **案例研究**：展示了 LLM 生成的具体规则（如“突发决策规则”、“权力转移规则”）。
*   **充分性评价**：实验设计较为全面，涵盖了性能对比、鲁棒性分析和定性分析，能够客观证明 DALI 的有效性。

### 6. 主要结论与发现
*   **性能提升**：DALI 在所有基础模型上均实现了性能增强，尤其在 AGREE 模型上，在马蜂窝数据集上的平均提升达到了 **19.9%**。
*   **超越现有框架**：DALI 的表现显著优于之前的领导力识别框架 LARGE，且在小规模稳定群组（CAMRa2011）中表现出更好的鲁棒性。
*   **自适应能力**：LLM 能够根据训练过程中的性能波动，精准提取出人类难以手工设计的复杂领导力识别规则。

### 7. 优点
*   **首创性**：首次将 LLM 智能体引入群组推荐的领导力识别任务。
*   **神经符号融合**：结合了神经网络的泛化能力和符号规则的可解释性，解决了“黑盒”模型难以理解决策逻辑的问题。
*   **闭环自优化**：通过反馈机制实现规则的自主迭代，使模型能够适应不断变化的群组动态。

### 8. 不足与局限
*   **推理成本**：引入 LLM 智能体（尤其是频繁调用 API 或本地大模型）会显著增加系统的推理延迟和计算开销。
*   **数据集覆盖**：虽然使用了两个数据集，但群组推荐领域的数据集普遍存在稀疏性问题，DALI 在更大规模、更复杂社交网络中的表现仍待验证。
*   **LLM 偏差风险**：LLM 生成的规则可能受到预训练数据偏差的影响，虽然有验证环节，但仍存在潜在的逻辑风险。

（完）
