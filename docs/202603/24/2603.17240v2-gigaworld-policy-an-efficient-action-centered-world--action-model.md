---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v2"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于机器人策略学习和高效动作解码的世界-动作模型
tldr: 本研究针对现有世界-动作模型（WAM）推理开销大且视觉与动作表征耦合的问题，提出了GigaWorld-Policy。该模型采用以动作为中心的设计，通过因果建模确保动作预测不依赖于未来视频生成，从而实现高效推理。在推理时，视频生成变为可选，显著提升了速度。实验表明，该模型在保持高成功率的同时，推理速度比领先基准快9倍，在RoboTwin 2.0任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的世界-动作模型因视觉与动作推理耦合导致计算开销巨大，且动作预测精度受限于视频生成质量。
method: 提出一种以动作为中心的因果设计，使模型在预测动作的同时可选地生成视频，并通过大规模机器人数据集进行预训练。
result: "在真实机器人平台上，该模型比Motus快9倍且成功率提升7%，在RoboTwin 2.0上性能比pi-0.5提升了95%。"
conclusion: GigaWorld-Policy证明了通过解耦动作预测与视频生成，可以在不牺牲物理一致性的前提下显著提升机器人策略的推理效率和任务表现。
---

## 摘要
基于预训练视频生成骨干网络初始化的世界-动作模型（WAM）在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模往往使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们推出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习二维像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练表述为两个耦合的部分：模型根据当前观测预测未来动作序列，并同时根据预测的动作和相同的观测生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束促使产生符合物理规律的动作。通过防止未来视频标记（tokens）影响动作标记的因果设计，在推理时显式生成未来视频是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化的大规模机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其作为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

### GigaWorld-Policy：高效的以动作为中心的世界-动作模型论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文针对机器人领域中**视觉-语言-动作（VLA）模型**和**世界-动作模型（WAM）**存在的关键瓶颈提出方案。
*   **VLA 的局限性**：尽管语义理解强，但面临“监督稀疏”问题，即高维观测与稀疏动作标签之间的关联难以捕捉物理一致性。
*   **现有 WAM 的局限性**：虽然引入视频生成作为密集监督，但在推理时通常需要迭代采样生成未来视频才能得出动作，导致**推理延迟极高**（如 Motus 需 3 秒以上），且动作预测过度依赖视频生成质量，容易产生误差累积。
*   **核心目标**：开发一种既能利用视频生成的密集监督信号，又能在推理时实现低延迟、解耦动作预测的高效模型。

#### 2. 论文提出的方法论
GigaWorld-Policy 提出了一种**以动作为中心（Action-Centered）**的架构设计：
*   **核心思想**：将未来视觉动力学作为训练时的“推理信号”和“密集监督源”，但在推理时将其设为可选。
*   **关键技术细节**：
    *   **因果序列建模**：将观测（$T_o$）、状态（$T_s$）、动作（$T_a$）和未来视频（$T_f$）打包进统一的 Token 序列。
    *   **因果掩码（Causal Mask）设计**：这是解耦的关键。掩码规定：动作 Token 只能关注当前观测和状态，不能关注未来视频 Token；而未来视频 Token 可以关注动作。这确保了动作预测不依赖于视频生成。
    *   **流匹配（Flow Matching）**：采用流匹配目标函数同时优化动作预测损失（$L_{action}$）和视频生成损失（$L_{video}$）。
    *   **课程学习预训练**：分为三个阶段：1. 通用视频基础模型初始化；2. 具身数据预训练（结合 10,000 小时的人类/机器人视频）；3. 目标机器人任务微调。
*   **推理流程**：在部署时，模型仅执行动作解码路径，跳过视频 Token 的生成，从而大幅降低计算开销。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 **RoboTwin 2.0** 平台，涵盖 50 多个复杂的操纵任务（如调整瓶子、堆叠方块、打开笔记本等），并包含清理场景和随机化场景。
    *   **真实世界**：使用 AgileX PiPER 6自由度机械臂，设计了四个任务：清理桌面、扫描二维码、叠碗、扫垃圾。
*   **Benchmark 与对比方法**：
    *   **VLA 基准**：$\pi_{0.5}$、GigaBrain-0、X-VLA。
    *   **WAM 基准**：Motus、Cosmos-Policy。

#### 4. 资源与算力
*   **模型规模**：基于 5B 参数的 Diffusion Transformer (Wan 2.2)。
*   **预训练算力**：在公共数据集上进行了 **6000 GPU 小时**的预训练，全球批次大小（Global Batch Size）为 256。
*   **推理算力**：推理延迟测试基于 **NVIDIA A100 GPU**。
*   **训练时长**：未明确给出总天数，但给出了 GPU 小时数作为参考。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖 50+ 任务，每个任务 100 个测试回合，数据量充足。
    *   真实世界实验每个任务进行 20 次试验，采用分级评分制（如抓取成功 0.5，放置成功 0.5）。
    *   **消融实验**：针对预训练配置、未来帧预测数量（$\Delta$ 步长）、因果掩码的作用进行了详细对比。
*   **充分性评价**：实验设计较为全面，对比了当前最前沿的同类模型，且同时覆盖了仿真与实物，验证了模型在不同分布下的鲁棒性，实验结果具有较高的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **高效性**：GigaWorld-Policy 在 A100 上实现了 **0.36s** 的单步推理延迟，比 SOTA 世界模型 Motus 快 **9 倍**。
*   **性能提升**：在真实世界中，成功率比 Motus 高出 **7%**；在仿真环境 RoboTwin 2.0 中，性能比强力基准 $\pi_{0.5}$ 提升了 **95%**。
*   **数据效率**：由于引入了视频先验，该模型仅需 **10%** 的训练数据即可达到传统 VLA 模型使用 100% 数据时的成功率。
*   **视频监督的作用**：消融实验证明，预测未来帧能显著提升动作理解，成功率从 0.65（无视频预测）提升至 0.83。

#### 7. 优点
*   **架构解耦**：巧妙利用因果掩码解决了 WAM 推理慢的顽疾，实现了“训练时有视频监督，推理时无视频负担”。
*   **密集监督**：通过预测未来视觉演变，弥补了动作标签稀疏的缺陷，使模型学习到更深层的物理规律。
*   **强大的预训练范式**：通过大规模具身数据（1万小时）的预训练，显著增强了模型对不同视角和交互动力学的鲁棒性。

#### 8. 不足与局限
*   **模型体量**：虽然推理快，但 5B 的参数量对于嵌入式边缘设备（如直接运行在机器人板载计算单元上）可能仍有一定压力。
*   **任务复杂度限制**：真实世界实验虽然包含多步任务（如扫码），但任务种类（4种）相对较少，尚未在大规模开放域长程任务中进行极大规模验证。
*   **多视角依赖**：模型输入依赖固定的三视角组合，对于视角缺失或单视角机器人的泛化能力未在文中深入探讨。

（完）
