---
title: "RoboAlign: Learning Test-Time Reasoning for Language-Action Alignment in Vision-Language-Action Models"
title_zh: RoboAlign：在视觉-语言-动作模型中学习用于语言-动作对齐的测试时推理
authors: "Dongyoung Kim, Sumin Park, Woomin Song, Seungku Kim, Taeyoung Kim, Huiwon Jang, Jinwoo Shin, Jaehyung Kim, Younggyo Seo"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21341v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 通过强化学习改进视觉-语言-动作模型
tldr: RoboAlign 针对视觉-语言-动作模型（VLA）中具身推理不稳定的问题，提出了一种全新的训练框架。该方法通过零样本自然语言推理采样动作标记，并利用强化学习（RL）优化推理过程，从而增强语言与底层动作之间的对齐。在 LIBERO、CALVIN 及真实环境中的实验表明，RoboAlign 仅需极少量数据即可显著提升 VLA 的动作准确性，为多模态大模型向机器人动作的知识迁移提供了有效路径。
selection_source: fresh_fetch
motivation: 现有的通过视觉问答监督提升具身推理的方法在 VLA 性能上表现不稳定，往往只能获得微小甚至负面的收益。
method: 提出 RoboAlign 框架，通过零样本自然语言推理采样动作，并结合强化学习对推理过程进行对齐和优化。
result: "在 LIBERO、CALVIN 和真实世界基准测试中，RoboAlign 相比 SFT 基线分别实现了 17.5%、18.9% 和 106.6% 的显著性能提升。"
conclusion: RoboAlign 有效弥合了语言与底层动作之间的模态鸿沟，证明了通过强化学习优化推理过程是提升 VLA 性能的可靠且高效的途径。
---

## 摘要
提升多模态大语言模型（MLLMs）中的具身推理能力，对于在其基础上构建视觉-语言-动作模型（VLAs）以将多模态理解转化为底层动作至关重要。因此，最近的研究探索了通过视觉问答类型的监督来增强 MLLMs 中的具身推理。然而，据报道这些方法会导致 VLA 性能不稳定，通常只能产生微小甚至负面的收益。在本文中，我们提出了一种更系统的 MLLM 训练框架 RoboAlign，能够可靠地提升 VLA 性能。我们的核心思想是通过零样本自然语言推理采样动作标记，并利用强化学习（RL）优化该推理过程，从而提高动作准确性。因此，RoboAlign 弥合了 MLLMs 中语言与底层动作之间的模态差距，并促进了从 MLLM 到 VLA 的知识迁移。为了验证 RoboAlign 的有效性，我们通过在 MLLM 骨干网络上添加基于扩散的动作头来训练 VLA，并在主要的机器人基准测试中对其进行评估。值得注意的是，通过在监督微调（SFT）后使用不到 1% 的数据进行基于强化学习的对齐，RoboAlign 在 LIBERO、CALVIN 和真实世界环境中，相较于 SFT 基准分别实现了 17.5%、18.9% 和 106.6% 的性能提升。

## Abstract
Improving embodied reasoning in multimodal-large-language models (MLLMs) is essential for building vision-language-action models (VLAs) on top of them to readily translate multimodal understanding into low-level actions. Accordingly, recent work has explored enhancing embodied reasoning in MLLMs through supervision of vision-question-answering type. However, these approaches have been reported to result in unstable VLA performance, often yielding only marginal or even negative gains. In this paper, we propose a more systematic MLLM training framework RoboAlign that reliably improves VLA performance. Our key idea is to sample action tokens via zero-shot natural language reasoning and refines this reasoning using reinforcement learning (RL) to improve action accuracy. As a result, RoboAlign bridges the modality gap between language and low-level actions in MLLMs, and facilitate knowledge transfer from MLLM to VLA. To validate the effectiveness of RoboAlign, we train VLAs by adding a diffusion-based action head on top of an MLLM backbone and evaluate them on major robotics benchmarks. Remarkably, by performing RL-based alignment after SFT using less than 1\% of the data, RoboAlign achieves performance improvements of 17.5\%, 18.9\%, and 106.6\% over SFT baselines on LIBERO, CALVIN, and real-world environments, respectively.

---

## 论文详细总结（自动生成）

### RoboAlign 论文深度总结

#### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言-动作模型（VLA）通常构建在多模态大语言模型（MLLM）之上。尽管通过视觉问答（VQA）提升 MLLM 的“具身推理”能力是当前主流，但研究发现这种推理能力的提升并不总能转化为更好的机器人动作性能，甚至可能导致性能下降（即“推理好，动作差”的矛盾）。
*   **核心问题**：语言推理与底层动作执行之间存在**模态鸿沟**。论文旨在探索如何通过系统性的训练框架，将 MLLM 的推理过程与底层动作标记（Low-level Action Tokens）直接对齐，从而可靠地提升 VLA 的实际操作表现。

#### 2. 方法论
*   **核心思想**：借鉴 DeepSeek-R1 的思路，让模型在输出动作前先进行“思考”（Chain-of-Thought），并利用强化学习（RL）根据最终动作的准确性来优化整个推理链条。
*   **关键技术细节**：
    *   **动作离散化（FAST Tokenization）**：将机器人的连续动作（位置、姿态、夹爪）通过离散余弦变换（DCT）和字节对编码（BPE）压缩为离散的“动作标记”。
    *   **两阶段训练流程**：
        1.  **阶段 1：机器人感知 SFT**。使用混合数据集（通用 VQA、具身推理 VQA、FAST 动作数据）进行监督微调，使模型学会生成 `<think>推理过程</think><answer>动作标记</answer>` 的格式。
        2.  **阶段 2：基于 RL 的动作对齐**。使用 **GRPO（组相对策略优化）** 算法。对同一指令采样多个响应，根据“格式奖励”和“动作准确度奖励”（计算生成标记与真实标记的前缀匹配度）来优化模型。
    *   **VLA 转换**：在训练好的 MLLM 骨干上添加一个扩散动作头（Diffusion Action Head），在特定机器人数据集上微调该动作头，同时保持 MLLM 骨干冻结。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含空间、对象、目标和长程任务（共 40 个任务）。
    *   **CALVIN**：测试在未见环境（ABC->D）中的零样本任务迁移能力。
    *   **真实世界**：使用 Franka Research 3 机械臂执行四类抓取放置任务。
*   **对比方法**：
    *   基准 MLLM（Qwen2.5-VL-7B-Instruct）。
    *   仅语言 SFT、仅动作 SFT。
    *   其他对齐策略：高层动作 RL、2D 视觉轨迹 RL、SFT 形式的具身思维链（ECoT）。
*   **评估指标**：任务成功率、连续任务完成长度。

#### 4. 资源与算力
*   **MLLM 训练**：使用 **8 × NVIDIA H200 GPU**。SFT 阶段耗时约 30 小时，RL 阶段耗时约 1 小时。
*   **VLA 训练**：使用 **2 × NVIDIA A100 GPU**。每 10K 步训练约需 1 小时。
*   **训练规模**：LIBERO 训练 60K 步，CALVIN 训练 100K 步，真实机器人训练 30K 步。

#### 5. 实验数量与充分性
*   **实验规模**：进行了仿真环境（数千次测试循环）、真实世界机器人验证、以及跨模型架构（Qwen3-VL）的泛化性测试。
*   **充分性**：实验设计非常全面。不仅对比了最终的动作成功率，还通过 **KNN 表征分析** 验证了模型内部特征的判别力，并测试了模型在通用 MLLM 基准（如 MMStar, BLINK）上的表现，确保没有出现“灾难性遗忘”。对比实验涵盖了当前主流的多种对齐路径，逻辑闭环完整。

#### 6. 主要结论与发现
*   **性能飞跃**：RoboAlign 在 LIBERO 上提升了 17.5%，CALVIN 提升 18.9%，真实世界任务提升达 **106.6%**。
*   **RL 的高效性**：RL 阶段仅使用了不到 SFT 阶段 1% 的数据量（12.8K vs 2.28M），却贡献了主要的性能增益。
*   **底层对齐的优势**：直接对齐“底层动作标记”比对齐“高层语言指令”或“视觉轨迹”更能显著提升长程任务（Long-horizon）的成功率。
*   **表征增强**：通过 RL 对齐，MLLM 的内部表征对机器人状态的判别准确率从 39% 提升至 70%，证明模型学到了更精细的物理特征。

#### 7. 优点
*   **创新性**：首次系统地将大规模强化学习推理（R1 范式）应用于 VLA 的底层动作对齐。
*   **数据效率**：证明了极少量的动作对齐数据即可激活 MLLM 强大的具身推理潜力。
*   **通用性**：在不同版本的 Qwen 模型上均表现出一致的提升，具有良好的架构迁移性。
*   **平衡性**：在大幅提升机器人操作能力的同时，保持甚至增强了模型的通用视觉理解能力。

#### 8. 不足与局限
*   **动作空间依赖**：高度依赖 FAST Tokenization 这一特定的离散化方案，对于其他动作表示形式（如连续值回归）的适用性尚未讨论。
*   **任务复杂度限制**：实验主要集中在机械臂的抓取与放置，对于更复杂的双臂协作、动态避障或全身控制任务尚待验证。
*   **安全风险**：论文提到，如果 RL 阶段使用的动作轨迹包含不安全行为，模型可能会强化这些负面偏见。

（完）
