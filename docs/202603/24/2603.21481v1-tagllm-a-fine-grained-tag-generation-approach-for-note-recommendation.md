---
title: "TagLLM: A Fine-Grained Tag Generation Approach for Note Recommendation"
title_zh: TagLLM：一种用于笔记推荐的细粒度标签生成方法
authors: "Zhijian Chen, Likai Wang, Lei Chen, Yaguang Dou, Jialiang Shi, Tian Qi, Dongdong Hao, Mengying Lu, Cheng Ye, Chao Wei"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21481v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 基于大模型的笔记推荐标签生成
tldr: 针对电商社区笔记推荐中标签生成冗余且粗粒度的问题，本文提出 TagLLM。该方法利用多模态大模型（MLLM）的生成能力，通过用户兴趣手册引导和多模态思维链提取，生成细粒度且可解释的标签。同时，采用知识蒸馏技术将生成能力迁移至小模型以提升推理效率。实验证明，TagLLM 在在线 A/B 测试中显著提升了用户互动指标，并在冷启动场景下表现优异。
selection_source: fresh_fetch
motivation: 现有的标签生成方法因缺乏引导导致生成的标签冗余且粒度较粗，无法精准刻画用户兴趣并有效支持推荐系统。
method: 提出 TagLLM 框架，通过用户兴趣手册捕捉兴趣、多模态思维链提取细粒度标签，并利用知识蒸馏优化推理效率。
result: "在线 A/B 测试显示，该方法提升了人均观看时长和互动量，并在冷启动场景下将页面点击率显著提高了 32.37%。"
conclusion: TagLLM 证明了利用大模型生成细粒度、可解释标签能有效增强推荐系统的性能，特别是在处理冷启动问题上具有显著优势。
---

## 摘要
大语言模型（LLMs）在电商社区推荐中展现出巨大的潜力。虽然 LLMs 和多模态大语言模型（MLLMs）被广泛用于将笔记编码为隐式嵌入，但利用其生成能力通过可解释标签来表示笔记的研究仍有待探索。在标签生成领域，传统的封闭式方法严重依赖于标签池的设计，而直接应用于笔记推荐的现有开放式方法面临两个局限性：（1）MLLMs 在生成过程中缺乏引导，导致生成的冗余标签无法捕捉用户兴趣；（2）生成的标签通常粒度较粗，缺乏对笔记的细粒度表示，从而干扰了下游推荐。为了解决这些局限性，我们提出了 TagLLM，一种用于笔记推荐的细粒度标签生成方法。TagLLM 通过用户兴趣手册（User Interest Handbook）捕捉不同笔记类别的用户兴趣，并利用多模态思维链（CoT）提取构建细粒度标签数据。此外，我们开发了一种标签知识蒸馏方法，使小模型具备具有竞争力的生成能力，从而提高推理效率。在在线 A/B 测试中，TagLLM 使人均平均观看时长增加了 0.31%，人均平均交互次数增加了 0.96%，冷启动场景下的页面浏览点击率提升了 32.37%，证明了其有效性。

## Abstract
Large Language Models (LLMs) have shown promising potential in E-commerce community recommendation. While LLMs and Multimodal LLMs (MLLMs) are widely used to encode notes into implicit embeddings, leveraging their generative capabilities to represent notes with interpretable tags remains unexplored. In the field of tag generation, traditional close-ended methods heavily rely on the design of tag pools, while existing open-ended methods applied directly to note recommendations face two limitations: (1) MLLMs lack guidance during generation, resulting in redundant tags that fail to capture user interests; (2) The generated tags are often coarse and lack fine-grained representation of notes, interfering with downstream recommendations. To address these limitations, we propose TagLLM, a fine-grained tag generation method for note recommendation. TagLLM captures user interests across note categories through a User Interest Handbook and constructs fine-grained tag data using multimodal CoT Extraction. A Tag Knowledge Distillation method is developed to equip small models with competitive generation capabilities, enhancing inference efficiency. In online A/B test, TagLLM increases average view duration per user by 0.31%, average interactions per user by 0.96%, and page view click-through rate in cold-start scenario by 32.37%, demonstrating its effectiveness.

---

## 论文详细总结（自动生成）

### TagLLM：一种用于笔记推荐的细粒度标签生成方法

#### 1. 核心问题与研究动机
在电商社区（如得物、小红书）的笔记推荐中，现有的多模态大模型（MLLM）应用主要集中在将笔记编码为隐式嵌入（Embedding），但这种方式缺乏透明度和可解释性。传统的标签生成方法存在两大痛点：
*   **封闭式方法局限性：** 依赖预定义的标签池，难以扩展，无法捕捉笔记间的细微差异。
*   **开放式大模型生成问题：** 缺乏引导，导致生成的标签冗余、聚焦不足（如将所有运动产品都贴上“阿迪达斯”标签），且粒度过粗，无法精准刻画用户兴趣，干扰下游推荐效果。

#### 2. 方法论：TagLLM 框架
论文提出了一套从标签定义到模型蒸馏，再到推荐应用的完整流程：
*   **用户兴趣手册（User Interest Handbook）生成：** 针对 18 个笔记类别（如穿搭、游戏、数码），利用 LLM 扩展并结合专家修正，定义每个类别下用户关注的细粒度维度（如材质、风格、IP、适用场景等）。
*   **多模态 CoT 标签提取：** 整合图像、文本和语音（ASR）信息，通过思维链（CoT）分三步提取：
    1.  **基于手册生成：** 依据维度指引生成原始标签。
    2.  **低信息合并：** 合并冗余、琐碎或无关的标签。
    3.  **重要性排序：** 对精炼后的标签进行 1-5 分的重要性打分。
*   **标签知识蒸馏：** 为了线上推理效率，将大模型的生成能力迁移至小参数模型（2B-4B）。采用两阶段训练：
    1.  **SFT（监督微调）：** 学习标签生成的分布。
    2.  **DPO（直接偏好优化）：** 利用“大模型作为裁判”的机制构建偏好对，进一步优化生成质量。
*   **推荐特征转换：** 将生成的细粒度标签转化为“用户画像标签”和“标签嵌入向量”，输入双塔推荐模型进行召回和排序。

#### 3. 实验设计
*   **数据集：** 使用得物（Dewu）平台的真实数据，涵盖 18 个类别。数据集分为 SFT 集合（约 11.9 万）、DPO 池（约 2 万）和评估集（约 2 万）。
*   **Benchmark 与对比方法：** 
    *   **离线评估：** 对比了 InternVL3/3.5、Qwen3-VL 等不同尺寸（2B、4B）的基座模型在 SFT 前后的表现。
    *   **裁判模型：** 使用 `doubao-1.6-thinking` 作为裁判，评估标签的真实性（Truth）、覆盖率（Coverage）和重要性（Importance）。
    *   **在线 A/B 测试：** 在真实推荐场景下对比传统标签方法。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   文中提到了模型参数规模（2B 和 4B 级别），并指出通过蒸馏到小模型显著提升了工业界的推理效率。

#### 5. 实验数量与充分性
*   **实验规模：** 离线实验涵盖了 5 种不同的 MLLM 基座模型，并针对 18 个类别进行了细分评估。
*   **消融实验：** 针对输入模态（文本、图像、ASR、产品信息）做了消融，验证了各模态对不同类别（如鞋类 vs 玩具）的影响差异。
*   **对齐实验：** 专门对比了不同大模型（GPT-4o, GPT-5 等）作为裁判与人类专家偏好的对齐程度，确保了评估指标的客观性。
*   **充分性评价：** 实验设计较为全面，不仅有离线指标，还有大规模在线 A/B 测试和冷启动专项测试，结果具有较高的工业参考价值。

#### 6. 主要结论与发现
*   **性能提升：** 在线 A/B 测试显示，TagLLM 使人均观看时长提升 0.31%，人均互动量提升 0.96%。
*   **冷启动奇效：** 在冷启动场景下，页面点击率（PVCTR）大幅提升 **32.37%**，互动率提升 46.91%，证明细粒度标签能有效弥补新笔记交互数据的匮乏。
*   **模型能力：** 经过 SFT+DPO 蒸馏后的 4B 模型在标签生成质量上已接近更大参数规模的模型。

#### 7. 优点
*   **工业落地性强：** 解决了大模型在推荐系统中推理成本高、标签质量不可控的实际问题。
*   **可解释性：** 相比隐式 Embedding，细粒度标签为推荐理由提供了直观的解释。
*   **引导式生成：** 引入“用户兴趣手册”和“CoT 提取”，有效解决了开放式生成容易产生的“幻觉”和“聚焦不足”问题。

#### 8. 不足与局限
*   **视频理解局限：** 目前仅采用固定频率抽帧处理视频，对于长视频或复杂动态语义的捕捉能力有限。
*   **推理幻觉：** 在笔记信息极度残缺时，模型仍可能产生推理幻觉（如将本属于本田的车型误认为丰田）。
*   **逻辑矛盾处理：** 当用户发布的笔记内容本身存在矛盾（如文字说是 A 角色，图片是 B 角色）时，模型有时会被误导。

（完）
