---
title: "AgenticRec: End-to-End Tool-Integrated Policy Optimization for Ranking-Oriented Recommender Agents"
title_zh: AgenticRec：面向排序推荐智能体的端到端工具集成策略优化
authors: "Tianyi Li, Zixuan Wang, Guidong Lei, Xiaodong Li, Hui Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21613v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 基于大模型的智能体推荐与策略优化
tldr: AgenticRec是一个面向排序的推荐智能体框架，旨在解决现有LLM推荐系统在推理与排序反馈脱节及细粒度偏好捕捉不足的问题。该框架通过ReAct循环集成推荐专用工具，并提出List-Wise GRPO算法对包含推理、工具调用和排序生成的全决策轨迹进行端到端优化。此外，引入渐进式偏好细化（PPR）机制，通过挖掘难负样本和双向对齐来消除偏好歧义。实验证明，该方法在多个基准测试中显著优于现有模型。
selection_source: fresh_fetch
motivation: 现有推荐智能体在中间推理过程与最终排序反馈之间存在脱节，且难以捕捉用户的细粒度偏好。
method: 提出了集成推荐工具的ReAct循环，并结合List-Wise GRPO策略优化和渐进式偏好细化（PPR）机制进行端到端训练。
result: 实验结果显示 AgenticRec 在多个基准数据集上显著超越了现有基准模型，验证了该框架的有效性。
conclusion: 统一推理、工具使用与排序优化的端到端策略是提升推荐智能体性能的关键。
---

## 摘要
基于大语言模型的推荐智能体为推荐系统提供了一种极具前景的范式。然而，现有的推荐智能体通常面临中间推理与最终排序反馈脱节的问题，且无法捕捉细粒度的偏好。为了解决这一问题，我们提出了 AgenticRec，这是一个面向排序的智能体推荐框架，能够在稀疏隐式反馈下优化整个决策轨迹（包括中间推理、工具调用和最终排序列表生成）。本研究具有三个核心贡献：首先，我们设计了一套集成在 ReAct 循环中的推荐专用工具，以支持基于证据的推理；其次，我们提出了理论上无偏的列表级组相对策略优化（list-wise GRPO），以最大化排序效用，确保复杂工具使用轨迹的准确信用分配；第三，我们引入了渐进式偏好细化（PPR）来解决细粒度的偏好歧义。通过从排序违规中挖掘难负样本并应用双向偏好对齐，PPR 最小化了成对排序误差的凸上界。基准测试实验证实，AgenticRec 显著优于基准模型，验证了统一推理、工具使用和排序优化的必要性。

## Abstract
Recommender agents built on Large Language Models offer a promising paradigm for recommendation. However, existing recommender agents typically suffer from a disconnect between intermediate reasoning and final ranking feedback, and are unable to capture fine-grained preferences. To address this, we present AgenticRec, a ranking-oriented agentic recommendation framework that optimizes the entire decision-making trajectory (including intermediate reasoning, tool invocation, and final ranking list generation) under sparse implicit feedback. Our approach makes three key contributions. First, we design a suite of recommendation-specific tools integrated into a ReAct loop to support evidence-grounded reasoning. Second, we propose theoretically unbiased List-Wise Group Relative Policy Optimization (list-wise GRPO) to maximize ranking utility, ensuring accurate credit assignment for complex tool-use trajectories. Third, we introduce Progressive Preference Refinement (PPR) to resolve fine-grained preference ambiguities. By mining hard negatives from ranking violations and applying bidirectional preference alignment, PPR minimizes the convex upper bound of pairwise ranking errors. Experiments on benchmarks confirm that AgenticRec significantly outperforms baselines, validating the necessity of unifying reasoning, tool use, and ranking optimization.

---

## 论文详细总结（自动生成）

### AgenticRec：面向排序推荐智能体的端到端工具集成策略优化

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）在推荐系统中的应用，推荐智能体（Recommender Agents）成为研究热点。然而，现有智能体面临两个核心挑战：
*   **推理与反馈脱节**：智能体的中间推理过程和工具调用通常依赖静态提示词或启发式规则，未能根据最终的排序反馈（如点击、购买）进行端到端优化。
*   **细粒度偏好捕捉不足**：在稀疏的隐式反馈下，智能体难以区分高度相似的候选物品之间的细微偏好差异。
**AgenticRec** 旨在通过端到端的策略优化，将推理、工具使用与排序目标统一起来，提升推荐的准确性。

#### 2. 方法论
AgenticRec 采用两阶段训练范式，核心技术包括：
*   **工具集成排序推理（TIRR）**：在 ReAct（Think-Act-Observation）循环中集成推荐专用工具（用户画像、物品信息、行为统计、协同过滤工具）。工具调用被视为决策轨迹的一部分，由模型自主决定何时、为何调用。
*   **列表级组相对策略优化（List-Wise GRPO）**：
    *   **核心思想**：借鉴 DeepSeek-V3 的 GRPO 算法，通过在一组采样轨迹中计算相对奖励来减少方差。
    *   **奖励设计**：以 NDCG@K 作为核心奖励，辅以格式约束惩罚和鼓励有效工具调用的奖励。
    *   **公式逻辑**：通过最大化预期列表效用，确保排序质量的梯度能回传至中间推理和工具调用步骤。
*   **渐进式偏好细化（PPR）**：
    *   **难负样本挖掘**：从模型预测错误的排序结果中自动提取排在正样本之前的负样本。
    *   **双向偏好推理**：不仅训练模型识别“用户更可能买什么”，还训练其识别“用户更不可能买什么”，通过双向对齐强化偏好边界。

#### 3. 实验设计
*   **数据集**：使用 Amazon 2023 蓝图中的四个子集：CDs、Instruments（乐器）、Office（办公用品）、Games（游戏）。
*   **任务设置**：从 20 个候选物品中生成 Top-10 排序列表。
*   **对比方法（Benchmark）**：
    *   **传统序列推荐**：Caser, GRU4Rec, SASRec, ReaRec。
    *   **免训练 LLM 方法**：LLMRank, InteRecAgent（基于 GPT-4）。
    *   **可训练 LLM 方法**：TALLRec, LLaRA, S-DPO, ReRe。

#### 4. 资源与算力
*   **模型底座**：主要使用 **Qwen3-4B-Instruct**，并测试了 1.7B 和 8B 版本。
*   **硬件设备**：使用了 **4 张 NVIDIA A800 GPU (80GB)**。
*   **训练细节**：第一阶段训练 3 个 epoch，第二阶段训练 1 个 epoch。学习率设为 $1 \times 10^{-6}$，Batch Size 为 64。

#### 5. 实验数量与充分性
论文进行了多维度的实验验证：
*   **主实验**：在 4 个数据集上对比了 10 种基准方法，涵盖了从传统模型到最新 LLM 推荐模型。
*   **消融实验**：验证了推理（R）、工具集成（TIRR）和偏好细化（PPR）各自的贡献。
*   **超参数分析**：探讨了 GRPO 组大小（Group Size）对性能的影响。
*   **扩展性分析**：验证了方法在不同参数规模（1.7B 到 8B）底座模型上的表现。
*   **案例研究**：通过具体案例展示了模型如何通过工具调用修正推理偏差。
**评价**：实验设计全面，对比基准具有代表性，且包含了理论证明（无偏梯度估计和误差界限分析），客观性与充分性较高。

#### 6. 主要结论与发现
*   **端到端优化的必要性**：单纯依靠 LLM 先验知识调用工具效果有限，通过 List-Wise GRPO 进行端到端训练能显著提升工具调用的有效性。
*   **AgenticRec 性能卓越**：在所有数据集和指标（Hit@K, NDCG@K）上均显著优于现有最强基准（如 LLaRA 和 ReRe）。
*   **双向推理的价值**：PPR 阶段通过引入“负向偏好”监督，有效解决了高相似候选物之间的排序歧义。
*   **模型扩展性**：随着底座模型参数量增加，AgenticRec 的性能持续提升，展现了良好的 Scaling Law 特性。

#### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还给出了 List-Wise GRPO 无偏性的数学证明。
*   **工具驱动的决策**：将工具调用深度融入推理链，使推荐过程更具解释性和证据支撑。
*   **创新的 PPR 机制**：利用模型自身的错误进行自我进化，无需额外人工标注。

#### 8. 不足与局限
*   **推理延迟**：由于采用了多步 ReAct 推理和多次工具调用，在线推理的延迟（Latency）会高于单次预测模型。
*   **候选集依赖**：实验基于预先检索好的 20 个候选物品，尚未探讨在大规模全量库检索场景下的表现。
*   **长期记忆缺失**：论文提到目前尚未充分利用长期记忆机制处理极长用户序列，这被列为未来工作。

（完）
