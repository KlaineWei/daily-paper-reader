---
title: "Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe"
title_zh: 揭秘长程工具使用智能体的强化学习：一份全面指南
authors: "Xixi Wu, Qianguo Sun, Ruiyang Zhang, Chao Song, Junlong Wu, Yiyan Qi, Hong Cheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21972v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于长程工具使用智能体的强化学习及模型缩放效应
tldr: 本研究针对大语言模型在长程规划和工具使用中缺乏实用强化学习（RL）训练方案的问题，利用TravelPlanner基准进行了系统性实证研究。通过探索奖励建模、模型规模、数据组成、算法选择和环境稳定性五个核心维度，总结出7项关键实践准则。实验表明，该方案能显著提升智能体性能，在TravelPlanner上超越了现有领先模型，为构建自主智能体提供了可扩展的RL训练指南。
selection_source: fresh_fetch
motivation: 旨在填补大语言模型在复杂、多轮工具使用场景下缺乏高效强化学习训练实践方案的空白。
method: 基于TravelPlanner任务，从奖励函数设计、模型缩放、数据配比、算法优化及环境稳定性五个维度开展受控实验。
result: 发现1K左右的平衡难度样本是训练甜点位，且大模型在简单密集奖励下即可高效收敛，最终取得SOTA性能。
conclusion: 本文提炼的强化学习训练“食谱”为开发具备长程规划能力的自主工具使用智能体提供了重要参考。
---

## 摘要
强化学习 (RL) 对于将大语言模型 (LLMs) 演进为具备长程规划能力的自主智能体至关重要，然而在复杂的、多轮对话环境中扩展 RL 的实用方案仍然难以捉摸。本文利用 TravelPlanner 开展了一项系统的实证研究，TravelPlanner 是一个具有挑战性的测试平台，要求通过工具编排来满足多方面的约束。我们从五个维度分解了智能体 RL 的设计空间：奖励塑造、模型缩放、数据组成、算法选择和环境稳定性。我们的受控实验得出了 7 个关键结论，例如：(1) 奖励和算法的选择具有规模依赖性，较小的模型受益于阶段性奖励和增强的探索，而较大的模型则能通过更简单的稠密奖励高效收敛；(2) 约 1000 个难度均衡的训练样本是域内和域外性能的最佳平衡点；(3) 环境稳定性对于防止策略退化至关重要。基于我们提炼的方案，经过 RL 训练的模型在 TravelPlanner 上达到了最先进的性能，显著优于领先的大语言模型。

## Abstract
Reinforcement Learning (RL) is essential for evolving Large Language Models (LLMs) into autonomous agents capable of long-horizon planning, yet a practical recipe for scaling RL in complex, multi-turn environments remains elusive. This paper presents a systematic empirical study using TravelPlanner, a challenging testbed requiring tool orchestration to satisfy multifaceted constraints. We decompose the agentic RL design space along 5 axes: reward shaping, model scaling, data composition, algorithm selection, and environmental stability. Our controlled experiments yield 7 key takeaways, e.g., (1) reward and algorithm choices are scale-dependent as smaller models benefit from staged rewards and enhanced exploration, whereas larger models converge efficiently with simpler dense rewards, (2) ~ 1K training samples with a balanced difficulty mixture mark a sweet spot for both in-domain and out-of-domain performance, and (3) environmental stability is critical to prevent policy degradation. Based on our distilled recipe, our RL-trained models achieve state-of-the-art performance on TravelPlanner, significantly outperforming leading LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何系统性地利用强化学习（RL）提升大语言模型（LLM）在**长程（Long-Horizon）**、**多轮工具使用**场景下的规划能力？
*   **研究动机**：虽然 RL 已被证明能增强 LLM 的推理能力，但现有研究多集中在短程任务（如单步推理或少量对话）。在需要数十次工具调用、满足复杂约束的长程任务中，如何设计奖励函数、选择数据规模、匹配算法以及应对环境不稳定性，目前缺乏一份全面且可扩展的实践指南（Recipe）。

### 2. 论文提出的方法论
论文提出了 **STAR (Synthesis, Training, And Reinforcement)** 统一后训练框架，包含三个阶段：
*   **数据合成 (Synthesis)**：通过采样原子元素并验证可行性，合成具有可控难度（简单、中等、困难）的查询。
*   **监督微调 (SFT)**：利用强教师模型（如 DeepSeek-V3.2-Exp-Thinking）生成高质量轨迹，通过拒绝采样筛选出成功案例进行微调，解决 RL 的冷启动问题。
*   **强化学习 (RL)**：
    *   **核心算法**：采用 **GRPO**（群体相对策略优化），并探索了其变体（DAPO, ARPO）。
    *   **设计空间分解**：从五个关键维度进行受控实验：
        1.  **奖励塑造**：对比稠密奖励（SUM）、半稀疏奖励（MACRO）、纯稀疏奖励（SUCCESS）及课程奖励（CURRICULUM）。
        2.  **模型缩放**：测试 1.5B、3B 和 7B 不同参数规模。
        3.  **数据组成**：探索样本数量（100 到 2000）和难度配比。
        4.  **算法选择**：对比标准 GRPO 与增强探索的算法。
        5.  **环境稳定性**：模拟工具调用失败对训练的影响。

### 3. 实验设计
*   **主要测试平台**：**TravelPlanner**。这是一个模拟真实旅行社场景的基准，要求智能体调用 6 种工具（如搜机票、搜酒店）来满足预算、偏好等复杂约束。
*   **域外（OOD）评估**：在 7 个知识密集型问答基准（如 NQ, TriviaQA, HotpotQA）上测试泛化能力。
*   **对比方法**：
    *   **基座模型**：Qwen2.5-Instruct 系列。
    *   **领先 LLMs**：GPT-5 (预估)、Kimi-K2.5、Gemini-1.5-Pro、DeepSeek-V3 等。
    *   **SOTA 智能体**：如 Planner-R1。

### 4. 资源与算力
*   **算力设备**：
    *   **1.5B 和 3B 模型**：使用单节点 8 × NVIDIA A100-80G。
    *   **7B 模型**：使用双节点共 16 × NVIDIA A100-80G。
*   **训练时长**：
    *   标准 GRPO 训练 7B 模型约需 **368 GPU 小时**。
    *   探索性更强的算法（如 ARPO）在 7B 规模下需 **547 GPU 小时**。
    *   单步训练时间随模型规模和算法复杂度增加（约 8-13 分钟/步）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的受控实验，涵盖了 5 个维度的消融研究。
*   **充分性**：
    *   **多维度覆盖**：不仅关注性能，还关注训练动态（收敛速度、熵变化）和 OOD 泛化。
    *   **客观性**：所有模型使用统一的格式化工具（DeepSeek-V3.2-Exp）进行评估，确保公平。
    *   **数据量**：合成了超过 10K 条数据，并从中筛选出 1K 条用于 RL 核心实验。

### 6. 主要结论与发现
1.  **奖励设计具有规模依赖性**：小模型（1.5B/3B）极度依赖**课程奖励**（从稠密到稀疏过渡）；而大模型（7B）直接使用**稠密奖励**即可高效收敛。
2.  **泛化税（Alignment Tax）**：过度任务特定的稠密奖励会损害 OOD 性能，半稀疏奖励（MACRO）是平衡域内性能与泛化的最佳选择。
3.  **数据甜点位**：**1000 个样本**是训练的黄金分割点，超过此量级会导致模型过拟合，泛化能力下降。
4.  **难度配比**：必须包含简单样本以提供初始奖励信号，纯困难样本会导致奖励稀疏从而训练失败。
5.  **算法必要性**：对于 7B 及以上模型，标准 GRPO 性能已足够，无需复杂的探索增强算法（如 ARPO）。
6.  **环境鲁棒性**：智能体能容忍 5% 以下的工具调用错误率，但 10% 的错误率会导致性能显著退化。

### 7. 优点
*   **实战性强**：为开发者提供了明确的“食谱”，包括在不同模型规模下该选什么奖励和算法。
*   **性能卓越**：通过 RL 训练的 7B 模型在 TravelPlanner 上的成功率（62.8%）远超 Kimi-K2.5（11.8%）和 GPT-4o。
*   **开源贡献**：提供了 STAR 框架和训练方案，有助于社区复现长程智能体。

### 8. 不足与局限
*   **环境局限**：TravelPlanner 虽然复杂，但仍是模拟沙盒，无法完全代表真实互联网环境的不可预测性。
*   **模型规模上限**：实验最高仅覆盖到 7B 参数，更大规模模型（如 70B+）的 RL 行为是否符合该 Recipe 仍待验证。
*   **OOD 范围有限**：泛化测试主要集中在问答任务，缺乏跨领域（如从旅行规划到代码生成）的鲁棒性评估。
*   **单因素分析**：主要采取控制变量法，多个因素（如数据难度与算法选择）之间的复杂交互作用研究较少。

（完）
