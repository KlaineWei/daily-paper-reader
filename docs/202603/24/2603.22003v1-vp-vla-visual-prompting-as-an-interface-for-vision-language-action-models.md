---
title: "VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models"
title_zh: VP-VLA：视觉提示作为视觉-语言-动作模型的接口
authors: "Zixuan Wang, Yuxin Chen, Yuqi Liu, Jinhui Ye, Pengguang Chen, Changsheng Lu, Shu Liu, Jiaya Jia"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22003v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 解耦高层规划与底层执行的VLA框架
tldr: 针对视觉-语言-动作（VLA）模型在空间精度和鲁棒性上的不足，本文提出VP-VLA框架。该框架采用双系统架构，通过“系统2规划器”将复杂指令分解并生成视觉提示（如十字准星和边界框），再由“系统1控制器”结合这些提示执行精确动作。实验表明，VP-VLA在多个机器人仿真基准测试中显著提升了任务成功率，超越了QwenOFT和GR00T-N1.6等主流模型。
selection_source: fresh_fetch
motivation: 传统的VLA模型采用黑盒映射方式，难以同时兼顾指令理解、空间定位和底层控制，导致空间精度和泛化能力受限。
method: 提出一种双系统框架，利用规划器生成结构化视觉提示作为接口，并结合辅助视觉定位目标来指导控制器生成精确动作。
result: "在Robocasa和SimplerEnv基准测试中，VP-VLA的成功率分别提升了5%和8.3%，优于QwenOFT和GR00T-N1.6等基准模型。"
conclusion: 通过引入视觉提示接口解耦高层推理与底层执行，能有效增强VLA模型的空间感知能力和操作鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）模型通常将视觉观测和语言指令直接映射为机器人控制信号。这种“黑盒”映射迫使单次前向传播同时处理指令解释、空间定位和底层控制，往往导致空间精度较差，且在分布外（out-of-distribution）场景中的鲁棒性有限。为了解决这些局限性，我们提出了 VP-VLA，这是一个双系统框架，通过结构化视觉提示接口将高层推理与底层执行解耦。具体而言，“系统 2 规划器”将复杂指令分解为子任务，并识别相关的目标物体和目标位置。这些空间锚点随后作为结构化视觉提示（如十字准星和边界框）直接叠加在视觉观测上。在这些提示的引导下，并通过训练过程中引入的一种新型辅助视觉定位目标进行增强，“系统 1 控制器”能够可靠地生成精确的底层执行动作。在 Robocasa-GR1-Tabletop 基准测试和 SimplerEnv 仿真中的实验表明，VP-VLA 将成功率分别提高了 5% 和 8.3%，超越了包括 QwenOFT 和 GR00T-N1.6 在内的竞争基准模型。

## Abstract
Vision-Language-Action (VLA) models typically map visual observations and linguistic instructions directly to robotic control signals. This "black-box" mapping forces a single forward pass to simultaneously handle instruction interpretation, spatial grounding, and low-level control, often leading to poor spatial precision and limited robustness in out-of-distribution scenarios. To address these limitations, we propose VP-VLA, a dual-system framework that decouples high-level reasoning and low-level execution via a structured visual prompting interface. Specifically, a "System 2 Planner" decomposes complex instructions into sub-tasks and identifies relevant target objects and goal locations. These spatial anchors are then overlaid directly onto visual observations as structured visual prompts, such as crosshairs and bounding boxes. Guided by these prompts and enhanced by a novel auxiliary visual grounding objective during training, a "System 1 Controller" reliably generates precise low-level execution motions. Experiments on the Robocasa-GR1-Tabletop benchmark and SimplerEnv simulation demonstrate that VP-VLA improves success rates by 5% and 8.3%, surpassing competitive baselines including QwenOFT and GR00T-N1.6.

---

## 论文详细总结（自动生成）

这是一份关于论文《VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
传统的视觉-语言-动作（VLA）模型通常采用“端到端黑盒”架构，将视觉观测和文本指令直接映射为控制信号。这种方式存在三大缺陷：
*   **空间精度差**：模型难以在单次前向传播中同时完成高层语义理解和底层精确空间定位。
*   **鲁棒性不足**：模型容易过拟合训练集的场景分布，在面对新物体或新位置（OOD 场景）时表现脆弱。
*   **推理与执行耦合**：难以处理多阶段复杂任务，且无法有效利用预训练视觉语言模型（VLM）的强大推理能力。

**核心含义**：本文提出通过“视觉提示（Visual Prompting）”作为中介接口，将高层规划与底层执行解耦，从而提升机器人的操作精度和泛化能力。

### 2. 方法论：双系统架构
VP-VLA 借鉴了认知科学中的双系统理论：
*   **系统 2 规划器（System 2 Planner）**：
    *   **任务分解**：利用预训练 VLM（如 Qwen3-VL）将复杂指令分解为原子子任务（如“抓取”、“放置”）。
    *   **事件驱动触发**：规划器并非逐帧运行，而是由“事件”（如夹持器状态改变）触发，降低计算开销。
    *   **视觉提示生成**：识别目标物体和位置，利用分割模型（SAM3）生成结构化视觉提示——**十字准星（Crosshair）**表示交互锚点，**边界框（Bounding Box）**表示放置约束，并将其叠加在原始图像上。
*   **系统 1 控制器（System 1 Controller）**：
    *   **输入**：接收原始图像、视觉提示图像和文本指令。
    *   **辅助定位目标（Grounding Loss）**：在训练时引入辅助任务，要求模型预测视觉提示的坐标（离散化为 1000 个 bin），强制模型内化空间感知，而非将其视为噪声。
    *   **动作生成**：输出连续的动作序列（Action Chunking）。

### 3. 实验设计
*   **仿真基准测试**：
    *   **Robocasa-GR1-Tabletop**：包含 24 个复杂的厨房操作任务，涉及多步交互。
    *   **SimplerEnv**：基于 BridgeDataV2 和 Fractal 数据集，评估模型在不同环境下的泛化性。
*   **真实世界场景**：
    *   使用 Franka Research 3 机械臂，设计了三个实验套件：垃圾分类（语义理解）、按属性引用物体（颜色识别）、精确位置放置（空间坐标定位）。
*   **对比方法**：包括 QwenOFT、GR00T-N1.6、$\pi_0$、$\pi_{0.5}$、OpenVLA 等当前最先进的 VLA 模型。

### 4. 资源与算力
*   **硬件**：实验在 **8 张 GPU** 上进行（具体型号未标明，但根据 VLA 训练惯例推测为 A100 或 H100 级别）。
*   **训练细节**：
    *   在 SimplerEnv 任务中微调了 70k 步。
    *   使用 AdamW 优化器，VLM 学习率为 1e-5，动作模型学习率为 1e-4。
    *   Batch Size 为 256（真实世界微调）或 32/每卡（仿真）。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验涵盖了数万个视频样本，每个任务进行 50 次独立试验以确保统计显著性。
*   **消融实验**：针对辅助定位损失、视觉提示类型（点 vs 十字准星）、提示叠加方式、任务分解必要性等进行了详细对比。
*   **客观性**：实验不仅测试了分布内（ID）性能，还重点评估了新物体、新位置的分布外（OOD）性能，证明了方法的泛化优势。整体实验设计严谨，对比基准均为目前领域内的 SOTA 模型。

### 6. 主要结论与发现
*   **性能提升**：在 Robocasa 上平均成功率提升 **5%**，在 SimplerEnv 上提升 **8.3%**。
*   **解耦的价值**：通过视觉提示接口，模型不再需要从模糊的文本中猜测空间位置，而是直接“追踪”视觉锚点，显著降低了操作误差。
*   **OOD 鲁棒性**：在面对训练集中未见过的物体颜色或网格位置时，VP-VLA 的性能下降远小于基准模型，表现出极强的空间泛化能力。

### 7. 优点（亮点）
*   **结构化接口**：将抽象的语言指令转化为具体的几何提示，降低了底层策略的学习难度。
*   **事件驱动机制**：巧妙地利用夹持器状态作为触发点，平衡了高层推理的深度与底层控制的实时性。
*   **辅助任务增强**：引入 Grounding Loss 解决了视觉提示可能被模型忽略的问题，确保了提示的有效性。

### 8. 不足与局限
*   **依赖外部模型**：系统 2 的表现高度依赖于 VLM 规划器和分割模型（SAM3）的准确性，若前端定位错误，后端执行将失效。
*   **推理延迟**：虽然采用了事件驱动，但调用大型 VLM 生成提示仍会引入额外的计算延迟，可能影响极高频动作的实时响应。
*   **场景局限**：目前主要集中在桌面级操作（Tabletop），对于更复杂的全身移动或动态环境下的避障操作尚待验证。

（完）
