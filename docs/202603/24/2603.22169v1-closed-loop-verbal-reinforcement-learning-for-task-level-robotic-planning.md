---
title: Closed-Loop Verbal Reinforcement Learning for Task-Level Robotic Planning
title_zh: 面向任务级机器人规划的闭环言语强化学习
authors: "Dmitrii Plotnikov, Iaroslav Kolomiets, Dmitrii Maliukov, Dmitrij Kosenkov, Daniia Zinniatullina, Artem Trandofilov, Georgii Gazaryan, Kirill Bogatikov, Timofei Kozlov, Igor Duchinskii, Mikhail Konenkov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22169v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 结合VLM和LLM的机器人任务级规划语言强化学习
tldr: 针对移动机器人在不确定环境下的任务规划，本文提出了一种闭环言语强化学习（VRL）框架。该框架利用大语言模型（LLM）作为执行者生成行为树，并通过视觉语言模型（VLM）作为评论者提供自然语言反馈，实现策略的迭代优化。该方法无需梯度优化，具有高可解释性和透明度，在真实机器人的多阶段任务中验证了其应对执行失败和闭环自适应的能力。
selection_source: fresh_fetch
motivation: 旨在解决传统机器人强化学习在复杂任务规划中缺乏可解释性以及难以应对执行不确定性的问题。
method: 采用闭环架构，由LLM生成行为树，并利用VLM根据执行轨迹提供结构化自然语言反馈进行符号层面的策略迭代。
result: 实验证明该框架能在真实移动机器人上实现可解释的策略改进，并能有效应对多阶段任务中的执行失败。
conclusion: 言语强化学习为实现透明、可解释且具备自适应能力的机器人任务规划提供了一种有效的非梯度优化方案。
---

## 摘要
我们提出了一种新的言语强化学习（VRL）框架，用于在执行不确定性下运行的移动机器人系统中进行可解释的任务级规划。该框架采用闭环架构，通过与物理环境的交互实现策略的迭代改进。在我们的框架中，可执行的行为树由大语言模型（LLM）执行器（actor）进行反复优化，该执行器使用由视觉语言模型（VLM）评论器（critic）生成的结构化自然语言反馈，而该评论器负责观察物理机器人和执行轨迹。与传统的强化学习不同，VRL 中的策略更新直接发生在符号规划层面，无需基于梯度的优化。这实现了透明的推理、显式的因果反馈以及人类可解释的策略演化。我们在执行不确定性下执行多阶段操作和导航任务的真实移动机器人上验证了所提出的框架。实验结果表明，该框架支持可解释的策略改进、对执行失败的闭环自适应以及在物理机器人系统上的可靠部署。

## Abstract
We propose a new Verbal Reinforcement Learning (VRL) framework for interpretable task-level planning in mobile robotic systems operating under execution uncertainty. The framework follows a closed-loop architecture that enables iterative policy improvement through interaction with the physical environment. In our framework, executable Behavior Trees are repeatedly refined by a Large Language Model actor using structured natural-language feedback produced by a Vision-Language Model critic that observes the physical robot and execution traces. Unlike conventional reinforcement learning, policy updates in VRL occur directly at the symbolic planning level, without gradient-based optimization. This enables transparent reasoning, explicit causal feedback, and human-interpretable policy evolution. We validate the proposed framework on a real mobile robot performing a multi-stage manipulation and navigation task under execution uncertainty. Experimental results show that the framework supports explainable policy improvements, closed-loop adaptation to execution failures, and reliable deployment on physical robotic systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**言语强化学习（Verbal Reinforcement Learning, VRL）**的框架，旨在解决移动机器人在现实不确定环境下进行任务级规划的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的机器人任务规划（如行为树 BT）通常需要大量人工调试来应对环境不确定性（如定位误差、操作失败）。而传统的强化学习（RL）虽然能自主改进，但其策略通常是“黑盒”式的（亚符号空间），缺乏可解释性且难以在真实硬件上直接优化。
*   **核心问题**：如何构建一个既能像 RL 一样通过闭环反馈自主进化，又具备高度可解释性、能直接在物理机器人上部署的任务级规划系统？
*   **整体含义**：论文提出将大语言模型（LLM）作为“执行者”修改符号化的行为树，将视觉语言模型（VLM）作为“评论者”提供自然语言形式的诊断反馈，从而实现无需梯度的闭环策略优化。

### 2. 方法论
*   **核心思想**：将策略表示为**可执行的行为树（Behavior Trees, BTs）**，通过“执行-观察-反馈-修正”的循环进行迭代。
*   **关键技术组件**：
    *   **策略表示**：使用 BT 保证模块化和透明度。LLM 并不直接控制电机，而是修改 BT 的结构和参数。
    *   **LLM 执行者（Actor）**：接收任务定义、环境规范、BT 节点库和来自评论者的反馈，生成改进后的 BT 代码。
    *   **VLM 评论者（Critic）**：观察机器人执行过程中的视觉快照和 BT 执行轨迹（Trace）。它不输出标量奖励，而是输出结构化的自然语言反馈、警报分数（$s \in [0, 1]$）和置信度（$c \in [0, 1]$）。
*   **算法流程（VRL 闭环）**：
    1.  执行当前 BT。
    2.  VLM 评论者在执行前、中、后期进行多阶段评估。
    3.  如果警报分数过高，通知人工干预；否则，将反馈和得分传给 LLM 执行者。
    4.  LLM 执行者根据反馈更新 BT，进入下一轮迭代。

### 3. 实验设计
*   **实验场景**：一个真实的仓库物流任务。机器人需要从装载区收集木块（需识别颜色并判断正反面），将其运输并放置到卸载区，同时还需要移动一个货架。
*   **对比方法（Benchmark）**：
    1.  **无评论者（Baseline）**：仅依靠 BT 轨迹和最终得分进行更新。
    2.  **Qwen2.5-VL-7B**：使用通用大模型作为评论者。
    3.  **Qwen2.5-VL-3B（微调版）**：在结构化执行反馈数据集上经过微调的小模型。
    4.  **Gemini 3 Pro Preview**：高性能多模态模型作为评论者。
    5.  **Gemini 3 Pro + 符号化信息**：在反馈中额外加入木块颜色/方向的感知数据（消融实验）。
*   **评估指标**：任务得分（基于完成质量和时间）、评论者准确率（IDR）、置信度、人工警报率。

### 4. 资源与算力
*   **硬件平台**：定制的三轮全向移动机器人，配备真空吸盘、LiDAR、Intel NUC 机载电脑和 STM32 控制器。
*   **算力说明**：
    *   论文提到对 **Qwen2.5-VL-3B** 进行了针对性微调，以使其输出更符合结构化反馈要求。
    *   **未明确说明**具体的 GPU 型号、训练时长或微调所消耗的具体算力资源。推理过程涉及 API 调用（Gemini）或本地模型运行。

### 5. 实验数量与充分性
*   **实验规模**：针对每种配置，在 **5 种不同的场地布局**下进行了测试，每种布局进行 **10 轮迭代**（共计 50 轮/配置）。
*   **充分性**：
    *   实验涵盖了从感知（VLM 准确性）到决策（LLM 改进效果）的全链路评估。
    *   包含了消融实验（如加入符号化感知信息），验证了感知瓶颈对 VRL 的影响。
    *   **客观性**：在真实物理机器人上进行，而非纯仿真，这大大增强了结论的说服力。

### 6. 主要结论与发现
*   **反馈质量胜过模型规模**：微调后的 3B 模型表现优于未微调的 7B 模型，说明领域适配（对执行失败的因果诊断）比通用视觉能力更重要。
*   **结构化反馈的必要性**：仅靠数值奖励（无评论者模式）几乎无法实现有效的策略改进，因为 LLM 无法区分是“规划错误”还是“硬件执行随机误差”。
*   **感知是主要瓶颈**：引入额外的符号化感知信息（BlockInfo）能显著提升学习稳定性和最终得分。
*   **协同进化**：LLM 执行者会逐渐“适应”评论者的局限性，例如当评论者无法可靠识别颜色时，执行者会选择更保守的策略。

### 7. 优点
*   **高可解释性**：所有的策略更新和失败诊断都以自然语言和 BT 结构呈现，人类可以轻松理解机器人为什么要修改策略。
*   **无需模拟器**：直接在物理环境中小样本学习，避开了复杂的 Sim-to-Real 鸿沟。
*   **闭环自适应**：通过 VLM 引入了实时反馈，解决了以往 LLM 规划器“开环运行、不撞南墙不回头”的问题。

### 8. 不足与局限
*   **时间跨度限制**：目前的评论者基于快照，难以诊断具有长程依赖关系的潜在故障（即当前错误在很久之后才显现）。
*   **感知依赖**：系统的上限受限于 VLM 的视觉识别精度。如果 VLM 误判了环境状态，LLM 可能会做出错误的策略修正。
*   **实时性与成本**：依赖大型多模态模型（尤其是 API 调用）可能带来较高的推理延迟和运行成本，限制了其在高速动态环境下的应用。

（完）
