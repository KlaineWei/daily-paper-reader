---
title: "Rewarding DINO: Predicting Dense Rewards with Vision Foundation Models"
title_zh: Rewarding DINO：利用视觉基础模型预测稠密奖励
authors: "Pierre Krack, Tobias Jülg, Wolfram Burgard, Florian Walter"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16978v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 利用视觉模型进行机器人操作的稠密奖励预测
tldr: Rewarding DINO 是一种基于视觉基础模型的语言条件奖励建模方法，旨在解决机器人操作中稠密奖励函数设计难的问题。该方法通过排序损失在 Meta-World+ 任务上训练，能够从图像中推断任务进度而非仅仅模仿专家轨迹。其模型轻量且具有良好的泛化能力，在仿真和真实世界中均表现出色，可直接作为强化学习的奖励函数。
selection_source: fresh_fetch
motivation: 传统的稠密奖励设计依赖仿真特权信息，而基于演示的奖励模型往往受限于特定轨迹且泛化性差。
method: 提出 Rewarding DINO，利用视觉基础模型和排序损失函数，学习语言条件的稠密奖励预测模型。
result: 模型在 Meta-World+ 任务中表现优异，展现出强大的跨场景泛化能力，并成功应用于强化学习任务。
conclusion: Rewarding DINO 证明了视觉基础模型能有效学习任务语义并提供可靠的稠密奖励，是替代解析式奖励函数的有效方案。
---

## 摘要
在机器人操纵中，设计良好的稠密奖励函数不仅能指示任务是否完成，还能编码过程中的进度。通常，设计稠密奖励具有挑战性，且通常需要访问仅在仿真中可用、而在现实世界实验中无法获取的特权状态信息。这使得从摄像头图像推断任务状态信息的奖励预测模型变得极具吸引力。一种常见的方法是基于视觉相似性或序列帧排序，从专家演示中预测奖励。然而，这会使生成的奖励函数偏向于特定的解决方案，并导致其在演示未覆盖的状态下定义不明。在这项工作中，我们介绍了 Rewarding DINO，这是一种语言调节的奖励建模方法，它学习的是实际的奖励函数而非特定的轨迹。该模型紧凑的尺寸使其能够以较低的计算开销直接替代解析奖励函数。我们使用基于排名的损失函数，在从 24 个 Meta-World+ 任务中采样的数据上训练模型，并评估了两两准确度、排名相关性和校准度。Rewarding DINO 在训练集任务中取得了具有竞争力的性能，并能泛化到仿真和现实世界中的新设置，这表明它学习到了任务语义。我们还使用现成的强化学习算法测试了该模型，以解决 Meta-World+ 训练集中的任务。

## Abstract
Well-designed dense reward functions in robot manipulation not only indicate whether a task is completed but also encode progress along the way. Generally, designing dense rewards is challenging and usually requires access to privileged state information available only in simulation, not in real-world experiments. This makes reward prediction models that infer task state information from camera images attractive. A common approach is to predict rewards from expert demonstrations based on visual similarity or sequential frame ordering. However, this biases the resulting reward function towards a specific solution and leaves it undefined in states not covered by the demonstrations. In this work, we introduce Rewarding DINO, a method for language-conditioned reward modeling that learns actual reward functions rather than specific trajectories. The model's compact size allows it to serve as a direct replacement for analytical reward functions with comparatively low computational overhead. We train our model on data sampled from 24 Meta-World+ tasks using a rank-based loss and evaluate pairwise accuracy, rank correlation, and calibration. Rewarding DINO achieves competitive performance in tasks from the training set and generalizes to new settings in simulation and the real world, indicating that it learns task semantics. We also test the model with off-the-shelf reinforcement learning algorithms to solve tasks from our Meta-World+ training set.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rewarding DINO: Predicting Dense Rewards with Vision Foundation Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在没有仿真环境“特权信息”（如物体精确坐标）的情况下，仅通过视觉图像为机器人操作任务提供高质量的**稠密奖励（Dense Rewards）**？
*   **研究动机**：
    *   **设计困难**：手动设计稠密奖励函数极具挑战，且通常依赖于现实中无法获取的状态信息。
    *   **现有方法局限**：目前的视觉奖励模型多基于“专家演示”的相似性或时间顺序，这导致模型过度拟合特定轨迹，在强化学习（RL）初期的随机探索阶段表现不佳。
    *   **目标**：利用预训练的视觉基础模型（VFM）学习任务的语义本质，从而预测一个通用的、不依赖特定解法的稠密奖励函数。

### 2. 方法论
*   **核心思想**：将奖励预测建模为一个**成对偏好学习（Pairwise Preference Learning）**问题，利用冻结的视觉和语言编码器提取特征，通过轻量级头部预测标量奖励。
*   **关键技术细节**：
    *   **模型架构**：
        *   **视觉编码器**：使用冻结的 **DINOv3 (ViT-S/16)**，侧重于提取稠密的特征表示。
        *   **语言编码器**：使用冻结的 **all-MiniLM-L6-v2 (SBERT)** 处理自然语言任务描述。
        *   **特征融合**：采用 **FiLM (Feature-wise Linear Modulation)** 层，将语言嵌入作为条件来调制图像特征。
        *   **输入**：双视角图像（减少遮挡）+ 任务文本。
    *   **损失函数**：采用 **RankNet 风格的成对逻辑损失（Pairwise Logistic Loss）**：
        $$\ell(\Delta s) = \log(1 + \exp(-y \Delta s / \tau))$$
        其中 $y$ 是基于地面真值奖励的排序标签，$\Delta s$ 是预测分数的差值。这种方法对奖励的绝对缩放不敏感，只关注状态的优劣排序。
    *   **数据处理**：通过对笛卡尔坐标和奖励空间进行分箱（Binning）来去重，确保训练数据的覆盖范围和多样性。

### 3. 实验设计
*   **数据集/场景**：
    *   **Meta-World+**：使用了 24 个操纵任务（如开门、拨动开关、压手柄等）。
    *   **Pick-Cube**：自定义的仿真与真实世界拾取任务，用于验证 Sim-to-Real 泛化性。
*   **Benchmark 与对比**：
    *   **评估指标**：成对准确度（Pairwise Accuracy）、肯德尔等级相关系数（Kendall’s Tau）、预期校准误差（ECE）。
    *   **对比方法**：在强化学习实验中，对比了**解析式稠密奖励（Ground Truth）**和**稀疏奖励（Sparse Rewards）**。
*   **强化学习应用**：使用 PPO 算法，并将预测奖励通过**基于势能的奖励塑造（PBRS）**引入，以保证最优策略的不变性。

### 4. 资源与算力
*   **算力支持**：文中提到使用了埃尔朗根国家高性能计算中心（NHR@FAU）的资源。
*   **具体细节**：
    *   **训练参数**：Batch Size 为 512，训练 3000 个 Epoch（每个 Epoch 包含 10 万对样本）。
    *   **模型规模**：视觉骨干网络为 21M 参数的 ViT-S，奖励预测头部非常轻量（MLP 层级为 4096-512-64-8）。
    *   **硬件型号**：文中未明确列出具体的 GPU 型号和总训练时长，但强调了模型的小型化使其具有较低的推理开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖 24 个不同类型的任务。
    *   数据集包含 770 条轨迹，去重后约 16.9 万个步骤，涉及超过 40 亿对图像对比。
*   **充分性与客观性**：
    *   **多维度评估**：不仅测试了准确率，还深入分析了概率校准度（Calibration）和等级相关性。
    *   **泛化性测试**：包括了提示词变体（Prompt Variation）、未见任务（Task Variation）以及 Sim-to-Real 的定性分析。
    *   **闭环验证**：通过实际训练 RL 策略来验证奖励模型的有效性，而非仅仅停留在离线指标上。实验设计较为全面且公平。

### 6. 主要结论与发现
*   **视觉模型潜力**：预训练的 DINOv3 特征足以支持高精度的稠密奖励预测，甚至能区分时间间隔极短（1/80秒）的状态优劣。
*   **校准性**：经过校准后，模型预测的得分差值可以很好地转化为“状态 A 优于状态 B”的概率。
*   **泛化能力**：模型能够理解任务语义（例如从“开抽屉”泛化到“关抽屉”），并能在真实世界机器人上产生合理的奖励曲线。
*   **RL 增益**：在多个任务中，使用 Rewarding DINO 引导的 PPO 算法能够达到与解析式稠密奖励相近的成功率，远优于稀疏奖励。

### 7. 优点
*   **轻量高效**：模型体积小，可作为解析奖励函数的直接替代品，推理延迟低。
*   **不依赖演示**：通过学习奖励函数而非模仿轨迹，解决了 RL 初期探索阶段奖励未定义的问题。
*   **理论完备**：结合 PBRS 框架，为视觉奖励模型在强化学习中的应用提供了理论保障。
*   **语言条件化**：支持自然语言指令，增强了任务的灵活性和交互性。

### 8. 不足与局限
*   **非马尔可夫风险**：由于图像是部分观测（Partial Observation），模型可能对视觉相似但物理状态不同的场景产生误判（文中提到增加记忆单元或处理整个轨迹可能解决此问题）。
*   **成功检测缺失**：模型未专门针对“任务完成”这一离散状态进行优化，可能在任务终点附近的判定不够精确。
*   **任务复杂度限制**：目前的实验主要集中在桌面操纵任务，对于更复杂、长程或涉及多物体交互的任务，其表现尚待验证。
*   **Sim-to-Real 深度不足**：真实世界实验主要停留在定性观察（奖励曲线可视化），缺乏大规模的真实世界 RL 训练数据。

（完）
