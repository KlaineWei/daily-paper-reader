---
title: Causal Direct Preference Optimization for Distributionally Robust Generative Recommendation
title_zh: 面向分布鲁棒生成式推荐的因果直接偏好优化
authors: "Chu Zhao, Enneng Yang, Jianzhe Zhao, Guibing Guo"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.22335v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于生成式推荐LLM的因果直接偏好优化
tldr: 针对大语言模型生成式推荐中直接偏好优化（DPO）易受环境混杂因素干扰、导致分布外（OOD）泛化能力差的问题，本文提出CausalDPO。该方法结合因果不变性学习，通过后门调整消除混杂干扰，并利用软聚类建模潜在环境分布，显著提升了模型在多样化环境下的鲁棒性和推荐性能。
selection_source: fresh_fetch
motivation: 传统的DPO在对齐过程中会放大环境混杂因素导致的伪相关，削弱了生成式推荐模型在分布外场景下的泛化能力。
method: 提出CausalDPO，引入因果后门调整策略和软聚类环境建模，通过不变性约束捕获用户跨环境的稳定偏好。
result: "在四种代表性的分布偏移设置下，CausalDPO在四项评估指标上平均实现了17.17%的性能提升。"
conclusion: CausalDPO能有效消除环境干扰并提取稳定偏好结构，显著增强了基于LLM的推荐系统在复杂分布偏移下的鲁棒性。
---

## 摘要
直接偏好优化 (DPO) 通过最小化偏好对齐损失，引导大语言模型 (LLM) 生成与用户历史行为分布一致的推荐。然而，我们的系统性实证研究和理论分析表明，DPO 在对齐过程中往往会放大由环境混杂因素引起的虚假相关性，从而显著削弱了基于 LLM 的生成式推荐方法在分布外 (OOD) 场景下的泛化能力。为了缓解这一问题，我们提出了 CausalDPO，这是 DPO 的一种扩展，它结合了因果不变性学习机制。该方法在偏好对齐阶段引入了后门调整策略以消除环境混杂因素的干扰，利用软聚类方法对潜在环境分布进行显式建模，并通过不变性约束增强了不同环境下的鲁棒一致性。理论分析表明，CausalDPO 能够有效捕捉用户在多个环境中的稳定偏好结构，从而提高基于 LLM 的推荐模型的 OOD 泛化性能。我们在四种代表性的分布偏移设置下进行了广泛的实验以验证 CausalDPO 的有效性，在四项评估指标上实现了平均 17.17% 的性能提升。

## Abstract
Direct Preference Optimization (DPO) guides large language models (LLMs) to generate recommendations aligned with user historical behavior distributions by minimizing preference alignment loss. However, our systematic empirical research and theoretical analysis reveal that DPO tends to amplify spurious correlations caused by environmental confounders during the alignment process, significantly undermining the generalization capability of LLM-based generative recommendation methods in out of distribution (OOD) scenarios. To mitigate this issue, we propose CausalDPO, an extension of DPO that incorporates a causal invariance learning mechanism. This method introduces a backdoor adjustment strategy during the preference alignment phase to eliminate interference from environmental confounders, explicitly models the latent environmental distribution using a soft clustering approach, and enhances robust consistency across diverse environments through invariance constraints. Theoretical analysis demonstrates that CausalDPO can effectively capture users stable preference structures across multiple environments, thereby improving the OOD generalization performance of LLM-based recommendation models. We conduct extensive experiments under four representative distribution shift settings to validate the effectiveness of CausalDPO, achieving an average performance improvement of 17.17% across four evaluation metrics.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：CausalDPO

#### 1. 核心问题与整体含义（研究动机和背景）
本文关注的是**基于大语言模型（LLM）的生成式推荐系统在分布外（OOD）场景下的泛化问题**。
*   **背景**：目前主流方法通过直接偏好优化（DPO）将 LLM 的输出与用户历史行为对齐。
*   **核心问题**：作者通过实证和理论分析发现，训练数据中普遍存在“环境混杂因素”（如流行度偏差、时间漂移、政策变化等）。DPO 在对齐过程中不仅会学习这些虚假相关性（Spurious Correlations），甚至会**放大**这些偏差。例如，模型可能将“疫情期间对医疗用品的普遍需求”误认为是个人的稳定偏好。当测试环境发生变化时，这种依赖虚假特征的模型性能会大幅下降。

#### 2. 方法论：CausalDPO
为了解决上述问题，论文提出了 **CausalDPO**，其核心思想是结合**因果不变性学习**来消除环境干扰。
*   **因果建模**：构建结构因果模型（SCM），分析环境 $E$ 如何影响输入 $X$ 和输出 $Y$。通过因果干预中的“后门调整”策略，切断环境对输入的直接影响。
*   **潜在环境发现（软聚类）**：由于现实中环境标签通常不可见，CausalDPO 使用 **DBSCAN 聚类**结合 **Softmax 映射**，从 LLM 的隐藏层表示中隐式地推断“伪环境”分布。
*   **不变性约束（MMD 损失）**：引入**最大均值差异（MMD）**作为正则项。其目标是最小化不同伪环境之间模型输出分布的差异，迫使模型忽略环境特有的噪声，转而捕捉跨环境稳定的、真实的因果偏好结构。
*   **优化目标**：最终损失函数由标准的 DPO 损失和 MMD 不变性正则项加权组成：$L = L_{DPO} + \lambda \cdot L_{MMD}$。

#### 3. 实验设计
*   **数据集与场景**：在三个标准基准上模拟了四种分布偏移（OOD）场景：
    1.  **流行度偏移（Popularity Shift）**：Yelp2018 数据集。
    2.  **时间偏移（Temporal Shift）**：Movielens-10M 数据集。
    3.  **曝光偏移（Exposure Shift）**：Book-Crossing 数据集（通过矩阵分解补全模拟全曝光）。
    4.  **混合偏移（Mixed Shift）**：结合流行度和时间偏移的复杂场景。
*   **Benchmark（对比方法）**：
    *   传统模型：SASRec。
    *   SFT 增强模型：BIGRec, RW, D3。
    *   DPO 变体：DMPO, SDPO, RosePO, SPRec（目前最强的基准之一）。
*   **评估指标**：HR@10/20 和 NDCG@10/20。

#### 4. 资源与算力
*   **硬件配置**：实验使用了 **8 张 NVIDIA A800 GPU (80GB VRAM)**。
*   **模型规模**：主要基于 **Llama-3.1-8B**，同时也测试了 Qwen2.5-7B 和 Qwen3-8B。
*   **训练时长**：以 Book-Crossing 数据集为例，CausalDPO 每轮（epoch）训练耗时约 **2971 秒**（约 50 分钟），相比标准 DPO（2482 秒）增加了约 19.7% 的计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 3 个数据集、4 种 OOD 场景、1 种 IID 场景，并对比了 8 种基准方法。
*   **消融实验**：针对 SFT 阶段、DPO 阶段以及因果模块进行了详细的消融分析。
*   **超参数分析**：探讨了迭代次数、负样本数量、$\beta$ 值以及 $\lambda$ 值对性能的影响。
*   **充分性评价**：实验设计非常全面且具有针对性，不仅验证了性能提升，还通过可视化手段展示了聚类环境与真实偏移因素（如流行度分层）之间的关联，论证过程客观且严谨。

#### 6. 主要结论与发现
*   **性能提升**：CausalDPO 在四种 OOD 场景下平均性能提升了 **17.17%**。
*   **鲁棒性**：该方法能有效缓解 DPO 放大虚假相关性的缺陷，使模型在长尾推荐（冷门物品）和未来时间预测上表现更稳定。
*   **通用性**：将 CausalDPO 的因果正则项集成到其他 DPO 变体（如 SimPO, CPO）中，也能带来显著的 OOD 性能增益。
*   **理论证实**：通过数学证明展示了 CausalDPO 的泛化误差界是可控的，且能诱导模型满足不变性和充分性条件。

#### 7. 优点
*   **理论与实证结合**：不仅提出了算法，还从理论上证明了 DPO 放大偏差的机制以及新方法的有效性。
*   **无需显式标签**：通过软聚类自动发现潜在环境，解决了现实推荐场景中环境因素难以标注的痛点。
*   **即插即用**：作为一种正则化策略，它可以轻松集成到现有的各种偏好优化框架中。

#### 8. 不足与局限
*   **计算复杂度**：引入 MMD 损失导致计算复杂度增加了 $O(B^2)$（$B$ 为 Batch Size），在大规模 Batch 训练时可能会带来更高的显存和时间压力。
*   **聚类依赖性**：模型的表现高度依赖于软聚类模块能否准确识别潜在的环境结构，若数据特征极其稀疏或噪声过大，聚类失效可能影响效果。
*   **实验覆盖**：虽然涵盖了主流偏移，但对于更极端的“概念漂移”（如用户兴趣完全反转）的讨论较少。

（完）
