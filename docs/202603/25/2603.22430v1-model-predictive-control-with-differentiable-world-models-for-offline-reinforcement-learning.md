---
title: Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning
title_zh: 基于可微世界模型的离线强化学习模型预测控制
authors: "Rohan Deb, Stephen J. Wright, Arindam Banerjee"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22430v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 结合可微世界模型与模型预测控制的离线强化学习
tldr: 本文针对离线强化学习在推理阶段策略固定的局限性，提出了一种结合可微世界模型（DWM）的模型预测控制（MPC）框架。该框架利用预训练策略和学习到的动力学模型，在推理时通过想象轨迹的端到端梯度计算来实时优化策略参数。在D4RL基准测试上的实验表明，这种推理时自适应机制能显著提升离线RL算法的性能。
selection_source: fresh_fetch
motivation: 传统的离线强化学习方法在推理阶段缺乏灵活性，无法利用环境反馈或模型预测来动态调整已学习的策略。
method: 构建了一个可微世界模型流水线，通过MPC在推理时对想象的轨迹进行端到端梯度优化，从而实现策略参数的即时微调。
result: 在D4RL的MuJoCo运动任务和AntMaze任务中，该方法均表现出优于现有强离线强化学习基线的性能。
conclusion: 通过可微世界模型在推理阶段利用实时信息优化策略，是增强离线强化学习效果的一种有效且一致的手段。
---

## 摘要
离线强化学习（RL）旨在从固定的离线数据集中学习最优策略，而无需与环境进行进一步交互。此类方法通常训练一个离线策略（或价值函数），并在推理阶段直接应用，而不进行进一步的优化。我们引入了一种受模型预测控制（MPC）启发的推理阶段自适应框架，该框架利用预训练策略以及学习到的状态转移和奖励的世界模型。虽然现有的世界模型和扩散规划方法在训练期间使用学习到的动力学来生成想象轨迹，或在推理阶段采样候选规划，但它们并未利用推理阶段的信息来实时优化策略参数。相比之下，我们的设计是一个可微世界模型（DWM）流水线，它能够通过想象展开（imagined rollouts）进行端到端梯度计算，从而在推理阶段基于 MPC 进行策略优化。我们在 D4RL 连续控制基准测试（MuJoCo 运动任务和 AntMaze）上评估了我们的算法，结果表明，利用推理阶段信息来优化策略参数，相比强大的离线强化学习基准方法，能够获得持续的性能提升。

## Abstract
Offline Reinforcement Learning (RL) aims to learn optimal policies from fixed offline datasets, without further interactions with the environment. Such methods train an offline policy (or value function), and apply it at inference time without further refinement. We introduce an inference time adaptation framework inspired by model predictive control (MPC) that utilizes a pretrained policy along with a learned world model of state transitions and rewards. While existing world model and diffusion-planning methods use learned dynamics to generate imagined trajectories during training, or to sample candidate plans at inference time, they do not use inference-time information to optimize the policy parameters on the fly. In contrast, our design is a Differentiable World Model (DWM) pipeline that enables endto-end gradient computation through imagined rollouts for policy optimization at inference time based on MPC. We evaluate our algorithm on D4RL continuous-control benchmarks (MuJoCo locomotion tasks and AntMaze), and show that exploiting inference-time information to optimize the policy parameters yields consistent gains over strong offline RL baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MPCwDWM**（基于可微世界模型的模型预测控制）的新框架，旨在提升离线强化学习（Offline RL）在推理阶段的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的离线 RL 方法在训练完成后，推理阶段通常直接执行固定的策略网络，无法根据推理时遇到的具体状态进行动态调整。
*   **研究动机**：
    *   **Q值估计难题**：在离线场景下，准确估计长时程的 Q 值非常困难，因为这涉及对分布外（OOD）动作的泛化。
    *   **局部动力学的优势**：相比于全局价值函数，单步状态转移动力学 $P(s'|s, a)$ 和奖励函数 $r(s, a)$ 是局部对象，更容易从离线数据中准确学习。
    *   **推理时计算的潜力**：借鉴大语言模型（LLM）通过推理时计算（如搜索、思维链）提升性能的思路，探索离线 RL 是否也能通过推理时的策略优化获得增益。

### 2. 方法论：MPCwDWM 框架
*   **核心思想**：构建一个**端到端可微**的世界模型流水线，在推理阶段利用模型预测控制（MPC）的思想，通过在“想象”的轨迹中进行梯度反向传播，实时微调策略参数。
*   **关键技术细节**：
    1.  **可微扩散采样器 ($f_\theta$)**：使用条件扩散概率模型作为动力学模型。通过将扩散模型的反向去噪过程表示为确定性计算图，使得生成的下一状态对输入状态和动作是可微的。
    2.  **可微奖励模型 ($r_\xi$)**：通过监督学习训练一个回归模型来预测即时奖励。
    3.  **终端价值函数 ($Q_\phi$)**：利用预训练的离线 RL 算法（如 ReBRAC）提供的 Critic 网络作为有限时程展开后的终端价值估计。
*   **算法流程**：
    *   在每个真实环境步 $t$，从当前状态 $s_t$ 出发，利用当前策略 $\pi_\psi$ 和扩散模型 $f_\theta$ 展开 $H$ 步想象轨迹。
    *   计算代理目标函数（累积奖励 + 终端 Q 值）。
    *   **推理时优化**：通过反向传播计算梯度 $\nabla_\psi J$，执行 $E$ 次梯度上升步骤来更新策略参数 $\psi$。
    *   执行更新后策略产生的第一步动作，然后进入下一环境状态。

### 3. 实验设计
*   **数据集/场景**：使用了标准的 **D4RL** 连续控制基准测试。
    *   **Gym-MuJoCo 运动任务**：包含 HalfCheetah、Hopper、Walker2d 的 18 个子任务（Random, Medium, Expert 等）。
    *   **AntMaze 任务**：包含 6 个更具挑战性的迷宫任务（u-maze, medium, large）。
*   **对比基准 (Benchmarks)**：
    *   **强离线 RL 基线**：TD3+BC, IQL, CQL, SAC-RND, ReBRAC。
    *   **生成式/模型化方法**：Decision Transformer (DT), Trajectory Transformer (TT), MOPO, Diffuser 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练/推理的总时长。
*   **潜在开销分析**：由于需要在推理的每一步都进行多次扩散模型的反向传播和梯度更新，该方法的推理延迟（Inference Latency）显著高于传统的单次前向传播策略。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 24 个不同的 D4RL 数据集，每个任务使用了 **10 个不同的随机种子**进行评估。
*   **充分性评价**：
    *   **对比广泛**：不仅对比了传统的判别式离线 RL，还对比了最新的生成式和基于扩散模型的规划方法。
    *   **统计显著**：通过 10 个种子的均值和标准差展示结果，证明了性能提升的稳定性。
    *   **消融分析**：通过对比预训练策略（ReBRAC）与优化后策略的得分，直接证明了推理时自适应的有效性。

### 6. 主要结论与发现
*   **性能提升**：MPCwDWM 在 18 个 MuJoCo 任务中的 16 个上实现了性能提升，平均得分（85.33）优于所有对比基线。
*   **复杂任务表现优异**：在极具挑战性的 AntMaze-Large 任务中，性能提升尤为显著（例如从 51 分提升至 66 分），证明了在长时程规划任务中，推理时优化能有效纠正离线策略的偏差。
*   **超越扩散规划**：相比于同样使用扩散模型的 Diffuser（平均 77.5 分），MPCwDWM（平均 94.4 分）通过直接优化策略参数而非仅仅采样轨迹，表现出更强的决策能力。

### 7. 优点与亮点
*   **端到端可微性**：巧妙地将扩散模型转化为可微计算图，解决了扩散模型与梯度优化结合的难题。
*   **推理时自适应**：打破了离线 RL “训练一次，到处运行”的僵化模式，为提升离线算法的泛化和鲁棒性提供了新思路。
*   **模块化设计**：该框架可以插入任何预训练的 Actor-Critic 算法之上，具有良好的通用性。

### 8. 不足与局限
*   **推理延迟**：由于涉及多步扩散采样和反向传播，实时性较差，可能难以应用于对响应速度要求极高的实时控制场景（如高速无人机）。
*   **模型误差累积**：虽然扩散模型很强大，但在长时程（$H$ 较大）展开时，世界模型的预测误差仍可能导致策略被误导（Model Exploitation）。
*   **算力需求**：推理阶段的计算量远大于传统方法，对部署端的算力提出了更高要求。

（完）
