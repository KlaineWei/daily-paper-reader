---
title: "KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao"
title_zh: KARMA：面向淘宝个性化搜索的知识-行为正则化多模态对齐
authors: "Zhi Sun, Wenming Zhang, Yi Wei, Liren Yu, Zhixuan Zhang, Dan Ou, Haihong Tang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22779v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 微调大模型用于个性化搜索和下一物品预测
tldr: "针对淘宝个性化搜索中大语言模型（LLM）微调导致的“知识-行为鸿沟”和语义坍缩问题，本文提出KARMA框架。该框架将语义重构作为训练正则项，在优化兴趣嵌入以进行检索的同时，通过历史条件生成和嵌入条件重构来保持LLM的语义解码能力。实验证明，KARMA有效缓解了语义坍缩，显著提升了淘宝搜索的召回、粗排和精排性能，并已成功上线带来0.5%的点击增长。"
selection_source: fresh_fetch
motivation: 直接在个性化任务上微调LLM会导致预训练语义知识与特定行为目标冲突，引发语义坍缩并限制泛化能力。
method: 提出KARMA框架，通过引入历史条件下的语义生成和基于嵌入的语义重构作为正则化约束，确保模型在学习用户行为的同时保留语义信息。
result: "在淘宝搜索系统中，KARMA显著提升了点击率AUC和各阶段召回率，并在实际线上部署中实现了0.5%的商品点击量增长。"
conclusion: 通过在训练中强制执行语义可解码性，可以有效弥合知识与行为之间的差距，充分发挥LLM在工业级个性化搜索中的语义泛化潜力。
---

## 摘要
大语言模型（LLMs）具备深厚的语义知识，使其成为为个性化搜索系统注入语义泛化能力的自然选择。然而，在实践中我们发现，在工业级个性化任务（如预测下一个商品）上直接微调 LLM 往往效果欠佳。我们将这一瓶颈归因于关键的“知识-行为鸿沟”（Knowledge-Action Gap）：即保留预训练语义知识与通过判别式目标对齐特定个性化行为之间存在的内在冲突。经验表明，仅针对行为的训练目标会导致“语义崩溃”（Semantic Collapse），例如出现“注意力汇聚”（attention sinks）现象。这种退化严重削弱了 LLM 的泛化能力，导致其无法为个性化搜索系统带来提升。我们提出了 KARMA（知识-行为正则化多模态对齐），这是一个将语义重构视为仅训练期正则化项的统一框架。KARMA 在优化用于检索的下一兴趣嵌入（行为）的同时，通过两个互补目标强制实现语义可解码性（知识）：(i) 基于历史条件的语义生成，将优化锚定在 LLM 原生的下一标记（next-token）分布上；(ii) 基于嵌入条件的语义重构，约束兴趣嵌入保持语义可恢复性。在淘宝搜索系统中，KARMA 缓解了语义崩溃（注意力汇聚分析），并提升了行为指标和语义保真度。在消融实验中，语义可解码性带来了高达 +22.5 的 HR@200 提升。凭借 KARMA，我们在精排阶段实现了 +0.25 的 CTR AUC 提升，在粗排阶段实现了 +1.86 的 HR 提升，在召回阶段实现了 +2.51 的 HR 提升。KARMA 已在线部署，精排阶段推理开销较低，带动商品点击量增长了 +0.5%。

## Abstract
Large Language Models (LLMs) are equipped with profound semantic knowledge, making them a natural choice for injecting semantic generalization into personalized search systems. However, in practice we find that directly fine-tuning LLMs on industrial personalized tasks (e.g. next item prediction) often yields suboptimal results. We attribute this bottleneck to a critical Knowledge--Action Gap: the inherent conflict between preserving pre-trained semantic knowledge and aligning with specific personalized actions by discriminative objectives. Empirically, action-only training objectives induce Semantic Collapse, such as attention ``sinks''. This degradation severely cripples the LLM's generalization, failing to bring improvements to personalized search systems.   We propose KARMA (Knowledge--Action Regularized Multimodal Alignment), a unified framework that treats semantic reconstruction as a train-only regularizer. KARMA optimizes a next-interest embedding for retrieval (Action) while enforcing semantic decodability (Knowledge) through two complementary objectives: (i) history-conditioned semantic generation, which anchors optimization to the LLM's native next-token distribution, and (ii) embedding-conditioned semantic reconstruction, which constrains the interest embedding to remain semantically recoverable.   On Taobao search system, KARMA mitigates semantic collapse (attention-sink analysis) and improves both action metrics and semantic fidelity. In ablations, semantic decodability yields up to +22.5 HR@200. With KARMA, we achieve +0.25 CTR AUC in ranking, +1.86 HR in pre-ranking and +2.51 HR in recalling. Deployed online with low inference overhead at ranking stage, KARMA drives +0.5% increase in Item Click.

---

## 论文详细总结（自动生成）

以下是对论文《KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在工业级个性化搜索中，直接微调大语言模型（LLM）进行“下一物品预测”往往效果不佳。
*   **研究动机**：作者发现存在**“知识-行为鸿沟”（Knowledge-Action Gap）**。当使用纯判别式目标（如点击行为对齐）训练时，模型会发生**“语义坍缩”（Semantic Collapse）**。
*   **现象观察**：模型在训练中会寻找“捷径”，将连续向量视为不透明的 ID，导致注意力机制出现“注意力汇聚”（Attention Sinks）现象（表现为条形码状的稀疏模式），丧失了 LLM 原有的语义泛化能力。

### 2. 方法论
KARMA 提出了一种统一框架，将语义重构作为**仅训练期（Train-only）**的正则化项，核心思想是确保生成的兴趣嵌入在对齐行为的同时，必须保持“语义可解码性”。
*   **核心架构**：采用 Decoder-only Transformer（如 Qwen3）作为用户行为解码器，将历史商品压缩为连续 Token。
*   **关键技术细节（两个正则化任务）**：
    1.  **任务 1：历史条件语义生成（Lgen）**：根据用户历史直接预测目标商品的文本 Token，将优化锚定在 LLM 原生的 Token 分布上，防止知识遗忘。
    2.  **任务 2：嵌入条件语义重构（Lrecon）**：强制要求模型生成的“下一兴趣嵌入”（$h_t$）能够重新解码出目标商品的文本。这形成了一个语义瓶颈，防止模型学习到非语义的 ID 类捷径。
*   **多模态扩展**：引入视觉语义重构，利用扩散模型（Diffusion）或流匹配（Flow-matching）头，根据兴趣嵌入重构商品的视觉特征。
*   **训练策略**：采用两阶段对齐。第一阶段进行语义预热（从单个商品嵌入解码语义）；第二阶段进行联合行为-语义训练。
*   **推理效率**：所有解码头在推理阶段均被移除，实现**零额外推理开销**。

### 3. 实验设计
*   **数据集/场景**：使用淘宝搜索的大规模匿名日志数据，包含用户历史行为、点击商品（正样本）及曝光未点击商品（硬负样本）。
*   **Benchmark 与对比方法**：
    *   **基准线**：仅使用行为目标（Pairwise Cross-Entropy）训练的连续 Token 接口模型。
    *   **评估指标**：行为指标（gAUC、HR@K）和语义指标（JS@50，即检索结果与真值的文本重合度，用于检测语义坍缩）。
*   **消融实验**：对比了仅任务 1、仅任务 2、任务 1+2 以及加入多模态信号的效果。

### 4. 资源与算力
*   **模型规格**：用户解码器使用 Qwen3-0.6B，商品编码器使用 Qwen3-1.7B（文本）和 Qwen3-VL-2B（多模态）。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练总时长。但考虑到其模型参数量（0.6B 到 2B）和淘宝的业务规模，推测使用了大规模 GPU 集群（如 A100/H800）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了离线评估、消融实验、定性分析（注意力图可视化）以及为期 14 天的线上 A/B 测试。
*   **充分性**：
    *   **多阶段验证**：在召回、粗排、精排三个阶段均进行了验证，证明了方法的通用性。
    *   **定性分析**：通过注意力图直观展示了 KARMA 如何修复“注意力汇聚”问题，增强了结论的说服力。
    *   **客观性**：引入 JS@50 指标从语义维度评估模型，而不仅仅依赖点击指标，能够更全面地揭示模型退化问题。

### 6. 主要结论与发现
*   **缓解坍缩**：KARMA 有效消除了注意力汇聚现象，使模型注意力分布更均匀，保留了语义保真度。
*   **性能提升**：在淘宝搜索中，KARMA 使精排 CTR AUC 提升 +0.25，粗排 HR 提升 +1.86，召回 HR 提升 +2.51。
*   **线上效果**：实际部署后，商品点击量（Item Click）提升了 **0.5%**。
*   **生成 vs 检索**：实验发现扩散模型适合作为语义正则项（Mode-seeking），但不适合直接生成检索嵌入（Mean-seeking），因为检索需要稳定的质心而非高保真采样。

### 7. 优点
*   **理论与实践结合**：深刻揭示了 LLM 在推荐/搜索中失效的底层原因（语义坍缩），并给出了优雅的正则化解决方案。
*   **工业友好**：通过“训练期重构、推理期移除”的设计，在不增加任何线上延迟的前提下提升了模型能力。
*   **多模态融合**：成功将视觉特征重构整合进统一的正则化框架中。

### 8. 不足与局限
*   **算力开销**：虽然推理无开销，但训练阶段需要运行多个解码头和扩散模型，训练算力成本显著增加。
*   **场景局限**：实验主要基于淘宝这种商品信息极其丰富的电商场景，在文本信息稀疏或无多模态信息的搜索场景下，提升幅度可能受限。
*   **超参数敏感性**：正则化项的权重 $\lambda$ 需要精细调优，以平衡行为对齐和语义保留。

（完）
