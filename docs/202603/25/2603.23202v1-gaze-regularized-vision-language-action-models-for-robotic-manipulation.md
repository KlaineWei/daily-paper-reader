---
title: Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation
title_zh: 机器人操作中基于注视正则化的视觉-语言-动作模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23202v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于机器人操作的视觉-语言-动作模型
tldr: 针对视觉-语言-动作（VLA）模型在精细操作任务中缺乏主动视觉注意力的问题，本文提出一种注视正则化训练框架。该方法通过KL散度将人类注视热图转化为补丁级分布，引导模型内部注意力对齐人类视觉模式。在不改变架构和推理开销的前提下，该框架显著提升了模型性能、收敛速度及鲁棒性，并增强了决策的可解释性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理精细操作时缺乏有效的视觉注意力分配机制，而人类注视数据蕴含了丰富的任务意图和规划信息。
method: 利用KL散度将时间聚合的人类注视热图转化为补丁级分布，以此正则化Transformer的注意力权重，使模型学习到人类的感知偏置。
result: "在多个操作基准测试中实现了4-12%的性能提升，且在光照变化和传感器噪声下表现出更强的鲁棒性。"
conclusion: 引入人类感知先验能有效加速机器人学习并提升任务表现，同时无需额外硬件即可增强系统的可解释性。
---

## 摘要
尽管视觉-语言-动作（VLA）模型取得了显著进展，但由于当前模型缺乏主动视觉注意力分配机制，机器人在处理细粒度操作任务时仍面临挑战。人类的注视自然地编码了意图、规划和执行模式，为引导机器人感知提供了强大的监督信号。我们提出了一种注视正则化训练框架，在不改变模型架构或增加推理开销的情况下，将 VLA 模型的内部注意力与人类视觉模式对齐。该方法将时间聚合的注视热图转换为补丁级（patch-level）分布，并通过 KL 散度对 Transformer 的注意力进行正则化，在保持部署效率的同时，为任务相关特征建立了归纳偏置。实验表明，将该方法集成到现有的 VLA 架构中后，在各项操作基准测试中实现了 4-12% 的性能提升。注视正则化模型能以更少的训练步数达到同等性能，并在光照变化和传感器噪声下保持鲁棒性。除了性能指标外，学习到的注意力模式还产生了反映人类策略的可解释可视化结果，增强了用户对机器人系统的信任。此外，该框架无需眼动追踪设备，可直接应用于现有数据集。这些结果表明，人类感知先验可以显著加速机器人学习，同时提升任务性能和系统可解释性。

## Abstract
Despite advances in Vision-Language-Action (VLA) models, robotic manipulation struggles with fine-grained tasks because current models lack mechanisms for active visual attention allocation. Human gaze naturally encodes intent, planning, and execution patterns -- offering a powerful supervisory signal for guiding robot perception. We introduce a gaze-regularized training framework that aligns VLA models' internal attention with human visual patterns without architectural modifications or inference-time overhead. Our method transforms temporally aggregated gaze heatmaps into patch-level distributions and regularizes the transformer's attention through KL divergence, creating an inductive bias toward task-relevant features while preserving deployment efficiency. When integrated into existing VLA architectures, our approach yields 4-12% improvements across manipulation benchmarks. The gaze-regularized models reach equivalent performance with fewer training steps and maintain robustness under lighting variations and sensor noise. Beyond performance metrics, the learned attention patterns produce interpretable visualizations that mirror human strategies, enhancing trust in robotic systems. Moreover, our framework requires no eye-tracking equipment and applies directly to existing datasets. These results demonstrate that human perceptual priors can significantly accelerate robot learning while improving both task performance and system interpretability.

---

## 论文详细总结（自动生成）

### 机器人操作中基于注视正则化的视觉-语言-动作模型总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型在机器人操作中表现出“被动感知”的局限性。模型通常需要同时学习“看哪里”和“如何做”，这导致在处理细粒度任务时训练效率低下、收敛缓慢，且在复杂环境下成功率遭遇瓶颈（约86%）。
相比之下，人类在操作时会通过**主动视觉注意力（注视）**来编码意图和规划。论文的核心动机是利用人类注视模式作为监督信号，引导 VLA 模型的内部注意力分配，使其从被动观察者转变为主动感知者，从而提升操作的精准度、学习效率和系统可解释性。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**注视正则化（Gaze-Regularized）**训练框架，其核心思想是在训练阶段通过辅助损失函数将模型的注意力图与人类注视分布对齐。

*   **核心流程：**
    1.  **注视先验生成：** 由于现有机器人数据集缺乏人类眼动数据，研究者使用预训练的注视预测模型（如 GLC 网络）在视频演示上生成合成注视热图（Heatmaps）。
    2.  **时间聚合：** 为了捕捉人类的预判性注视，对连续帧的热图进行加权时间平均，生成更稳定的注视先验。
    3.  **补丁级分布转换：** 将连续的热图投影到与 Transformer 视觉 Token 相同的网格上，转换为离散的概率分布 $G_{i,t}$。
    4.  **注意力提取：** 从 VLA 模型（如 Pi-0）的最后一个视觉-语言 Transformer 层提取交叉注意力分布 $S_{i,t}$，该分布反映了语言指令对视觉区域的关注度。
    5.  **正则化损失：** 使用 **KL 散度（Kullback-Leibler Divergence）** 来衡量注视分布与模型注意力分布之间的差异：
        $L_{total} = L_{action} + \lambda \cdot D_{KL}(G_{i,t} \parallel S_{i,t})$
*   **推理阶段：** 该框架仅在训练时使用，推理时不需要注视预测模型或眼动设备，保持了部署的轻量化和实时性。

#### 3. 实验设计
研究者在仿真和现实世界中进行了广泛验证：
*   **数据集/基准测试：**
    *   **LIBERO Suite：** 包括 Spatial（空间关系）、Object（物体识别）、Goal（目标导向）和 LIBERO-10（多任务泛化）。
    *   **ALOHA-Sim：** 用于验证跨域泛化能力（如立方体传递、挂钩插入）。
    *   **真实世界：** 使用 Aloha 机器人完成抓取立方体放置、多杯子连续放置等任务。
*   **对比方法：** 主要对比了未加正则化的基准模型（Base Pi-0 和 OpenVLA）。
*   **评估指标：** 任务成功率（Success Rate）和学习曲线（收敛速度）。

#### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了训练步数（如 10k, 20k, 30k 步），并指出在 20,000 步时，注视正则化模型即可达到基准模型在更长训练时间下的性能，暗示了该方法具有更高的算力利用效率。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度验证：** 涵盖了四个不同的 LIBERO 子集和 ALOHA 仿真环境，证明了方法的普适性。
*   **消融实验：** 深入探讨了正则化系数 $\lambda$ 的敏感性（发现弱正则化效果最好）、注视先验的质量（对比了 DeepGaze 和均匀分布）、以及视觉扰动（光照、噪声）下的表现。
*   **架构迁移：** 在 Pi-0 和 OpenVLA 两种不同的架构上均实现了性能提升，证明了方法的模块化特征。
*   **真实部署：** 补充了物理机器人的实验，验证了从仿真到现实的迁移能力。

#### 6. 主要结论与发现
*   **性能显著提升：** 在 LIBERO-Spatial 上成功率从 85.9% 提升至 95.5%，平均提升 4-12%。
*   **样本效率更高：** 正则化模型在训练早期（10k-20k 步）就表现出明显的领先优势，加速了策略收敛。
*   **鲁棒性增强：** 在光照变化和传感器噪声环境下，注视正则化模型比基准模型表现出更强的抗干扰能力。
*   **可解释性：** 可视化结果显示，正则化后的模型注意力更集中于任务相关区域（如目标物体及其周围），而非背景噪声，这增强了人类对机器人决策的信任。

#### 7. 优点
*   **训练即插即用：** 无需修改模型架构，不增加推理延迟，易于集成到现有的 VLA 流程中。
*   **无需额外硬件：** 通过合成注视模型解决了眼动数据稀缺的问题，降低了应用门槛。
*   **感知偏置有效：** 成功将人类的“扫描-规划-行动”感知先验转化为模型的归纳偏置。

#### 8. 不足与局限
*   **合成注视的偏差：** 依赖预训练的 GLC 模型生成注视数据，若预测模型本身存在偏差，可能会误导训练（虽然实验证明合成注视已足够有效）。
*   **任务依赖性：** 对于某些不需要精细视觉引导的简单任务，注视正则化的边际收益可能较小。
*   **真实注视数据的缺失：** 论文主要使用合成数据，未来若能结合真实的专家眼动数据，性能上限可能进一步提升。
*   **算力细节缺失：** 缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
