---
title: "OCP: Orthogonal Constrained Projection for Sparse Scaling in Industrial Commodity Recommendation"
title_zh: OCP：工业级商品推荐中稀疏扩展的正交约束投影
authors: "Chen Sun, Beilin Xu, Boheng Tan, Jiacheng Wang, Yuefeng Sun, Rite Bo, Ying He, Yaqiang Zang, Pinghua Gong"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18697v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 工业商品推荐中的正交约束投影
tldr: 针对工业级商品推荐中Item-Id词表在稀疏扩展时面临的低频信息干扰和表示崩溃问题，本文提出了正交约束投影（OCP）方法。该方法通过在投影过程中强制执行正交性，约束反向传播流形，使嵌入表示的奇异值谱与正交基对齐，从而保持各向同性的泛化特征并抑制过拟合。实验证明OCP能显著加速收敛并提升模型扩展性，在京东大规模部署中取得了显著的业务增长。
selection_source: fresh_fetch
motivation: 传统Item-Id词表在稀疏扩展时易受低频噪声干扰，导致表示崩溃并限制了模型对海量商品的表达能力。
method: 提出正交约束投影（OCP），通过强制正交性来约束反向传播流形，确保嵌入表示具有高奇异熵并抑制伪相关。
result: "该方法在京东商城大规模部署后，实现了UCXR提升12.97%和GMV增长8.9%的显著业务收益。"
conclusion: OCP有效解决了稀疏词表扩展中的表示质量问题，为工业级推荐系统的模型扩展提供了稳健且高效的优化方案。
---

## 摘要
在工业级商品推荐系统中，Item-Id 词表的表示质量直接影响推荐模型的可扩展性和泛化能力。一个关键挑战是，传统的 Item-Id 词表在进行稀疏扩展时，会受到低频信息的干扰，这限制了其对海量物品集的表达能力，并导致表示崩溃。为了解决这一问题，我们提出了一种正交约束投影（OCP）方法来优化嵌入表示。通过强制正交性，该投影约束了反向传播流形，使学习到的嵌入的奇异值谱与正交基对齐。这种对齐确保了高奇异熵，从而在抑制伪相关和对稀有物品过拟合的同时，保留了各向同性的泛化特征。实验结果表明，OCP 加速了损失收敛并增强了模型的可扩展性；值得注意的是，在扩展稠密层规模时，它能带来持续的性能提升。在京东的大规模工业部署进一步证实了其有效性，UCXR 提升了 12.97%，GMV 提升了 8.9%，突显了其在扩展稀疏词表和稠密架构方面的强大实用性。

## Abstract
In industrial commodity recommendation systems, the representation quality of Item-Id vocabularies directly impacts the scalability and generalization ability of recommendation models. A key challenge is that traditional Item-Id vocabularies, when subjected to sparse scaling, suffer from low-frequency information interference, which restricts their expressive power for massive item sets and leads to representation collapse. To address this issue, we propose an Orthogonal Constrained Projection method to optimize embedding representation. By enforcing orthogonality, the projection constrains the backpropagation manifold, aligning the singular value spectrum of the learned embeddings with the orthogonal basis. This alignment ensures high singular entropy, thereby preserving isotropic generalized features while suppressing spurious correlations and overfitting to rare items. Empirical results demonstrate that OCP accelerates loss convergence and enhances the model's scalability; notably, it enables consistent performance gains when scaling up dense layers. Large-scale industrial deployment on JD.com further confirms its efficacy, yielding a 12.97% increase in UCXR and an 8.9% uplift in GMV, highlighting its robust utility for scaling up both sparse vocabularies and dense architectures.

---

## 论文详细总结（自动生成）

以下是对论文《OCP: Orthogonal Constrained Projection for Sparse Scaling in Industrial Commodity Recommendation》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在工业级推荐系统中，为了覆盖长尾商品，Item-ID 词表规模正从百万级向十亿级扩展（即“稀疏扩展”）。然而，由于用户交互数据高度倾斜，长尾商品更新频率极低，导致嵌入空间出现**表示崩溃（Embedding Collapse）**，即有效秩降低，模型倾向于记住稀疏噪声而非学习可迁移的特征。
*   **研究背景**：传统的无约束投影层在处理海量稀疏 ID 时，无法保证梯度流的几何稳定性，导致模型在高维空间中仅利用了极少数的主导维度，限制了模型的泛化能力和扩展上限。

### 2. 核心方法论
*   **核心思想**：提出**正交约束投影（OCP）**，通过在 Stiefel 流形上约束投影矩阵 $P$，强制其保持列正交性（$P^\top P = I$）。
*   **关键技术细节**：
    *   **正交约束**：将 Item-ID 嵌入矩阵 $E$ 映射到交互空间的投影矩阵 $P$ 限制在 Stiefel 流形上。
    *   **更新机制**：采用基于 QR 分解的折返（Retraction）步骤更新参数：$P \leftarrow QR(P - \eta \nabla_P L)$，确保 $P$ 在训练过程中始终保持正交。
    *   **奇异值保持**：理论证明正交投影在反向传播中是等距的，能够将上层任务的梯度奇异值谱完整传递给嵌入层，防止梯度流在投影过程中发生秩侵蚀。
    *   **奇异熵（Singular Entropy, SE）**：引入 SE 指标来量化嵌入空间的各向同性，SE 越高表示空间利用率越充分。

### 3. 实验设计
*   **场景与数据集**：基于京东（JD.com）的真实工业场景，涵盖了**生成式检索（Generative Retrieval）**和**排序（Ranking）**两大任务。
*   **Benchmark（基准）**：
    *   检索任务：以 OxygenREC（基于语义 ID 的生成式框架）为基准。
    *   排序任务：以京东线上生产环境的 Transformer 排序模型为基准。
*   **对比实验**：
    *   对比了不同参数规模（0.7B 到 3.2B）下的 Dense Scaling。
    *   对比了不同词表规模（1亿到 10亿）下的 Sparse Scaling。
    *   进行了针对高频/低频商品的奇异熵对比分析。

### 4. 资源与算力
*   **训练规模**：
    *   生成式模型：在 64 亿个样本上训练了 19.6 万步。
    *   排序模型：在 29 亿个样本上训练了 72 万步（使用 4 个月的交互日志）。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号和数量，但从样本规模和参数量（最高 3.2B）来看，属于典型的大规模工业级分布式训练。

### 5. 实验数量与充分性
*   **实验量**：论文涵盖了离线指标对比（Hit@k, AUC, GAUC）、奇异熵几何分析、稠密扩展定律（Dense Scaling Law）验证、稀疏扩展定律（Sparse Scaling Law）验证以及线上 A/B 测试。
*   **充分性与客观性**：实验设计较为全面，不仅验证了业务指标的提升，还通过奇异值分布图深入解释了方法生效的物理机制。通过控制变量法（如固定词表大小改变模型参数，或反之）验证了 OCP 在不同维度扩展下的稳健性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **缓解崩溃**：OCP 有效抑制了嵌入空间的维度冗余，显著提升了低频长尾商品的表示质量（奇异熵提升）。
*   **加速收敛**：在相同的训练步数下，OCP 能使模型达到更低的 Loss，并展现出更好的扩展性（Scaling Law）。
*   **业务价值**：线上 A/B 测试显示，OCP 支撑了词表从 1.78 亿扩展到 10 亿，带来了 **+12.97% 的 UCXR**（用户转化率）和 **+8.95% 的 GMV** 增长。

### 7. 优点
*   **理论与实践结合**：将流形优化（Stiefel Manifold）引入推荐系统，并给出了严谨的奇异值保持证明。
*   **双重扩展性**：证明了该方法在增加模型深度（Dense）和增加词表宽度（Sparse）时均能带来持续收益。
*   **工业实用性**：QR 分解带来的额外计算开销相对于大规模 Embedding 查表和 Transformer 计算而言微乎其微，易于在大规模系统中部署。

### 8. 不足与局限
*   **算力开销细节缺失**：虽然提到开销较小，但未给出具体的训练耗时增加比例。
*   **流形优化器单一**：目前仅探索了 QR 折返机制，未对比其他流形优化算法（如 Cayley 变换等）的性能差异。
*   **分布偏移挑战**：论文主要关注静态词表的扩展，对于极端的冷启动爆发或剧烈的季节性分布偏移，正交约束的适应性仍需进一步研究。

（完）
