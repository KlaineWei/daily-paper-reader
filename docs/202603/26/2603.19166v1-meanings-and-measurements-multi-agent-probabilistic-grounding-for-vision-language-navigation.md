---
title: "Meanings and Measurements: Multi-Agent Probabilistic Grounding for Vision-Language Navigation"
title_zh: 意义与度量：用于视觉语言导航的多智能体概率关联
authors: "Swagat Padhan, Lakshya Jain, Bhavya Minesh Shah, Omkar Patil, Thao Nguyen, Nakul Gopalan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19166v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于机器人目标执行的视觉语言导航与接地
tldr: 针对机器人视觉语言导航中视觉语言模型（VLM）难以处理复杂度量约束的问题，本文提出了MAPG框架。该框架通过多智能体协作将语言查询分解为结构化子组件，利用VLM进行语义定位，并结合概率组合生成符合度量一致性的3D空间决策。实验证明MAPG在HM-EQA及新提出的MAPG-Bench基准上表现优异，并成功应用于真实机器人。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理包含精确度量约束（如“冰箱右侧两米”）的复杂语义查询时表现不佳。
method: 提出一种多智能体概率定位框架，将查询分解后利用VLM定位，再通过概率组合实现度量一致的决策。
result: 在HM-EQA和新基准MAPG-Bench上均取得显著性能提升，并完成了真实世界的机器人演示。
conclusion: MAPG有效地结合了VLM的语义能力与概率推理的度量精度，提升了机器人在复杂物理环境中的语言理解与执行能力。
---

## 摘要
与人类协作的机器人必须将自然语言目标转化为可执行的、物理关联的决策。例如，执行诸如“走到冰箱右侧两米处”之类的指令，需要对 3D 场景中的语义引用、空间关系和度量约束进行关联。虽然近期的视觉语言模型 (VLM) 展示了强大的语义关联能力，但它们并非专门为在物理定义的空间中推理度量约束而设计的。在这项工作中，我们通过实验证明，最先进的基于 VLM 的关联方法在处理复杂的度量-语义语言查询时表现不佳。为了解决这一局限性，我们提出了 MAPG（多智能体概率关联），这是一个智能体框架，它将语言查询分解为结构化的子组件，并调用 VLM 对每个组件进行关联。随后，MAPG 以概率方式组合这些关联输出，从而在 3D 空间中生成度量一致且可执行的决策。我们在 HM-EQA 基准测试上评估了 MAPG，并展示了其相对于强基准模型的持续性能提升。此外，我们引入了一个新的基准测试 MAPG-Bench，专门用于评估度量-语义目标关联，填补了现有语言关联评估中的空白。我们还展示了真实世界的机器人演示，表明当存在结构化场景表示时，MAPG 可以从仿真环境迁移到现实世界。

## Abstract
Robots collaborating with humans must convert natural language goals into actionable, physically grounded decisions. For example, executing a command such as "go two meters to the right of the fridge" requires grounding semantic references, spatial relations, and metric constraints within a 3D scene. While recent vision language models (VLMs) demonstrate strong semantic grounding capabilities, they are not explicitly designed to reason about metric constraints in physically defined spaces. In this work, we empirically demonstrate that state-of-the-art VLM-based grounding approaches struggle with complex metric-semantic language queries. To address this limitation, we propose MAPG (Multi-Agent Probabilistic Grounding), an agentic framework that decomposes language queries into structured subcomponents and queries a VLM to ground each component. MAPG then probabilistically composes these grounded outputs to produce metrically consistent, actionable decisions in 3D space. We evaluate MAPG on the HM-EQA benchmark and show consistent performance improvements over strong baselines. Furthermore, we introduce a new benchmark, MAPG-Bench, specifically designed to evaluate metric-semantic goal grounding, addressing a gap in existing language grounding evaluations. We also present a real-world robot demonstration showing that MAPG transfers beyond simulation when a structured scene representation is available.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MAPG (Multi-Agent Probabilistic Grounding)** 的框架，旨在解决机器人视觉语言导航（VLN）中精确度量与语义理解相结合的难题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：人类在指挥机器人时经常使用包含**度量（Metric）**和**语义（Semantic）**的混合指令（例如：“去冰箱右边两米处”）。
*   **核心挑战**：现有的视觉语言模型（VLM）虽然语义理解能力强，但在处理物理空间中的精确度量约束（距离、角度、比例）时表现不佳。此外，现有的导航系统往往将目标定位视为单步决策，容易产生累积误差。
*   **研究目标**：开发一种能够将复杂的自然语言指令分解，并在 3D 空间中生成连续、可执行的概率分布的目标定位框架。

### 2. 方法论
MAPG 采用了一种多智能体协作的概率定位方法，核心流程如下：
*   **编排器 (Orchestrator)**：将自然语言查询解析为结构化的**空间描述子句 (SDCs)**，提取出锚点对象（如“冰箱”）、空间谓词（如“右侧”）和度量约束（如“2米”）。
*   **定位智能体 (Grounding Agent)**：利用 3D 语义场景图（Scene Graph）、字符串匹配和 CLIP 相似度，将文本中的锚点解析为环境中具体的 3D 物体实例。
*   **空间智能体 (Spatial Agent)**：为每个约束生成连续的概率密度函数 (PDF)：
    *   **方向核 (Directional Kernel)**：使用 **von Mises-Fisher 分布**建模空间谓词（如“左”、“前”）。
    *   **度量核 (Metric Kernel)**：使用 **径向高斯分布 (Radial Gaussian)** 建模距离约束。
*   **概率组合**：通过在对数空间中叠加这些核函数（类似于专家乘积模型 PoE），生成最终的目标位置分布。
*   **规划接口**：从最终分布中采样候选点，通过 RRT* 等路径规划算法生成机器人的执行轨迹。

### 3. 实验设计
*   **数据集/场景**：使用了 **HM3D (Habitat-Matterport 3D)** 数据集，包含 30 个真实的室内房屋场景。
*   **基准测试 (Benchmark)**：
    1.  **HM-EQA**：现有的具身问答基准，侧重于语义推理。
    2.  **MAPG-Bench (新提出)**：专门针对度量-语义定位设计的基准，包含 100 个带标注的复杂查询，评估指标包括距离误差（O-O/O-W Error）、角度误差（Yaw/Pitch Error）和任务成功率（TSR）。
*   **对比方法**：对比了 **GraphEQA**（基于场景图的强基准）、**SRGPT**（空间专用模型）以及多种主流 VLM（如 GPT 系列、Gemini、Claude）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **模型使用**：由于该框架主要基于现有的预训练大模型（如 GPT-4o/5.2, Gemini 3 Pro 等）进行零样本（Zero-shot）或智能体化推理，因此其核心开销可能集中在 API 调用和在线 3D 场景图的构建上，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：在 30 个不同场景下进行了 100 组详细的度量-语义查询实验。
*   **消融实验**：进行了多项消融研究，包括：
    *   验证“显式空间推理器”与“仅靠思维链（CoT）提示词”的效果对比。
    *   评估在物体被遮挡（Occlusion）情况下的鲁棒性。
*   **充分性评价**：实验设计较为全面，涵盖了仿真环境对比、消融分析以及真实世界的机器人演示（使用 Robotis AI Worker 机器人），能够客观地证明模块化分解与概率组合的有效性。

### 6. 主要结论与发现
*   **精度大幅提升**：MAPG 将物体到世界（O-W）的定位误差从基准模型的 **5.82米 降低至 0.07米**（降低了 98.8%）。
*   **方向一致性**：偏航角（Yaw）误差从 13.5° 降至 1.9°，显著提升了空间指向的准确性。
*   **结构化优势**：实验证明，简单的 Prompting 无法让 VLM 获得精确的度量能力，必须通过显式的数学核函数进行几何约束。
*   **现实迁移**：MAPG 在拥有结构化场景表示的情况下，能够成功从仿真环境迁移到真实物理环境。

### 7. 优点
*   **模块化与可解释性**：将模糊的语言转化为清晰的数学概率分布，使定位过程透明且可调。
*   **填补空白**：提出了 MAPG-Bench，为后续研究度量-语义结合的具身智能提供了标准。
*   **鲁棒性**：通过多视图证据积累和概率维护，能够有效处理部分观测和遮挡问题。

### 8. 不足与局限
*   **依赖场景图**：系统的性能高度依赖于底层 3D 场景图的完整性。如果物体未被扫描或识别，定位将失效。
*   **参考系歧义**：对于“椅子前面”这类依赖物体自身朝向（Intrinsic Frame）的谓词，当物体朝向不明时仍存在推理困难。
*   **地图对齐风险**：自由空间地图与语义场景图之间的微小错位可能会影响导航点的精确选取。

（完）
