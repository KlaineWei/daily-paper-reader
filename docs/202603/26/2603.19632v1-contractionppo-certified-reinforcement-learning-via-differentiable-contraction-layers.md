---
title: "ContractionPPO: Certified Reinforcement Learning via Differentiable Contraction Layers"
title_zh: ContractionPPO：通过可微收缩层实现的保证式强化学习
authors: "Vrushabh Zinage, Narek Harutyunyan, Eric Verheyden, Fred Y. Hadaegh, Soon-Jo Chung"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19632v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于足式机器人认证鲁棒规划与控制的强化学习
tldr: 针对足式机器人在复杂环境下的鲁棒控制难题，本文提出 ContractionPPO 框架。该方法将近端策略优化（PPO）与可微收缩度量层相结合，在最大化强化学习性能的同时，通过 Lipschitz 神经网络参数化的度量层为闭环系统提供增量指数稳定性的形式化保证。实验证明，该方法在四足机器人上实现了具备稳定性认证的鲁棒运动，能有效应对外部扰动。
selection_source: fresh_fetch
motivation: 传统的足式机器人控制方法依赖复杂的参考轨迹设计，而标准强化学习缺乏应对扰动的形式化稳定性保证。
method: 在 PPO 框架中引入一个由 Lipschitz 神经网络参数化的状态相关收缩度量层，与策略同步训练以认证系统的稳定性。
result: 硬件实验表明，ContractionPPO 使四足机器人在受到强外部扰动时仍能保持鲁棒且可认证稳定的运动。
conclusion: 该研究通过将收缩理论与强化学习结合，为高维、多接触系统的鲁棒控制提供了一种兼具性能与形式化安全保证的新途径。
---

## 摘要
非结构化环境中的足式运动不仅需要高性能的控制策略，还需要形式化的保证以确保在扰动下的鲁棒性。控制方法通常需要精心设计的参考轨迹，而这在诸如四足机器人等高维、多接触系统中极具挑战性。相比之下，强化学习（RL）直接学习隐式生成运动的策略，并独特地受益于在训练期间获取特权信息（如完整状态和动力学），而这些信息在部署时是无法获取的。我们提出了 ContractionPPO，这是一个通过为近端策略优化（PPO）强化学习增加一个状态相关的收缩度量层，来实现足式机器人保证式鲁棒规划与控制的框架。该方法使策略在最大化性能的同时，产生一个收缩度量，用以证明模拟闭环系统的增量指数稳定性。该度量被参数化为一个 Lipschitz 神经网络，并与策略联合训练，可以采用并行方式或作为 PPO 骨干网络的辅助头。虽然收缩度量在实际执行过程中并不部署，但我们推导了最坏情况收缩率的上界，并证明这些上界确保了所学习的收缩度量能够从仿真泛化到实际部署。我们在四足运动上的硬件实验表明，ContractionPPO 即使在强烈的外部扰动下也能实现鲁棒且可保证稳定的控制。

## Abstract
Legged locomotion in unstructured environments demands not only high-performance control policies but also formal guarantees to ensure robustness under perturbations. Control methods often require carefully designed reference trajectories, which are challenging to construct in high-dimensional, contact-rich systems such as quadruped robots. In contrast, Reinforcement Learning (RL) directly learns policies that implicitly generate motion, and uniquely benefits from access to privileged information, such as full state and dynamics during training, that is not available at deployment. We present ContractionPPO, a framework for certified robust planning and control of legged robots by augmenting Proximal Policy Optimization (PPO) RL with a state-dependent contraction metric layer. This approach enables the policy to maximize performance while simultaneously producing a contraction metric that certifies incremental exponential stability of the simulated closed-loop system. The metric is parameterized as a Lipschitz neural network and trained jointly with the policy, either in parallel or as an auxiliary head of the PPO backbone. While the contraction metric is not deployed during real-world execution, we derive upper bounds on the worst-case contraction rate and show that these bounds ensure the learned contraction metric generalizes from simulation to real-world deployment. Our hardware experiments on quadruped locomotion demonstrate that ContractionPPO enables robust, certifiably stable control even under strong external perturbations.

---

## 论文详细总结（自动生成）

### 论文总结：ContractionPPO —— 通过可微收缩层实现的保证式强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
足式机器人在复杂、非结构化环境中的运动控制面临双重挑战：一方面需要**高性能**以应对非线性动力学和多接触环境，这通常由强化学习（RL）实现；另一方面需要**形式化稳定性保证**以确保在扰动下的鲁棒性，这通常是传统控制理论的范畴。
*   **研究动机**：标准的 RL 策略（如 PPO）虽然表现优异，但缺乏稳定性认证，在面对传感器噪声、模型失配或外部剧烈扰动时容易失效。
*   **核心问题**：如何将非线性稳定性理论（收缩理论）集成到深度强化学习框架中，使学习到的策略既具备 RL 的灵活性，又拥有可证明的增量指数稳定性（IES）。

#### 2. 方法论
论文提出了 **ContractionPPO** 框架，其核心是将收缩度量（Contraction Metric）作为可微层引入 PPO 训练中。
*   **核心思想**：利用收缩理论，证明系统所有轨迹都会指数级地相互收敛。通过学习一个状态相关的正定矩阵函数 $M(x)$（收缩度量），来认证闭环系统的稳定性。
*   **关键技术细节**：
    *   **可微收缩层**：使用 Lipschitz 神经网络参数化收缩度量 $M_\phi(x) = \Theta^\top \Theta$，确保其正定性。
    *   **特权信息利用**：在仿真训练阶段，利用完整的系统状态（特权信息）来训练收缩度量层，而部署的策略网络仅依赖部分观测值。
    *   **联合损失函数**：总损失 $L_{total} = L_{PPO} + w_{contr} L_{contr} + w_{PD} L_{PD}$。其中 $L_{contr}$ 惩罚违反收缩条件（$\dot{V} + \alpha V \leq -\epsilon_\alpha$）的行为。
    *   **理论保证**：推导了收缩率 $\alpha$ 的显式下界，并证明了通过 Lipschitz 约束和仿真中的边际误差 $\epsilon_\alpha$，可以确保稳定性从仿真泛化到现实世界。

#### 3. 实验设计
*   **实验场景**：
    1.  **四足机器人倒立（Handstand）**：在移动平台上保持倒立姿态并限制在 0.8m 半径圆圈内。
    2.  **外部风力扰动**：在未训练过风力的情况下，测试机器人在 4.8m/s 至 9.6m/s 风速下的稳定性。
    3.  **人形机器人（Booster T1）稳定化**：验证方法在不同形态机器人上的通用性。
*   **Benchmark 与对比方法**：
    *   **PPO**：标准近端策略优化。
    *   **RMA (Rapid Motor Adaptation)**：经典的足式机器人自适应 RL 方法。
    *   **TumblerNet**：一种针对足式机器人稳定性的近期基准方法。

#### 4. 资源与算力
*   **硬件环境**：使用配备 Intel Core i7-13700F CPU 和 **NVIDIA RTX 3080 GPU** 的桌面电脑。
*   **训练规模**：利用 IsaacLab 框架，并行运行 **4,096 个智能体**。
*   **训练时长**：完成完整训练大约需要 **2 小时**。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 2500 组测试回合（Combined episodes），涵盖了不同数量的控制点（难度梯度）。
*   **充分性**：实验涵盖了仿真验证与硬件实机实验。通过消融实验（如固定度量 $M=I$ 与学习度量的对比）证明了状态相关度量的必要性。
*   **客观性**：对比了当前最先进的自适应方法（RMA），并在完全未见过的极端风力扰动下进行测试，展示了极强的泛化能力和客观的鲁棒性指标。

#### 6. 主要结论与发现
*   **稳定性认证有效**：ContractionPPO 是唯一能确保机器人始终保持在规定区域（0.8m 圆圈）内的算法，而 PPO、RMA 和 TumblerNet 均出现了不同程度的漂移或失败。
*   **零样本鲁棒性**：即使未针对风力进行训练，ContractionPPO 也能在强风扰动后迅速恢复稳定，证明了收缩理论提供的稳定性边际具有实际物理意义。
*   **泛化能力**：该框架成功应用于四足和人形两种截然不同的机器人形态，证明了其通用性。

#### 7. 优点
*   **理论与实践结合**：将严谨的收缩理论转化为可微的神经网络损失项，填补了形式化方法与深度 RL 之间的鸿沟。
*   **Sim-to-Real 保证**：通过推导 Lipschitz 边界，为策略从仿真到现实的迁移提供了数学上的稳定性信心。
*   **无需参考轨迹**：与传统控制方法不同，该方法不需要预先设计复杂的参考轨迹，降低了高维系统控制的门槛。

#### 8. 不足与局限
*   **状态空间限制**：理论保证仅在训练覆盖的紧凑状态空间 $K$ 内有效。如果机器人进入完全未探索的极端状态，稳定性保证可能失效。
*   **超参数敏感性**：收缩率 $\alpha$ 与近似误差 $\epsilon_\alpha$ 之间存在权衡，选择不当可能导致训练难以收敛或保证过于保守。
*   **计算开销**：虽然部署时仅运行策略网络，但在训练阶段计算 $A_{cl}$（闭环雅可比）和 $\dot{M}$ 需要自动微分，增加了训练的计算复杂度。

（完）
