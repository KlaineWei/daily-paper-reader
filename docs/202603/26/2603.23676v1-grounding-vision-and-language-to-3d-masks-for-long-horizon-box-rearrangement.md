---
title: Grounding Vision and Language to 3D Masks for Long-Horizon Box Rearrangement
title_zh: 将视觉与语言对齐至 3D 掩码以实现长时程箱体重排
authors: "Ashish Malik, Caleb Lowe, Aayam Shrestha, Stefan Lee, Fuxin Li, Alan Fern"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23676v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 使用视觉语言模型在3D环境中进行长程规划
tldr: "本研究针对长程3D物体重新排列任务，提出RAMP-3D模型。该模型将视觉语言模型扩展至3D掩码预测，通过序列化预测“抓取物体”和“放置区域”的掩码对来生成动作。在包含多达30个物体的复杂仓库场景中，RAMP-3D实现了79.5%的成功率，显著优于2D VLM基准，证明了掩码式反应策略在长程规划中的潜力。"
selection_source: fresh_fetch
motivation: 现有符号规划器和2D VLM在处理复杂3D几何及多物体长程任务时鲁棒性不足。
method: 提出RAMP-3D，通过预测成对的3D掩码来反应式地生成多步拾取与放置动作。
result: "在11种任务变体中达到79.5%的成功率，性能大幅领先于基于2D视觉语言模型的方法。"
conclusion: 基于3D掩码的反应式策略为长程3D任务规划提供了一种比传统符号流水线更有效的替代方案。
---

## 摘要
我们研究了在 3D 环境中，仅利用视觉观测从不完全指定的自然语言目标出发进行长时程规划，重点关注多步 3D 箱体重排任务。现有方法通常依赖于符号规划器（其状态与目标的关联关系较为脆弱），或者依赖于 2D 视觉语言模型（VLM）直接生成动作序列。这两种方法在处理大量物体、丰富的 3D 几何形状以及隐式语义约束的推理时都面临困难。3D VLM 的最新进展展示了自然语言指代物与 3D 分割掩码之间的强对齐能力，这表明了实现更通用规划能力的潜力。我们扩展了现有的 3D 对齐模型，并提出了反应式动作掩码规划器（RAMP-3D），该规划器将长时程规划建模为成对 3D 掩码的序列化反应式预测：一个“哪个物体”掩码指示抓取对象，一个“哪个目标区域”掩码指定放置位置。该系统处理 RGB-D 观测和自然语言任务规范，以反应式地生成用于 3D 箱体重排的多步取放动作。我们在包含 1 到 30 个箱体及多种自然语言约束的仓库式环境中，针对 11 种任务变体进行了实验。RAMP-3D 在长时程重排任务上达到了 79.5% 的成功率，并显著优于基于 2D VLM 的基准模型，证明了基于掩码的反应式策略是长时程规划中替代符号流水线的一种极具前景的选择。

## Abstract
We study long-horizon planning in 3D environments from under-specified natural-language goals using only visual observations, focusing on multi-step 3D box rearrangement tasks. Existing approaches typically rely on symbolic planners with brittle relational grounding of states and goals, or on direct action-sequence generation from 2D vision-language models (VLMs). Both approaches struggle with reasoning over many objects, rich 3D geometry, and implicit semantic constraints. Recent advances in 3D VLMs demonstrate strong grounding of natural-language referents to 3D segmentation masks, suggesting the potential for more general planning capabilities. We extend existing 3D grounding models and propose Reactive Action Mask Planner (RAMP-3D), which formulates long-horizon planning as sequential reactive prediction of paired 3D masks: a "which-object" mask indicating what to pick and a "which-target-region" mask specifying where to place it. The resulting system processes RGB-D observations and natural-language task specifications to reactively generate multi-step pick-and-place actions for 3D box rearrangement. We conduct experiments across 11 task variants in warehouse-style environments with 1-30 boxes and diverse natural-language constraints. RAMP-3D achieves 79.5% success rate on long-horizon rearrangement tasks and significantly outperforms 2D VLM-based baselines, establishing mask-based reactive policies as a promising alternative to symbolic pipelines for long-horizon planning.

---

## 论文详细总结（自动生成）

### 论文详细总结：将视觉与语言对齐至 3D 掩码以实现长时程箱体重排

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**长时程（Long-Horizon）3D 物体（箱体）重排任务**。在仓库等复杂环境中，机器人需要根据模糊的自然语言指令（如“将大箱子移到后排货架，堆叠高度不超过2层”）进行操作。
*   **现有挑战**：传统的符号规划器依赖于脆弱的手写谓词和状态表示，难以处理复杂的 3D 几何约束；而基于 2D 图像的视觉语言模型（VLM）缺乏对 3D 空间、遮挡和体积关系的深度理解。
*   **研究动机**：利用 3D VLM 强大的语言-空间对齐能力，直接将长时程规划任务转化为 3D 掩码预测问题，从而避免复杂的符号层设计。

#### 2. 方法论：RAMP-3D
RAMP-3D（Reactive Action Mask Planner）的核心思想是将高层规划动作表示为一对 **3D 分割掩码**。
*   **核心架构**：基于 UniVLG 模型。输入多视角 RGB-D 图像和自然语言指令。
    *   **视觉编码**：使用 Swin Transformer 提取 2D 特征，通过深度图反投影到 3D 空间并进行体素化处理。
    *   **语言编码**：使用 RoBERTa 编码任务指令。
    *   **掩码解码器**：通过 Transformer 解码器生成查询（Queries），输出两个关键掩码：**“抓取哪个物体”（Which-object）**和**“放置在哪个区域”（Which-target-region）**。
*   **关键技术细节**：
    *   **配对对比特征（Pair-contrastive features）**：引入对比损失函数，确保预测的抓取物体与放置目标在逻辑上是匹配且一致的。
    *   **终止预测头（Done head）**：一个轻量级 MLP，用于判断任务是否完成并停止规划。
    *   **掩码空间动作表示**：直接在 3D 点云上预测掩码，随后将其映射回物体 ID 或几何坐标，驱动底层的取放控制器。

#### 3. 实验设计
*   **场景与数据集**：在 IsaacSim 仿真环境中构建了一个 12m x 12m 的仓库，包含 1-30 个箱子、托盘、货架和干扰物。
*   **任务变体**：设计了 **11 种任务类型**，涵盖堆叠高度限制、颜色优先级、货架/托盘优先级、空间排序、可达性约束等。
*   **Benchmark 与对比方法**：
    *   **2D-pointer（基准）**：结合了 GPT-5.1（负责高层逻辑描述）和 Molmo-72B（负责在 2D 俯视图中“指点”目标位置）。
    *   **评估模式**：分为“对齐目标模式”（Snap-to-target，理想化放置）和“自由形式模式”（Free-form，完全由模型预测坐标）。

#### 4. 资源与算力
*   **模型训练**：论文提到了使用 `gpt-oss-120b` 进行自然语言增强（改写指令），并使用了 RoBERTa-base 和 Swin Transformer。
*   **算力细节**：**文中未明确说明**具体的 GPU 型号、数量、训练时长或内存消耗。考虑到 3D VLM 的计算特性，通常需要高性能 GPU 集群（如 A100/H100）。

#### 5. 实验数量与充分性
*   **实验规模**：生成了约 **9.5k 个独特场景样本**，其中 2.2k 用于测试。
*   **充分性评价**：
    *   **多维度评估**：涵盖了单步预测准确率、掩码质量（IoU）、长时程规划成功率。
    *   **消融实验**：对辅助预测任务（如识别空位、已放置箱子等）和语言泛化性（保留未见过的指令模板）进行了消融测试。
    *   **客观性**：实验对比了 2D 和 3D 方法在不同物体密度（1-30 个箱子）下的表现，展示了性能随复杂度下降的趋势，评估较为客观。

#### 6. 主要结论与发现
*   **性能卓越**：RAMP-3D 在长时程任务中达到了 **79.5% 的成功率**，远超 2D 基准模型（2D 模型在复杂 3D 场景中经常失效）。
*   **3D 掩码的优势**：直接在 3D 空间操作可以更好地处理遮挡和几何约束，单步动作有效性高达 96.8%。
*   **复杂性挑战**：随着箱子数量增加（20-30 个），成功率有所下降，主要原因是长时程操作中的误差累积。
*   **语言泛化**：通过 LLM 增强训练，模型对未见过的指令表达方式具有较强的鲁棒性。

#### 7. 优点
*   **端到端简化**：避免了传统规划中繁琐的符号定义和谓词逻辑转换。
*   **几何感知力强**：利用 3D 掩码直接编码几何可行性和占用空间，天然适合 3D 操作任务。
*   **反应式规划**：能够根据环境变化实时调整动作，无需复杂的重规划搜索。

#### 8. 不足与局限
*   **缺乏前瞻性（Lookahead）**：模型是纯反应式的，在处理需要全局最优解或具有复杂前置条件的非直观任务时（如“箱子可达性”任务），表现较弱。
*   **视觉多样性有限**：实验主要集中在箱体（Box）重排，物体的几何形状较为单一，尚未验证在更通用、形状各异的物体上的表现。
*   **误差累积**：在“自由形式”执行模式下，微小的放置偏差会随步数增加而放大，导致长时程任务失败。
*   **算力透明度**：未公开训练成本，难以评估其在实际部署中的资源效率。

（完）
