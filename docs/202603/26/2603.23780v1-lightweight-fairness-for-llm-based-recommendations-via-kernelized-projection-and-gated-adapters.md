---
title: Lightweight Fairness for LLM-Based Recommendations via Kernelized Projection and Gated Adapters
title_zh: 基于核化投影与门控适配器的 LLM 推荐系统轻量级公平性研究
authors: "Nan Cui, Wendy Hui Wang, Yue Ning"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23780v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 基于LLM的推荐系统，具有公平性和门控适配器
tldr: 针对大语言模型推荐系统存在的社会偏见问题，本文提出了一种轻量级且可扩展的公平性缓解方法。该方法结合了核化迭代零空间投影（INLP）和门控混合专家（MoE）适配器，在无需额外训练参数的情况下，通过闭式投影消除表示中的敏感属性，并利用MoE适配器在保持推荐准确性的同时防止偏见重新引入。实验证明该方法能有效减少属性泄露并维持高推荐性能。
selection_source: fresh_fetch
motivation: 现有的LLM推荐系统公平性方案通常面临微调参数过多或优化不稳定的挑战。
method: 提出结合核化迭代零空间投影（INLP）与门控混合专家（MoE）适配器，实现敏感属性的闭式消除与任务效用的选择性恢复。
result: 在两个公开数据集上的实验表明，该方法在显著降低多个受保护变量泄露的同时，保持了竞争性的推荐准确度。
conclusion: 本研究证明了通过轻量级的投影技术和门控机制，可以在不牺牲性能的前提下有效提升大语言模型推荐系统的公平性。
---

## 摘要
大语言模型（LLMs）为推荐系统引入了新的能力，实现了动态、上下文感知和对话式推荐。然而，基于 LLM 的推荐系统继承并可能放大预训练数据中嵌入的社会偏见，尤其是在存在人口统计学线索时。现有的公平性解决方案要么需要额外的参数微调，要么面临优化不稳定的问题。我们提出了一种轻量级且可扩展的偏见缓解方法，该方法结合了核化迭代零空间投影（INLP）与门控混合专家（MoE）适配器。我们的方法估计了一个闭式投影，在不增加额外可训练参数的情况下，从 LLM 表示中移除单个或多个敏感属性。为了保持任务效用，我们引入了一个两级 MoE 适配器，在不重新引入偏见的情况下选择性地恢复有用信号。在两个公开数据集上的实验表明，我们的方法在保持竞争性推荐准确度的同时，减少了多个受保护变量的属性泄漏。

## Abstract
Large Language Models (LLMs) have introduced new capabilities to recommender systems, enabling dynamic, context-aware, and conversational recommendations. However, LLM-based recommender systems inherit and may amplify social biases embedded in their pre-training data, especially when demographic cues are present. Existing fairness solutions either require extra parameters fine-tuning, or suffer from optimization instability. We propose a lightweight and scalable bias mitigation method that combines a kernelized Iterative Null-space Projection (INLP) with a gated Mixture-of-Experts (MoE) adapter. Our approach estimates a closed-form projection that removes single or multiple sensitive attributes from LLM representations with no additional trainable parameters. To preserve task utility, we introduce a two-level MoE adapter that selectively restores useful signals without reintroducing bias. Experiments on two public datasets show that our method reduces attribute leakage across multiple protected variables while maintaining competitive recommendation accuracy.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **RFF-INLP + Gated MoE Adapter** 的轻量级框架，旨在解决基于大语言模型（LLM）的推荐系统在处理敏感属性（如性别、年龄、职业）时的公平性问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于 LLM 的推荐系统（RecLLMs）虽然表现出色，但容易继承并放大预训练数据中的社会偏见。当输入包含人口统计学信息时，推荐结果可能产生歧视。
*   **研究动机**：现有的公平性方法（如对抗性训练）通常需要大量额外的可训练参数，且优化过程不稳定（如梯度反转层难以收敛）。
*   **整体含义**：本文寻求一种“轻量级”方案，既能通过数学闭式解（Closed-form）快速消除偏见，又能通过适配器结构在不重新引入偏见的前提下恢复推荐的准确性。

### 2. 论文提出的方法论
该方法分为两个核心阶段，遵循“先擦除、后修复”的逻辑：

*   **阶段一：核化迭代零空间投影（RFF-INLP）**
    *   **核心思想**：利用线性代数中的零空间投影（INLP）来消除敏感信息。
    *   **关键技术**：
        1.  **随机傅里叶变换（RFF）**：将原始表示映射到高维核空间，使非线性偏见信号线性化，从而能被线性投影捕获。
        2.  **各向同性高斯扰动**：在拟合探测器前加入微小噪声，增强零空间的鲁棒性。
        3.  **闭式投影**：通过最小二乘法求解投影矩阵 $P$，将表示投影到与敏感属性探测器正交的零空间中，此过程无需梯度下降。
*   **阶段二：两级门控混合专家（MoE）适配器**
    *   **核心思想**：处理多个敏感属性并恢复因投影损失的任务效用。
    *   **关键技术**：
        1.  **一级门控（属性权重）**：根据当前上下文动态调整不同属性投影矩阵的混合比例，实现精细化脱敏。
        2.  **二级门控（专家融合）**：引入低秩 LoRA 专家，在脱敏后的表示基础上进行微调，仅恢复对推荐任务有用的信号，同时通过门控机制防止偏见回流。

### 3. 实验设计
*   **数据集**：
    *   **MovieLens-1M**：包含性别、年龄、职业等敏感属性。
    *   **Insurance（保险数据集）**：包含婚姻状况、年龄、职业等属性。
*   **实验场景**：
    *   **直接推荐（Direct Recommendation）**：根据用户 ID 和候选列表直接预测。
    *   **序列推荐（Sequential Recommendation）**：根据用户历史交互序列预测下一项。
*   **对比方法（Baselines）**：
    *   **LLaRA**：作为基础的 LLM 推荐模型。
    *   **P5 / UP5**：经典的基于 Prompt 的推荐模型及其公平性增强版本。
*   **评价指标**：
    *   **效用指标**：Hit@1, Hit@3, Hit@10。
    *   **公平性指标**：反事实泄露间隙（$\Delta CL$），衡量敏感属性的可预测性，越接近 0 越公平。

### 4. 资源与算力
*   **模型骨干**：使用了 **Llama-3.2-1B-Instruct** 作为冻结的基座模型。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但作者强调了该方法的“轻量级”特性，指出投影矩阵是闭式解，且 MoE 适配器仅引入了极少量的额外参数（$O(Kdr)$），暗示其对算力需求较低。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域（电影、保险）的数据集上进行了测试，涵盖了单属性和多属性组合（如性别+年龄+职业）的场景。
*   **消融与对比**：对比了直接推荐和序列推荐两种模式，并与当前最先进的公平性模型 UP5 进行了直接对比。
*   **充分性评价**：实验设计较为充分，涵盖了效用与公平性的权衡分析。通过 $\Delta CL$ 指标客观地展示了属性泄露的减少。然而，实验主要集中在 1B 规模的模型上，对于更大规模模型（如 7B 或 70B）的扩展性实验略显不足。

### 6. 论文的主要结论与发现
*   **公平性显著提升**：该方法能将敏感属性的泄露间隙（$\Delta CL$）降低至接近 0（例如在 MovieLens 上从 17.8% 降至 0.17%）。
*   **效用保持甚至提升**：在显著降低偏见的同时，推荐准确率（Hit Rate）在多个指标上优于 UP5，甚至在某些场景下超过了未做公平性处理的基准模型。
*   **多属性处理能力**：两级门控机制有效地解决了多个相关敏感属性同时存在时的冲突问题。

### 7. 优点
*   **高效轻量**：投影矩阵的计算是代数运算而非迭代优化，极大地降低了训练负担。
*   **非线性偏见消除**：通过 RFF 核化技术，克服了传统线性投影无法处理复杂非线性偏见泄露的局限。
*   **即插即用**：作为适配器模块，可以方便地集成到现有的 PEFT（如 LoRA）流程中。

### 8. 不足与局限
*   **历史依赖性**：模型性能高度依赖于用户的交互历史，对于冷启动用户或历史稀疏场景的公平性表现未做深入探讨。
*   **属性已知假设**：该方法假设在训练阶段敏感属性标签是已知的，但在隐私受限的实际场景中，获取这些标签可能存在困难。
*   **数据集规模**：实验使用的数据集（MovieLens-1M）虽然经典，但在当前 LLM 时代规模相对较小，在大规模工业级数据上的表现有待验证。

（完）
