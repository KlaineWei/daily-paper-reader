---
title: "SOMA: Strategic Orchestration and Memory-Augmented System for Vision-Language-Action Model Robustness via In-Context Adaptation"
title_zh: SOMA：通过上下文自适应提升视觉-语言-动作模型鲁棒性的策略编排与记忆增强系统
authors: "Zhuoran Li, Zhiyang Li, Kaijun Zhou, Jinyu Gu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24060v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 升级VLA策略以实现长期记忆和鲁棒自适应
tldr: 针对视觉-语言-动作（VLA）模型在分布外任务中鲁棒性不足的问题，本文提出SOMA系统。该系统无需微调参数，通过双存储检索增强生成（RAG）、LLM编排器及模型上下文协议（MCP）干预，为冻结的VLA策略提供上下文自适应能力。实验表明，SOMA在多个基准测试中显著提升了任务成功率，尤其在长程任务链中表现优异。
selection_source: fresh_fetch
motivation: 现有的VLA模型因缺乏长期记忆、因果失败归因和动态干预能力，在面对感知噪声和环境变化时鲁棒性受限。
method: 提出一种结合双存储RAG、LLM编排器和MCP干预的在线框架，并辅以离线存储巩固模块来提炼执行轨迹。
result: "在LIBERO-PRO等基准上，SOMA使平均成功率提升了56.6%，在长程任务链中更是实现了89.1%的显著增长。"
conclusion: SOMA证明了通过外部记忆增强和策略编排，可以在不改变模型参数的情况下显著增强VLA模型的鲁棒性和自适应能力。
---

## 摘要
尽管视觉-语言-动作（VLA）模型作为通用机器人控制器具有巨大潜力，但由于缺乏长期记忆、因果故障归因和动态干预能力，它们在分布外（OOD）任务中应对感知噪声和环境变化的鲁棒性仍然受到根本性限制。为了解决这一问题，我们提出了 SOMA，这是一种策略编排与记忆增强系统，旨在无需参数微调的情况下升级冻结的 VLA 策略，以实现鲁棒的上下文自适应。具体而言，SOMA 通过由对比式双记忆检索增强生成（RAG）、归因驱动的大语言模型（LLM）编排器以及可扩展的模型上下文协议（MCP）干预组成的在线流水线运行，同时离线记忆整合模块不断将执行轨迹提炼为可靠的先验知识。在 LIBERO-PRO 和我们提出的 LIBERO-SOMA 基准测试中，对三种骨干模型（pi0、pi0.5 和 SmolVLA）进行的实验评估表明，SOMA 实现了 56.6% 的平均绝对成功率提升。这包括在长时程任务链中 89.1% 的显著绝对提升。项目页面和源代码可在以下网址获取：https://github.com/LZY-1021/SOMA。

## Abstract
Despite the promise of Vision-Language-Action (VLA) models as generalist robotic controllers, their robustness against perceptual noise and environmental variations in out-of-distribution (OOD) tasks remains fundamentally limited by the absence of long-term memory, causal failure attribution, and dynamic intervention capability. To address this, we propose SOMA, a Strategic Orchestration and Memory-Augmented System that upgrades frozen VLA policies for robust in-context adaptation without parameter fine-tuning. Specifically, SOMA operates through an online pipeline of contrastive Dual-Memory Retrieval-Augmented Generation (RAG), an Attribution-Driven Large-Language-Model (LLM) Orchestrator, and extensible Model Context Protocol (MCP) interventions, while an offline Memory Consolidation module continuously distills the execution traces into reliable priors. Experimental evaluations across three backbone models (pi0, pi0.5, and SmolVLA) on LIBERO-PRO and our proposed LIBERO-SOMA benchmarks demonstrate that SOMA achieves an average absolute success rate gain of 56.6%. This includes a significant absolute improvement of 89.1% in long-horizon task chaining. Project page and source code are available at: https://github.com/LZY-1021/SOMA.

---

## 论文详细总结（自动生成）

### SOMA：通过策略编排与记忆增强提升 VLA 模型鲁棒性的系统总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在机器人控制领域展现出巨大潜力，但在面对**分布外（OOD）**任务时表现脆弱。其核心局限性在于：
*   **缺乏长期记忆**：模型通常是“无状态”的，无法从过去的成功或失败中学习。
*   **缺乏因果归因**：无法诊断感知噪声（如光照、遮挡）或指令歧义导致的失败原因。
*   **缺乏动态干预**：现有的 VLA 策略通常是静态的，难以在执行过程中根据环境变化实时调整输入或控制流。
**SOMA** 的提出旨在不微调模型参数的前提下，通过外部系统架构为冻结的 VLA 模型提供“上下文自适应”能力。

#### 2. 方法论：核心思想与关键技术
SOMA 采用了解耦的架构，分为**在线执行**和**离线记忆整合**两个流水线：

*   **对比式双存储 RAG（Dual-Memory RAG）**：
    *   不同于传统只存储成功案例的 RAG，SOMA 同时存储**成功**和**失败**的轨迹。
    *   利用 CLIP 编码器进行相似度检索，为 LLM 提供对比参考，帮助其识别当前任务与历史失败模式的关联。
*   **归因驱动的 LLM 编排器（LLM Orchestrator）**：
    *   使用 **Qwen3-VL-32B** 作为推理核心。
    *   通过分析当前观测与检索到的记忆之间的差异（Gap），诊断失败原因（如感知漂移或规划偏差），并决定调用哪些干预工具。
*   **可扩展的 MCP 干预工具链**：
    *   **Paint-to-Action**：通过视觉覆盖（Overlay）解决视觉域偏移。
    *   **Eraser**：擦除环境中的干扰物（Distractors），减轻因果混淆。
    *   **Prompt-Refiner**：将模糊或口语化的指令重写为策略对齐的规范指令。
    *   **Chaining-Step**：将长程任务分解为短程子任务，并引入重置与缓冲机制。
    *   **Encore**：在检测到执行停滞时，通过关键帧回滚和重试实现物理恢复。
*   **双阶段记忆整合（Memory Consolidation）**：
    *   离线对新轨迹进行初步诊断并存入库。
    *   定期进行跨任务差异分析，修正历史记忆中的错误归因，实现知识的自我进化。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO-PRO**：测试模型在位置交换（Pos）和任务语义偏移（Task）下的鲁棒性。
    *   **LIBERO-SOMA（自提）**：包含五个维度：视觉聚焦、杂物移除、噪声指令、执行脆弱性和长程任务链。
*   **基准模型（Backbones）**：$\pi_0$、$\pi_{0.5}$ 和 SmolVLA。
*   **对比方法**：主要对比各 VLA 模型的原始版本（Base），验证 SOMA 作为插件的提升效果。

#### 4. 资源与算力
*   **推理模型**：使用了 Qwen3-VL-32B 作为编排器。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于 SOMA 强调“无需参数微调（Parameter-free）”，其主要算力消耗集中在 LLM 的在线推理和离线记忆整合阶段。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 25 个多样化任务，每个任务进行 199 次随机初始化实验，以减少偶然性。
*   **消融实验**：针对“双存储银行”的必要性、记忆整合的阶段性效果进行了详细对比。
*   **客观性**：通过平均成功率（SR）和“成功所需平均轮数”等量化指标进行评估，实验设计较为严谨，覆盖了视觉、语言、时序等多个故障维度。

#### 6. 主要结论与发现
*   **显著提升**：SOMA 使 VLA 模型的平均绝对成功率提升了 **56.6%**。
*   **长程任务突破**：在长程任务链中，成功率提升高达 **89.1%**，证明了任务分解与重试机制的有效性。
*   **零样本自适应**：在 LIBERO-PRO 这种基准模型几乎完全失效（成功率接近 0%）的 OOD 场景下，SOMA 成功恢复了约 55% 的性能。
*   **记忆的重要性**：双存储机制比单存储（仅成功案例）能更显著地减少 LLM 的推理轮数，提高干预效率。

#### 7. 优点与亮点
*   **即插即用**：无需对底层 VLA 模型进行昂贵的微调，适用于各种冻结的策略模型。
*   **闭环自进化**：通过离线整合模块，系统能够不断提炼经验，避免了知识停滞。
*   **多模态干预**：MCP 协议允许系统同时从视觉、语言和控制流三个维度对机器人进行实时修正。

#### 8. 不足与局限
*   **推理延迟**：引入 LLM 编排器和多步干预会显著增加单步动作的推理延迟，可能影响对实时性要求极高的动态任务。
*   **LLM 依赖性**：系统的性能高度依赖于底层 LLM（如 Qwen3-VL）的推理能力，若 LLM 归因错误，可能导致错误的干预。
*   **仿真与现实差距**：实验主要在高质量仿真环境（LIBERO）中进行，虽然考虑了噪声，但在真实物理世界中的复杂光影和机械误差下，MCP 工具的可靠性仍需进一步验证。

（完）
