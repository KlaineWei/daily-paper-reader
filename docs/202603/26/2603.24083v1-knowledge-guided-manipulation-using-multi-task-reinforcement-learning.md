---
title: Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning
title_zh: 基于多任务强化学习的知识引导操纵
authors: "Aditya Narendra, Mukhammadrizo Maribjonov, Dmitry Makarov, Dmitry Yudin, Aleksandr Panov"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24083v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于多任务机器人操作的基于模型的策略优化
tldr: 本研究针对部分可观测环境下的多任务机器人操作，提出了KG-M3PO框架。该框架将第一视角视觉与在线3D场景图结合，通过动态关系机制实时更新空间和语义信息。通过将图神经网络编码器与强化学习目标端到端训练，使关系特征直接服务于控制决策。实验证明，该方法在处理遮挡、干扰和布局变化时表现优异，显著提升了任务成功率、采样效率及对新物体的泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决机器人在部分可观测和复杂环境下的多任务操作中，缺乏结构化世界知识导致泛化性和鲁棒性不足的问题。
method: 提出KG-M3PO框架，利用在线3D场景图整合多模态观测，并通过端到端强化学习优化关系表征以指导控制策略。
result: 在包含遮挡和干扰的多种操作任务中，该方法在成功率、采样效率以及对新场景和物体的泛化性上均优于强基准模型。
conclusion: 结构化且持续维护的世界知识是实现可扩展和通用机器人操作的强大归纳偏置，能有效提升复杂环境下的长程行为能力。
---

## 摘要
本文介绍了基于知识图谱的大规模多任务基于模型的策略优化（KG-M3PO），这是一个在部分可观测环境下统一了感知、知识和策略的多任务机器人操纵框架。该方法通过在线 3D 场景图增强了第一视角视觉，将开放词汇检测落地为度量式的关系表示。动态关系机制在每一步更新空间、包含和可供性（affordance）边，并且图神经网络编码器通过强化学习（RL）目标进行端到端训练，使得关系特征直接由控制性能塑造。多种观测模态（视觉、本体感受、语言和基于图的）被编码到一个共享的潜空间中，RL 智能体在此基础上运行以驱动控制循环。策略以轻量级图查询以及视觉和本体感受输入为条件，为决策提供了一个紧凑且具有语义信息的决策状态。在一系列包含遮挡、干扰物和布局变化的操纵任务上的实验表明，该方法相比强基线模型取得了持续的提升：知识条件化智能体实现了更高的成功率、更高的样本效率，以及对新物体和未见场景配置更强的泛化能力。这些结果支持了这样一个前提，即结构化、持续维护的世界知识是实现可扩展、可泛化操纵的强大归纳偏置：当知识模块参与 RL 计算图时，关系表示与控制相对齐，从而在部分可观测性下实现稳健的长程行为。

## Abstract
This paper introduces Knowledge Graph based Massively Multi-task Model-based Policy Optimization (KG-M3PO), a framework for multi-task robotic manipulation in partially observable settings that unifies Perception, Knowledge, and Policy. The method augments egocentric vision with an online 3D scene graph that grounds open-vocabulary detections into a metric, relational representation. A dynamic-relation mechanism updates spatial, containment, and affordance edges at every step, and a graph neural encoder is trained end-to-end through the RL objective so that relational features are shaped directly by control performance. Multiple observation modalities (visual, proprioceptive, linguistic, and graph-based) are encoded into a shared latent space, upon which the RL agent operates to drive the control loop. The policy conditions on lightweight graph queries alongside visual and proprioceptive inputs, yielding a compact, semantically informed state for decision making.   Experiments on a suite of manipulation tasks with occlusions, distractors, and layout shifts demonstrate consistent gains over strong baselines: the knowledge-conditioned agent achieves higher success rates, improved sample efficiency, and stronger generalization to novel objects and unseen scene configurations. These results support the premise that structured, continuously maintained world knowledge is a powerful inductive bias for scalable, generalizable manipulation: when the knowledge module participates in the RL computation graph, relational representations align with control, enabling robust long-horizon behavior under partial observability.

---

## 论文详细总结（自动生成）

### 论文总结：基于知识引导的多任务强化学习机器人操纵 (KG-M3PO)

#### 1. 核心问题与整体含义（研究动机和背景）
在非结构化和日常环境中，机器人操纵面临严重的**部分可观测性（Partial Observability）**问题，如物体被遮挡、放置在容器内或因先前动作而移动。传统的纯反应式策略（直接将像素映射到动作）容易产生状态混淆，且在长程任务中难以进行有效的信用分配。
本文的研究动机在于：为了实现可扩展且通用的机器人操纵，需要将**结构化的世界知识**（持久化的状态表示）、**策略学习**（利用知识消除观测歧义）和**多任务泛化**（跨任务和机器人的表示学习）统一起来。

#### 2. 方法论：核心思想与关键技术
论文提出了 **KG-M3PO**（Knowledge-Guided Massively Multi-task Model-based Policy Optimization）框架，其核心思想是“感知 $\rightarrow$ 知识 $\rightarrow$ 策略”的端到端集成。

*   **在线 3D 场景图 (3DSG)**：利用 BBQ 算法从第一视角 RGB-D 流中实时构建和更新场景图。每个节点代表一个物体，包含 3D 边界框、空间位置和 512 维 CLIP 语义嵌入。
*   **动态关系机制**：每步更新物体间的空间关系（如“在……之上”、“在……之后”）、包含关系和可供性（Affordance）边。每 10 个步骤刷新受影响的子图，以保持低计算开销。
*   **端到端 GNN 编码器**：使用图神经网络（GNN）将场景图编码为紧凑的知识向量 $k_t$。关键创新在于 **GNN 的参数直接通过强化学习（RL）的损失函数进行训练**，这意味着关系特征的提取是直接由控制性能驱动的，而非预训练或独立训练。
*   **多模态融合策略**：将视觉特征（腕部相机）、知识向量（GNN 输出）、任务指令（语言嵌入）和本体感受数据（关节状态）拼接，输入到基于 M3PO 的控制主干中。

#### 3. 实验设计
*   **实验环境**：基于 NVIDIA Isaac Lab (Isaac Sim) 构建的物理仿真环境，支持 GPU 加速的并行渲染和物理模拟。
*   **机器人平台**：Franka Emika Panda (7-DoF) 和 UR5 (6-DoF)。
*   **任务设置**：
    *   **全观测 (FO)**：抓取方块、放置方块、打开柜子。
    *   **部分观测 (PO)**：从墙后抓取物体、移除遮挡物（圆锥）后进行抓取和放置。
*   **对比基准 (Baselines)**：
    *   **算法对比**：PPO, SAC, DreamerV3, TD-MPC2, IMPALA, 以及基础版 M3PO。
    *   **输入对比**：仅相机输入 (Camera-only) vs. 相机+知识图谱 (Camera+KG)。

#### 4. 资源与算力
*   **硬件设施**：单台工作站，配备 **NVIDIA RTX 5070 Ti (16 GB VRAM)** 和 64 GB RAM。
*   **并行规模**：利用 Isaac Lab 的矢量化 API，在单个进程中同时运行 **1024 个并行环境**。
*   **训练时长**：总训练步数达到 4 亿步。
    *   M3PO (仅相机) 训练 4 亿步约需 0.61 GPU-days。
    *   KG-M3PO (相机+KG) 训练 4 亿步约需 0.79 GPU-days（由于增加了图更新和编码开销，计算成本增加了约 30%）。

#### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了 5 种不同难度的操纵任务，并在两种不同的机器人手臂上进行了验证。
*   **消融实验**：专门针对**场景图噪声**进行了鲁棒性测试，包括边界框中心偏移（2%-5%）、尺寸缩放（5%-10%）和旋转噪声（5°）。
*   **客观性**：采用了归一化评分（Normalized Score, 0-1000），将智能体表现与随机策略和专家水平进行锚定，确保了跨任务的可比性。实验结果显示了 KG-M3PO 在不同随机种子下的均值和方差，具有较高的统计可信度。

#### 6. 主要结论与发现
*   **知识引导的必要性**：在部分可观测任务中，纯视觉策略几乎完全失败，而 KG-M3PO 能够通过持久化的场景图维持对遮挡物体的记忆，从而成功完成任务。
*   **效率提升**：在全观测任务中，引入知识图谱显著提升了采样效率（达到相同成功率所需的步数更少）。
*   **端到端优势**：将 GNN 纳入 RL 计算图，使得关系表示能更好地服务于长程信用分配。
*   **鲁棒性**：即使在存在中等程度的几何感知噪声（如检测不准）时，系统性能下降也较为平缓，仍优于纯视觉基线。

#### 7. 优点
*   **架构统一**：成功将符号化的关系知识与亚符号的深度强化学习结合。
*   **动态更新**：在线更新机制解决了静态场景图容易过时的问题。
*   **多任务扩展性**：框架天然支持多任务学习，通过任务 ID 或语言指令即可在不同任务间切换。

#### 8. 不足与局限
*   **真机验证缺失**：所有实验均在仿真环境（Isaac Sim）中完成，尚未在真实物理机器人上验证 sim-to-real 的迁移效果。
*   **感知依赖**：系统高度依赖底层检测器和深度估计的质量，虽然有噪声消融，但在极端光照或复杂堆叠场景下的表现尚不明确。
*   **计算开销**：虽然通过局部更新优化了速度，但相比纯视觉模型，维护 3D 场景图仍带来了额外的计算负担。
*   **动态物体限制**：目前主要针对静态或由机器人移动的物体，对于环境中存在其他自主移动干扰物的情况处理能力有限。

（完）
