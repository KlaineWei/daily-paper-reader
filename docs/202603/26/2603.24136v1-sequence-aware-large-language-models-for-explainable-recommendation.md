---
title: Sequence-aware Large Language Models for Explainable Recommendation
title_zh: 序列感知的大语言模型用于可解释推荐
authors: "Gangyi Zhang, Runzhe Teng, Chongming Gao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24136v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 基于序列感知LLM的可解释推荐框架
tldr: 本研究针对现有大语言模型在可解释推荐中忽视用户行为序列动态且评估指标与实际效用脱节的问题，提出了SELLER框架。该框架通过双路径编码器捕捉用户行为与物品语义，并利用混合专家适配器实现信号对齐。此外，引入了结合文本质量与推荐效用的统一评估体系。实验证明，SELLER在解释质量和实际推荐效用上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有基于大语言模型的可解释推荐方法往往忽略了用户行为的序列动态，且缺乏与实际推荐效用对齐的评估指标。
method: 提出了SELLER框架，利用双路径编码器捕捉行为与语义，并通过混合专家适配器与统一评估体系提升解释的准确性与效用。
result: 在公开基准数据集上的实验表明，SELLER在解释生成的文本质量和对推荐结果的实际贡献方面均显著优于现有方法。
conclusion: SELLER通过整合序列感知建模与效用感知评估，有效增强了大语言模型在可解释推荐系统中的实用性和表现。
---

## 摘要
大语言模型（LLMs）在为推荐系统生成自然语言解释方面展现出强大的潜力。然而，现有方法往往忽略了用户行为的序列动态，且依赖于与实际效用不匹配的评估指标。我们提出了 SELLER（基于序列感知 LLM 的可解释推荐框架），该框架将解释生成与效用感知评估相结合。SELLER 结合了一个双路径编码器（同时捕捉用户行为和物品语义）以及一个混合专家（MoE）适配器，以将这些信号与 LLMs 对齐。一个统一的评估框架通过文本质量及其对推荐结果的影响来评估解释。在公开基准测试上的实验表明，SELLER 在解释质量和实际效用方面始终优于以往的方法。

## Abstract
Large Language Models (LLMs) have shown strong potential in generating natural language explanations for recommender systems. However, existing methods often overlook the sequential dynamics of user behavior and rely on evaluation metrics misaligned with practical utility. We propose SELLER (SEquence-aware LLM-based framework for Explainable Recommendation), which integrates explanation generation with utility-aware evaluation. SELLER combines a dual-path encoder-capturing both user behavior and item semantics with a Mixture-of-Experts adapter to align these signals with LLMs. A unified evaluation framework assesses explanations via both textual quality and their effect on recommendation outcomes. Experiments on public benchmarks show that SELLER consistently outperforms prior methods in explanation quality and real-world utility.

---

## 论文详细总结（自动生成）

### 论文总结：SELLER —— 序列感知的大语言模型用于可解释推荐

#### 1. 核心问题与研究动机
*   **核心问题**：现有的基于大语言模型（LLM）的可解释推荐（ER）方法存在两个主要缺陷：
    1.  **忽视序列动态**：大多模型依赖静态输入，无法捕捉用户随时间演变的偏好（如从“动作片”转向“科幻惊悚片”）。
    2.  **评估指标脱节**：常用的 BLEU 或 BERTScore 仅衡量文本表面相似度，无法评估解释是否真正帮助用户做出决策，且以评论作为基准（Ground Truth）往往偏向物品特征而非用户的决策逻辑。
*   **研究背景**：为了提高推荐系统的透明度和用户信任，需要生成既符合用户行为演变规律，又具有实际推荐效用的解释。

#### 2. 方法论
SELLER 框架由两个核心组件组成：
*   **序列感知解释生成器 (SEG)**：
    *   **双路径序列编码**：采用 **SASRec** 捕捉隐式行为模式（Behavioral Path），采用 **Sentence-T5** 提取物品描述的显式语义信号（Semantic Path）。
    *   **混合专家 (MoE) 适配器**：引入并行 MoE 适配器，通过门控网络动态融合行为和语义特征，将异构序列表示对齐到 LLM 的语义空间。
    *   **序列感知微调**：将适配后的嵌入注入 LLM（如 LLaMA2-7B）的输入层和注意力机制中，生成个性化解释。
*   **统一评估框架 (EER)**：
    *   **解释增强推荐器**：利用生成的解释作为额外信号。通过**超网络 (Hypernetwork)** 生成动态适配器参数，调整推荐模型（SASRec）的状态。
    *   **效用评估逻辑**：如果生成的解释能显著提升下游推荐任务的准确性（如 Recall, NDCG），则认为该解释具有高“效用”。

#### 3. 实验设计
*   **数据集**：
    *   **Yelp**：商业服务互动数据，包含丰富的元数据和评论。
    *   **KuaiRec**：短视频推荐数据，具有全观测的互动矩阵。
*   **基准方法 (Benchmarks)**：
    *   **PETER**：基于 ID 的预训练语言模型微调。
    *   **PEPLER**：基于连续提示（Prompt）学习的解释生成。
    *   **XRec**：结合协同过滤信号与显式特征的 LLM 方法。
    *   **对照组**：Random（随机解释）和 Empty（无解释）。
*   **评估指标**：
    *   **生成质量**：BLEU、BERTScore。
    *   **实际效用**：Recall@5/10、NDCG@5/10。

#### 4. 资源与算力
*   **模型规模**：使用了 **LLaMA2-7B** 作为骨干 LLM。
*   **训练细节**：
    *   SEG 训练 5 个 epoch，Batch Size 为 8，学习率 1e-4。
    *   EER 训练 50 个 epoch，Batch Size 为 256，学习率 5e-4。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提到了训练和推理的相对效率（SELLER 的推理耗时约为 XRec 的 44.8%）。

#### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域的代表性数据集上进行了测试。
*   **消融实验**：针对行为嵌入（BE）、语义嵌入（SE）、类别文本（CT）等组件进行了详细的消融分析，验证了双路径编码的必要性。
*   **效率分析**：对比了不同方法的训练与推理时间。
*   **充分性评价**：实验设计较为全面，不仅对比了 SOTA 方法，还通过超网络验证了不同解释类型（类别级 vs 意图级）的影响，实验结果具有统计说服力。

#### 6. 主要结论与发现
*   **性能领先**：SELLER 在所有指标上均优于现有方法，尤其在 Yelp 数据集上，Recall@10 提升了 18.75%，NDCG@10 提升了 27.52%。
*   **序列的重要性**：捕捉用户偏好的演变对于生成高质量解释至关重要，单纯依赖静态 ID 或嵌入会限制模型的表达能力。
*   **指标背离现象**：实验发现，文本相似度（BLEU）的提升并不总是等同于推荐效用的提升，证明了引入效用感知评估框架的必要性。

#### 7. 优点
*   **创新性架构**：通过双路径编码和 MoE 适配器，优雅地解决了异构推荐信号与 LLM 的对齐问题。
*   **评估闭环**：提出了“解释增强推荐”的思路，将解释的生成与实际推荐效果挂钩，解决了 ER 领域长期存在的评估难问题。
*   **高效性**：相比于输入大量文本描述的方法（如 XRec），SELLER 通过嵌入注入方式显著降低了计算开销。

#### 8. 不足与局限
*   **基准解释的偏差**：训练用的 Ground Truth 是由 Claude 3.5 生成的，可能存在 LLM 固有的偏见，未必完全等同于用户的真实意图。
*   **缺乏在线实验**：所有评估均在离线数据集上完成，未进行真实场景下的用户主观调研（User Study），难以衡量用户对解释的真实信任度。
*   **模态局限**：目前主要处理文本和行为序列，尚未扩展到图像、视频等多模态信息的解释生成。

（完）
