---
title: "3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models"
title_zh: 3D-Mix for VLA：一种将基于 VGGT 的 3D 信息集成到视觉-语言-动作模型中的即插即用模块
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Haishan Liu, Changti Wu, Hang Yuan, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24393v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 将3D信息集成到视觉-语言-动作模型中
tldr: 针对视觉-语言-动作（VLA）模型在3D空间感知能力的不足，本文提出了一种名为3D-Mix的即插即用模块。通过系统研究九种集成方案，发现语义调节的门控融合效果最佳。3D-Mix能在不改变原有模型结构的情况下，将VGGT提供的3D几何信息有效融入多种VLA架构，显著提升了机器人在复杂任务中的空间智能和泛化性能。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要基于2D数据训练，导致其在机器人操纵任务中缺乏必要的3D空间感知和几何理解能力。
method: 提出3D-Mix模块，通过语义调节的门控融合机制，将VGGT提取的3D几何特征与MLLM的2D语义特征进行自适应集成。
result: "在多个基准测试和九种模型变体上，3D-Mix均表现出性能增益，在SIMPLER分布外测试中平均成功率提升了7.0%。"
conclusion: 3D-Mix作为一种通用的即插即用模块，为提升VLA系统的空间智能和跨任务泛化能力提供了一种规范且有效的路径。
---

## 摘要
视觉-语言-动作（VLA）模型利用多模态大语言模型（MLLM）进行机器人控制，但近期研究表明，由于主要在 2D 数据上进行训练，MLLM 表现出有限的空间智能，导致其在操作任务中的 3D 感知能力不足。虽然近期的方法引入了如 VGGT 等专门的 3D 视觉模型来增强空间理解，但它们采用了多种集成机制且缺乏系统性研究，使得最优融合策略尚不明确。我们在标准化基准上进行了一项全面的初步研究，对比了九种 VGGT 集成方案，并发现语义条件门控融合在所有九种评估方案中表现最强；该方案能根据任务上下文自适应地平衡 2D 语义特征和 3D 几何特征。我们提出了 3D-Mix，这是一个即插即用模块，可以集成到多种 VLA 架构（如 GR00T 风格和 π 风格）中，而无需修改现有的 MLLM 或动作专家组件。在 SIMPLER 和 LIBERO 基准上针对六个 MLLM 系列（九个模型变体，参数量为 2B-8B）进行的实验表明，3D-Mix 带来了持续的性能提升，在所有九个 GR00T 风格变体中，其在分布外（OOD）SIMPLER 基准上的平均提升达 +7.0%，为增强 VLA 系统的空间智能建立了一种原则性方法。

## Abstract
Vision-Language-Action (VLA) models leverage Multimodal Large Language Models (MLLMs) for robotic control, but recent studies reveal that MLLMs exhibit limited spatial intelligence due to training predominantly on 2D data, resulting in inadequate 3D perception for manipulation tasks. While recent approaches incorporate specialized 3D vision models such as VGGT to enhance spatial understanding, they employ diverse integration mechanisms without systematic investigation, leaving the optimal fusion strategy unclear. We conduct a comprehensive pilot study comparing nine VGGT integration schemes on standardized benchmarks and find that semantic-conditioned gated fusion, which adaptively balances 2D semantic and 3D geometric features based on task context, achieved the strongest performance among all nine evaluated fusion schemes in our pilot study. We present 3D-Mix, a plug-and-play module that integrates into diverse VLA architectures (GR00T-style and $π$-style) without modifying existing MLLM or action expert components. Experiments across six MLLM series (nine model variants, 2B--8B parameters) on SIMPLER and LIBERO show that 3D-Mix delivers consistent performance gains, averaging +7.0% on the out-of-domain (OOD) SIMPLER benchmark across all nine GR00T-style variants, establishing a principled approach for enhancing spatial intelligence in VLA systems.

---

## 论文详细总结（自动生成）

### 论文总结：3D-Mix for VLA

#### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型主要依赖于在海量 2D 图像-文本数据上预训练的多模态大语言模型（MLLM）。然而，由于缺乏显式的 3D 几何监督，这些模型在处理需要精确空间推理、深度感知和物体抓取姿态估计的机器人操纵任务时表现不佳。
*   **核心问题**：虽然已有研究尝试引入专门的 3D 视觉模型（如 VGGT）来增强空间能力，但如何将 3D 几何特征与 2D 语义特征进行最优融合仍缺乏系统性研究。本文旨在探索并确定一种通用的、高效的 3D 信息集成策略。

#### 2. 论文提出的方法论
*   **核心思想**：提出 **3D-Mix** 模块，采用**语义调节的自适应门控（Semantic-conditioned Adaptive Gating）**机制。其核心逻辑是：不同的任务对语义理解和几何精度的依赖程度不同，因此应根据任务上下文动态调整 2D 和 3D 特征的融合比例。
*   **关键技术细节**：
    *   **特征提取与投影**：使用冻结的 VGGT 提取几何特征，并通过线性层投影到 MLLM 的隐藏空间。
    *   **语义上下文提取**：对 MLLM 的隐藏状态进行平均池化，生成全局语义摘要。
    *   **门控机制**：将全局语义上下文与位置特定的几何特征拼接，通过一个可学习的门控网络（Sigmoid 激活）计算每个 token 位置的融合权重。
    *   **自适应融合**：根据门控权重对语义和几何特征进行加权混合，生成富含空间信息的增强序列。
*   **架构集成**：
    *   **GR00T 风格**：作为 MLLM 与动作专家（DiT）之间的桥梁，进行单层融合。
    *   **π 风格**：支持层级化融合，将 3D 特征独立注入到 DiT 的多个交叉注意力层中。

#### 3. 实验设计
*   **初步研究（Pilot Study）**：对比了 9 种融合方案，包括 AE-Fusion（动作专家端融合）、Early Fusion（输入端融合）、Gated Fusion（门控融合）、Spatial Forcing（空间强迫对齐）等。
*   **数据集与场景**：
    *   **SIMPLER**：一个 Real-to-Sim 的分布外（OOD）基准测试，衡量模型在未见过的物体配置和视觉变化下的泛化能力。
    *   **LIBERO**：包含四个任务套件（空间、物体、目标、长程任务），用于评估域内多任务学习能力。
*   **对比方法**：以 Qwen2.5-VL、Qwen3-VL、RoboBrain 等 6 个系列的 9 种 MLLM 变体作为基座模型，对比加入 3D-Mix 前后的性能差异。

#### 4. 资源与算力
*   **算力设备**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练配置**：采用 DeepSpeed ZeRO-2 优化器。
*   **训练时长**：每个模型训练 **60,000 步**。
*   **学习率**：MLLM 主干为 $10^{-5}$，动作专家及融合模块为 $10^{-4}$。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2B 到 8B 参数规模的 9 种模型变体，跨越了两种主流 VLA 架构（GR00T-style 和 π-style）。
*   **消融实验**：进行了 VGGT 冻结 vs. 微调、3D 信息敏感度（噪声干扰）、稀疏层融合深度等消融实验。
*   **充分性评价**：实验设计非常充分且客观。通过初步研究筛选出最优方案，再在多种基座模型上验证通用性，并利用 OOD 测试（SIMPLER）证明了 3D 信息对泛化能力的实质性提升，而非简单的参数量增加。

#### 6. 论文的主要结论与发现
*   **最优策略**：语义调节的门控融合（Gated Fusion）在所有方案中表现最佳，能够最有效地平衡语义与几何信息。
*   **性能提升**：3D-Mix 在 GR00T 风格模型上使 SIMPLER 平均成功率提升了 **7.0%**，其中 RynnBrain-8B 提升高达 **12.51%**。
*   **通用性**：该模块是即插即用的，无需修改 MLLM 或动作专家的内部结构，且在不同规模和架构的模型上均表现出一致的增益。
*   **3D 信息的必要性**：消融实验证明，若将 3D 特征替换为零向量或高斯噪声，性能会显著下降，证实了 VGGT 提供的几何信息的真实价值。

#### 7. 优点
*   **系统性强**：首次对 VLA 中的 3D 融合策略进行了详尽的对比研究，填补了行业空白。
*   **即插即用**：设计轻量且模块化，极大地降低了现有 VLA 模型升级空间感知能力的门槛。
*   **泛化性验证**：重点关注 OOD 性能（SIMPLER），这对于实际机器人应用中的鲁棒性至关重要。

#### 8. 不足与局限
*   **推理开销**：虽然 3D-Mix 模块本身很轻量，但引入额外的 VGGT 模型会增加推理时的计算负担和内存占用。
*   **VGGT 依赖**：性能高度依赖于预训练 VGGT 的质量，且实验中发现冻结 VGGT 效果更好，这可能限制了模型在极端特殊场景下的进一步微调潜力。
*   **实机验证**：论文主要基于仿真环境（SimplerEnv 和 LIBERO）进行评估，缺乏在真实物理机器人上的大规模部署数据。

（完）
