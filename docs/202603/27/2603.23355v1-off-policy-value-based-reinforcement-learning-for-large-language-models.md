---
title: Off-Policy Value-Based Reinforcement Learning for Large Language Models
title_zh: 大语言模型的离策基于价值的强化学习
authors: "Peng-Yuan Wang, Ziniu Li, Tian Xu, Bohan Yang, Tian-Shuo Liu, ChenYang Wang, Xiong-Hui Chen, Yi-Chen Li, Tianyun Yang, Congliang Chen, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23355v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 针对大语言模型长程任务的强化学习扩展
tldr: 针对大语言模型强化学习中策略梯度方法采样效率低的问题，本文提出了一种基于价值的离线策略强化学习框架 ReVal。该方法通过结合步进式内部一致性信号与轨迹级结果验证信号，利用贝尔曼更新和经验回放机制实现数据的高效复用。实验证明，ReVal 在数学推理任务中收敛更快且性能优于 GRPO，为 LLM 训练提供了一种高效的替代方案。
selection_source: fresh_fetch
motivation: 现有的 LLM 强化学习多采用在线策略方法，导致昂贵的采样数据无法重复利用，训练效率低下。
method: 提出 ReVal 框架，通过贝尔曼更新结合步进式一致性与轨迹级验证信号，并利用经验回放缓冲区实现离线策略学习。
result: "在 DeepSeek-R1-Distill-1.5B 模型上，ReVal 在 AIME24 和 GPQA 任务中分别比 GRPO 提升了 2.7% 和 4.5%，且收敛速度更快。"
conclusion: 基于价值的强化学习是提升大语言模型训练效率和性能的一种切实可行的策略梯度替代方案。
---

## 摘要
提高数据利用效率对于在生成轨迹成本高昂的长程任务中扩展强化学习（RL）至关重要。然而，目前主流的大语言模型（LLM）强化学习方法大多是同策（on-policy）的：它们对每批数据仅更新一次后便将其丢弃，随后收集新样本，导致样本效率低下。在这项工作中，我们探索了一种替代的基于价值的 LLM 强化学习框架，该框架天然支持离策（off-policy）学习。我们提出了 ReVal，这是一种基于贝尔曼更新（Bellman-update）的方法，它结合了捕捉内部一致性的步进式信号和源自结果验证的轨迹级信号。ReVal 天然支持基于经验回放池（replay-buffer）的训练，从而实现了对过去轨迹的高效重用。在标准数学推理基准测试上的实验表明，ReVal 不仅收敛更快，而且在最终性能上也优于 GRPO。在 DeepSeek-R1-Distill-1.5B 上，ReVal 提高了训练效率，相比 GRPO 在 AIME24 上提升了 2.7%，在域外基准测试 GPQA 上提升了 4.5%。这些结果表明，基于价值的强化学习是 LLM 训练中基于策略方法的实用替代方案。

## Abstract
Improving data utilization efficiency is critical for scaling reinforcement learning (RL) for long-horizon tasks where generating trajectories is expensive. However, the dominant RL methods for LLMs are largely on-policy: they update each batch of data only once, discard it, and then collect fresh samples, resulting in poor sample efficiency. In this work, we explore an alternative value-based RL framework for LLMs that naturally enables off-policy learning. We propose ReVal, a Bellman-update-based method that combines stepwise signals capturing internal consistency with trajectory-level signals derived from outcome verification. ReVal naturally supports replay-buffer-based training, allowing efficient reuse of past trajectories. Experiments on standard mathematical reasoning benchmarks show that ReVal not only converges faster but also outperforms GRPO in final performance. On DeepSeek-R1-Distill-1.5B, ReVal improves training efficiency and achieves improvement of 2.7% in AIME24 and 4.5% in out-of-domain benchmark GPQA over GRPO. These results suggest that value-based RL is a practical alternative to policy-based methods for LLM training.

---

## 论文详细总结（自动生成）

以下是对论文《Off-Policy Value-Based Reinforcement Learning for Large Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）的主流强化学习方法（如 PPO、GRPO）大多是**同策（On-policy）**的。这意味着每批生成的采样数据在更新一次参数后即被丢弃，导致数据利用率极低。在长程推理任务中，自动生成轨迹的成本非常高昂，这种低效性成为了扩展 LLM 推理能力的瓶颈。
*   **核心问题**：如何构建一个既能保持低计算开销（无需额外价值网络），又能实现**离策（Off-policy）**数据复用的强化学习框架，以提升训练效率和最终性能。

### 2. 方法论：ReVal 框架
*   **核心思想**：将 LLM 的 Logits 直接解释为 Q 值（Action-Value），从而在单一模型内统一策略和价值函数。通过引入贝尔曼更新（Bellman Update）和经验回放机制，实现对历史轨迹的高效复用。
*   **关键技术细节**：
    *   **Logit-as-Q 参数化**：利用预训练模型已有的 Logits 作为隐式 Q 函数，避免了引入 Critic 网络带来的额外显存和计算开销。
    *   **校准初始化（Calibrated Initialization）**：针对前人工作（如 TBRM）在无奖励时会产生策略漂移的问题，ReVal 引入了**奖励塑造（Reward Shaping）**。通过在贝尔曼算子中加入参考策略的偏移项，确保在奖励为零时模型能退化回参考策略。
    *   **轨迹级贝尔曼残差损失**：设计了一种结合了步进式内部一致性（Token 间关系）和轨迹级结果验证（最终答案正确性）的损失函数。
    *   **经验回放池（Replay Buffer）**：采用 FIFO（先进先出）缓冲区存储历史轨迹。在每次迭代中，模型不仅从当前策略采样，还从缓冲区抽取旧数据进行多次梯度更新。

### 3. 实验设计
*   **实验场景**：专注于数学推理任务，这类任务具有可验证的客观奖励（Verifiable Rewards）。
*   **模型与基准（Benchmark）**：
    *   **模型**：DeepSeek-R1-Distill-1.5B、Qwen2.5-Math-7B。
    *   **数据集**：AIME (2024/2025)、AMC、MATH、MINERVA、Olympiad Bench、GPQA（用于测试域外泛化能力）。
*   **对比方法**：
    *   **GRPO**：目前主流的 Actor-only 同策策略梯度方法。
    *   **TBRM**：一种基于价值但通常以同策方式运行的基准方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及总卡数。
*   **训练效率指标**：文中提到 ReVal 在达到相同性能时，收敛速度比 GRPO 快约 **4.3 倍**。在 N=1（每轮仅采样 1 条轨迹）的设定下，ReVal 相比 GRPO 减少了约 18% 的总训练时间（例如从 7.5 小时缩短至 6.2 小时）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同规模和系列的开源模型，并在 7 个主流推理榜单上进行了验证。
*   **消融实验**：非常充分。作者详细探讨了：
    1.  **数据复用频率**：验证了增加离策更新步数对加速收敛的作用。
    2.  **参考策略更新**：研究了定期重置参考模型对缓解 KL 约束过紧的影响。
    3.  **超参数 $\beta$**：分析了奖励缩放对训练稳定性的影响。
    4.  **奖励设计**：对比了 0/1 奖励、±1 奖励及归一化优势（Advantage）的效果。
*   **客观性**：实验在相同的生成预算和 Prompt 集合下进行，对比具有较高的公平性。

### 6. 主要结论与发现
*   **性能超越**：ReVal 在多个榜单上显著优于 GRPO。在 1.5B 模型上，AIME24 提升 2.7%，GPQA 提升 4.5%。
*   **采样效率极高**：在采样受限（N=1）的情况下，ReVal 的优势扩大，证明了离策复用在昂贵采样场景下的价值。
*   **泛化能力强**：在域外基准 GPQA 上的显著提升表明，基于价值的学习能帮助模型学到更稳健的推理逻辑。
*   **收敛更快**：通过对同一批数据的多次学习，ReVal 显著减少了所需的生成轮数。

### 7. 优点
*   **架构优雅**：无需额外的 Critic 网络，实现了单模型下的价值学习，非常适合大规模 LLM 训练。
*   **理论修正**：通过奖励塑造解决了价值学习中的初始化校准问题，增强了训练稳定性。
*   **实用性强**：直接兼容现有的分布式训练框架（如 Verl），且在低资源（少采样）场景下表现优异。

### 8. 不足与局限
*   **任务覆盖面**：目前仅在数学推理任务上验证，尚未证明其在缺乏硬性验证信号（如创意写作、主观对话）的任务中的有效性。
*   **回放策略简单**：目前仅使用了简单的 FIFO 缓冲区，未探索如“优先经验回放（PER）”等更高级的离策技术。
*   **超参敏感度**：$\beta$ 参数与响应长度高度耦合，不同长度的任务可能需要精细的超参调优。

（完）
