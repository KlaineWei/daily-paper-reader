---
title: "UniScale: Synergistic Entire Space Data and Model Scaling for Search Ranking"
title_zh: UniScale：搜索排序中协同的全空间数据与模型缩放
authors: "Liren Yu, Caiyuan Li, Feiyi Dong, Tao Zhang, Zhixuan Zhang, Dan Ou, Haihong Tang, Bo Zheng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24226v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 推荐系统中的缩放定律研究与模型缩放
tldr: 针对工业搜索排序中模型规模增加带来的边际收益递减和数据异构性挑战，本文提出UniScale框架。该框架通过协同设计数据与架构，引入全空间样本系统（ES$^3$）以扩展高质量训练信号，并结合异构层次样本融合Transformer（HHSFT）来有效建模复杂分布。在大型电商搜索平台的实验证明，UniScale显著提升了业务指标并展现出清晰的扩展趋势。
selection_source: fresh_fetch
motivation: 现有的缩放定律研究多关注架构而忽视了数据与架构的协同，导致单纯增加模型参数的收益递减且难以处理复杂的数据异构性。
method: 提出了包含全空间样本系统（ES$^3$）和异构层次样本融合Transformer（HHSFT）的协同设计框架，通过扩展训练信号和优化特征交互来提升性能。
result: 在大规模真实电商搜索平台上的实验表明，UniScale在关键业务指标上取得了显著提升，并表现出明显的模型扩展效应。
conclusion: 通过数据与架构的深度协同优化，UniScale成功突破了仅靠结构调整的性能瓶颈，为搜索排序系统的规模化提供了新路径。
---

## 摘要
近期大语言模型（LLMs）的进展激发了工业界搜索、广告和推荐系统中缩放法则（scaling law）研究的热潮。然而，现有方法主要集中在架构改进上，忽视了数据与架构设计之间关键的协同作用。我们观察到，仅缩放模型参数表现出收益递减，即随着模型规模的增加，性能的边际收益稳步下降，且由复杂异构数据分布引起的性能退化通常难以仅通过模型设计来修复。在本文中，我们提出了 UniScale 来解决这些局限性。这是一个新颖的协同设计框架，通过联合优化数据和架构来释放模型缩放的全部潜力，其包含两个核心部分：(1) ES$^3$（全空间样本系统），一个高质量的数据缩放系统，它通过由层级标签归因构建的全局监督信号从域内请求上下文，以及在搜索领域相似内容曝光环境下对齐用户决策本质的跨域样本，扩展了传统采样策略之外的训练信号；(2) HHSFT（异构层级样本融合 Transformer），一种新颖的架构，旨在通过异构层级特征交互和全空间用户兴趣融合，有效建模缩放数据的复杂异构分布并利用全空间用户行为数据，从而突破仅靠结构调整的模型调优性能瓶颈。在大规模真实世界电子商务搜索平台上的广泛实验表明，UniScale 通过数据与架构的协同设计实现了显著改进，并表现出明显的缩放趋势，在关键业务指标上带来了实质性提升。

## Abstract
Recent advances in Large Language Models (LLMs) have inspired a surge of scaling law research in industrial search, advertising, and recommendation systems. However, existing approaches focus mainly on architectural improvements, overlooking the critical synergy between data and architecture design. We observe that scaling model parameters alone exhibits diminishing returns, i.e., the marginal gain in performance steadily declines as model size increases, and that the performance degradation caused by complex heterogeneous data distributions is often irrecoverable through model design alone. In this paper, we propose UniScale to address these limitation, a novel co-design framework that jointly optimizes data and architecture to unlock the full potential of model scaling, which includes two core parts: (1) ES$^3$ (Entire-Space Sample System), a high-quality data scaling system that expands the training signal beyond conventional sampling strategies from both intra-domain request contexts with global supervised signal constructed by hierarchical label attribution and cross-domain samples aligning with the essence of user decision under similar content exposure environment in search domain; and (2) HHSFT (Heterogeneous Hierarchical Sample Fusion Transformer), a novel architecture designed to effectively model the complex heterogeneous distribution of scaled data and to harness the entire space user behavior data with Heterogeneous Hierarchical Feature Interaction and Entire Space User Interest Fusion, thereby surpassing the performance ceiling of structure-only model tuning. Extensive experiments on large-scale real world E-commerce search platform demonstrate that UniScale achieves significant improvements through the synergistic co-design of data and architecture and exhibits clear scaling trends, delivering substantial gains in key business metrics.

---

## 论文详细总结（自动生成）

这篇论文由阿里巴巴淘宝与天猫技术团队提出，旨在解决工业搜索排序系统中模型缩放（Scaling）面临的边际收益递减问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在搜索和推荐领域，单纯增加模型参数（模型缩放）往往会遇到瓶颈，表现为性能提升放缓。作者认为这是因为训练数据的“信息容量”有限，且复杂的异构数据分布（如搜索与推荐数据的差异）会导致模型难以通过单一架构优化来消化。
*   **研究背景**：受大语言模型（LLM）缩放定律的启发，工业界尝试通过增大模型规模提升排序效果，但忽视了**数据与架构的协同设计（Co-design）**。

### 2. 论文提出的方法论：UniScale 框架
UniScale 包含两个核心组件，实现了数据与架构的深度耦合：

*   **ES³ (Entire-Space Sample System，全空间样本系统)**：
    *   **域内样本与标签扩展**：引入未曝光样本作为“简单负例”以缓解选择偏差；利用**层级标签归因（Hierarchical Label Attribution）**，将跨域（如推荐页）的转化信号回溯给搜索样本，解决标签稀疏问题。
    *   **跨域样本“搜索化”（Searchification）**：将非搜索行为（如推荐点击）转化为搜索对齐的样本。通过**负采样生成模块**保持语义一致性，并利用**特征对齐模块**（历史查询重用、共现统计、语义检索）为非搜索样本伪造语义关联的 Query。
*   **HHSFT (Heterogeneous Hierarchical Sample Fusion Transformer，异构层级样本融合 Transformer)**：
    *   **HHFI (异构层级特征交互)**：将特征按语义分块并 Token 化。采用**Token 特定投影矩阵（HFA）**保留异构特征的分布特性，再通过**全局特征注意力（GFA）**建模高阶交互。
    *   **ESUIF (全空间用户兴趣融合)**：使用**领域路由专家融合（DREF）**分离共享知识与领域特有知识；引入**领域感知个性化门控注意力（DAPGA）**，以目标域（搜索）为中心选择性地吸收跨域兴趣。

### 3. 实验设计
*   **数据集/场景**：基于淘宝真实业务场景，构建了包含数十亿级用户项交互的工业数据集。
*   **Benchmark 与对比方法**：
    *   **基准模型**：DLRM-MLP, DCNv2。
    *   **SOTA 模型**：AutoInt, HiFormer, Wukong, RankMixer。
*   **评估指标**：离线使用 AUC、GAUC、Hitrate@5；在线使用购买量（Purchase）和 GMV。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **工程优化**：提到了为了支持大规模部署所做的底层优化，包括：异步数据读取、特征预哈希（Pre-hashing）、基于 RDMA 的高性能集群通信、FP16 量化、算子融合（Fused Kernel）等。这些优化使 GPU 推理成本降低了约 55%，训练开销降低了约 40%。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模离线对比实验、消融实验（针对 HHFI 和 ESUIF 模块）以及为期 10 天的在线 A/B 测试。
*   **充分性与客观性**：
    *   **消融实验**：详细验证了从 MLP 到 Transformer、再到异构注意力机制的增益。
    *   **缩放定律验证**：通过 1x 到 4x 参数规模的对比，验证了 UniScale 在数据与模型协同缩放下的非线性增长趋势。
    *   **公平性**：对比了相同参数预算下的不同架构表现，实验设计较为严谨。

### 6. 主要结论与发现
*   **协同效应**：模型架构的升级必须配合高质量数据的扩展才能发挥最大潜力。当模型规模扩大时，UniScale 利用全空间数据的优势会进一步放大（AUC 增益从 1x 规模的 0.12% 扩大到 4x 规模的 0.32%）。
*   **业务价值**：在线 A/B 测试显示，UniScale 相比生产环境基线提升了 **1.70% 的购买量**和 **2.04% 的 GMV**。
*   **架构优势**：HHSFT 的层级设计比传统的平铺式 Transformer 能更有效地分配算力给高阶特征融合。

### 7. 优点
*   **系统性强**：不仅关注模型结构，还系统性地解决了工业界数据闭环中的选择偏差、曝光偏差和跨域盲点。
*   **工程落地性高**：针对大规模在线推理进行了深度的算子级优化，证明了复杂 Transformer 模型在超大规模搜索系统中的可行性。
*   **数据处理创新**：提出的“样本搜索化”方法为如何利用异构跨域数据提供了清晰的工业路径。

### 8. 不足与局限
*   **算力细节缺失**：缺乏具体的硬件资源消耗数据，外部研究者难以评估其训练成本。
*   **伪标签风险**：在“搜索化”过程中为非搜索样本伪造 Query 可能会引入语义噪声，虽然论文提到了相似性约束，但长尾样本的对齐质量仍存疑。
*   **领域依赖**：该框架高度依赖于阿里巴巴这种具有多业务场景（搜索、推荐、详情页等）的生态系统，对于业务单一的平台，其跨域融合的收益可能受限。

（完）
