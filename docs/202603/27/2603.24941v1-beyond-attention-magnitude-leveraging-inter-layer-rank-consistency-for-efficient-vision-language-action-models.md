---
title: "Beyond Attention Magnitude: Leveraging Inter-layer Rank Consistency for Efficient Vision-Language-Action Models"
title_zh: 超越注意力幅度：利用层间排名一致性构建高效视觉-语言-动作模型
authors: "Peiju Liu, Jinming Liu, Xipeng Qiu, Xuanjing Huang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24941v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 通过令牌减少实现机器人操作的高效视觉-语言-动作（VLA）模型
tldr: "视觉-语言-动作（VLA）模型在机器人操控中表现出色，但密集的视觉标记导致推理延迟高。本文指出传统的基于注意力权重的标记缩减方法存在局限性，进而提出TIES框架。TIES利用层间标记排名的连贯性进行动态选择，无需额外训练即可平衡注意力强度与排名一致性。该方法在减少78%标记使用的同时，将成功率提升了6%，展现了极高的效率与泛化性。"
selection_source: fresh_fetch
motivation: 现有的基于注意力权重的标记缩减方法在VLA模型中表现不稳定，且高权重标记有时反而会损害策略性能。
method: 提出TIES框架，通过引入层间排名一致性（Tau引导）来动态、自适应地筛选关键视觉标记，且无需重新训练模型。
result: "在CogACT + SIMPLER基准测试中，TIES在减少78%视觉标记的情况下，将平均任务成功率提升了6%。"
conclusion: 通过利用层间排名一致性，可以在显著降低VLA模型计算开销的同时，增强其在复杂任务中的鲁棒性和性能。
---

## 摘要
视觉-语言-动作（VLA）模型在机器人操控方面表现出色，但由于处理密集的视觉标记（tokens），面临显著的推理延迟。现有的标记削减方法主要依赖注意力幅度作为静态选择标准。在这项工作中，我们对这一假设提出了挑战，揭示了高注意力标记具有任务依赖性，甚至可能降低策略性能。为解决这一问题，我们引入了 TIES（Tau 引导的层间高效选择），这是一个由层间标记排名一致性引导的动态框架。通过自适应地平衡注意力幅度与排名一致性，TIES 在无需额外训练的情况下确保了稳健的标记选择。在 CogACT + SIMPLER 基准测试中，TIES 在减少 78% 标记使用的同时，将平均成功率提高了 6%，并在多种解码器和基准测试中展现出强大的泛化能力。

## Abstract
Vision-Language-Action (VLA) models excel in robotic manipulation but suffer from significant inference latency due to processing dense visual tokens. Existing token reduction methods predominantly rely on attention magnitude as a static selection. In this work, we challenge this assumption, revealing that high-attention tokens are task-dependent and can even degrade policy performance. To address this, we introduce \textbf{TIES} (\textbf{T}au-guided \textbf{I}nter-layer \textbf{E}fficient \textbf{S}election), a dynamic framework guided by inter-layer token ranking consistency. By adaptively balancing attention magnitude with ranking consistency, TIES ensures robust token selection without requiring additional training. On the CogACT + SIMPLER benchmark, TIES improves average success rates by 6\% while reducing token usage by 78\%, and demonstrate strong generalization across diverse decoders and benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Attention Magnitude: Leveraging Inter-layer Rank Consistency for Efficient Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言-动作（VLA）模型通过融合多模态输入直接生成机器人指令，但在处理大量视觉 Token 时，Transformer 的二次方复杂度导致了严重的推理延迟，阻碍了实时部署。
*   **核心问题**：现有的 Token 缩减方法大多假设“注意力权重（Magnitude）越高，Token 越重要”。本文挑战了这一假设，指出高注意力 Token 往往具有任务依赖性，且容易受到环境噪声（如背景纹理）的干扰，盲目保留高注意力 Token 反而可能导致策略性能下降。
*   **研究动机**：寻找一种比注意力权重更稳健的指标，以实现高效且不损失（甚至提升）性能的动态 Token 筛选。

### 2. 方法论
*   **核心思想**：引入**层间排名一致性（Inter-layer Rank Consistency）**作为 Token 可信度的度量。
*   **关键技术细节**：
    *   **指标定义**：使用 **Kendall rank correlation coefficient ($\tau$)** 来衡量相邻 Transformer 层之间 Top-k Token 排名的稳定性。
    *   **发现规律**：研究发现，当 $\tau$ 较高时，模型往往陷入“伪相关锁定（Spurious Locking）”，即固定关注某些无意义特征；而当 $\tau$ 较低时，代表模型在迭代更新关注点，信息流更健康。
    *   **TIES 框架流程**：
        1.  **离线校准**：通过少量样本估计 $\tau$ 的分布和中位数。
        2.  **动态推理**：实时计算当前帧的 $\tau$。
        3.  **自适应选择**：若 $\tau$ 显示当前注意力不可信，则减少对高权重 Token 的依赖，转而结合**均匀采样（Uniform Sampling）**以保证特征多样性；若可信，则保留高权重 Token。
        4.  **相似性触发**：利用视频流的时间冗余，仅在视觉变化显著时重新计算剪枝策略，进一步降低开销。

### 3. 实验设计
*   **数据集与场景**：
    *   **SIMPLER Benchmark**：包含 Google Robot 和 WidowX 平台的操控任务（如抓取可乐罐、开抽屉等）。
    *   **LIBERO Benchmark**：用于验证跨任务域的泛化能力。
*   **评估协议**：Visual Matching（视觉匹配）和 Variant Aggregation（环境变体聚合，包含光照、背景变化）。
*   **对比方法**：FastV（静态剪枝）、VLA-Cache（缓存机制）、EfficientVLA（综合加速方案）以及全 Token 基准（Full-token Baseline）。
*   **适配模型**：CogACT（基于 Diffusion）、OpenVLA（自回归）、OpenVLA-OFT。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A800 GPU** 上执行。
*   **算力特点**：论文强调 TIES 是 **Training-free（无需训练）** 的方法。虽然未详细列出总训练时长（因为不需要重训），但实验结果显示该方法能减少约 **83.6% 的 FLOPs**（在保留 28 个 Token 的情况下）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 2 种主流 VLA 架构和 3 个不同的 Benchmark。
    *   在 SIMPLER 上进行了多任务（4 类主要任务）的详尽测试。
    *   **消融实验**：对比了不同剪枝比例（28 到 256 Token）、不同的替代采样策略（Middle, Uniform, DivPrune）。
*   **充分性评价**：实验设计较为客观，不仅验证了加速效果，还重点分析了在环境扰动下的鲁棒性，通过“性能反转”现象有力支撑了其核心论点。

### 6. 主要结论与发现
*   **注意力陷阱**：高注意力 Token 并不等同于任务相关性，在某些任务（如开抽屉）中，排除最高权重的 Token 反而能提升成功率。
*   **一致性指标有效性**：Kendall $\tau$ 在预测注意力可靠性方面表现优异，AUC 达到 0.91。
*   **性能提升**：在 CogACT 模型上，TIES 在减少 78% Token 的同时，将平均成功率提升了 **6%**。
*   **泛化性**：该方法在不同解码器（自回归 vs 扩散模型）上均表现出优于传统剪枝方法的性能。

### 7. 优点
*   **即插即用**：无需重新训练或微调模型，部署成本极低。
*   **动态去噪**：通过识别“伪相关”，TIES 实际上起到了策略去噪的作用，增强了模型在复杂环境下的鲁棒性。
*   **高效平衡**：利用时间冗余触发更新，极大地缓解了计算 $\tau$ 带来的额外开销。

### 8. 不足与局限
*   **理论深度**：虽然实验证明了 $\tau$ 的有效性，但对于“为什么层间一致性高就代表伪相关”的深层因果逻辑仍处于假设阶段，缺乏严格的理论证明。
*   **校准依赖**：虽然只需要 100 帧左右的数据，但仍需针对新环境进行一次性的离线校准。
*   **模态局限**：目前主要针对视觉 Token 冗余，未充分探讨视觉与复杂长文本指令之间的跨模态精细化交互剪枝。

（完）
