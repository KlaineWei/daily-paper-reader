---
title: Hyena Operator for Fast Sequential Recommendation
title_zh: 用于快速序列推荐的 Hyena 算子
authors: "Jiahao Liu, Lin Li, Zhiyuan Li, Kaixi Hu, Kaize Shi, Jingling Yuan"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25027v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于快速序列推荐的亚二次复杂度算子
tldr: 针对序列推荐中注意力机制计算复杂度高且Hyena算子在稀疏长序列上表现不佳的问题，本文提出HyenaRec。该模型结合勒让德正交多项式核参数化与门控卷积，在保持线性复杂度的同时，有效捕捉长短期用户兴趣。实验证明HyenaRec在准确率上优于主流基准，且训练速度提升达6倍，是高效序列推荐的新方案。
selection_source: fresh_fetch
motivation: 现有的注意力机制复杂度过高，而次线性算子Hyena在处理稀疏且漫长的推荐序列时表达能力不足。
method: 提出HyenaRec，利用勒让德多项式构建卷积核以建模长期依赖，并结合门控机制捕捉短期行为爆发。
result: 在多个真实数据集上，HyenaRec的排名准确率超越了注意力及循环神经网络模型，且训练速度最高提升了6倍。
conclusion: 基于多项式的核参数化方法是序列推荐中替代注意力机制的一种兼具原则性与可扩展性的高效方案。
---

## 摘要
序列推荐模型，特别是基于注意力机制的模型，虽然取得了很高的准确率，但其二次复杂度使得处理长用户历史记录的成本极高。诸如 Hyena 之类的亚二次算子在语言建模中提供了高效的替代方案，但它们在推荐系统中的潜力仍未得到充分探索。我们认为，由于在稀疏且漫长的用户序列上表示能力有限，Hyena 在推荐领域面临挑战。为了应对这些挑战，我们提出了 HyenaRec，这是一种将基于多项式的核参数化与门控卷积相结合的新型序列推荐器。具体而言，我们利用勒让德正交多项式设计卷积核，为建模长期时间依赖关系提供了一个平滑且紧凑的基。互补的门控机制捕捉细粒度的短期行为爆发，从而产生一种混合架构，在稀疏反馈下平衡全局时间演化与局部用户兴趣。这种结构在增强表达能力的同时，随序列长度呈线性扩展。在多个真实世界数据集上的广泛实验表明，HyenaRec 在排名准确率上始终优于基于注意力、循环神经网络及其他基准模型。此外，它的训练速度显著加快（最高可达 6 倍加速），在长序列场景下优势尤为突出，在保持效率的同时不牺牲准确率。这些结果强调了基于多项式的核参数化是序列推荐中注意力机制的一种有原则且可扩展的替代方案。

## Abstract
Sequential recommendation models, particularly those based on attention, achieve strong accuracy but incur quadratic complexity, making long user histories prohibitively expensive. Sub-quadratic operators such as Hyena provide efficient alternatives in language modeling, but their potential in recommendation remains underexplored. We argue that Hyena faces challenges in recommendation due to limited representation capacity on sparse, long user sequences. To address these challenges, we propose HyenaRec, a novel sequential recommender that integrates polynomial-based kernel parameterization with gated convolutions. Specifically, we design convolutional kernels using Legendre orthogonal polynomials, which provides a smooth and compact basis for modeling long-term temporal dependencies. A complementary gating mechanism captures fine-grained short-term behavioral bursts, yielding a hybrid architecture that balances global temporal evolution with localized user interests under sparse feedback. This construction enhances expressiveness while scaling linearly with sequence length. Extensive experiments on multiple real-world datasets demonstrate that HyenaRec consistently outperforms Attention-, Recurrent-, and other baselines in ranking accuracy. Moreover, it trains significantly faster (up to 6x speedup), with particularly pronounced advantages on long-sequence scenarios where efficiency is maintained without sacrificing accuracy. These results highlight polynomial-based kernel parameterization as a principled and scalable alternative to attention for sequential recommendation.

---

## 论文详细总结（自动生成）

以下是对论文《Hyena Operator for Fast Sequential Recommendation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的序列推荐模型（如基于 Transformer 的 SASRec）虽然准确率高，但其自注意力机制具有**二次计算复杂度** $O(L^2)$，导致在处理长用户历史序列时计算成本极高。
*   **研究动机**：虽然 NLP 领域出现了 Hyena 等亚二次复杂度算子，但直接将其应用于推荐系统面临挑战：推荐数据具有**高度稀疏性**和**噪声**，且用户兴趣存在多尺度演化（长期偏好与短期爆发）。原有的隐式滤波器参数化方式在稀疏数据下容易过拟合或不稳定。
*   **整体含义**：本文旨在通过引入**勒让德正交多项式（Legendre Polynomials）**来参数化卷积核，构建一种既能保持线性扩展性，又能稳定捕捉长短期兴趣的高效序列推荐模型 **HyenaRec**。

### 2. 论文提出的方法论
*   **核心思想**：将 Hyena 算子的长卷积能力与正交多项式的数学稳定性相结合，通过受限的参数空间实现对长程依赖的平滑建模。
*   **关键技术细节**：
    *   **LegendreHyenaFilter（勒让德滤波器）**：卷积核 $K$ 不再直接学习每个点的值，而是表示为前 $K$ 个勒让德多项式基函数的线性组合。公式表达为 $K = CP$，其中 $P$ 是基矩阵，$C$ 是可学习系数。
    *   **平滑性与正则化**：勒让德多项式在 $[-1, 1]$ 区间正交且边界受控，天然具有低通滤波特性，能抑制稀疏交互中的高频噪声。
    *   **混合架构**：采用门控卷积结构，包含一条由轻量级卷积构成的“短路径”（捕捉短期行为爆发）和一条由勒让德滤波器构成的“长路径”（建模长期演化）。
    *   **因果归一化**：对卷积核进行 $\ell_1$ 归一化，确保信号在多层堆叠中不会爆炸或消失，增强了深层网络的训练稳定性。

### 3. 实验设计
*   **数据集**：
    *   **ML-1M**（电影）、**Steam**（游戏）、**Video**（亚马逊视频）：中短序列场景。
    *   **XLong**：专门用于测试长序列能力的超长数据集（平均长度达 958.8）。
*   **Benchmark 与对比方法**：
    *   **RNN 类**：GRU4Rec, NARM, LRURec（线性循环单元）。
    *   **Attention 类**：SASRec, BERT4Rec, EulerFormer。
    *   **生成式/状态空间类**：HSTU, Mamba4Rec。
*   **评价指标**：NDCG@10/20, Recall@10/20。

### 4. 资源与算力
*   **硬件配置**：单张 **NVIDIA RTX 3080 Ti (12GB)** GPU。
*   **其他环境**：12 vCPU Intel Xeon Silver 4214R @ 2.40GHz，90GB RAM。
*   **训练细节**：使用 AdamW 优化器，学习率 1e-3，Batch Size 为 128（XLong 为 32），最高训练 500 轮，并采用早停机制。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个不同规模和特征的数据集上进行了全量对比。
    *   **消融实验**：验证了多项式核（PK）、门控单元（GLU）以及不同基函数（傅里叶、切比雪夫）的影响。
    *   **效率实验**：对比了不同模型的训练耗时比（TTR）。
    *   **敏感性分析**：探讨了 Dropout、权重衰减以及勒让德阶数 $K$ 对性能的影响。
*   **充分性评价**：实验设计较为充分，涵盖了从准确率到效率、从短序列到超长序列的全面评估，对比基准包含了 2024 年最新的 SOTA 模型（如 LRURec, HSTU），具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：HyenaRec 在所有数据集上均达到最优或次优表现，尤其在 **XLong 长序列**上，NDCG@10 提升高达 **17.65%**。
*   **极高效率**：相比 SASRec 和 BERT4Rec，HyenaRec 的训练速度提升了 **1.7x 到 6x**，且内存占用随序列长度线性增长。
*   **稳定性**：勒让德多项式基函数相比傅里叶基函数在处理非周期性的推荐数据时更稳定，能有效缓解稀疏数据下的过拟合问题。

### 7. 优点
*   **数学严谨性**：引入正交多项式为卷积核提供结构化先验，解决了纯隐式参数化在推荐系统中的不稳定性。
*   **扩展性强**：亚二次复杂度的设计使其能够处理数千甚至上万长度的用户历史，突破了 Transformer 的瓶颈。
*   **即插即用**：作为一种算子创新，可以较容易地集成到现有的序列建模框架中。

### 8. 不足与局限
*   **复杂度权衡**：虽然计算复杂度降低，但引入勒让德基函数计算和 FFT 卷积在极短序列上可能不如简单模型高效。
*   **特征融合单一**：目前主要关注 ID 序列，对于包含多模态信息（如图像、文本描述）或复杂上下文特征的融合讨论较少。
*   **应用限制**：论文主要验证了其在“下一项预测”任务上的表现，在重排序或多目标预测等更复杂的工业场景下表现尚待验证。

（完）
