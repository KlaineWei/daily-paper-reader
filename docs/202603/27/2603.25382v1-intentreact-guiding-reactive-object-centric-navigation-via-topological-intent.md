---
title: "IntentReact: Guiding Reactive Object-Centric Navigation via Topological Intent"
title_zh: IntentReact：通过拓扑意图引导反应式以对象为中心的导航
authors: "Yanmei Jiao, Anpeng Lu, Wenhan Hu, Rong Xiong, Yue Wang, Huajin Tang, Wen-an Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25382v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 机器人的目标物体视觉导航和长程导航
tldr: 针对目标驱动视觉导航中全局拓扑规划与局部反应式控制脱节的问题，本文提出 IntentReact 框架。该框架引入“意图”作为低维方向信号，将全局拓扑引导融入局部航点预测策略中。这使得机器人在局部观测受限或误导时，能根据全局拓扑一致性快速调整方向，在保持反应式控制鲁棒性的同时，显著提升了导航效率和成功率。
selection_source: fresh_fetch
motivation: 现有的拓扑导航方法在局部决策时仅依赖当前视角，导致机器人在局部观测误导时无法有效利用全局信息进行及时重定向。
method: 提出一种意图条件化的对象中心导航框架，通过将全局拓扑引导编码为低维方向信号来指导局部航点预测策略。
result: 实验结果表明，IntentReact 在导航成功率和执行质量上均显著优于现有的对象中心导航方法。
conclusion: 通过在全局规划与局部控制间建立紧凑的意图接口，可以有效克服局部观测的局限性，实现更高效且鲁棒的机器人导航。
---

## 摘要
目标物体视觉导航要求机器人在部分可观测性下对语义结构进行推理并有效行动。最近基于对象级拓扑地图的方法实现了无需密集几何重建的长程导航，但其执行仍受限于全局拓扑引导与局部感知驱动控制之间的差距。特别是，局部决策仅根据当前的自我中心观测做出，无法获取机器人视野之外的信息。结果是，即使机器人最初偏离目标方向，它也可能坚持当前的航向，向无法减小全局拓扑距离的方向移动。在这项工作中，我们提出了 IntentReact，这是一个以意图为条件的以对象为中心的导航框架，它在全局拓扑规划和反应式以对象为中心的控制之间引入了一个紧凑的接口。我们的方法将全局拓扑引导编码为一种低维方向信号，称为“意图”，该信号调节学习到的路标点预测策略，使导航偏向于拓扑一致的进展。这种设计使机器人能够在局部观测产生误导时迅速重新定向，引导运动朝向减小全局拓扑距离的方向，同时保留以对象为中心的控制的反应性和鲁棒性。我们通过广泛的实验评估了所提出的框架，证明了与之前的以对象为中心的导航方法相比，导航成功率和执行质量都有所提高。

## Abstract
Object-goal visual navigation requires robots to reason over semantic structure and act effectively under partial observability. Recent approaches based on object-level topological maps enable long-horizon navigation without dense geometric reconstruction, but their execution remains limited by the gap between global topological guidance and local perception-driven control. In particular, local decisions are made solely from the current egocentric observation, without access to information beyond the robot's field of view. As a result, the robot may persist along its current heading even when initially oriented away from the goal, moving toward directions that do not decrease the global topological distance. In this work, we propose IntentReact, an intent-conditioned object-centric navigation framework that introduces a compact interface between global topological planning and reactive object-centric control. Our approach encodes global topological guidance as a low-dimensional directional signal, termed intent, which conditions a learned waypoint prediction policy to bias navigation toward topologically consistent progression. This design enables the robot to promptly reorient when local observations are misleading, guiding motion toward directions that decrease global topological distance while preserving the reactivity and robustness of object-centric control. We evaluate the proposed framework through extensive experiments, demonstrating improved navigation success and execution quality compared to prior object-centric navigation methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **IntentReact** 的导航框架，旨在解决目标驱动视觉导航中全局规划与局部控制脱节的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在以对象为中心的导航中，机器人通常依赖局部观测进行决策。由于**部分可观测性（Partial Observability）**，当局部视野中的物体具有误导性（如对称布局或初始朝向背离目标）时，机器人往往会沿着错误航向持续移动，无法有效利用全局拓扑信息进行纠偏。
*   **研究背景**：现有的拓扑导航方法虽然能进行长程推理，但其全局规划（拓扑路径）与局部控制（基于当前帧的反应式动作）之间缺乏紧密的耦合接口，导致导航效率低下，容易陷入局部最优。

### 2. 方法论：核心思想与技术细节
IntentReact 的核心是在全局规划和局部控制之间引入一个紧凑的**“意图（Intent）”**信号。
*   **拓扑建图**：利用 SAM2/FastSAM 提取语义对象作为节点，通过空间共视和时间跟踪建立边，构建对象级拓扑图。
*   **2-步跳跃意图（2-hop Intent）**：
    *   从当前位置出发，在全局最短路径上寻找第一个拓扑距离严格减小的节点（即 2-hop 节点）。
    *   将该节点相对于机器人的方向编码为一个二维单位向量 $z = [\cos \phi, \sin \phi]^T$。
    *   **设计理由**：相比于直接指向最终目标，2-hop 意图能更好地引导机器人绕过障碍物或穿过走廊，提供更符合拓扑结构的即时方向引导。
*   **意图条件化控制**：
    *   使用 **FiLM（特征线性调制）** 技术，将意图向量 $z$ 注入到航点预测网络的特征图中。
    *   这种设计允许意图信号作为一种“软偏置”来调制内部特征，而不是硬性规定动作，从而保留了局部感知的灵活性。
*   **可行性感知航点精细化**：
    *   引入轻量级的 **BEV（鸟瞰图）可通行性地图**。
    *   将预测的航点投影到 BEV 地图上，若航点位于不可通行区域，则将其修正到最近的自由空间，确保几何安全性。

### 3. 实验设计
*   **数据集与场景**：使用 **Habitat-Matterport3D (HM3D)** 数据集，基于 **IIN-HM3D-v3** 基准进行评估，涵盖 36 个室内环境。
*   **实验任务**：设置了五种任务模式：
    1.  **Imitate**（轨迹模仿）
    2.  **Alt Goal**（寻找已观测但未访问的目标）
    3.  **Shortcut**（寻找更短路径，测试效率）
    4.  **Reverse**（反向穿越）
    5.  **Opposite**（初始朝向完全相反，专门测试纠偏能力）
*   **对比方法**：对比了 **RoboHop**、**TANGO** 和 **ObjectReact**（该领域的 SOTA 方法）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **模型组件**：提到了使用 SAM2、FastSAM、SuperPoint、LightGlue 和 NetVLAD 等基础模型进行感知和定位，这些模型通常需要主流显卡（如 RTX 3090/4090 或 A100）支持。

### 5. 实验数量与充分性
*   **实验规模**：每个任务评估 108 个回合（Episodes），总计进行了数百次完整的导航测试。
*   **充分性**：
    *   **消融实验**：详细分析了意图信号、FiLM 调制方式、BEV 约束等组件的贡献。
    *   **鲁棒性测试**：针对初始朝向偏差（0° 到 180°）和意图估计误差（加入 5° 到 30° 的噪声）进行了压力测试。
    *   **感知泛化**：在真值感知（GT Perception）和学习感知（Learned Perception，如 NetVLAD 定位）两种设置下均进行了验证。
*   **评价**：实验设计全面，覆盖了从理想状态到现实噪声环境的多种情况，对比基准公平且具有说服力。

### 6. 主要结论与发现
*   **纠偏能力强**：在初始朝向背离目标的极端情况下（180° 偏差），IntentReact 的成功加权路径长度（SPL）比最强基准提升了 **87%**。
*   **效率提升**：通过意图引导，机器人能更早地发现“捷径”，显著减少了导航步数。
*   **软引导优势**：实验证明，即使意图信号存在一定角度偏差（如 20° 噪声），系统性能依然稳定，说明模型学会了结合意图与局部视觉信息，而非盲目跟随。
*   **模块化有效性**：BEV 约束解决了学习策略可能产生的碰撞问题，与意图引导形成了互补。

### 7. 优点与亮点
*   **接口简洁**：将复杂的全局规划压缩为低维方向信号，既降低了计算负担，又易于集成到不同的导航架构中。
*   **解决痛点**：精准打击了反应式导航在部分可观测性下的“短视”问题。
*   **泛化性好**：不依赖特定的 RGB 输入，而是基于对象成本图，这使得模型在不同环境和仿真到现实（Sim-to-Real）的迁移中更具潜力。

### 8. 不足与局限
*   **依赖先验地图**：虽然框架是模块化的，但实验主要基于已知环境的拓扑地图，在完全未知的探索场景下的表现尚待验证。
*   **感知依赖**：在完全学习感知的设置下，性能虽优于基准但较真值仍有下降，说明系统对底层语义分割和定位的准确性仍有一定依赖。
*   **动态环境**：论文未深入探讨在包含移动行人或物体位置频繁变动的动态环境中的表现。

（完）
