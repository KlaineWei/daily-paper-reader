---
title: "Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance"
title_zh: Fast-dVLA：将离散扩散 VLA 加速至实时性能
authors: "Wenxuan Song, Jiayi Chen, Shuai Chen, Jingbo Wang, Pengxiang Ding, Han Zhao, Yikai Qin, Xinhu Zheng, Donglin Wang, Yan Wang, Haoang Li"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25661v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 加速VLA模型并提高微调期间的性能
tldr: 针对预训练VLA模型在标准微调中性能提升有限且计算开销大的问题，本文提出Fast-dVLA。该方法通过在参数空间解耦辅助任务目标，提取增强通用能力的“能力向量”并与预训练参数融合，构建增强型元模型。结合轻量级正交正则化损失，在降低计算开销的同时，实现了与复杂辅助微调相当的性能，显著提升了机器人任务的适应效率。
selection_source: fresh_fetch
motivation: 现有的辅助任务微调虽能提升VLA模型性能，但其带来的额外计算开销限制了实时性与微调效率。
method: 在参数空间解耦通用能力增强与特定任务拟合目标，通过融合提取的能力向量构建元模型，并配合轻量级正交正则化进行微调。
result: 实验证明该方法在多种机器人任务中达到了与辅助微调基准相当的性能，且显著降低了计算开销。
conclusion: 本研究通过参数空间解耦与融合，为高效、低成本地提升VLA模型在多样化机器人任务中的表现提供了新途径。
---

## 摘要
本文提出了一种新颖的方法，旨在解决预训练视觉-语言-动作（VLA）模型在标准监督微调（SFT）过程中往往无法有效提升性能并降低适配成本的挑战。一些带有辅助训练目标的高级微调方法可以提高性能并减少收敛步数，但由于辅助任务带来的额外损失函数计算，它们通常会产生显著的计算开销。为了在保持标准 SFT 简洁性的同时实现辅助训练的增强能力，我们在参数空间内解耦了辅助任务训练的两个目标，即增强通用能力和拟合特定任务的动作分布。为实现这一目标，我们只需使用两种不同的训练策略，使模型在小规模任务集上收敛。由此产生的模型参数差异可以被解释为辅助任务提供的能力向量。随后，这些向量与预训练参数合并，形成一个能力增强的元模型。此外，当标准 SFT 辅以轻量级的正交正则化损失时，合并后的模型在计算开销降低的情况下，达到了与辅助微调基准相当的性能。实验结果表明，该方法在多种机器人任务中均非常有效。项目主页：https://chris1220313648.github.io/Fast-dVLA/

## Abstract
This paper proposes a novel approach to address the challenge that pretrained VLA models often fail to effectively improve performance and reduce adaptation costs during standard supervised finetuning (SFT). Some advanced finetuning methods with auxiliary training objectives can improve performance and reduce the number of convergence steps. However, they typically incur significant computational overhead due to the additional losses from auxiliary tasks. To simultaneously achieve the enhanced capabilities of auxiliary training with the simplicity of standard SFT, we decouple the two objectives of auxiliary task training within the parameter space, namely, enhancing general capabilities and fitting task-specific action distributions. To deliver this goal, we only need to train the model to converge on a small-scale task set using two distinct training strategies. The difference between the resulting model parameters can then be interpreted as capability vectors provided by auxiliary tasks. These vectors are then merged with pretrained parameters to form a capability-enhanced meta model. Moreover, when standard SFT is augmented with a lightweight orthogonal regularization loss, the merged model attains performance comparable to auxiliary finetuned baselines with reduced computational overhead. Experimental results demonstrate that this approach is highly effective across diverse robot tasks. Project page: https://chris1220313648.github.io/Fast-dVLA/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：离散扩散视觉-语言-动作模型（dVLA）在多模态对齐和保留预训练知识方面表现优异，但其推理速度极慢，远低于机器人实时控制所需的 30Hz。
*   **核心瓶颈**：dVLA 通常采用双向注意力机制（Bidirectional Attention），导致每一轮去噪迭代时，所有 Token 的 Key-Value (KV) 状态都会改变，无法像自回归（AR）模型那样复用 KV Cache。
*   **关键观察**：作者发现即使在双向注意力下，dVLA 仍表现出隐式的“从左到右”解码趋势（即序列靠前的动作块先被去噪）。这为引入块状自回归机制提供了理论可能。

### 2. 方法论
Fast-dVLA 的核心思想是结合“块状自回归”的效率与“扩散模型”的并行性：
*   **块状注意力（Block-Wise Attention）**：将动作序列划分为块，限制注意力仅能看向当前块及之前的块。一旦某个块去噪完成，其 KV 状态即固定，后续迭代可直接复用缓存。
*   **扩散强迫（Diffusion Forcing）**：在训练和推理中，为不同位置的块分配不同的噪声水平（前块噪声低，后块噪声高），允许模型在精细化前块的同时，并行地对后块进行初步去噪。
*   **非对称蒸馏（Asymmetric Distillation）**：为了高效训练，使用预训练的双向注意力 dVLA 作为教师模型，引导具有块状注意力的学生模型进行学习。这种方式比从头训练快 10 倍。
*   **流水线并行解码（Pipelined Parallel Decoding）**：在推理阶段，设计了“半激活”和“全激活”状态。根据前一个块的去噪置信度动态引入新块，实现跨块的流水线并行处理。

### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：评估长程（Long-horizon）语言指令操作任务。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估泛化与知识迁移。
    *   **SimplerEnv**：真实到仿真的评估，使用 WidowX 机器人模型。
    *   **真实世界**：在 AgileX 双臂机器人平台上进行传送带抓取、蔬菜分类与检索任务。
*   **对比方法**：
    *   **基础模型**：Dream-VLA, DD-VLA, UD-VLA。
    *   **加速方案**：Fast-dLLM, Block Diffusion。
    *   **SOTA 基准**：π0, π0-FAST, OpenVLA, GR00T-N1 等。

### 4. 资源与算力
*   **训练细节**：采用 LoRA（Rank=32）进行高效微调。
*   **训练时长**：在 LIBERO 上，蒸馏仅需约 4000 步（Dream-VLA/DD-VLA）或 3000 步（UD-VLA）， batch size 为 12。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号（如 A100 数量）及总训练机时，但强调了其蒸馏方案比标准微调节省了约 80%-90% 的计算资源。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大主流仿真基准和三类真实世界任务，每类任务均进行了数十至数百次 Rollouts。
*   **消融实验**：针对块大小（Block Size）、置信度阈值（$\tau_{conf}$）、激活阈值（$\tau_{add}, \tau_{act}$）以及不同的训练策略（从头训练 vs 蒸馏）进行了详细对比。
*   **评价**：实验设计较为全面，不仅验证了速度提升（2.8x - 4.1x），还证明了在加速后成功率几乎无损甚至略有提升，对比实验涵盖了当前最前沿的流匹配（Flow-matching）和 AR 模型，结果具有客观性。

### 6. 主要结论与发现
*   **性能飞跃**：Fast-dVLA 实现了 **2.8x 至 4.1x** 的推理加速，成功将 dVLA 的执行频率提升至 **30Hz**，满足实时控制需求。
*   **精度保持**：在加速的同时，模型在 LIBERO 和 CALVIN 等基准上的成功率保持了 SOTA 水平，甚至在长程任务上表现更稳健。
*   **训练高效性**：非对称蒸馏被证明是极低成本的迁移方案，仅需原微调预算的 1/10 即可完成模型转换。

### 7. 优点与亮点
*   **架构创新**：巧妙地在扩散模型中引入块状因果掩码，解决了扩散模型长期以来无法有效利用 KV Cache 的难题。
*   **实时性突破**：这是首个将离散扩散 VLA 优化至实时工业标准（30Hz）的工作。
*   **通用性强**：该方案可无缝应用于多种 dVLA 变体（如带视觉预测的 UD-VLA），具有很强的普适性。

### 8. 不足与局限
*   **超参数依赖**：流水线解码中的置信度阈值和激活阈值需要针对不同任务进行一定程度的调优，以平衡速度与精度。
*   **离散限制**：目前仅针对离散动作空间的扩散模型，对于直接输出连续动作值的扩散模型（如基于 MLP 或 CNN head 的）适用性尚未讨论。
*   **硬件开销**：虽然推理频率达标，但作为大模型，其对显存的占用依然显著，文中未深入讨论在嵌入式边缘设备上的部署表现。

（完）
