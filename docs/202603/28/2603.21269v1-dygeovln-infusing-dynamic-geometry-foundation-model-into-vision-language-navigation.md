---
title: "DyGeoVLN: Infusing Dynamic Geometry Foundation Model into Vision-Language Navigation"
title_zh: DyGeoVLN：将动态几何基础模型注入视觉语言导航
authors: "Xiangchen Liu, Hanghan Zheng, Jeil Jeong, Minsung Yoon, Lin Zhao, Zhide Zhong, Haoang Li, Sung-Eui Yoon"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21269v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 具有长程令牌剪枝的视觉语言导航
tldr: 针对视觉语言导航（VLN）在动态现实场景中泛化能力不足的问题，本文提出DyGeoVLN框架。该框架通过将动态几何基础模型融入VLN，利用跨分支特征融合实现显式的3D空间表示和视觉语义推理。此外，引入了无位姿、自适应分辨率的令牌剪枝策略，有效降低了长程导航的推理成本。实验证明该方法在多个基准测试中达到SOTA，并具有极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的VLN方法多基于静态场景假设，难以应对动态且复杂的现实环境。
method: 提出DyGeoVLN框架，通过融合动态几何基础模型增强3D空间感知，并采用自适应令牌剪枝策略优化长程导航效率。
result: 在多个主流基准测试上取得了最先进的性能，并在真实世界环境中表现出显著的鲁棒性。
conclusion: 将动态几何感知与高效的令牌管理相结合，是提升视觉语言导航智能体在动态场景中表现的有效途径。
---

## 摘要
视觉语言导航（VLN）要求智能体理解视觉观测和语言指令，以便在未知环境中进行导航。大多数现有方法依赖于静态场景假设，难以泛化到动态的现实场景中。为了应对这一挑战，我们提出了 DyGeoVLN，一个动态几何感知的 VLN 框架。我们的方法通过跨分支特征融合将动态几何基础模型注入 VLN 框架，从而实现显式的 3D 空间表示和视觉语义推理。为了在长时程、动态导航中高效压缩历史 token 信息，我们进一步引入了一种新颖的无位姿且自适应分辨率的 token 剪枝策略。该策略可以去除时空冗余 token，以降低推理成本。大量的实验表明，我们的方法在多个基准测试上达到了最先进的性能，并在现实环境中表现出强大的鲁棒性。

## Abstract
Vision-language Navigation (VLN) requires an agent to understand visual observations and language instructions to navigate in unseen environments. Most existing approaches rely on static scene assumptions and struggle to generalize in dynamic, real-world scenarios. To address this challenge, we propose DyGeoVLN, a dynamic geometry-aware VLN framework. Our method infuses a dynamic geometry foundation model into the VLN framework through cross-branch feature fusion to enable explicit 3D spatial representation and visual-semantic reasoning. To efficiently compress historical token information in long-horizon, dynamic navigation, we further introduce a novel pose-free and adaptive-resolution token-pruning strategy. This strategy can remove spatio-temporal redundant tokens to reduce inference cost. Extensive experiments demonstrate that our approach achieves state-of-the-art performance on multiple benchmarks and exhibits strong robustness in real-world environments.

---

## 论文详细总结（自动生成）

### 论文总结：DyGeoVLN —— 将动态几何基础模型注入视觉语言导航

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言导航（VLN）要求智能体在未知环境中根据指令行动。现有的多模态大语言模型（MLLM）虽然在语义理解上表现出色，但存在两个核心缺陷：
*   **缺乏 3D 空间推理：** 现有的视觉基础模型多基于 2D 图像-文本对预训练，难以在长程轨迹中保持全局一致的 3D 空间感知。
*   **动态环境适应性差：** 现有的几何模型在面对动态场景（如移动的人员）时，几何表示会严重退化。
*   **Token 爆炸问题：** 长程导航会积累海量视觉 Token，导致推理成本激增。
**DyGeoVLN** 的提出旨在通过引入动态几何基础模型和高效的 Token 剪枝策略，解决智能体在动态、长程现实环境中的导航难题。

#### 2. 方法论
DyGeoVLN 采用了一种双分支融合架构，核心组件包括：
*   **动态几何基础模型 (DGFM)：**
    *   **深度引导：** 利用单目深度估计（Depth Anything）生成局部点云图。
    *   **多级 3D 嵌入：** 通过 Point-map Transformer 提取分层几何特征。
    *   **动态感知融合：** 使用“零均值卷积”（Zero-mean Conv）将 3D 几何特征注入预训练的视觉骨干网络，在保留原始语义先验的同时增强几何感知。
    *   **分层潜变量解码：** 专门的解码器分别预测相机位姿、局部点云和全局几何 Token。
*   **DyHM3D 数据集：** 为训练 DGFM，构建了一个包含 50,000 条轨迹的动态人体中心 3D 数据集，模拟室内环境中的人体运动。
*   **跨分支特征融合：** 将 Qwen2-VL 的 2D 语义 Token 与 DGFM 的 3D 空间 Token 通过交叉注意力机制（Cross-Attention）融合，形成统一的空间-语义表示。
*   **自适应分辨率与占用感知 Token 剪枝：**
    *   **无位姿依赖：** 利用 DGFM 自主推断的位姿和点云进行体素化。
    *   **自适应分辨率：** 根据深度调整体素大小（近处精细，远处粗糙）。
    *   **占用感知：** 仅保留每个体素内的代表性 Token，并结合时间平滑技术确保一致性。

#### 3. 实验设计
*   **数据集与基准：**
    *   **动态基准：** HA-VLN（包含多人类交互、社交建模）。
    *   **静态基准：** R2R-CE（连续环境下的标准 VLN 测试）。
*   **评估指标：** 成功率 (SR)、导航误差 (NE)、碰撞率 (CR/TCR)、路径长度加权成功率 (SPL) 等。
*   **对比方法：** 包括 NaVid, Uni-NaVid, NaVILA, StreamVLN, g3D-LF 等最先进的 MLLM 导航模型。
*   **真实世界验证：** 使用 Unitree Go1 四足机器人，在走廊、大厅、拥挤房间等场景进行实地测试。

#### 4. 资源与算力
*   **训练阶段：** 论文提到 DGFM 在 DyHM3D 上训练，VLN 部分在 R2R-CE、HA-VLN 等多个数据集上微调。**文中未明确说明具体的 GPU 型号、数量及总训练时长。**
*   **部署阶段：** 机器人端使用 NVIDIA Jetson Orin Nano 进行数据采集与初步处理，策略运行在远程服务器上，通过 Wi-Fi 实现实时推理。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从仿真（静态与动态）到现实世界的全方位评估。
*   **消融实验：** 针对双分支架构、动态空间注入、Token 剪枝等核心组件进行了详细的消融测试，验证了各部分的贡献。
*   **客观性：** 在动态基准 HA-VLN 上，对比了使用不同观测数据（全景 RGB-D vs 单目 RGB）的方法，证明了该模型在仅使用单目 RGB 的情况下仍能超越部分使用全景深度信息的方法，实验设计较为公平且具有说服力。

#### 6. 主要结论与发现
*   **性能领先：** DyGeoVLN 在动态 HA-VLN 基准上将成功率（SR）提升了约 12%-13%，碰撞率（CR）降低了约 10%-11%。
*   **静态鲁棒：** 在 R2R-CE 静态基准上同样达到了 SOTA 性能（SR 60.8%），证明了几何增强对基础导航能力的普适提升。
*   **高效性：** 提出的 Token 剪枝策略在减少计算负担的同时，通过去除冗余信息反而提升了导航的准确性。
*   **现实可行性：** 成功在四足机器人上部署，证明了模型在处理真实动态行人时的避障与指令遵循能力。

#### 7. 优点
*   **动态几何感知：** 不同于以往将几何视为插件的做法，DyGeoVLN 深度融合了动态几何基础模型，显著提升了对移动物体的建模能力。
*   **系统解耦与简化：** 实现了“无位姿”剪枝，不依赖模拟器提供的完美位姿或额外传感器，降低了硬件部署门槛。
*   **创新的数据集：** DyHM3D 填补了 VLN 领域缺乏大规模动态人体 3D 训练数据的空白。

#### 8. 不足与局限
*   **单目深度依赖：** 尽管使用了强大的深度估计模型，但单目视觉在极端光照或纹理缺失区域仍可能存在深度误差，影响几何重建。
*   **动态物体多样性：** 目前主要针对“人”这一动态要素进行优化，对于其他类型的动态物体（如宠物、移动家具）的泛化能力尚待进一步验证。
*   **算力细节缺失：** 论文未披露训练成本，难以评估该方法在大规模推广时的资源消耗。

（完）
