---
title: Unbiased Multimodal Reranking for Long-Tail Short-Video Search
title_zh: 长尾短视频搜索中的无偏多模态重排序
authors: "Wenyi Xu, Feiran Zhu, Songyang Li, Renzhe Zhou, Chao Zhang, Chenglei Dai, Yuren Mao, Yunjun Gao, Yi Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24975v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 大模型驱动的视频搜索多模态重排序
tldr: 针对短视频搜索中长尾查询因行为数据稀疏导致的“马太效应”和低质内容问题，本文提出一种基于大语言模型（LLM）的多模态重排序框架。该框架利用LLM的先验知识评估视频质量，通过两阶段训练（监督微调与偏好优化）生成用户体验评分，并结合强化学习优化页面布局。实验证明该方法在离线指标和快手线上A/B测试中均显著提升了长尾搜索的质量与用户体验。
selection_source: fresh_fetch
motivation: 长尾查询下用户行为数据稀疏，导致传统模型倾向于推荐点击诱导等低质内容，产生严重的马太效应。
method: 提出一种LLM驱动的多模态重排序框架，通过多模态证据构建标注进行监督微调，并利用成对偏好优化和强化学习提升排序质量。
result: "离线实验在AUC、NDCG等指标上取得提升，线上15%流量的A/B测试显示用户体验和消费指标均有显著增长。"
conclusion: 利用LLM的知识评估内容质量能有效缓解长尾搜索中的数据偏差，是提升短视频搜索公平性与质量的有效途径。
---

## 摘要
快手每日处理数亿次搜索，短视频搜索的质量至关重要。然而，长尾查询面临严重的马太效应：稀疏的用户行为数据导致模型放大了点击诱饵和浅层内容等低质量内容。大语言模型（LLM）的最新进展提供了一种新范式，其内在的世界知识提供了一种强大的机制来评估内容质量，且不依赖于稀疏的用户交互。为此，我们提出了一种 LLM 驱动的多模态重排序框架，该框架在没有真实用户行为的情况下估计用户体验。该方法包含一个两阶段训练过程：第一阶段利用多模态证据构建高质量标注用于有监督微调，第二阶段引入成对偏好优化以帮助模型学习候选视频之间的偏序关系。在推理阶段，生成的体验分数被用于在重排序中提升高质量但曝光不足的视频，并通过强化学习进一步指导页面级优化。实验表明，该方法在包括 AUC、NDCG@K 和人工偏好判断在内的离线指标上，相较于强基准模型取得了持续的提升。覆盖 15% 流量的在线 A/B 测试进一步证明了在用户体验和消费指标上的收益，证实了该方法在长尾视频搜索场景中的实用价值。

## Abstract
Kuaishou serving hundreds of millions of searches daily, the quality of short-video search is paramount. However, it suffers from a severe Matthew effect on long-tail queries: sparse user behavior data causes models to amplify low-quality content such as clickbait and shallow content. The recent advancements in Large Language Models (LLMs) offer a new paradigm, as their inherent world knowledge provides a powerful mechanism to assess content quality, agnostic to sparse user interactions. To this end, we propose a LLM-driven multimodal reranking framework, which estimates user experience without real user behavior. The approach involves a two-stage training process: the first stage uses multimodal evidence to construct high-quality annotations for supervised fine-tuning, while the second stage incorporates pairwise preference optimization to help the model learn partial orderings among candidates. At inference time, the resulting experience scores are used to promote high-quality but underexposed videos in reranking, and further guide page-level optimization through reinforcement learning. Experiments show that the proposed method achieves consistent improvements over strong baselines in offline metrics including AUC, NDCG@K, and human preference judgement. An online A/B test covering 15\% of traffic further demonstrates gains in both user experience and consumption metrics, confirming the practical value of the approach in long-tail video search scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：长尾短视频搜索中的无偏多模态重排序

#### 1. 核心问题与研究动机
在短视频搜索场景中，约 20% 的流量来自**长尾查询（Long-tail Queries）**。由于这些查询的用户行为数据极其稀疏，传统排序模型容易陷入“马太效应”：过度依赖点击率等行为信号，导致点击诱饵（Clickbait）、封面党和低质内容被放大，而高质量但曝光不足的内容被埋没。此外，长尾搜索还面临位置偏置、流行度偏置以及图文不符（多模态不一致）等挑战。本文旨在利用大语言模型（LLM）内在的世界知识，在不依赖用户行为数据的情况下，构建一种客观的多模态内容质量评估机制。

#### 2. 方法论：LLM 驱动的多模态重排序框架
该框架通过“离线评分-在线集成”的思路，分为三个关键阶段：

*   **多模态质量对齐（SFT 阶段）：**
    *   利用多模态大模型（如 Qwen2.5-VL）对视频的标题、ASR（语音识别）、OCR（文本识别）、封面和关键帧进行综合解析。
    *   通过精心设计的 Prompt，引导模型从相关性、安全性、时效性和可信度等维度生成详细的质量分析报告，并以此数据对基础模型进行有监督微调（SFT）。
*   **成对排序对齐（PFT 阶段）：**
    *   构建同查询下的视频对，利用 LLM 生成偏好标签。
    *   引入成对排序损失（Pairwise Loss）训练评分模型（ExpModel），使其输出可比较的标量“体验分”，公式上结合了 Sigmoid 差异损失和得分分布的正则化项。
*   **重排序流水线集成：**
    *   **阶段 I（预训练增强）：** 修改重排序模型的训练目标序列，将点击项排在最前，未点击项按“体验分”降序排列，引导模型学习高质量内容的特征。
    *   **阶段 II（页面级对齐）：** 采用 **GRPO（群体相对策略优化）** 强化学习算法。奖励函数结合了原始业务奖励和基于体验分计算的 nDCG 指标，从而在保持业务指标的同时优化页面整体体验。

#### 3. 实验设计
*   **数据集：** 采样自快手搜索日志，定义 7 天 PV < 70 的查询为长尾查询。SFT 阶段包含约 18.7 万个样本，PFT 阶段包含 34.8 万个偏好对。
*   **对比基准（Benchmarks）：**
    *   **API 模型：** GPT-4o（分为纯文本/多模态、点对/列表对模式）。
    *   **重排序模型：** RankGPT（基于滑动窗口的列表重排序）。
    *   **检索模型：** BGE-m3（双编码器语义匹配）。
*   **评估指标：** NDCG@{1, 5, 10}、成对准确率（Acc/AUC）、人工偏好判断（GSB：Good/Same/Bad）。

#### 4. 资源与算力
*   **离线标注：** 使用 Qwen2.5-VL-32B 模型生成标注数据。
*   **模型训练：** 分布式设置，共 6 个节点，每个节点配备 **2 张 Tesla T4 GPU (16GB)**。
*   **在线推理：** 部署了一个轻量化的 **4M 参数** 重排序模型，使用 2 个节点，每个节点配备 **2 张 NVIDIA A10 GPU**。
*   **更新频率：** 模型权重每 12 小时刷新一次，以适应数据分布变化。

#### 5. 实验数量与充分性
论文进行了多维度的实验验证：
*   **离线对比：** 在人工标注集上对比了 5 种主流基准方法，证明了 ExpModel 的领先性。
*   **消融实验：** 验证了“SFT+PFT”两阶段训练的必要性，以及多模态信息（视觉+文本）对提升排序质量的贡献。
*   **线上 A/B 测试：** 在快手真实流量下进行了为期两周的测试，覆盖了每日超过 5000 万次查询。
*   **充分性评价：** 实验设计涵盖了从模型内在能力到端到端系统表现的完整链路，样本量大且包含真实工业场景验证，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **长尾提升显著：** 线上 A/B 测试显示，长尾查询的意图改写率（IQRR）下降 1.28%，点击率（CTR）提升 1.24%，长效观看率（LVR）提升 1.67%。
*   **多模态的重要性：** 视觉信号的加入显著提升了模型识别“封面党”和评估内容一致性的能力，尤其在人工偏好指标上提升明显。
*   **行为与质量的平衡：** 通过强化学习将体验分注入排序，可以在不显著损害短期消费指标（如点击、时长）的前提下，大幅提升搜索结果的长期用户体验。

#### 7. 优点与亮点
*   **解耦行为偏差：** 成功利用 LLM 的先验知识替代了稀疏且有偏的用户反馈，解决了长尾场景下的冷启动和马太效应问题。
*   **工业级落地：** 提出了一套从大模型离线知识蒸馏到轻量化小模型在线部署的完整方案，兼顾了效果与推理效率。
*   **页面级优化：** 不仅关注单点得分，还通过 GRPO 优化页面整体的 nDCG，提升了结果列表的连贯性。

#### 8. 不足与局限
*   **LLM 依赖性：** 训练数据的质量高度依赖于作为“教师”的多模态 LLM，若 LLM 本身存在偏见或知识盲区，会影响评分的公正性。
*   **计算成本：** 离线生成数十万条多模态标注数据的计算开销较大，对于资源受限的小型平台可能难以负担。
*   **动态性挑战：** 视频内容和用户兴趣瞬息万变，12 小时的模型更新频率在处理极具时效性的热点长尾查询时可能仍有滞后。

（完）
