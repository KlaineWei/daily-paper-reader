---
title: "Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning"
title_zh: 持久性机器人世界模型：通过强化学习稳定多步外推
authors: "Jai Bardhan, Patrik Drozdik, Josef Sivic, Vladimir Petrik"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25685v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于稳定机器人世界模型的强化学习
tldr: 针对机器人世界模型在长程自回归预测中容易出现误差累积和视觉质量退化的问题，本文提出了一种基于强化学习（RL）的后训练方案。该方法通过在模型自身的预测序列上进行训练，并结合对比RL目标、多视图视觉保真度奖励以及变长未来对比协议，显著提升了多步预测的稳定性。在DROID数据集上的实验表明，该方法在各项指标上均达到SOTA，并获得了极高的人类偏好率。
selection_source: fresh_fetch
motivation: 传统的机器人世界模型在长程自回归部署时，由于误差累积会导致预测画面迅速崩溃，难以支持长时间跨度的任务模拟。
method: 引入一种针对扩散模型的对比强化学习后训练方案，利用多视图视觉保真度奖励对模型自身的预测序列进行优化和偏好对齐。
result: "在DROID数据集上刷新了多步预测保真度的记录，LPIPS降低了14%，SSIM提升了9.1%，且在人类盲测中获得80%的偏好率。"
conclusion: 通过强化学习对世界模型进行自回归稳定性训练，可以有效解决长程视频生成的质量退化问题，为复杂机器人任务模拟提供了更可靠的工具。
---

## 摘要
动作条件机器人世界模型根据机器人动作序列生成操作场景的未来视频帧，为模拟传统物理引擎难以建模的任务提供了一种极具前景的替代方案。然而，这些模型通常针对短期预测进行优化，在自回归部署时会失效：每个预测的片段都会作为下一个片段的上下文反馈，导致误差累积，视觉质量迅速下降。我们通过以下贡献解决了这一问题。首先，我们引入了一种强化学习 (RL) 后训练方案，该方案在世界模型自身的自回归外推（rollouts）上进行训练，而非基于真实历史数据。我们通过将近期用于扩散模型的对比强化学习目标适配到我们的设置中来实现这一点，并证明其收敛性保证可以完全延续。其次，我们设计了一种训练协议，从同一个外推状态生成并比较多个候选的可变长度未来，从而强化高保真度预测并抑制低保真度预测。第三，我们开发了高效的多视图视觉保真度奖励，结合了跨摄像机视图的互补感知指标，并在片段级别进行聚合，以获得密集、低方差的训练信号。第四，我们证明了我们的方法在 DROID 数据集的外推保真度上达到了新的最先进水平，在所有指标上均优于最强的基准模型（例如，外部摄像机的 LPIPS 降低了 14%，手腕摄像机的 SSIM 提高了 9.1%），在成对比较中胜率为 98%，并在盲测人类研究中获得了 80% 的偏好率。

## Abstract
Action-conditioned robot world models generate future video frames of the manipulated scene given a robot action sequence, offering a promising alternative for simulating tasks that are difficult to model with traditional physics engines. However, these models are optimized for short-term prediction and break down when deployed autoregressively: each predicted clip feeds back as context for the next, causing errors to compound and visual quality to rapidly degrade. We address this through the following contributions. First, we introduce a reinforcement learning (RL) post-training scheme that trains the world model on its own autoregressive rollouts rather than on ground-truth histories. We achieve this by adapting a recent contrastive RL objective for diffusion models to our setting and show that its convergence guarantees carry over exactly. Second, we design a training protocol that generates and compares multiple candidate variable-length futures from the same rollout state, reinforcing higher-fidelity predictions over lower-fidelity ones. Third, we develop efficient, multi-view visual fidelity rewards that combine complementary perceptual metrics across camera views and are aggregated at the clip level for dense, low-variance training signal. Fourth, we show that our approach establishes a new state-of-the-art for rollout fidelity on the DROID dataset, outperforming the strongest baseline on all metrics (e.g., LPIPS reduced by 14% on external cameras, SSIM improved by 9.1% on the wrist camera), winning 98% of paired comparisons, and achieving an 80% preference rate in a blind human study.

---

## 论文详细总结（自动生成）

### 论文总结：Persistent Robot World Models (PersistWorld)

#### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**机器人动作条件世界模型（Action-conditioned World Models）**在长程预测中的失效问题。
*   **研究背景**：基于扩散模型的视频生成技术已被用于模拟机器人操作，但这些模型通常在“教师强制”（Teacher Forcing）模式下训练，即预测时依赖真实的过去帧。
*   **核心问题（曝光偏差）**：在实际部署（自回归外推）时，模型必须依赖自己生成的、带有微小误差的先前帧作为上下文。这些误差会迅速累积，导致生成的视频在几秒钟内崩溃——表现为物体溶解、机器人手臂结构解体或场景漂移。
*   **研究动机**：通过强化学习（RL）后训练，让模型在处理自身生成的“不完美”历史时保持稳定，从而实现持久、高保真的长程模拟。

#### 2. 方法论
论文提出了一种名为 **PersistWorld** 的框架，核心思想是利用 RL 优化模型在外推过程中的表现：
*   **对比 RL 目标适配**：借鉴了 DiffusionNFT 的对比学习思路，将其从流匹配（Flow-matching）模型适配到 $x_0$ 预测参数化的扩散模型（如 Ctrl-World 的 SVD 后干网络）。
*   **训练协议（分支对比）**：
    1.  **生成共享前缀**：从真实初始状态开始，自回归生成变长（0-9步）的视频片段，模拟不同程度的误差累积。
    2.  **分支候选序列**：从该状态出发，独立采样 $K=16$ 个候选后续片段。
    3.  **评分与排序**：使用多视图视觉奖励对候选片段进行评分，并进行组内归一化（Z-score）。
    4.  **对比更新**：通过加权损失函数，强化高奖励的预测方向，排斥低奖励的方向。
*   **多视图视觉奖励**：结合了 LPIPS（感知相似度）、SSIM（结构相似度）和 PSNR（峰值信噪比），并在手腕相机和两个外部相机视图间进行聚合，提供密集的片段级反馈。
*   **关键公式**：采用奖励加权的去噪损失 $L(\theta) = \mathbb{E} [r \| \hat{x}_0^+ - x_0 \|_2^2 + (1-r) \| \hat{x}_0^- - x_0 \|_2^2]$，其中 $r$ 是归一化奖励，$\hat{x}_0^+$ 和 $\hat{x}_0^-$ 分别是基于当前模型和参考模型构建的正负预测分支。

#### 3. 实验设计
*   **数据集**：使用大规模机器人操作数据集 **DROID**，包含多样化的桌面操作任务和三视角相机设置。
*   **Benchmark 设置**：进行 14 步自回归外推（约 11 秒视频生成），远超常规训练的单步预测。
*   **对比方法**：
    *   **WPE** (World Models as Environments)
    *   **IRASim** (Interaction-aware Robot Action Simulator)
    *   **Ctrl-World** (本文的原始基准模型)
*   **评估指标**：全帧及掩码区域（物体中心、机器人中心）的 SSIM、PSNR、LPIPS，以及人类偏好评分和策略评估相关性（Pearson Correlation）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA H200 GPU**。
*   **时长**：训练耗时 **3 天**。
*   **参数更新**：仅微调 LoRA 适配器（Rank=64）和动作编码器，冻结了 UNet 主干和 VAE。

#### 5. 实验数量与充分性
*   **实验规模**：在 DROID 验证集上进行了详尽的定量评估，并进行了 1 对 1 的成对样本对比（覆盖约 98% 的验证样本）。
*   **消融实验**：非常充分，涵盖了学习率、奖励函数构成、前缀长度采样策略、Group Size（K 值）、预测步长（H）、视图权重、KL 正则化等多个维度。
*   **客观性**：引入了**盲测人类偏好研究**（80% 偏好率）和**掩码评估**（排除背景干扰，专注于物体和机器人动态），确保了结果的客观性和任务相关性。

#### 6. 主要结论与发现
*   **性能飞跃**：PersistWorld 在所有视觉指标上均刷新了 SOTA。在手腕相机（最难预测的视角）上，SSIM 提升了 9.1%，LPIPS 显著下降。
*   **长程稳定性**：相比基准模型在 5 秒后画面崩溃，PersistWorld 能在 11 秒的外推中保持物体结构完整。
*   **策略评估效用**：该模型在评估不同机器人策略（如 $\pi_0$, GROOT）时，其预测结果与真实世界表现的相关性更高（Pearson $r=0.822$）。
*   **误差鲁棒性**：通过在自身生成的历史数据上训练，模型学会了纠正漂移，而非仅仅记忆真值轨迹。

#### 7. 优点
*   **理论严谨**：给出了 $x_0$ 参数化下对比 RL 目标的完整数学证明。
*   **训练协议创新**：变长前缀采样有效地覆盖了从“轻微漂移”到“严重崩溃”的各种分布，增强了鲁棒性。
*   **实用性强**：显著提升了世界模型作为“虚拟仿真环境”的可靠性，为离线策略评估提供了可能。

#### 8. 不足与局限
*   **计算开销**：后训练阶段需要为每个更新步骤采样 $K=16$ 个候选序列，推理成本较高，训练效率低于标准监督微调。
*   **物理约束缺失**：目前的奖励函数完全基于视觉像素/感知指标，尚未显式引入物理定律（如重力、碰撞检测）或几何一致性约束。
*   **应用范围**：目前仅在 DROID 数据集的桌面操作场景验证，尚未测试在更复杂、非结构化环境下的泛化能力。

（完）
