---
title: "PivotRL: High Accuracy Agentic Post-Training at Low Compute Cost"
title_zh: PivotRL：低计算成本下的高精度智能体后训练
authors: "Junkeun Yi, Damon Mosk-Aoyama, Baihe Huang, Ritu Gala, Charles Wang, Sugam Dipak Devare, Khushi Bhardwaj, Abhibha Gupta, Oleksii Kuchaiev, Jiantao Jiao, Jian Zhang, Venkat Srinivasan"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21383v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 使用强化学习进行长程智能体任务的后训练
tldr: PivotRL 是一种针对长程智能体任务的高效后训练框架，旨在平衡监督微调（SFT）的计算效率与端到端强化学习（E2E RL）的泛化能力。它通过在现有 SFT 轨迹上执行局部策略采样来识别关键“枢纽”步骤，并利用功能等效奖励进行优化。该方法在显著降低计算成本的同时，提升了领域内及域外任务的准确率，已成功应用于 NVIDIA 的大规模生产模型中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21383v1/fig-001.webp\", \"caption\": \"Figure 2 | 𝜏2-Bench accuracy over RL training. 𝒟𝑎𝑑𝑣 yields the strongest optimization and outperform same-data SFT.\", \"page\": 10, \"index\": 1, \"width\": 461, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2603.21383v1/fig-002.webp\", \"caption\": \"Figure 3 | 𝜏2-Bench reward standard deviation during RL training. 𝒟𝑎𝑑𝑣 sets maintain higher reward variance and therefore more informative local updates.\", \"page\": 10, \"index\": 2, \"width\": 463, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2603.21383v1/fig-003.webp\", \"caption\": \"Table 6 shows a monotonic benefit from more selective filtering: random pivots already improve over SFT (59.68 vs. 58.44), and low-reward-mean pivots yield the best result (63.81).\", \"page\": 18, \"index\": 3, \"width\": 933, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2603.21383v1/fig-004.webp\", \"caption\": \"Figure 1 | SWE-Bench accuracy vs. (a) cumulative rollout turns and (b) cumulative rollout time during training, using the same number of compute nodes. PivotRL reaches comparable accuracy to E2E RL with ∼4× fewer rollout turns and ∼5.5× less wall-clock time.\", \"page\": 8, \"index\": 4, \"width\": 983, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2603.21383v1/fig-005.webp\", \"caption\": \"Table 2 | OOD performance change (Δ) relative to Base, averaged across the four training runs in Table 1. SFT degrades by −9.83 on average; PivotRL maintains near-zero change (+0.21).\", \"page\": 8, \"index\": 5, \"width\": 517, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2603.21383v1/fig-006.webp\", \"caption\": \"Table 3 | Per-domain OOD breakdown across four single-domain training runs. Each cell shows score followed by Δ relative to Base. PivotRL (rl) consistently preserves OOD performance, whereas SFT (sft) causes broad regression—particularly after terminal-domain training.\", \"page\": 9, \"index\": 6, \"width\": 908, \"height\": 659}]"
motivation: 旨在解决 SFT 在智能体任务中泛化能力不足以及端到端强化学习计算成本过高之间的矛盾。
method: 通过在 SFT 轨迹中识别结果方差大的关键步骤（枢纽）进行局部在线采样，并采用功能等效奖励机制进行策略优化。
result: "在多个领域内准确率提升 4.17%，域外准确率提升 10.04%，且在编码任务中仅需 E2E RL 四分之一的采样量即可达到同等性能。"
conclusion: PivotRL 证明了结合局部在线采样与功能性奖励可以在低计算成本下实现高精度的智能体后训练，具有极强的实用价值。
---

## 摘要
长程智能体任务的后训练在计算效率与泛化能力之间存在矛盾。虽然有监督微调（SFT）计算效率高，但往往面临域外（OOD）性能退化的问题。相反，端到端强化学习（E2E RL）能够保持 OOD 能力，但由于需要多轮策略内（on-policy）采样，导致计算成本极高。我们提出了 PivotRL，这是一个基于现有 SFT 轨迹运行的新型框架，旨在结合 SFT 的计算效率与 E2E RL 的 OOD 准确率。PivotRL 依赖两个关键机制：首先，它执行局部策略内采样并筛选“枢轴点”（pivots），即采样动作在结果上表现出高方差的信息性中间轮次；其次，它利用功能等效动作的奖励，而非要求与 SFT 数据示例进行严格的字符串匹配。我们从理论上证明，这些机制能激励具有高自然梯度范数的强学习信号，同时最大限度地保留与训练任务无关动作的策略概率排序。与在相同数据上进行的标准 SFT 相比，我们证明 PivotRL 在四个智能体领域中的平均域内准确率提高了 4.17%，在非智能体任务中的 OOD 准确率提高了 10.04%。值得注意的是，在智能体编程任务中，PivotRL 以少 4 倍的采样轮次实现了与 E2E RL 相当的准确率。PivotRL 已被 NVIDIA 的 Nemotron-3-Super-120B-A12B 采用，成为生产级智能体后训练的核心工具。

## Abstract
Post-training for long-horizon agentic tasks has a tension between compute efficiency and generalization. While supervised fine-tuning (SFT) is compute efficient, it often suffers from out-of-domain (OOD) degradation. Conversely, end-to-end reinforcement learning (E2E RL) preserves OOD capabilities, but incurs high compute costs due to many turns of on-policy rollout. We introduce PivotRL, a novel framework that operates on existing SFT trajectories to combine the compute efficiency of SFT with the OOD accuracy of E2E RL. PivotRL relies on two key mechanisms: first, it executes local, on-policy rollouts and filters for pivots: informative intermediate turns where sampled actions exhibit high variance in outcomes; second, it utilizes rewards for functional-equivalent actions rather than demanding strict string matching with the SFT data demonstration. We theoretically show that these mechanisms incentivize strong learning signals with high natural gradient norm, while maximally preserving policy probability ordering on actions unrelated to training tasks. In comparison to standard SFT on identical data, we demonstrate that PivotRL achieves +4.17% higher in-domain accuracy on average across four agentic domains, and +10.04% higher OOD accuracy in non-agentic tasks. Notably, on agentic coding tasks, PivotRL achieves competitive accuracy with E2E RL with 4x fewer rollout turns. PivotRL is adopted by NVIDIA's Nemotron-3-Super-120B-A12B, acting as the workhorse in production-scale agentic post-training.

---

## 论文详细总结（自动生成）

### PivotRL：低计算成本下的高精度智能体后训练论文总结

#### 1. 核心问题与整体含义
论文针对长程智能体任务（如工具调用、编程、终端交互、网页搜索）的后训练（Post-training）提出了 **PivotRL** 框架。
*   **研究动机**：现有的后训练方法存在“效率与泛化的矛盾”。**监督微调（SFT）**计算效率高但容易导致模型在域外（OOD）任务上性能崩溃（灾难性遗忘）；**端到端强化学习（E2E RL）**泛化性好但计算成本极高，因为每一步更新都需要进行多轮完整的环境交互采样。
*   **核心目标**：在保留 SFT 数据效率的同时，获得 E2E RL 的泛化能力，且不产生高昂的采样开销。

#### 2. 方法论
PivotRL 的核心思想是**在现有的 SFT 专家轨迹上进行局部、高信息量的策略内（On-policy）采样与优化**。
*   **枢纽筛选（Pivot Filtering）**：
    *   从 SFT 轨迹中提取中间步骤（Turn）。
    *   **离线评估**：使用参考模型对每个步骤进行多次采样，计算奖励的方差（$\sigma^2$）和均值（$\mu$）。
    *   **筛选准则**：仅保留“枢纽点”（Pivots），即那些结果具有高方差（既有成功也有失败）且均值较低（具有挑战性）的步骤。这确保了训练信号具有较高的自然梯度范数。
*   **功能等效奖励（Functional Rewards）**：
    *   抛弃传统的“严格字符串匹配”（即要求模型输出与 SFT 专家完全一致）。
    *   使用**领域校验器（Verifiers）**（如代码执行结果、Schema 校验、LLM 裁判）来奖励功能上正确但文本上可能不同的动作。
*   **算法流程**：
    1. 提取 SFT 轨迹中的所有步骤作为候选。
    2. 离线筛选出枢纽步骤。
    3. 在枢纽步骤上进行局部单步采样。
    4. 使用基于组相对策略优化（GRPO）的目标函数进行训练，结合 KL 散度约束以保持模型稳定性。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **对话工具调用**：$\tau^2$-Bench。
    *   **软件工程/编程**：SWE-Bench Verified。
    *   **终端控制**：Terminal-Bench。
    *   **网页浏览**：BrowseComp。
*   **对比方法**：
    *   **Base 模型**：Qwen3-30B-A3B-Thinking。
    *   **SFT**：在相同专家轨迹上进行标准微调。
    *   **E2E RL**：在 SWE-Bench 上进行完整的端到端强化学习。
*   **域外（OOD）评估**：在 AIME25（数学）、MMLU-Pro（综合知识）、LiveCodeBench（竞赛编程）等 8 个非智能体基准上测试泛化能力。

#### 4. 资源与算力
*   **算力效率**：论文强调了 PivotRL 的低成本。在 SWE-Bench 上，达到相同准确率时，PivotRL 比 E2E RL 减少了 **4 倍**的采样轮次（Rollout turns），节省了约 **5.5 倍**的实际训练时间（Wall-clock time）。
*   **具体配置**：实验使用了 NVIDIA 的 Nemo-RL 和 Nemo-Gym 库。虽然未详细列出具体的 GPU 总数（如 H100 数量），但明确提到在“相同计算节点数”下进行了对比。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主要的智能体领域和 8 个 OOD 领域，实验设计较为全面。
*   **消融实验**：详细对比了“有无枢纽筛选”和“有无功能奖励”的影响，证明了两个组件缺一不可。
*   **客观性**：通过在相同数据、相同 Prompt 和相同 Base 模型下对比 SFT 与 PivotRL，确保了实验的公平性。此外，该方法已在 NVIDIA 生产级模型 Nemotron-3-Super 中得到验证。

#### 6. 主要结论与发现
*   **域内提升**：PivotRL 在四个智能体领域平均比 SFT 高出 **4.17%** 的准确率。
*   **OOD 保持**：SFT 在训练后导致 OOD 任务平均下降 9.83%（灾难性遗忘），而 PivotRL 几乎没有下降（+0.21%），成功解决了泛化退化问题。
*   **理论验证**：论文从理论上证明了奖励方差决定了学习信号的强度，且功能性奖励能最大程度保留非相关任务的概率排序。
*   **生产价值**：PivotRL 能够以极低的成本实现接近甚至优于大规模 E2E RL 的效果。

#### 7. 优点
*   **高性价比**：通过局部采样避开了长程轨迹采样的巨大开销。
*   **解决遗忘问题**：通过 KL 约束和功能性奖励，在提升特定能力的同时保护了模型的通用能力。
*   **理论与实践结合**：不仅有严谨的数学证明，还有大规模生产环境的部署验证。
*   **灵活性**：筛选机制可以根据计算预算动态调整训练数据的难度和规模。

#### 8. 不足与局限
*   **依赖专家轨迹**：PivotRL 仍然需要初始的 SFT 轨迹作为采样的“锚点”，无法完全脱离高质量数据进行冷启动。
*   **校验器设计成本**：虽然功能奖励效果好，但为每个新领域设计可靠的 Verifier（校验器）仍需要一定的人工努力。
*   **局部性限制**：由于只在单步（Turn-level）进行优化，可能无法完全捕捉到某些极度依赖长程逻辑耦合的复杂策略（尽管实验显示其在 SWE-Bench 上表现良好）。

（完）
