---
title: "SumRank: Aligning Summarization Models for Long-Document Listwise Reranking"
title_zh: SumRank：面向长文档列表式重排序的摘要模型对齐
authors: "Jincheng Feng, Wenhan Liu, Zhicheng Dou"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24204v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 通过摘要和强化学习实现LLM文档排序与检索
tldr: 针对大语言模型在长文档列表重排序中面临的效率与效果挑战，本文提出 SumRank 模型。该模型通过三阶段训练（SFT、RL数据构建、排序驱动对齐）将长文档压缩为保留关键排序信号的摘要。实验表明，SumRank 在 TREC DL 多个数据集上达到 SOTA 性能，显著降低了计算开销并提升了重排序效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24204v1/fig-001.webp\", \"caption\": \"Figure 1: The left part shows the method of naive LLMbased long document listwise rerank. The right part shows our proposed SumRank, which is better than the naive listwise method both in effectiveness and efficiency. Sum1, Sum2, . . . ,SumN denote the summaries generated by SumRank.\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2603.24204v1/fig-002.webp\", \"caption\": \"Table 1: The results (NDCG@10 and MAP@100) on TREC DL 19-23 benchmark. All baselines rerank the BM25-retrieved top-100 documents. The best results are highlighted in bold, and the second-best results are underlined. The strongest baseline in each column is marked with †.\", \"page\": 6, \"index\": 2, \"width\": 941, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2603.24204v1/fig-003.webp\", \"caption\": \"Table 2: Ablation study and impact anlysis of data construction stage. “TREC Avg.” reports the average NDCG@10 scores evaluated across all five TREC DL benchmarks (DL19–23). ∆ indicates the absolute performance degradation on specific training stages or different data construction strategies.\", \"page\": 7, \"index\": 3, \"width\": 473, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2603.24204v1/fig-004.webp\", \"caption\": \"Figure 2: The overall architecture of SumRank, which consists of three training stages and the Summary-then-Rank pipeline (Listwise Rerank with Pointwise Summary).\", \"page\": 4, \"index\": 4, \"width\": 946, \"height\": 535}]"
motivation: 大语言模型在处理长文档列表重排序时，由于上下文过长导致效率低下且排序效果受损。
method: 提出一种与下游排序目标对齐的逐点摘要模型 SumRank，通过强化学习确保摘要保留核心检索相关性信号。
result: 在 TREC DL 19-23 五个基准数据集上，SumRank 实现了最先进的排序性能，并大幅减少了推理延迟。
conclusion: 将摘要模型与排序任务对齐是解决长文档重排序效率与效果矛盾的有效途径。
---

## 摘要
大语言模型（LLMs）在列表式段落重排序任务中表现出了卓越的性能。然而，由于上下文长度大幅增加，直接将其应用于长文档排序会带来有效性和效率方面的问题。为了应对这一挑战，我们提出了一种与下游列表式重排序相对齐的逐点摘要模型 SumRank，旨在最终列表式重排序阶段之前，将长文档压缩为简洁且与排序对齐的摘要。为了获得 SumRank 摘要模型，我们引入了一个三阶段训练流水线，包括冷启动有监督微调（SFT）、专门的强化学习（RL）数据构建以及通过强化学习进行的排序驱动对齐。该范式使 SumRank 与下游排序目标保持一致，从而保留相关性信号。我们在来自 TREC 深度学习赛道（TREC DL 19-23）的五个基准数据集上进行了广泛的实验。结果表明，我们的轻量级 SumRank 模型在达到最先进（SOTA）排序性能的同时，通过减少摘要开销和重排序复杂度，显著提高了效率。

## Abstract
Large Language Models (LLMs) have demonstrated superior performance in listwise passage reranking task. However, directly applying them to rank long-form documents introduces both effectiveness and efficiency issues due to the substantially increased context length. To address this challenge, we propose a pointwise summarization model SumRank, aligned with downstream listwise reranking, to compress long-form documents into concise rank-aligned summaries before the final listwise reranking stage. To obtain our summarization model SumRank, we introduce a three-stage training pipeline comprising cold-start Supervised Fine-Tuning (SFT), specialized RL data construction, and rank-driven alignment via Reinforcement Learning. This paradigm aligns the SumRank with downstream ranking objectives to preserve relevance signals. We conduct extensive experiments on five benchmark datasets from the TREC Deep Learning tracks (TREC DL 19-23). Results show that our lightweight SumRank model achieves state-of-the-art (SOTA) ranking performance while significantly improving efficiency by reducing both summarization overhead and reranking complexity.

---

## 论文详细总结（自动生成）

### SumRank：面向长文档列表式重排序的摘要模型对齐

#### 1. 核心问题与整体含义（研究动机和背景）
在信息检索（IR）领域，大语言模型（LLM）在列表式重排序（Listwise Reranking）任务中表现卓越，能够捕捉文档间的全局相关性。然而，当处理**长文档**时，直接应用 LLM 面临两大挑战：
*   **效率瓶颈**：长文本导致计算复杂度呈平方级增长，推理延迟极高。
*   **效果受损**：过长的上下文增加了模型的建模负担，且简单的截断（Truncation）或分块（Chunking）策略会丢失关键信息或破坏语义连贯性。
**SumRank** 的核心动机是：通过一个专门训练的摘要模型，将长文档压缩为保留“排序信号”的短摘要，从而在保证排序精度的同时大幅提升效率。

#### 2. 方法论：核心思想与技术细节
SumRank 采用“先摘要后排序”（Summarize-then-Rank）的范式，其核心在于通过三阶段训练流水线，使摘要模型与下游排序目标对齐：

*   **阶段 1：冷启动有监督微调（Cold-Start SFT）**
    *   使用强力教师模型（如 Qwen2.5-72B）对长文档生成高质量摘要，作为金标准数据。
    *   轻量级学生模型（3B/7B）通过最小化自回归交叉熵损失进行蒸馏，获得基础的文档压缩能力。
*   **阶段 2：强化学习（RL）数据构建**
    *   构建包含查询、正样本和负样本的候选列表。
    *   为减少 RL 训练中的计算开销和方差，采用“静态背景摘要”策略：在评估某一目标文档的摘要质量时，保持列表中其他文档的摘要固定不变。
*   **阶段 3：排序驱动的 GRPO 对齐**
    *   使用**组相对策略优化（GRPO）**算法。
    *   **奖励函数（Reward）**：直接使用下游 LLM 排序器在生成的摘要列表上计算出的 **NDCG@10** 作为奖励信号。
    *   **安全机制**：引入惩罚因子 $\lambda$ 和语义匹配指标，强制模型在文档不相关时输出“未发现相关信息”，防止模型伪造相关性信号。

#### 3. 实验设计
*   **数据集**：使用了 TREC Deep Learning (DL) 赛道的五个标准基准数据集：TREC DL 19、20、21、22、23（均源自 MS MARCO）。
*   **评价指标**：NDCG@10 和 MAP@100。
*   **对比方法（Baselines）**：
    *   **规则类**：BM25、FirstP（截断前 128/256/512 令牌）。
    *   **传统摘要模型**：BART、LED（长文档 Transformer）、PEGASUS。
    *   **零样本 LLM**：Qwen2.5-3B/7B-Instruct 直接生成摘要。
*   **下游排序器**：统一使用 Qwen2.5-32B-Instruct 进行列表式重排序。

#### 4. 资源与算力
论文详细列出了实验所用的算力资源：
*   **SFT 阶段**：使用 4 台 **NVIDIA A800 (80GB)** GPU，训练 5 个 epoch，学习率为 $5 \times 10^{-6}$。
*   **RL (GRPO) 阶段**：
    *   3B 模型：使用 2 台 NVIDIA A800。
    *   7B 模型：使用 4 台 NVIDIA A800。
    *   训练 5 个 epoch，学习率为 $1 \times 10^{-6}$，每组采样 $G=8$ 个 rollout。
*   **教师模型/排序器**：调用了 Qwen2.5-72B-Instruct 作为教师和奖励计算器。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度对比**：在 5 个不同年份的 TREC 数据集上验证，证明了模型的鲁棒性和泛化能力。
*   **消融实验**：分别验证了“冷启动 SFT”和“排序驱动 GRPO”的必要性，结果显示缺少任何一环都会导致性能显著下降。
*   **策略分析**：对比了动态更新背景摘要与静态背景摘要的效果，证明了静态策略在效率和稳定性上的优势。
*   **效率分析**：详细对比了不同方法的推理延迟（Latency），证明了 SumRank 在速度上的巨大优势。

#### 6. 主要结论与发现
*   **性能领先**：SumRank (7B) 在所有数据集上均达到了 SOTA 性能，显著优于传统的摘要模型和简单的截断策略。
*   **以小博大**：经过对齐训练的 SumRank (3B) 在排序效果上接近 72B 模型的直接排序结果，且推理速度快了 **42 倍**；SumRank (7B) 则在效果和速度上全面超越了 72B 模型。
*   **对齐的重要性**：传统的摘要模型（如 BART）虽然生成的文本通顺，但会过滤掉排序所需的关键匹配信号，而 SumRank 通过 RL 成功保留了这些信号。

#### 7. 优点
*   **任务对齐思想**：不追求通用的摘要质量，而是将摘要模型作为排序任务的“特征提取器”，目标函数直接指向最终的检索指标。
*   **效率与效果的双赢**：通过压缩输入长度，解决了 LLM 处理长文本时的二次方复杂度问题，同时通过 RL 弥补了信息压缩带来的损失。
*   **训练策略巧妙**：采用 GRPO 和静态背景摘要列表，极大地降低了列表式强化学习的计算门槛。

#### 8. 不足与局限
*   **两阶段架构开销**：虽然推理速度快，但仍需维护“摘要”和“排序”两个模型，增加了系统部署的复杂性。
*   **模型规模限制**：受限于算力，未在更大规模（如 14B 或 32B）的开源模型上验证对齐效果。
*   **端到端缺失**：目前摘要和排序是分离的，未来若能实现端到端的统一架构（一个模型同时完成压缩与评分），可能会进一步优化性能。

（完）
