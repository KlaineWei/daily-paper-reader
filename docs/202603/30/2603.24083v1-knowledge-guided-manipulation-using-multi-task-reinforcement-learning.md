---
title: Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning
title_zh: 基于多任务强化学习的知识引导操纵
authors: "Aditya Narendra, Mukhammadrizo Maribjonov, Dmitry Makarov, Dmitry Yudin, Aleksandr Panov"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24083v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 基于模型的强化学习用于多任务机器人操控
tldr: 本研究提出KG-M3PO框架，旨在解决部分可观测环境下的多任务机器人操作问题。该框架通过将自我中心视觉与在线3D场景图结合，构建包含空间、包含和功能关系的动态关系表示。通过端到端强化学习训练图神经网络编码器，使关系特征直接服务于控制决策。实验证明，该方法在处理遮挡、干扰和布局变化时，显著提升了成功率、采样效率及对新物体的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24083v1/fig-001.webp\", \"caption\": \"TABLE II: Short- & long-horizon performance (M3PO vs. KG-M3PO).\", \"page\": 6, \"index\": 1, \"width\": 498, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.24083v1/fig-002.webp\", \"caption\": \"Fig. 6: Single-task results. Average normalized score vs. timesteps. Adding the KG improves sample efficiency and final score. For the M3PO backbone, the camera-only variant corresponds to M3PO, while camera+KG corresponds to KGM3PO.\", \"page\": 7, \"index\": 2, \"width\": 1054, \"height\": 538}]"
motivation: 针对部分可观测和复杂环境下的多任务机器人操作，传统方法缺乏结构化知识引导，导致泛化性和效率不足。
method: 提出KG-M3PO框架，利用动态3D场景图整合多模态信息，并通过端到端强化学习优化关系特征以驱动控制策略。
result: 在包含遮挡和干扰的多种操作任务中，该方法在成功率、采样效率和对新场景的泛化性上均优于强基准模型。
conclusion: 结构化且持续维护的世界知识是实现可扩展、通用化机器人操作的强大归纳偏置，能有效提升复杂环境下的控制鲁棒性。
---

## 摘要
本文介绍了基于知识图谱的大规模多任务基于模型的策略优化（KG-M3PO），这是一个在部分可观测环境下统一了感知、知识和策略的多任务机器人操纵框架。该方法通过在线 3D 场景图增强了第一视角视觉，将开放词汇检测落地为度量式的关系表示。动态关系机制在每一步更新空间、包含和可供性（affordance）边，并且图神经网络编码器通过强化学习（RL）目标进行端到端训练，使得关系特征直接由控制性能塑造。多种观测模态（视觉、本体感受、语言和基于图的）被编码到一个共享的潜空间中，RL 智能体在此基础上运行以驱动控制循环。策略以轻量级图查询以及视觉和本体感受输入为条件，为决策提供了一个紧凑且具有语义信息的决策状态。在一系列包含遮挡、干扰物和布局变化的操纵任务上的实验表明，该方法相比强基线模型取得了持续的提升：知识条件化智能体实现了更高的成功率、更高的样本效率，以及对新物体和未见场景配置更强的泛化能力。这些结果支持了这样一个前提，即结构化、持续维护的世界知识是实现可扩展、可泛化操纵的强大归纳偏置：当知识模块参与 RL 计算图时，关系表示与控制相对齐，从而在部分可观测性下实现稳健的长程行为。

## Abstract
This paper introduces Knowledge Graph based Massively Multi-task Model-based Policy Optimization (KG-M3PO), a framework for multi-task robotic manipulation in partially observable settings that unifies Perception, Knowledge, and Policy. The method augments egocentric vision with an online 3D scene graph that grounds open-vocabulary detections into a metric, relational representation. A dynamic-relation mechanism updates spatial, containment, and affordance edges at every step, and a graph neural encoder is trained end-to-end through the RL objective so that relational features are shaped directly by control performance. Multiple observation modalities (visual, proprioceptive, linguistic, and graph-based) are encoded into a shared latent space, upon which the RL agent operates to drive the control loop. The policy conditions on lightweight graph queries alongside visual and proprioceptive inputs, yielding a compact, semantically informed state for decision making.   Experiments on a suite of manipulation tasks with occlusions, distractors, and layout shifts demonstrate consistent gains over strong baselines: the knowledge-conditioned agent achieves higher success rates, improved sample efficiency, and stronger generalization to novel objects and unseen scene configurations. These results support the premise that structured, continuously maintained world knowledge is a powerful inductive bias for scalable, generalizable manipulation: when the knowledge module participates in the RL computation graph, relational representations align with control, enabling robust long-horizon behavior under partial observability.

---

## 论文详细总结（自动生成）

以下是对论文《Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人在非结构化日常环境中的操纵任务面临**部分可观测性（Partial Observability）**的挑战。物体常被遮挡、移动或隐藏在容器内，纯反应式策略（如像素到扭矩的映射）难以处理长程任务和状态混淆。
*   **研究动机**：
    *   现有的端到端强化学习（RL）缺乏持久的结构化知识来应对遮挡。
    *   传统的基于模型的路径规划依赖于脆弱的手工几何流水线。
    *   需要一个统一的框架，将**感知（Perception）**、**知识（Knowledge）**和**策略（Policy）**结合，以实现具备多任务能力且可泛化的机器人操纵。

### 2. 方法论：KG-M3PO 框架
*   **核心思想**：在基于模型的策略优化算法（M3PO）基础上，引入动态更新的**3D 场景图（3DSG）**作为结构化世界知识，并通过强化学习目标函数**端到端地训练**图神经网络（GNN）编码器。
*   **关键技术细节**：
    *   **在线场景图构建**：利用 BBQ 算法从自我中心视觉流中提取物体（CLIP 嵌入、3D 边界框）及其空间关系（如“在……之上”、“在……之后”）。
    *   **动态关系机制**：每 10 个步骤刷新受影响的子图，实时更新空间、包含和可供性（Affordance）边缘。
    *   **多模态融合**：将视觉特征（腕部摄像头）、本体感受（关节状态）、语言目标和图知识向量（GNN 输出）编码到共享的潜空间中。
    *   **端到端训练**：GNN 编码器的参数 $\phi$ 直接通过 RL 的策略梯度和价值损失进行优化。这意味着关系特征的提取是直接由控制性能（即任务成功率）驱动的，而非辅助监督任务。

### 3. 实验设计
*   **实验场景**：基于 NVIDIA Isaac Lab 构建的物理仿真环境，使用 GPU 加速的 PhysX 5 动力学和 RTX 渲染。
*   **机器人平台**：Franka Emika Panda（7自由度）和 UR5（6自由度）。
*   **任务设置**：
    *   **全可观测（FO）任务**：抓取方块、放置方块、打开柜子。
    *   **部分可观测（PO）任务**：抓取被墙遮挡的物体、移除遮挡物后抓取并放置物体。
*   **基准模型（Benchmarks）**：对比了 PPO、SAC、DreamerV3、TD-MPC2、IMPALA 以及原始的 M3PO（仅相机输入）。
*   **评估指标**：归一化得分（0-1000，锚定随机策略和专家表现）和任务成功率。

### 4. 资源与算力
*   **硬件配置**：单台工作站，配备 **NVIDIA RTX 5070 Ti (16 GB VRAM)** 和 64 GB RAM。
*   **并行规模**：利用 Isaac Lab 的平铺渲染 API，在单个进程中并行运行 **1024 个环境**。
*   **训练时长**：
    *   标准 M3PO（仅相机）：4亿步训练约需 0.61 GPU-天。
    *   KG-M3PO（带知识图谱）：4亿步训练约需 0.79 GPU-天。
    *   虽然 KG 增加了约 30% 的计算开销，但在达到相同性能（如得分 600）时，KG-M3PO 所需的总步数和总时长远少于基线模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种核心任务，并在两种不同的机器人平台上进行了验证。
*   **消融实验**：
    *   **输入模态对比**：对比了“仅相机”与“相机+知识图谱”的表现。
    *   **噪声鲁棒性实验**：针对场景图的几何不确定性（中心点噪声、尺寸噪声、旋转噪声）进行了三级强度测试。
*   **充分性评价**：实验设计较为全面，不仅验证了性能提升，还通过多任务学习和噪声消融证明了框架的泛化性和鲁棒性。对比算法涵盖了当前主流的在线、离线、基于模型和无模型的 RL 算法，具有较高的客观性。

### 6. 主要结论与发现
*   **知识引导的必要性**：在部分可观测任务中，仅靠相机输入的基线模型几乎无法完成任务，而 KG-M3PO 能够通过持久的图结构维持物体状态，显著提升成功率。
*   **采样效率提升**：即使在全可观测任务中，结构化知识作为一种强大的归纳偏置，也能显著加快学习速度。
*   **端到端优势**：将 GNN 纳入 RL 计算图，使得关系表示能更好地服务于控制决策，优于预训练或分离的图特征。
*   **鲁棒性**：策略对中等程度的几何噪声（如 5% 的位置偏移）具有较强的容错能力。

### 7. 优点与亮点
*   **统一性**：成功将高层语义关系（场景图）与底层连续控制（RL）在同一个计算图中统一。
*   **动态更新**：提出的在线关系更新机制解决了静态场景图容易过时的问题。
*   **多任务扩展性**：展示了单个智能体同时处理多种异构任务的能力，且在不同机器人平台上具有一致性。

### 8. 不足与局限
*   **缺乏实机验证**：所有实验均在仿真环境（Isaac Sim）中完成，尚未在真实物理机器人上进行 Sim-to-Real 迁移测试。
*   **对感知质量的依赖**：虽然有噪声消融，但如果底层物体检测完全失效，场景图的构建将受到根本性影响。
*   **计算开销**：引入场景图和 GNN 增加了每一步的推理延迟，对于实时性要求极高的任务可能存在挑战。
*   **场景复杂度限制**：实验场景相对受限（桌面操纵），尚未在更大规模、更复杂的室内导航+操纵任务中验证。

（完）
