---
title: "UniScale: Synergistic Entire Space Data and Model Scaling for Search Ranking"
title_zh: UniScale：搜索排序中协同的全空间数据与模型缩放
authors: "Liren Yu, Caiyuan Li, Feiyi Dong, Tao Zhang, Zhixuan Zhang, Dan Ou, Haihong Tang, Bo Zheng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24226v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 推荐与搜索排序的缩放定律
tldr: 针对工业搜索排序中模型规模增加带来的边际收益递减和数据异构性挑战，本文提出UniScale框架。该框架通过协同设计数据与架构，引入全空间样本系统（ES3）以扩展高质量训练信号，并结合异构层次样本融合Transformer（HHSFT）来有效建模复杂分布。在大型电商搜索平台的实验证明，UniScale显著提升了业务指标并展现出清晰的扩展趋势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24226v1/fig-001.webp\", \"caption\": \"Figure 1: Overall Architecture of Proposed UniScale, which combines Entire Space Sample System (ES3) for high quality data scaling and Heterogeneous Hierarchical Sample Fusion Transformer (HHSFT) for fully incorporating user interests expressed by entire space user behavior.\", \"page\": 3, \"index\": 1, \"width\": 506, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2603.24226v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of the Entire-Space Sample System (ES3). ES3 constructs a unified, bias-mitigated training set by synergistically integrating samples across the entire user behavior space. The framework comprises two core modules: (i) Intradomain Sample and Label Expansion; (ii) Cross-domain Sample Searchification.\", \"page\": 4, \"index\": 2, \"width\": 506, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.24226v1/fig-003.webp\", \"caption\": \"Figure 3: Overview of Heterogeneous Hierarchical Sample Fusion Transformer (HHSFT) architecture. The HHSFT is designed to exploit model and data scaling in ranking systems through two primary stages. First, the Heterogeneous Hierarchical Feature Interaction (HHFI) stage employs heterogeneous feature attention layers with token-specific projection matrices and FFNs to preserve domain-unique distributions, followed by a global feature attention layer to capture high-order cross-domain interactions. Second, the Entire Space User Interest Fusion (ESUIF) stage comprises: Domain-Routed Expert Fusion (DREF) utilizes sample routing constraints to disentangle shared knowledge from domain-specific patterns; and Domain-Aware Personalized Gated Attention (DAPGA) adaptively modulates cross-domain information transfer via personalized gating.\", \"page\": 5, \"index\": 3, \"width\": 1056, \"height\": 400}]"
motivation: 现有的缩放定律研究多关注架构而忽视了数据与架构的协同，导致单纯增加模型参数的收益递减且难以处理复杂的数据异构性。
method: 提出了包含全空间样本系统（ES3）和异构层次样本融合Transformer（HHSFT）的协同设计框架，从数据扩展和架构优化两方面提升性能。
result: 在大规模真实电商搜索平台上的实验表明，UniScale在关键业务指标上取得了显著提升，并表现出明显的扩展效应。
conclusion: 通过数据与架构的协同优化，UniScale成功突破了仅靠模型调优的性能瓶颈，为搜索排序系统的规模化提供了有效路径。
---

## 摘要
近期大语言模型（LLMs）的进展激发了工业界搜索、广告和推荐系统中缩放法则（scaling law）研究的热潮。然而，现有方法主要集中在架构改进上，忽视了数据与架构设计之间关键的协同作用。我们观察到，仅缩放模型参数表现出收益递减，即随着模型规模的增加，性能的边际收益稳步下降，且由复杂异构数据分布引起的性能退化通常难以仅通过模型设计来修复。在本文中，我们提出了 UniScale 来解决这些局限性，这是一种联合优化数据和架构以释放模型缩放全部潜力的新型协同设计框架，包含两个核心部分：(1) ES$^3$（全空间样本系统），一个高质量的数据缩放系统，它通过分层标签归因构建的全局监督信号从域内请求上下文，以及在搜索领域相似内容曝光环境下对齐用户决策本质的跨域样本，扩展了传统采样策略之外的训练信号；(2) HHSFT（异构分层样本融合 Transformer），一种旨在有效建模缩放数据复杂异构分布的新型架构，通过异构分层特征交互和全空间用户兴趣融合来利用全空间用户行为数据，从而突破了仅靠结构模型调优的性能瓶颈。在大规模真实电商搜索平台上的广泛实验表明，UniScale 通过数据与架构的协同设计实现了显著改进，并表现出明显的缩放趋势，在关键业务指标上带来了实质性提升。

## Abstract
Recent advances in Large Language Models (LLMs) have inspired a surge of scaling law research in industrial search, advertising, and recommendation systems. However, existing approaches focus mainly on architectural improvements, overlooking the critical synergy between data and architecture design. We observe that scaling model parameters alone exhibits diminishing returns, i.e., the marginal gain in performance steadily declines as model size increases, and that the performance degradation caused by complex heterogeneous data distributions is often irrecoverable through model design alone. In this paper, we propose UniScale to address these limitation, a novel co-design framework that jointly optimizes data and architecture to unlock the full potential of model scaling, which includes two core parts: (1) ES$^3$ (Entire-Space Sample System), a high-quality data scaling system that expands the training signal beyond conventional sampling strategies from both intra-domain request contexts with global supervised signal constructed by hierarchical label attribution and cross-domain samples aligning with the essence of user decision under similar content exposure environment in search domain; and (2) HHSFT (Heterogeneous Hierarchical Sample Fusion Transformer), a novel architecture designed to effectively model the complex heterogeneous distribution of scaled data and to harness the entire space user behavior data with Heterogeneous Hierarchical Feature Interaction and Entire Space User Interest Fusion, thereby surpassing the performance ceiling of structure-only model tuning. Extensive experiments on large-scale real world E-commerce search platform demonstrate that UniScale achieves significant improvements through the synergistic co-design of data and architecture and exhibits clear scaling trends, delivering substantial gains in key business metrics.

---

## 论文详细总结（自动生成）

### 论文总结：UniScale —— 搜索排序中协同的全空间数据与模型缩放

#### 1. 核心问题与研究动机
*   **核心问题**：在工业级搜索和推荐系统中，单纯通过增加模型参数（模型缩放）来提升性能正面临**边际收益递减**的瓶颈。
*   **研究动机**：
    *   **数据限制**：固定规模的训练数据限制了高表达能力模型的潜力。
    *   **分布异构性**：搜索数据具有复杂的异构分布，仅靠架构优化难以修复数据质量带来的性能损失。
    *   **选择偏差与稀疏性**：传统模型仅在曝光样本上训练，存在严重的样本选择偏差和标签稀疏问题。
*   **整体含义**：提出 **UniScale** 框架，强调**数据与架构的协同设计（Co-design）**，通过扩展高质量全空间数据并匹配高效的异构 Transformer 架构，突破性能天花板。

#### 2. 方法论：UniScale 框架
UniScale 由两个核心组件构成：

*   **ES³ (Entire-Space Sample System) —— 全空间样本系统**：
    *   **域内样本与标签扩展**：引入未曝光样本作为负样本以缓解选择偏差；采用**分层标签归因（Hierarchical Label Attribution）**，将跨域（如推荐页）的点击和转化信号回溯给搜索样本。
    *   **跨域样本“搜索化”（Searchification）**：将非搜索行为（如推荐点击）转化为搜索对齐的样本。通过**负采样生成模块**保持语义一致性，并利用**特征对齐模块**（历史查询重用、共现统计、语义检索）为非搜索样本合成伪查询（Pseudo-queries）。
*   **HHSFT (Heterogeneous Hierarchical Sample Fusion Transformer) —— 异构分层样本融合架构**：
    *   **异构分层特征交互 (HHFI)**：
        *   **异构特征注意力 (HFA)**：为不同语义块（用户、商品、查询）配置独立的投影矩阵和前馈网络（FFN），保留异构特征的分布特性。
        *   **全局特征注意力 (GFA)**：通过复合投影机制捕捉所有 Token 之间的高阶全局交互。
    *   **全空间用户兴趣融合 (ESUIF)**：
        *   **域路由专家融合 (DREF)**：使用硬路由约束，将专家划分为共享专家和域私有专家，解耦通用知识与域特定噪声。
        *   **域感知个性化门控注意力 (DAPGA)**：利用目标域表示作为 Query，通过注意力机制和个性化门控选择性地注入跨域兴趣。

#### 3. 实验设计
*   **数据集与场景**：基于阿里巴巴淘宝（Taobao）真实电商搜索场景，包含数十亿级的用户项交互数据。
*   **Benchmark 与对比方法**：
    *   **基础模型**：DLRM-MLP, DCNv2。
    *   **Transformer 类模型**：AutoInt, HiFormer。
    *   **缩放类模型**：Wukong, RankMixer。
*   **评估指标**：离线使用 AUC、GAUC、Hitrate@5；在线使用买家数（Purchase）和商品交易总额（GMV）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **工程优化**：提到了多项工业级部署优化技术，包括：
    *   **RDMA 通信**：替代传统 TCP 协议以提升分布式训练效率。
    *   **算子融合与量化**：开发了融合的 QKV 注意力内核，并使用 FP16 量化进行推理。
    *   **特征预哈希**：减少内存开销。
    *   **效果**：推理成本降低约 55%，训练开销降低约 40%。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **离线对比**：在数十亿规模的数据集上对比了 6 种 SOTA 模型。
    *   **消融实验**：分别针对 HHFI 模块和 ESUIF 模块进行了详细的组件拆解实验。
    *   **缩放定律验证**：通过改变模型参数规模（1x 到 4x）和数据量，验证了数据与模型协同缩放的非线性增益。
    *   **在线 A/B 测试**：在真实流量中进行了为期 10 天的测试。
*   **充分性评价**：实验设计较为充分，涵盖了从离线指标到在线业务指标的全链路验证，且通过缩放曲线证明了协同设计的必要性，具有较强的说服力。

#### 6. 主要结论与发现
*   **协同效应**：模型缩放与数据缩放具有协同作用。随着模型容量增大，利用全空间异构数据带来的 AUC 增益从 0.12% 扩大到 0.32%。
*   **架构优势**：HHSFT 在相同参数预算下显著优于传统 DNN 和其他 Transformer 变体。
*   **业务价值**：在线 A/B 测试显示，UniScale 使买家数提升 **1.70%**，GMV 提升 **2.04%**。
*   **路由必要性**：在引入全空间异构数据时，如果没有 DREF 等解耦机制，直接混合数据会导致严重的负迁移（AUC 下降 0.43%）。

#### 7. 优点
*   **协同设计理念**：突破了“只改模型”或“只加数据”的单一维度，提出了数据与架构匹配的系统化方案。
*   **标签归因创新**：通过分层标签归因解决了搜索场景中的曝光偏差和标签稀疏问题。
*   **工业落地性强**：不仅有算法创新，还配套了完整的工程优化方案，证明了超大规模 Transformer 在搜索排序中的可行性。

#### 8. 不足与局限
*   **合成数据偏差**：跨域样本“搜索化”依赖于合成查询，可能引入语义漂移或模型幻觉，论文未深入探讨合成查询的准确性对长尾流量的影响。
*   **算力细节缺失**：缺乏具体的硬件资源消耗数据，外部研究者难以评估其训练成本。
*   **领域局限性**：该框架高度针对电商搜索场景设计，在新闻推荐或短视频等交互成本较低、查询特征缺失的场景下的普适性有待验证。

（完）
