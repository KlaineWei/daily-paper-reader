---
title: "UniScale: Synergistic Entire Space Data and Model Scaling for Search Ranking"
title_zh: UniScale：搜索排序中协同的全空间数据与模型缩放
authors: "Liren Yu, Caiyuan Li, Feiyi Dong, Tao Zhang, Zhixuan Zhang, Dan Ou, Haihong Tang, Bo Zheng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24226v2"
tags: ["query:llmrec"]
score: 9.0
evidence: 搜索与推荐排序中的模型参数与数据规模化协同设计
tldr: 针对工业搜索排序中模型规模增加带来的边际收益递减问题，本文提出UniScale框架。该框架通过协同设计数据与架构，利用ES^3系统扩展全空间高质量训练样本，并结合HHSFT架构处理异构数据分布。实验证明，UniScale在电商搜索场景中显著提升了购买率和GMV，展示了数据与模型协同缩放的巨大潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24226v2/fig-001.webp\", \"caption\": \"Figure 1: Overall Architecture of Proposed UniScale, which combines Entire Space Sample System (ES3) for high quality data scaling and Heterogeneous Hierarchical Sample Fusion Transformer (HHSFT) for fully incorporating user interests expressed by entire space user behavior.\", \"page\": 3, \"index\": 1, \"width\": 506, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2603.24226v2/fig-002.webp\", \"caption\": \"Figure 2: Overview of the Entire-Space Sample System (ES3). ES3 constructs a unified, bias-mitigated training set by synergistically integrating samples across the entire user behavior space. The framework comprises two core modules: (i) Intradomain Sample and Label Expansion; (ii) Cross-domain Sample Searchification.\", \"page\": 4, \"index\": 2, \"width\": 506, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.24226v2/fig-003.webp\", \"caption\": \"Figure 3: Overview of Heterogeneous Hierarchical Sample Fusion Transformer (HHSFT) architecture. The HHSFT is designed to exploit model and data scaling in ranking systems through two primary stages. First, the Heterogeneous Hierarchical Feature Interaction (HHFI) stage employs heterogeneous feature attention layers with token-specific projection matrices and FFNs to preserve domain-unique distributions, followed by a global feature attention layer to capture high-order cross-domain interactions. Second, the Entire Space User Interest Fusion (ESUIF) stage comprises: Domain-Routed Expert Fusion (DREF) utilizes sample routing constraints to disentangle shared knowledge from domain-specific patterns; and Domain-Aware Personalized Gated Attention (DAPGA) adaptively modulates cross-domain information transfer via personalized gating.\", \"page\": 5, \"index\": 3, \"width\": 1056, \"height\": 400}]"
motivation: 现有的缩放定律研究多关注架构而忽视了数据与架构的协同，导致单纯增加模型参数面临性能瓶颈。
method: 提出UniScale框架，包含用于扩展全空间高质量样本的ES^3系统和用于融合异构特征与用户兴趣的HHSFT架构。
result: "在真实电商搜索平台的在线测试中，该方法使购买率提升了1.70%，GMV提升了2.04%。"
conclusion: 通过数据与架构的协同优化，可以有效突破单一模型缩放的性能上限，实现搜索排序系统的持续性能增长。
---

## 摘要
大语言模型（LLMs）的最新进展激发了工业搜索、广告和推荐系统中缩放法则（scaling law）研究的热潮。然而，现有方法主要集中在架构改进上，忽视了数据与架构设计之间关键的协同作用。我们观察到，仅缩放模型参数表现出收益递减，即随着模型规模的增加，性能的边际收益稳步下降，且由复杂异构数据分布引起的性能退化通常难以仅通过模型设计来修复。在本文中，我们提出了 UniScale 来解决这些局限性。这是一个新颖的协同设计框架，通过联合优化数据和架构来释放模型缩放的全部潜力，其包含两个核心部分：(1) ES$^3$（全空间样本系统），一个高质量的数据缩放系统，它通过由层级标签归因构建的全局监督信号从域内请求上下文，以及在搜索领域相似内容曝光环境下对齐用户决策本质的跨域样本，扩展了传统采样策略之外的训练信号；(2) HHSFT（异构层级样本融合 Transformer），一种新颖的架构，旨在有效建模缩放数据的复杂异构分布，并通过异构层级特征交互和全空间用户兴趣融合来利用全空间用户行为数据，从而突破仅靠结构调整的模型调优性能瓶颈。大量实验表明，UniScale 通过数据与架构的协同设计实现了显著提升，并展现出缩放趋势。在真实电商搜索平台上的在线 A/B 测试进一步显示，购买量增长了 1.70%，商品交易总额（GMV）增长了 2.04%。

## Abstract
Recent advances in Large Language Models (LLMs) have inspired a surge of scaling law research in industrial search, advertising, and recommendation systems. However, existing approaches focus mainly on architectural improvements, overlooking the critical synergy between data and architecture design. We observe that scaling model parameters alone exhibits diminishing returns, i.e., the marginal gain in performance steadily declines as model size increases, and that the performance degradation caused by complex heterogeneous data distributions is often irrecoverable through model design alone. In this paper, we propose UniScale to address these limitations, a novel co-design framework that jointly optimizes data and architecture to unlock the full potential of model scaling, which includes two core parts: (1) ES$^3$ (Entire-Space Sample System), a high-quality data scaling system that expands the training signal beyond conventional sampling strategies from both intra-domain request contexts with global supervised signal constructed by hierarchical label attribution and cross-domain samples aligning with the essence of user decision under similar content exposure environment in search domain; and (2) HHSFT (Heterogeneous Hierarchical Sample Fusion Transformer), a novel architecture designed to effectively model the complex heterogeneous distribution of scaled data and to harness the entire space user behavior data with Heterogeneous Hierarchical Feature Interaction and Entire Space User Interest Fusion, thereby surpassing the performance ceiling of structure-only model tuning. Extensive experiments demonstrate that UniScale achieves significant improvements through the synergistic co-design of data and architecture and exhibits scaling trends. Online A/B tests on a real-world e-commerce search platform further show gains of 1.70% in purchase and 2.04% in Gross Merchandise Volume (GMV).

---

## 论文详细总结（自动生成）

这是一份关于论文《UniScale: Synergistic Entire Space Data and Model Scaling for Search Ranking》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：在工业级搜索和推荐系统中，单纯增加模型参数（模型缩放）正面临**边际收益递减**的瓶颈。
*   **研究背景**：受大语言模型（LLM）启发，业界尝试通过增大模型规模提升排序性能。但作者发现，若不协同优化数据，复杂的异构数据分布会导致性能退化，且仅靠架构调整无法修复。
*   **整体含义**：提出 **UniScale** 框架，强调**数据缩放与模型缩放的协同设计**。通过构建全空间样本系统（ES³）和异构层级融合架构（HHSFT），突破现有搜索排序系统的性能天花板。

### 2. 方法论：核心思想与关键技术
UniScale 由两个核心组件构成：

#### A. ES³ (全空间样本系统) —— 数据缩放
*   **域内样本与标签扩展**：
    *   引入**未曝光样本**作为“简单负例”，缓解选择偏差。
    *   **层级标签归因**：将跨域（如推荐页）的点击和转化信号回传给搜索样本，解决标签稀疏和曝光偏差。
*   **跨域样本“搜索化”（Searchification）**：
    *   将非搜索行为（如推荐点击）转化为搜索对齐的样本。
    *   **负采样模块**：基于语义相似度生成负例，保持语义一致性。
    *   **特征对齐模块**：通过历史查询、共现统计或语义检索（ANN）为非搜索样本伪造“合成查询（Synthetic Query）”。

#### B. HHSFT (异构层级样本融合 Transformer) —— 模型架构
*   **HHFI (异构层级特征交互)**：
    *   **异构特征注意力 (HFA)**：为不同语义块（用户、商品、查询）配置独立的投影矩阵（QKV）和 FFN，保留异构数据的分布特性。
    *   **全局特征注意力 (GFA)**：在 HFA 基础上通过复合投影建模高阶全局交互。
*   **ESUIF (全空间用户兴趣融合)**：
    *   **域路由专家融合 (DREF)**：采用硬路由约束，将专家分为“域共享”和“域特定”，隔离跨域噪声。
    *   **域感知个性化门控注意力 (DAPGA)**：利用目标域（搜索）作为 Query 引导跨域知识迁移，并使用梯度截断（Gradient Stop）防止噪声干扰。

### 3. 实验设计
*   **数据集与场景**：基于阿里巴巴淘宝（Taobao）数十亿级的真实电商搜索数据。
*   **Benchmark 与对比方法**：
    *   **基础模型**：DLRM-MLP, DCNv2。
    *   **Transformer 类**：AutoInt, HiFormer。
    *   **缩放类 SOTA**：Wukong (悟空), RankMixer。
*   **评估指标**：离线使用 AUC、GAUC、Hitrate@5；在线使用购买量（Purchase）和 GMV。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **工程优化**：提到了使用了 **RDMA** 高速网络通信、**FP16** 量化、**算子融合（Kernel Fusion）**以及特征预哈希等技术。
*   **效率提升**：优化后 GPU 推理成本降低了约 55%，训练开销降低了约 40%。

### 5. 实验数量与充分性
*   **实验规模**：在十亿级数据集上进行了验证，规模极具代表性。
*   **实验维度**：
    *   **离线对比**：与 6 种 SOTA 模型对比，UniScale 显著领先。
    *   **消融实验**：分别验证了 HHFI 模块和 ESUIF 模块中各组件（如 DREF, DAPGA）的有效性。
    *   **缩放定律验证**：通过改变参数规模（1x 到 4x）观察 AUC 增益，验证了模型与数据协同缩放的非线性加速效应。
    *   **在线 A/B 测试**：在真实流量下进行了为期 10 天的测试。
*   **充分性评价**：实验设计非常充分，涵盖了从理论验证到工业部署的全流程，对比实验考虑了参数量对齐，具有较高的客观性。

### 6. 主要结论与发现
*   **协同效应**：模型规模越大，利用全空间异构数据的收益就越高（1x 规模增益 0.12%，4x 规模增益扩大到 0.32%）。
*   **架构必要性**：如果只增加数据而不改进架构（如直接用普通 Transformer 跑全空间数据），性能反而会因为负迁移而下降（AUC -0.43%）。
*   **业务价值**：在线测试显示购买量提升 **1.70%**，GMV 提升 **2.04%**，证明了该框架的巨大商业价值。

### 7. 优点
*   **视角独特**：跳出了单纯堆参数的怪圈，从“数据+架构”协同的角度重新定义了排序模型的 Scaling Law。
*   **工业落地性强**：ES³ 系统解决了搜索场景中长期存在的样本偏差和跨域数据利用难题；HHSFT 兼顾了建模能力与推理效率。
*   **系统性优化**：不仅有算法创新，还包含了针对大规模分布式训练和实时推理的底层工程优化。

### 8. 不足与局限
*   **复杂度较高**：ES³ 的数据清洗和“搜索化”流程较为复杂，对离线数据处理流水线有极高要求。
*   **场景局限**：该研究高度依赖于电商生态（有丰富的跨域行为如推荐、详情页等），在数据生态较单一的搜索场景中可能效果受限。
*   **算力细节缺失**：未披露具体硬件资源消耗，外部研究者难以评估其训练成本的绝对值。

（完）
