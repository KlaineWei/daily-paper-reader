---
title: "RCLRec: Reverse Curriculum Learning for Modeling Sparse Conversions in Generative Recommendation"
title_zh: RCLRec：生成式推荐中用于稀疏转化建模的逆向课程学习
authors: "Yulei Huang, Hao Deng, Haibo Xing, Jinxin Hu, Chuanfei Xu, Zulong Chen, Yu Zhang, Xiaoyi Zeng"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28124v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 基于逆课程学习的生成式推荐
tldr: 针对生成式推荐系统中转化信号稀疏且难以优化的挑战，本文提出了RCLRec框架。该框架采用逆向课程学习策略，通过从历史记录中逆向筛选与转化相关的子序列作为解码器前缀，为模型提供实例级的中间监督。结合课程质量感知损失，RCLRec有效缓解了转化稀疏问题，并在离线实验和在线A/B测试中显著提升了广告收入和订单量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28124v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of RCLRec. It uses a Reverse Curriculum Prefix Module (RCPM) to select conversion-relevant interactions from history as a decoder-side prefix, and is trained with a curriculum quality-aware loss.\", \"page\": 3, \"index\": 1, \"width\": 934, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2603.28124v1/fig-002.webp\", \"caption\": \"Figure 1: (a) Multi-behavior history–target semantic relevance. (b) Standard GR method. (c) Behavior-wise relevance statistics showing that conversions are preceded by more coherent clusters of related items. (d) Our proposed RCLRec.\", \"page\": 2, \"index\": 2, \"width\": 461, \"height\": 261}]"
motivation: 现有的生成式推荐模型在处理大规模系统中稀疏的转化目标时，缺乏足够的监督信号和对关键决策过程的建模。
method: 提出逆向课程学习框架，通过逆向选择历史中与转化相关的项作为前缀进行联合生成，并引入质量感知损失进行优化。
result: "离线实验表现优异，在线部署后广告收入提升2.09%，订单量增加1.86%。"
conclusion: RCLRec通过逆向课程学习强化了对稀疏转化信号的建模，是提升生成式推荐系统转化预测能力的有效方案。
---

## 摘要
大规模推荐系统中的转化目标非常稀疏，导致其难以优化。生成式推荐（GR）通过将多种类型的行为组织成具有共享表示的统一 Token 序列，部分缓解了数据稀疏性，但转化信号的建模仍然不够充分。虽然最近的行为感知 GR 模型对行为类型进行编码，并利用行为感知注意力机制来突出与决策相关的中间行为，但它们仍然依赖于对完整历史记录的标准注意力，且没有为转化提供额外的监督，使得转化稀疏性问题在很大程度上仍未得到解决。为了应对这些挑战，我们提出了 RCLRec，这是一种基于逆向课程学习的 GR 框架，用于稀疏转化监督。对于每个转化目标，RCLRec 通过反向从历史记录中选择与转化相关的物品子序列，构建一个简短的课程。这些物品的语义 Token 与目标转化 Token 一起作为前缀输入到解码器中，并置于联合生成目标下。这种设计提供了额外的针对特定实例的中间监督，缓解了转化稀疏性，并使模型专注于用户的关键决策过程。我们进一步引入了课程质量感知损失，以确保所选课程对转化预测具有信息量。在离线数据集和在线 A/B 测试上的实验表明，RCLRec 取得了优异的性能，在线部署中广告收入增长了 2.09%，订单量增长了 1.86%。

## Abstract
Conversion objectives in large-scale recommender systems are sparse, making them difficult to optimize. Generative recommendation (GR) partially alleviates data sparsity by organizing multi-type behaviors into a unified token sequence with shared representations, but conversion signals remain insufficiently modeled. While recent behavior-aware GR models encode behavior types and employ behavior-aware attention to highlight decision-related intermediate behaviors, they still rely on standard attention over the full history and provide no additional supervision for conversions, leaving conversion sparsity largely unresolved. To address these challenges, we propose RCLRec, a reverse curriculum learning-based GR framework for sparse conversion supervision. For each conversion target, RCLRec constructs a short curriculum by selecting a subsequence of conversion-related items from the history in reverse. Their semantic tokens are fed to the decoder as a prefix, together with the target conversion tokens, under a joint generation objective. This design provides additional instance-specific intermediate supervision, alleviating conversion sparsity and focusing the model on the user's critical decision process. We further introduce a curriculum quality-aware loss to ensure that the selected curricula are informative for conversion prediction. Experiments on offline datasets and an online A/B test show that RCLRec achieves superior performance, with +2.09% advertising revenue and +1.86% orders in online deployment.

---

## 论文详细总结（自动生成）

以下是对论文《RCLRec: Reverse Curriculum Learning for Modeling Sparse Conversions in Generative Recommendation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在生成式推荐（Generative Recommendation, GR）中，**转化行为（如购买、下单）极其稀疏**，远少于点击和曝光。现有的 GR 模型虽然能通过共享语义 Token 缓解数据稀疏，但对转化目标的建模仍不充分，缺乏针对转化决策过程的显式监督。
*   **研究动机**：作者观察到转化发生前通常伴随着一系列高度相关的决策行为（如对比同类商品）。因此，论文提出通过**逆向课程学习（Reverse Curriculum Learning）**，从用户冗长的历史中提取关键的“决策子序列”，并将其作为解码器的前缀，为稀疏的转化目标提供额外的实例级监督信号。

### 2. 方法论：核心思想与关键技术
RCLRec 框架主要包含两个阶段（预训练 + 微调）和两个核心组件：
*   **核心思想**：将转化预测任务转化为“给定关键历史前缀”的生成任务。
*   **逆向课程前缀模块 (RCPM)**：
    *   **查询构建**：利用用户特征和转化行为嵌入（$e_{pay}$）通过 MLP 生成个性化查询向量 $q$。
    *   **相关性评分**：计算 $q$ 与编码器历史状态 $H_u$ 的内积，得到历史行为对转化的贡献度分布。
    *   **Top-k 筛选与前缀注入**：选择相关性最高的 $k$ 个历史物品，按相关性升序排列其语义 Token，作为解码器的前缀（Prefix），与目标物品 Token 拼接进行联合生成。
    *   **端到端优化 (IBQ)**：引入索引反向传播量化（Index Backpropagation Quantization），解决 Top-k 离散选择不可导的问题，实现选择模块的端到端训练。
*   **课程质量感知损失 (Curriculum Quality-Aware Loss)**：
    *   引入一个铰链损失（Hinge Loss），强制要求带有“课程前缀”的生成概率要优于不带前缀的基准概率，确保筛选出的子序列确实有助于转化预测。

### 3. 实验设计
*   **数据集**：
    *   **工业数据集**：来自亚洲某电商平台，包含超过 10 亿次交互，转化占比仅 1.23%。
    *   **Tmall 公开数据集**：经典的电商多行为数据集，转化占比 0.95%。
*   **Benchmark 与对比方法**：
    *   **传统序列模型**：SASRec, BERT4Rec, HSTU。
    *   **生成式/多行为模型**：TIGER, MBGen, GEAR, PBAT。
*   **评估指标**：Recall@5/10, NDCG@5/10。

### 4. 资源与算力
*   **模型配置**：使用 4 层 Transformer 编码器和 2 层解码器，隐藏层维度为 256，8 个注意力头。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但考虑到工业数据集规模达 10 亿级，推测其在分布式集群上进行了大规模训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同规模的数据集（一个超大规模工业级，一个公开级）上进行了对比。
    *   **消融实验**：验证了 RCPM 模块、最近 $k$ 个策略（Recent-k）以及质量感知损失的有效性。
    *   **敏感性分析**：探讨了课程长度 $k$ 对性能的影响（发现 $k=4$ 左右效果最佳）。
    *   **在线 A/B 测试**：在真实电商广告场景下进行了为期 8 天的测试。
*   **充分性评价**：实验设计非常充分且客观。不仅有离线指标的显著提升，还有在线业务指标（收入、订单）的支撑，证明了方法的实用性。

### 6. 主要结论与发现
*   **性能领先**：RCLRec 在所有离线指标上均显著优于现有最强基准（如 MBGen 和 GEAR），Recall 提升幅度超过 10%。
*   **决策序列的重要性**：实验证明，简单地取最近的行为（Recent-k）效果不如基于相关性筛选的逆向课程，说明精准识别决策路径对转化建模至关重要。
*   **在线价值**：在线部署后，广告收入提升 **2.09%**，订单量提升 **1.86%**，证明了该模型在处理稀疏转化目标时的商业价值。

### 7. 优点（亮点）
*   **创新性引入逆向课程**：将强化学习中的逆向课程思想引入推荐系统，解决了长序列中关键信息被淹没的问题。
*   **可导的筛选机制**：通过 IBQ 技术解决了 Top-k 选择的梯度回传问题，使模型能自动学习“哪些历史对转化最重要”。
*   **即插即用**：作为一种前缀增强机制，它可以较容易地集成到其他基于 Transformer 的生成式推荐框架中。

### 8. 不足与局限
*   **计算开销**：在解码器端引入额外的 $k$ 个物品前缀会增加推理时的计算延迟和显存占用，论文未详细讨论推理效率的优化。
*   **超参数敏感性**：课程长度 $k$ 需要人工调优，且对于不同类别的商品，最优的决策序列长度可能不同，固定 $k$ 可能缺乏灵活性。
*   **依赖预训练质量**：该方法依赖于第一阶段预训练生成的语义 Token 和行为嵌入，如果底层表示学习不佳，逆向筛选的效果会受限。

（完）
