---
title: Unbiased Multimodal Reranking for Long-Tail Short-Video Search
title_zh: 长尾短视频搜索中的无偏多模态重排序
authors: "Wenyi Xu, Feiran Zhu, Songyang Li, Renzhe Zhou, Chao Zhang, Chenglei Dai, Yuren Mao, Yunjun Gao, Yi Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24975v2"
tags: ["query:llmrec"]
score: 9.0
evidence: 大模型驱动的多模态视频搜索重排序
tldr: 针对短视频搜索中长尾查询因行为数据稀疏导致的马太效应和低质内容问题，本文提出一种基于大语言模型（LLM）的多模态重排序框架。该框架利用LLM的先验知识评估视频质量，通过两阶段训练（监督微调与成对偏好优化）构建用户体验评分模型，并在推理阶段结合强化学习优化页面布局。实验证明该方法在离线指标和快手线上A/B测试中均显著提升了长尾搜索的质量与用户体验。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24975v2/fig-001.webp\", \"caption\": \"Table 4: Evaluation ofmodels on long-play and click-through metrics.\", \"page\": 7, \"index\": 1, \"width\": 498, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.24975v2/fig-002.webp\", \"caption\": \"Figure 1: Data construction and two-stage training of the multimodal experience-scoring model, including supervised finetuning with multimodal annotations and pairwise fine-tuning with preference data.\", \"page\": 3, \"index\": 2, \"width\": 1035, \"height\": 541}, {\"url\": \"assets/figures/arxiv/2603.24975v2/fig-003.webp\", \"caption\": \"Figure 3: Ablations within the two-stage optimization in reranking. Bars report NDCG under four label types: click: click-based labels, long = long-play labels, dur = watch-duration labels, human = human-preference labels.\", \"page\": 8, \"index\": 3, \"width\": 977, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2603.24975v2/fig-004.webp\", \"caption\": \"Figure 2: Overview of the two-stage reranking integration. Stage I enhances pre-training with experience-score-ordered supervision; Stage II aligns page-level ranking through GRPO-based reward optimization guided by experience-derived nDCG.\", \"page\": 4, \"index\": 4, \"width\": 1038, \"height\": 238}]"
motivation: 长尾查询下用户行为数据稀疏，导致传统模型容易放大标题党等低质内容，产生严重的马太效应。
method: 提出一种两阶段训练的LLM驱动多模态重排序框架，结合多模态证据进行监督微调和成对偏好优化，并利用强化学习进行页面级优化。
result: "离线实验在AUC、NDCG等指标上取得提升，线上15%流量的A/B测试显示用户体验和消费指标均有显著增长。"
conclusion: 利用LLM的内在知识进行无偏多模态重排序，能有效解决长尾视频搜索中的质量评估难题，具有极高的实用价值。
---

## 摘要
快手每日处理数亿次搜索，短视频搜索的质量至关重要。然而，长尾查询面临严重的马太效应：稀疏的用户行为数据导致模型放大了点击诱饵和浅层内容等低质量内容。大语言模型（LLM）的最新进展提供了一种新范式，其内在的世界知识提供了一种强大的机制来评估内容质量，且不依赖于稀疏的用户交互。为此，我们提出了一种 LLM 驱动的多模态重排序框架，该框架在没有真实用户行为的情况下评估用户体验。该方法包含一个两阶段训练过程：第一阶段利用多模态证据构建高质量标注用于有监督微调，第二阶段引入成对偏好优化以帮助模型学习候选视频之间的偏序关系。在推理阶段，生成的体验分数被用于在重排序中提升高质量但曝光不足的视频，并通过强化学习进一步指导页面级优化。实验表明，该方法在包括 AUC、NDCG@K 和人工偏好判断在内的离线指标上，相较于强基准模型取得了持续的提升。覆盖 15% 流量的在线 A/B 测试进一步证明了在用户体验和消费指标上的收益，证实了该方法在长尾视频搜索场景中的实际价值。

## Abstract
Kuaishou serving hundreds of millions of searches daily, the quality of short-video search is paramount. However, it suffers from a severe Matthew effect on long-tail queries: sparse user behavior data causes models to amplify low-quality content such as clickbait and shallow content. The recent advancements in Large Language Models (LLMs) offer a new paradigm, as their inherent world knowledge provides a powerful mechanism to assess content quality, agnostic to sparse user interactions. To this end, we propose a LLM-driven multimodal reranking framework, which estimates user experience without real user behavior. The approach involves a two-stage training process: the first stage uses multimodal evidence to construct high-quality annotations for supervised fine-tuning, while the second stage incorporates pairwise preference optimization to help the model learn partial orderings among candidates. At inference time, the resulting experience scores are used to promote high-quality but underexposed videos in reranking, and further guide page-level optimization through reinforcement learning. Experiments show that the proposed method achieves consistent improvements over strong baselines in offline metrics including AUC, NDCG@K, and human preference judgement. An online A/B test covering 15\% of traffic further demonstrates gains in both user experience and consumption metrics, confirming the practical value of the approach in long-tail video search scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《Unbiased Multimodal Reranking for Long-Tail Short-Video Search》（长尾短视频搜索中的无偏多模态重排序）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在短视频搜索中，约20%的流量属于“长尾查询”（Long-tail queries）。由于这些查询的用户行为数据稀疏，传统排序模型容易陷入“马太效应”，过度依赖点击诱饵（标题党）、封面党或高热度视频，导致搜索结果质量低下，损害用户信任。
*   **研究动机**：现有的查询改写或通用质量分方法无法处理多模态不一致性（如标题与内容不符）或查询相关的相关性问题。作者提出利用大语言模型（LLM）内在的世界知识，在不依赖用户行为信号的情况下，通过多模态证据评估视频的真实质量和相关性。

### 2. 方法论
该框架分为**经验评分模型训练**和**重排序系统集成**两个主要阶段：

#### A. 经验评分模型（ExpModel）的两阶段训练
1.  **多模态质量对齐（SFT阶段）**：
    *   利用LLM（如Qwen2.5-VL）对视频的标题、ASR（语音识别）、OCR（文字识别）、封面和关键帧进行综合分析。
    *   通过Prompt引导模型生成多维度的质量分析报告（相关性、安全性、时效性、可信度等），作为监督信号进行微调。
2.  **成对排序对齐（PFT阶段）**：
    *   构建同查询下的视频对，让LLM判断偏好。
    *   将生成式模型转为评分模型，使用成对排序损失（Pairwise Ranking Loss）训练，使模型输出可比的标量分数，学习视频间的偏序关系。

#### B. 重排序管线集成
1.  **阶段I：预训练增强**：在重排序模型的训练中，根据经验评分重新排列目标序列。将点击过的视频按经验分排序放在前面，未点击/未曝光的视频按经验分排序放在后面，引导模型学习高质量内容的特征。
2.  **阶段II：页面级对齐**：引入强化学习算法（GRPO），将经验评分生成的理想排序（nDCG）作为奖励函数的一部分，优化整个搜索结果页的布局一致性。

### 3. 实验设计
*   **数据集**：来自快手搜索日志。长尾查询定义为7天内PV（页面浏览量）小于70的查询。
    *   SFT数据：18.7万条 <查询, 视频> 样本。
    *   PFT数据：34.8万个偏好对。
*   **Benchmark与对比方法**：
    *   **API模型**：GPT-4o（文本模式、多模态模式、点对点/列表模式）。
    *   **重排序基准**：RankGPT（基于滑动窗口的列表重排序）。
    *   **检索模型**：BGE-m3（双编码器文本检索）。
*   **评价指标**：NDCG@K（1, 5, 10）、Pairwise Accuracy（AUC）、人工偏好判断（GSB：Good/Same/Bad）。

### 4. 资源与算力
*   **训练算力**：使用了6个节点，每个节点配备2张 Tesla T4 GPU（16GB），用于多模态标注数据的训练。
*   **推理算力**：线上部署使用2个节点，每个节点配备2张 NVIDIA A10 GPU。
*   **模型规模**：线上重排序模型非常轻量化（仅4M参数），以满足严苛的延迟要求；而离线标注和评分模型使用了较大规模的LLM（如Qwen2.5-VL-32B/3B）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了离线指标评估、消融实验、人工评估以及大规模线上A/B测试。
*   **充分性**：
    *   **离线实验**：对比了多种GPT-4o配置和主流重排序模型，验证了ExpModel的领先性。
    *   **消融实验**：分别验证了“SFT+PFT”两阶段训练的必要性，以及多模态信息（视觉+文本）相对于纯文本的增益。
    *   **线上测试**：在快手真实流量中进行了为期两周的15%流量测试，处理了每日超过5000万次查询，数据规模巨大，结果具有统计学意义。

### 6. 主要结论与发现
*   **长尾提升显著**：线上A/B测试显示，长尾查询的查询改写率（IQRR）下降1.28%，点击率（CTR）提升1.24%，长效观看率（LVR）提升1.67%。
*   **多模态的重要性**：实验证明加入视觉特征后，模型在判断“标题党”和内容一致性方面准确率大幅提升。
*   **体验与消费平衡**：该方法在显著提升用户体验指标的同时，并未牺牲（甚至略微提升了）大盘的点击和观看时长等消费指标。

### 7. 优点
*   **行为无关性**：成功解决了长尾场景下因缺乏点击数据而无法有效排序的难题。
*   **多模态深度融合**：不仅看文本相关性，还通过LLM理解视频帧、ASR等深层语义，识别虚假宣传。
*   **工程落地性强**：通过“大模型离线评分+轻量模型在线蒸馏/集成”的思路，解决了LLM推理延迟高无法直接上线的痛点。

### 8. 不足与局限
*   **标注成本与偏差**：虽然使用了LLM生成标注，但LLM本身可能存在幻觉或偏见，且离线标注过程对算力资源仍有一定消耗。
*   **长尾定义限制**：实验主要针对PV < 70的查询，对于极度冷启动（PV为0或1）的查询效果文中未详细展开。
*   **音频语义利用不足**：目前主要利用了ASR文本，对于音频中的情绪、背景音乐等非文本语义信息的利用仍有提升空间。

（完）
