---
title: A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic
title_zh: 一种通过哈密顿 Actor-Critic 实现的基于模型强化学习的庞特里亚金方法
authors: "Chengyang Gu, Yuxin Pan, Hui Xiong, Yize Chen"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28971v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于长程价值估计的模型强化学习
tldr: 针对基于模型的强化学习（MBRL）中长时界价值估计受模型误差累积影响的问题，本文受庞特里亚金极大值原理（PMP）启发，提出了哈密顿强化学习（HAC）方法。该方法通过直接优化定义在动力学和奖励上的哈密顿量，避免了显式的价值函数学习，从而降低了对模型误差的敏感性。实验表明，HAC在连续控制任务的在线和离线设置中均优于现有基准，具有更强的鲁棒性和样本效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28971v1/fig-001.webp\", \"caption\": \"Fig. 2: Learning Curves Comparison (Offline RL). We compare our HAC with IQL, SAC-Off and MOPO on LQR, Pendulum, and MountainCar, Swimmer. For MOPO and HAC which require multi-step imaginary rollouts, rollout horizon is to K = 10 for LQR and Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer. Results are averaged over 5 random seeds.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-002.webp\", \"caption\": \"Fig. 4: Critic Estimation Comparison: Plots comparing normalized estimated Q-values (in MVE-DDPG) and Hamiltonian (in our HAC) against ground-truth sampled from 10 random test episodes in LQR. An ideal critic should concentrates on the red unity line.\", \"page\": 18, \"index\": 2, \"width\": 1054, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-003.webp\", \"caption\": \"Fig. 1: Learning Curves Comparison (Online RL): We compare our HAC with DDPG, SAC and MVE-DDPG on Pendulum, MountainCar, Swimmer and Hopper. For MVEDDPG and HAC which require multi-step imaginary rollouts, we set the rollout horizon to K = 10 for Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer, and K = 3 for Hopper. All results are averaged over 5 random seeds.\", \"page\": 6, \"index\": 3, \"width\": 512, \"height\": 422}]"
motivation: 传统的MBRL方法在长时界价值估计时容易受到模型误差累积的影响，导致策略优化效果受限。
method: 提出哈密顿强化学习（HAC），利用庞特里亚金极大值原理直接优化哈密顿量，从而省去显式的价值函数近似过程。
result: 在多个连续控制基准测试中，HAC在收敛速度、控制性能以及对分布偏移的鲁棒性方面均优于模型无关和MVE基准。
conclusion: HAC通过引入控制理论中的哈密顿方法，为解决MBRL中的价值估计偏差问题提供了一种高效且稳健的新途径。
---

## 摘要
基于模型的强化学习（MBRL）通过利用学习到的动力学模型进行策略优化，从而提高了样本效率。然而，Actor-Critic 等方法的有效性往往受到复合模型误差的限制，这些误差会降低长时界价值估计的准确性。现有方法（如基于模型的价值扩展，MVE）通过多步展开部分缓解了这一问题，但仍对展开时界的选取和残差模型偏差较为敏感。受庞特里亚金极大值原理（PMP）的启发，我们提出了哈密顿 Actor-Critic（HAC）。这是一种基于模型的方法，通过直接优化在确定性系统的学习动力学和奖励上定义的哈密顿量，消除了显式的价值函数学习。通过避免价值近似，HAC 在降低对模型误差敏感性的同时，还提供了收敛性保证。在在线和离线强化学习设置下的连续控制基准测试中，大量实验表明，HAC 在控制性能、收敛速度以及对分布偏移（包括分布外 OOD 场景）的鲁棒性方面均优于无模型和基于 MVE 的基准方法。在数据有限的离线设置中，HAC 达到或超过了现有最先进方法的水平，突显了其强大的样本效率。

## Abstract
Model-based reinforcement learning (MBRL) improves sample efficiency by leveraging learned dynamics models for policy optimization. However, the effectiveness of methods such as actor-critic is often limited by compounding model errors, which degrade long-horizon value estimation. Existing approaches, such as Model-Based Value Expansion (MVE), partially mitigate this issue through multi-step rollouts, but remain sensitive to rollout horizon selection and residual model bias. Motivated by the Pontryagin Maximum Principle (PMP), we propose Hamiltonian Actor-Critic (HAC), a model-based approach that eliminates explicit value function learning by directly optimizing a Hamiltonian defined over the learned dynamics and reward for deterministic systems. By avoiding value approximation, HAC reduces sensitivity to model errors while admitting convergence guarantees. Extensive experiments on continuous control benchmarks, in both online and offline RL settings, demonstrate that HAC outperforms model-free and MVE-based baselines in control performance, convergence speed, and robustness to distributional shift, including out-of-distribution (OOD) scenarios. In offline settings with limited data, HAC matches or exceeds state-of-the-art methods, highlighting its strong sample efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在基于模型的强化学习（MBRL）中，学习到的动力学模型不可避免地存在误差。在 Actor-Critic 架构下，这些误差会随时间步累积（复合误差），导致 Critic（价值函数）的估计严重偏离真实值，进而误导策略（Actor）优化。
*   **研究背景**：现有的方法如 MVE（模型价值扩展）尝试通过多步展开来改善价值估计，但其效果对展开时界 $K$ 的选择极其敏感，且仍需依赖一个显式的、可能存在近似偏差的 Critic 网络。
*   **动机**：受控制理论中庞特里亚金极大值原理（PMP）的启发，探索是否可以**完全取消显式的 Critic 网络**，直接利用动力学模型定义的哈密顿量（Hamiltonian）来指导策略优化。

### 2. 方法论：哈密顿 Actor-Critic (HAC)
*   **核心思想**：将强化学习目标转化为最优控制问题。根据 PMP，最优控制量应使哈密顿量最小化。HAC 利用学习到的动力学模型 $\hat{f}$ 和奖励函数 $r$ 定义哈密顿量，并以此作为策略更新的依据。
*   **关键技术细节**：
    *   **哈密顿量定义**：$H(s_t, a_t, \lambda_{t+1}) = c(s_t, a_t) + \lambda_{t+1}^T \hat{f}(s_t, a_t)$，其中 $c$ 为代价（负奖励），$\lambda$ 为协态（Costate）。
    *   **后向递归计算协态**：通过 $\lambda_t = \nabla_{s_t} H$ 从终端状态反向计算每一时刻的协态，无需学习价值函数。
    *   **解析雅可比技术（Analytical Jacobian）**：针对 ReLU 激活函数的神经网络动力学模型，推导出 Jacobian 的闭式解。这避免了 PyTorch 等框架在处理高阶梯度时的运行错误，并显著提升了计算效率。
    *   **策略更新**：Actor 的损失函数直接定义为 $K$ 步展开轨迹上哈密顿量的平均值，通过梯度下降更新策略参数 $\theta$。

### 3. 实验设计
*   **实验场景**：
    *   基础控制：LQR（线性二次调节器）、Pendulum（倒立摆）。
    *   复杂控制：MountainCar（登山车）、MuJoCo 环境（Swimmer, Hopper）。
*   **Benchmark 与对比方法**：
    *   **在线 RL**：对比 DDPG、SAC 以及基于模型的 MVE-DDPG。
    *   **离线 RL**：对比 IQL、SAC-Off 以及最先进的基于模型的方法 MOPO。
    *   **鲁棒性测试**：在 LQR 环境中引入初始状态偏移，测试模型对分布外（OOD）数据的泛化能力。

### 4. 资源与算力
*   **硬件环境**：使用 Google Colaboratory 平台，配置为 **NVIDIA T4 GPU** (12GB RAM)。
*   **模型规模**：动力学模型、策略网络均采用简单的 3 层 MLP（每层 128 个神经元）。
*   **算力说明**：文中提到 HAC 虽然涉及后向递归计算，但由于采用了解析雅可比技术，其单步梯度更新时间与带有集成 Critic 的 MVE 方法相比具有竞争性，甚至更优。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单线性系统到复杂多关节机器人的 5 种环境，并在在线和离线两种主流 RL 模式下进行了验证。
*   **充分性**：
    *   所有实验均采用 **5 个随机种子** 取平均值，并标注了标准差，结果具有统计意义。
    *   包含了**消融实验**，对比了 HAC 与不同集成数量（Ensemble size）的 MVE 方法在性能和计算耗时上的平衡。
    *   **理论验证**：专门针对 LQR 任务绘制了 Critic 估计误差曲线，验证了理论推导的误差界。

### 6. 主要结论与发现
*   **性能优越**：HAC 在多数任务中达到了与最先进方法持平或更优的水平，尤其在短时界任务（如 Pendulum）中优势巨大。
*   **收敛极快**：在离线 RL 场景下，HAC 仅需基准方法约 **25%-30% 的样本量** 即可收敛到近乎最优的解。
*   **误差界更紧**：理论证明并实验证实，HAC 的哈密顿量估计误差上限低于 MVE 的价值函数估计误差上限。
*   **鲁棒性强**：在面对初始状态偏移等 OOD 干扰时，HAC 表现出比 MVE 更好的轨迹稳定性。

### 7. 优点（亮点）
*   **理论与实践结合**：成功将经典的庞特里亚金极大值原理（PMP）引入深度 MBRL，提供了一套严谨的数学框架。
*   **架构极简**：去除了显式的 Critic 网络，减少了神经网络近似带来的偏差，同时也减少了超参数调优（如 Critic 网络结构、集成数量等）的工作量。
*   **计算高效**：解析雅可比技术解决了自动微分在处理控制理论公式时的痛点。

### 8. 不足与局限
*   **系统假设**：目前的方法主要针对**确定性动力学系统**设计，虽然附录提到了随机系统的扩展思路，但主实验尚未全面覆盖随机环境。
*   **时界截断误差**：当展开步数 $K$ 远小于任务总步数 $T$ 时，哈密顿量的估计仍会受到终端协态设置的影响（文中通过分析证明在某些条件下该影响可忽略，但在极端非线性任务中可能存在风险）。
*   **环境限制**：在 Hopper 等具有复杂“健康奖励”（非连续代价函数）的环境中，性能提升不如在纯二次代价函数环境中明显。

（完）
