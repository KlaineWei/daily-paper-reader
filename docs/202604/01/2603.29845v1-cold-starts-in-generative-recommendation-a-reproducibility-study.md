---
title: "Cold-Starts in Generative Recommendation: A Reproducibility Study"
title_zh: 生成式推荐中的冷启动：一项可复现性研究
authors: "Zhen Zhang, Jujia Zhao, Xinyu Ma, Xin Xin, Maarten de Rijke, Zhaochun Ren"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29845v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 生成式推荐与冷启动挑战
tldr: 本研究针对生成式推荐系统在冷启动场景下的表现进行了系统性的复现研究。尽管基于预训练语言模型的生成式推荐被认为能通过语义信息缓解冷启动问题，但现有研究缺乏统一的评估标准，且模型规模、标识符设计及训练策略等关键设计选择往往相互交织。本文通过统一的冷启动协议，深入探讨了这些因素对用户和物品冷启动的影响，为未来生成式推荐系统的设计提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29845v1/fig-001.webp\", \"caption\": \"Table 4: Item cold-start recommendation performance comparison. Performance is measured using Recall@10 (R@10) and NDCG@10 (N@10), with the best and second-best results in each category bolded and underlined, respectively. The best performing models achieve significant improvements over the other baselines (paired t-test, p<0.05).\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2603.29845v1/fig-002.webp\", \"caption\": \"Table 3: User cold-start recommendation performance comparison. Performance is measured using Recall@10 (R@10) and NDCG@10 (N@10), with the best and second-best results in each category bolded and underlined, respectively. The best performing models achieve significant improvements over the other baselines (paired t-test, p<0.05).\", \"page\": 6, \"index\": 2, \"width\": 976, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2603.29845v1/fig-003.webp\", \"caption\": \"Figure 1: Generative recommendation pipeline.\", \"page\": 2, \"index\": 3, \"width\": 482, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2603.29845v1/fig-004.webp\", \"caption\": \"Figure 2: Comparison of recall performance under warmstart and cold-start conditions across different model scales. The plot shows Recall@10 for both item and user cold-start settings as model size increases, using representative generative recommender methods (TIGER) with different variants of Flan-T5.\", \"page\": 7, \"index\": 4, \"width\": 494, \"height\": 556}, {\"url\": \"assets/figures/arxiv/2603.29845v1/fig-005.webp\", \"caption\": \"Figure 3: Performance comparison across different identifier designs. This figure illustrates the Recall@10 performance under warm-start, item cold-start, and user cold-start conditions for various identifier types: Atomic IDs, Textual Titles, and Semantic Codes (RQ-VAE, Balanced k-means, and OPQ).\", \"page\": 8, \"index\": 5, \"width\": 493, \"height\": 673}]"
motivation: 现有生成式推荐研究在冷启动评估上缺乏统一标准，且关键设计选择对性能的影响难以区分。
method: 在统一的冷启动协议下，对基于预训练语言模型的生成式推荐系统进行系统性的复现和对比实验。
result: 揭示了模型规模、标识符设计和训练策略等因素在解决用户和物品冷启动问题中的具体作用及其局限性。
conclusion: 本研究为生成式推荐系统在冷启动环境下的优化提供了实证依据，强调了系统性评估的重要性。
---

## 摘要
冷启动推荐在动态、开放世界平台中仍然是一个核心挑战，要求模型在交互信号稀疏或缺失的情况下，为新注册用户进行推荐（用户冷启动），并将新引入的物品推荐给现有用户（物品冷启动）。最近基于预训练语言模型（PLMs）构建的生成式推荐系统通常被期望通过利用物品语义信息（如标题和描述）以及在测试时基于有限的用户上下文进行条件化处理，来缓解冷启动问题。然而，在现有研究中，冷启动很少被视为主要的评估设置，且由于模型规模、标识符设计和训练策略等关键设计选择经常被同时更改，所报告的性能提升难以解释。在这项工作中，我们在统一的冷启动协议套件下，对生成式推荐进行了系统的可复现性研究。

## Abstract
Cold-start recommendation remains a central challenge in dynamic, open-world platforms, requiring models to recommend for newly registered users (user cold-start) and to recommend newly introduced items to existing users (item cold-start) under sparse or missing interaction signals. Recent generative recommenders built on pre-trained language models (PLMs) are often expected to mitigate cold-start by using item semantic information (e.g., titles and descriptions) and test-time conditioning on limited user context. However, cold-start is rarely treated as a primary evaluation setting in existing studies, and reported gains are difficult to interpret because key design choices, such as model scale, identifier design, and training strategy, are frequently changed together. In this work, we present a systematic reproducibility study of generative recommendation under a unified suite of cold-start protocols.

---

## 论文详细总结（自动生成）

这篇论文《Cold-Starts in Generative Recommendation: A Reproducibility Study》对生成式推荐系统在冷启动场景下的表现进行了深入且系统的复现研究。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：生成式推荐（Generative Recommendation, GenRec）利用预训练语言模型（PLM）将推荐任务转化为序列生成任务。虽然理论上 PLM 的语义理解能力能缓解冷启动问题，但现有研究往往将模型规模、标识符（ID）设计和训练策略等多个变量混杂在一起，导致无法准确评估各因素对冷启动性能的真实贡献。
*   **核心问题**：在统一的评估协议下，生成式推荐在用户冷启动和物品冷启动中的表现究竟如何？模型规模、ID 设计和训练策略这三个关键维度分别起到了什么作用？

### 2. 论文提出的方法论
论文并未提出全新的模型，而是构建了一个**统一的复现与评估框架**，通过控制变量法隔离以下三个维度的影响：
*   **模型规模（Model Scale）**：采用不同参数量的 Flan-T5（Small, Base, Large, XL）作为骨干网络，观察性能随规模的变化趋势。
*   **标识符设计（Identifier Design）**：对比了三种主流 ID 类型：
    *   **原子 ID（Atomic IDs）**：无语义的随机数字或索引。
    *   **文本 ID（Textual IDs）**：使用物品标题（Titles）作为标识符。
    *   **语义 ID（Semantic Codes）**：通过量化方法（如 RQ-VAE、Balanced k-means、OPQ）生成的离散语义编码。
*   **训练策略（Training Strategy）**：对比了仅使用**监督微调（SFT）**与在 SFT 基础上增加**强化学习（RL/DPO）**的效果。
*   **算法流程**：输入用户历史交互序列，通过 PLM 自回归生成目标物品的 ID 序列，并在推理时使用前缀树（Trie）进行约束解码。

### 3. 实验设计
*   **数据集**：使用了三个具有丰富元数据的公开数据集：**Amazon-Toys**（电商）、**MicroLens**（短视频）、**Steam**（游戏）。
*   **评估场景**：
    *   **Warm-start**：训练集中出现过的用户和物品。
    *   **User Cold-start**：新用户（仅提供 1-10 条交互作为上下文）。
    *   **Item Cold-start**：新物品（训练集中未出现，仅靠语义信息推荐）。
*   **对比方法（Benchmark）**：
    *   **传统基准**：SASRec, GRU4Rec。
    *   **生成式基准**：RID, TID, P5, GenRec, TIGER, LC-Rec, OneRec, DiffGRM。

### 4. 资源与算力
*   **算力设备**：实验在 **8 台 NVIDIA A800 GPU** 上进行。
*   **训练细节**：采用了分布式训练以适配大规模参数模型（如 Flan-T5-XL）。论文中未详细列出每个模型的具体训练时长，但强调了严格遵循原始论文的超参数配置以确保复现的准确性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集、3 种评估场景、10 种对比算法，并针对模型规模（4 种）、ID 设计（5 种）和训练策略进行了详尽的消融实验。
*   **充分性与公平性**：实验设计非常充分。通过在同一框架下运行所有模型，并统一数据分割协议（90% 训练，10% 测试，严格区分冷启动实体），消除了因数据处理差异导致的性能偏差，具有高度的客观性和公平性。

### 6. 主要结论与发现
*   **冷启动表现的不对称性**：用户冷启动相对容易处理（性能下降温和），而**物品冷启动极其困难**，大多数模型在物品冷启动时性能会发生剧烈下滑。
*   **模型规模的局限性**：增加模型参数量（Scaling）虽然能带来提升，但**边际效应递减**，且无法从根本上消除冷启动带来的性能差距。
*   **ID 设计的权衡（Trade-off）**：
    *   **文本 ID** 在物品冷启动中表现最好，但在热启动和用户冷启动中表现较差（存在语义歧义）。
    *   **语义 ID** 在热启动中更稳健，但在物品冷启动中容易失效。
    *   **组合式语义编码（如 OPQ）** 在保持热启动性能的同时，对冷启动表现出更好的鲁棒性。
*   **强化学习的负面影响**：在冷启动场景下，RL 并不总能提升性能，甚至可能因为过度拟合训练分布而导致泛化能力下降。

### 7. 优点
*   **系统性强**：首次系统地解耦了生成式推荐中多个复杂的设计因素。
*   **实战指导意义**：为开发者在不同业务场景（如新用户多还是新物品多）下选择 ID 类型和模型规模提供了明确的实证依据。
*   **开源贡献**：提供了统一的评估框架和代码，有助于推动该领域的标准化。

### 8. 不足与局限
*   **多模态缺失**：目前的分析主要集中在文本语义，未深入探讨视频、图像等多模态信息在生成式推荐冷启动中的作用。
*   **理论深度**：虽然观察到了量化结构（如 OPQ）的优越性，但缺乏更深层次的理论解释（例如为什么某种量化结构能更好地处理分布偏移）。
*   **动态性模拟**：实验环境仍是静态离线的，未能完全模拟真实世界中物品池快速更迭的动态过程。

（完）
