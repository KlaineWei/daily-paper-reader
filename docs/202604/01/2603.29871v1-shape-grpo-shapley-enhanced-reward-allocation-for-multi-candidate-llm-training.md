---
title: "ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training"
title_zh: ShapE-GRPO：面向多候选大语言模型训练的 Shapley 增强型奖励分配
authors: "Rui Ai, Yu Pan, David Simchi-Levi, Chonghuan Wang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29871v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 基于强化学习的LLM推荐系统及集合效用优化
tldr: 针对LLM在推荐、代码建议等场景中生成候选集合时，传统GRPO方法将集合奖励平均分配导致训练信号模糊的问题，本文提出ShapE-GRPO。该方法利用合作博弈论中的Shapley值，将集合级奖励分解为候选特定的细粒度信号，有效解决了低质量候选者“搭便车”的现象。实验证明，该方法在多个数据集上优于标准GRPO，且训练收敛更快。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29871v1/fig-001.webp\", \"caption\": \"Figure 1: When the user considers the ratings for the three suggestions to be 5.0, 4.0, and 3.0, respectively, our ShapE-GRPO performs candidate-level (with broadcasting to token-level) reward allocation as above. However, GRPO only uses a single +5.0 sequence-level reward shared across tokens.\", \"page\": 2, \"index\": 1, \"width\": 827, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-002.webp\", \"caption\": \"Figure 4: Response reward as a function of the number of candidates across different datasets using QWEN3-8B.\", \"page\": 18, \"index\": 2, \"width\": 826, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-003.webp\", \"caption\": \"Table 2: Main training hyperparameters used in our experiments.\", \"page\": 23, \"index\": 3, \"width\": 714, \"height\": 231}]"
motivation: 现有强化学习方法在处理候选集合时，将相同的集合奖励分配给所有候选者，导致低质量候选者因高质量同伴而获得虚高奖励，产生训练噪声。
method: 提出ShapE-GRPO，利用Shapley值将集合级效用分解为针对每个候选者的特定奖励信号，并确保计算复杂度在多项式时间内。
result: ShapE-GRPO在多个多样化数据集上一致优于标准GRPO，并显著加快了训练过程中的收敛速度。
conclusion: 通过引入Shapley值进行细粒度奖励分配，可以有效提升多候选场景下LLM强化学习训练的效率和性能。
---

## 摘要
在推荐、头脑风暴和代码建议等用户-代理交互场景中，大语言模型（LLMs）通常会生成一组候选推荐，其目标是最大化整个集合的集体效用，而非独立地最大化单个候选。然而，现有的强化学习后训练范式（如群体相对策略优化，GRPO）通常会为集合中的每个候选分配相同的集合级标量奖励。这导致了训练信号的噪声，表现为表现较差的候选会“搭便车”，利用单个强力候选产生的高奖励，从而导致探索效率低下。为了解决这一问题，我们提出了 Shapley 增强型 GRPO（ShapE-GRPO）。通过利用集合级效用的置换不变性，我们从合作博弈论中推导出一种 Shapley 增强公式，将集合级奖励分解为细粒度的、针对特定候选的信号。我们证明了该公式在保持多项式时间复杂度的计算效率的同时，保留了 Shapley 值的基本公理。实验表明，ShapE-GRPO 在多种数据集上始终优于标准的 GRPO，并在训练过程中实现了更快的收敛。

## Abstract
In user-agent interaction scenarios such as recommendation, brainstorming, and code suggestion, Large Language Models (LLMs) often generate sets of candidate recommendations where the objective is to maximize the collective utility of the entire set rather than individual candidates independently. However, existing reinforcement learning post-training paradigms, such as Group Relative Policy Optimization (GRPO), typically assign the same set-level scalar reward to every candidate in the set. This leads to noisy training signals where poor candidates free-ride on the high reward produced by a single strong peer, resulting in suboptimal exploration. To address this, we propose Shapley-Enhanced GRPO (ShapE-GRPO). By leveraging the permutation-invariant nature of set-level utility, we derive a Shapley-enhanced formulation from cooperative game theory to decompose set-level rewards into granular, candidate-specific signals. We show that our formulation preserves the fundamental axioms of the Shapley value while remaining computationally efficient with polynomial-time complexity. Empirically, ShapE-GRPO consistently outperforms standard GRPO across diverse datasets with accelerated convergence during training.

---

## 论文详细总结（自动生成）

以下是对论文《ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在推荐、代码建议、头脑风暴等场景中，LLM 通常生成一组候选方案（Set of Candidates）。目标是最大化整个集合的集体效用（如覆盖度、多样性），而非单个候选的独立质量。
*   **核心问题**：现有的强化学习后训练方法（如 GRPO）通常将集合级别的标量奖励（Set-level Reward）平均分配给集合中的所有候选。这导致了**“搭便车”（Free-riding）问题**：一个极差的候选可能因为同组中有一个极优秀的候选而获得高奖励，产生严重的训练噪声，降低探索效率。
*   **整体含义**：本文旨在通过博弈论中的 Shapley 值理论，将集合奖励公平、精确地分解到每个候选个体上，从而提供更清晰的训练信号。

### 2. 方法论
*   **核心思想**：将一次生成中的多个候选视为合作博弈中的“玩家”，利用 Shapley 值衡量每个候选对集合总奖励的边际贡献。
*   **关键技术细节**：
    *   **奖励分解**：对于包含 $K$ 个候选的响应，计算每个候选 $c_i$ 的 Shapley 值 $\phi_i$。
    *   **奖励缩放**：为了保持与原始 GRPO 奖励量级一致，将候选的奖励设为 $K \cdot \phi_i$。
    *   **Token 级分配**：响应中的推理部分（Reasoning）保留原始集合奖励；候选部分的 Token 则分配其对应的 Shapley 增强奖励。
    *   **计算优化**：论文证明了在“LLM 作为推荐系统”的特定结构下（奖励通常由集合中最好的候选决定），Shapley 值不需要指数级计算，可以通过排序在 **$O(K^2)$** 甚至 **$O(K)$**（二值奖励下）的时间复杂度内精确求得。
*   **算法流程**：采样一组响应 -> 提取候选 -> 计算每个候选的边际贡献（Shapley） -> 更新策略梯度。

### 3. 实验设计
*   **数据集/场景**：
    1.  **摘要生成 (ACLSum)**：科学论文摘要，使用 GPT-5.2 作为裁判进行多维度打分。
    2.  **代码建议 (DS-1000)**：数据科学编程任务，通过单元测试执行结果作为二值奖励。
    3.  **电影推荐 (Netflix)**：基于 Netflix Prize 数据构建，根据用户历史评分预测其对推荐电影的满意度。
*   **对比方法 (Baselines)**：
    *   **标准 GRPO**：所有候选共享相同的集合奖励。
    *   **Winner-Takes-All (WTA)**：仅将奖励分配给得分最高的候选，其余为零。
    *   **Pretrained Model**：未经过 RL 训练的基座模型。

### 4. 资源与算力
*   **算力设备**：使用了 **2 台 NVIDIA GH200 GPU**。
*   **训练配置**：训练 1 个 epoch，学习率为 1e-6，最大响应长度 4096。
*   **模型**：主要在 **Qwen 3-8B** 上进行实验，并在 **Llama 3.1-8B-Instruct** 上验证了通用性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 NLP 摘要、代码生成、推荐系统三个完全不同的领域，实验设置较为全面。
*   **充分性**：
    *   对比了共享奖励（GRPO）和极端分配（WTA），证明了 Shapley 分配的优越性。
    *   提供了训练收敛曲线，展示了 ShapE-GRPO 在收敛速度和稳定性上的优势。
    *   在两种主流模型架构（Qwen 和 Llama）上均取得了一致的提升，证明了方法的鲁棒性。
*   **客观性**：所有任务使用统一的超参数，未针对特定任务进行过度调优，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能提升**：ShapE-GRPO 在所有任务上均显著优于标准 GRPO 和 WTA。
*   **收敛加速**：在 Netflix 推荐任务中，ShapE-GRPO 仅需约 160 步即可收敛，而 GRPO 需要 240 步且波动剧烈。
*   **解决搭便车**：理论和实验均证明，该方法能有效抑制错误候选获得正向激励，使模型更精准地学习高质量候选的特征。
*   **上下文利用**：模型在经过 ShapE-GRPO 训练后，能更好地利用长上下文中的用户历史信息。

### 7. 优点
*   **理论严谨**：引入 Shapley 公理化体系（效率、对称性、加性、哑玩家属性），为奖励分配提供了坚实的数学基础。
*   **计算高效**：通过数学推导规避了 Shapley 值的指数级计算难题，使其在大规模 LLM 训练中具有实际可行性。
*   **即插即用**：作为一种奖励分配机制，它可以轻松集成到现有的 GRPO 或 PPO 框架中。

### 8. 不足与局限
*   **候选提取依赖**：该方法依赖于模型能够生成结构化输出以便提取候选（如使用特定的 Tag），对于非结构化输出的解析可能存在偏差。
*   **长度假设**：理论推导中假设各候选的 Token 长度大致相等，虽然实验观察支持这一假设，但在极端不平衡的情况下可能引入微小偏差。
*   **长上下文挑战**：尽管有提升，但 Llama 3.1 在处理极长历史信息时仍表现出局限，这可能属于模型架构本身的瓶颈，而非奖励分配算法能完全解决。

（完）
