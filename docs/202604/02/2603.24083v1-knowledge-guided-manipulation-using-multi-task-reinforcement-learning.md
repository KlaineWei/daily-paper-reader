---
title: Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning
title_zh: 基于多任务强化学习的知识引导操纵
authors: "Aditya Narendra, Mukhammadrizo Maribjonov, Dmitry Makarov, Dmitry Yudin, Aleksandr Panov"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24083v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于多任务机器人操纵的基于模型的策略优化
tldr: 本研究提出KG-M3PO框架，旨在解决部分可观测环境下的多任务机器人操纵问题。该方法将第一视角视觉与在线3D场景图结合，通过动态关系机制实时更新空间和功能属性，并利用图神经网络将关系特征与强化学习目标端到端对齐。通过融合视觉、本体感知、语言和图表征，该智能体在处理遮挡、干扰和布局变化时表现出更强的鲁棒性、采样效率和泛化能力，为可扩展的通用操纵提供了结构化知识引导的新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24083v1/fig-001.webp\", \"caption\": \"TABLE II: Short- & long-horizon performance (M3PO vs. KG-M3PO).\", \"page\": 6, \"index\": 1, \"width\": 498, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.24083v1/fig-002.webp\", \"caption\": \"Fig. 6: Single-task results. Average normalized score vs. timesteps. Adding the KG improves sample efficiency and final score. For the M3PO backbone, the camera-only variant corresponds to M3PO, while camera+KG corresponds to KGM3PO.\", \"page\": 7, \"index\": 2, \"width\": 1054, \"height\": 538}]"
motivation: 针对部分可观测环境下机器人操纵任务中缺乏结构化环境理解和长程推理能力的问题。
method: 提出KG-M3PO框架，通过在线3D场景图构建动态关系表征，并将其与多模态输入融合进行端到端的强化学习训练。
result: 实验证明该方法在存在遮挡和干扰的复杂任务中，其成功率、采样效率及对新物体的泛化性能均显著优于强基线模型。
conclusion: 结构化且持续维护的世界知识是实现可扩展、通用化机器人操纵的强大归纳偏置，能有效提升复杂环境下的控制性能。
---

## 摘要
本文介绍了基于知识图谱的大规模多任务模型化策略优化（KG-M3PO），这是一个在部分可观测环境下统一了感知、知识和策略的多任务机器人操纵框架。该方法通过在线 3D 场景图增强了第一视角视觉，将开放词汇检测落地为度量式的关系表示。动态关系机制在每一步更新空间、包含和示能性（affordance）边，并且图神经编码器通过强化学习（RL）目标进行端到端训练，使得关系特征直接由控制性能塑造。多种观测模态（视觉、本体感受、语言和基于图的）被编码到一个共享的潜空间中，RL 智能体在此基础上运行以驱动控制循环。策略以轻量级图查询以及视觉和本体感受输入为条件，为决策提供了一个紧凑的、具有语义信息的决策状态。在一系列包含遮挡、干扰物和布局变化的操纵任务上的实验表明，该方法相对于强基线模型具有持续的提升：知识条件化智能体实现了更高的成功率、更高的样本效率，以及对新物体和未见场景配置更强的泛化能力。这些结果支持了这样一个前提：结构化的、持续维护的世界知识是实现可扩展、可泛化操纵的强大归纳偏置；当知识模块参与 RL 计算图时，关系表示与控制相对齐，从而在部分可观测性下实现鲁棒的长程行为。

## Abstract
This paper introduces Knowledge Graph based Massively Multi-task Model-based Policy Optimization (KG-M3PO), a framework for multi-task robotic manipulation in partially observable settings that unifies Perception, Knowledge, and Policy. The method augments egocentric vision with an online 3D scene graph that grounds open-vocabulary detections into a metric, relational representation. A dynamic-relation mechanism updates spatial, containment, and affordance edges at every step, and a graph neural encoder is trained end-to-end through the RL objective so that relational features are shaped directly by control performance. Multiple observation modalities (visual, proprioceptive, linguistic, and graph-based) are encoded into a shared latent space, upon which the RL agent operates to drive the control loop. The policy conditions on lightweight graph queries alongside visual and proprioceptive inputs, yielding a compact, semantically informed state for decision making.   Experiments on a suite of manipulation tasks with occlusions, distractors, and layout shifts demonstrate consistent gains over strong baselines: the knowledge-conditioned agent achieves higher success rates, improved sample efficiency, and stronger generalization to novel objects and unseen scene configurations. These results support the premise that structured, continuously maintained world knowledge is a powerful inductive bias for scalable, generalizable manipulation: when the knowledge module participates in the RL computation graph, relational representations align with control, enabling robust long-horizon behavior under partial observability.

---

## 论文详细总结（自动生成）

这是一份关于论文《Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：机器人在非结构化日常环境中进行操纵时，面临严重的**部分可观测性（Partial Observability）**问题（如物体被遮挡、置于容器内）。
*   **研究动机**：纯反应式策略（从像素到转矩）容易产生状态混淆，且在长程任务中难以进行信用分配。传统的基于模型的方法往往依赖脆弱的手工几何管线。
*   **目标**：建立一个统一的“感知→知识→策略”框架，通过结构化的世界知识（3D 场景图）来弥补感知与策略之间的鸿沟，实现具备多任务能力且泛化性强的机器人操纵。

### 2. 方法论（KG-M3PO 框架）
*   **核心思想**：将动态更新的 **3D 场景图（3DSG）** 集成到基于模型的强化学习算法（M3PO）中。
*   **关键技术细节**：
    *   **在线场景图构建**：利用 BBQ 算法从第一视角 RGB-D 流中提取物体节点（包含 3D 边界框和 CLIP 语义嵌入）及空间关系（如“在...之上”、“在...之后”）。
    *   **动态关系机制**：每 10 个步骤局部刷新受影响的子图，实时更新物体的空间、包含和示能性（Affordance）边缘。
    *   **多模态融合**：将视觉嵌入（卷积网络）、知识嵌入（图神经网络 GNN）、语言目标嵌入和本体感受数据融合为一个共享的潜在状态空间。
    *   **端到端训练**：**关键创新点**在于 GNN 编码器直接通过强化学习的梯度进行训练（而非独立预训练）。这意味着关系特征的提取是直接由控制性能驱动的，确保了知识表示与操纵任务的对齐。
    *   **奖励函数**：采用混合奖励机制，结合了终端成功奖励、基于势能的状态塑造奖励以及针对动作平滑度和碰撞的惩罚。

### 3. 实验设计
*   **实验环境**：基于 NVIDIA Isaac Lab (Isaac Sim) 构建的物理仿真环境，支持光线追踪渲染和 GPU 加速的并行仿真。
*   **机器人平台**：Franka Emika Panda (7-DoF) 和 UR5 (6-DoF)。
*   **任务设置**：
    *   **完全可观测（FO）任务**：抓取方块、放置方块、打开柜子。
    *   **部分可观测（PO）任务**：抓取被墙遮挡的物体、移除遮挡物（圆锥）后进行抓取放置。
*   **对比基线（Baselines）**：
    *   **无图基线**：PPO, SAC, DreamerV3, TD-MPC2, IMPALA, 原始 M3PO（仅相机输入）。
    *   **知识增强版本**：KG-M3PO（本文方法）。

### 4. 资源与算力
*   **硬件配置**：单台工作站，配备 **NVIDIA RTX 5070 Ti (16 GB VRAM)** 和 64 GB RAM。
*   **并行规模**：利用 Isaac Lab 的平铺渲染 API，在单个进程中并行运行 **1024 个环境**。
*   **训练时长**：
    *   标准 M3PO（仅相机）：4 亿步训练约需 0.61 GPU 天。
    *   KG-M3PO（含知识图谱）：4 亿步训练约需 **0.79 GPU 天**。
    *   虽然 KG 增加了计算开销，但在达到相同性能（如 600 分）时，KG-M3PO 所需的总步数和时间远少于基线。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单任务、多任务、完全可观测及部分可观测等多种配置。
*   **消融实验**：专门针对**场景图噪声**进行了鲁棒性测试，包括边界框中心位移、尺寸缩放和旋转噪声。
*   **充分性评价**：实验设计较为全面，通过跨机器人平台（Franka 和 UR5）验证了通用性。对比了当前主流的多种 RL 算法，且使用了标准化的归一化评分（0-1000 分）进行评估，保证了客观性和公平性。

### 6. 主要结论与发现
*   **知识图谱的必要性**：在部分可观测任务中，仅靠相机的基线模型几乎无法完成任务，而 KG-M3PO 能够通过持久的结构化状态维持对遮挡物体的记忆，从而成功解决任务。
*   **效率提升**：在完全可观测任务中，引入知识图谱显著提高了采样效率和最终成功率。
*   **端到端优势**：将 GNN 纳入 RL 计算图，使得关系特征能够根据控制需求自适应调整，优于分离的知识表示。
*   **鲁棒性**：策略对中等程度的几何噪声（如检测误差）具有较强的容忍度，性能下降平缓。

### 7. 优点与亮点
*   **闭环集成**：成功将高层语义知识（场景图）与底层连续控制（RL）在端到端框架下统一。
*   **动态更新**：提出的在线关系更新机制解决了静态场景图容易过时的问题。
*   **多任务泛化**：展示了单个策略在处理不同机器人架构和异构任务目标时的潜力。

### 8. 不足与局限
*   **缺乏真机验证**：所有实验均在仿真环境（Isaac Sim）中完成，尚未在真实物理机器人上进行验证，存在 Sim-to-Real 的鸿沟。
*   **计算开销**：引入图处理流程增加了每一步的计算延迟（约增加 30% 的训练时间）。
*   **动态环境限制**：对于物体高速运动或频繁发生拓扑变化的极端动态场景，场景图的维护可能面临挑战。
*   **依赖检测质量**：虽然有噪声消融实验，但极端的检测失败（如物体完全丢失）仍可能导致策略崩溃。

（完）
