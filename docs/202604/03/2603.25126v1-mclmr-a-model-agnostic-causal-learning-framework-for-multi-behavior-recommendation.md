---
title: "MCLMR: A Model-Agnostic Causal Learning Framework for Multi-Behavior Recommendation"
title_zh: MCLMR：一种用于多行为推荐的模型无关因果学习框架
authors: "Ranxu Zhang, Junjie Meng, Ying Sun, Ziqi Xu, Bing Yin, Hao Li, Yanyong Zhang, Chao Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25126v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于多行为推荐的模型无关因果学习框架
tldr: 针对多行为推荐中存在的混杂效应、异构行为聚合困难及行为表征偏差等挑战，本文提出了一种模型无关的因果学习框架MCLMR。该框架通过构建因果图并进行干预以实现无偏偏好估计，同时结合基于混合专家机制的自适应聚合模块和偏差感知的对比学习来优化行为表征。实验证明，MCLMR能显著提升多种基准模型的性能，具有极强的通用性和有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25126v1/fig-001.webp\", \"caption\": \"Table 3: Performance Comparison with Pluggable Causal Models (The best results for each backbone are in bold).\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2603.25126v1/fig-002.webp\", \"caption\": \"Table 2: Performance Comparison with Various Methods (The best performance is highlighted in bold). The statistical significance of improvements over the best baseline is determined by a paired t-test. Symbols ∗, ∗∗, and ∗∗∗ denote statistical significance at the level of 𝑝 < 0.05, 𝑝 < 0.01, and 𝑝 < 0.001, respectively.\", \"page\": 6, \"index\": 2, \"width\": 1056, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2603.25126v1/fig-003.webp\", \"caption\": \"Figure 1: The overall framework of the MCLMR model\", \"page\": 4, \"index\": 3, \"width\": 888, \"height\": 481}]"
motivation: 现有多行为推荐方法缺乏处理用户习惯与项目分布混杂效应的理论框架，且在异构行为融合与跨行为表征对齐方面存在局限。
method: 提出MCLMR框架，利用因果干预消除偏差，通过MoE结构动态聚合辅助行为，并引入偏差感知对比学习实现跨行为语义对齐。
result: 在三个真实数据集上的实验表明，MCLMR作为通用框架能显著增强各类基准模型的推荐性能。
conclusion: MCLMR是一个有效且通用的因果学习框架，为多行为推荐中的无偏估计和多源信息融合提供了新方案。
---

## 摘要
多行为推荐（MBR）利用多种用户交互类型（如浏览、点击、购买）来丰富偏好建模，并缓解传统单行为方法中的数据稀疏问题。然而，现有的 MBR 方法面临着根本性的挑战：它们缺乏规范的框架来建模来自用户行为习惯和物品多行为分布的复杂混杂效应，难以有效聚合异构辅助行为，且在考虑偏差失真的同时，无法跨越语义鸿沟对齐行为表示。为了解决这些局限性，我们提出了 MCLMR，这是一种新颖的模型无关因果学习框架，可以无缝集成到各种 MBR 架构中。MCLMR 首先构建一个因果图来建模混杂效应，并进行干预以实现无偏偏好估计。在该因果框架下，它采用基于混合专家模型（Mixture-of-Experts）的自适应聚合模块来动态融合辅助行为信息，并利用偏差感知对比学习模块以偏差感知的方式对齐跨行为表示。在三个真实世界数据集上的广泛实验表明，MCLMR 在各种基准模型上均实现了显著的性能提升，验证了其有效性和通用性。所有数据和代码都将公开。代码链接为：https://github.com/gitrxh/MCLMR。

## Abstract
Multi-Behavior Recommendation (MBR) leverages multiple user interaction types (e.g., views, clicks, purchases) to enrich preference modeling and alleviate data sparsity issues in traditional single-behavior approaches. However, existing MBR methods face fundamental challenges: they lack principled frameworks to model complex confounding effects from user behavioral habits and item multi-behavior distributions, struggle with effective aggregation of heterogeneous auxiliary behaviors, and fail to align behavioral representations across semantic gaps while accounting for bias distortions. To address these limitations, we propose MCLMR, a novel model-agnostic causal learning framework that can be seamlessly integrated into various MBR architectures. MCLMR first constructs a causal graph to model confounding effects and performs interventions for unbiased preference estimation. Under this causal framework, it employs an Adaptive Aggregation module based on Mixture-of-Experts to dynamically fuse auxiliary behavior information and a Bias-aware Contrastive Learning module to align cross-behavior representations in a bias-aware manner. Extensive experiments on three real-world datasets demonstrate that MCLMR achieves significant performance improvements across various baseline models, validating its effectiveness and generality. All data and code will be made publicly available. For anonymous review, our code is available at the following the link: https://github.com/gitrxh/MCLMR.

---

## 论文详细总结（自动生成）

### 论文总结：MCLMR —— 多行为推荐的模型无关因果学习框架

#### 1. 核心问题与研究动机
多行为推荐（MBR）旨在通过利用多种交互类型（如浏览、加购、收藏）来缓解目标行为（如购买）的数据稀疏问题。然而，现有方法面临三大挑战：
*   **混杂效应（Confounding Effects）：** 用户行为习惯和物品的多行为分布构成了隐性偏差，导致模型学习到虚假相关性而非真实偏好。
*   **异构行为聚合困难：** 不同辅助行为对目标行为的贡献度随用户和物品动态变化，简单的融合方式难以捕捉这种复杂性。
*   **语义鸿沟与偏差失真：** 不同行为之间存在语义差异，且偏差会扭曲跨行为的表征对齐。

#### 2. 方法论：MCLMR 框架
MCLMR 被设计为一个**模型无关（Model-Agnostic）**的插件，其核心思想是通过因果干预消除偏差，并动态聚合多源信息。
*   **因果偏好估计（Causal Preference Estimation）：** 
    *   构建因果图，将用户习惯（$B_u$）和物品倾向（$B_i$）建模为混杂变量。
    *   利用 Pearl 的 **后门调整（Backdoor Adjustment）** 公式进行因果干预，推导出无偏的偏好估计概率。
    *   使用广义行为转化率作为偏差代理（Proxy），将复杂的因果推导转化为可计算的神经模块。
*   **自适应聚合模块（Adaptive Aggregation）：** 
    *   采用**双路径门控机制**：路径一利用 **Jaccard 相似度** 捕捉结构相关性；路径二利用 **混合专家网络（MoE）** 捕捉深层语义相关性。
    *   动态调整辅助行为对目标行为预测的贡献权重。
*   **偏差感知对比学习（Bias-aware Contrastive Learning）：** 
    *   在不同行为视图间进行用户和物品的双重对齐。
    *   引入**偏差感知自适应温度系数（$\tau$）**：根据行为偏差的大小动态调整对比学习的强度，偏差大的行为分配更低的温度以强化对齐，从而平衡主任务与辅助任务。

#### 3. 实验设计
*   **数据集：** 使用了三个公开的真实世界数据集：**Tmall**、**Jdata** 和 **Taobao**。
*   **Benchmark 与对比方法：**
    *   **单行为方法：** MF-BPR, NCF, LightGCN。
    *   **多行为方法：** MBGCN, GNMR, S-MBRec, CRGCN, BCIPM, HEC-GCN 等。
    *   **因果/去偏方法：** CVID, CMSR（集成式）；SNIPS, CausE, Multi-IPW, DCCL（可插拔式）。
*   **评估指标：** HR@K 和 NDCG@K（K=10, 50）。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA RTX 4090 GPU** 上实现。
*   **运行分析：** 论文在附录中补充了在 **NVIDIA H20-3e GPU** 上的实测数据。
*   **训练时长：** 以 HEC-GCN 为例，在 Tmall 数据集上每轮（epoch）训练时间从 625s 增加到 650s（约增加 4%），开销较小。
*   **推理延迟：** 由于因果模块仅在训练期生效，推理时仅需进行向量内积，因此**在线推理延迟为零增量**。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了多维度的实验，包括总体性能对比、与可插拔去偏方法的横向对比、消融实验（6组变体）、参数敏感性分析（偏差系数、MoE 维度、温度策略）。
*   **深度分析：** 针对活跃/非活跃用户组进行了细分实验，并分析了决策/辅助行为比例对推荐质量的影响。
*   **评价：** 实验设计非常充分，涵盖了从宏观效果到微观机制的全面验证，对比基准涵盖了 2025 年最新的 SOTA 模型（如 HEC-GCN），体现了客观性和公平性。

#### 6. 主要结论与发现
*   **普适性提升：** MCLMR 作为插件，能显著提升包括 LightGCN、CRGCN、BCIPM 在内的所有基准模型的性能。
*   **长尾效应缓解：** 实验证明 MCLMR 对**低活跃度（长尾）用户**的提升尤为显著，有效缓解了推荐系统中的“马太效应”。
*   **高质量推荐：** 通过消除混杂因子，模型更倾向于推荐具有高决策价值的物品，而非仅仅是高点击量的物品。

#### 7. 优点与亮点
*   **模型无关性：** 能够无缝集成到任何基于嵌入的推荐模型中，具有极高的工程实用价值。
*   **因果理论支撑：** 不同于纯启发式的去偏方法，MCLMR 基于严谨的因果图和 do-算子推导，解释性更强。
*   **动态温度机制：** 在对比学习中引入偏差感知的自适应温度，巧妙解决了多行为数据中信号强度不一的问题。

#### 8. 不足与局限
*   **偏差代理的简化：** 论文使用相对频率作为隐性混杂因子的代理变量，虽然符合惯例，但可能无法完全捕捉极其复杂的潜在偏差。
*   **计算复杂度：** 在集成到某些特定模型（如 BCIPM）时，由于对比学习涉及批次内负采样，计算复杂度随 Batch Size 的平方增长，在大规模部署时需注意内存管理。
*   **应用限制：** 主要针对具有显式多行为阶段的电商场景，在行为类型较少或行为间无明显序贯关系的场景下，提升幅度可能受限。

（完）
