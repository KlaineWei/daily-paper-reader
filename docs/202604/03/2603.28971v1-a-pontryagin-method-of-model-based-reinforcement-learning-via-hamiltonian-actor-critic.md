---
title: A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic
title_zh: 一种通过哈密顿 Actor-Critic 实现的基于模型的强化学习庞特里亚金方法
authors: "Chengyang Gu, Yuxin Pan, Hui Xiong, Yize Chen"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28971v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 基于模型的强化学习用于长程价值估计
tldr: 针对基于模型的强化学习中长时序价值估计受模型复合误差影响的问题，本文受庞特里亚金极大值原理启发，提出了哈密顿行动者-评论家（HAC）算法。该方法通过直接优化基于动力学和奖励定义的哈密顿量，避免了显式的价值函数学习，从而降低了对模型误差的敏感性。实验证明，HAC在连续控制任务的在线和离线设置中均优于现有基准，展现出极高的采样效率和对分布偏移的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28971v1/fig-001.webp\", \"caption\": \"Fig. 2: Learning Curves Comparison (Offline RL). We compare our HAC with IQL, SAC-Off and MOPO on LQR, Pendulum, and MountainCar, Swimmer. For MOPO and HAC which require multi-step imaginary rollouts, rollout horizon is to K = 10 for LQR and Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer. Results are averaged over 5 random seeds.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-002.webp\", \"caption\": \"Fig. 4: Critic Estimation Comparison: Plots comparing normalized estimated Q-values (in MVE-DDPG) and Hamiltonian (in our HAC) against ground-truth sampled from 10 random test episodes in LQR. An ideal critic should concentrates on the red unity line.\", \"page\": 18, \"index\": 2, \"width\": 1054, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-003.webp\", \"caption\": \"Fig. 1: Learning Curves Comparison (Online RL): We compare our HAC with DDPG, SAC and MVE-DDPG on Pendulum, MountainCar, Swimmer and Hopper. For MVEDDPG and HAC which require multi-step imaginary rollouts, we set the rollout horizon to K = 10 for Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer, and K = 3 for Hopper. All results are averaged over 5 random seeds.\", \"page\": 6, \"index\": 3, \"width\": 512, \"height\": 422}]"
motivation: 传统的基于模型的强化学习方法在进行长时序价值估计时，容易受到模型复合误差和价值函数近似偏差的影响。
method: 提出HAC算法，利用庞特里亚金极大值原理直接优化定义的哈密顿量，从而在不依赖显式价值函数的情况下实现策略优化。
result: 在连续控制基准测试中，HAC在收敛速度、控制性能以及处理离线数据和分布外场景的鲁棒性方面均优于主流基准方法。
conclusion: HAC通过引入哈密顿优化机制，有效规避了价值函数学习带来的误差累积问题，为提升模型强化学习的效率和稳定性提供了新路径。
---

## 摘要
基于模型的强化学习（MBRL）通过利用学习到的动力学模型进行策略优化，显著提高了样本效率。然而，Actor-Critic 等方法的有效性往往受到复合模型误差的限制，这会降低长时界价值估计的准确性。现有方法（如基于模型的价值扩展 MVE）通过多步展开在一定程度上缓解了这一问题，但仍对展开时界的选取和残差模型偏差较为敏感。受庞特里亚金极大值原理（PMP）的启发，我们提出了哈密顿 Actor-Critic（HAC）。这是一种基于模型的方法，通过直接优化在确定性系统的学习动力学和奖励上定义的哈密顿量，消除了显式的价值函数学习。通过避免价值近似，HAC 在降低对模型误差敏感性的同时，提供了收敛性保证。在在线和离线强化学习设置下的连续控制基准测试中，大量实验表明，HAC 在控制性能、收敛速度以及对分布偏移（包括分布外 OOD 场景）的鲁棒性方面均优于无模型和基于 MVE 的基准方法。在数据有限的离线设置中，HAC 达到或超过了当前最先进的方法，凸显了其强大的样本效率。

## Abstract
Model-based reinforcement learning (MBRL) improves sample efficiency by leveraging learned dynamics models for policy optimization. However, the effectiveness of methods such as actor-critic is often limited by compounding model errors, which degrade long-horizon value estimation. Existing approaches, such as Model-Based Value Expansion (MVE), partially mitigate this issue through multi-step rollouts, but remain sensitive to rollout horizon selection and residual model bias. Motivated by the Pontryagin Maximum Principle (PMP), we propose Hamiltonian Actor-Critic (HAC), a model-based approach that eliminates explicit value function learning by directly optimizing a Hamiltonian defined over the learned dynamics and reward for deterministic systems. By avoiding value approximation, HAC reduces sensitivity to model errors while admitting convergence guarantees. Extensive experiments on continuous control benchmarks, in both online and offline RL settings, demonstrate that HAC outperforms model-free and MVE-based baselines in control performance, convergence speed, and robustness to distributional shift, including out-of-distribution (OOD) scenarios. In offline settings with limited data, HAC matches or exceeds state-of-the-art methods, highlighting its strong sample efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：《A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic》

#### 1. 核心问题与整体含义（研究动机和背景）
基于模型的强化学习（MBRL）通过学习环境动力学模型来提高采样效率，但在实际应用中面临**复合模型误差（Compounding Model Errors）**的挑战。在传统的 Actor-Critic 架构中，Critic 网络依赖于模型生成的虚构轨迹进行价值估计，模型偏差会随时间步累积，导致价值函数近似不准，进而误导策略（Actor）的优化。现有的改进方案（如 MVE）虽然引入了多步展开，但仍无法摆脱对显式价值函数近似的依赖。本文的研究动机是：**能否在 MBRL 中完全取消显式的 Critic 网络，直接利用动力学信息进行策略优化？**

#### 2. 论文提出的方法论
论文提出了 **Hamiltonian Actor-Critic (HAC)** 算法，其核心思想是将强化学习目标转化为最优控制问题，并利用**庞特里亚金极大值原理（PMP）**求解。

*   **核心思想**：不再学习 $Q$ 函数或 $V$ 函数，而是定义一个**哈密顿量（Hamiltonian）** $H(s, a, \lambda) = c(s, a) + \lambda^T f(s, a)$。其中 $c$ 是代价函数，$f$ 是动力学模型，$\lambda$ 是协态（costate）。
*   **关键技术细节**：
    *   **协态递归计算**：通过反向递归 $\lambda_t = \nabla_{s_t} H$ 计算协态，它代表了未来累积代价对当前状态的敏感度。
    *   **解析雅可比技术（Analytical Jacobian Technique）**：为了解决自动求导（Autograd）在处理高阶梯度时的计算开销和运行错误，作者针对 ReLU 激活函数的神经网络动力学模型，推导出了雅可比矩阵的闭式解，实现了高效、无图（graph-free）的梯度计算。
    *   **策略优化**：Actor 直接通过最小化虚构轨迹上的哈密顿量均值来更新参数，无需传统的 Critic 网络。
*   **理论保证**：论文证明了 HAC 在 Lipschitz 连续条件下具有收敛性，并且在特定奖励函数（如二次型）下，其 Critic 估计误差的上界比 MVE 更紧。

#### 3. 实验设计
*   **实验场景**：涵盖了从简单到复杂的连续控制任务，包括线性二次调节器（LQR）、经典控制（Pendulum, MountainCar）以及 MuJoCo 物理仿真环境（Swimmer, Hopper）。
*   **实验设置**：同时验证了**在线 RL**（与环境交互）和**离线 RL**（仅利用静态数据集）两种模式。
*   **Benchmark 与对比方法**：
    *   **无模型方法**：DDPG, SAC, IQL（离线）, SAC-Off（离线）。
    *   **基于模型方法**：MVE-DDPG, MOPO（离线）。
    *   **鲁棒性测试**：针对初始状态偏移（Initial State Shift）的分布外（OOD）实验。

#### 4. 资源与算力
*   **硬件环境**：实验在 **Google Colaboratory** 上运行，配置为 **NVIDIA T4 GPU** 和 **12GB RAM**。
*   **软件框架**：使用 PyTorch 实现。
*   **训练时长**：文中未明确给出总训练时长，但在消融实验中对比了单次梯度步的计算耗时（HAC 约为 0.017s - 0.030s，略高于单 Critic 的 MVE，但远低于多 Critic 集合的方法）。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同的基准环境上进行了测试，每个实验均使用 **5 个随机种子**取平均值，并给出了标准差。
*   **消融实验**：针对 Critic 集合（Ensemble）数量进行了消融研究，对比了 HAC 与不同规模 Ensemble MVE-DDPG 的性能与计算效率。
*   **充分性评价**：实验设计较为全面，涵盖了性能、收敛速度、采样效率和鲁棒性四个维度。通过在线与离线的双重验证，证明了该方法在不同数据获取模式下的普适性，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能优越**：HAC 在大多数任务中（尤其是短时界任务）的控制性能优于 DDPG、SAC 和 MVE-DDPG。
*   **收敛极快**：在离线 RL 设置下，HAC 仅需基准方法约 25%-30% 的样本量即可达到同等甚至更高的性能。
*   **鲁棒性强**：在面对初始状态偏移等 OOD 场景时，HAC 表现出比 MVE 更好的稳定性，证明了哈密顿量作为优化目标比近似价值函数更可靠。
*   **计算效率平衡**：虽然 HAC 的反向递归增加了单步计算量，但由于不需要训练多个 Critic 网络，其总体计算开销在达到同等性能时更具优势。

#### 7. 优点
*   **理论创新**：成功将经典控制理论（PMP）与深度强化学习结合，提供了一个无需显式 Critic 的新范式。
*   **误差抑制**：通过哈密顿守恒定律（Hamiltonian Conservation Law）的视角，从理论上解释并证明了为何 HAC 能获得更准确的价值导向。
*   **工程优化**：解析雅可比技术的引入解决了深度学习框架在处理控制理论公式时的效率瓶颈。

#### 8. 不足与局限
*   **系统限制**：目前的方法主要针对**确定性动力学系统**设计。虽然附录中讨论了随机系统的扩展，但核心实验尚未在高度随机的环境中验证。
*   **长程任务挑战**：在 Hopper 等复杂长程任务中，HAC 的表现受限于虚构展开步数 $K$。如果 $K$ 远小于任务总步数 $T$，哈密顿量的估计可能存在偏差。
*   **健康奖励敏感性**：在具有“生存奖励”（如 Hopper 的 Healthy Reward）的环境中，HAC 的收敛速度较慢，说明该方法在处理非平滑或非二次型奖励函数时仍有优化空间。

（完）
