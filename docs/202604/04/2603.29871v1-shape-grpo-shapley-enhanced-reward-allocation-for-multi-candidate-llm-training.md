---
title: "ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training"
title_zh: ShapE-GRPO：面向多候选大语言模型训练的 Shapley 增强奖励分配
authors: "Rui Ai, Yu Pan, David Simchi-Levi, Chonghuan Wang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29871v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 针对多候选LLM推荐集的强化学习优化
tldr: 针对推荐、头脑风暴等需要生成候选集合的场景，传统强化学习方法（如GRPO）将集合整体奖励平均分配给所有候选者，导致训练信号模糊。本文提出ShapE-GRPO，利用合作博弈论中的Shapley值将集合级奖励分解为候选者特有的细粒度信号。该方法在保持计算效率的同时，有效解决了“搭便车”问题，在多个数据集上实现了更快的收敛速度和更优的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29871v1/fig-001.webp\", \"caption\": \"Figure 1: When the user considers the ratings for the three suggestions to be 5.0, 4.0, and 3.0, respectively, our ShapE-GRPO performs candidate-level (with broadcasting to token-level) reward allocation as above. However, GRPO only uses a single +5.0 sequence-level reward shared across tokens.\", \"page\": 2, \"index\": 1, \"width\": 827, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-002.webp\", \"caption\": \"Figure 4: Response reward as a function of the number of candidates across different datasets using QWEN3-8B.\", \"page\": 18, \"index\": 2, \"width\": 826, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-003.webp\", \"caption\": \"Table 2: Main training hyperparameters used in our experiments.\", \"page\": 23, \"index\": 3, \"width\": 714, \"height\": 231}]"
motivation: 现有的GRPO等方法在处理多候选集合时，将相同的集合奖励分配给所有候选者，导致低质量候选者因高质量同伴而获得虚高奖励，产生训练噪声。
method: 引入Shapley值理论，将具有置换不变性的集合效用分解为候选者特有的奖励信号，并确保计算复杂度在多项式时间内。
result: 实验表明，ShapE-GRPO在多个任务上均优于标准GRPO，且训练收敛速度显著提升。
conclusion: 通过细粒度的奖励分配，ShapE-GRPO有效提升了多候选生成场景下大语言模型的训练效率和最终性能。
---

## 摘要
在推荐、头脑风暴和代码建议等用户-代理交互场景中，大语言模型（LLMs）通常会生成一组候选推荐，其目标是最大化整个集合的集体效用，而非独立地最大化单个候选。然而，现有的强化学习后训练范式（如群体相对策略优化，GRPO）通常将相同的集合级标量奖励分配给集合中的每一个候选。这会导致训练信号中存在噪声，表现为较差的候选会“搭便车”，利用单个优秀同伴产生的高奖励，从而导致次优的探索。为解决这一问题，我们提出了 Shapley 增强的 GRPO（ShapE-GRPO）。通过利用集合级效用的置换不变性，我们从合作博弈论中推导出一种 Shapley 增强公式，将集合级奖励分解为细粒度的、针对特定候选的信号。我们证明了该公式在保持多项式时间复杂度的计算效率的同时，保留了 Shapley 值的基本公理。实验表明，ShapE-GRPO 在多种数据集上均一致优于标准 GRPO，并在训练过程中实现了加速收敛。

## Abstract
In user-agent interaction scenarios such as recommendation, brainstorming, and code suggestion, Large Language Models (LLMs) often generate sets of candidate recommendations where the objective is to maximize the collective utility of the entire set rather than individual candidates independently. However, existing reinforcement learning post-training paradigms, such as Group Relative Policy Optimization (GRPO), typically assign the same set-level scalar reward to every candidate in the set. This leads to noisy training signals where poor candidates free-ride on the high reward produced by a single strong peer, resulting in suboptimal exploration. To address this, we propose Shapley-Enhanced GRPO (ShapE-GRPO). By leveraging the permutation-invariant nature of set-level utility, we derive a Shapley-enhanced formulation from cooperative game theory to decompose set-level rewards into granular, candidate-specific signals. We show that our formulation preserves the fundamental axioms of the Shapley value while remaining computationally efficient with polynomial-time complexity. Empirically, ShapE-GRPO consistently outperforms standard GRPO across diverse datasets with accelerated convergence during training.

---

## 论文详细总结（自动生成）

以下是对论文《ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在推荐、代码建议或头脑风暴等场景中，LLM 通常生成一个**候选集合**。现有的强化学习（RL）方法（如 GRPO）通常将集合整体的奖励（Set-level Reward）平均分配给集合中的每一个候选者。
*   **研究动机**：这种“一刀切”的奖励分配会导致**“搭便车”（Free-riding）问题**——即一个平庸甚至错误的候选者，可能因为同集合中存在一个极其优秀的候选者而获得高额奖励。这产生了严重的训练噪声，降低了探索效率，导致模型难以精准学习哪些特征真正贡献了效用。

### 2. 方法论：ShapE-GRPO
*   **核心思想**：借鉴合作博弈论中的 **Shapley 值（Shapley Value）**，将集合视为一个博弈，候选者视为玩家。通过计算每个候选者对集合总奖励的**边际贡献**，实现公平且细粒度的奖励分配。
*   **关键技术细节**：
    *   **候选级分配**：不同于以往研究尝试在 Token 级别做分配（容易受语序影响），该方法在候选者（Candidate）级别进行分配，保持了置换不变性。
    *   **奖励分解**：定义集合奖励为 $R(o) = \max_i R(c_i)$（即用户采纳了最好的那个）。利用 Shapley 公式计算每个候选者 $c_i$ 的得分 $\phi_i$。
    *   **计算优化**：论文证明了在 LLM 推荐场景下，Shapley 值不需要指数级计算。通过对候选者奖励进行排序，可以在 **$O(K^2)$**（甚至二元奖励下 $O(K)$）的多项式时间内精确计算，其中 $K$ 是候选者数量。
    *   **算法流程**：在 GRPO 框架下，将原始的优势函数（Advantage）替换为基于 Shapley 值缩放后的优势函数，引导模型对高贡献候选者进行正向激励，对低贡献或错误候选者进行抑制。

### 3. 实验设计
*   **应用场景与数据集**：
    1.  **文本摘要 (ACLSum)**：科学论文摘要生成，使用 GPT-4o 作为评委进行连续打分。
    2.  **代码建议 (DS-1000)**：数据科学编程任务，采用二元奖励（代码是否通过测试）。
    3.  **电影推荐 (Netflix)**：基于 Netflix Prize 数据构建的推荐任务，评估模型根据用户历史预测评分的能力。
*   **对比基准 (Baselines)**：
    *   **标准 GRPO**：全集合共享奖励。
    *   **Winner-Takes-All (WTA)**：仅奖励得分最高的候选者，其余为零。
    *   **Pretrained Model**：未经过 RL 训练的原始模型。
*   **测试模型**：Qwen 3-8B 和 Llama 3.1-8B-Instruct。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA GH200 GPU**。
*   **训练细节**：设置候选者数量 $K=4$，训练 Epoch 为 1，学习率 1e-6，最大响应长度 4096。论文提到 ShapE-GRPO 引入的额外计算开销极小（仅涉及简单的浮点运算，无需额外梯度计算）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种截然不同的任务（摘要、代码、推荐），并使用了两种主流的开源模型架构。
*   **充分性评价**：实验设计较为充分。除了主实验外，还包含了训练收敛曲线对比、不同候选者数量的影响分析、以及针对不同模型架构的泛化性测试。通过引入 WTA 基准，排除了“仅仅是因为奖励了最好样本才变强”的简单假设，证明了 Shapley 协同分配的必要性。

### 6. 主要结论与发现
*   **性能提升**：ShapE-GRPO 在所有任务上均显著优于标准 GRPO 和 WTA。
*   **收敛加速**：在 Netflix 推荐任务中，ShapE-GRPO 仅需约 160 步即可达到饱和性能，而标准 GRPO 需要 240 步且波动剧烈。
*   **解决搭便车**：理论和实验均证明，错误或低质量的候选者在 ShapE-GRPO 下会获得负值或零优势信号，从而被有效抑制。

### 7. 优点
*   **理论严谨**：将经典的 Shapley 公理（效率、对称性、加性、哑玩家属性）引入 LLM 训练，提供了坚实的数学基础。
*   **计算高效**：巧妙利用排序特性将指数级复杂度降至多项式级，使其在大规模后训练中具有极高的实用价值。
*   **稳定性强**：相比于标准 GRPO 稀疏且模糊的信号，细粒度的奖励分配显著降低了训练方差。

### 8. 不足与局限
*   **结构化依赖**：该方法依赖于模型能够按照预设格式（如特定的 Tag）生成 $K$ 个清晰的候选者，如果模型指令遵循能力较弱，提取候选者可能会失败。
*   **任务局限**：目前主要针对“集合效用”类任务（如推荐），对于单次长文本生成（如写小说）的内部逻辑分配是否适用尚待研究。
*   **超参数敏感性**：虽然论文使用了统一参数，但在实际应用中，推理时的温度（Temperature）和候选者数量 $K$ 的选择可能会显著影响 Shapley 值的分布。

（完）
