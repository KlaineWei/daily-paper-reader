---
title: "Difference Feedback: Generating Multimodal Process-Level Supervision for VLM Reinforcement Learning"
title_zh: 差异反馈：为 VLM 强化学习生成多模态过程级监督
authors: "Feiding, Yongkang Zhang, Yuhao Liao, Zijian Zeng, Chunzheng Zhu, Yaozong Zheng, Yafei Liu, Yeling Peng, Youwei Wang, Sibo Wang, Huiming Yang, Linglin Liao, Shunzhi Yang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27482v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 具有过程级监督的VLM强化学习，用于多步推理
tldr: "针对视觉语言模型（VLM）在强化学习对齐中面临的终端奖励稀疏、中间推理步骤与视觉证据脱节及幻觉问题，本文提出“差异反馈”（Difference Feedback）方法。该方法通过自动修复错误推理轨迹来构建Token或步骤级的监督掩码，无需大规模人工标注即可实现过程级视觉对齐。实验表明，在相同计算预算下，该方法在MMMStar和MathVista等基准测试上平均提升了3%，为多模态推理过程对齐提供了一种低成本且高效的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27482v1/fig-001.webp\", \"caption\": \"Table 1. Main experimental results. For selected experiments, we report the mean over five random seeds together with the 95% bootstrap confidence interval (mean ± 95% CI). Improvements over the baseline methods are statistically significant under a paired bootstrap test (p < 0.01).\", \"page\": 12, \"index\": 1, \"width\": 668, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2603.27482v1/fig-002.webp\", \"caption\": \"Fig. 1. Difference Feedback (DF) provides fine-grained process supervision for VLM alignment. When the policy produces an incorrect trajectory, a small-edit repair is generated; the difference between the two outputs yields a token-level mask that gates gradient updates.\", \"page\": 2, \"index\": 2, \"width\": 725, \"height\": 271}]"
motivation: 传统的终端奖励在多步推理中存在信用分配稀疏问题，导致视觉证据与推理步骤脱节并引发模型幻觉。
method: 提出一种自动修复错误轨迹并生成Token/步骤级监督掩码的差异反馈机制，并将其集成到类GRPO的强化学习框架中。
result: "在MMMStar和MathVista等多个多模态推理基准测试中，该方法在相同计算资源下实现了平均3%的性能提升。"
conclusion: 差异反馈为VLM的过程级对齐提供了一种无需高昂人工标注成本的有效路径，显著增强了视觉推理的准确性。
---

## 摘要
视觉语言模型（VLM）越来越多地通过类 GRPO（Group Relative Policy Optimization）训练进行对齐。然而，仅依赖终端结果奖励会导致多步推理中的信用分配稀疏，削弱了视觉证据与中间步骤之间的联系，并常导致优化不稳定和视觉幻觉。我们提出了差异反馈（Differential Feedback），通过修复错误的推理轨迹自动构建 token/步骤级监督掩码，明确标记需要修正的关键位置。无需昂贵的大规模逐步人工标注，我们的方法即可实现过程级视觉对齐，并能无缝集成到现有的类 GRPO 框架中。在包括 MMMStar 和 MathVista 在内的多模态推理基准测试上的实验表明，在相同的计算预算下，平均性能提升了 3%。我们的方法为实现准确的视觉-推理过程对齐提供了一种有效且低成本的解决方案。

## Abstract
Vision--language models (VLMs) are increasingly aligned via Group Relative Policy Optimization (GRPO)-style training. However, relying solely on terminal outcome rewards yields sparse credit assignment in multi-step reasoning, weakening the linkage between visual evidence and intermediate steps and often causing unstable optimization and visual hallucinations. We propose Differential Feedback, which automatically constructs token/step-level supervision masks by repairing erroneous reasoning trajectories, explicitly marking the key positions that require correction. Without costly large-scale step-by-step human annotations, our method enables process-level visual alignment and can be seamlessly integrated into existing GRPO-like frameworks. Experiments on multimodal reasoning benchmarks including MMMStar and MathVista show an average 3% improvement under matched compute budgets. Our approach offers an effective, low-cost solution for accurate vision--reasoning process alignment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Difference Feedback: Generating Multimodal Process-Level Supervision for VLM Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM）在进行多步推理对齐时，通常依赖“终端结果奖励”（Outcome Supervision）。这种方式存在**信用分配稀疏**的问题，即模型无法得知中间哪一步出错，导致视觉证据与推理步骤脱节，引发训练不稳定和视觉幻觉。
*   **研究背景**：现有的过程监督（Process Supervision）通常需要昂贵的人工逐步标注，或者依赖复杂的搜索算法（如 MCTS），难以在大规模强化学习（RL）训练中高效扩展。

### 2. 方法论：差异反馈（Difference Feedback, DF）
*   **核心思想**：当策略模型生成错误轨迹时，利用一个“修复模型”生成一个仅需少量修改（Small-edit）即可转正的正确轨迹。通过对比错误轨迹与修复轨迹的差异，自动生成 Token 或步骤级的**监督掩码（Mask）**，从而精确定位需要修正的关键位置。
*   **关键技术细节**：
    *   **修复模型训练**：采用两阶段训练。第一阶段是少量人工标注数据的 SFT；第二阶段是强化学习，奖励函数结合了任务正确性、编辑距离惩罚（鼓励最小化修改）以及“图像移除检查”（防止模型不看图只靠文本先验答题）。
    *   **差异对齐与掩码生成**：使用 Levenshtein 距离或最长公共子序列（LCS）算法对齐原始错误输出和修复输出，将差异点标记为掩码 $m$。
    *   **插件式集成**：将掩码 $m$ 作为梯度门控（Gating）引入 RL 目标函数（如 GRPO、PPO）。对于错误样本，仅更新掩码标记的 Token 梯度，减少无效更新。
    *   **DF-GRPO 流程**：在采样组中，将错误轨迹替换为其修复版本，并应用双向差异掩码来引导策略优化。

### 3. 实验设计
*   **数据集与基准**：
    *   **数学推理**：MathVista, MathVerse, MathVision。
    *   **综合多模态推理**：MMMStar, MMMU, AI2D。
*   **对比方法**：
    *   **基座模型**：Qwen2.5-VL (7B/32B), InternVL3.5-8B-MPO。
    *   **对齐算法**：GSPO, GRPO, PPO, 以及引入隐式过程奖励的方法（如 PRIME）。
    *   **其他变体**：ThinkLite-7B, R1-ShareVL, Vision-G1 等近期主流 VLM 强化学习模型。

### 4. 资源与算力
*   **硬件设备**：使用了 **32 台 NVIDIA A800 GPU**。
*   **训练细节**：
    *   Qwen2.5-VL 实验中，Rollout Batch Size 为 128，学习率为 $1 \times 10^{-6}$。
    *   InternVL 实验中，Rollout Batch Size 为 512。
*   **计算对齐协议**：为了公平竞争，论文采用了“计算量匹配协议”，即 DF 方法的总训练时间（包含训练修复模型的时间 + 策略训练时间）与基准方法（如纯 GRPO）的总训练时间完全一致。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 不同参数规模的模型，并在 6 个主流多模态基准上进行了测试。
*   **消融实验**：非常详尽，验证了对齐算法（LCS vs Levenshtein）、权重方案（硬掩码 vs 软权重）、修复模型训练阶段（SFT vs RL）、编辑惩罚系数 $\lambda$ 以及采样数 $n$ 的影响。
*   **客观性**：通过计算量匹配确保了对比的公平性，并提供了 95% 置信区间和显著性检验（p < 0.01），实验设计严谨客观。

### 6. 主要结论与发现
*   **性能提升**：在相同计算预算下，DF 机制使模型在多模态推理任务上平均提升了 **3%**。
*   **缓解退化**：在 32B 大模型上，标准 GSPO 容易出现性能下滑（由于无效更新导致的灾难性遗忘），而 DF-GSPO 能够有效回收并超越基准。
*   **过程对齐**：证明了通过自动生成的差异掩码进行过程级干预，比单纯的终端奖励更能提高视觉证据的利用率。

### 7. 优点与亮点
*   **低成本扩展**：无需大规模人工过程标注，通过模型自我修复实现自动化过程监督。
*   **通用插件**：不改变模型架构或核心优化器，可无缝集成到 PPO、GRPO 等多种 RL 框架中。
*   **精准定位**：通过最小编辑原则定位错误根源，显著降低了强化学习中的梯度方差。

### 8. 不足与局限
*   **修复模型依赖**：DF 的效果高度依赖修复模型的质量。如果修复模型生成的轨迹虽然正确但逻辑跳跃过大，掩码可能会引入噪声。
*   **计算开销**：虽然在训练中通过计算量匹配进行了公平对比，但在实际操作中，修复模型需要额外的推理步骤，增加了训练流程的复杂性。
*   **语义对齐风险**：最小表面编辑（Surface Edit）并不总是等同于真正的因果错误（Causal Error），可能存在语义表达不同但逻辑正确导致的误判。

（完）
