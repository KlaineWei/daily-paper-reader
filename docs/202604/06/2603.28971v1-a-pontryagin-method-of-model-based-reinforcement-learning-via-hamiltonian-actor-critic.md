---
title: A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic
title_zh: 一种基于哈密顿 Actor-Critic 的庞特里亚金模型强化学习方法
authors: "Chengyang Gu, Yuxin Pan, Hui Xiong, Yize Chen"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28971v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于长程价值估计与规划的模型化强化学习
tldr: 本研究针对基于模型的强化学习中模型误差累积导致价值估计失效的问题，提出了一种基于庞特里亚金最大值原理（PMP）的新型算法——哈密顿行动者-评论家（HAC）。该方法通过在学习到的动力学和奖励函数上定义并直接优化哈密顿函数，消除了对显式价值函数的依赖，从而降低了对模型误差的敏感性并提供了收敛保证。实验证明，HAC在在线和离线连续控制任务中均表现优异，具有更快的收敛速度和更强的分布偏移鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28971v1/fig-001.webp\", \"caption\": \"Fig. 2: Learning Curves Comparison (Offline RL). We compare our HAC with IQL, SAC-Off and MOPO on LQR, Pendulum, and MountainCar, Swimmer. For MOPO and HAC which require multi-step imaginary rollouts, rollout horizon is to K = 10 for LQR and Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer. Results are averaged over 5 random seeds.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-002.webp\", \"caption\": \"Fig. 4: Critic Estimation Comparison: Plots comparing normalized estimated Q-values (in MVE-DDPG) and Hamiltonian (in our HAC) against ground-truth sampled from 10 random test episodes in LQR. An ideal critic should concentrates on the red unity line.\", \"page\": 18, \"index\": 2, \"width\": 1054, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-003.webp\", \"caption\": \"Fig. 1: Learning Curves Comparison (Online RL): We compare our HAC with DDPG, SAC and MVE-DDPG on Pendulum, MountainCar, Swimmer and Hopper. For MVEDDPG and HAC which require multi-step imaginary rollouts, we set the rollout horizon to K = 10 for Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer, and K = 3 for Hopper. All results are averaged over 5 random seeds.\", \"page\": 6, \"index\": 3, \"width\": 512, \"height\": 422}]"
motivation: 解决基于模型的强化学习中，由于模型误差累积导致的长时域价值估计不准确及对超参数敏感的问题。
method: 基于庞特里亚金最大值原理，提出HAC算法，通过直接优化哈密顿函数取代显式的价值函数学习。
result: 在连续控制基准测试中，HAC在收敛速度、控制性能及对分布偏移的鲁棒性上均优于模型无关和基于MVE的基准方法。
conclusion: HAC通过避免价值函数近似，有效提升了模型学习的样本效率和稳定性，为确定性系统的强化学习提供了一种高效的新方案。
---

## 摘要
基于模型的强化学习 (MBRL) 通过利用学习到的动力学模型进行策略优化，从而提高了样本效率。然而，Actor-Critic 等方法的有效性往往受到复合模型误差的限制，这些误差会降低长时限价值估计的准确性。现有方法（如基于模型的价值扩展，MVE）通过多步展开部分缓解了这一问题，但仍对展开时限的选择和残差模型偏差较为敏感。受庞特里亚金极大值原理 (PMP) 的启发，我们提出了哈密顿 Actor-Critic (HAC)。这是一种基于模型的方法，通过直接优化在确定性系统的学习动力学和奖励上定义的哈密顿量，消除了显式的价值函数学习。通过避免价值近似，HAC 降低了对模型误差的敏感性，同时提供了收敛性保证。在在线和离线强化学习设置下的连续控制基准测试中，大量实验表明，HAC 在控制性能、收敛速度以及对分布偏移（包括分布外 OOD 场景）的鲁棒性方面均优于无模型和基于 MVE 的基准方法。在数据有限的离线设置中，HAC 达到或超过了现有最先进方法的水平，突显了其强大的样本效率。

## Abstract
Model-based reinforcement learning (MBRL) improves sample efficiency by leveraging learned dynamics models for policy optimization. However, the effectiveness of methods such as actor-critic is often limited by compounding model errors, which degrade long-horizon value estimation. Existing approaches, such as Model-Based Value Expansion (MVE), partially mitigate this issue through multi-step rollouts, but remain sensitive to rollout horizon selection and residual model bias. Motivated by the Pontryagin Maximum Principle (PMP), we propose Hamiltonian Actor-Critic (HAC), a model-based approach that eliminates explicit value function learning by directly optimizing a Hamiltonian defined over the learned dynamics and reward for deterministic systems. By avoiding value approximation, HAC reduces sensitivity to model errors while admitting convergence guarantees. Extensive experiments on continuous control benchmarks, in both online and offline RL settings, demonstrate that HAC outperforms model-free and MVE-based baselines in control performance, convergence speed, and robustness to distributional shift, including out-of-distribution (OOD) scenarios. In offline settings with limited data, HAC matches or exceeds state-of-the-art methods, highlighting its strong sample efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：在基于模型的强化学习（MBRL）中，学习到的动力学模型不可避免地存在误差。这些误差在长时域展开（Rollout）时会不断累积（复合误差），导致 Critic 网络（价值函数近似器）产生严重的估计偏差，进而误导策略（Actor）的学习。
*   **研究背景**：现有的方法如 MVE（模型价值扩展）尝试通过多步预测来改善价值估计，但其性能高度依赖于展开步长 $K$ 的选择，且依然无法摆脱对显式 Critic 网络的依赖。
*   **动机**：受最优控制理论中庞特里亚金最大值原理（PMP）的启发，探索是否可以**完全舍弃显式的 Critic 网络**，直接利用动力学模型和哈密顿量（Hamiltonian）来指导策略优化。

### 2. 方法论：哈密顿行动者-评论家 (HAC)
*   **核心思想**：将强化学习目标转化为最优控制问题。根据 PMP 原理，最优控制序列必须满足哈密顿量最小化。HAC 利用学习到的动力学模型定义哈密顿量，通过计算“伴随状态”（Costate）来代替传统的价值函数梯度。
*   **关键技术细节**：
    *   **哈密顿量定义**：$H = \text{当前成本} + \text{伴随状态} \times \text{下一步状态}$。在 HAC 中，最小化哈密顿量等价于最大化累积奖励。
    *   **伴随状态递归**：通过后向递归计算伴随状态 $\lambda_t$，它捕捉了未来奖励对当前状态的敏感度。
    *   **解析雅可比技术（Analytical Jacobian）**：为了解决自动微分在处理高阶梯度和递归时的计算开销及运行错误，作者为 ReLU 激活的神经网络动力学模型推导了闭式解析雅可比矩阵，实现了无图（Graph-free）的高效计算。
    *   **算法流程**：在 Dyna 风格的框架下，智能体在真实环境收集数据更新动力学模型；随后在想象空间进行 $K$ 步展开，计算伴随状态和哈密顿量，最后通过梯度下降更新 Actor 参数。

### 3. 实验设计
*   **实验场景**：涵盖了从简单到复杂的连续控制任务，包括：
    *   基础控制：LQR（线性二次调节器）、Pendulum（倒立摆）。
    *   经典控制：MountainCar（小车上山）。
    *   复杂动力学：MuJoCo 环境中的 Swimmer（游泳者）和 Hopper（跳跃者）。
*   **Benchmark 与对比方法**：
    *   **在线 RL**：对比了 DDPG、SAC（无模型）以及 MVE-DDPG（基于模型）。
    *   **离线 RL**：对比了 IQL、SAC-Off、MOPO（最先进的离线 MBRL 方法）。
    *   **鲁棒性测试**：针对初始状态偏移（OOD 场景）进行对比。

### 4. 资源与算力
*   **硬件环境**：实验使用了 Google Colaboratory 平台，配置为 **NVIDIA T4 GPU** 和 **12 GB RAM**。
*   **模型规模**：动力学模型、Critic 和 Actor 均采用 3 层 MLP（每层 128 个神经元）。
*   **训练时长**：文中未明确给出总训练小时数，但在消融实验中对比了单次梯度步的计算耗时（HAC 约为 0.017s - 0.030s，略高于单 Critic 的 MVE，但远低于多 Critic 集成方法）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流基准环境上进行了测试，每个实验均采用 **5 个随机种子** 取平均值，并给出了标准差。
*   **充分性**：
    *   涵盖了在线和离线两种主流 RL 范式。
    *   进行了**消融实验**，探讨了集成数量对性能与计算时间的影响。
    *   提供了**理论证明的实验验证**，通过 LQR 任务展示了 HAC 的 Critic 估计误差确实比 MVE 更紧（更接近真实值）。
*   **客观性**：实验不仅展示了 HAC 的优势，也诚实地指出了在 Hopper 环境中由于“健康奖励”机制导致 HAC 收敛较慢的局限性。

### 6. 主要结论与发现
*   **性能优越**：HAC 在大多数任务中的控制性能优于或等同于现有最强基准，尤其在短时域任务中表现极佳。
*   **样本效率高**：在离线 RL 设置下，HAC 仅需约 25%-30% 的样本量即可达到与其他方法相当的性能。
*   **鲁棒性强**：在面对分布外（OOD）的初始状态偏移时，HAC 表现出比 MVE 显著更强的稳定性，不会出现轨迹发散。
*   **理论一致性**：证明了 HAC 的价值估计误差界比 MVE 更紧，且在满足 Lipschitz 连续性条件下保证收敛。

### 7. 优点与亮点
*   **架构创新**：打破了 Actor-Critic 必须依赖 Critic 网络的思维定式，利用控制理论中的哈密顿量实现了“无 Critic”的策略优化。
*   **计算优化**：解析雅可比技术的引入解决了 MBRL 中常见的计算效率瓶颈，具有很强的工程实用价值。
*   **理论支撑**：不仅有算法创新，还提供了收敛性证明和误差界分析，使算法更具解释性。

### 8. 不足与局限
*   **系统限制**：目前的方法主要针对**确定性动力学系统**设计。虽然附录讨论了随机系统的扩展，但核心实验尚未全面覆盖高度随机的环境。
*   **奖励函数依赖**：HAC 需要奖励函数是可微的（如二次型），对于不可微或稀疏的阶跃奖励（如 Hopper 的生存奖励）处理效果受限。
*   **计算复杂度**：虽然比集成方法快，但由于需要后向递归计算伴随状态，其单步更新耗时仍高于基础的 MVE。

（完）
