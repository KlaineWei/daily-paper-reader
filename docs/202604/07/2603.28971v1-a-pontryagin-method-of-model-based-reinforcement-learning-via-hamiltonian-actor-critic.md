---
title: A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic
title_zh: 一种基于哈密顿 Actor-Critic 的模型强化学习庞特里亚金方法
authors: "Chengyang Gu, Yuxin Pan, Hui Xiong, Yize Chen"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28971v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于长程价值估计和策略优化的模型强化学习方法
tldr: 针对基于模型的强化学习中长时界价值估计受模型误差累积影响的问题，本文受庞特里亚金极大值原理启发，提出了哈密顿量演员-评论家（HAC）算法。该方法通过直接优化定义在动力学和奖励函数上的哈密顿量，避免了显式的价值函数学习，从而降低了对模型误差的敏感性。实验表明，HAC在连续控制任务的在线和离线设置中均优于现有基准，具有更强的鲁棒性和样本效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28971v1/fig-001.webp\", \"caption\": \"Fig. 2: Learning Curves Comparison (Offline RL). We compare our HAC with IQL, SAC-Off and MOPO on LQR, Pendulum, and MountainCar, Swimmer. For MOPO and HAC which require multi-step imaginary rollouts, rollout horizon is to K = 10 for LQR and Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer. Results are averaged over 5 random seeds.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-002.webp\", \"caption\": \"Fig. 4: Critic Estimation Comparison: Plots comparing normalized estimated Q-values (in MVE-DDPG) and Hamiltonian (in our HAC) against ground-truth sampled from 10 random test episodes in LQR. An ideal critic should concentrates on the red unity line.\", \"page\": 18, \"index\": 2, \"width\": 1054, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-003.webp\", \"caption\": \"Fig. 1: Learning Curves Comparison (Online RL): We compare our HAC with DDPG, SAC and MVE-DDPG on Pendulum, MountainCar, Swimmer and Hopper. For MVEDDPG and HAC which require multi-step imaginary rollouts, we set the rollout horizon to K = 10 for Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer, and K = 3 for Hopper. All results are averaged over 5 random seeds.\", \"page\": 6, \"index\": 3, \"width\": 512, \"height\": 422}]"
motivation: 解决基于模型的强化学习中由于模型误差累积导致的长时界价值估计不准确及对展开步数敏感的问题。
method: 提出HAC算法，利用庞特里亚金极大值原理直接优化哈密顿量，从而在确定性系统中省去显式的价值函数近似。
result: 在多个连续控制基准测试中，HAC在控制性能、收敛速度以及对分布偏移的鲁棒性方面均优于模型无关和基于MVE的方法。
conclusion: HAC通过结合最优控制理论与强化学习，为提高模型驱动强化学习的样本效率和稳健性提供了一种无需价值函数的新路径。
---

## 摘要
基于模型的强化学习（MBRL）通过利用学习到的动力学模型进行策略优化来提高样本效率。然而，Actor-Critic 等方法的有效性往往受到复合模型误差的限制，这会降低长时域价值估计的质量。现有方法（如基于模型的价值扩展 MVE）通过多步展开部分缓解了这一问题，但仍对展开时域的选择和残余模型偏差敏感。受庞特里亚金极大值原理（PMP）的启发，我们提出了哈密顿 Actor-Critic（HAC），这是一种基于模型的方法，通过直接优化在确定性系统的学习动力学和奖励上定义的哈密顿量，消除了显式的价值函数学习。通过避免价值近似，HAC 降低了对模型误差的敏感性，同时具备收敛性保证。在在线和离线强化学习设置下的连续控制基准测试中，大量实验表明，HAC 在控制性能、收敛速度以及对分布偏移（包括分布外 OOD 场景）的鲁棒性方面均优于无模型和基于 MVE 的基准方法。在数据有限的离线设置中，HAC 达到或超过了最先进的方法，突显了其强大的样本效率。

## Abstract
Model-based reinforcement learning (MBRL) improves sample efficiency by leveraging learned dynamics models for policy optimization. However, the effectiveness of methods such as actor-critic is often limited by compounding model errors, which degrade long-horizon value estimation. Existing approaches, such as Model-Based Value Expansion (MVE), partially mitigate this issue through multi-step rollouts, but remain sensitive to rollout horizon selection and residual model bias. Motivated by the Pontryagin Maximum Principle (PMP), we propose Hamiltonian Actor-Critic (HAC), a model-based approach that eliminates explicit value function learning by directly optimizing a Hamiltonian defined over the learned dynamics and reward for deterministic systems. By avoiding value approximation, HAC reduces sensitivity to model errors while admitting convergence guarantees. Extensive experiments on continuous control benchmarks, in both online and offline RL settings, demonstrate that HAC outperforms model-free and MVE-based baselines in control performance, convergence speed, and robustness to distributional shift, including out-of-distribution (OOD) scenarios. In offline settings with limited data, HAC matches or exceeds state-of-the-art methods, highlighting its strong sample efficiency.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：Hamiltonian Actor-Critic (HAC)

#### 1. 核心问题与研究动机
*   **核心问题**：在基于模型的强化学习（MBRL）中，如何解决由于动力学模型误差随时间步累积（Compounding Errors）导致的价值函数（Critic）估计不准，进而引发策略优化失效的问题。
*   **研究背景**：传统的 Dyna 风格 MBRL（如 MVE）依赖于学习一个显式的价值函数。然而，当模型不完美时，长程展开（Long-horizon rollouts）会引入巨大的偏差，使得 Critic 难以收敛，且算法对展开步数 $K$ 的选择极其敏感。

#### 2. 方法论：哈密顿 Actor-Critic (HAC)
*   **核心思想**：受最优控制理论中的**庞特里亚金极大值原理（PMP）**启发，HAC 提出在确定性系统中**完全取消显式的价值函数（Critic 网络）学习**。它直接定义并优化一个“哈密顿量”（Hamiltonian），将其作为策略改进的代理目标。
*   **关键技术细节**：
    *   **哈密顿量定义**：$H(s_t, a_t, \lambda_{t+1}) = c(s_t, a_t) + \lambda_{t+1}^\top f(s_t, a_t)$，其中 $c$ 是代价函数，$f$ 是动力学模型，$\lambda$ 是协态变量（Costate）。
    *   **协态递归计算**：通过反向传播计算协态变量 $\lambda_t = \nabla_{s_t} H$，这相当于将终端奖励的梯度沿轨迹反向传播。
    *   **解析雅可比技术（Analytical Jacobian）**：为了避免 PyTorch 自动微分在处理高阶梯度时的计算冗余和运行错误，作者为 ReLU 激活的神经网络动力学模型推导了闭式雅可比矩阵计算公式，实现了无图（Graph-free）的高效计算。
    *   **算法流程**：在每一轮迭代中，利用当前策略和动力学模型生成 $K$ 步虚拟轨迹，计算各步的协态变量和哈密顿量，最后通过最小化哈密顿量来更新 Actor 网络参数。

#### 3. 实验设计
*   **实验场景**：涵盖了从简单到复杂的连续控制任务：
    *   基础控制：LQR（线性二次调节器）、Pendulum（倒立摆）、MountainCar（小车上山）。
    *   复杂动力学：MuJoCo 环境中的 Swimmer（游泳者）和 Hopper（跳跃者）。
*   **Benchmark 与对比方法**：
    *   **在线 RL**：对比了 DDPG、SAC（无模型基准）和 MVE-DDPG（基于模型的价值扩展基准）。
    *   **离线 RL**：对比了 IQL、SAC-Off（无模型离线）和 MOPO（最先进的基于模型的离线方法）。
    *   **鲁棒性测试**：针对初始状态偏移（Initial State Shift）的分布外（OOD）场景进行测试。

#### 4. 资源与算力
*   **硬件配置**：论文明确提到使用了 **Google Colaboratory** 实例，配备 **1 块 NVIDIA T4 GPU** 和 **12 GB RAM**。
*   **模型规模**：动力学模型、Critic 和 Actor 均采用简单的 3 层 MLP（每层 128 个神经元）。
*   **训练时长**：未明确给出总训练小时数，但在消融实验中对比了单步梯度更新的时间（HAC 约为 0.017s - 0.030s，略高于单 Critic 的 MVE，但远低于多 Critic 集合方法）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个不同复杂度的环境上进行了在线和离线对比。
    *   所有实验均基于 **5 个随机种子**取平均值，并给出了标准差。
    *   包含针对 Critic 估计误差的定量分析（LQR 环境下的可视化）。
    *   包含针对 Critic 集合数量（Ensemble size）的消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了在线/离线、简单/复杂、分布内/分布外等多个维度。通过 LQR 的解析解对比，客观证明了哈密顿量估计比 Q 函数估计更接近真实值。

#### 6. 主要结论与发现
*   **性能优越性**：HAC 在大多数任务中实现了比 MVE 更高的累积奖励和更快的收敛速度。
*   **样本效率**：在离线设置下，HAC 仅需约 25%-30% 的样本量即可达到与其他基准相当的性能。
*   **误差界限**：理论证明并实验验证了 HAC 的哈密顿量估计误差界限比 MVE 的价值估计误差界限更紧（Tighter Bound）。
*   **鲁棒性**：在面对初始状态偏移时，HAC 表现出比 MVE-DDPG 强得多的稳定性，证明了其对模型偏差的敏感度较低。

#### 7. 优点与亮点
*   **理论扎实**：将经典的最优控制理论（PMP）与现代深度强化学习结合，提供了严谨的收敛性证明。
*   **架构简洁**：通过消除 Critic 网络，简化了 Actor-Critic 架构，减少了超参数（如 Critic 学习率、目标网络更新频率等）的调优负担。
*   **计算创新**：提出的解析雅可比技术解决了深度学习框架在处理递归梯度时的效率瓶颈。

#### 8. 不足与局限
*   **系统限制**：目前的方法主要针对**确定性动力学系统**设计。虽然附录中讨论了随机系统的扩展可能性，但主实验并未覆盖随机环境。
*   **长程偏差**：在 Hopper 等具有复杂终止条件（Healthy Reward）的任务中，当展开步数 $K$ 远小于实际任务时限 $T$ 时，HAC 的表现会受到协态变量估计不准的影响。
*   **计算开销**：虽然省去了 Critic 网络，但反向递归计算协态变量的计算量仍高于简单的单步价值函数前向传播。

（完）
