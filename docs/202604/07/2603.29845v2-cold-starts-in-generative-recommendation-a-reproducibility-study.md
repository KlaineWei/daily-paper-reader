---
title: "Cold-Starts in Generative Recommendation: A Reproducibility Study"
title_zh: 生成式推荐中的冷启动：一项可复现性研究
authors: "Zhen Zhang, Jujia Zhao, Xinyu Ma, Xin Xin, Maarten de Rijke, Zhaochun Ren"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29845v2"
tags: ["query:llmrec"]
score: 9.0
evidence: 使用预训练语言模型的生成式推荐中的冷启动挑战
tldr: 本研究针对生成式推荐系统在冷启动场景（新用户和新物品）下的表现进行了系统的可复现性研究。虽然基于预训练语言模型的生成式推荐器被认为能利用语义信息缓解冷启动问题，但现有研究缺乏统一的评估标准，且模型设计选择（如规模、标识符设计）的影响难以区分。本文通过统一的评估协议，深入分析了不同设计决策对冷启动性能的影响，为构建更鲁棒的生成式推荐系统提供了实证依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29845v2/fig-001.webp\", \"caption\": \"Table 4: Item cold-start recommendation performance comparison. Performance is measured using Recall@10 (R@10) and NDCG@10 (N@10), with the best and second-best results in each category bolded and underlined, respectively. The best performing models achieve significant improvements over the other baselines (paired t-test, p<0.05).\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2603.29845v2/fig-002.webp\", \"caption\": \"Table 3: User cold-start recommendation performance comparison. Performance is measured using Recall@10 (R@10) and NDCG@10 (N@10), with the best and second-best results in each category bolded and underlined, respectively. The best performing models achieve significant improvements over the other baselines (paired t-test, p<0.05).\", \"page\": 6, \"index\": 2, \"width\": 976, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2603.29845v2/fig-003.webp\", \"caption\": \"Figure 1: Generative recommendation pipeline.\", \"page\": 2, \"index\": 3, \"width\": 482, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2603.29845v2/fig-004.webp\", \"caption\": \"Figure 2: Comparison of recall performance under warmstart and cold-start conditions across different model scales. The plot shows Recall@10 for both item and user cold-start settings as model size increases, using representative generative recommender methods (TIGER) with different variants of Flan-T5.\", \"page\": 7, \"index\": 4, \"width\": 494, \"height\": 556}, {\"url\": \"assets/figures/arxiv/2603.29845v2/fig-005.webp\", \"caption\": \"Figure 3: Performance comparison across different identifier designs. This figure illustrates the Recall@10 performance under warm-start, item cold-start, and user cold-start conditions for various identifier types: Atomic IDs, Textual Titles, and Semantic Codes (RQ-VAE, Balanced k-means, and OPQ).\", \"page\": 8, \"index\": 5, \"width\": 493, \"height\": 673}]"
motivation: 现有生成式推荐系统在冷启动场景下的评估缺乏统一标准，且关键设计选择对性能的贡献难以解释。
method: 在统一的冷启动评估协议下，对基于预训练语言模型的生成式推荐系统进行系统的可复现性研究。
result: 揭示了模型规模、标识符设计和训练策略等关键因素在缓解用户和物品冷启动问题中的具体作用。
conclusion: 该研究为生成式推荐系统在冷启动环境下的设计提供了清晰的指导，并强调了标准化评估的重要性。
---

## 摘要
冷启动推荐在动态、开放世界平台中仍然是一个核心挑战，要求模型在交互信号稀疏或缺失的情况下，为新注册用户进行推荐（用户冷启动），以及向现有用户推荐新引入的物品（物品冷启动）。最近基于预训练语言模型（PLM）构建的生成式推荐系统，通常被期望通过利用物品语义信息（如标题和描述）以及在测试时基于有限的用户上下文进行条件化处理，来缓解冷启动问题。然而，在现有研究中，冷启动很少被视为主要的评估设置，且由于模型规模、标识符设计和训练策略等关键设计选择经常被同时更改，所报道的性能提升难以解释。在这项工作中，我们在一套统一的冷启动协议下，对生成式推荐进行了系统的可复现性研究。

## Abstract
Cold-start recommendation remains a central challenge in dynamic, open-world platforms, requiring models to recommend for newly registered users (user cold-start) and to recommend newly introduced items to existing users (item cold-start) under sparse or missing interaction signals. Recent generative recommenders built on pre-trained language models (PLMs) are often expected to mitigate cold-start by using item semantic information (e.g., titles and descriptions) and test-time conditioning on limited user context. However, cold-start is rarely treated as a primary evaluation setting in existing studies, and reported gains are difficult to interpret because key design choices, such as model scale, identifier design, and training strategy, are frequently changed together. In this work, we present a systematic reproducibility study of generative recommendation under a unified suite of cold-start protocols.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cold-Starts in Generative Recommendation: A Reproducibility Study》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：探讨基于预训练语言模型（PLM）的生成式推荐系统（Generative Recommendation）在**用户冷启动**和**物品冷启动**场景下的真实表现。
*   **研究动机**：尽管生成式推荐被寄予厚望能通过语义信息缓解冷启动，但现有研究通常将冷启动作为次要评估指标，且模型规模、标识符（Identifier）设计、训练策略等多个变量往往被同时改变，导致无法准确归因性能提升的来源。该研究旨在通过统一的评估协议，隔离并量化这些关键设计维度对冷启动性能的影响。

### 2. 方法论与核心思想
该论文并非提出一种新算法，而是建立了一个**统一的可复现性研究框架**，重点分析以下三个维度：
*   **模型规模（Model Scale）**：研究增加 PLM 参数量（如从 Flan-T5 Small 到 XL）是否能自动弥合冷启动差距。
*   **标识符设计（Identifier Design）**：对比了三种主流 ID 类型：
    *   **原子 ID（Atomic IDs）**：无语义的随机数字或索引。
    *   **文本标识符（Textual IDs）**：使用物品标题等文本信息。
    *   **语义代码（Semantic Codes）**：通过量化方法（如 RQ-VAE、Balanced K-Means、OPQ）生成的结构化离散代码。
*   **训练策略（Training Strategy）**：对比了仅使用**监督微调（SFT）**与在 SFT 基础上增加**强化学习（RL）**优化排序目标的效果。
*   **核心流程**：将推荐任务建模为条件生成任务，给定用户历史 $H_u$，模型自回归生成目标物品的标识符序列 $y_{k+1}$，并在推理时使用前缀树（Trie）进行约束解码。

### 3. 实验设计
*   **数据集**：使用了三个具有丰富元数据的公开基准数据集：**Amazon-Toys**（电商）、**MicroLens**（短视频）、**Steam**（游戏）。
*   **评估场景**：
    *   **Warm-start**：训练集中出现过的用户和物品。
    *   **User Cold-start**：新用户，仅提供 1-10 条交互作为上下文。
    *   **Item Cold-start**：新物品，其标识符在训练阶段未被优化过。
*   **对比方法（Baselines）**：
    *   **传统模型**：SASRec, GRU4Rec。
    *   **生成式模型**：P5, GenRec, TIGER, LC-Rec, OneRec, DiffGRM 等。

### 4. 资源与算力
*   **硬件环境**：实验在 **8 台 NVIDIA A800 GPU** 上进行。
*   **训练细节**：采用了分布式训练以适配大规模参数模型。
*   **说明**：文中提到了 GPU 型号和数量，但未详细列出每个模型的具体训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集、3 种评估协议（Warm/User-Cold/Item-Cold）、4 种模型规模、5 种标识符设计以及 2 种训练策略。
*   **充分性与公平性**：
    *   **充分性**：实验设计非常全面，通过控制变量法逐一验证了研究问题（RQ1-RQ4）。
    *   **客观性**：使用了官方实现并遵循原始论文的超参数设置，确保了对比的公平性。
    *   **统计显著性**：对核心结果进行了配对 t 检验（p < 0.05）。

### 6. 主要结论与发现
*   **冷启动的不对称性**：物品冷启动比用户冷启动挑战大得多。现有模型在用户冷启动下表现尚可，但在物品冷启动下往往出现性能骤降。
*   **规模效应有限**：增加模型规模虽有提升，但边际效应递减，无法从根本上解决冷启动带来的分布偏移问题。
*   **标识符的权衡（Trade-off）**：
    *   **文本标识符**能显著提升物品冷启动性能，但会严重损害 Warm-start 和用户冷启动的表现（因语义歧义和区分度不足）。
    *   **语义代码**在 Warm 和用户冷启动中更稳健，但在物品冷启动中表现不稳定。
    *   **OPQ（优化乘积量化）**等组合编码方案在保持 Warm 性能的同时，对物品冷启动表现出更好的鲁棒性。
*   **强化学习的局限**：在冷启动场景下，RL 并不总能带来提升，甚至可能因为过度拟合训练分布而降低模型的泛化能力。

### 7. 优点与亮点
*   **系统性归因**：首次系统性地隔离了生成式推荐中多个混淆的设计因素，提供了清晰的实证指导。
*   **可复现性贡献**：提供了统一的代码库和预处理脚本，解决了生成式推荐领域评估标准不一的问题。
*   **深入的洞察**：揭示了标识符设计中“语义泛化”与“唯一区分度”之间的内在矛盾。

### 8. 不足与局限
*   **场景覆盖**：主要关注文本元数据，未深入探讨多模态信息（如视频、图像特征）在生成式冷启动中的作用。
*   **理论深度**：虽然观察到了不同量化方案的影响，但缺乏对量化结构如何影响解码稳定性的深层理论分析。
*   **动态性限制**：实验环境仍基于静态数据集切分，未能完全模拟现实中物品池快速更迭的极端动态环境。

（完）
