---
title: LLM-based Listwise Reranking under the Effect of Positional Bias
title_zh: 位置偏置影响下的基于大语言模型的列表式重排序
authors: "Jingfen Qiao, Jin Huang, Xinyu Ma, Shuaiqiang Wang, Dawei Yin, Evangelos Kanoulas, Andrew Yates"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03642v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 基于LLM的列表重排序及位置偏差缓解
tldr: 本研究针对基于大语言模型（LLM）的列表重排序中存在的位置偏置问题，即模型倾向于忽略输入列表末尾的相关文档。研究分析发现该偏置源于模型架构和训练数据分布不均，并据此提出了DebiasFirst方法。该方法通过逆倾向评分进行位置校准，并结合位置感知的数据增强技术，在微调阶段显著提升了重排序的有效性与鲁棒性，有效缓解了位置偏置对检索性能的影响。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03642v1/fig-001.webp\", \"caption\": \"Fig. 6: Comparison of positional calibration with IPS and Pos-Aug on the MS MARCO Dev Set. All models are evaluated using NDCG@10.\", \"page\": 11, \"index\": 1, \"width\": 726, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.03642v1/fig-002.webp\", \"caption\": \"Fig. 5: Evaluating the complementary effects of inference-Stage (PermSC approach [34]) and tuning-stage (our approach) debiasing in LLM-Based listwise passage reranking; Bars represent the performance on each shuffled ordering; Lines represent the aggregated performance using PermSC rank aggregation.\", \"page\": 10, \"index\": 2, \"width\": 725, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2604.03642v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of the proposed positional calibration using IPS. Each document’s relevance score fθ(xi) is calibrated by multiplying it with estimated inverse propensity values πq(xi) to account for positional bias. The heatmap on the right visualizes the estimated inverse propensities across input and output positions.\", \"page\": 5, \"index\": 3, \"width\": 725, \"height\": 248}]"
motivation: 解决LLM在列表重排序中因模型架构和数据分布不均导致的位置偏置问题，即输入末尾的相关文档难以被排至前列。
method: 提出DebiasFirst方法，通过逆倾向评分（IPS）在损失函数中进行位置校准，并利用位置感知数据增强确保文档在训练中均匀分布。
result: "实验表明该方法显著提升了NDCG@10指标，增强了模型对初始排名顺序的鲁棒性，并减少了性能对文档位置的依赖。"
conclusion: DebiasFirst为缓解LLM重排序位置偏置提供了实用的微调方案，且能与推理阶段的去偏方法有效互补。
---

## 摘要
基于大语言模型（LLM）的列表式段落重排序因其在候选段落排序中的有效性而备受关注。然而，这些模型受到位置偏置的影响，即位于输入末尾的段落被移动到排序前列的可能性较低。我们假设位置偏置主要有两个来源：(1) LLM 固有的架构偏置，以及 (2) 相关文档位置分布的不平衡。为解决这一问题，我们提出了 DebiasFirst，这是一种在微调过程中整合了位置校准和位置感知数据增强的方法。位置校准利用逆倾向评分，通过在训练时重新加权损失函数中不同位置的贡献来调整位置偏置。位置感知增强通过扩充训练数据，确保每个段落在输入列表的不同位置中出现的概率均等。该方法显著增强了在不同初筛检索器下的有效性以及对原始排序的鲁棒性，降低了 NDCG@10 性能对相关文档位置的依赖。DebiasFirst 还与推理阶段的去偏方法互补，为缓解重排序中的位置偏置提供了一种实用的解决方案。

## Abstract
LLM-based listwise passage reranking has attracted attention for its effectiveness in ranking candidate passages. However, these models suffer from positional bias, where passages positioned towards the end of the input are less likely to be moved to top positions in the ranking. We hypothesize that there are two primary sources of positional bias: (1) architectural bias inherent in LLMs and (2) the imbalanced positioning of relevant documents. To address this, we propose DebiasFirst, a method that integrates positional calibration and position-aware data augmentation during fine-tuning. Positional calibration uses inverse propensity scoring to adjust for positional bias by re-weighting the contributions of different positions in the loss function when training. Position-aware augmentation augments training data to ensure that each passage appears equally across varied positions in the input list. This approach markedly enhances both effectiveness and robustness to the original ranking across diverse first-stage retrievers, reducing the dependence of NDCG@10 performance on the position of relevant documents. DebiasFirst also complements the inference-stage debiasing methods, offering a practical solution for mitigating positional bias in reranking.

---

## 论文详细总结（自动生成）

以下是对论文《LLM-based Listwise Reranking under the Effect of Positional Bias》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：基于大语言模型（LLM）的列表式重排序（Listwise Reranking）存在严重的**位置偏置（Positional Bias）**。即模型倾向于优先选择输入列表前端的文档，而位于输入末尾的相关文档很难被排到前列。
*   **研究动机**：
    *   **架构偏置**：LLM 的注意力机制（如 RoPE 相对位置编码）随距离衰减，且序列开头的 Token 往往获得不成比例的高注意力权重。
    *   **数据偏置**：现有的微调数据集（如 MS MARCO）中，相关文档在输入列表中的分布极不平衡（通常集中在顶部），导致模型在微调过程中习得了这种位置依赖。
    *   **可靠性需求**：为了使重排序结果不随输入顺序的改变而剧烈波动，需要一种在微调阶段就能消除偏置的方法。

### 2. 论文提出的方法论：DebiasFirst
该方法通过在微调阶段引入两个核心组件来缓解偏置：
*   **基于逆倾向评分（IPS）的位置校准**：
    *   **核心思想**：借鉴去偏排序（Unbiased LTR）的思想，通过调整损失函数中不同位置的权重来抵消偏置。
    *   **技术细节**：首先通过随机洗牌估计“倾向值”（Propensity），即特定输入位置到输出位置的转换频率。在训练时，对 RankNet 损失函数进行加权：如果某种位置转换在数据中过度代表，则降低其权重；反之则提高权重。
*   **位置感知数据增强（Pos-Aug）**：
    *   **核心思想**：通过构造训练样本，确保每个段落在输入列表的各个位置上出现的概率均等。
    *   **流程**：采用 Fisher-Yates 洗牌算法生成无偏排列，并结合分组旋转（Rotation）策略，使相关文档均匀分布在输入序列的 1 到 20 位。
*   **联合训练目标**：结合了校准后的排序损失（LRank-IPS）和标准的语言模型生成损失（LLM Loss），并使用单 Token 解码（Single Token Decoding）以提高推理效率。

### 3. 实验设计
*   **数据集与场景**：
    *   **域内（In-domain）**：MS MARCO Dev、TREC DL 2019、TREC DL 2020。
    *   **跨域（Out-of-domain）**：BEIR 基准测试（包含 FiQA、HotpotQA、NQ 等 9 个子集）。
*   **初筛检索器（First-stage Retrievers）**：BM25、Contriever、Splade++ 以及 RRF 融合结果。
*   **对比方法（Benchmarks）**：
    *   **微调模型**：RankZephyr、First、ListT5。
    *   **推理阶段去偏方法**：PermSC（通过多次推理并聚合结果来去偏）。

### 4. 资源与算力
*   **模型基础**：使用 Zephyr-7B-β（基于 Mistral-7B 架构）。
*   **硬件资源**：使用了 **4 台 40GB Nvidia A100 GPU**。
*   **训练细节**：采用 DeepSpeed 优化，微调 3 个 Epoch，有效 Batch Size 为 8，学习率为 5e-6。
*   **训练时长**：整个微调过程耗时约 **7 小时**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **受控位置实验**：系统性地将相关文档放置在 1-20 的每个位置进行测试，直观展示了性能随位置变化的曲线。
    *   **多检索器测试**：在 4 种不同质量的初筛结果上验证了鲁棒性。
    *   **消融实验**：分别验证了 IPS、Pos-Aug 以及 LM Loss 对最终性能和方差的影响。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了整体指标（NDCG@10），还通过“洗牌实验”验证了模型在极端不利输入顺序下的稳定性，并探讨了微调去偏与推理去偏的互补性。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：DebiasFirst 在相关文档处于输入列表末尾时，性能远超 RankZephyr 和 First 等基线模型。
*   **整体性能优化**：在原始排序（Original Order）下，DebiasFirst 在多数数据集上实现了 2%-4% 的 NDCG@10 提升；在随机洗牌顺序下，优势更加明显。
*   **去偏阶段对比**：微调阶段的去偏（DebiasFirst）比推理阶段的聚合去偏（PermSC）更高效且效果更好，且两者结合能进一步提升性能。
*   **缓解“迷失中段”**：有效解决了 LLM 在处理长上下文时忽略中间或末尾信息的顽疾。

### 7. 优点与亮点
*   **因果分析驱动**：论文先通过因果图分析偏置来源，再针对性提出方案，逻辑严密。
*   **效率与效果兼顾**：采用单 Token 解码保证了推理速度，同时通过 IPS 解决了微调中的分布不均问题。
*   **即插即用**：该方法不改变模型架构，仅通过数据增强和损失函数调整实现，具有较强的通用性。

### 8. 不足与局限
*   **窗口大小限制**：实验主要集中在窗口大小为 20 的场景，对于更长上下文（如一次性重排序 100 个文档）的有效性尚未验证。
*   **性能权衡**：实验观察到，为了实现全位置的公平性，DebiasFirst 在相关文档恰好处于输入第 1 位时的表现偶尔略逊于某些基线模型（这是一种“为了整体鲁棒性而牺牲局部极端性能”的权衡）。
*   **依赖高质量初筛**：虽然对不同检索器有鲁棒性，但重排序的上限仍受限于初筛召回的文档质量。

（完）
