---
title: Fusion and Alignment Enhancement with Large Language Models for Tail-item Sequential Recommendation
title_zh: 基于大语言模型的长尾物品序列推荐融合与对齐增强框架
authors: "Zhifu Wei, Yizhou Dang, Guibing Guo, Chuang Zhao, Zhu Sun"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03688v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 基于大语言模型的长尾项目序列推荐
tldr: 针对序列推荐中的长尾物品问题，本文提出FAERec框架，利用大语言模型（LLM）增强物品表示。该框架通过自适应门控机制动态融合ID与LLM语义嵌入，并设计包含物品级对比学习与特征级相关性约束的双层对齐策略，以解决嵌入空间结构不一致问题。结合课程学习优化训练过程，FAERec在多个基准模型上显著提升了长尾物品的推荐准确性与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03688v1/fig-001.webp\", \"caption\": \"Table 2: Performance comparison of different methods on Backbone models. The best performance is bolded. The ‘Impr.’ represents the relative improvement of FAERec over the best baseline method.\", \"page\": 6, \"index\": 1, \"width\": 1058, \"height\": 789}, {\"url\": \"assets/figures/arxiv/2604.03688v1/fig-002.webp\", \"caption\": \"Table 3: The ablation study results on the Beauty dataset with three sequential recommendation backbones.\", \"page\": 7, \"index\": 2, \"width\": 1002, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.03688v1/fig-003.webp\", \"caption\": \"Figure 4: The group analysis results on three dataset with SASRec as the backbone.\", \"page\": 8, \"index\": 3, \"width\": 1018, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2604.03688v1/fig-004.webp\", \"caption\": \"Table 4: Results on more sequential recommendation backbones.\", \"page\": 8, \"index\": 4, \"width\": 994, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2604.03688v1/fig-005.webp\", \"caption\": \"Figure 2: The overall framework of our proposed FAERec.\", \"page\": 4, \"index\": 5, \"width\": 1042, \"height\": 509}, {\"url\": \"assets/figures/arxiv/2604.03688v1/fig-006.webp\", \"caption\": \"Figure 5: Visualization of ID and LLM embedding spaces on the Beauty dataset with SASRec as the backbone.\", \"page\": 9, \"index\": 6, \"width\": 489, \"height\": 474}]"
motivation: 现有方法在融合协作信号与语义知识时效果不佳，且ID与LLM嵌入空间存在结构不一致，导致长尾物品表征质量受限。
method: 提出FAERec框架，采用自适应门控融合机制及双层对齐方法（物品级与特征级），并引入课程学习调度器优化对齐权重。
result: 在三个主流数据集上的实验证明，该框架能有效集成到多种序列推荐模型中，并显著优于现有的长尾推荐增强方法。
conclusion: 通过有效融合LLM语义信息并解决空间对齐挑战，FAERec显著增强了长尾物品的推荐性能，为缓解推荐系统数据稀疏性提供了新方案。
---

## 摘要
序列推荐（SR）通过用户的历史交互序列学习其偏好并提供个性化建议。在现实场景中，大多数物品的交互非常稀疏，即长尾物品问题。这一问题限制了模型准确捕捉物品转移模式的能力。为了解决这一挑战，大语言模型（LLMs）通过捕捉物品间的语义关系提供了一种极具前景的方案。尽管先前已有研究利用 LLM 生成的嵌入来丰富长尾物品的表示，但仍面临以下局限性：1）难以有效融合协同信号与语义知识，导致物品嵌入质量欠佳；2）现有方法忽视了 ID 嵌入空间与 LLM 嵌入空间之间的结构不一致性，导致信号冲突并降低推荐准确性。在这项工作中，我们提出了一种基于大语言模型的长尾物品序列推荐融合与对齐增强框架（FAERec），通过生成连贯融合且结构一致的嵌入来改进物品表示。针对信息融合挑战，我们设计了一种自适应门控机制来动态融合 ID 嵌入和 LLM 嵌入。随后，我们提出了一种双层对齐方法来缓解结构不一致性。物品级对齐通过对比学习建立同一物品的 ID 嵌入与 LLM 嵌入之间的对应关系，而特征级对齐则约束两个嵌入空间中对应维度之间的相关性模式。此外，通过课程学习调度器调整这两个对齐任务的权重，以避免对复杂的特征级目标进行过早优化。在三个广泛使用的数据集上，结合多种代表性序列推荐骨干网络进行的大量实验证明了我们框架的有效性和泛化性。

## Abstract
Sequential Recommendation (SR) learns user preferences from their historical interaction sequences and provides personalized suggestions. In real-world scenarios, most items exhibit sparse interactions, known as the tail-item problem. This issue limits the model's ability to accurately capture item transition patterns. To tackle this, large language models (LLMs) offer a promising solution by capturing semantic relationships between items. Despite previous efforts to leverage LLM-derived embeddings for enriching tail items, they still face the following limitations: 1) They struggle to effectively fuse collaborative signals with semantic knowledge, leading to suboptimal item embedding quality. 2) Existing methods overlook the structural inconsistency between the ID and LLM embedding spaces, causing conflicting signals that degrade recommendation accuracy. In this work, we propose a Fusion and Alignment Enhancement framework with LLMs for Tail-item Sequential Recommendation (FAERec), which improves item representations by generating coherently-fused and structurally consistent embeddings. For the information fusion challenge, we design an adaptive gating mechanism that dynamically fuses ID and LLM embeddings. Then, we propose a dual-level alignment approach to mitigate structural inconsistency. The item-level alignment establishes correspondences between ID and LLM embeddings of the same item through contrastive learning, while the feature-level alignment constrains the correlation patterns between corresponding dimensions across the two embedding spaces. Furthermore, the weights of the two alignments are adjusted by a curriculum learning scheduler to avoid premature optimization of the complex feature-level objective. Extensive experiments across three widely used datasets with multiple representative SR backbones demonstrate the effectiveness and generalizability of our framework.

---

## 论文详细总结（自动生成）

以下是对论文《Fusion and Alignment Enhancement with Large Language Models for Tail-item Sequential Recommendation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：序列推荐（SR）中普遍存在的**长尾物品（Tail-item）问题**。
*   **研究背景**：传统的基于 ID 的推荐模型高度依赖用户-物品交互数据。长尾物品由于交互稀疏，模型难以捕捉其转移模式。
*   **研究动机**：虽然大语言模型（LLM）能提供丰富的语义信息来增强长尾物品表征，但现有方法存在两大缺陷：
    1.  **语义利用不足**：现有方法多将 LLM 嵌入作为辅助信号（如初始化或知识蒸馏），在最终表征中未能充分保留语义知识。
    2.  **空间结构不一致**：ID 嵌入空间（捕捉协作信号）与 LLM 嵌入空间（捕捉语义属性）在维度编码和物品邻域结构上存在冲突，直接融合会产生噪声，降低推荐精度。

### 2. 论文提出的方法论：FAERec 框架
FAERec 旨在通过“融合”与“对齐”双重增强来提升长尾物品的表征质量。
*   **自适应门控融合（Adaptive Gating Fusion, AGF）**：
    *   利用 LLM（如 OpenAI API）提取物品属性的语义嵌入，并通过 MLP 进行维度投影。
    *   设计**维度级门控机制**，根据物品特征动态计算 ID 嵌入与 LLM 嵌入的融合权重，实现协作信号与语义知识的精细化互补。
*   **双层对齐（Dual-Level Alignment）**：
    *   **物品级对齐（ILA）**：采用对比学习（Contrastive Learning），将同一物品的 ID 嵌入和 LLM 嵌入视为正样本，最大化其相似度，建立点对点的对应关系。
    *   **特征级对齐（FLA）**：借鉴 Barlow Twins 思想，计算两个嵌入空间的互相关矩阵。通过约束对角线元素接近 1（增强对应维度相关性）和非对角线元素接近 0（减少冗余），实现空间结构的整体一致性。
    *   **流行度分组策略**：在 FLA 中根据物品流行度进行分组计算，防止头部物品主导标准化过程，确保长尾物品的对齐效果。
*   **课程学习调度器（Curriculum Learning Scheduler, CLS）**：
    *   引入余弦退火调度器动态调整 ILA 和 FLA 的权重。初期侧重简单的物品级对齐，后期逐渐引入复杂的特征级结构约束，避免训练初期的噪声干扰。

### 3. 实验设计
*   **数据集**：Amazon Beauty、Amazon Grocery、Yelp（涵盖电商与生活服务场景）。
*   **骨干网络（Backbones）**：SASRec（Transformer类）、FMLP-Rec（全MLP类）、LRURec（线性递归类），以及 NARM、LightSANs、gMLP。
*   **基准方法（Benchmarks）**：
    *   **传统长尾模型**：CITIES、MELT、LOAM。
    *   **LLM 增强模型**：RLMRec (Con/Gen)、LLMInit、LLM-ESR。
*   **评价指标**：Hit Ratio@K (H@K) 和 NDCG@K (N@K)，重点关注全量物品（Overall）和长尾物品（Tail）的性能。

### 4. 资源与算力
*   **LLM 嵌入获取**：使用了 OpenAI 的 `text-embedding-ada-002` 接口离线生成嵌入并缓存，避免了在线推理的巨大开销。
*   **硬件与时长**：论文**未明确说明**具体的 GPU 型号、数量及训练时长。但提到嵌入维度设为 128，Batch Size 为 256，属于常规学术实验配置。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上配合 6 种不同的骨干网络进行了主实验。
    *   进行了详尽的**消融实验**（验证 AGF、ILA、FLA、CLS 等模块的有效性）。
    *   包含**参数敏感性分析**（针对对齐权重 $\alpha$ 和冗余减少系数 $\lambda$）。
    *   包含**分组分析**（按流行度分 5 组观察性能提升）和 **t-SNE 可视化**（验证空间对齐效果）。
    *   增加了**推荐多样性分析**（Coverage 和 Tail_Coverage）。
*   **充分性评价**：实验设计非常全面，不仅验证了准确性，还探讨了泛化性、多样性和空间对齐的直观效果，逻辑严密且对比公平。

### 6. 论文的主要结论与发现
*   **性能卓越**：FAERec 在所有数据集和骨干网络上均显著优于现有基准，尤其在长尾物品上的提升幅度巨大（部分场景提升超过 100%）。
*   **对齐的重要性**：实验证明，仅做语义融合而不解决空间结构不一致问题（如 LLM-ESR）会导致效果受限；双层对齐能显著提升表征质量。
*   **缓解“跷跷板”效应**：传统长尾方法往往在提升长尾性能时损害头部物品表现，而 FAERec 在大幅增强长尾推荐的同时，依然保持或提升了整体推荐水平。
*   **多样性提升**：通过引入 LLM 语义，模型能够推荐更多样化的长尾物品，提升了系统的覆盖率。

### 7. 优点（亮点）
*   **视角新颖**：敏锐捕捉到了 ID 空间与语义空间“结构不一致”这一关键痛点，并给出了系统性的对齐方案。
*   **模型无关性**：作为一个插件式的增强框架，FAERec 可以轻松集成到任何现有的序列推荐模型中。
*   **训练策略科学**：引入课程学习来平衡不同难度的对齐任务，体现了对深度学习优化过程的深刻理解。

### 8. 不足与局限
*   **外部依赖**：依赖于高质量的 LLM 嵌入（如 OpenAI API），对于无法获取此类资源或文本信息极度匮乏的场景（如仅有 ID 的匿名数据集）无法适用。
*   **超参数敏感性**：引入了较多的超参数（如 $\alpha, \lambda, \tau$ 以及课程学习的周期），在不同数据集上可能需要精细调优。
*   **计算复杂度**：虽然 LLM 嵌入是离线生成的，但特征级对齐（FLA）涉及批次内的互相关矩阵计算，在大规模 Batch Size 下会增加一定的计算负担。

（完）
