---
title: "Rank, Don't Generate: Statement-level Ranking for Explainable Recommendation"
title_zh: 排序而非生成：面向可解释推荐的陈述级排序
authors: "Ben Kabongo, Arthur Satouf, Vincent Guigue"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03724v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 使用LLM进行可解释推荐的陈述级排序
tldr: 该研究针对可解释推荐中大语言模型生成式解释存在的幻觉和评估困难等问题，提出将解释任务重新定义为“语句级排序”问题。通过从真实评论中提取解释性、原子化且唯一的语句并进行排序，该方法在根源上杜绝了幻觉，并支持标准化的排序指标评估。研究构建了StaR基准数据集，并揭示了现有模型在个性化解释排序方面的局限性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03724v1/fig-001.webp\", \"caption\": \"Figure 3: Statement clustering pipeline. (1) ANN retrieves the top-𝐾 semantically similar candidates per statement using dense\", \"page\": 6, \"index\": 1, \"width\": 994, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2604.03724v1/fig-002.webp\", \"caption\": \"Figure 5: Impact of the number of layers 𝐿 on ExpGCN per-\", \"page\": 8, \"index\": 2, \"width\": 496, \"height\": 257}]"
motivation: 生成式解释难以避免事实幻觉且缺乏统一的评估标准，因此需要一种更具事实性且可量化评估的解释方案。
method: 提出将解释建模为对候选语句的排序任务，并利用LLM提取原子化语句结合语义聚类构建了StaR基准数据集。
result: 实验发现流行度基准在项目级排序中表现强劲，甚至在平均水平上优于现有的先进模型。
conclusion: 语句级排序为可解释推荐提供了更可靠的评估框架，同时也暴露了当前模型在实现精准个性化解释方面的不足。
---

## 摘要
使用大语言模型（LLMs）生成的文本解释正越来越多地被用于解释推荐结果。然而，评估这些解释仍然是一个关键挑战。我们主张转变目标：排序，而非生成。我们将可解释推荐形式化为一个陈述级排序问题，即系统对源自评论的候选解释性陈述进行排序，并返回前 k 个作为解释。这种形式化方法通过构建方式减轻了幻觉问题，并支持细粒度的事实分析。它还通过相关性得分对因素重要性进行建模，并支持使用既有的排序指标进行标准化、可重复的评估。然而，有意义的评估要求每个陈述都具有解释性（影响用户体验的物品事实）、原子性（关于一个方面的单一观点）和唯一性（合并了同义改写），而从噪声较大的评论中获取这些陈述具有挑战性。我们通过以下方式解决这一问题：(i) 一个基于 LLM 的提取流水线，用于产生解释性和原子性的陈述；(ii) 一种可扩展的语义聚类方法，通过合并同义改写来确保唯一性。基于该流水线，我们推出了 StaR，这是一个用于可解释推荐中陈述排序的基准测试，由 2014 年亚马逊评论（Amazon Reviews 2014）的四个产品类别构建而成。我们在全局级（所有陈述）和物品级（目标物品陈述）排序下评估了基于流行度的基准模型和最先进的模型。流行度基准在全局级排序中具有竞争力，但在物品级排序中平均表现优于最先进的模型，这揭示了个性化解释排序中的关键局限性。

## Abstract
Textual explanations, generated with large language models (LLMs), are increasingly used to justify recommendations. Yet, evaluating these explanations remains a critical challenge. We advocate a shift in objective: rank, don't generate. We formalize explainable recommendation as a statement-level ranking problem, where systems rank candidate explanatory statements derived from reviews and return the top-k as explanation. This formulation mitigates hallucination by construction and enables fine-grained factual analysis. It also models factor importance through relevance scores and supports standardized, reproducible evaluation with established ranking metrics. Meaningful assessment, however, requires each statement to be explanatory (item facts affecting user experience), atomic (one opinion about one aspect), and unique (paraphrases consolidated), which is challenging to obtain from noisy reviews. We address this with (i) an LLM-based extraction pipeline producing explanatory and atomic statements, and (ii) a scalable, semantic clustering method consolidating paraphrases to enforce uniqueness. Building on this pipeline, we introduce StaR, a benchmark for statement ranking in explainable recommendation, constructed from four Amazon Reviews 2014 product categories. We evaluate popularity-based baselines and state-of-the-art models under global-level (all statements) and item-level (target item statements) ranking. Popularity baselines are competitive in global-level ranking but outperform state-of-the-art models on average in item-level ranking, exposing critical limitations in personalized explanation ranking.

---

## 论文详细总结（自动生成）

### 论文总结：排序而非生成（Rank, Don't Generate）

#### 1. 论文的核心问题与整体含义
*   **研究动机**：当前可解释推荐系统主要依赖大语言模型（LLM）生成自然语言解释。然而，生成式方法存在三大痛点：
    1.  **事实幻觉**：模型可能生成与实际商品不符或无中生有的陈述。
    2.  **评估困难**：传统的词法指标（如 BLEU/ROUGE）难以处理同义改写，而语义指标或 LLM 评分存在不可复现、提示词敏感等问题。
    3.  **缺乏粒度**：整段文字解释难以区分不同解释因素的重要性。
*   **核心思想**：主张将“生成任务”转变为“陈述级排序任务”。通过从真实评论中提取原子化的陈述，让系统在候选库中进行排序。这在构建上杜绝了幻觉，并允许使用标准的信息检索指标（Precision, Recall, NDCG）进行客观评估。

#### 2. 论文提出的方法论
*   **核心流程**：
    1.  **陈述提取与验证**：利用 LLM（Qwen3-14B）从原始评论中提取具有解释性（Explanatoriness）和原子性（Atomicity）的陈述，并标注情感极性。随后通过第二个 LLM（Qwen3-8B）进行验证，剔除冗余或不合规的陈述。
    2.  **语义聚类（确保唯一性）**：为了防止同义句干扰评估，采用三步走策略：
        *   **ANN 搜索**：使用向量嵌入（Qwen3-Embedding）检索语义相近的候选对。
        *   **成对过滤**：利用交叉编码器（Cross-encoder）作为重排序器，精确判定是否为同义改写。
        *   **图细化**：构建相似性图，提取连通分量作为初始簇，并对低内聚力的簇进行拆分，最终选出代表性陈述。
    3.  **排序任务形式化**：定义了两种场景：
        *   **全局级排序（Global-level）**：在全量陈述库中检索。
        *   **项目级排序（Item-level）**：仅在与目标商品相关的陈述中排序，侧重于个性化偏好。

#### 3. 实验设计
*   **数据集**：基于 Amazon Reviews 2014 构建了 **StaR** 基准数据集，涵盖四个类别：玩具（Toys）、服装（Clothes）、美妆（Beauty）和运动（Sports）。
*   **对比方法**：
    *   **基准模型**：随机排序（Random）、用户流行度（UserPop）、项目流行度（ItemPop）、全局流行度（GlobalPop）。
    *   **SOTA 模型**：BPER+（基于矩阵分解和 BERT）、ExpGCN（基于图卷积网络）。
*   **评估指标**：P@k, R@k, NDCG@k (k=5, 10)。

#### 4. 资源与算力
*   **算力支持**：论文明确提到使用了 GENCI–IDRIS 的高性能计算（HPC）资源（资助编号 2025-AD011016963 等）。
*   **模型细节**：使用了 Qwen3 系列模型（14B 用于提取，8B 用于验证，0.6B 用于嵌入和重排序）。
*   **训练配置**：模型训练上限为 500 个 epoch，但未详细列出具体的 GPU 型号数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的数据集上进行了测试，样本量从 11.5 万到 29.4 万次交互不等，包含超过 70 万个三元组。
*   **实验维度**：
    *   对比了全局级和项目级两种排序范式。
    *   进行了超参数敏感性分析（如 BPER+ 的权重参数 $\mu$ 和 ExpGCN 的层数 $L$）。
    *   引入了人工标注（每数据集 75 样本）和 LLM 自动评估（1 万样本）来验证陈述提取的质量。
*   **充分性评价**：实验设计较为全面，通过流行度基准的对比揭示了现有复杂模型的潜在弱点，具有较高的客观性和公平性。

#### 6. 论文的主要结论与发现
*   **性能表现**：在全局排序中，ExpGCN 表现最优；但在**项目级排序**中，简单的“用户历史流行度（UserPop）”基准往往能匹配甚至超越 SOTA 模型。
*   **个性化鸿沟**：实验结果暴露了当前模型在处理“特定交互下的个性化解释”方面存在显著局限，模型往往倾向于捕捉全局或项目特征，而非用户特定的偏好。
*   **方法有效性**：LLM 提取+语义聚类的流水线能显著提升陈述的质量，人工评估显示 92% 以上的陈述具有解释性。

#### 7. 优点
*   **范式创新**：通过“排序代替生成”彻底解决了可解释推荐中的幻觉顽疾。
*   **评估标准化**：将模糊的文本生成评估转化为成熟的检索评估，使结果可复现、可对比。
*   **数据质量高**：提出的语义聚类算法比传统的 LSH（局部敏感哈希）更能有效合并同义陈述，确保了基准测试的严谨性。

#### 8. 不足与局限
*   **依赖预处理**：系统性能高度依赖于前期 LLM 提取陈述的质量，若提取阶段存在偏差，会影响后续所有环节。
*   **二元相关性限制**：目前仅采用二元相关性（属于或不属于 ground-truth），未能区分解释因素的权重等级（如“非常相关” vs “一般相关”）。
*   **实时性挑战**：在大规模在线系统中，对海量陈述进行实时排序可能面临计算压力。

（完）
