---
title: "FAVE: Flow-based Average Velocity Establishment for Sequential Recommendation"
title_zh: FAVE：面向序列推荐的基于流的平均速度建立框架
authors: "Ke Shi, Yao Zhang, Feng Guo, Jinyuan Zhang, JunShuo Zhang, Shen Gao, Shuo Shang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04427v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 针对序列用户意图的生成式推荐
tldr: 针对序列推荐中生成式模型存在的“噪声到数据”范式导致的推理效率低和先验不匹配问题，本文提出了FAVE框架。该框架通过两阶段训练策略，利用语义锚点先验和全局平均速度建立，将多步生成简化为单步位移，并结合JVP一致性约束确保轨迹平直。实验证明FAVE在保持SOTA性能的同时，显著提升了推理速度，适用于低延迟场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04427v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of generative recommendation paradigms. (Left) Existing “Noise-to-Data” generative methods suffer from prior mismatch and linear redundancy. (Right) Our proposed Fave employs direct trajectory transport from a semantic anchor prior.\", \"page\": 1, \"index\": 1, \"width\": 504, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.04427v1/fig-002.webp\", \"caption\": \"Table 1: Overall performance comparison. Subscripts indicate the performance gap relative to the baseline model in each category. We highlight the highest-performing metric values in bold and the second-best values in underlined.\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.04427v1/fig-003.webp\", \"caption\": \"Table 3: Inference efficiency comparison.Metrics are reported per sample for FLOPs and Latency.\", \"page\": 8, \"index\": 3, \"width\": 506, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.04427v1/fig-004.webp\", \"caption\": \"Figure 2: The overall framework of Fave which adopts a two-stage training strategy. It first constructs a basic manifold by learning the instantaneous velocity field from Gaussian noise. The second stage incorporates a semantic anchor prior and average velocity modeling to address the prior mismatch and linear redundancy issue.\", \"page\": 4, \"index\": 4, \"width\": 1054, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2604.04427v1/fig-005.webp\", \"caption\": \"Figure 5: Performance of different hyperparameters. (a) weight 𝛼 for L𝑡𝑔𝑡 , (b) weight 𝛽 for L𝑠𝑟𝑐 , (c) weight 𝛾 for L𝑐𝑜𝑛𝑠 , and (d) retention rate 𝜌 .\", \"page\": 9, \"index\": 5, \"width\": 498, \"height\": 298}]"
motivation: 传统的流匹配生成推荐受限于无信息噪声初始化和冗余的迭代求解过程，导致推理效率低下且难以捕捉确定性的偏好转移。
method: 采用两阶段策略：首先通过双端语义对齐构建稳定偏好空间，随后引入语义锚点先验并学习全局平均速度，实现单步生成。
result: 在三个基准数据集上的实验表明，FAVE在达到最先进推荐性能的同时，实现了推理效率的数量级提升。
conclusion: FAVE通过优化生成轨迹和先验初始化，成功解决了生成式推荐在效率与精度之间的权衡问题。
---

## 摘要
生成式推荐已成为捕捉序列推荐中用户意图动态演变的一种变革性范式。虽然基于流的方法提高了扩散模型的效率，但它们仍受限于“噪声到数据”范式，这引入了两个关键的低效问题：一是先验失配，即生成过程从无信息的噪声开始，迫使经历漫长的恢复轨迹；二是线性冗余，即迭代求解器在建模确定性偏好转换时浪费了大量计算。为了解决这些局限性，我们提出了一种用于单步生成推荐的基于流的平均速度建立（Fave）框架，该框架学习从有信息先验到目标分布的直接轨迹。Fave 通过渐进式两阶段训练策略构建。在第一阶段，我们通过双端语义对齐建立稳定的偏好空间，在源端（用户历史）和目标端（下一物品）同时施加约束以防止表示崩溃。在第二阶段，我们通过引入语义锚点先验直接解决效率瓶颈，该先验使用用户交互历史中的掩码嵌入来初始化流，从而提供一个有信息的起点。随后，我们学习全局平均速度，将多步轨迹整合为单个位移向量，并通过基于 JVP 的一致性约束来强制轨迹平直，以确保单步生成。在三个基准数据集上的广泛实验表明，Fave 不仅实现了最先进的推荐性能，还在推理效率上提升了一个数量级，使其在延迟敏感的场景中更具实用性。

## Abstract
Generative recommendation has emerged as a transformative paradigm for capturing the dynamic evolution of user intents in sequential recommendation. While flow-based methods improve the efficiency of diffusion models, they remain hindered by the ``Noise-to-Data'' paradigm, which introduces two critical inefficiencies: prior mismatch, where generation starts from uninformative noise, forcing a lengthy recovery trajectory; and linear redundancy, where iterative solvers waste computation on modeling deterministic preference transitions. To address these limitations, we propose a Flow-based Average Velocity Establishment (Fave) framework for one-step generation recommendation that learns a direct trajectory from an informative prior to the target distribution. Fave is structured via a progressive two-stage training strategy. In Stage 1, we establish a stable preference space through dual-end semantic alignment, applying constraints at both the source (user history) and target (next item) to prevent representation collapse. In Stage 2, we directly resolve the efficiency bottlenecks by introducing a semantic anchor prior, which initializes the flow with a masked embedding from the user's interaction history, providing an informative starting point. Then we learn a global average velocity, consolidating the multi-step trajectory into a single displacement vector, and enforce trajectory straightness via a JVP-based consistency constraint to ensure one-step generation. Extensive experiments on three benchmarks demonstrate that Fave not only achieves state-of-the-art recommendation performance but also delivers an order-of-magnitude improvement in inference efficiency, making it practical for latency-sensitive scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《FAVE: Flow-based Average Velocity Establishment for Sequential Recommendation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的生成式序列推荐模型（如扩散模型和流匹配模型）在推理效率上存在严重瓶颈。
*   **研究动机**：
    *   **先验失配（Prior Mismatch）**：传统模型遵循“从噪声到数据”的范式，从无信息的随机高斯噪声开始生成，导致需要漫长的轨迹才能恢复用户偏好。
    *   **线性冗余（Linear Redundancy）**：在推荐场景中，偏好转移往往是确定性的。现有的迭代求解器（如 Euler 求解器）在每一阶段重复执行相似的线性变换，造成了大量的计算浪费。
*   **整体含义**：论文旨在构建一种“单步生成”的流匹配框架，通过有信息的先验和全局平均速度建模，实现兼顾高精度与极高效率的推荐。

### 2. 方法论：FAVE 框架
FAVE 采用渐进式两阶段训练策略：
*   **第一阶段：基础流形构建（Basic Manifold Construction）**
    *   **双时间流建模**：引入双时间参数 $(t, r)$，其中 $t$ 编码轨迹位置，$r$ 作为时间锚点，帮助模型学习瞬时速度场。
    *   **双端语义对齐**：在源端（用户历史序列）引入重构损失，在目标端（下一物品）引入交叉熵损失，防止物品嵌入在连续空间中发生表示崩溃，确保潜在空间的稳定性。
*   **第二阶段：单步整合（Single-Step Consolidation）**
    *   **语义锚点先验（Semantic Anchor Prior）**：放弃高斯噪声，改用用户历史交互物品的掩码嵌入（Masked Embedding）作为起点 $x_0$。这缩短了生成轨迹，使起点在语义上更接近目标。
    *   **平均速度场建立**：将 ODE（常微分方程）在 $[t, r]$ 区间内积分，定义“平均速度”为全局位移向量。通过学习这个位移，模型可以在一次前向传播中直接预测目标状态。
    *   **JVP 一致性约束**：利用雅可比向量积（Jacobian-Vector Product, JVP）算子施加曲率惩罚，强制生成轨迹保持平直（加速度趋于零），从而保证单步推理的准确性。

### 3. 实验设计
*   **数据集**：使用了三个具有代表性的公开基准数据集：**ML-100k**（电影，稠密）、**Amazon-Beauty**（美妆，稀疏）、**Steam**（游戏，大规模）。
*   **对比方法（Baselines）**：
    *   **传统深度学习**：GRU4Rec, Caser。
    *   **Transformer 类**：SASRec, BERT4Rec, STOSA。
    *   **生成式模型**：DiffuRec（扩散模型）、AutoSeqRec、DreamRec、FMRec（流匹配模型）。
*   **评价指标**：Hit Rate (H@10/20) 和 NDCG (N@10/20)，采用全排名（all-ranking）协议。

### 4. 资源与算力
*   **硬件环境**：论文明确提到所有实验均在单张 **NVIDIA L40 GPU** 上进行。
*   **算力表现**：在推理效率对比中，FAVE 的 FLOPs 仅为 0.04G~0.07G，延迟（Latency）约为 4ms 左右。
*   **训练时长**：文中未详细列出具体的训练总时长，但强调了其两阶段训练的收敛性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **总体性能对比**：在 3 个数据集上对比了 9 种基线模型。
    *   **消融实验**：针对第二阶段训练、一致性损失、匹配损失、语义先验等 4 个核心组件进行了剔除实验。
    *   **效率分析**：对比了 FLOPs、推理延迟和总推理时间。
    *   **可视化分析**：包括推理轨迹的 t-SNE 可视化、嵌入分布可视化。
    *   **超参数分析**：对 4 个关键超参数（损失权重、掩码保留率）进行了敏感度测试。
*   **充分性评价**：实验设计非常全面，不仅验证了推荐精度（SOTA），还深入探讨了效率提升的来源以及生成多样性（ILD 指标），论证过程客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：FAVE 在所有数据集上均优于现有 SOTA 模型，尤其在 ML-100k 上 NDCG@20 提升了 9.90%。
*   **效率飞跃**：相比于最先进的流匹配模型 FMRec，FAVE 实现了 **20 倍以上** 的推理加速。
*   **先验重要性**：实验证明，使用历史信息作为“语义锚点”比随机噪声能显著降低学习难度并提高稳定性。
*   **多样性保持**：尽管是单步生成，但通过平均速度建模，FAVE 依然保持了良好的推荐多样性，未出现模式崩溃（Mode Collapse）。

### 7. 优点与亮点
*   **理论创新**：将流匹配的瞬时速度建模转化为全局平均速度建模，从数学上解决了迭代求解的冗余问题。
*   **效率极高**：单步生成机制使其能够满足工业级实时推荐的低延迟要求。
*   **冷启动潜力**：语义锚点先验的设计为处理冷启动或稀疏数据提供了一种更稳健的初始化方式。
*   **轨迹平直化**：引入 JVP 约束是生成式推荐领域的一个亮点，有效减少了推理过程中的累积误差。

### 8. 不足与局限
*   **掩码策略依赖**：模型性能对语义锚点的掩码保留率（$\rho$）较为敏感，需要针对不同数据集进行精细调优。
*   **两阶段训练复杂性**：虽然推理快，但两阶段训练增加了训练流程的复杂度和超参数调优的工作量。
*   **应用范围**：目前仅在序列推荐任务上验证，对于涉及复杂图结构或多模态信息的推荐场景，其平均速度场的构建可能面临更大挑战。

（完）
