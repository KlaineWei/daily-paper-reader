---
title: "ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training"
title_zh: ShapE-GRPO：面向多候选大语言模型训练的 Shapley 增强奖励分配
authors: "Rui Ai, Yu Pan, David Simchi-Levi, Chonghuan Wang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29871v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 推荐场景下大语言模型训练的奖励分配
tldr: 针对大语言模型在生成候选集（如推荐、头脑风暴）时，传统强化学习（GRPO）将集合奖励平均分配导致训练信号模糊的问题，本文提出 ShapE-GRPO。该方法利用合作博弈论中的 Shapley 值，将集合级奖励分解为候选特定的细粒度信号。实验证明，该方法在保持计算效率的同时，显著提升了模型性能并加速了训练收敛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29871v1/fig-001.webp\", \"caption\": \"Figure 1: When the user considers the ratings for the three suggestions to be 5.0, 4.0, and 3.0, respectively, our ShapE-GRPO performs candidate-level (with broadcasting to token-level) reward allocation as above. However, GRPO only uses a single +5.0 sequence-level reward shared across tokens.\", \"page\": 2, \"index\": 1, \"width\": 827, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-002.webp\", \"caption\": \"Figure 4: Response reward as a function of the number of candidates across different datasets using QWEN3-8B.\", \"page\": 18, \"index\": 2, \"width\": 826, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-003.webp\", \"caption\": \"Table 2: Main training hyperparameters used in our experiments.\", \"page\": 23, \"index\": 3, \"width\": 714, \"height\": 231}]"
motivation: 传统的 GRPO 在处理集合生成任务时，将相同的集合奖励分配给所有候选者，导致表现差的候选者“搭便车”并产生训练噪声。
method: 引入 Shapley 值理论，将集合奖励分解为针对每个候选者的细粒度奖励信号，并确保计算复杂度在多项式时间内。
result: ShapE-GRPO 在多个数据集上均优于标准 GRPO，且在训练过程中表现出更快的收敛速度。
conclusion: 通过 Shapley 值进行奖励分配能有效解决多候选集训练中的信号噪声问题，显著提升了 LLM 的集合生成能力。
---

## 摘要
在推荐、头脑风暴和代码建议等用户-智能体交互场景中，大语言模型（LLMs）通常生成候选推荐集合，其目标是最大化整个集合的集体效用，而非独立地最大化单个候选。然而，现有的强化学习后训练范式（如群体相对策略优化，GRPO）通常将相同的集合级标量奖励分配给集合中的每个候选。这会导致训练信号中存在噪声，表现较差的候选会“搭便车”，利用单个优秀候选产生的高奖励，从而导致次优的探索。为了解决这一问题，我们提出了 Shapley 增强的 GRPO（ShapE-GRPO）。通过利用集合级效用的置换不变性，我们从合作博弈论中推导出一种 Shapley 增强公式，将集合级奖励分解为细粒度的、针对特定候选的信号。我们证明了该公式在保持多项式时间复杂度的计算效率的同时，保留了 Shapley 值的基本公理。实验表明，ShapE-GRPO 在多个数据集上始终优于标准的 GRPO，并在训练过程中实现了加速收敛。

## Abstract
In user-agent interaction scenarios such as recommendation, brainstorming, and code suggestion, Large Language Models (LLMs) often generate sets of candidate recommendations where the objective is to maximize the collective utility of the entire set rather than individual candidates independently. However, existing reinforcement learning post-training paradigms, such as Group Relative Policy Optimization (GRPO), typically assign the same set-level scalar reward to every candidate in the set. This leads to noisy training signals where poor candidates free-ride on the high reward produced by a single strong peer, resulting in suboptimal exploration. To address this, we propose Shapley-Enhanced GRPO (ShapE-GRPO). By leveraging the permutation-invariant nature of set-level utility, we derive a Shapley-enhanced formulation from cooperative game theory to decompose set-level rewards into granular, candidate-specific signals. We show that our formulation preserves the fundamental axioms of the Shapley value while remaining computationally efficient with polynomial-time complexity. Empirically, ShapE-GRPO consistently outperforms standard GRPO across diverse datasets with accelerated convergence during training.

---

## 论文详细总结（自动生成）

以下是对论文《ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在推荐系统、头脑风暴和代码建议等场景中，LLM 通常生成一组候选方案（Candidate Set）。此时的目标是**最大化集合的整体效用**（例如：只要有一个建议被用户采纳即可），而非独立地优化每个候选。
*   **研究动机**：现有的强化学习算法（如 GRPO）通常将集合级别的单一奖励（Scalar Reward）平均分配给集合中的所有候选。
*   **核心痛点**：这导致了“**搭便车（Free-riding）**”问题——表现差的候选者因为同组中有一个表现优异的“队友”而获得了高奖励。这种模糊的训练信号增加了噪声，降低了探索效率，导致模型难以收敛到最优策略。

### 2. 方法论：ShapE-GRPO
论文提出了 **ShapE-GRPO**，其核心思想是利用合作博弈论中的 **Shapley Value** 来实现公平且细粒度的奖励分配。
*   **核心思想**：将生成的一组候选看作博弈中的参与者，根据每个候选对集合总奖励的“边际贡献”来分配奖励。
*   **关键技术细节**：
    *   **候选级分配**：将 LLM 的输出分解为“推理部分（Reasoning）”和“候选集（Candidates）”。推理部分保留原始集合奖励，而候选部分则根据 Shapley 值分配奖励。
    *   **计算优化**：通用的 Shapley 值计算具有指数级复杂度 $O(2^K)$。但论文证明，在“LLM 作为推荐者”的特定场景下（奖励通常由集合中表现最好的候选决定，即 $max$ 函数），Shapley 值可以在 **$O(K^2)$** 的多项式时间内精确计算，甚至在二值奖励下可优化至 $O(K)$。
    *   **公理保证**：该分配机制严格遵循 Shapley 值的四个基本公理：效率性（Efficiency）、对称性（Symmetry）、加性（Additivity）和哑玩家属性（Null Player）。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **科学论文摘要（ACLSum）**：评估生成多个摘要候选的质量。
    2.  **代码建议（DS-1000）**：评估生成多个代码方案的正确性（二值奖励）。
    3.  **电影推荐（Netflix）**：基于 Netflix Prize 数据构建，模拟真实推荐场景，分为“有用户历史”和“无用户历史”两个子任务。
*   **对比方法（Baselines）**：
    *   **标准 GRPO**：集合内所有候选共享相同奖励。
    *   **Winner-Takes-All (WTA)**：仅将奖励分配给得分最高的候选，其余为零。
*   **评估模型**：Qwen 3-8B 和 Llama 3.1-8B-Instruct。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 张 NVIDIA GH200 GPU**。
*   **训练细节**：
    *   训练 Epoch 为 1。
    *   最大响应长度限制为 4096 tokens。
    *   使用了 verl 训练框架。
    *   论文未明确给出总训练时长，但提到 ShapE-GRPO 在约 160 步时即可收敛，而标准 GRPO 需要约 240 步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了文本摘要、代码生成、个性化推荐三个完全不同的领域，验证了方法的通用性。
*   **模型覆盖**：在 Qwen 和 Llama 两个主流开源模型系列上进行了验证。
*   **消融与对比**：通过与 WTA 的对比，证明了 Shapley 值的“协作分配”优于简单的“只奖第一名”策略。
*   **充分性评价**：实验设计较为充分且客观。论文不仅关注最终性能，还详细分析了训练过程中的收敛速度和稳定性（训练曲线），并发布了新的 Netflix 推荐 Benchmark。

### 6. 主要结论与发现
*   **性能提升**：ShapE-GRPO 在所有数据集上均显著优于标准 GRPO 和 WTA。
*   **收敛加速**：相比 GRPO，ShapE-GRPO 表现出更快的收敛速度和更稳定的训练轨迹（波动更小）。
*   **解决搭便车**：理论和实验均证明，该方法能有效识别并惩罚集合中的无效候选，提供更准确的梯度导向。
*   **上下文利用**：在 Netflix 任务中，经过 ShapE-GRPO 训练的模型能更好地利用长上下文中的用户历史信息。

### 7. 优点
*   **理论优雅**：将经典的博弈论工具（Shapley Value）巧妙应用于 LLM 强化学习，解决了实际存在的奖励分配偏差问题。
*   **计算高效**：通过数学推导将复杂度降至多项式级别，几乎不增加额外的训练开销。
*   **即插即用**：作为一种奖励分配策略，它可以轻松集成到现有的 RLHF 框架中。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 8B 规模的模型上，尚未在大规模参数模型（如 70B+）上验证其扩展性。
*   **长上下文挑战**：尽管有所提升，但 Llama 3.1 在处理极长用户历史时仍表现出利用率不足的问题，这可能受限于模型本身的注意力机制而非奖励分配算法。
*   **候选数量假设**：实验中设定候选数量 $K=4$，在候选数量极多（如 $K>20$）的极端场景下，其性能表现和计算开销尚待进一步探索。

（完）
