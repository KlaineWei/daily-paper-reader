---
title: "UniMixer: A Unified Architecture for Scaling Laws in Recommendation Systems"
title_zh: UniMixer：推荐系统缩放法则的统一架构
authors: "Mingming Ha, Guanchen Wang, Linxun Chen, Xuan Rao, Yuexin Shi, Tianbao Ma, Zhaojie Liu, Yunqian Fan, Zilong Lu, Yanan Niu, Han Li, Kun Gai"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00590v2"
tags: ["query:llmrec"]
score: 9.0
evidence: 推荐系统的统一缩放架构
tldr: 本研究针对推荐系统中缩放法则（Scaling Laws）日益增长的需求，提出了统一架构 UniMixer。当前推荐模型缩放主要依赖注意力机制、TokenMixer 和因子分解机，但三者设计理念迥异。UniMixer 通过将规则化的 TokenMixer 转换为等效的参数化结构，构建了通用的特征混合模块，实现了三种主流架构的理论统一。此外，论文还设计了轻量级的 UniMixing-Lite 以提升缩放投资回报率。实验证明，UniMixer 在离线和在线场景下均展现出卓越的缩放能力和性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00590v2/fig-001.webp\", \"caption\": \"Figure 3: (a) The global mixing weights of different methods. (b) Equivalent parameterization of the rule-based TokenMixer.\", \"page\": 6, \"index\": 1, \"width\": 419, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.00590v2/fig-002.webp\", \"caption\": \"Figure 4: The scaling laws between AUC and Parameters/FLOPs for UniMixer-2-Blocks/UniMixerLite-2-Blocks and RankMixer architectures. The x-axis is presented on a logarithmic scale.\", \"page\": 11, \"index\": 2, \"width\": 738, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.00590v2/fig-003.webp\", \"caption\": \"Table 2: Performance and efficiency of ∼ 100M-parameter UniMixer and SOTA models in ad serving scenarios.\", \"page\": 10, \"index\": 3, \"width\": 825, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2604.00590v2/fig-004.webp\", \"caption\": \"Table 4: Effects of the basis number, rank, and UniMixer block number in UniMixing-Lite.\", \"page\": 13, \"index\": 4, \"width\": 825, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.00590v2/fig-005.webp\", \"caption\": \"Figure 6: The scaling curves between AUC and Parameters/FLOPs for RankMixer and UniMixer/UniMixer-Lite with 2 blocks and 4 blocks.\", \"page\": 13, \"index\": 5, \"width\": 740, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2604.00590v2/fig-006.webp\", \"caption\": \"Figure 5: W̄G and W̄ i B of UniMixer-Lite with different temperature coefficients. (a) W̄G with τ = 1; (b) {W̄ i B}6i=1 with τ = 1; (c) W̄G with τ = 0.05; (d) {W̄ i B}6i=1 with τ = 0.05.\", \"page\": 12, \"index\": 6, \"width\": 654, \"height\": 515}, {\"url\": \"assets/figures/arxiv/2604.00590v2/fig-007.webp\", \"caption\": \"Figure 2: The UniMixer architecture for scaling laws in recommendation systems.\", \"page\": 5, \"index\": 7, \"width\": 748, \"height\": 360}]"
motivation: 现有的推荐系统缩放架构在设计理念上存在显著差异，缺乏一个能够统一注意力机制、TokenMixer 和因子分解机的通用框架来优化缩放效率。
method: 提出 UniMixer 架构，通过将 TokenMixer 参数化实现特征混合模式的可学习性，并设计了轻量化的 UniMixing-Lite 模块以压缩参数和计算成本。
result: 广泛的离线和在线实验结果表明，UniMixer 在不同的参数规模下均表现出优于现有主流模型的缩放能力和性能表现。
conclusion: UniMixer 成功建立了一个统一的推荐系统缩放模块设计框架，为提升推荐模型的缩放效率和性能提供了有效的理论与实践指导。
---

## 摘要
近年来，推荐模型的缩放法则（scaling laws）引起了越来越多的关注，这些法则揭示了推荐系统性能与参数量/计算量（FLOPs）之间的关系。目前，实现推荐模型缩放有三种主流架构，即基于注意力机制（attention-based）、基于 TokenMixer 以及基于因子分解机（factorization-machine-based）的方法，它们在设计理念和架构结构上存在根本差异。在本文中，我们提出了一种用于推荐系统的统一缩放架构，即 UniMixer，旨在提高缩放效率并建立一个统一主流缩放模块的理论框架。通过将基于规则的 TokenMixer 转换为等效的参数化结构，我们构建了一个通用的参数化特征混合模块，使得 Token 混合模式能够在模型训练过程中进行优化和学习。同时，通用的参数化 Token 混合消除了 TokenMixer 中要求多头数量必须等于 Token 数量的限制。此外，我们为推荐系统建立了一个统一的缩放模块设计框架，桥接了基于注意力机制、基于 TokenMixer 和基于因子分解机的方法之间的联系。为了进一步提升缩放投资回报率（ROI），我们设计了一个轻量级的 UniMixing 模块，即 UniMixing-Lite，它在进一步压缩模型参数和计算成本的同时，显著提升了模型性能。缩放曲线如图所示。我们进行了广泛的线下和线上实验，验证了 UniMixer 卓越的缩放能力。

## Abstract
In recent years, the scaling laws of recommendation models have attracted increasing attention, which govern the relationship between performance and parameters/FLOPs of recommenders. Currently, there are three mainstream architectures for achieving scaling in recommendation models, namely attention-based, TokenMixer-based, and factorization-machine-based methods, which exhibit fundamental differences in both design philosophy and architectural structure. In this paper, we propose a unified scaling architecture for recommendation systems, namely \textbf{UniMixer}, to improve scaling efficiency and establish a unified theoretical framework that unifies the mainstream scaling blocks. By transforming the rule-based TokenMixer to an equivalent parameterized structure, we construct a generalized parameterized feature mixing module that allows the token mixing patterns to be optimized and learned during model training. Meanwhile, the generalized parameterized token mixing removes the constraint in TokenMixer that requires the number of heads to be equal to the number of tokens. Furthermore, we establish a unified scaling module design framework for recommender systems, which bridges the connections among attention-based, TokenMixer-based, and factorization-machine-based methods. To further boost scaling ROI, a lightweight UniMixing module is designed, \textbf{UniMixing-Lite}, which further compresses the model parameters and computational cost while significantly improve the model performance. The scaling curves are shown in the following figure. Extensive offline and online experiments are conducted to verify the superior scaling abilities of \textbf{UniMixer}.

---

## 论文详细总结（自动生成）

这是一份关于论文《UniMixer: A Unified Architecture for Scaling Laws in Recommendation Systems》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）的成功证明了性能随规模增长的“缩放法则”（Scaling Laws）。推荐系统领域也在探索类似的架构，以通过增加参数量和计算量（FLOPs）来持续提升模型效果。
*   **核心问题**：当前推荐系统存在三种主流的缩放架构：**基于注意力机制（Attention-based）**、**基于 TokenMixer** 和 **基于因子分解机（FM-based）**。这三者在设计哲学和结构上完全不同，缺乏统一的理论框架，且各自存在局限性（如注意力机制的计算开销大、TokenMixer 缺乏可学习性、FM 难以捕捉高阶交互）。
*   **研究目标**：提出一个统一的架构，融合现有方法的优势，提高推荐系统的缩放效率（Scaling ROI）。

### 2. 方法论：UniMixer 架构
*   **核心思想**：将原本基于固定规则的 TokenMixer 操作转化为等效的**参数化结构**，从而推导出一种通用的特征混合模块，在理论上将注意力、TokenMixer 和 FM 统一在同一个公式下。
*   **关键技术细节**：
    *   **参数化 TokenMixer**：发现 TokenMixer 的置换操作可分解为两个小矩阵的克罗内克积（Kronecker product）。通过参数化这些矩阵，使特征混合模式可学习。
    *   **UniMixing 模块**：定义了“局部混合”（Local Mixing，处理块内交互）和“全局混合”（Global Mixing，处理块间交互）。通过优化计算流水线，将计算复杂度从 $O(L^2)$ 降低到 $O(L^2/B + LB)$。
    *   **UniMixing-Lite**：为了进一步提升效率，引入基矩阵（Basis matrices）动态生成局部权重，并对全局权重进行低秩近似，大幅减少参数量。
    *   **约束机制**：使用 Sinkhorn-Knopp 迭代确保参数矩阵满足双标置换（Doubly Stochasticity）和对称性，并引入温度系数控制稀疏性。
    *   **SiameseNorm**：引入双流耦合归一化结构，解决深层模型训练不稳定的问题，支持模型向深度方向缩放。

### 3. 实验设计
*   **数据集与场景**：使用快手（Kuaishou）真实广告投放场景的日志数据，样本量超过 **7 亿**，涵盖用户留存预测任务。
*   **评估指标**：AUC、UAUC（用户级 AUC）、参数量（Params）、计算量（FLOPs）。
*   **对比方法（Baselines）**：
    *   **Attention 类**：HiFormer, FAT, Heterogeneous Attention。
    *   **TokenMixer 类**：RankMixer, TokenMixer-Large。
    *   **FM 类**：Wukong。

### 4. 资源与算力
*   **算力配置**：实验在一个包含 **40 张 GPU** 的混合分布式训练框架上运行。
*   **训练细节**：使用 Adam 优化器，学习率为 0.001。文中提到了温度退火策略（从 1.0 降至 0.05）以稳定训练。
*   **未明确信息**：论文未明确说明具体的 GPU 型号（如 A100 或 H100）以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：对比了 6 种 SOTA 模型在 100M 参数量级的表现。
    *   **缩放实验**：绘制了从 10M 到 100M+ 参数规模的缩放曲线，验证了幂律趋势。
    *   **消融实验**：针对温度系数、对称约束、局部权重、模型预热、SiameseNorm 等 5 个关键组件进行了对比。
    *   **超参实验**：研究了基矩阵数量、秩的大小以及模型深度（2/4/8 层）的影响。
    *   **线上实验**：在快手真实业务中进行了 A/B 测试。
*   **充分性评价**：实验设计非常充分，不仅有超大规模的离线数据验证，还有工业级的线上闭环测试，且对比了当前领域内最强的一批 Baseline，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **统一性验证**：UniMixer 在理论和实验上成功统一了主流架构，证明了 TokenMixer 可以看作是注意力机制的一种特殊/约束形式。
*   **缩放效率**：UniMixer-Lite 表现出最强的缩放效率，其缩放指数（Scaling Exponent）高于 RankMixer，意味着随着资源投入增加，其性能提升幅度更大。
*   **深度优势**：相比 RankMixer 在堆叠层数时可能出现的性能退化，UniMixer 配合 SiameseNorm 在增加深度时表现出持续的增益。
*   **业务价值**：线上 A/B 测试显示，用户 30 天累积活跃天数（CAD）平均提升了 **15% 以上**。

### 7. 优点与亮点
*   **理论深度**：不仅提出了新模型，还通过数学推导建立了不同流派推荐模型之间的内在联系，具有很强的启发性。
*   **计算优化**：通过克罗内克积分解和计算流水线优化，解决了参数化大矩阵带来的显存和计算瓶颈。
*   **工业实用性**：UniMixing-Lite 的设计兼顾了性能与成本，非常适合大规模工业部署。

### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在广告场景的留存预测（二分类任务），虽然提到可扩展到生成式推荐，但文中未给出序列建模或生成任务的具体实验数据。
*   **超参敏感性**：模型依赖温度退火和模型预热（Warm-up）策略，这可能增加了调参的复杂度。
*   **开源情况**：作为企业（快手）的研究成果，论文未提及代码开源计划，外部研究者复现难度较大。

（完）
