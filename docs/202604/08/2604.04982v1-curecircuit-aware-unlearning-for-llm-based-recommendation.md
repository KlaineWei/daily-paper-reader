---
title: "CURE:Circuit-Aware Unlearning for LLM-based Recommendation"
title_zh: CURE：面向基于大语言模型推荐的电路感知机器遗忘
authors: "Ziheng Chen, Jiali Cheng, Zezhong Fan, Hadi Amiri, Yunzhi Yao, Xiangguo Sun, Yang Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04982v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 基于大语言模型的推荐与隐私擦除
tldr: 针对大语言模型推荐系统（LLMRec）中的隐私风险，本文提出了一种名为CURE的电路感知遗忘学习框架。该方法通过识别并解耦模型中负责特定任务的计算子图（电路），将模块分为遗忘特定、保留特定和任务共享组，并应用差异化更新规则，有效解决了传统方法中遗忘与保留目标间的梯度冲突问题，在保证模型性能的同时实现了高效且透明的隐私数据擦除。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04982v1/fig-001.webp\", \"caption\": \"Figure 3: Unlearning effectivenss and model performance on Movie and Goodreads.\", \"page\": 7, \"index\": 1, \"width\": 1056, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2604.04982v1/fig-002.webp\", \"caption\": \"Table 1: Unlearning time comparison (s) ↓.\", \"page\": 8, \"index\": 2, \"width\": 474, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.04982v1/fig-003.webp\", \"caption\": \"Figure 2: Schematics of CURE: (i) Locating Circuits in LLMREC; (ii)Selective Circuits Updating\", \"page\": 4, \"index\": 3, \"width\": 1056, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2604.04982v1/fig-004.webp\", \"caption\": \"Figure 4: Transparent comparison of circuit-aware unlearning with ROME and WISE\", \"page\": 9, \"index\": 4, \"width\": 506, \"height\": 417}]"
motivation: 现有LLMRec遗忘学习方法因统一更新参数导致遗忘与保留目标之间存在梯度冲突，且过程缺乏透明度。
method: 提出CURE框架，通过提取推荐核心电路并将其模块分类为遗忘特定、保留特定和任务共享组，实施功能特定的更新规则。
result: 在真实数据集上的实验证明，该方法在遗忘有效性和维持模型推荐效用方面均显著优于现有基准。
conclusion: 通过电路感知的参数解耦与选择性更新，可以有效解决LLMRec遗忘学习中的优化不稳定问题并提升系统可信度。
---

## 摘要
大语言模型（LLMs）的最新进展通过实现对用户兴趣和物品属性的丰富语义理解与推理，为推荐系统带来了新的机遇。然而，随着隐私法规的收紧，将用户数据纳入基于大语言模型的推荐（LLMRec）引入了显著的隐私风险，使得机器遗忘（unlearning）算法对于实际部署变得日益关键。尽管对 LLMRec 机器遗忘的关注日益增加，但大多数现有方法将遗忘过程表述为遗忘目标和保留目标的加权组合，并以统一的方式更新模型参数。这种表述不可避免地会导致两个目标之间的梯度冲突，从而引起优化不稳定，并导致遗忘无效或模型效用严重下降。此外，遗忘过程在很大程度上仍是一个黑盒，损害了其透明度和可信度。为了应对这些挑战，我们提出了 CURE，这是一个电路感知（circuit-aware）的机器遗忘框架，它将模型组件解耦为功能不同的子集并进行选择性更新。在这里，电路是指对特定任务行为负有因果责任的计算子图。具体而言，我们提取了物品推荐背后的核心电路，并分析了这些电路中的各个模块如何对遗忘和保留目标做出贡献。基于这一分析，这些模块被分为遗忘特定组、保留特定组和任务共享组，每组都遵循特定的更新规则，以减轻遗忘过程中的梯度冲突。在真实世界数据集上的实验表明，与现有基准相比，我们的方法实现了更有效的机器遗忘。

## Abstract
Recent advances in large language models (LLMs) have opened new opportunities for recommender systems by enabling rich semantic understanding and reasoning about user interests and item attributes. However, as privacy regulations tighten, incorporating user data into LLM-based recommendation (LLMRec) introduces significant privacy risks, making unlearning algorithms increasingly crucial for practical deployment. Despite growing interest in LLMRec unlearning, most existing approaches formulate unlearning as a weighted combination of forgetting and retaining objectives while updating model parameters in a uniform manner. Such formulations inevitably induce gradient conflicts between the two objectives, leading to unstable optimization and resulting in either ineffective unlearning or severe degradation of model utility. Moreover, the unlearning procedure remains largely black-box, undermining its transparency and trustworthiness. To tackle these challenges, we propose CURE, a circuit-aware unlearning framework that disentangles model components into functionally distinct subsets and selectively updates them. Here, a circuit refers to a computational subgraph that is causally responsible for task-specific behaviors. Specifically, we extract the core circuits underlying item recommendation and analyze how individual modules within these circuits contribute to the forget and retain objectives. Based on this analysis, these modules are categorized into forget-specific, retain-specific, and task-shared groups, each subject to function-specific update rules to mitigate gradient conflicts during unlearning. Experiments on real-world datasets show that our approach achieves more effective unlearning than existing baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《CURE: Circuit-Aware Unlearning for LLM-based Recommendation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：大语言模型推荐系统（LLMRec）通过微调用户历史交互数据获得了强大的推荐能力，但随之而来的隐私法规要求系统必须具备“机器遗忘”能力，即从已训练模型中彻底擦除特定用户或交互的影响。
*   **核心问题**：
    1.  **梯度冲突**：现有方法通常将“遗忘”和“保留”目标简单加权求和。在优化过程中，这两个目标的梯度方向往往相反，导致模型要么忘不掉，要么推荐性能大幅下降。
    2.  **黑盒化**：缺乏对模型内部哪些模块（如特定的注意力头或MLP）存储了敏感信息的理解，导致遗忘过程缺乏透明度和可信度。
*   **研究动机**：利用“机械解释性”（Mechanistic Interpretability）中的**电路（Circuit）**概念，识别并解耦模型中负责不同功能的计算子图，从而实现精准且无冲突的参数更新。

### 2. 方法论：CURE 框架
CURE（Circuit-Aware Unlearning）分为两个主要阶段：

*   **阶段一：定位 LLMRec 电路**
    *   **核心思想**：将 LLM 视为有向无环图，节点为注意力头和 MLP。通过量化边对预测结果的贡献来识别核心电路。
    *   **关键技术**：
        *   **激活干预（Activation Intervention）**：利用一阶泰勒展开快速估计屏蔽某条边对输出概率的影响。
        *   **激活修补（Activation Patching）**：通过对比原始输入和“损坏输入”（Corrupt Sample）的激活差异来精确定位。
        *   **图增强样本构建**：利用用户-物品图上的个性化 PageRank（PPR）算法，找到与用户兴趣相关但与目标物品弱相关的替代项，构建高质量的对比样本。
*   **阶段二：选择性电路更新**
    *   **模块分类**：将识别出的电路模块分为：
        1.  **遗忘特定神经元 ($\Theta_f$)**：仅存在于遗忘电路中，只根据遗忘损失更新。
        2.  **保留特定神经元 ($\Theta_r$)**：仅存在于保留电路中，只根据保留损失更新。
        3.  **功能共享神经元 ($\Theta_{sh}$)**：两者共有，采用**投影梯度策略**（如 PCGrad），将冲突梯度投影到法平面，消除破坏性分量。

### 3. 实验设计
*   **数据集**：MovieLens-1M (ML-1M) 和 GoodReads (GD)。
*   **基准模型（Backbones）**：GPT-2 Large 和 Llama-2 (7B)。
*   **对比方法**：
    *   **重训类**：Retrain（从头训练，金标准）、SISA（分片重训）、RecEraser。
    *   **遗忘算法类**：E2URec（基于教师-学生框架的 SOTA）。
    *   **模型编辑类**：ROME、WISE。
    *   **梯度优化类**：PCGrad。
*   **评估指标**：推荐性能（AUC, ACC, LogLoss）、遗忘有效性（JSD，衡量与重训模型输出分布的接近程度）、遗忘效率（训练耗时）。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 GPU**。
*   **技术细节**：针对 Llama-2 (7B) 使用了 **LoRA**（Rank=8）以提高训练效率。
*   **效率表现**：CURE 在 Llama-2 上的遗忘速度比 E2URec 快约 **3.5倍**，比重训快 **10倍以上**。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域（电影、图书）的数据集和两种不同规模（GPT-2, Llama-2）的架构上进行了验证。
*   **消融实验**：针对超参数 $\omega_r$（平衡因子）进行了详细分析。
*   **可视化分析**：提供了梯度冲突分布的统计图，以及电路定位的透明度对比图。
*   **充分性评价**：实验设计较为全面，涵盖了效用、安全性、效率三个维度，对比了从传统重训到最新模型编辑的各类方法，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：CURE 在遗忘效率上提升了 18%，模型效用（推荐准确度）提升了 6%。
*   **冲突缓解**：通过电路感知更新，CURE 显著减少了严重冲突梯度对（$\cos \psi < -0.02$）的比例，降幅达 55%-76%。
*   **透明度**：实验证明 CURE 能够识别出与特定语义（如电影类型）相关的 MLP 模块，使遗忘过程具备了可解释性。

### 7. 优点（亮点）
*   **理论支撑**：提供了数学证明（Theorem 5.1），确保单步梯度更新下 CURE 的损失函数优于普通梯度下降。
*   **机制创新**：首次将机械解释性中的电路发现技术引入推荐系统遗忘学习，从“黑盒更新”转向“功能解耦更新”。
*   **效率与效果兼顾**：在保持极高遗忘速度的同时，极大地缩小了与“从头重训”模型之间的性能差距。

### 8. 不足与局限
*   **电路提取开销**：虽然使用了泰勒近似，但对于超大规模模型，提取精确电路仍需额外的计算步骤。
*   **应用限制**：目前主要针对二分类推荐任务（Yes/No），对于生成式推荐或排序任务的适用性有待进一步验证。
*   **偏差风险**：依赖于 PPR 算法构建对比样本，如果底层用户-物品图本身存在严重偏差，可能会影响电路定位的准确性。

（完）
