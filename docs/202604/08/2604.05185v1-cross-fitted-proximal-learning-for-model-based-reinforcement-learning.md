---
title: Cross-fitted Proximal Learning for Model-Based Reinforcement Learning
title_zh: 基于模型的强化学习中的交叉拟合近端学习
authors: "Nishanth Venkatesh, Andreas A. Malikopoulos"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05185v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于序列决策和规划的基于模型的强化学习
tldr: 本研究针对存在隐性混杂因素的离线模型强化学习（POMDPs）中模型估计偏差问题，提出了一种交叉拟合的近端学习方法。该方法通过估计满足条件矩限制（CMR）的奖励-发射和观测-转移桥函数，并结合K折交叉拟合技术，提高了数据利用效率。研究还推导了该估计器的理论误差界限，并将其分解为干扰项估计误差和经验平均误差，为复杂环境下的策略评估提供了更稳健的统计工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05185v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of observation-transition recovery\", \"page\": 6, \"index\": 1, \"width\": 512, \"height\": 325}]"
motivation: 在存在隐性混杂因素的离线部分可观测马尔可夫决策过程中，直接从观测数据学习模型会导致偏差，需要更有效的桥函数估计方法。
method: 提出一种K折交叉拟合的二阶段桥函数估计器，将桥函数学习建模为包含干扰项（如条件均值嵌入和条件密度）的条件矩限制问题。
result: 建立了交叉拟合估计器的Oracle比较界限，并成功将误差分解为干扰项估计引起的阶段I误差和经验平均引起的阶段II误差。
conclusion: 该方法在保持桥函数识别策略的同时，显著提升了数据效率和统计估计的稳健性，为混杂环境下的模型强化学习提供了理论支撑。
---

## 摘要
基于模型的强化学习在序列决策中极具吸引力，因为它显式地估计奖励和转移模型，并随后通过模拟展开支持规划。然而，在存在隐性混杂因素的离线设置中，直接从观测数据学习的模型可能会产生偏差。这一挑战在部分可观测系统中尤为突出，其中潜在因素可能共同影响动作、奖励和未来的观测。最近的研究表明，在此类受混杂影响的部分可观测马尔可夫决策过程（POMDPs）中，策略评估可以简化为估计满足条件矩限制（CMRs）的奖励-发射和观测-转移桥函数。在本文中，我们研究了这些桥函数的统计估计。我们将桥函数学习建模为一个 CMR 问题，其冗余对象由条件均值嵌入和条件密度给出。随后，我们为现有的两阶段桥估计器开发了一种 $K$ 折交叉拟合扩展。所提出的程序保留了原始的基于桥函数的识别策略，同时比单次样本分割更有效地利用了可用数据。我们还推导了交叉拟合估计器的 oracle-comparator 界，并将产生的误差分解为由冗余估计引起的阶段 I 项和由经验平均引起的阶段 II 项。

## Abstract
Model-based reinforcement learning is attractive for sequential decision-making because it explicitly estimates reward and transition models and then supports planning through simulated rollouts. In offline settings with hidden confounding, however, models learned directly from observational data may be biased. This challenge is especially pronounced in partially observable systems, where latent factors may jointly affect actions, rewards, and future observations. Recent work has shown that policy evaluation in such confounded partially observable Markov decision processes (POMDPs) can be reduced to estimating reward-emission and observation-transition bridge functions satisfying conditional moment restrictions (CMRs). In this paper, we study the statistical estimation of these bridge functions. We formulate bridge learning as a CMR problem with nuisance objects given by a conditional mean embedding and a conditional density. We then develop a $K$-fold cross-fitted extension of the existing two-stage bridge estimator. The proposed procedure preserves the original bridge-based identification strategy while using the available data more efficiently than a single sample split. We also derive an oracle-comparator bound for the cross-fitted estimator and decompose the resulting error into a Stage I term induced by nuisance estimation and a Stage II term induced by empirical averaging.

---

## 论文详细总结（自动生成）

### 论文总结：基于模型的强化学习中的交叉拟合近端学习

#### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的序列决策场景（如医疗记录、自动驾驶、人机交互）中，系统往往是**部分可观测的（POMDP）**，且存在**隐性混杂因素（Hidden Confounding）**。这些潜在因素会同时影响动作选择和系统演化，导致直接从离线观测数据中学习到的转移和奖励模型产生偏差（伪相关）。
本文的核心问题是如何在存在混杂因素的离线设置下，准确且高效地估计用于策略评估的“桥函数（Bridge Functions）”。研究背景基于近期的近端因果推断理论，旨在通过解决条件矩限制（CMR）问题来消除混杂偏差。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**交叉拟合（Cross-fitting）的两阶段桥函数估计框架**，其核心思想是将数据效率最大化并减少一阶段估计误差对二阶段目标的传播。
*   **核心思想**：将桥函数学习重新表述为一个包含“冗余对象（Nuisance Objects）”的 CMR 问题。冗余对象包括条件均值嵌入（CME）和条件密度。
*   **关键技术细节**：
    *   **K折交叉拟合**：将数据集分为 $K$ 份。在每一折中，使用其余 $K-1$ 份数据（辅助样本）估计冗余对象，而在当前折（留出样本）上评估桥函数准则。
    *   **两阶段流程**：
        *   **阶段 I（冗余估计）**：利用核方法（RKHS）进行正则化算子回归来估计条件均值嵌入，并使用条件似然学习器估计条件密度。
        *   **阶段 II（桥函数估计）**：在留出折上，通过最小化经验风险（包含蒙特卡洛近似的积分项和正则化项）来求解桥函数。
    *   **理论贡献**：推导了 **Oracle-Comparator 界限**，将总误差分解为阶段 I 的冗余估计误差和阶段 II 的经验过程误差。

#### 3. 实验设计
*   **场景与数据集**：采用了一个**2步混杂 POMDP 模拟环境**。该系统是一个低维非线性高斯 POMDP，包含二进制动作和隐藏的二进制上下文因子。
*   **Benchmark 与对比方法**：
    *   **基准方法**：单次样本分割（One-shot Sample-splitting，50-50 分割）的二阶段估计器。
    *   **对比目标**：评估观测-转移桥函数的恢复准确度，衡量指标为去混杂观测演化的均方误差（MSE）。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或训练时长。由于实验涉及的是低维非线性高斯系统和核方法，通常这类实验在标准工作站 CPU 或单块中端 GPU 上即可完成，计算开销相对较小。

#### 5. 实验数量与充分性
*   **实验规模**：实验展示了在不同样本量（$N=120$ 到 $N=1920$）下，交叉拟合与单次分割方法的性能对比。
*   **充分性评价**：实验数量相对**较少**。虽然展示了随样本量增加的性能提升趋势，但仅限于一个合成的低维 POMDP 环境。缺乏在更复杂、高维环境或真实世界数据集上的验证，也未进行针对 $K$ 值选择或不同核函数选择的消融实验。

#### 6. 主要结论与发现
*   **性能提升**：交叉拟合估计器在所有测试样本量下均显著优于单次样本分割方法。例如，在 $N=1920$ 时，MSE 从 0.0771 降至 0.0248。
*   **数据效率**：交叉拟合能更有效地利用可用数据，减少了估计器对单一样本划分的依赖。
*   **理论验证**：理论上的误差分解为理解两阶段学习中的误差传播提供了清晰的框架。

#### 7. 优点
*   **统计稳健性**：通过交叉拟合技术，有效缓解了第一阶段正则化偏差对第二阶段的影响。
*   **理论严谨**：提供了明确的 Oracle 误差界限分解，将复杂的桥函数学习问题纳入了现代双重机器学习（Double Machine Learning）的分析框架。
*   **通用性**：该方法不依赖于特定的桥函数形式，可扩展至多种满足 CMR 的估计问题。

#### 8. 不足与局限
*   **实验覆盖面窄**：仅在简单的合成环境上进行了验证，缺乏大规模深度强化学习任务的测试。
*   **正交性缺失**：作者承认尚未推导出 Neyman-正交得分（Neyman-orthogonal score），这意味着该估计器对第一阶段误差的敏感度虽然有所降低，但尚未达到理论上的最优鲁棒性。
*   **计算复杂度**：$K$ 折交叉拟合需要训练 $K$ 次模型，对于大规模深度学习模型，计算成本会显著增加。
*   **应用限制**：桥函数的存在性和完整性假设（Assumptions 2 & 3）在某些复杂现实系统中可能难以验证或满足。

（完）
