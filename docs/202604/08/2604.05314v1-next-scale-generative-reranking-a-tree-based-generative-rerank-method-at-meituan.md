---
title: "Next-Scale Generative Reranking: A Tree-based Generative Rerank Method at Meituan"
title_zh: 下一尺度生成式重排：美团的一种基于树的生成式重排方法
authors: "Shuli Wang, Changhao Li, Ke Fan, Senjie Kou Junwei Yin, Chi Wang, Yinhua Zhu, Haitao Wang, Xingxing Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05314v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 多阶段推荐系统中的生成式重排序方法
tldr: 本文针对推荐系统重排阶段，提出了一种名为NSGR的树状生成式重排框架。针对现有生成式方法缺乏全局与局部视角以及生成器与评估器目标不一致的问题，NSGR通过由粗到细的树状生成机制平衡多尺度信息，并利用多尺度评估器提供精准指导。该方法在公开及美团工业数据集上表现优异，并已成功在美团外卖平台上线应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05314v1/fig-001.webp\", \"caption\": \"Figure 6: Positional distribution of Top-10 items following NSGR.\", \"page\": 8, \"index\": 1, \"width\": 938, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.05314v1/fig-002.webp\", \"caption\": \"Figure 2: The overall architecture of NSGR.\", \"page\": 4, \"index\": 2, \"width\": 1006, \"height\": 442}]"
motivation: 现有生成式重排模型在生成策略上难以兼顾全局与局部视角，且训练时生成器与评估器存在目标不一致的挑战。
method: 提出NSGR框架，包含一个由粗到细扩展列表的树状生成器（NSG）和一个提供多尺度指导信号的评估器（MSE）。
result: 实验证明NSGR在多个数据集上均取得显著提升，并在美团外卖平台的实际业务中验证了其有效性。
conclusion: 通过引入多尺度树状生成与评估机制，NSGR有效解决了生成式重排的视角缺失与目标失配问题，具有极高的实用价值。
---

## 摘要
在现代多阶段推荐系统中，重排通过建模上下文信息发挥着至关重要的作用。由于组合空间复杂度等固有挑战，越来越多的方法采用生成式范式：生成器在推理过程中生成最优列表，而评估器在训练阶段指导生成器的优化。然而，这些方法仍面临两个问题。首先，无论生成策略是自回归还是非自回归，由于缺乏局部和全局视角，这些生成器都无法产生最优的生成结果。其次，训练过程中生成器与评估器之间的目标不一致问题使指导信号复杂化，从而导致性能欠佳。为了解决这些问题，我们提出了下一尺度生成重排（Next-Scale Generation Reranking, NSGR），这是一种基于树的生成式框架。具体而言，我们引入了一个下一尺度生成器（NSG），它以由粗到细的方式从用户兴趣中逐步扩展推荐列表，平衡了全局和局部视角。此外，我们设计了一种多尺度邻域损失，利用基于树的多尺度评估器（MSE）在每个尺度上为 NSG 提供特定尺度的指导。在公开和工业数据集上的广泛实验验证了 NSGR 的有效性。目前，NSGR 已成功部署在美团外卖平台。

## Abstract
In modern multi-stage recommendation systems, reranking plays a critical role by modeling contextual information. Due to inherent challenges such as the combinatorial space complexity, an increasing number of methods adopt the generative paradigm: the generator produces the optimal list during inference, while an evaluator guides the generator's optimization during the training phase. However, these methods still face two problems. Firstly, these generators fail to produce optimal generation results due to the lack of both local and global perspectives, regardless of whether the generation strategy is autoregressive or non-autoregressive. Secondly, the goal inconsistency problem between the generator and the evaluator during training complicates the guidance signal and leading to suboptimal performance. To address these issues, we propose the \textbf{N}ext-\textbf{S}cale \textbf{G}eneration \textbf{R}eranking (NSGR), a tree-based generative framework. Specifically, we introduce a next-scale generator (NSG) that progressively expands a recommendation list from user interests in a coarse-to-fine manner, balancing global and local perspectives. Furthermore, we design a multi-scale neighbor loss, which leverages a tree-based multi-scale evaluator (MSE) to provide scale-specific guidance to the NSG at each scale. Extensive experiments on public and industrial datasets validate the effectiveness of NSGR. And NSGR has been successfully deployed on the Meituan food delivery platform.

---

## 论文详细总结（自动生成）

这篇论文由美团技术团队提出，介绍了一种名为 **NSGR (Next-Scale Generative Reranking)** 的新型生成式重排框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
在推荐系统的重排（Reranking）阶段，目标是从候选集中选出最优的有序列表。现有生成式重排方法存在两大痛点：
*   **视角缺失问题**：自回归生成（逐个生成）缺乏全局视野，非自回归生成（一步到位）缺乏局部细节，多步迭代生成则容易陷入局部最优。
*   **目标不一致问题**：生成器（寻找最优排列）与评估器（估计列表得分）在训练时的优化目标不统一，导致评估器给出的指导信号难以有效提升生成器的性能。

### 2. 方法论：NSGR 框架
NSGR 采用了一种**基于树的由粗到细（Coarse-to-fine）**的生成策略，核心组件包括：

*   **下一尺度生成器 (NSG)**：
    *   **核心思想**：模拟“一分二、二分四”的树状扩展过程。从用户全局兴趣出发，逐步将候选集划分为更小的子集，直到确定每个位置的具体物品。
    *   **关键技术**：引入物品优先级评分、配对关系分类（竞争抑制、互补增强、中性）、以及基于集合上下文的物品表征精炼。通过“物品-树注意力”机制计算相关性得分，实现二叉分裂。
*   **多尺度评估器 (MSE)**：
    *   利用 **HSTU (Hierarchical Sequential Transduction Unit)** 提取用户长短期兴趣。
    *   采用多尺度自注意力机制捕捉列表在不同粒度下的上下文关系，为生成器提供精准的价值评估。
*   **多尺度邻域损失 (MSNL)**：
    *   为了解决目标不一致问题，NSGR 构造了多个尺度的“邻域列表”（通过交换位置或替换物品）。
    *   利用 MSE 作为奖励模型，通过对比生成列表与邻域列表的相对得分，指导 NSG 在树的每个节点（尺度）上进行优化。

### 3. 实验设计
*   **数据集**：
    *   **Taobao Ad**：公开数据集，包含 2600 万条交互记录。
    *   **Meituan**：美团外卖真实工业数据集，包含 2.42 亿条记录，涉及 239 个特征。
*   **对比方法 (Baselines)**：
    *   **自回归类**：PRM, GRN。
    *   **一步生成类**：NAR4Rec。
    *   **多步迭代类**：DCDR, NLGR。
    *   **评估器类**：YOLOR。
*   **评估指标**：离线使用 AUC、GAUC、HR (Hit Ratio)；在线使用 CTR、CVR、GMV 及耗时（ms）。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100-80GB GPU** 进行训练。
*   **软件框架**：基于 TensorFlow 1.15.0 实现。
*   **细节**：Batch size 为 512，Embedding 维度为 8。文中未明确给出具体的训练总时长，但提到通过预计算用户向量（e_u）和上下文缓存（Context Cache）来优化推理效率。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了离线对比实验、消融实验、超参数敏感性分析（如温度系数 $\tau$ 和采样比例 $\beta$）以及长达 8 周的在线 A/B 测试。
*   **充分性与客观性**：
    *   在 $A_{20}^{20}$ 极大的排列空间下进行了 HR 估算，并在 $A_{8}^{8}$ 小空间下进行了全排列穷举对比，验证了生成器接近最优解的能力。
    *   所有实验重复 5 次取平均值，确保了结果的可靠性。
    *   消融实验分别验证了语义 ID (SID)、多尺度评估单元 (MSEU)、生成单元 (NSGU) 和邻域损失 (MSNL) 的必要性，逻辑严密。

### 6. 主要结论与发现
*   **性能领先**：NSGR 在公开和工业数据集上的 AUC/GAUC 均显著优于现有 SOTA 模型。
*   **一致性强**：NSGR 生成的列表在命中率（HR）上表现优异，证明其生成器能高度还原评估器的偏好。
*   **业务价值**：在美团外卖上线后，相比基线模型，**CTR 提升了 2.89%，GMV 提升了 3.15%**，且推理耗时有所下降（得益于树状结构和缓存机制）。

### 7. 优点与亮点
*   **结构创新**：首次将“下一尺度（Next-Scale）”生成范式引入重排任务，巧妙平衡了全局视角与局部细节。
*   **解决痛点**：通过多尺度邻域损失有效缓解了生成器与评估器之间的目标失配问题。
*   **工业落地**：方案考虑了在线推理延迟，通过复用排序模型特征和树状并行计算，实现了高性能部署。

### 8. 不足与局限
*   **复杂度**：树状生成机制相比简单的非自回归模型在逻辑实现上更为复杂。
*   **列表长度限制**：实验主要针对长度为 8 或 20 的列表。对于极长列表（如信息流无限滚动），树的深度增加可能会带来额外的计算开销或误差累积。
*   **冷启动依赖**：模型高度依赖用户行为序列提取的语义 ID，对于行为稀疏的新用户，提升效果可能受限。

（完）
