---
title: "Curr-RLCER:Curriculum Reinforcement Learning For Coherence Explainable Recommendation"
title_zh: Curr-RLCER：面向连贯可解释推荐的课程强化学习
authors: "Xiangchen Pan, Wei Wei"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05341v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于解释连贯推荐的强化学习框架
tldr: 本研究针对可解释推荐系统中评分预测与解释生成不一致的问题，提出了Curr-RLCER框架。该框架结合了课程强化学习，通过从基础点击率预测到开放式解释生成的循序渐进训练，提升了系统的稳定性。核心贡献在于引入了一致性驱动的奖励机制，确保生成的解释与预测评分逻辑相符。实验表明，该方法在多个数据集上均取得了显著效果，有效增强了推荐系统的透明度与可信度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05341v1/fig-001.webp\", \"caption\": \"Table 3: Rating Prediction Performance Comparison Across Datasets. “R” means RMSE, and “M” means MAE.\", \"page\": 11, \"index\": 1, \"width\": 708, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.05341v1/fig-002.webp\", \"caption\": \"Table 2: Explainability and Stability results on Three Datasets. \\\"↑\\\" means higher is better; \\\"↓\\\" means lower is better. Superscripts “P,” “R,” and “F1” denote Precision, Recall, and F1-Score, respectively. The subscript “std” indicates the standard deviation of each metric. Bold indicates the best results, while underlined denotes the second-best. (The significant improvements with p-value < 0.05 based on paired t-tests)\", \"page\": 10, \"index\": 2, \"width\": 719, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2604.05341v1/fig-003.webp\", \"caption\": \"Fig. 1: The overview framework of Curr-RLCER.\", \"page\": 4, \"index\": 3, \"width\": 719, \"height\": 465}]"
motivation: 解决现有可解释推荐系统中评分预测与解释生成目标不一致、缺乏逻辑连贯性的挑战。
method: 采用课程强化学习策略，结合动态评分对齐和专门设计的一致性驱动奖励机制来优化模型。
result: 在三个公开的可解释推荐数据集上进行的广泛实验验证了所提框架的优越性能。
conclusion: 该研究证明了通过课程学习和一致性约束可以有效解决推荐系统中的解释冲突问题，提升了系统的稳定性与可信度。
---

## 摘要
可解释推荐系统 (RSs) 旨在显式揭示每项推荐背后的逻辑，从而增强推荐系统的透明度和可信度。以往的方法通常联合预测评分并生成解释，但忽略了这两个目标之间的不连贯性。为了解决这一问题，我们提出了 Curr-RLCER，这是一个具有动态评分对齐功能的强化学习框架，用于实现解释连贯的推荐。该框架采用课程学习，从基础预测（即点击率-CTR、基于选择的评分）过渡到开放式的推荐解释生成。特别地，每个阶段的奖励旨在逐步增强推荐系统的稳定性。此外，还提出了一种连贯性驱动的奖励机制，通过专门设计的评估方案，强制生成的解释与预测的评分之间保持连贯。在三个可解释推荐数据集上的广泛实验结果表明，所提出的框架是有效的。代码和数据集可在 https://github.com/pxcstart/Curr-RLCER 获取。

## Abstract
Explainable recommendation systems (RSs) are designed to explicitly uncover the rationale of each recommendation, thereby enhancing the transparency and credibility of RSs. Previous methods often jointly predicted ratings and generated explanations, but overlooked the incoherence of such two objectives. To address this issue, we propose Curr-RLCER, a reinforcement learning framework for explanation coherent recommendation with dynamic rating alignment. It employs curriculum learning, transitioning from basic predictions (i.e., click through rating-CTR, selection-based rating) to open-ended recommendation explanation generation. In particular, the rewards of each stage are designed for progressively enhancing the stability of RSs. Furthermore, a coherence-driven reward mechanism is also proposed to enforce the coherence between generated explanations and predicted ratings, supported by a specifically designed evaluation scheme. The extensive experimental results on three explainable recommendation datasets indicate that the proposed framework is effective. Codes and datasets are available at https://github.com/pxcstart/Curr-RLCER.

---

## 论文详细总结（自动生成）

以下是对论文《Curr-RLCER: Curriculum Reinforcement Learning For Coherence Explainable Recommendation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
可解释推荐系统（RS）不仅要提供精准的推荐，还需给出合理的解释以增强透明度和用户信任。然而，现有方法（如多任务学习）在**评分预测**与**解释生成**之间往往存在**不连贯性**（Incoherence）——例如，系统给出了高评分，但生成的解释文本却包含负面评价。此外，现有模型高度依赖高质量的监督数据，且在处理多任务时容易出现“灾难性遗忘”。本研究旨在通过强化学习（RL）和课程学习（Curriculum Learning）解决这些矛盾，确保推荐逻辑的一致性。

### 2. 方法论：核心思想与技术细节
论文提出了 **Curr-RLCER** 框架，其核心思想是将推荐任务分解为由易到难的三个阶段，并利用强化学习进行对齐：

*   **核心算法**：采用了 **GRPO**（群组相对策略优化，借鉴自 DeepSeek R1）和 **DPO**（直接偏好优化）。GRPO 通过组内相对表现评估来消除对额外 Critic 模型的依赖。
*   **三阶段课程学习流程**：
    1.  **阶段1：判断（CTR预测）**：让模型判断用户是否喜欢某商品（Yes/No），使其适应推荐数据。
    2.  **阶段2：选择（评分预测）**：让模型从1-5分中选择最合适的评分，强化其对用户偏好程度的量化能力。
    3.  **阶段3：生成（解释生成）**：
        *   **Phase 1 (DPO)**：利用 GPT-3.5 生成的高质量解释作为正例进行偏好对齐，让模型学会生成流畅文本。
        *   **Phase 2 (GRPO)**：引入多重奖励函数进行微调。
*   **关键奖励机制**：
    *   **连贯性奖励（Coherence Reward）**：使用预训练的情感分类器对生成的解释进行打分，计算该分数与预测评分之间的差异，强制两者逻辑一致。
    *   **准确性与格式奖励**：确保模型遵循特定的推理格式（`<think>` 和 `<answer>` 标签）并与 Ground Truth 语义接近。

### 3. 实验设计
*   **数据集**：使用了 Amazon Review Dataset 的三个子集：**Baby**、**Sports** 和 **Clothing**。
*   **Benchmark 与对比方法**：
    *   **传统深度学习模型**：Att2Seq、NRT、PETER、CER（专门解决连贯性的前沿模型）。
    *   **大语言模型方法**：XRec（基于 GNN+LLM）、以及原始的 Qwen2.5-7B/14B 指令微调模型。
*   **评估指标**：
    *   **解释质量**：GPTscore、BERTscore、BARTscore、BLEURT、USR（唯一性）。
    *   **评分预测**：RMSE、MAE。
    *   **连贯性评估**：自定义 Acc 指标，结合了评分准确度与解释-评分一致性。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA RTX-4090 GPU**。
*   **模型基座**：主要实验基于 **Qwen2.5-3B-Instruct**。
*   **训练时长**：每个阶段的训练时间约为 **12 小时**（总计约 36 小时），推理 1000 条测试样本约需 0.5 小时。

### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **多维度对比**：在三个不同领域的数据集上验证了生成质量、评分精度和连贯性。
*   **消融实验**：分别验证了 DPO 预训练和连贯性奖励机制的必要性。
*   **鲁棒性实验**：通过对输入指令添加不同比例的噪声（随机掩码），对比了 Curr-RLCER 与 XRec 的抗干扰能力。
*   **人工评估**：引入了 5 名研究人员进行人工标注，增强了评估的客观性。

### 6. 主要结论与发现
*   **连贯性显著提升**：Curr-RLCER 在所有数据集上的连贯性指标均大幅超过 CER 等基准模型。
*   **评分预测更准**：相比于专门的评分预测模型，该框架在 RMSE 和 MAE 上有 27%~60% 的显著改进。
*   **小模型胜大模型**：经过课程强化学习训练的 3B 模型在解释质量和评分准确性上，甚至优于未经针对性训练的 7B 和 14B 模型。
*   **鲁棒性强**：在输入信息受损（噪声增加）时，强化学习训练出的模型比基于 SFT 的模型表现更稳定。

### 7. 优点（亮点）
*   **创新的训练策略**：首次将 GRPO 和课程学习引入可解释推荐，有效缓解了多任务学习中的冲突和遗忘问题。
*   **闭环的连贯性约束**：通过情感分类器将文本解释转化为标量评分，并将其作为 RL 奖励，实现了端到端的逻辑对齐。
*   **评估体系完善**：不仅关注文本相似度，还通过 GPT-4、情感分类器和人工三方验证连贯性，解决了以往评估维度单一的问题。

### 8. 不足与局限
*   **计算开销**：虽然推理较快，但三阶段的强化学习训练过程相对耗时且对显存有一定要求。
*   **外部工具依赖**：连贯性奖励依赖于预训练的情感分类器，若分类器本身存在领域偏差，可能会影响奖励信号的质量。
*   **冷启动挑战**：实验主要基于有丰富评论的数据集，对于缺乏历史评论的冷启动场景，生成高质量解释的难度依然较大。

（完）
