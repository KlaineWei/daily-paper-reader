---
title: "CURE:Circuit-Aware Unlearning for LLM-based Recommendation"
title_zh: CURE：面向基于大语言模型推荐的电路感知机器遗忘
authors: "Ziheng Chen, Jiali Cheng, Zezhong Fan, Hadi Amiri, Yunzhi Yao, Xiangguo Sun, Yang Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04982v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 基于大语言模型推荐系统的电路感知遗忘学习
tldr: 针对大语言模型推荐系统（LLMRec）中的隐私风险，本文提出了CURE框架。该框架通过识别模型中负责特定任务的“电路”（计算子图），将模型组件解耦为遗忘特定、保留特定和任务共享组。通过对不同组应用特定的更新规则，CURE有效解决了传统卸载方法中的梯度冲突问题，在保证推荐性能的同时实现了更彻底的隐私擦除，提升了卸载过程的透明度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04982v1/fig-001.webp\", \"caption\": \"Figure 3: Unlearning effectivenss and model performance on Movie and Goodreads.\", \"page\": 7, \"index\": 1, \"width\": 1056, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2604.04982v1/fig-002.webp\", \"caption\": \"Table 1: Unlearning time comparison (s) ↓.\", \"page\": 8, \"index\": 2, \"width\": 474, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.04982v1/fig-003.webp\", \"caption\": \"Figure 2: Schematics of CURE: (i) Locating Circuits in LLMREC; (ii)Selective Circuits Updating\", \"page\": 4, \"index\": 3, \"width\": 1056, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2604.04982v1/fig-004.webp\", \"caption\": \"Figure 4: Transparent comparison of circuit-aware unlearning with ROME and WISE\", \"page\": 9, \"index\": 4, \"width\": 506, \"height\": 417}]"
motivation: 现有的LLMRec卸载方法因统一更新参数导致遗忘与保留目标之间存在梯度冲突，且缺乏透明度。
method: 提出电路感知卸载框架，通过提取核心电路并根据功能将模块分类，实施差异化的更新策略以缓解冲突。
result: 在真实数据集上的实验证明，CURE在卸载效果和模型效用保持方面均优于现有基准方法。
conclusion: CURE通过电路级的精细化管理，为LLMRec提供了一种更稳定、透明且高效的隐私保护卸载方案。
---

## 摘要
大语言模型（LLMs）的最新进展通过实现对用户兴趣和物品属性的丰富语义理解与推理，为推荐系统开辟了新的机遇。然而，随着隐私法规的收紧，将用户数据纳入基于大语言模型的推荐（LLMRec）引入了显著的隐私风险，使得机器遗忘（unlearning）算法对于实际部署变得日益关键。尽管对 LLMRec 机器遗忘的关注日益增加，但大多数现有方法将遗忘过程表述为遗忘目标和保留目标的加权组合，并以统一的方式更新模型参数。这种表述不可避免地导致两个目标之间产生梯度冲突，从而引起优化不稳定，并导致遗忘无效或模型效用严重下降。此外，遗忘过程在很大程度上仍是一个黑盒，削弱了其透明度和可信度。为了应对这些挑战，我们提出了 CURE，一个电路感知的机器遗忘框架，它将模型组件解耦为功能不同的子集并选择性地更新它们。在这里，电路是指对特定任务行为具有因果责任的计算子图。具体而言，我们提取了物品推荐背后的核心电路，并分析了这些电路中的各个模块如何对遗忘和保留目标做出贡献。基于这一分析，这些模块被分为遗忘特定组、保留特定组和任务共享组，每组都遵循特定的更新规则，以减轻遗忘过程中的梯度冲突。在真实世界数据集上的实验表明，我们的方法比现有基准实现了更有效的遗忘。

## Abstract
Recent advances in large language models (LLMs) have opened new opportunities for recommender systems by enabling rich semantic understanding and reasoning about user interests and item attributes. However, as privacy regulations tighten, incorporating user data into LLM-based recommendation (LLMRec) introduces significant privacy risks, making unlearning algorithms increasingly crucial for practical deployment. Despite growing interest in LLMRec unlearning, most existing approaches formulate unlearning as a weighted combination of forgetting and retaining objectives while updating model parameters in a uniform manner. Such formulations inevitably induce gradient conflicts between the two objectives, leading to unstable optimization and resulting in either ineffective unlearning or severe degradation of model utility. Moreover, the unlearning procedure remains largely black-box, undermining its transparency and trustworthiness. To tackle these challenges, we propose CURE, a circuit-aware unlearning framework that disentangles model components into functionally distinct subsets and selectively updates them. Here, a circuit refers to a computational subgraph that is causally responsible for task-specific behaviors. Specifically, we extract the core circuits underlying item recommendation and analyze how individual modules within these circuits contribute to the forget and retain objectives. Based on this analysis, these modules are categorized into forget-specific, retain-specific, and task-shared groups, each subject to function-specific update rules to mitigate gradient conflicts during unlearning. Experiments on real-world datasets show that our approach achieves more effective unlearning than existing baselines.

---

## 论文详细总结（自动生成）

### 论文总结：CURE —— 面向基于大语言模型推荐的电路感知机器遗忘

#### 1. 核心问题与整体含义
随着隐私法规（如 GDPR）的收紧，如何从已训练的基于大语言模型的推荐系统（LLMRec）中彻底删除特定用户数据（即“机器遗忘”）成为关键挑战。
*   **研究动机**：现有的 LLMRec 遗忘方法通常将“遗忘目标”和“保留目标”进行简单的加权优化。这种做法会导致**梯度冲突**（优化遗忘会损害模型性能，反之亦然），且遗忘过程如同“黑盒”，缺乏透明度和可解释性。
*   **核心目标**：提出一种透明且能缓解梯度冲突的框架，通过识别模型内部负责特定任务的“计算电路”来实现精准遗忘。

#### 2. 方法论
CURE 框架分为两个主要阶段：
*   **阶段一：电路定位（Locating Circuits）**
    *   **核心思想**：将 LLM 视为有向无环图，识别出对推荐任务起关键作用的神经元连接（电路）。
    *   **技术细节**：
        *   **激活干预（AI）**：利用一阶泰勒展开快速估计各条边对预测结果的贡献，实现粗粒度的电路定位。
        *   **激活修补（AP）**：通过构造“损坏样本”进行对比。利用**个性化 PageRank (PPR)** 算法在用户-物品图上寻找与目标物品语义接近但偏好较弱的替代品，通过对比激活差异精确定位电路。
*   **阶段二：选择性电路更新（Selective Updating）**
    *   根据模块在电路中的角色将其分为三类：
        1.  **遗忘特定神经元 ($\Theta_f$)**：仅在遗忘电路上活跃，只根据遗忘损失更新。
        2.  **保留特定神经元 ($\Theta_r$)**：仅在保留电路上活跃，只根据保留损失更新。
        3.  **任务共享神经元 ($\Theta_{sh}$)**：在两者上均活跃。采用**梯度投影策略**，当两个目标的梯度方向相反时，将一个梯度投影到另一个梯度的法平面上，消除冲突。

#### 3. 实验设计
*   **数据集**：MovieLens-1M (ML-1M) 和 GoodReads (GD)。
*   **骨干模型（Backbones）**：GPT-2 Large 和 Llama-2 (7B)。
*   **Benchmark 与对比方法**：
    *   **金标准**：Retrain（完全重训练）。
    *   **传统遗忘/分区方法**：SISA, RecEraser。
    *   **LLM 专用遗忘方法**：E2URec。
    *   **模型编辑方法**：ROME, WISE。
    *   **梯度优化方法**：PCGrad。
*   **评估指标**：推荐效用（AUC, ACC, LogLoss）、遗忘有效性（JSD 散度，越低越接近重训练效果）、遗忘效率（耗时）。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100 GPU**。
*   **技术实现**：针对 Llama-2 (7B) 使用了 **LoRA (rank=8)** 技术以降低显存占用并提高微调效率。
*   **训练时长**：文中对比了各方法的耗时（见 Table 1）。例如在 Llama-2 上，CURE 的耗时约为 4000-11000 秒，虽然比简单的模型编辑（ROME）略慢，但比 E2URec 快 3.5 倍，比完全重训练快 10 倍以上。

#### 5. 实验数量与充分性
*   **实验规模**：在 2 个数据集和 2 种不同架构、不同参数规模的 LLM 上进行了交叉验证。
*   **消融与分析**：
    *   进行了 $\omega_r$ 权重影响的消融实验。
    *   提供了梯度冲突分布的定量分析（cos $\psi$ 分布图）。
    *   通过可视化展示了电路定位的透明度。
*   **充分性评价**：实验设计较为严谨，不仅关注了推荐准确率，还通过 JSD 指标深入探讨了遗忘的彻底性，对比基准涵盖了从传统重训练到最新模型编辑的各类前沿技术，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：CURE 在保持推荐效用的同时，实现了最接近重训练效果的遗忘（JSD 最低）。
*   **效率提升**：相比主流基准 E2URec，遗忘效率提升了 18%，推荐效用提升了 6%，且运行速度快 3.5 倍。
*   **冲突缓解**：定量分析证明 CURE 能减少 55%-76% 的严重梯度冲突，使优化过程更稳定。
*   **可解释性**：证明了 LLM 内部确实存在特定电路负责特定类别的推荐（如特定流派的电影），这为透明化遗忘提供了物理基础。

#### 7. 优点
*   **理论与实践结合**：提供了数学证明（Theorem 5.1），确保了电路感知更新在理论上的优越性。
*   **透明度高**：打破了遗忘过程的“黑盒”状态，能够明确指出哪些模块被更新以及为什么要更新。
*   **模型无关性**：该框架不依赖于特定的 LLM 架构，具有较强的通用性。

#### 8. 不足与局限
*   **图结构依赖**：在激活修补阶段，构造损坏样本高度依赖于用户-物品的图结构信息，对于缺乏显式图结构的推荐场景（如纯文本流推荐）可能需要额外转换。
*   **计算开销**：虽然比迭代剪枝快，但电路定位过程（尤其是激活修补）仍比直接进行梯度上升的遗忘方法多出了一定的计算步骤。
*   **超参数调优**：电路提取的阈值选择和梯度投影的优先级设定可能需要针对不同任务进行精细化调整。

（完）
