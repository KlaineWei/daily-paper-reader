---
title: Leveraging LLMs and Heterogeneous Knowledge Graphs for Persona-Driven Session-Based Recommendation
title_zh: 利用大语言模型与异构知识图谱实现画像驱动的会话式推荐
authors: "Muskan Gupta, Suraj Thapa, Jyotsana Khatri"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06928v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 大模型增强的会话式推荐
tldr: 针对会话式推荐系统（SBRS）中匿名会话导致个性化不足的问题，本文提出一种结合大语言模型（LLM）和异构知识图谱（KG）的画像驱动框架。该框架通过两阶段架构，首先利用LLM生成的嵌入初始化KG并学习潜在用户画像，随后将画像融入数据驱动的推荐流程中。实验证明，该方法在处理稀疏数据和冷启动问题上表现优异，显著提升了推荐的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06928v1/fig-001.webp\", \"caption\": \"Fig. 1. An overview of our proposed framework: Persona-driven session based recommendation via knowledge graph\", \"page\": 6, \"index\": 1, \"width\": 621, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.06928v1/fig-002.webp\", \"caption\": \"Table 2. Prompts used for user persona generation.\", \"page\": 11, \"index\": 2, \"width\": 725, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2604.06928v1/fig-003.webp\", \"caption\": \"Table 3. Performance comparison of user representation strategies used in the retriever module\", \"page\": 13, \"index\": 3, \"width\": 712, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2604.06928v1/fig-004.webp\", \"caption\": \"Fig. 2. Construction of KG\", \"page\": 9, \"index\": 4, \"width\": 682, \"height\": 304}]"
motivation: 传统的会话式推荐系统通常假设会话是匿名的，难以在数据稀疏或冷启动情况下捕捉用户的个性化偏好。
method: 提出一种两阶段框架，利用LLM生成的嵌入初始化异构知识图谱，通过无监督学习提取用户画像，并将其整合进改进的SBRS架构中进行候选生成与重排序。
result: "在Amazon Books和Amazon Movies & TV数据集上的实验结果显示，该方法在各项指标上均优于仅依赖序列建模或文本表示的基准模型。"
conclusion: 结合结构化关系信号与LLM的语义能力，能有效建模潜在用户画像，从而显著增强会话式推荐系统的个性化性能。
---

## 摘要
会话式推荐系统（SBRS）旨在从交互序列中捕捉用户的短期意图。然而，匿名会话的普遍假设限制了其个性化能力，尤其是在数据稀疏或冷启动场景下。近期大语言模型（LLM）增强推荐的研究进展表明，LLM 能够生成丰富的物品表示，但受限于匿名会话，利用 LLM 建模用户画像（User Personas）仍具挑战。本研究提出了一个画像驱动的 SBRS 框架，该框架显式建模了从异构知识图谱（KG）中推断出的潜在用户画像，并将其整合到数据驱动的推荐流程中。受近期思维链推荐方法的启发，我们的框架采用了包含个性化信息提取和个性化信息利用的两阶段架构。在个性化信息提取阶段，我们构建了一个异构知识图谱，整合了与时间无关的用户-物品、物品-物品、物品-特征关联以及来自 DBpedia 的元数据。随后，我们在由 LLM 衍生物品嵌入初始化的知识图谱上，利用异构深度图互信息（HDGI）目标以无监督方式学习潜在用户画像。在个性化信息利用阶段，学习到的画像表示与 LLM 衍生物品嵌入被共同引入改进的数据驱动 SBRS 架构中，以生成相关物品候选集，并由基础序列模型进行重排序以强化短期会话意图。不同于以往仅依赖序列建模或基于文本的用户表示的方法，本方法将用户画像建模建立在源自知识图谱的结构化关系信号之上。在 Amazon Books 和 Amazon Movies & TV 数据集上的实验证明，我们的方法相较于利用会话历史生成用户嵌入的序列模型具有持续的性能提升。

## Abstract
Session-based recommendation systems (SBRS) aim to capture user's short-term intent from interaction sequences. However, the common assumption of anonymous sessions limits personalization, particularly under sparse or cold-start conditions. Recent advances in LLM-augmented recommendation have shown that LLMs can generate rich item representations, but modeling user personas with LLMs remains challenging due to anonymous sessions. In this work, we propose a persona-driven SBRS framework that explicitly models latent user personas inferred from a heterogeneous knowledge graph (KG) and integrates them into a data-driven recommendation pipeline.Our framework adopts a two-stage architecture consisting of personalized information extraction and personalized information utilization, inspired by recent chain-of-thought recommendation approaches. In the personalized information extraction stage, we construct a heterogeneous KG that integrates time-independent user-item, item-item, item-feature association, and metadata from DBpedia. We then learn latent user personas in an unsupervised manner using a Heterogeneous Deep Graph Infomax (HDGI) objective over a KG initialized with LLM-derived item embeddings. In the personalized information utilization stage, the learned persona representations together with LLM-derived item embeddings are incorporated into a modified architecture of data-driven SBRS to generate a candidate set of relevant items, followed by reranking using the base sequential model to emphasize short-term session intent. Unlike prior approaches that rely solely on sequence modeling or text-based user representations, our method grounds user persona modeling in structured relational signals derived from a KG. Experiments on Amazon Books and Amazon Movies & TV demonstrate that our approach consistently improves over sequential models with user embeddings derived using session history.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合大语言模型（LLM）与异构知识图谱（HKG）的画像驱动会话式推荐框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的会话式推荐系统（SBRS）通常假设会话是匿名的，仅关注短期交互序列。这导致系统难以捕捉用户跨会话的稳定偏好（即“用户画像”），在数据稀疏或冷启动场景下表现较差。
*   **研究动机**：
    *   **LLM 的局限性**：虽然 LLM 擅长处理物品语义，但直接用 LLM 建模用户画像计算成本高，且在缺乏结构化约束时容易产生幻觉。
    *   **KG 的优势**：异构知识图谱能提供结构化的关系信号（如导演、类别、品牌等），有助于推断稳定的用户偏好。
    *   **结合点**：论文旨在融合 LLM 的语义表达能力与 KG 的结构化推理能力，以解决匿名会话中的个性化缺失问题。

### 2. 方法论
该框架采用两阶段架构，灵感源自思维链（CoT）推荐方法：
*   **阶段一：个性化信息提取（Personalized Information Extraction）**
    *   **KG 构建与初始化**：构建包含用户、物品、属性（来自 DBpedia）的异构图。利用 LLM（如 Qwen-3-8B）对物品标题和描述进行编码，初始化物品节点嵌入；用户节点则随机初始化。
    *   **无监督画像学习**：采用**异构深度图互信息（HDGI）**目标。通过元路径（Meta-path）聚合邻域信息，并利用对比学习最大化局部节点表示与全局图摘要之间的互信息，从而在不依赖特定任务标签的情况下学习到稳定的用户画像嵌入。
*   **阶段二：个性化信息利用（Personalized Information Utilization）**
    *   **候选检索**：将学习到的用户画像嵌入与 LLM 衍生的物品语义嵌入注入到改进的数据驱动推荐模型中，生成候选物品集。
    *   **知识感知重排序**：使用基础序列模型（如 SASRec）对候选集进行重排序，以重新强调会话内的短期意图，确保推荐既符合长期画像又符合当前上下文。

### 3. 实验设计
*   **数据集**：使用了 Amazon 平台的两个公开数据集：**Amazon Books** 和 **Amazon Movies & TV**。这些数据集包含丰富的物品元数据和用户交互序列。
*   **Benchmark 与对比方法**：
    *   **None**：不使用用户画像。
    *   **Random**：用户画像随机初始化并在训练中学习。
    *   **LLM**：直接使用 LLM 根据交互历史生成的文本摘要作为画像。
    *   **KG-LLM（本文方法）**：基于异构 KG 和 HDGI 学习的画像。
*   **评估指标**：Hit Rate (HR@K), NDCG@K, Mean Reciprocal Rank (MRR)。

### 4. 资源与算力
*   **模型使用**：论文明确提到使用了 **Qwen-3-8B** 进行文本生成和描述提取，使用 **Qwen3-Embedding-8B** 生成文本嵌入。
*   **算力细节**：**文中未明确说明**具体的 GPU 型号、数量、训练总时长或内存消耗。仅提到了超参数设置（如 Batch Size 为 128-1024，维度为 64-256 等）。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域的标准数据集上进行了测试。
*   **对比维度**：对比了四种不同的用户表示策略，并分别测试了重排序（Reranking）前后的性能变化。
*   **充分性评价**：实验设计较为合理，涵盖了从“无画像”到“纯 LLM 画像”再到“KG 增强画像”的递进对比。然而，实验主要集中在 Amazon 生态系统内，缺乏对其他非电商场景（如新闻或短视频）的验证。

### 6. 主要结论与发现
*   **画像的有效性**：引入 KG 驱动的用户画像显著提升了推荐性能，尤其是在 HR@100 等检索指标上表现突出。
*   **KG vs LLM**：基于 KG 学习的画像在捕捉长期偏好方面优于纯 LLM 生成的文本画像，证明了结构化关系信号的价值。
*   **重排序的作用**：虽然画像提升了候选集的召回质量，但通过 SASRec 进行重排序能进一步优化 NDCG 和 MRR，说明短期意图与长期画像的结合至关重要。

### 7. 优点与亮点
*   **架构创新**：将无监督图表示学习（HDGI）与 LLM 语义初始化相结合，既保留了文本深度语义，又利用了图的结构化推理。
*   **解决冷启动**：通过外部知识（DBpedia）和异构关系，为匿名或交互稀疏的会话提供了更强的先验信息。
*   **效率平衡**：相比于全流程调用 LLM 推理，该方法通过预计算 KG 嵌入来降低在线推理的计算负担。

### 8. 不足与局限
*   **算力信息缺失**：未提供训练和推理的实际开销，难以评估其在工业界大规模部署的成本效益。
*   **数据集单一**：仅限于 Amazon 评论数据集，其用户行为模式可能具有特定倾向。
*   **实时性挑战**：异构 KG 的构建和 HDGI 的预训练过程较为复杂，对于实时更新频繁的推荐场景，如何快速更新用户画像嵌入仍是一个挑战。

（完）
