---
title: "Soft MPCritic: Amortized Model Predictive Value Iteration"
title_zh: Soft MPCritic：摊销式模型预测价值迭代
authors: "Thomas Banker, Nathan P. Lawrence, Ali Mesbah"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01477v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 结合强化学习与模型预测控制进行长程规划
tldr: 本研究提出 Soft MPCritic 框架，旨在解决强化学习与模型预测控制在大规模结合时的计算难题。该框架利用 MPPI 进行在线控制和价值目标生成，并通过拟合价值迭代训练终端 Q 函数以隐式延长规划时域。通过引入摊销预热策略复用动作序列，显著提升了计算效率。实验证明，该方法在经典及复杂控制任务中表现优异，为合成鲁棒且可扩展的 MPC 策略提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01477v1/fig-001.webp\", \"caption\": \"Fig. 4: Cumulative reward for softMPCritic with uniform prior and RL baselines (SAC and DDPG). Lines represent the median of 10 seeds, shading percentiles. The horizontal dashed lines corresponds to SAC and DDPG after 106 time steps.\", \"page\": 6, \"index\": 1, \"width\": 476, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.01477v1/fig-002.webp\", \"caption\": \"Fig. 3: Ablation of softMPCritic with Gaussian prior. Left: using only a dynamics model ensemble (f Ensemble), terminal Q-function (Q), or both. Right: using MPPI only for control, Q-function targets, or both. Lines represent the median of 10 seeds, shading percentiles.\", \"page\": 6, \"index\": 2, \"width\": 484, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.01477v1/fig-003.webp\", \"caption\": \"Fig. 2: Performance of warm and cold starting MPPI target computations when utilizing a single or ensemble of dynamic models f . Lines represent the median of 10 seeds, shading percentiles. The horizontal dashed line corresponds to SAC.\", \"page\": 5, \"index\": 3, \"width\": 483, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2604.01477v1/fig-004.webp\", \"caption\": \"Fig. 1: softMPCritic effectively combines key approximations of RL and MPPI for autonomous, self-improvement. During online control, the value-space planner updates to initial open-loop actions υ to align with the soft minimum of value function Qϕ. The updated sequence υ+, along with transition (s, a, ℓ, s′), is stored in the replay buffer to warmstart the planner’s value targets VMPPI\", \"page\": 3, \"index\": 4, \"width\": 500, \"height\": 331}]"
motivation: 旨在解决强化学习与模型预测控制在大规模应用中结合困难且计算开销大的问题。
method: 结合基于样本的 MPPI 规划与终端 Q 函数训练，并采用摊销预热策略复用动作序列以加速计算。
result: 在经典和复杂控制任务中实现了高效学习，证明了短时域规划配合学习价值函数的有效性。
conclusion: Soft MPCritic 为在策略提取或长时域规划失效的场景下合成高效、可扩展的 MPC 策略提供了实用蓝图。
---

## 摘要
强化学习（RL）与模型预测控制（MPC）具有互补的优势，但在大规模应用中将两者结合仍面临计算上的挑战。我们提出了 Soft MPCritic，这是一个 RL-MPC 框架，它在（软）价值空间中进行学习，同时利用基于采样的规划进行在线控制和价值目标生成。Soft MPCritic 通过模型预测路径积分控制（MPPI）实例化 MPC，并利用拟合价值迭代训练终端 Q 函数，使学习到的价值函数与规划器保持一致，并隐式地延长了有效规划时域。我们引入了一种摊销式热启动策略，在计算基于 MPPI 的批量价值目标时，回收来自在线观测的已规划开环动作序列。这使得 Soft MPCritic 在保持解的质量的同时，在计算上具有可行性。Soft MPCritic 以基于情景的方式进行规划，并使用一组为提高下一步预测精度而训练的动力学模型集成。这些要素共同使 Soft MPCritic 能够通过在经典和复杂控制任务上的鲁棒短时域规划进行有效学习。这些结果证明了 Soft MPCritic 是一个实用且可扩展的蓝图，适用于在策略提取和直接长时域规划可能失效的场景下合成 MPC 策略。

## Abstract
Reinforcement learning (RL) and model predictive control (MPC) offer complementary strengths, yet combining them at scale remains computationally challenging. We propose soft MPCritic, an RL-MPC framework that learns in (soft) value space while using sample-based planning for both online control and value target generation. soft MPCritic instantiates MPC through model predictive path integral control (MPPI) and trains a terminal Q-function with fitted value iteration, aligning the learned value function with the planner and implicitly extending the effective planning horizon. We introduce an amortized warm-start strategy that recycles planned open-loop action sequences from online observations when computing batched MPPI-based value targets. This makes soft MPCritic computationally practical, while preserving solution quality. soft MPCritic plans in a scenario-based fashion with an ensemble of dynamic models trained for next-step prediction accuracy. Together, these ingredients enable soft MPCritic to learn effectively through robust, short-horizon planning on classic and complex control tasks. These results establish soft MPCritic as a practical and scalable blueprint for synthesizing MPC policies in settings where policy extraction and direct, long-horizon planning may fail.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Soft MPCritic** 的新型强化学习（RL）与模型预测控制（MPC）结合的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：RL 和 MPC 具有互补优势——MPC 具有显式的动力学建模和约束处理能力，而 RL 擅长通过价值函数进行长程规划。然而，将两者结合（尤其是大规模应用）面临巨大的**计算开销**，且直接进行长时域（Long-horizon）规划对模型精度要求极高，容易因模型偏差导致失败。
*   **核心问题**：如何在保持 MPC 规划优势的同时，降低计算成本，并利用 RL 的价值函数来弥补短时域规划的不足？
*   **整体含义**：提出一种在“软价值空间”内运行的框架，通过“摊销（Amortized）”计算压力，使基于采样的 MPC 能够高效地集成到 RL 训练循环中。

### 2. 方法论
*   **核心思想**：将模型预测路径积分控制（MPPI）作为规划器，既用于在线控制，也用于生成拟合价值迭代（Fitted Value Iteration）的目标值。
*   **关键技术细节**：
    *   **软价值迭代（Soft Value Iteration）**：利用自由能（Free Energy）概念，将 MPC 的优化目标与软贝尔曼方程（Soft Bellman Equation）对齐，使用 Softmax 算子代替传统的硬最大化。
    *   **终端 Q 函数**：在 MPPI 的预测时域末端引入一个可学习的 Q 函数，用以代表剩余时域的期望回报，从而实现“短时域规划、长时域视野”。
    *   **摊销预热策略（Amortized Warm-start）**：这是本文的核心创新。在计算 RL 更新目标时，不从随机序列开始优化 MPPI，而是从经验回放池中提取并复用之前的开环动作序列。
    *   **模型集成（Ensemble）**：训练一组动力学模型，通过情景化规划（Scenario-based planning）来应对模型不确定性。
*   **算法流程**：在线控制时更新动作序列并存入回放池；训练时，从池中取出序列进行少量步数的 MPPI 优化以生成目标值，再将优化后的序列存回池中供下次使用。

### 3. 实验设计
*   **实验场景**：
    1.  **经典控制**：双倒立摆（Double Inverted Pendulum），用于验证计算效率和基础性能。
    2.  **高维机器人任务**：Hopper-v5，用于测试算法在复杂动力学下的鲁棒性。
*   **Benchmark（基准）**：
    *   **SAC (Soft Actor-Critic)**：主流的离策（Off-policy）强化学习算法。
    *   **DDPG (Deep Deterministic Policy Gradient)**：经典的确定性策略梯度算法。
*   **对比实验**：对比了“热启动（Warm-start）”与“冷启动（Cold-start）”的效率，以及有无模型集成、有无终端 Q 函数的性能差异。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号或数量。
*   **计算效率数据**：作者通过“每秒环境步数（SPS）”来衡量效率。实验表明，采用预热策略（Warm-start）在达到同等性能的前提下，比冷启动方法节省了约 **50% 以上的计算时间**。

### 5. 实验数量与充分性
*   **实验规模**：每个主要实验均采用了 **10 个随机种子**进行测试，并给出了中位数和百分位区间，具有较好的统计说服力。
*   **消融实验**：非常充分。作者分别验证了：
    1.  MPPI 仅用于控制、仅用于目标生成、或两者兼有的影响。
    2.  模型集成对处理高维任务不确定性的必要性。
    3.  终端 Q 函数对短时域规划性能的决定性作用。
*   **客观性**：实验设计涵盖了从简单到复杂的任务，且对比了工业界和学术界公认的强基准算法（SAC），结果展示较为客观。

### 6. 主要结论与发现
*   **性能卓越**：Soft MPCritic 在 Hopper 任务中，仅需约 **20 万步**训练即可达到 SAC 和 DDPG 运行 **100 万步**后的渐近性能。
*   **计算可行性**：通过摊销预热策略，原本计算极其昂贵的“MPC 产生 RL 目标”过程变得在普通硬件上可行。
*   **对齐的重要性**：将规划器与价值函数在同一“软价值”框架下对齐，能够有效避免策略提取（Policy Extraction）过程中的信息损失。

### 7. 优点
*   **高效性**：通过复用动作序列，极大地缓解了 MPC 在 RL 训练中的计算瓶颈。
*   **鲁棒性**：结合了模型集成和采样规划，比纯基于梯度的模型预测控制更不容易陷入局部最优。
*   **灵活性**：不需要对动力学模型求导（Derivative-free），可以适配各种复杂的黑盒模型。

### 8. 不足与局限
*   **实验覆盖面**：虽然在 Hopper 上表现良好，但尚未在更复杂的任务（如 Humanoid 或真实物理机器人）上进行验证。
*   **超参数敏感性**：MPPI 的采样数、温度参数 $\lambda$ 以及时域 $H$ 等超参数可能需要针对不同任务进行精细调优。
*   **依赖奖励函数**：该方法假设奖励函数（Cost function）是已知的，在某些奖励函数极度稀疏或未知的场景下可能受限。

（完）
