---
title: Bridging Discrete Planning and Continuous Execution for Redundant Robot
title_zh: 冗余机器人离散规划与连续执行的桥接框架
authors: "Teng Yan, Yue Yu, Yihan Liu, Bingzhuo Zhong"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02021v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 冗余机器人机械臂路径规划的强化学习
tldr: 针对冗余机械臂在体素网格强化学习规划中存在的步长抖动、关节突变及奇异点不稳定等问题，本文提出了一种连接离散规划与连续执行的桥接框架。该框架在规划端引入步长归一化的26邻域动作与几何平局打破机制，在执行端采用任务优先级阻尼最小二乘（TP-DLS）逆运动学层。实验表明，该方法显著提升了复杂环境下的规划成功率，缩短了路径长度，并大幅降低了关节加速度，实现了平滑且精准的运动控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02021v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative comparison on a 7-DoF arm. (a) Baseline (unnormalised actions + Num-IK) shows irregular steps and joint-limit configurations. (b) Proposed bridge (normalised 26-neighbour + TP–DLS) equalises step size and keeps joints away from limits.\", \"page\": 6, \"index\": 1, \"width\": 527, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2604.02021v1/fig-002.webp\", \"caption\": \"Fig. 2. Overall architecture of the proposed bridging framework combining discrete Q-learning planning, geometric regularization, and continuous TP-DLS execution.\", \"page\": 4, \"index\": 2, \"width\": 1079, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.02021v1/fig-003.webp\", \"caption\": \"Table 1. Three Planners Under Different Obstacle Densities\", \"page\": 7, \"index\": 3, \"width\": 693, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2604.02021v1/fig-004.webp\", \"caption\": \"Fig. 1. Conceptual motivation. Left: classical planning - feasible collision-free path (Q1); Right: practical deployment - high-quality path (Q2). The proposed method meets the second requirement by bridging a voxel RL planner with a continuous execution layer on a redundant manipulator.\", \"page\": 2, \"index\": 4, \"width\": 500, \"height\": 419}]"
motivation: 解决冗余机械臂在执行基于体素网格的离散规划路径时，因数值逆运动学导致的步长抖动、关节突变及奇异点附近不稳定的问题。
method: 在规划端采用步长归一化的26邻域动作和几何平局打破机制，在执行端结合任务优先级阻尼最小二乘法、零空间投影及置信域裁剪技术。
result: "在稠密环境下将规划成功率从0.58提升至1.00，路径长度缩短约28%，且在保持毫米级精度的同时将峰值关节加速度降低了一个数量级以上。"
conclusion: 该桥接框架在不改变原有离散规划器的前提下，显著增强了冗余机械臂在复杂环境中的路径质量与连续执行的稳定性。
---

## 摘要
基于体素网格的强化学习因其简单性和可重复性，被广泛应用于冗余机械臂的路径规划。然而，在 7 自由度（7-DoF）机械臂上通过逐点数值逆运动学直接执行，往往会导致步长抖动、关节突变以及奇异位形附近的不稳定性。本研究提出了一种连接离散规划与连续执行的桥接框架，且无需修改离散规划器本身。在规划端，引入了步长归一化的 26 邻域笛卡尔动作和几何决胜（tie-breaking）机制，以抑制不必要的转向并消除步长振荡。在执行端，实现了一个任务优先级阻尼最小二乘（TP-DLS）逆运动学层。该层将末端执行器位置视为主要任务，而将姿态和关节中心化处理为投影到零空间的次要任务，并结合了置信域裁剪和关节速度约束。在随机稀疏、中等和密集环境下的 7 自由度机械臂实验表明，该桥接框架将密集场景下的规划成功率从约 0.58 提升至 1.00，典型路径长度从约 1.53 米缩短至 1.10 米；在保持末端执行器误差低于 1 毫米的同时，将峰值关节加速度降低了一个数量级以上，显著提升了冗余机械臂上基于体素强化学习路径的连续执行质量。

## Abstract
Voxel-grid reinforcement learning is widely adopted for path planning in redundant manipulators due to its simplicity and reproducibility. However, direct execution through point-wise numerical inverse kinematics on 7-DoF arms often yields step-size jitter, abrupt joint transitions, and instability near singular configurations. This work proposes a bridging framework between discrete planning and continuous execution without modifying the discrete planner itself. On the planning side, step-normalized 26-neighbor Cartesian actions and a geometric tie-breaking mechanism are introduced to suppress unnecessary turns and eliminate step-size oscillations. On the execution side, a task-priority damped least-squares (TP-DLS) inverse kinematics layer is implemented. This layer treats end-effector position as a primary task, while posture and joint centering are handled as subordinate tasks projected into the null space, combined with trust-region clipping and joint velocity constraints. On a 7-DoF manipulator in random sparse, medium, and dense environments, this bridge raises planning success in dense scenes from about 0.58 to 1.00, shortens representative path length from roughly 1.53 m to 1.10 m, and while keeping end-effector error below 1 mm, reduces peak joint accelerations by over an order of magnitude, substantially improving the continuous execution quality of voxel-based RL paths on redundant manipulators.

---

## 论文详细总结（自动生成）

这篇论文针对冗余机械臂在离散路径规划与连续物理执行之间的衔接问题，提出了一种轻量级的桥接框架。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：冗余机械臂（如 7-DoF 臂）在使用基于体素网格（Voxel-grid）的强化学习（RL）进行路径规划时，生成的离散笛卡尔路径在连续空间执行时存在严重不匹配。
*   **研究动机**：
    *   **步长不均**：传统的 26 邻域动作在轴向、面角和体角方向的物理长度不同，导致路径出现“锯齿状”振荡。
    *   **执行不稳定**：直接使用逐点数值逆运动学（IK）求解离散点，会导致关节配置大幅跳变、步长抖动，且在奇异位形附近极不稳定。
    *   **质量缺失**：现有的 RL 规划器往往只关注“能否到达”，而忽略了路径是否短、平滑以及是否符合动力学约束。

### 2. 方法论：桥接框架设计
该框架分为**几何层（规划端）**和**物理层（执行端）**，旨在不修改底层 RL 算法的前提下优化接口。

*   **几何层（规划端）优化**：
    *   **步长归一化 26 邻域**：将所有 26 个邻域动作的物理位移统一归一化为固定步长 $\delta$，消除离散网格带来的各向异性。
    *   **几何平局打破（Geometric Tie-breaking）**：当多个动作的 Q 值接近时，引入几何评分函数，优先选择转向角小、避障间隙大的动作，从而抑制路径冗余转向。
    *   **样条平滑与微步控制**：对离散路径进行三次样条插值，并根据弧长重新采样，确保输入执行器的参考点是连续且等距的。

*   **物理层（执行端）优化**：
    *   **任务优先级阻尼最小二乘（TP-DLS）**：
        *   **主任务**：末端执行器位置跟踪，使用阻尼最小二乘法处理奇异点。
        *   **次要任务**：关节中心化（Joint-centering），将关节尽量维持在限位中点，通过零空间投影（Null-space projection）实现，不干扰主任务。
    *   **置信域裁剪**：对关节增量进行全局范数裁剪和单关节步长限制，严格控制关节速度和加速度。

### 3. 实验设计
*   **实验环境**：基于 MATLAB 的 7-DoF 冗余机械臂仿真平台。
*   **场景设置**：根据障碍物数量分为三类：稀疏（0-30个）、中等（31-99个）、密集（100个以上）。
*   **Benchmark 与对比方法**：
    *   **规划器对比**：Original（6 邻域）、NoNorm（非归一化 26 邻域）、Improved（本文方法）。
    *   **执行器对比**：Num-IK（标准数值 IK 工具箱）、TP-DLS（本文方法）。
*   **评估指标**：规划成功率、路径长度、转向次数、最小避障间隙、关节加速度峰值、关节限位裕度等。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU/CPU 型号）及 RL 训练的具体时长。
*   实验主要在 MATLAB 仿真环境下完成，侧重于算法逻辑的验证而非大规模分布式训练。

### 5. 实验数量与充分性
*   **实验规模**：针对每种障碍物密度各生成了 1000 个随机任务，总计 3000 个测试案例。
*   **充分性**：实验覆盖了从简单到极高难度的环境，样本量足以支撑统计学意义上的结论。
*   **客观性**：通过固定规划器对比执行器、固定执行器对比规划器的“控制变量法”，清晰地展示了每一层改进对最终性能的贡献，实验设计较为公平。

### 6. 主要结论与发现
*   **规划性能提升**：在密集障碍物场景下，本文方法将规划成功率从 **0.58 提升至 1.00**，路径长度显著缩短（约 28%）。
*   **执行质量飞跃**：在保持末端执行器误差小于 1mm 的前提下，TP-DLS 将关节峰值加速度降低了**一个数量级以上**。
*   **稳定性增强**：通过零空间任务，机械臂在执行过程中能更好地避开关节限位，且消除了数值 IK 常见的配置突变现象。

### 7. 优点与亮点
*   **通用性强**：作为“桥接层”，它对底层的 RL 算法（如 Q-Learning, PPO 等）和网络结构是不可知的，具有很强的即插即用性。
*   **解耦设计**：成功地将离散的逻辑决策与连续的物理约束解耦，解决了“离散规划好做但难用”的痛点。
*   **多目标平衡**：在不牺牲避障安全性的前提下，同时优化了路径几何质量和动力学平滑度。

### 8. 不足与局限
*   **计算开销**：TP-DLS 涉及多次小步长迭代和零空间投影，其计算时间（Time）和回溯次数（Backtracks）显著高于标准数值 IK，可能影响实时性。
*   **环境限制**：目前仅在静态障碍物环境下验证，未讨论动态障碍物或自碰撞（Self-collision）规避。
*   **物理验证缺失**：实验全部在仿真中完成，尚未在真实的物理机械臂上进行部署验证，需考虑真实传感器的噪声和控制延迟。

（完）
