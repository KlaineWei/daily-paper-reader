---
title: "Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control"
title_zh: 基于漂移的策略优化：面向在线机器人控制的原生单步策略学习
authors: "Yuxuan Gao, Yedong Shen, Shiqi Zhang, Wenhao Yu, Yifan Duan, Jia pan, Jiajia Wu, Jiajun Deng, Yanyong Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03540v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于在线机器人控制和强化学习的一步生成策略
tldr: 针对多步生成式策略（如扩散模型）在机器人控制中推理速度慢、难以满足高频闭环控制需求的问题，本文提出了一种原生单步生成式策略框架。该框架通过漂移策略（DBP）将迭代优化过程内化至模型参数中，并结合漂移策略优化（DBPO）实现稳定的在线强化学习。该方法在保持多峰分布建模能力的同时，将推理速度提升了近百倍，并在真实双臂机器人上实现了105.2 Hz的高频稳定控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03540v1/fig-001.webp\", \"caption\": \"Fig. 4: Online PPO fine-tuning results on RoboMimic and D4RL with anchor ablation (DBPO vs. DBPO w/o anchor). Solid bars denote offline initialization, and hatched bars denote gains after fine-tuning. In this evaluation setting, DBPO achieves the strongest post-fine-tuning performance, while removing the anchor consistently reduces gains over pretrained baselines.\", \"page\": 7, \"index\": 1, \"width\": 525, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2604.03540v1/fig-002.webp\", \"caption\": \"TABLE II: Point-cloud imitation comparison on Adroit and Meta-World under the MP1/OMP protocol. Ours refers to DriftBased Policy. Entries report success rate (%, mean ± std over 3 seeds). Best results are in bold; second-best are underlined.\", \"page\": 8, \"index\": 2, \"width\": 1048, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2604.03540v1/fig-003.webp\", \"caption\": \"Fig. 5: Real-world bimanual deployment on the physical UR5 testbed. Drift-Based Policy executes precision Lift, Can, and synchronized bimanual Transport using raw trilateral camera inputs.\", \"page\": 8, \"index\": 3, \"width\": 1057, \"height\": 327}]"
motivation: 现有的多步生成式策略因推理时需要多次迭代计算，导致其在高频闭环控制和在线强化学习中成本过高且效率低下。
method: 提出基于固定点漂移目标的DBP策略实现单步生成，并开发DBPO框架以支持预训练模型在在线环境下的稳定策略改进。
result: 实验表明DBP在性能上媲美或超越了多步扩散策略，推理速度提升达100倍，并在复杂操作任务中优于现有的单步基准模型。
conclusion: 该研究成功实现了兼顾多峰建模能力与极高推理效率的原生单步策略，为高性能实时机器人控制提供了有效的解决方案。
---

## 摘要
尽管多步生成式策略通过建模多峰动作分布在机器人操作中取得了强劲性能，但它们在推理时需要多步迭代去噪。因此，每个动作都需要数十到数百次网络函数评估（NFEs），这使得它们在进行高频闭环控制和在线强化学习（RL）时成本高昂。为了解决这一局限性，我们提出了一个原生单步生成式策略的两阶段框架，将精炼过程从推理阶段转移到训练阶段。首先，我们引入了基于漂移的策略（Drift-Based Policy, DBP），它利用不动点漂移目标将迭代精炼过程内化到模型参数中，从而在设计上产生了一个单步生成式骨干网络，同时保留了多峰动作建模能力。其次，我们开发了基于漂移的策略优化（Drift-Based Policy Optimization, DBPO），这是一个在线强化学习框架，为预训练的骨干网络配备了兼容的随机接口，在不牺牲单步部署特性的情况下实现了稳定的同策略（on-policy）更新。广泛的实验证明了该框架在离线模仿学习、在线微调和真实世界控制场景中的有效性。DBP 在达到或超过多步扩散策略性能的同时，实现了高达 100 倍的推理加速。在具有挑战性的操作基准测试中，它也始终优于现有的单步基准模型。此外，DBPO 在在线设置中实现了有效且稳定的策略提升。在真实双臂机器人上的实验展示了 105.2 Hz 的可靠高频控制。

## Abstract
Although multi-step generative policies achieve strong performance in robotic manipulation by modeling multimodal action distributions, they require multi-step iterative denoising at inference time. Each action therefore needs tens to hundreds of network function evaluations (NFEs), making them costly for high-frequency closed-loop control and online reinforcement learning (RL). To address this limitation, we propose a two-stage framework for native one-step generative policies that shifts refinement from inference to training. First, we introduce the Drift-Based Policy (DBP), which leverages fixed-point drifting objectives to internalize iterative refinement into the model parameters, yielding a one-step generative backbone by design while preserving multimodal action modeling capacity. Second, we develop Drift-Based Policy Optimization (DBPO), an online RL framework that equips the pretrained backbone with a compatible stochastic interface, enabling stable on-policy updates without sacrificing the one-step deployment property. Extensive experiments demonstrate the effectiveness of the proposed framework across offline imitation learning, online fine-tuning, and real-world control scenarios. DBP matches or exceeds the performance of multi-step diffusion policies while achieving up to $100\times$ faster inference. It also consistently outperforms existing one-step baselines on challenging manipulation benchmarks. Moreover, DBPO enables effective and stable policy improvement in online settings. Experiments on a real-world dual-arm robot demonstrate reliable high-frequency control at 105.2 Hz.

---

## 论文详细总结（自动生成）

这是一份关于论文《Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：当前机器人操作领域的主流是基于扩散模型（Diffusion Policy）的多步生成式策略。虽然它们能很好地处理动作的多峰分布（即完成同一任务有多种有效路径），但推理时需要数十甚至数百次网络函数评估（NFE），导致高延迟，难以满足高频闭环控制（如 100Hz 以上）和在线强化学习（RL）的需求。
*   **核心问题**：如何在保持生成式模型对复杂动作分布建模能力的同时，实现“原生单步”（Native One-Step）推理，即仅需一次网络前向计算即可生成高质量动作。

### 2. 方法论：DBP 与 DBPO
论文提出了一个两阶段框架，将迭代精炼的过程从“推理时”转移到了“训练时”：
*   **Drift-Based Policy (DBP) - 离线阶段**：
    *   **核心思想**：借鉴“漂移模型”（Drifting Models）原理，通过在训练中引入“吸引-排斥”机制，使模型参数直接内化（Internalize）复杂的修正动力学。
    *   **技术细节**：在动作空间中构建漂移场。训练时，生成的多个假设动作（Hypotheses）会受到专家示例的“吸引”以及彼此之间的“排斥”。通过最小化当前预测与“漂移后的目标”之间的均方误差，模型学会了在单次前向传播中直接输出位于高概率区域的动作。
*   **Drift-Based Policy Optimization (DBPO) - 在线阶段**：
    *   **核心思想**：为单步骨干网络添加一个轻量级的随机适配器，使其兼容标准的同策略（On-policy）强化学习算法（如 PPO）。
    *   **技术细节**：引入一个可训练的随机动作头，利用预训练骨干输出的特征来预测动作均值和方差。为了防止在线更新时偏离预训练分布太远，引入了**锚点正则化（Anchor Regularization）**，约束更新后的策略在原始潜在空间附近波动。

### 3. 实验设计与基准测试
*   **数据集与场景**：
    1.  **Diffusion Policy Suite**：包含 Push-T、BlockPush、RoboMimic、Kitchen 等 12 个任务（涉及图像和低维状态输入）。
    2.  **点云操作基准**：在 Adroit 和 Meta-World 上共 37 个任务，使用 3D 点云作为输入。
    3.  **在线 RL 任务**：RoboMimic 操纵任务和 D4RL 运动任务。
    4.  **真实世界部署**：使用双臂 UR5 机器人完成抓取（Lift）、拨罐（Can）和双臂协作运输（Transport）。
*   **对比方法**：
    *   **多步模型**：Diffusion Policy (DP), DP3。
    *   **单步/加速模型**：Consistency Policy, One-step Diffusion, MP1, OMP, FlowPolicy, AdaFlow 等。
    *   **在线 RL 基准**：ReinFlow, DMPO 等。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA RTX 3090 GPU**。
*   **训练时长**：在 Adroit 任务上训练 3000 个 epoch，在 Meta-World 任务上训练 1000 个 epoch。
*   **推理效率**：在 RTX 3090 上，端到端延迟仅为 **9.5 ms**，实现了 **105.2 Hz** 的控制频率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2D 仿真到 3D 点云，再到真实机器人的全维度测试。
*   **消融实验**：针对在线 RL 中的“锚点正则化”进行了专门的消融研究，证明了其对稳定性的必要性。
*   **客观性**：在对比实验中，作者严格控制了骨干网络（如 U-Net）和预处理流程（如点云采样数）与基准模型一致，确保了算法层面的公平比较。实验结果给出了均值和标准差，具有较高的统计可信度。

### 6. 主要结论与发现
*   **性能超越**：DBP 在 1-NFE（单步）下，在 Diffusion Policy 仿真套件上的平均得分从 0.79 提升至 0.83，且速度提升 100 倍。
*   **SOTA 表现**：在 37 个点云任务中，DBP 达到了 88.4% 的平均成功率，显著优于之前的单步领先模型 OMP (82.3%)。
*   **在线提升**：DBPO 证明了单步模型可以通过在线交互进一步突破离线数据的性能天花板。
*   **实机可行性**：在真实双臂机器人上实现了 75% 的综合成功率，证明了该方法在处理视觉遮挡和高频同步控制方面的实用性。

### 7. 优点与亮点
*   **原生单步性**：不同于通过蒸馏（Distillation）获得的单步模型，DBP 的单步特性源于其训练目标函数，设计更加优雅且不依赖复杂的教师模型。
*   **多模态保持**：在单步推理的同时，依然保留了生成式模型处理多峰分布的能力。
*   **极高的控制频率**：105.2 Hz 的表现使其非常适合需要快速反应的动态机器人任务。

### 8. 不足与局限
*   **任务复杂度限制**：目前的评估主要集中在桌面操纵和简单的运动任务，尚未在具有极长时序（Long-horizon）或极其复杂的接触动力学任务中进行验证。
*   **在线学习效率**：虽然支持在线 RL，但在“从零开始学习”（Learning from scratch）时的样本效率仍有提升空间，目前主要依赖离线预训练后的微调。
*   **实机失败分析**：在双臂协作中仍存在同步失败的情况，说明在极高维度的动作协调上仍有优化空间。

（完）
