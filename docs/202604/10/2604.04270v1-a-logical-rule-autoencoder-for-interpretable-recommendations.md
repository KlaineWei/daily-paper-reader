---
title: A Logical-Rule Autoencoder for Interpretable Recommendations
title_zh: 一种用于可解释推荐的逻辑规则自编码器
authors: "Jinhao Pan, Bowen Wei, Ziwei Zhu"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04270v1"
tags: ["query:llmrec"]
score: 7.5
evidence: 使用逻辑规则的可解释协同过滤
tldr: 针对深度学习推荐模型缺乏透明度的“黑盒”问题，本文提出了一种逻辑规则可解释自编码器（LIA）。该模型通过引入可学习的逻辑规则层，利用门控参数自动选择与/或逻辑，并通过连接权重的正负实现非逻辑，从而从数据中直接提取人类可读的重构规则。实验证明，LIA在保持完全可解释性的同时，推荐性能优于传统基准模型，实现了决策过程的直接溯源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04270v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of LIA. Left: Architecture with signed-weight negation, learnable logical rules with operator selection, and disjunctive aggregation. Right: Interpretation for recommending Item #1042, with rules visualized as trees showing learned operators, selected items, and negations (red). Natural language explanations are directly derived from the rule structure.\", \"page\": 2, \"index\": 1, \"width\": 906, \"height\": 307}]"
motivation: 现有的深度学习推荐模型大多依赖隐表示，缺乏内在可解释性，难以满足高透明度和问责制应用场景的需求。
method: 提出LIA模型，通过包含门控参数的逻辑规则层自动学习与/或逻辑，并利用权重符号编码否定关系，构建参数高效且功能完备的逻辑重构机制。
result: 在多个数据集上的实验表明，LIA在推荐性能上优于传统基准模型，并能生成人类可读的显式决策规则。
conclusion: LIA成功地将逻辑规则的透明性与自编码器的学习能力相结合，为构建兼顾性能与可解释性的推荐系统提供了有效方案。
---

## 摘要
大多数深度学习推荐模型以黑盒方式运行，依赖于掩盖其决策过程的隐表示。这种内在可解释性的缺失在需要透明度和问责制的应用中引起了担忧。在这项工作中，我们为协同过滤提出了一种逻辑规则可解释自编码器（LIA），其在设计上即具备可解释性。LIA 引入了一个可学习的逻辑规则层，其中每个规则神经元都配备了一个门控参数，在训练过程中自动在“与”（AND）和“或”（OR）算子之间进行选择，使模型能够直接从数据中发现多样化的逻辑模式。为了在不增加输入维度的情况下支持功能完备性，LIA 通过连接权重的符号对“非”操作进行编码，提供了一种参数高效的机制，用于在每个规则中表达正向和负向的项目条件。通过学习显式的、人类可读的重构规则，LIA 允许用户直接追踪每项推荐背后的决策过程。大量实验表明，我们的方法在保持完全可解释性的同时，比传统基准模型实现了更好的推荐性能。代码和数据可在 https://github.com/weibowen555/LIA 获取。

## Abstract
Most deep learning recommendation models operate as black boxes, relying on latent representations that obscure their decision process. This lack of intrinsic interpretability raises concerns in applications that require transparency and accountability. In this work, we propose a Logical-rule Interpretable Autoencoder (LIA) for collaborative filtering that is interpretable by design. LIA introduces a learnable logical rule layer in which each rule neuron is equipped with a gate parameter that automatically selects between AND and OR operators during training, enabling the model to discover diverse logical patterns directly from data. To support functional completeness without doubling the input dimensionality, LIA encodes negation through the sign of connection weights, providing a parameter-efficient mechanism for expressing both positive and negated item conditions within each rule. By learning explicit, human-readable reconstruction rules, LIA allows users to directly trace the decision process behind each recommendation. Extensive experiments show that our method achieves improved recommendation performance over traditional baselines while remaining fully interpretable. Code and data are available at https://github.com/weibowen555/LIA.