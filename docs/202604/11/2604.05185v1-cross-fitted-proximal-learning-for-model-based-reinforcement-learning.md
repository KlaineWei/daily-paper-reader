---
title: Cross-fitted Proximal Learning for Model-Based Reinforcement Learning
title_zh: 基于模型的强化学习的交叉拟合近端学习
authors: "Nishanth Venkatesh, Andreas A. Malikopoulos"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05185v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于顺序决策和规划的模型强化学习
tldr: 本文针对存在隐性混杂因素的离线模型强化学习（POMDPs），提出了一种基于交叉拟合（Cross-fitting）的近端学习方法。研究通过估计满足条件矩限制（CMR）的奖励-发射和观测-转移桥函数来实现策略评估。为了提高数据利用效率，作者开发了K折交叉拟合的两阶段估计器，并推导了其误差界限，将误差分解为干扰项估计和经验平均两部分，为复杂环境下的离线RL提供了更稳健的统计估计方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05185v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of observation-transition recovery\", \"page\": 6, \"index\": 1, \"width\": 512, \"height\": 325}]"
motivation: 在存在隐性混杂因素的离线部分可观测马尔可夫决策过程中，直接从观测数据学习的模型会因潜在因素同时影响动作、奖励和观测而产生偏差。
method: 提出一种K折交叉拟合的两阶段估计器，将桥函数学习建模为包含条件均值嵌入和条件密度等干扰项的条件矩限制（CMR）问题。
result: 推导出了交叉拟合估计器的Oracle比较界限，并成功将误差分解为干扰项估计引起的阶段I误差和经验平均引起的阶段II误差。
conclusion: 该方法在保持桥函数识别策略的同时，比单次样本分割更有效地利用了数据，显著提升了混杂环境下模型强化学习的统计估计效率。
---

## 摘要
基于模型的强化学习在序列决策中具有吸引力，因为它显式地估计奖励和转移模型，并支持通过模拟展开进行规划。然而，在存在隐性混杂因素的离线设置中，直接从观测数据学习的模型可能会产生偏差。这一挑战在部分可观测系统中尤为突出，其中潜在因素可能共同影响动作、奖励和未来的观测。最近的研究表明，在此类受混杂影响的部分可观测马尔可夫决策过程（POMDPs）中，策略评估可以简化为估计满足条件矩限制（CMRs）的奖励-发射和观测-转移桥函数。在本文中，我们研究了这些桥函数的统计估计。我们将桥函数学习建模为一个 CMR 问题，其中的冗余对象由条件均值嵌入和条件密度给出。随后，我们为现有的两阶段桥估计器开发了一个 $K$ 折交叉拟合扩展。所提出的程序保留了原始的基于桥函数的识别策略，同时比单次样本分割更有效地利用了可用数据。我们还为该交叉拟合估计器推导了一个 oracle-comparator 误差界，并将产生的误差分解为由冗余估计引起的阶段 I 项和由经验平均引起的阶段 II 项。

## Abstract
Model-based reinforcement learning is attractive for sequential decision-making because it explicitly estimates reward and transition models and then supports planning through simulated rollouts. In offline settings with hidden confounding, however, models learned directly from observational data may be biased. This challenge is especially pronounced in partially observable systems, where latent factors may jointly affect actions, rewards, and future observations. Recent work has shown that policy evaluation in such confounded partially observable Markov decision processes (POMDPs) can be reduced to estimating reward-emission and observation-transition bridge functions satisfying conditional moment restrictions (CMRs). In this paper, we study the statistical estimation of these bridge functions. We formulate bridge learning as a CMR problem with nuisance objects given by a conditional mean embedding and a conditional density. We then develop a $K$-fold cross-fitted extension of the existing two-stage bridge estimator. The proposed procedure preserves the original bridge-based identification strategy while using the available data more efficiently than a single sample split. We also derive an oracle-comparator bound for the cross-fitted estimator and decompose the resulting error into a Stage I term induced by nuisance estimation and a Stage II term induced by empirical averaging.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cross-fitted Proximal Learning for Model-Based Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在**离线强化学习（Offline RL）**中，当环境属于**受混杂影响的部分可观测马尔可夫决策过程（Confounded POMDPs）**时，如何准确估计系统模型的问题。
*   **研究动机**：在现实场景（如医疗、自动驾驶）中，往往存在无法观测的潜在因素（隐性混杂因素）同时影响决策和结果。传统的基于模型的 RL 直接从观测数据学习，会因这些混杂因素产生严重的统计偏差。
*   **核心问题**：如何从包含混杂因素的离线数据中，通过“桥函数”（Bridge Functions）识别出真实的奖励和转移机制，并解决现有估计方法在有限样本下数据利用率低、估计不稳健的问题。

### 2. 方法论：核心思想与关键技术
论文提出了一种**基于 $K$ 折交叉拟合（K-fold Cross-fitting）的两阶段近端学习框架**：
*   **核心思想**：将桥函数的学习重新表述为一个**条件矩限制（CMR）**问题。通过交叉拟合技术，在不改变识别策略的前提下，消除估计过程中的过拟合偏差，提高样本效率。
*   **关键技术细节**：
    1.  **CMR 建模**：将奖励-发射桥函数和观测-转移桥函数统一建模为满足特定条件期望方程的函数。
    2.  **两阶段估计**：
        *   **阶段 I（干扰项估计）**：在辅助样本上估计“干扰对象”（Nuisance Objects），包括条件均值嵌入（CME）和条件密度。
        *   **阶段 II（桥函数估计）**：在保留样本（Held-out fold）上，利用阶段 I 的估计值作为插件，通过最小化正则化经验风险来求解桥函数。
    3.  **K 折交叉拟合**：将数据集分为 $K$ 份，轮流作为保留样本进行评估，最后聚合各折的准则函数以获得最终估计器。
*   **理论贡献**：推导了 **Oracle 比较界限（Oracle-comparator bound）**，将总误差分解为阶段 I 的干扰项估计误差和阶段 II 的经验过程误差。

### 3. 实验设计
*   **实验场景**：一个 2 步时界的非线性高斯 POMDP 模拟系统。
*   **关键特征**：包含一个隐藏的二进制上下文因子（混杂因素），该因子同时影响行为策略（动作选择）和状态演化。
*   **Benchmark（基准）**：采用**单次样本分割（One-shot 50-50 sample-splitting）**作为对比基准。
*   **对比方法**：对比了在不同样本规模（$N=120$ 到 $N=1920$）下，所提交叉拟合估计器与单次分割估计器在恢复“去混杂观测转移目标”时的均方误差（MSE）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件资源（如 GPU 型号、数量）或具体的训练时长。由于实验部分主要基于数值模拟和核方法（Kernel Methods），通常对算力的需求远低于大规模深度强化学习。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一组核心的数值对比实验，样本量跨越了五个量级（120, 240, 480, 960, 1920）。
*   **充分性评价**：
    *   **统计性**：实验结果显示了明显的趋势，证明了交叉拟合在不同样本量下的优越性。
    *   **局限性**：实验仅限于低维的合成数据集（Synthetic Data），缺乏在复杂高维环境（如 Atari 或 MuJoCo 的变体）或真实世界数据集上的验证。
    *   **消融实验**：缺乏针对 $K$ 值选取、不同核函数选择等超参数影响的详细消融研究。

### 6. 主要结论与发现
*   **性能提升**：交叉拟合估计器在所有测试样本规模下均显著优于单次样本分割方法。例如，在 $N=1920$ 时，MSE 从 0.0771 降低到了 0.0248。
*   **样本效率**：交叉拟合能更有效地利用有限的离线数据，减少了估计器对单一数据划分方式的依赖。
*   **理论验证**：实验结果支持了理论推导，即通过交叉拟合可以有效降低第一阶段估计误差对最终目标的传播影响。

### 7. 优点（亮点）
*   **统计严谨性**：将桥函数学习与现代因果推断中的双重机器学习（Double ML）思想结合，提供了坚实的理论基础。
*   **误差分解清晰**：通过 Oracle 分析明确了误差来源，为后续改进干扰项学习器提供了指导。
*   **通用性**：提出的 CMR 框架不仅适用于 POMDP，也可扩展到其他存在代理变量（Proxy）的因果推理问题。

### 8. 不足与局限
*   **缺乏正交化**：作者在文中坦承，目前尚未实现 **Neyman 正交化（Neyman-orthogonality）**，这意味着估计器对第一阶段正则化偏差仍具有一定的敏感性。
*   **实验覆盖面窄**：仅在简单的非线性高斯系统上进行了验证，未探讨在大规模神经网络参数化下的表现。
*   **计算开销**：虽然提高了数据效率，但 $K$ 折交叉拟合相比单次分割会显著增加训练阶段的计算负担（需训练 $K$ 次模型）。

（完）
