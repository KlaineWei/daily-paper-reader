---
title: Long-Term Embeddings for Balanced Personalization
title_zh: 用于平衡个性化的长期嵌入
authors: "Andrii Dzhoha, Egor Malykh"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08181v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 序列推荐系统与长期偏好
tldr: 针对Transformer推荐系统存在的近期偏置和长期偏好缺失问题，本文提出长期嵌入（LTE）框架。该方法通过将嵌入约束在固定的语义基准上，解决了生产环境中特征版本不一致导致的离线在线失配挑战。通过将LTE作为上下文前缀引入模型，并在Zalando进行在线A/B测试，证明了该方法能显著提升用户参与度和业务指标。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08181v1/fig-001.webp\", \"caption\": \"Table 1: NDCG@500 Uplift for LTE integration strategies.\", \"page\": 4, \"index\": 1, \"width\": 491, \"height\": 772}, {\"url\": \"assets/figures/arxiv/2604.08181v1/fig-002.webp\", \"caption\": \"Figure 1: Statistical consistency of LTE attention migration. The delta (LTE - baseline) is computed after normalizing attention weights within themost recent 50 interactions to a local share of 1.0. The model appears to moderate mid-session fluctuations to reclaim attention share for older history. Gray bands indicate the 95% confidence interval calculated across the holdout set.\", \"page\": 5, \"index\": 2, \"width\": 496, \"height\": 293}]"
motivation: 现有序列推荐模型过度关注短期意图而忽略了稳定的长期偏好，且在生产部署中面临特征版本不一致导致的性能波动。
method: 提出LTE框架，利用基于内容的固定语义基准约束嵌入，并采用非对称自编码器将长期偏好作为上下文前缀集成到因果语言模型中。
result: 在Zalando进行的在线A/B测试显示，集成LTE作为上下文前缀显著提升了用户参与度及相关的财务指标。
conclusion: 通过引入具有语义一致性的长期嵌入，可以有效平衡推荐系统的长短期兴趣，并解决工业级部署中的特征一致性难题。
---

## 摘要
现代基于 Transformer 的序列推荐系统擅长捕捉短期意图，但往往受限于近期偏见，忽略了稳定的长期偏好。虽然延长序列长度是一种直观的解决方法，但其计算效率低下，且近期的交互往往会主导模型的注意力。我们提出了长期嵌入（Long-Term Embeddings, LTE）作为一种高惯性的上下文锚点，以弥补这一差距。我们解决了一个关键的生产挑战：由基础设施限制引起的时间点一致性问题，因为特征存储通常只托管特征的单一“实时”版本。这导致在模型部署和回滚期间出现离线与在线不匹配的问题，因为模型被迫处理在训练期间从未见过的演化后的表示。为了解决这个问题，我们引入了一个 LTE 框架，将嵌入约束在基于内容的物品表示的固定语义基上，从而确保跨版本兼容性。此外，我们研究了因果语言建模的集成策略，并考虑了当 LTE 与 Transformer 的短期序列共享时间范围时出现的数据泄漏问题。我们评估了两种表示方式：一种是启发式平均，另一种是带有固定解码器的非对称自动编码器，该解码器基于语义基以在保持稳定性的同时实现行为微调。在 Zalando 上的在线 A/B 测试表明，使用滞后窗口将 LTE 作为上下文前缀标记集成，可以显著提升用户参与度和财务指标。

## Abstract
Modern transformer-based sequential recommenders excel at capturing short-term intent but often suffer from recency bias, overlooking stable long-term preferences. While extending sequence lengths is an intuitive fix, it is computationally inefficient, and recent interactions tend to dominate the model's attention. We propose Long-Term Embeddings (LTE) as a high-inertia contextual anchor to bridge this gap. We address a critical production challenge: the point-in-time consistency problem caused by infrastructure constraints, as feature stores typically host only a single "live" version of features. This leads to an offline-online mismatch during model deployments and rollbacks, as models are forced to process evolved representations they never saw during training. To resolve this, we introduce an LTE framework that constrains embeddings to a fixed semantic basis of content-based item representations, ensuring cross-version compatibility. Furthermore, we investigate integration strategies for causal language modeling, considering the data leakage issue that occurs when the LTE and the transformer's short-term sequence share a temporal horizon. We evaluate two representations: a heuristic average and an asymmetric autoencoder with a fixed decoder grounded in the semantic basis to enable behavioral fine-tuning while maintaining stability. Online A/B tests on Zalando demonstrate that integrating LTE as a contextual prefix token using a lagged window yields significant uplifts in both user engagement and financial metrics.

---

## 论文详细总结（自动生成）

这是一份关于论文《Long-Term Embeddings for Balanced Personalization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于现代序列推荐系统（如基于 Transformer 的 SASRec）在工业界应用中的两大痛点：
*   **近期偏见（Recency Bias）：** Transformer 擅长捕捉短期意图（“现在想买什么”），但由于自注意力机制的 $O(N^2)$ 复杂度限制了输入序列长度，模型往往会忽略用户的长期稳定偏好（“我是谁”，如品牌忠诚度、风格偏好）。
*   **生产环境的一致性挑战：** 在大规模生产中，特征存储（Feature Store）通常只保存特征的最新版本。当模型回滚或更新滞后时，旧模型会遇到从未见过的“未来”特征表示，导致离线训练与在线推理的不匹配（Point-in-Time Consistency 问题）。

### 2. 方法论：核心思想与技术细节
论文提出了 **长期嵌入（Long-Term Embeddings, LTE）** 框架，旨在作为高惯性的上下文锚点来平衡长短期兴趣：
*   **固定语义基（Fixed Semantic Basis）：** 为了解决版本一致性，LTE 不直接学习用户向量，而是将其约束在基于内容的固定物品表示（如 CLIP 提取的视觉/文本嵌入）的线性组合中。这确保了即使模型版本变动，语义空间依然稳定。
*   **滞后窗口（Lagged Window）：** 为防止因果语言模型（CLM）中的数据泄露，LTE 计算的是较远期的历史（如过去 365 天到 60 天前），而 Transformer 处理最近 60 天的序列。
*   **集成策略：** 
    *   **上下文锚定（Contextual Anchoring）：** 将 LTE 作为序列的第一个“前缀 Token（Prefix Token）”，让后续所有短期交互通过注意力机制与其交互。
    *   **非对称自编码器（Asymmetric Autoencoder）：** 进一步提出一种微调方法，编码器学习行为权重，解码器则是固定的内容嵌入矩阵，从而在保持稳定性的同时捕捉价格敏感度等行为特征。

### 3. 实验设计
*   **数据集与场景：** 在欧洲大型电商平台 Zalando 的真实场景下进行，涵盖 25 个市场，涉及超过 7000 万用户。
*   **Benchmark：** 基础模型是基于 SASRec 架构的双塔 Transformer 排序模型，输入为最近 60 天的交互序列。
*   **对比方法：** 
    *   **集成方式对比：** 后融合（Late Fusion）、特征注入（Feature Injection，直接加到每个物品嵌入上）、上下文锚定（Prefix Token）。
    *   **权重方案对比：** 均匀平均（Uniform Average）与近期加权平均（Recency-weighted）。
    *   **序列长度对比：** 尝试将 Transformer 序列直接延长至 360 天作为对比。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号和数量。
*   但提到了计算成本的对比：将 Transformer 序列长度从 60 延长到 360 天会导致训练时间增加 **5 倍以上**，数据准备速度变慢 **8 倍**。相比之下，LTE 方案在不显著增加推理延迟的情况下实现了性能提升。

### 5. 实验数量与充分性
*   **离线实验：** 针对不同的集成策略、权重方案、数据泄露风险进行了多组消融实验，评估指标为 NDCG@500。
*   **注意力分析：** 专门分析了注意力权重的迁移，量化了近期偏见的减少程度（下降 7.64%）。
*   **稳定性测试：** 模拟了 1 天、5 天、10 天的模型回滚场景，验证了 LTE 的时间惯性。
*   **在线实验：** 在数百万用户的真实流量中进行了 A/B 测试。
*   **评价：** 实验设计非常充分且具有高度的工业实战价值，不仅关注模型精度，还深入探讨了系统鲁棒性和运维风险，非常客观。

### 6. 主要结论与发现
*   **上下文锚定最优：** 将 LTE 作为前缀 Token 效果最好（NDCG 提升 1.31%），因为它允许模型根据长期背景动态调整对短期行为的注意力。
*   **均匀平均优于加权：** 在长期特征中，简单的均匀平均效果更好，因为近期加权会与 Transformer 已有的功能重叠，产生冗余。
*   **解决近期偏见：** LTE 显著降低了模型对最后一次点击的过度依赖，使模型能够“找回”被遗忘的远期记忆。
*   **业务价值：** 在线 A/B 测试显示，浏览场景（Browse）的参与度提升了 1.16%，整体收入提升了 0.42%。

### 7. 优点
*   **工程落地性极强：** 充分考虑了 Feature Store 的单版本限制和模型回滚的痛点，提出了“高惯性”设计。
*   **架构优雅：** 通过前缀 Token 方式集成，无需大幅改动 Transformer 结构，计算开销极小。
*   **可解释性：** 通过注意力迁移分析，直观展示了 LTE 如何通过抑制会话噪声来强化长期偏好。

### 8. 不足与局限
*   **搜索场景提升有限：** 在搜索（Search）场景下提升不显著，说明强意图（Query）仍主导搜索结果，长期偏好的作用被稀释。
*   **冷启动依赖：** 对于完全没有历史记录的新用户，LTE 退化为零向量或全局平均值，对极短期的冷启动改善有限。
*   **语义基限制：** 模型的表现高度依赖于预训练内容模型（如 CLIP）的质量，如果内容嵌入无法捕捉某些关键属性，LTE 的表达力会受限。

（完）
