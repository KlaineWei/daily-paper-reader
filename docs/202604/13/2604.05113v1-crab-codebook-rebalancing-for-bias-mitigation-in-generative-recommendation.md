---
title: "CRAB: Codebook Rebalancing for Bias Mitigation in Generative Recommendation"
title_zh: CRAB：生成式推荐中用于缓解偏差的码本重平衡
authors: "Zezhong Fan, Ziheng Chen, Luyi Ma, Jin Huang, Lalitesh Morishetti, Kaushiki Nag, Sushant Kumar, Kannan Achan"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05113v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 使用离散语义标记和码本重平衡的生成式推荐
tldr: 生成式推荐（GeneRec）虽表现优异，但面临严重的流行度偏差。本文通过实证分析发现，不平衡的词表编码和训练过程对热门Token的过度偏向是主因。为此，提出了CRAB策略，通过拆分热门Token重构词表并引入树状结构正则项，在保持语义一致性的同时缓解频率失衡。实验证明CRAB能有效减轻偏差并提升推荐性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05113v1/fig-001.webp\", \"caption\": \"Table 1: Performance and Efficiency Comparison on Industrial and Office Datasets.\", \"page\": 5, \"index\": 1, \"width\": 980, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.05113v1/fig-002.webp\", \"caption\": \"Figure 1: Left: Popularity bias of GeneRec on the industrial dataset, with the x-axis representing item groups by popularity. Right: The GU of item groups grouped by token popularity.\", \"page\": 2, \"index\": 2, \"width\": 449, \"height\": 281}]"
motivation: 现有的生成式推荐模型因词表编码不平衡和训练过程偏向热门Token，导致严重的流行度偏差问题。
method: 提出CRAB策略，通过拆分过热Token重平衡词表，并引入树状结构正则化项以增强长尾Token的语义表示。
result: 在真实数据集上的实验表明，CRAB在有效缓解流行度偏差的同时，显著提升了推荐系统的整体性能。
conclusion: 通过重平衡语义词表和强化语义一致性，可以有效解决生成式推荐中的流行度偏差，实现更公平高效的推荐。
---

## 摘要
生成式推荐（GeneRec）引入了一种新的范式，将物品表示为离散的语义标记（tokens），并以生成的方式预测物品。尽管在多项推荐任务中表现出色，现有的 GeneRec 方法仍面临严重的流行度偏差（popularity bias），甚至可能加剧这种偏差。在这项工作中，我们进行了全面的实证分析以揭示这一现象的根本原因，并得出两个核心见解：1) 不平衡的标记化（tokenization）继承并可能进一步放大历史物品交互中的流行度偏差；2) 当前的训练过程过度偏向热门标记，而忽略了标记之间的语义关系，从而加剧了流行度偏差。基于这些见解，我们提出了 CRAB，这是一种针对 GeneRec 的事后（post-hoc）去偏策略，通过缓解语义标记之间的频率不平衡来减轻流行度偏差。具体而言，在给定训练好的模型的情况下，我们首先通过拆分过度热门的标记来重平衡码本，同时保留其层次化语义结构。基于调整后的码本，我们进一步引入了树状结构正则化项以增强语义一致性，鼓励在训练过程中为冷门标记生成更具信息量的表示。在真实世界数据集上的实验表明，CRAB 通过有效缓解流行度偏差，显著提升了推荐性能。

## Abstract
Generative recommendation (GeneRec) has introduced a new paradigm that represents items as discrete semantic tokens and predicts items in a generative manner. Despite its strong performance across multiple recommendation tasks, existing GeneRec approaches still suffer from severe popularity bias and may even exacerbate it. In this work, we conduct a comprehensive empirical analysis to uncover the root causes of this phenomenon, yielding two core insights: 1) imbalanced tokenization inherits and can further amplify popularity bias from historical item interactions; 2) current training procedures disproportionately favor popular tokens while neglecting semantic relationships among tokens, thereby intensifying popularity bias. Building on these insights, we propose CRAB, a post-hoc debiasing strategy for GeneRec that alleviates popularity bias by mitigating frequency imbalance among semantic tokens. Specifically, given a well-trained model, we first rebalance the codebook by splitting over-popular tokens while preserving their hierarchical semantic structure. Based on the adjusted codebook, we further introduce a tree-structured regularizer to enhance semantic consistency, encouraging more informative representations for unpopular tokens during training. Experiments on real-world datasets demonstrate that CRAB significantly improves recommendation performance by effectively alleviating popularity bias.

---

## 论文详细总结（自动生成）

这篇论文针对生成式推荐系统（Generative Recommendation, GeneRec）中的流行度偏差问题提出了名为 **CRAB** 的去偏框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：生成式推荐（将物品映射为离散语义 Token 并用 LLM 预测）虽然性能强劲，但存在严重的**流行度偏差**。实验发现，GeneRec 推荐热门物品的频率远高于传统模型（如 SASRec），甚至会进一步放大历史数据中的偏差。
*   **研究动机**：作者通过实证分析发现偏差源于两个层面：
    1.  **码本（Codebook）不平衡**：语义相似的热门物品被映射到相同的 Token，导致这些 Token 在训练集中出现频率极高，继承并放大了物品层面的偏差。
    2.  **训练过程失衡**：现有的训练目标过度关注高频 Token，忽略了 Token 间的语义关联，导致冷门 Token 的表示学习不充分。

### 2. 方法论
CRAB 提出了一种事后（Post-hoc）去偏策略，包含两个关键阶段：
*   **阶段一：码本重平衡（Codebook Rebalancing）**
    *   **识别与拆分**：识别出流行度最高的 Token（Top 5%），将其拆分为 $M$ 个新 Token。
    *   **正则化 K-means 聚类**：为了保持语义一致性，拆分过程被建模为一个带约束的聚类问题。它要求属于同一子节点的物品必须分配到同一个新父节点，同时通过平衡损失函数（Balanced Loss）确保新生成的 Token 在流行度上尽可能均衡。
*   **阶段二：层次语义对齐（Hierarchical Semantic Alignment）**
    *   **树状结构正则化器 ($L_T$)**：引入一个正则项，强制要求共享同一父节点的子 Token 在 LLM 的嵌入空间中保持表示一致性。
    *   **知识迁移**：通过这种一致性约束，冷门 Token 可以从其语义相关的“兄弟节点”中获取监督信号，从而增强其表示能力。
*   **优化策略**：结合推荐损失（交叉熵）与语义正则项，利用 LoRA 适配器对 LLM 进行高效微调。

### 3. 实验设计
*   **数据集**：使用了两个真实世界数据集：**Office**（公开数据集）和 **Industrial**（某工业级推荐场景数据集）。
*   **基准模型（Backbones）**：TIGER 和 MiniOneRec (MOR)。
*   **对比方法**：
    *   传统去偏方法：Reweighting（重采样/加权）、Reranking（后处理重排序）。
    *   LLM 专用去偏方法：D2LR（双重去偏）。
*   **评估指标**：推荐精度（HR@10, NDCG@10）和公平性指标（DGU@10, MGU@10，数值越低表示偏差越小）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练细节**：模型基于 Qwen2-0.5B 骨干网络，训练 10 个 epoch。
*   **效率表现**：CRAB 表现出极高的效率，训练时长仅为 0.28-0.38 小时，约为 D2LR 或 Reweighting 方法的 **1/10**。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了全量对比实验、消融实验（针对正则化系数 $\gamma$ 和 LoRA 的必要性）、超参数敏感性分析（针对拆分比例和拆分层级）。
*   **充分性评价**：实验设计较为充分。作者不仅对比了精度，还重点量化了偏差缓解程度，并讨论了在不同码本层级进行操作的影响（揭示了中间层 Token 往往是偏差聚集的“沙漏”瓶颈）。对比实验涵盖了从传统到前沿的多种去偏思路，结果具有说服力。

### 6. 主要结论与发现
*   **去偏效果显著**：CRAB 在 Industrial 数据集上将流行度偏差（MGU）降低了 **16.5%**，同时保持甚至略微提升了推荐精度（NDCG）。
*   **精度与公平的平衡**：相比于 Reweighting 等会导致精度大幅下降的传统方法，CRAB 实现了更好的帕累托最优。
*   **中间层是关键**：实验发现拆分码本的中间层（Level B）比拆分底层（Level C）对缓解偏差更有效。

### 7. 优点
*   **根源治理**：不同于仅在模型输出端做文章的方法，CRAB 深入到 GeneRec 的核心——“语义 Token 编码”去解决不平衡问题。
*   **高效性**：作为一种事后处理方案，它不需要从头训练庞大的 LLM，且计算开销极低，适合工业级应用。
*   **语义保持**：通过正则化 K-means 和层次约束，确保了去偏过程不会破坏物品原有的语义结构。

### 8. 不足与局限
*   **码本结构依赖**：该方法主要针对基于残差量化（RQ）的层次化码本设计，对于非层次化或随机 Token 化的生成式模型适用性待验证。
*   **超参数敏感性**：拆分比例（如 10%）和拆分数量 $M$ 的选择对结果有一定影响，可能需要针对不同数据集进行精细调优。
*   **长尾物品的本质限制**：虽然缓解了 Token 层面的偏差，但对于极其稀疏、完全缺乏语义描述的物品，提升空间可能依然受限。

（完）
