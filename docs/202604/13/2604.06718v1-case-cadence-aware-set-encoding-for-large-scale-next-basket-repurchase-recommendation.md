---
title: "CASE: Cadence-Aware Set Encoding for Large-Scale Next Basket Repurchase Recommendation"
title_zh: CASE：面向大规模下一次篮子复购推荐的节奏感知集合编码
authors: "Yanan Cao, Ashish Ranjan, Sinduja Subramaniam, Evren Korpeoglu, Kaushiki Nag, Kannan Achan"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06718v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 针对随时间变化的商品排序进行下一次购物篮复购推荐建模
tldr: 本研究针对零售推荐中的复购行为，提出了一种名为CASE的节奏感知集合编码模型。针对现有模型仅依赖离散访问顺序而忽略日历时间节奏的问题，CASE通过多尺度时间卷积显式建模商品的复购周期，并利用低复杂度的集合注意力处理商品间依赖。该方法在提升预测准确性的同时保证了工业级扩展性，在多个基准测试和大规模生产环境中均表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06718v1/fig-001.webp\", \"caption\": \"Table 2: Ablation study on the Instacart dataset. Higher is better. Best results are in bold; second best are underlined.\", \"page\": 4, \"index\": 1, \"width\": 1018, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.06718v1/fig-002.webp\", \"caption\": \"Table 1: Comparisons on Top-𝐾 performance for next basket repurchase recommendation. Higher is better. Best results per dataset and metric@K are in bold; second best are underlined.\", \"page\": 3, \"index\": 2, \"width\": 990, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2604.06718v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of CASE. Item purchase histories are transformed into binary𝑇 -day calendar-time signals, enablingmodeling of elapsed time and repurchase cadence. Shared multi-scale temporal convolutions learn population-wide recurring patterns and produce cadence embeddings c𝑖 , which are concatenated with item embeddings e𝑖 and processed by Induced Set Attention Blocks (ISAB) to model cross-item dependencies. A final MLP outputs per-item repurchase scores.\", \"page\": 2, \"index\": 3, \"width\": 1056, \"height\": 307}]"
motivation: 传统的下一次购物篮推荐模型主要基于离散的访问顺序，无法显式建模随日历时间变化的商品复购周期。
method: 采用多尺度时间卷积捕捉单商品的日历时间信号，并结合诱导集合注意力机制以亚二次方复杂度建模商品间的交互。
result: "在公开和私有数据集上均取得显著提升，工业规模评估显示Top-5准确率和召回率分别提升了8.6%和9.9%。"
conclusion: 节奏感知的建模方式结合高效的集合编码，是解决大规模复购推荐问题的有效方案。
---

## 摘要
复购行为是大规模零售推荐中的核心信号，尤其是在补货频繁的商品类别中：用户下一次购物篮中的许多商品通常是此前购买过的，且其购买时机遵循稳定的、特定于商品的节奏。然而，大多数下一次篮子复购推荐模型将历史记录表示为按访问顺序索引的离散篮子事件序列，这无法显式建模流逝的日历时间，也无法随着购买间隔天数的增加动态更新商品排名。我们提出了 CASE（用于下一次篮子复购推荐的节奏感知集合编码），它将商品级的节奏学习与跨商品交互解耦，在实现显式日历时间建模的同时保持了生产环境的可扩展性。CASE 将每个商品的购买历史表示为固定时间跨度内的日历时间信号，应用共享的多尺度时间卷积来捕捉循环节奏，并利用诱导集合注意力（induced set attention）以亚二次复杂度建模跨商品依赖关系，从而实现高效的大规模批量推理。在三个公开基准数据集和一个私有数据集上的实验表明，与强大的下一次篮子预测基线模型相比，CASE 在多个截断点下的精确率（Precision）、召回率（Recall）和 NDCG 均有一致提升。在涉及数千万用户和海量商品目录的生产规模评估中，CASE 在 Top-5 指标上实现了高达 8.6% 的相对精确率提升和 9.9% 的召回率提升，证明了可扩展的节奏感知建模在基准测试和工业场景中均能带来显著收益。

## Abstract
Repurchase behavior is a primary signal in large-scale retail recommendation, particularly in categories with frequent replenishment: many items in a user's next basket were previously purchased and their timing follows stable, item-specific cadences. Yet most next basket repurchase recommendation models represent history as a sequence of discrete basket events indexed by visit order, which cannot explicitly model elapsed calendar time or update item rankings as days pass between purchases. We present CASE (Cadence-Aware Set Encoding for next basket repurchase recommendation), which decouples item-level cadence learning from cross-item interaction, enabling explicit calendar-time modeling while remaining production-scalable. CASE represents each item's purchase history as a calendar-time signal over a fixed horizon, applies shared multi-scale temporal convolutions to capture recurring rhythms, and uses induced set attention to model cross-item dependencies with sub-quadratic complexity, allowing efficient batch inference at scale. Across three public benchmarks and a proprietary dataset, CASE consistently improves Precision, Recall, and NDCG at multiple cutoffs compared to strong next basket prediction baselines. In a production-scale evaluation with tens of millions of users and a large item catalog, CASE achieves up to 8.6% relative Precision and 9.9% Recall lift at top-5, demonstrating that scalable cadence-aware modeling yields measurable gains in both benchmark and industrial settings.

---

## 论文详细总结（自动生成）

这篇论文由沃尔玛全球科技（Walmart Global Tech）的研究团队提交至 SIGIR '26，提出了一种名为 **CASE** 的新型推荐模型，旨在解决大规模零售场景下的“下一次购物篮复购推荐（NBRR）”问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在零售场景中，用户经常重复购买特定商品（如每周买牛奶、每月买清洁剂）。现有的推荐模型大多将用户历史视为“购物篮序列”（按次序排列），忽略了**实际流逝的日历时间**。
*   **研究动机**：
    *   **时间错位**：基于次序的模型无法区分“1天内买了3次”和“3个月内买了3次”的区别，导致推荐时机不准。
    *   **静态评分**：由于不考虑日历时间，模型评分在两次交易之间是静态的，无法反映商品是否“过期”或“即将到期”。
    *   **扩展性瓶颈**：现有的强力基线（如基于 KNN 的方法）在处理数千万用户时，推理成本过高，难以在生产环境中部署。

### 2. 方法论：CASE 架构
CASE 的核心思想是将**商品级的节奏学习**与**跨商品交互**解耦，利用日历时间信号进行建模。
*   **日历时间历史表示**：为每个商品构建一个长度为 $T$ 天的二进制向量（0/1），记录该商品在过去 $T$ 天内的购买情况。这保留了真实的购买间隔。
*   **多尺度时间卷积（Multi-Scale CNN）**：使用共享权重的 1D 卷积核，分别对应**每周（7天）、每两周（14天）、每月（28天）、每季度（91天）和趋势（182天）**。这种设计能捕捉不同周期的复购节奏。
*   **诱导集合注意力（ISAB）**：为了建模购物篮内商品间的依赖关系，采用了 Set Transformer 中的诱导集合注意力块。相比于标准自注意力 $O(n^2)$ 的复杂度，ISAB 通过 $K$ 个诱导点将复杂度降低到 $O(nK)$，极大地提升了处理大规模候选商品集的速度。
*   **评分与训练**：将节奏嵌入与商品嵌入拼接，通过 MLP 输出复购概率。使用二元交叉熵损失进行训练。

### 3. 实验设计
*   **数据集**：
    *   **公开数据集**：Instacart（在线杂货）、TaFeng（大卖场）、DC（Dunnhumby 碳水化合物数据）。
    *   **私有数据集**：来自沃尔玛的大规模杂货数据集（包含数千万用户）。
*   **基准方法（Baselines）**：
    *   **TIFUKNN**：基于 KNN 的强力复购基线（但推理慢）。
    *   **DNNTSP**：基于 GNN 和时间注意力的模型。
    *   **BERT4NBR**：将 BERT 应用于购物篮序列的模型。
    *   **PIETSP**：基于集合建模的最新可扩展模型。
*   **评价指标**：Precision@k, Recall@k, NDCG@k ($k \in \{1, 3, 5, 10\}$)。

### 4. 资源与算力
*   **实现细节**：使用 PyTorch 实现，Adam 优化器，学习率 $10^{-3}$，训练 30 个 epoch。
*   **硬件说明**：论文**未明确提及**具体的 GPU 型号、数量或具体的训练时长。但提到其设计目标是“生产规模可扩展”，并在数千万用户级别进行了评估。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同特征的数据集（从稀疏到稠密），进行了全面的性能对比。
*   **消融实验**：针对 CNN 模块、集合编码器、商品嵌入等进行了拆解实验，验证了各部分的贡献。
*   **生产验证**：在真实的工业级生产流水线上进行了对比，结果显示了显著的相对提升。
*   **客观性**：实验对比了多种类型的基线（KNN、GNN、Transformer），且在不同规模的数据集上表现一致，具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **节奏建模是关键**：消融实验证明，多尺度 CNN 捕捉的日历时间节奏对性能提升贡献最大。
*   **性能优越**：CASE 在所有数据集上均达到或超过了最强基线 TIFUKNN，且在处理稀疏数据（如 TaFeng）时优势更明显。
*   **工业价值**：在生产环境中，CASE 相比现有模型在 Precision@5 上提升了 **8.6%**，Recall@5 提升了 **9.9%**。
*   **嵌入依赖降低**：即使去掉商品 Embedding，仅靠节奏信号，模型依然能保持较强性能，这有助于减少对庞大 Embedding 表的维护成本。

### 7. 优点与亮点
*   **显式时间建模**：通过二进制时间序列解决了传统模型无法感知日历时间流逝的痛点。
*   **高扩展性**：ISAB 的引入和共享 CNN 权重使得模型推理成本与用户总数解耦，真正支持千万级用户的实时推理。
*   **多尺度感知**：能够同时捕捉短期（周）和长期（季度）的消费习惯。

### 8. 不足与局限
*   **冷启动限制**：由于模型依赖于历史复购节奏，对于首次购买或购买次数极少的用户/商品，效果可能受限。
*   **特征单一**：目前主要聚焦于时间节奏和商品 ID，尚未显式引入价格、促销、季节性节假日等外部影响因素。
*   **窗口限制**：$T$ 天的固定窗口可能无法捕捉超过半年的超长周期复购行为。

（完）
