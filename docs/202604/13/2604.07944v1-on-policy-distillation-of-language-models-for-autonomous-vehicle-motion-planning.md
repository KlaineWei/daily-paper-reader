---
title: On-Policy Distillation of Language Models for Autonomous Vehicle Motion Planning
title_zh: 自动驾驶汽车运动规划中的语言模型同策略蒸馏
authors: "Amirhossein Afsharrad, Amirhesam Abedsoltan, Ahmadreza Moradipari, Sanjay Lall"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07944v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 将运动规划知识从大参数LLM蒸馏到更小、更易部署的学生模型中
tldr: 本研究旨在解决大型语言模型（LLM）在自动驾驶车载系统中部署难的问题，提出通过在线广义知识蒸馏（GKD）将运动规划知识从大模型迁移至小模型。基于GPT-Driver框架，研究对比了GKD与强化学习方案，证明了GKD能使参数量缩小5倍的学生模型在nuScenes基准上达到接近教师模型的性能，为高效部署LLM规划器提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07944v1/fig-001.webp\", \"caption\": \"Fig. 1. Example input prompt and expected model output. The model generates a chain-of-thought reasoning trace before producing the final trajectory coordinates.\", \"page\": 3, \"index\": 1, \"width\": 504, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.07944v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative trajectory comparison on a scenario where the ego vehicle executes a left turn. The ego vehicle is represented by the black box at the origin, and the other black boxes are the bounding boxes of surrounding agents detected by the perception system. The teacher (blue) correctly follows the ground-truth trajectory (black). The GKD student (green) misses the turn but stays closer to the ground truth (ADE 3.09 m) than the RL student (red), which deviates most severely (ADE 6.29 m vs. teacher ADE 1.51 m). No collision occurs in any prediction.\", \"page\": 6, \"index\": 2, \"width\": 922, \"height\": 312}]"
motivation: 针对大型语言模型在资源受限的自动驾驶车载系统中难以实时部署的挑战，探索高效的模型压缩与知识迁移方法。
method: 采用在线广义知识蒸馏（GKD）框架，利用教师模型的密集Token级反馈对学生模型自生成输出进行训练，并与强化学习基准进行对比。
result: 实验表明GKD显著优于强化学习基准，在模型规模缩小5倍的情况下，其规划性能仍能紧追教师模型水平。
conclusion: 在线蒸馏是实现基于LLM的自动驾驶规划器在实际系统中高效、轻量化部署的一种原则性且有效的方法。
---

## 摘要
大语言模型（LLMs）最近通过将轨迹预测重新表述为语言生成问题，在自动驾驶汽车运动规划中展现出巨大潜力。然而，在资源受限的车载系统中部署高性能 LLM 仍是一项根本性挑战。本文研究了如何有效地将运动规划知识从大型教师 LLM 迁移到更小、更易部署的学生模型。我们基于 GPT-Driver 框架（该框架将驾驶场景表示为语言提示词，并通过思维链推理生成路径点轨迹），探讨了两种学生模型训练范式：(i) 同策略广义知识蒸馏（GKD），利用教师模型的密集 Token 级反馈，在学生模型自身生成的输出上进行训练；(ii) 密集反馈强化学习（RL）基准，在策略梯度框架下将教师模型的对数概率作为逐 Token 的奖励信号。在 nuScenes 基准上的实验表明，尽管模型规模缩小了 5 倍，GKD 的表现仍显著优于 RL 基准，并接近教师模型的性能。这些结果突显了同策略蒸馏作为一种规范且有效的方法，在自动驾驶系统中部署基于 LLM 的规划器时所具有的实用价值。

## Abstract
Large language models (LLMs) have recently demonstrated strong potential for autonomous vehicle motion planning by reformulating trajectory prediction as a language generation problem. However, deploying capable LLMs in resource-constrained onboard systems remains a fundamental challenge. In this paper, we study how to effectively transfer motion planning knowledge from a large teacher LLM to a smaller, more deployable student model. We build on the GPT-Driver framework, which represents driving scenes as language prompts and generates waypoint trajectories with chain-of-thought reasoning, and investigate two student training paradigms: (i) on-policy generalized knowledge distillation (GKD), which trains the student on its own self-generated outputs using dense token-level feedback from the teacher, and (ii) a dense-feedback reinforcement learning (RL) baseline that uses the teacher's log-probabilities as per-token reward signals in a policy gradient framework. Experiments on the nuScenes benchmark show that GKD substantially outperforms the RL baseline and closely approaches teacher-level performance despite a 5$\times$ reduction in model size. These results highlight the practical value of on-policy distillation as a principled and effective approach to deploying LLM-based planners in autonomous driving systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《On-Policy Distillation of Language Models for Autonomous Vehicle Motion Planning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何将大型语言模型（LLM）强大的运动规划能力，在不显著损失性能的前提下，迁移到参数量更小、能够实时运行在车载嵌入式硬件上的小型模型中。
*   **背景**：虽然 LLM（如 GPT-Driver）在处理自动驾驶长尾场景和提供可解释性（思维链推理）方面表现出色，但其巨大的计算开销使其难以在实际车辆中部署。
*   **研究动机**：传统的监督蒸馏存在“训练-推理分布不一致”问题，即学生模型在推理时一旦产生微小偏差，错误会随时间步迅速累积。本文旨在通过“同策略（On-Policy）”蒸馏技术解决这一痛点。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用**广义知识蒸馏（GKD）**。不同于让学生模型模仿教师模型在标准答案上的表现，GKD 让学生模型自己生成轨迹（On-Policy），然后由教师模型对这些“自产”的 Token 序列给出全词表的概率分布反馈，指导学生纠正错误。
*   **关键技术细节**：
    *   **任务建模**：继承 GPT-Driver 框架，将感知数据、车辆状态和目标转化为自然语言 Prompt，输出包含“显著物体、潜在影响、元动作、轨迹坐标”的结构化文本。
    *   **GKD 目标函数**：最小化学生模型输出分布与教师模型分布之间的广义 Jensen-Shannon 散度（JSD）。这种方法比单纯的 KL 散度更平衡，既能覆盖模式也能寻求最优模式。
    *   **对比基准（RL 方案）**：实现了一个基于策略梯度的强化学习（RL）基准，将教师模型的对数概率作为标量奖励信号（Reward Shaping），用于对比“全分布反馈”与“标量奖励反馈”的优劣。

### 3. 实验设计
*   **数据集**：使用自动驾驶领域权威的 **nuScenes** 数据集，包含 1000 个复杂驾驶场景。
*   **模型配置**：
    *   **教师模型**：Qwen3-8B（经过有监督微调 SFT）。
    *   **学生模型**：Qwen3-1.7B（参数量缩小约 5 倍）。
*   **评估指标**：
    *   **轨迹精度**：L2 位移误差（分为 1s、2s、3s 跨度，采用 STP-3 和 UniAD 两种主流平均计算标准）。
    *   **安全性**：碰撞率（Collision Rate）。
    *   **可靠性**：格式错误率（Format Error）。
*   **对比方法**：教师模型（上限）、GKD 蒸馏学生模型、密集反馈 RL 蒸馏学生模型。

### 4. 资源与算力
*   **硬件设施**：使用了单节点 **8 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   教师模型：使用 DeepSpeed ZeRO-3 优化，学习率 $10^{-4}$，训练 5 个 Epoch。
    *   学生模型：使用 TRL 库的 `GKDTrainer`，学习率 $5 \times 10^{-5}$，有效 Batch Size 为 8。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到所有模型均训练了 5 个 Epoch，并根据验证集表现选择了最佳 Checkpoint（通常在第 1 或第 3 个 Epoch）。

### 5. 实验数量与充分性
*   **实验规模**：在 nuScenes 验证集的 **5,119 个帧**上进行了全面测试。
*   **充分性评价**：
    *   **客观性**：实验在相同的 Prompt 模板、分词器和评估脚本下进行，确保了公平性。
    *   **对比维度**：不仅对比了最终精度，还深入探讨了 RL 方案与 GKD 方案在处理坐标 Token 时的本质差异。
    *   **局限性**：实验主要集中在模型压缩算法的对比，缺乏针对不同参数规模（如从 70B 蒸馏到 7B）或不同架构模型之间的交叉验证。

### 6. 主要结论与发现
*   **GKD 表现优异**：1.7B 的 GKD 学生模型在 L2 误差上仅比 8B 教师模型高出约 5-6%，性能极其接近。
*   **显著超越 RL 方案**：GKD 在所有指标上均大幅领先 RL 基准（RL 的 L2 误差比 GKD 高出 40% 以上），证明了**全词表分布反馈**对于坐标生成这种结构化任务至关重要。
*   **安全性保持**：GKD 学生模型的碰撞率极低（0.138%），远优于 RL 方案（0.363%），证明了同策略蒸馏能有效提升安全性。
*   **训练稳定性**：GKD 训练过程比 RL 更加平滑稳定，不易出现策略梯度方法常见的过拟合或震荡。

### 7. 优点（亮点）
*   **实用性强**：针对 LLM 落地难的痛点，提供了切实可行的 5 倍压缩方案。
*   **理论联系实际**：深入解释了为什么运动规划这种序列决策任务需要“同策略”反馈，以及为什么全分布监督优于标量奖励。
*   **端到端透明**：保留了思维链（CoT）输出，使得压缩后的模型依然具备可解释性。

### 8. 不足与局限
*   **闭环仿真缺失**：实验均在 nuScenes 的离线数据集上完成（开环评估），未在 CARLA 等模拟器中进行闭环测试，无法完全反映模型在实时交互中的动态避障能力。
*   **输入模态单一**：目前主要依赖文本化的感知结果，未直接处理图像或点云等原始传感器数据。
*   **泛化性验证**：尚未在 nuScenes 之外的其他城市或国家数据集上验证蒸馏后的泛化性能。

（完）
