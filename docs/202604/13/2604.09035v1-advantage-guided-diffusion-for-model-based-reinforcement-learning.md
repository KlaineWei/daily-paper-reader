---
title: Advantage-Guided Diffusion for Model-Based Reinforcement Learning
title_zh: 优势引导的扩散模型在基于模型的强化学习中的应用
authors: "Daniele Foffano, Arvid Eriksson, David Broman, Karl H. Johansson, Alexandre Proutiere"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09035v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于模型强化学习的优势引导扩散，旨在提高长期回报
tldr: 针对基于扩散模型的世界模型在强化学习中存在的短视问题，本文提出优势引导扩散（AGD-MBRL）。该方法利用优势函数估计来引导逆向扩散过程，使生成的轨迹集中在具有更高长期回报的区域。通过引入SAG和EAG两种引导机制，在不改变训练目标的情况下显著提升了样本效率和最终性能，在MuJoCo任务中表现优于现有基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09035v1/fig-001.webp\", \"caption\": \"Fig. 2: Training curves for the Mujoco environments: HalfCheetah, Hopper, Walker and Reacher. Shaded areas indicate the standard error. The curves have been smoothed by a running average for better readability.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 526}]"
motivation: 现有的扩散世界模型引导方式要么忽略价值信息，要么因仅依赖即时奖励而导致在短预测步长下表现短视。
method: 提出利用优势函数引导状态生成的AGD-MBRL框架，并设计了Sigmoid和指数两种引导函数（SAG和EAG）来优化轨迹采样。
result: 在MuJoCo控制任务中，该方法在样本效率和最终回报上显著超过了PolyGRAD、奖励引导模型及PPO等基准，部分任务提升达2倍。
conclusion: 优势引导是解决扩散模型在基于模型的强化学习中短视问题的简单且有效的方案，能显著提升长时预测的质量。
---

## 摘要
基于自回归世界模型的强化学习（MBRL）面临复合误差问题，而扩散世界模型通过联合生成轨迹段来缓解这一问题。然而，现有的扩散引导方法要么仅基于策略（丢弃了价值信息），要么基于奖励（在扩散视界较短时会产生短视问题）。我们提出了用于 MBRL 的优势引导扩散（AGD-MBRL），它利用智能体的优势估计来引导逆向扩散过程，使采样集中在预期能产生超出生成窗口的高长期回报的轨迹上。我们开发了两种引导方式：(i) Sigmoid 优势引导 (SAG) 和 (ii) 指数优势引导 (EAG)。我们证明，通过 SAG 或 EAG 引导的扩散模型允许我们对轨迹进行重加权采样，其权重随状态-动作优势的增加而增加——这在标准假设下意味着策略改进。此外，我们表明，与无引导的扩散模型相比，AGD-MBRL 生成的轨迹遵循改进的策略（即具有更高的价值）。AGD 通过引导状态组件同时保持动作生成受策略条件约束，与 PolyGRAD 风格的架构无缝集成，且无需更改扩散训练目标。在 MuJoCo 控制任务（HalfCheetah、Hopper、Walker2D 和 Reacher）上，AGD-MBRL 在样本效率和最终回报方面优于 PolyGRAD、在线 Diffuser 风格的奖励引导以及无模型基准（PPO/TRPO），在某些情况下提升幅度达 2 倍。这些结果表明，优势感知引导是解决扩散模型 MBRL 中短视界问题的一种简单且有效的补救措施。

## Abstract
Model-based reinforcement learning (MBRL) with autoregressive world models suffers from compounding errors, whereas diffusion world models mitigate this by generating trajectory segments jointly. However, existing diffusion guides are either policy-only, discarding value information, or reward-based, which becomes myopic when the diffusion horizon is short. We introduce Advantage-Guided Diffusion for MBRL (AGD-MBRL), which steers the reverse diffusion process using the agent's advantage estimates so that sampling concentrates on trajectories expected to yield higher long-term return beyond the generated window. We develop two guides: (i) Sigmoid Advantage Guidance (SAG) and (ii) Exponential Advantage Guidance (EAG). We prove that a diffusion model guided through SAG or EAG allows us to perform reweighted sampling of trajectories with weights increasing in state-action advantage-implying policy improvement under standard assumptions. Additionally, we show that the trajectories generated from AGD-MBRL follow an improved policy (that is, with higher value) compared to an unguided diffusion model. AGD integrates seamlessly with PolyGRAD-style architectures by guiding the state components while leaving action generation policy-conditioned, and requires no change to the diffusion training objective. On MuJoCo control tasks (HalfCheetah, Hopper, Walker2D and Reacher), AGD-MBRL improves sample efficiency and final return over PolyGRAD, an online Diffuser-style reward guide, and model-free baselines (PPO/TRPO), in some cases by a margin of 2x. These results show that advantage-aware guidance is a simple, effective remedy for short-horizon myopia in diffusion-model MBRL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AGD-MBRL** 的新框架，旨在通过“优势引导”解决扩散模型在基于模型的强化学习（MBRL）中的短视问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：传统的自回归世界模型在生成长轨迹时会产生“复合误差”。扩散模型（Diffusion Models）通过联合生成整个轨迹段，有效缓解了这一问题。
*   **核心问题**：现有的扩散引导机制存在缺陷。例如，PolyGRAD 仅基于策略引导（忽略价值信息），而 Diffuser 基于奖励引导。由于计算成本限制，扩散模型的预测视界（Horizon）通常较短，仅依赖视界内的累积奖励会导致“短视”行为，忽略了视界之外的长期回报。
*   **研究动机**：利用强化学习中的**优势函数（Advantage Function）**来引导扩散过程。优势函数包含了对未来长期价值的估计，能够引导模型生成更有利于策略改进的高质量轨迹。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在逆向扩散（去噪）过程中，利用优势函数的梯度来修正生成的轨迹，使其向高优势值的区域偏移。
*   **关键技术细节**：
    *   **两种引导机制**：
        1.  **Sigmoid 优势引导 (SAG)**：将单步轨迹的“优越性”建模为优势函数的 Sigmoid 概率。它比较保守，能防止优势函数估计过高带来的偏差。
        2.  **指数优势引导 (EAG)**：采用能量模型形式，以优势函数的指数作为权重。它引导力度更强，在价值函数估计准确时收敛更快。
    *   **理论证明**：作者从数学上证明了使用 SAG 或 EAG 引导的采样等同于对轨迹进行重加权，且生成的轨迹分布对应于一个性能更优（J(π) 更高）的策略。
    *   **算法流程**：在 Dyna 风格的循环中，首先用当前策略收集真实数据训练扩散模型；然后在采样阶段，计算优势函数关于轨迹状态的梯度，并将其加入去噪步骤中；最后在这些生成的“想象”轨迹上改进策略。

### 3. 实验设计
*   **场景/数据集**：使用了 MuJoCo 连续控制基准测试中的四个典型任务：`HalfCheetah`、`Hopper`、`Walker2D` 和 `Reacher`。
*   **Benchmark 与对比方法**：
    *   **PolyGRAD**：基于策略引导的扩散 MBRL 基准。
    *   **Online Diffuser**：作者将原始 Diffuser 的奖励引导机制适配到在线环境作为对比。
    *   **无模型基准**：PPO 和 TRPO（经典的强化学习算法）。
*   **实验设置**：所有方法均在 150 万（1.5M）环境交互步数下进行训练。

### 4. 资源与算力
*   **算力支持**：论文致谢中提到使用了瑞典国家超级计算基础设施（NAISS）的资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或确切的训练时长。但提到扩散模型使用了基于 MLP 的架构，并使用 Adam 优化器。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的物理仿真环境下进行了对比实验，每个实验都包含了训练曲线和最终回报的统计。
*   **充分性与公平性**：
    *   实验对比了同类最先进的扩散 MBRL 方法以及经典的无模型方法，具有较好的代表性。
    *   展示了标准差（阴影区域），说明考虑了随机性影响。
    *   **不足**：实验环境主要集中在 MuJoCo 任务上，缺乏更复杂或高维视觉输入的任务验证；文中未见详细的消融实验（例如针对引导强度 $\alpha$ 的敏感度分析）。

### 6. 主要结论与发现
*   **性能提升**：AGD-MBRL 在大多数任务中显著优于 PolyGRAD 和奖励引导的 Diffuser，在 `HalfCheetah` 任务中性能提升接近 2 倍。
*   **解决短视**：实验证明，即使在短视界设置下，优势引导也能捕捉到长期的价值信息，避免了奖励引导带来的局部最优问题。
*   **稳定性**：AGD-MBRL 的训练曲线比其他扩散基准更平滑，表现出更好的学习稳定性。

### 7. 优点
*   **理论完备**：为优势引导提供了策略改进的理论保证，证明了引导采样与策略提升之间的等价性。
*   **即插即用**：该方法不需要改变扩散模型的训练目标，只需在推理（采样）阶段加入梯度引导，易于集成到现有框架。
*   **效果显著**：通过简单的数学变换（SAG/EAG）显著提升了样本效率。

### 8. 不足与局限
*   **计算开销**：扩散模型本身需要多次迭代去噪，生成速度慢，这在在线 MBRL 中是一个显著的计算瓶颈。
*   **依赖价值估计**：引导的效果高度依赖于 RL 智能体学习到的优势函数（A-function）的准确性。如果价值函数估计偏差很大，引导可能会失效或误导。
*   **环境覆盖面**：目前仅在 MuJoCo 这种相对简单的连续控制任务上验证，尚未在具有稀疏奖励或非平滑动力学的复杂任务中测试。

（完）
