---
title: "DIAURec: Dual-Intent Space Representation Optimization for Recommendation"
title_zh: DIAURec：面向推荐的双意图空间表示优化
authors: "Yu Zhang, Yiwen Zhang, Yi Zhang, Lei Sang"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09087v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 个性化推荐系统的表示优化
tldr: 针对推荐系统中稀疏交互导致用户表征不全的问题，本文提出DIAURec框架。该框架结合协同与语言信号，在原型和分布双重意图空间中重构表征，并引入对齐与均匀性优化目标及多粒度匹配策略。实验证明，该方法能有效增强表征一致性与鲁棒性，在三大数据集上性能均优于15种主流基线模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09087v1/fig-001.webp\", \"caption\": \"Figure 1: (a) Intent-based recommendation paradigm. (b) LLM-based recommendation paradigm. (c) Representation reconstruction through dual-intent space (i.e., prototype and distribution intents), with a comprehensive optimization strategy for personalized recommendation.\", \"page\": 2, \"index\": 1, \"width\": 1056, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.09087v1/fig-002.webp\", \"caption\": \"Table 2: A comparison between DIAURec and representative state-of-the-art baselines is reported. The best results are highlighted in bold, and the second-best are underlined. R@ and N@ denote Recall@ and NDCG@, respectively. “Improv.%” represents the relative improvement over the strongest baseline. Statistical significance is assessed using a t-test with 𝑝 < 0.05.\", \"page\": 7, \"index\": 2, \"width\": 1058, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.09087v1/fig-003.webp\", \"caption\": \"Figure 2: The complete framework of the proposed DIAURec. DIAURec contains two major components: (i) Prototype- and distribution-intent reconstruction for representations, and (ii) Comprehensive optimization of representation spaces with alignment–uniformity objectives, fine- and coarse-grained matching, and regularization technologies for recommendation.\", \"page\": 4, \"index\": 3, \"width\": 1056, \"height\": 457}, {\"url\": \"assets/figures/arxiv/2604.09087v1/fig-004.webp\", \"caption\": \"Figure 5: Hyperparameter sensitivities w.r.t. Recall@20 and NDCG@20 for the matching weight 𝜆1 and regularization weight 𝜆2 on Amazon-book and Yelp datasets.\", \"page\": 9, \"index\": 4, \"width\": 498, \"height\": 303}]"
motivation: 现有研究多关注模型可解释性而忽视了表征优化，导致在稀疏交互下难以准确捕捉用户偏好并容易出现表征坍塌。
method: 提出DIAURec框架，通过协同和语言信号构建双重意图空间，并结合对齐、均匀性优化及粗细粒度匹配策略来重构和优化用户表征。
result: 在三个公开数据集上的实验结果显示，DIAURec在各项评价指标上均显著优于包括意图建模和语言建模在内的15种最先进基线方法。
conclusion: 通过统一意图与语言建模并实施全面的表征优化策略，可以显著提升推荐系统在处理稀疏数据时的有效性和表征质量。
---

## 摘要
通用推荐系统通过学习用户和物品的表示来提供个性化服务，其核心挑战在于如何捕捉潜在的用户偏好。然而，从稀疏交互中导出的表示往往无法全面刻画用户行为，从而限制了推荐效果。近期的研究尝试通过复杂的建模策略（如意图建模或语言建模）来增强用户表示。尽管如此，大多数工作主要关注模型的可解释性，而非表示优化。这种不平衡导致进展有限，因为表示优化通过在特征空间中提升用户与其交互物品之间的亲和力，对推荐质量至关重要，但目前在很大程度上被忽视了。为了克服这些局限性，我们提出了 DIAURec，这是一种统一了推荐中意图建模和语言建模的新型表示学习框架。DIAURec 基于由协同信号和语言信号构成的原型意图空间和分布意图空间来重构表示。此外，我们设计了一套全面的表示优化策略。具体而言，我们采用对齐性（alignment）和均匀性（uniformity）作为主要优化目标，并结合粗粒度和细粒度匹配，以实现跨不同空间的有效对齐，从而增强表示的一致性。此外，我们还引入了空间内正则化和交互正则化，以增强模型的鲁棒性，并防止重构空间表示中的表示崩溃。在三个公开数据集上针对 15 种基准方法的实验表明，DIAURec 一贯优于最先进的基准模型，充分验证了其有效性和优越性。

## Abstract
General recommender systems deliver personalized services by learning user and item representations, with the central challenge being how to capture latent user preferences. However, representations derived from sparse interactions often fail to comprehensively characterize user behaviors, thereby limiting recommendation effectiveness. Recent studies attempt to enhance user representations through sophisticated modeling strategies ($e.g.,$ intent or language modeling). Nevertheless, most works primarily concentrate on model interpretability instead of representation optimization. This imbalance has led to limited progress, as representation optimization is crucial for recommendation quality by promoting the affinity between users and their interacted items in the feature space, yet remains largely overlooked. To overcome these limitations, we propose DIAURec, a novel representation learning framework that unifies intent and language modeling for recommendation. DIAURec reconstructs representations based on the prototype and distribution intent spaces formed by collaborative and language signals. Furthermore, we design a comprehensive representation optimization strategy. Specifically, we adopts alignment and uniformity as the primary optimization objectives, and incorporates both coarse- and fine-grained matching to achieve effective alignment across different spaces, thereby enhancing representational consistency. Additionally, we further introduce intra-space and interaction regularization to enhance model robustness and prevent representation collapse in reconstructed space representation. Experiments on three public datasets against fifteen baseline methods show that DIAURec consistently outperforms state-of-the-art baselines, fully validating its effectiveness and superiority.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《DIAURec: Dual-Intent Space Representation Optimization for Recommendation》** 的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：推荐系统主要通过学习用户和物品的表征（Representation）来捕捉偏好。然而，现有的方法面临三大挑战：
    1.  **交互稀疏性与不确定性**：仅依赖稀疏的交互数据难以构建鲁棒的表征，且忽略了用户行为中的噪声。
    2.  **语义利用效率低**：虽然引入大语言模型（LLM）能增强语义，但直接集成 LLM 会带来巨大的计算开销。
    3.  **表征优化被忽视**：现有研究多关注“如何建模意图”，而忽视了“如何优化表征空间”，导致不同来源的表征（如协同信号与语义信号）之间存在不一致性，甚至出现**表征坍塌（Representation Collapse）**。
*   **研究背景**：论文旨在通过统一意图建模与语言建模，并引入“对齐性（Alignment）”与“均匀性（Uniformity）”理论，直接优化推荐系统的表征空间。

### 2. 方法论：DIAURec 框架
DIAURec 的核心思想是构建双意图空间并实施多层次的表征优化：
*   **双意图空间建模**：
    *   **原型意图（Prototype Intent）**：利用 LLM 生成用户/物品画像，通过编码器转化为语义向量，并将其映射到一组可学习的“原型”上，捕捉稳定的语义偏好。
    *   **分布意图（Distribution Intent）**：将协同过滤（CF）学习到的 ID 表征视为 **von Mises–Fisher (vMF) 分布**，通过采样捕捉交互中的不确定性和潜在分布特征。
*   **表征重构**：将 GCN 提取的结构化表征与上述两种意图（原型+分布）进行融合，生成最终的重构表征。
*   **综合优化策略**：
    *   **对齐与均匀性（AU）损失**：对齐性鼓励正样本对（用户-物品）在空间上靠近；均匀性确保表征在超球面上均匀分布，避免特征退化。
    *   **多粒度匹配**：
        *   **粗粒度匹配**：通过正交映射将重构表征与语义锚点对齐，保持全局一致性。
        *   **细粒度匹配**：利用互信息（MI）理论，挖掘近邻样本进行局部相似性强化，缓解样本孤立问题。
    *   **双重正则化**：引入空间内（Intra-space）和交互（Interaction）对比学习正则化，增强模型鲁棒性。

### 3. 实验设计
*   **数据集**：使用了三个公开的真实世界基准数据集：**Amazon-book**、**Yelp** 和 **Steam**。
*   **基准方法（Benchmark）**：对比了 15 种最先进的方法，分为三类：
    1.  **基于 ID 的方法**：如 LightGCN, SimGCL, DCCF, BIGCF, SimGCF。
    2.  **基于 LLM 的方法**：如 KAR, LLMRec, RLMRec, AlphaRec, IRLLRec。
    3.  **基于 AU（对齐均匀性）的方法**：如 DirectAU, MAWU, GraphAU, CARec, SIURec。
*   **评价指标**：Recall@N 和 NDCG@N (N=5, 10, 20)。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 V100）及具体数量。
*   **效率分析**：文中通过实验对比了训练时长。结果显示 DIAURec 的训练效率优于传统的 AU 类方法（如 DirectAU），且与 IRLLRec 相当。
*   **LLM 使用**：LLM 主要用于预计算（Pre-compute）用户/物品的 Profile，这种离线处理方式避免了在线推理时的巨大延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 3 个数据集上对比 15 个基线，涵盖了 6 个评价指标。
    *   **消融实验**：设计了 6 种变体（如移除细粒度匹配、移除正则化等），验证了每个模块的必要性。
    *   **稀疏性分析**：将用户按活跃度分组，验证了模型在冷启动/稀疏场景下的优越性。
    *   **超参数敏感性**：对匹配权重、正则化权重、GCN 层数等进行了详细测试。
*   **充分性评价**：实验设计非常充分且客观。通过显著性检验（p-value < 0.05）证明了改进的统计学意义，且对比了不同类别的 SOTA 模型，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：DIAURec 在所有数据集上均显著优于所有基线。在 Amazon-Book 数据集上，Recall@20 提升了约 10.31%。
*   **表征优化的重要性**：实验发现，单纯增加语义信息而不进行空间优化（如对齐和均匀性调整）会导致性能受限。
*   **均匀性的双刃剑效应**：研究发现，对**用户**表征施加均匀性约束非常有益，但对**物品**表征过度施加均匀性可能会破坏语义聚类，因此 DIAURec 采用了选择性的均匀性优化。
*   **抗稀疏能力**：通过引入 LLM 语义作为“原型意图”，模型在用户交互极少的情况下依然能保持较高的推荐准确度。

### 7. 优点与亮点
*   **理论结合紧密**：将推荐系统中的意图建模与表征学习中的对齐/均匀性理论有机结合，具有较强的理论支撑。
*   **双意图互补**：同时考虑了语义的确定性（原型）和协同信号的不确定性（分布），建模更加全面。
*   **多粒度优化**：通过粗、细粒度匹配解决了跨模态空间对齐中的分布偏移问题。
*   **效率平衡**：在引入 LLM 增强的同时，通过表征重构而非实时 LLM 推理，保证了工业落地的可行性。

### 8. 不足与局限
*   **LLM 依赖性**：虽然训练效率高，但前期仍需依赖 LLM 生成高质量的文本 Profile，对于完全缺乏文本信息的场景无法适用。
*   **动态性不足**：目前模型主要针对静态偏好建模，尚未充分讨论用户意图随时间动态演化的序列推荐场景。
*   **超参数较多**：引入了多个损失函数项（对齐、均匀、匹配、正则化），在实际应用中可能需要较多的调参工作。

（完）
