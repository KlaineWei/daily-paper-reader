---
title: Dynamic Ranked List Truncation for Reranking Pipelines via LLM-generated Reference-Documents
title_zh: 基于 LLM 生成参考文档的重排序流水线动态排序列表截断
authors: "Nilanjan Sinhababu, Soumedhik Bharati, Debasis Ganguly, Pabitra Mitra"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09492v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于重排序和检索的LLM生成参考文档
tldr: "本研究针对大语言模型（LLM）在重排序任务中面临的计算开销大和上下文限制问题，提出了一种基于LLM生成“参考文档”的动态列表截断方法。通过让LLM生成作为相关与不相关文档分界点的参考文档，实现了更高效的列表截断和列表式重排序。实验表明，该方法在TREC深度学习基准测试中优于现有截断方法，并使重排序效率提升了高达66%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09492v1/fig-001.webp\", \"caption\": \"Figure 4: RLT cutoff distributions across in-domain and outof-domain test sets show our proposed method PSI-Rank generally truncates less than 30 out of 100 documents per query. The average cutoff across test topics is marked in a red dotted line. For example, in SciFact, our method never required more than approximately 25% of the documents to achieve equivalent performance to a fixed-100 cutoff.\", \"page\": 7, \"index\": 1, \"width\": 487, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.09492v1/fig-002.webp\", \"caption\": \"Figure 5: This graph demonstrates stable reference-document generation across different models, leading to stable performance of our listwise methods. Results in this graph are averaged over four runs with different random seeds. We test by varying model hyperparameters - temperature and topp reporting minimal variance (ΔnDCG ≈ 0.0066 and 0.0041 under BM25 and SPLADE). Pivots generated by Llama-8B are the most stable across experiments, resulting in higher reranking performance.\", \"page\": 10, \"index\": 2, \"width\": 488, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.09492v1/fig-003.webp\", \"caption\": \"Figure 1: Prompt P(𝑄, 𝜏) used for pivot generation. We adopt the relevance scale used by human NIST assessors [4] and LLM-based judge UMBRELA [37].\", \"page\": 3, \"index\": 3, \"width\": 456, \"height\": 432}, {\"url\": \"assets/figures/arxiv/2604.09492v1/fig-004.webp\", \"caption\": \"Figure 2: Comparison of document relevance levels for an example query. TheHighRelevance document provides a direct answer. The Low Relevance document discusses the entity but misses the intent. The Generated Pivot sits semantically in between: it acknowledges the growth aspect (marked in\", \"page\": 3, \"index\": 4, \"width\": 506, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2604.09492v1/fig-005.webp\", \"caption\": \"Table 2: Evaluation of our dynamic RLT (PSI-Rank) on in- and out-of-domain tests. We report MAP and nDCG@10 (in-domain) and nDCG@10 (out-of-domain). Efficiency is measured by Inferences Per-Query (IPQ). Best scores are bold, second-best are underlined. Symbols ∗ and † indicate significance (paired t-test, 𝑝 = 0.05) versus PSI-Rank𝐷𝑦𝑛 and PSI-Rank𝐴𝑣𝑔, respectively.\", \"page\": 8, \"index\": 5, \"width\": 1002, \"height\": 654}, {\"url\": \"assets/figures/arxiv/2604.09492v1/fig-006.webp\", \"caption\": \"Table 4: Performance comparison of listwise rerankingmethods across different LLM re-rankers. Best and second-best scores are bold and underlined. The symbol ∗ denotes statistical significance similar to Table 3.\", \"page\": 9, \"index\": 6, \"width\": 506, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.09492v1/fig-007.webp\", \"caption\": \"Table 3: Listwise LLM based reranking performance comparison on various in and out-of-domain datasets using the generated pivot for partitioning. The best and the second-best scores for each metric and method are bold and underlined. The symbol ∗ indicates the statistical significance of a method (paired t-test with 𝑝 = 0.05) compared with the Sliding Window baseline.\", \"page\": 9, \"index\": 7, \"width\": 898, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.09492v1/fig-008.webp\", \"caption\": \"Figure 3: This diagram shows the generation of a reference-document pivot (𝐷∗) for the Query (𝑄) and a retrieved ranked list of documents from a first-stage retriever (e.g., BM25 and SPLADE [8, 29]). The utility of this pivot across pointwise, pairwise (PSI-Rank) and listwise (SNOW, VS-Sliding, and GPTD-Part) configurations for improving ranking performance and efficiency is illustrated. For pointwise and pairwise methods, we use the pivot for adaptive RLT, requiring only a single inference to calculate the pivot score using the base-retrieval method (PSI-Rank). All documents with a score above the pivot (green coloured) are selected for reranking. For listwise setups, we rerank the entire ranked list without truncation. We propose three optimized reranking methods that use the pivot to make informed decisions during the ranking. SNOW uses a parallel non-overlapping window with the pivot enabling document movement across windows. VS-Sliding utilizes the pivot to promote variable stride. It takes long strides at the bottom and gradually takes smaller strides when ranking top positions, where the density of relevant documents is higher. Finally, we show GPTD-Part, an adaptation of TD-Part (an efficient listwise reranking framework), and show that our generated pivot further improves its performance.\", \"page\": 5, \"index\": 8, \"width\": 940, \"height\": 610}]"
motivation: 旨在解决LLM重排序中由于上下文长度限制和计算开销导致的效率低下，以及现有列表截断方法缺乏主题相关性的问题。
method: 利用LLM生成参考文档作为相关性判定基准，并结合并行批处理或自适应步长的重叠窗口技术进行动态列表截断与重排序。
result: "在TREC DL等基准测试中，该方法不仅在检索性能上优于现有截断方案，且在保持高精度的同时将重排序速度提升了66%。"
conclusion: 该研究证明了LLM生成的参考文档能有效优化重排序流水线，为实现高效且语义可控的LLM检索系统提供了新范式。
---

## 摘要
大语言模型（LLM）已广泛应用于重排序。计算开销和过长的上下文长度仍然是 LLM 重排序器面临的挑战性问题。高效的重排序通常涉及从第一阶段的排序列表中选择一个子集，即排序列表截断（RLT）。截断后的列表由重排序器进一步处理。对于 LLM 重排序器，排序列表通常被划分并按批次顺序处理，以减少上下文长度。这两个步骤都涉及超参数和与主题无关的启发式方法。最近，LLM 已被证明在相关性判断方面非常有效。与之对应，我们提出 LLM 可用于生成参考文档，作为排序列表中相关文档和非相关文档之间的枢纽。我们提出了利用这些生成的参考文档进行 RLT 以及高效列表级（listwise）重排序的方法。在重排序过程中，我们以非重叠窗口的并行批次或具有自适应步长的重叠窗口处理排序列表，改进了现有的固定步长设置。实验表明，生成的参考文档还能改进现有的高效列表级重排序框架。在 TREC 深度学习基准测试上的实验表明，我们的方法优于现有的基于 RLT 的方法。域内和跨域基准测试表明，与现有方法相比，我们提出的方法将基于 LLM 的列表级重排序加速了高达 66%。这项工作不仅为高效的基于 LLM 的重排序建立了一个实用的范式，还深入揭示了 LLM 利用相关性信号生成语义受控文档的能力。

## Abstract
Large Language Models (LLM) have been widely used in reranking. Computational overhead and large context lengths remain a challenging issue for LLM rerankers. Efficient reranking usually involves selecting a subset of the ranked list from the first stage, known as ranked list truncation (RLT). The truncated list is processed further by a reranker. For LLM rerankers, the ranked list is often partitioned and processed sequentially in batches to reduce the context length. Both these steps involve hyperparameters and topic-agnostic heuristics. Recently, LLMs have been shown to be effective for relevance judgment. Equivalently, we propose that LLMs can be used to generate reference documents that can act as a pivot between relevant and non-relevant documents in a ranked list. We propose methods to use these generated reference documents for RLT as well as for efficient listwise reranking. While reranking, we process the ranked list in either parallel batches of non-overlapping windows or overlapping windows with adaptive strides, improving the existing fixed stride setup. The generated reference documents are also shown to improve existing efficient listwise reranking frameworks. Experiments on TREC Deep Learning benchmarks show that our approach outperforms existing RLT-based approaches. In-domain and out-of-domain benchmarks demonstrate that our proposed methods accelerate LLM-based listwise reranking by up to 66\% compared to existing approaches. This work not only establishes a practical paradigm for efficient LLM-based reranking but also provides insight into the capability of LLMs to generate semantically controlled documents using relevance signals.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dynamic Ranked List Truncation for Reranking Pipelines via LLM-generated Reference-Documents》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
在现代搜索系统中，多阶段检索流水线（检索+重排序）是主流。然而，**大语言模型（LLM）重排序器**面临两大瓶颈：**计算开销巨大**和**上下文长度限制**。
*   **研究动机**：现有的“排序列表截断（RLT）”方法通常依赖于固定的截断深度或与查询无关的启发式分数，忽略了特定查询的相关性分布。
*   **核心问题**：如何利用 LLM 的语义理解能力，动态且高效地确定哪些文档值得被重排序，从而在不损失（甚至提升）检索效果的前提下，大幅降低计算成本？

### 2. 论文提出的方法论
论文的核心创新在于利用 LLM 生成一个**语义参考文档（Pivot Document, $D^*$）**，将其作为相关与不相关文档之间的“语义分界点”。

*   **核心思想**：通过 Prompt 引导 LLM 生成一个针对特定查询 $Q$ 且相关性等级恰好为“中等（Marginal）”的文档。这个文档不直接参与最终排名，但作为衡量其他文档相关性的“标尺”。
*   **关键技术细节**：
    *   **PSI-Rank (点位/对位重排序)**：利用第一阶段检索器（如 BM25）对 $D^*$ 进行打分，仅将得分高于 $D^*$ 的文档送入后续重排序阶段。
    *   **SNOW (共享非重叠窗口)**：在列表式重排序中，将列表划分为多个非重叠窗口并行处理，每个窗口都加入 $D^*$ 作为参考，实现跨窗口的文档“晋升”或“降级”。
    *   **VS-Sliding (自适应步长滑动窗口)**：根据 $D^*$ 估计的文档密度动态调整滑动窗口的步长。在相关文档稀疏的尾部采取大步长（快速跳过），在密集的头部采取小步长（精细排序）。
    *   **GPTD-Part**：改进了现有的 TD-Part 算法，用生成的 $D^*$ 替代不稳定的顶部文档作为分区锚点，增强了对检索噪声的鲁棒性。

### 3. 实验设计
*   **数据集与场景**：
    *   **域内（In-domain）**：MS MARCO 集合，使用 TREC Deep Learning 2019 (DL-19) 和 2020 (DL-20) 任务。
    *   **跨域（Out-of-domain）**：BEIR 基准测试中的五个子集（COVID, SciFact, Touché, DBPedia）。
*   **Benchmark 与对比方法**：
    *   **截断基准**：Fixed-k, Greedy-k, BiCut, 以及基于 Transformer 的监督模型（Choppy, AttnCut, MtCut）。
    *   **重排序基准**：Sliding Window（固定步长）、TD-Part。
    *   **模型组合**：第一阶段使用 BM25/SPLADE；重排序器使用 mono-T5, duo-T5, RankZephyr, RankVicuna, RankGPT (GPT-4o-mini)。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或具体的训练/推理总时长。
*   **模型使用**：实验中使用了 Llama-3.2-3B、Llama-3.1-8B 和 GPT-4o 来生成参考文档。作者提供了 GitHub 仓库地址，暗示了实验的可复现性。

### 5. 实验数量与充分性
*   **实验规模**：作者在 2 个域内数据集和 4 个跨域数据集上进行了广泛测试。
*   **充分性评价**：
    *   **多维度对比**：不仅对比了检索效果（MAP, nDCG），还详细对比了效率指标（每查询推理次数 IPQ、加速比 SU）。
    *   **消融与鲁棒性**：研究了不同 LLM 生成 Pivot 对结果的影响（RQ3），并测试了在不同重排序器下的表现。
    *   **客观性**：使用了显著性检验（paired t-test），确保改进并非偶然。整体实验设计较为严谨，覆盖了从点位到列表式的多种重排序范式。

### 6. 论文的主要结论与发现
*   **效率大幅提升**：在列表式重排序中，该方法相比强基准（Sliding Window）实现了高达 **66% 的加速**。
*   **性能持平或更优**：在大幅减少重排序文档数量的情况下，检索精度（nDCG@10）在多数情况下优于或等同于全量重排序。
*   **Pivot 的稳定性**：实验发现，即使是较小的模型（如 Llama-3B）生成的参考文档也能起到很好的分界作用，证明了该方法对 Pivot 生成模型的依赖度较低。
*   **自适应性**：PSI-Rank 能够根据查询难度动态调整截断深度，比固定阈值方法更具灵活性。

### 7. 优点（亮点）
*   **模型无关性**：该方法不需要对重排序模型进行微调，是一种即插即用的零样本（Zero-shot）方案。
*   **创新的语义锚点**：将 LLM 从单纯的“排序者”扩展为“参考标准生成者”，为解决 RLT 问题提供了新视角。
*   **并行化潜力**：SNOW 方法通过 Pivot 解决了非重叠窗口无法交换信息的难题，使得大规模并行重排序成为可能。

### 8. 不足与局限
*   **生成开销**：虽然重排序阶段加速了，但每个查询都需要先调用一次 LLM 生成 Pivot，对于极低延迟要求的场景，这一步可能成为新的瓶颈（尽管论文称其开销可忽略）。
*   **Prompt 敏感性**：虽然实验显示了稳定性，但 Pivot 的质量仍受 Prompt 设计的影响，不同领域的最佳相关性定义可能需要微调。
*   **长文档挑战**：实验主要集中在短文本（Passage）检索，在处理超长文档或复杂文档结构时的表现尚未验证。

（完）
