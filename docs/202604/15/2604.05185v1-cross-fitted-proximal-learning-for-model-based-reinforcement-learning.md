---
title: Cross-fitted Proximal Learning for Model-Based Reinforcement Learning
title_zh: 基于模型的强化学习的交叉拟合近端学习
authors: "Nishanth Venkatesh, Andreas A. Malikopoulos"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05185v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于序列决策和规划的基于模型的强化学习
tldr: 本文针对存在隐性混杂因素的离线模型强化学习（POMDPs），提出了一种基于交叉拟合（Cross-fitting）的近端学习方法。研究聚焦于估计满足条件矩限制（CMR）的奖励-发射和观测-转移桥函数。通过引入K折交叉拟合扩展现有的两阶段估计器，提高了数据利用效率，并推导了包含干扰项估计和经验平均误差的理论界限，为复杂环境下的策略评估提供了更稳健的统计工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05185v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of observation-transition recovery\", \"page\": 6, \"index\": 1, \"width\": 512, \"height\": 325}]"
motivation: 在存在隐性混杂因素的离线部分可观测马尔可夫决策过程中，直接从观测数据学习的模型因潜在因素同时影响动作和奖励而存在偏差。
method: 提出一种K折交叉拟合的近端学习框架，将桥函数学习建模为包含条件均值嵌入和条件密度等干扰对象的条件矩限制（CMR）问题。
result: 该方法在保留原始识别策略的同时，比单次样本分割更高效地利用了数据，并获得了由干扰项估计和经验平均误差组成的理论误差界限。
conclusion: 交叉拟合近端学习为解决混杂环境下的模型强化学习提供了更具统计效率的估计方案，并为复杂POMDPs的策略评估奠定了理论基础。
---

## 摘要
基于模型的强化学习在序列决策中具有吸引力，因为它显式地估计奖励和转移模型，并支持通过模拟展开进行规划。然而，在存在隐性混杂因素的离线设置中，直接从观测数据学习的模型可能会产生偏差。这一挑战在部分可观测系统中尤为突出，其中潜在因素可能共同影响动作、奖励和未来的观测。最近的研究表明，在此类受混杂影响的部分可观测马尔可夫决策过程（POMDPs）中，策略评估可以简化为估计满足条件矩限制（CMRs）的奖励-发射和观测-转移桥函数。在本文中，我们研究了这些桥函数的统计估计。我们将桥函数学习建模为一个 CMR 问题，其冗余对象由条件均值嵌入和条件密度给出。随后，我们为现有的两阶段桥估计器开发了一个 K 折交叉拟合扩展。所提出的程序保留了原始的基于桥函数的识别策略，同时比单次样本分割更有效地利用了可用数据。我们还为该交叉拟合估计器推导了一个 oracle-comparator 误差界，并将产生的误差分解为由冗余估计引起的阶段 I 项和由经验平均引起的阶段 II 项。

## Abstract
Model-based reinforcement learning is attractive for sequential decision-making because it explicitly estimates reward and transition models and then supports planning through simulated rollouts. In offline settings with hidden confounding, however, models learned directly from observational data may be biased. This challenge is especially pronounced in partially observable systems, where latent factors may jointly affect actions, rewards, and future observations. Recent work has shown that policy evaluation in such confounded partially observable Markov decision processes (POMDPs) can be reduced to estimating reward-emission and observation-transition bridge functions satisfying conditional moment restrictions (CMRs). In this paper, we study the statistical estimation of these bridge functions. We formulate bridge learning as a CMR problem with nuisance objects given by a conditional mean embedding and a conditional density. We then develop a $K$-fold cross-fitted extension of the existing two-stage bridge estimator. The proposed procedure preserves the original bridge-based identification strategy while using the available data more efficiently than a single sample split. We also derive an oracle-comparator bound for the cross-fitted estimator and decompose the resulting error into a Stage I term induced by nuisance estimation and a Stage II term induced by empirical averaging.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：基于模型的强化学习的交叉拟合近端学习

#### 1. 核心问题与整体含义（研究动机和背景）
在离线强化学习（Offline RL）中，决策者只能利用预先收集的观测数据。然而，现实场景（如医疗记录、自动驾驶）往往存在**隐性混杂因素（Hidden Confounding）**，即某些未观测到的变量同时影响动作选择和系统演化。在部分可观测马尔可夫决策过程（POMDP）中，这种混杂会导致学习到的转移和奖励模型产生偏差，进而使下游的策略规划失效。
本文的核心问题是如何在存在混杂因素的 POMDP 中，更高效、稳健地估计用于策略评估的“桥函数”（Bridge Functions）。

#### 2. 论文提出的方法论
论文提出了一种**交叉拟合近端学习（Cross-fitted Proximal Learning）**框架，将桥函数估计建模为**条件矩限制（CMR）**问题。
*   **核心思想**：利用近端因果推理（Proximal Causal Inference）的思想，通过可观测的“代理变量”来抵消隐性混杂的影响。引入 **K 折交叉拟合（K-fold Cross-fitting）** 技术，以克服传统两阶段估计中数据利用率低和对样本划分敏感的问题。
*   **关键技术细节**：
    *   **两阶段估计**：
        *   **阶段 I（干扰项估计）**：在辅助样本上估计“干扰对象”（Nuisance Objects），包括条件均值嵌入（CME）和条件概率密度。
        *   **阶段 II（桥函数学习）**：将阶段 I 的估计值带入，在留出的验证集上通过最小化正则化经验风险来学习桥函数。
    *   **核函数表示**：利用再生核希尔伯特空间（RKHS）将复杂的条件期望运算转化为线性内积形式。
    *   **误差分解**：推导了 Oracle-comparator 误差界限，将其分解为阶段 I 的干扰项估计误差和阶段 II 的经验过程误差。

#### 3. 实验设计
*   **场景**：一个 2 步（2-step）合成的非线性高斯 POMDP 模拟环境。
*   **任务**：估计第一阶段的观测-转移桥函数，目标是恢复去混杂后的观测演化规律。
*   **Benchmark（基准）**：单次样本分割（One-shot 50-50 sample-splitting）方法。
*   **对比维度**：在不同样本量（$N=120$ 到 $1920$）下，对比交叉拟合估计器与单次分割估计器的均方误差（MSE）。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）及训练时长。
*   由于实验基于低维合成环境和核方法，推测其对算力的需求较低，普通的 CPU 或单块商用 GPU 即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：主要进行了一组核心的对比实验，展示了 MSE 随样本量增加的变化曲线。
*   **充分性评价**：
    *   **理论验证充分**：实验结果清晰地支持了交叉拟合能提高样本效率的理论推导。
    *   **广度不足**：实验仅限于合成的低维环境，缺乏在复杂高维环境（如 Atari 或 MuJoCo 的变体）或真实世界数据集上的验证。
    *   **缺乏消融**：未对 K 折中的 $K$ 值取值影响进行深入探讨。

#### 6. 论文的主要结论与发现
*   **样本效率提升**：交叉拟合估计器在所有测试样本量下均显著优于单次样本分割方法。例如，在 $N=1920$ 时，MSE 从 0.0771 降至 0.0248。
*   **统计稳健性**：交叉拟合减少了估计器对特定数据划分的依赖，使结果更加稳定。
*   **理论贡献**：成功将双重机器学习（Double Machine Learning）中的交叉拟合思想引入到混杂 POMDP 的桥函数估计中，并提供了严谨的误差分解框架。

#### 7. 优点
*   **方法论创新**：将交叉拟合与近端强化学习结合，解决了离线 MBRL 中数据利用率与偏差控制的矛盾。
*   **理论完备**：提供了详细的 Oracle 误差界限分析，为后续研究奠定了数学基础。
*   **实用性**：该方法不改变原始的识别策略，易于集成到现有的基于桥函数的 RL 算法中。

#### 8. 不足与局限
*   **缺乏正交化（Orthogonality）**：作者承认尚未推导出 Neyman-orthogonal 得分函数，这意味着第二阶段的估计仍可能受到第一阶段正则化偏差的影响。
*   **实验环境单一**：仅在简单的模拟环境中测试，未验证其在处理高维图像输入或复杂动力学系统时的表现。
*   **下游任务缺失**：论文重点在于“估计桥函数”，但未展示这些桥函数在最终策略优化（Policy Optimization）任务中能带来多大的性能提升。

（完）
