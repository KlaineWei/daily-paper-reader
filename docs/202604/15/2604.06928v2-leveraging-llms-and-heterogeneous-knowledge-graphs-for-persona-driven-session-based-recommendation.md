---
title: Leveraging LLMs and Heterogeneous Knowledge Graphs for Persona-Driven Session-Based Recommendation
title_zh: 利用大语言模型和异构知识图谱实现画像驱动的会话式推荐
authors: "Muskan Gupta, Suraj Thapa, Jyotsana Khatri"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06928v2"
tags: ["query:llmrec"]
score: 9.0
evidence: 结合画像建模的LLM增强型会话推荐
tldr: 针对会话式推荐系统在匿名会话下难以捕捉个性化偏好的挑战，本文提出一种结合大语言模型（LLM）和异构知识图谱（KG）的画像驱动框架。该方法通过构建包含外部元数据的异构图，利用无监督学习提取潜在用户画像，并将其与LLM生成的项目嵌入集成到SBRS中。实验证明，该方法在处理冷启动和稀疏数据时表现优异，显著提升了推荐的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06928v2/fig-001.webp\", \"caption\": \"Fig. 1. An overview of our proposed framework: Persona-driven session based recommendation via knowledge graph\", \"page\": 6, \"index\": 1, \"width\": 621, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.06928v2/fig-002.webp\", \"caption\": \"Table 2. Prompts used for user persona generation.\", \"page\": 11, \"index\": 2, \"width\": 725, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2604.06928v2/fig-003.webp\", \"caption\": \"Table 3. Performance comparison of user representation strategies used in the retriever module\", \"page\": 13, \"index\": 3, \"width\": 712, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2604.06928v2/fig-004.webp\", \"caption\": \"Fig. 2. Construction of KG\", \"page\": 9, \"index\": 4, \"width\": 682, \"height\": 304}]"
motivation: 传统的会话式推荐系统通常假设会话是匿名的，导致在数据稀疏或冷启动情况下难以实现精准的个性化推荐。
method: 采用两阶段架构：首先利用LLM嵌入初始化异构知识图谱并通过无监督图学习提取用户画像，随后将画像与项目嵌入集成到改进的SBRS模型中进行候选集生成与重排序。
result: "在Amazon Books和Amazon Movies & TV数据集上的实验表明，该方法在各项指标上均优于仅依赖会话历史的传统序列模型。"
conclusion: 结合结构化知识图谱信号与LLM的语义能力，能有效建模匿名会话中的用户画像，从而显著增强会话式推荐系统的性能。
---

## 摘要
会话式推荐系统（SBRS）旨在从交互序列中捕捉用户的短期意图。然而，通常假设的匿名会话限制了个性化能力，特别是在数据稀疏或冷启动的情况下。近期大语言模型（LLM）增强推荐的研究进展表明，LLM 可以生成丰富的物品表示，但由于会话的匿名性，利用 LLM 建模用户画像（Persona）仍然具有挑战性。在这项工作中，我们提出了一个画像驱动的 SBRS 框架，该框架显式地建模从异构知识图谱（KG）中推断出的潜在用户画像，并将其集成到数据驱动的 SBRS 中。我们的框架采用两阶段架构，包括个性化信息提取和个性化信息利用。在个性化信息提取阶段，我们构建了一个异构知识图谱，整合了与时间无关的用户-物品交互、物品-物品关系、物品-特征关联以及来自 DBpedia 的外部元数据。随后，我们在使用 LLM 衍生物品嵌入初始化的知识图谱上，利用异构深度图信息最大化（HDGI）目标，以无监督方式学习潜在用户画像。在个性化信息利用阶段，学习到的画像表示与 LLM 衍生物品嵌入一起被整合到改进的数据驱动 SBRS 架构中，以生成相关物品的候选集，随后使用基础序列模型进行重排序，以强调短期会话意图。与以往仅依赖序列建模或基于文本的用户表示的方法不同，我们的方法将用户画像建模建立在源自异构知识图谱的结构化关系信号之上。在 Amazon Books 和 Amazon Movies & TV 数据集上的实验表明，我们的方法相比于使用会话历史推导用户嵌入的序列模型，取得了持续的性能提升。

## Abstract
Session-based recommendation systems (SBRS) aim to capture user's short-term intent from interaction sequences. However, the common assumption of anonymous sessions limits personalization, particularly under sparse or cold-start conditions. Recent advances in LLM augmented recommendation have shown that LLMs can generate rich item representations, but modeling user personas with LLMs remains challenging due to anonymous sessions. In this work, we propose a persona driven SBRS framework that explicitly models latent user personas inferred from a heterogeneous knowledge graph (KG) and integrates them into a data-driven SBRS. Our framework adopts a two-stage architecture consisting of personalized information extraction and personalized information utilization. In the personalized information extraction stage, we construct a heterogeneous KG that integrates time-independent user-item interactions, item-item relations, item-feature associations, and external metadata from DBpedia. We then learn latent user personas in an unsupervised manner using a Heterogeneous Deep Graph Infomax (HDGI) objective over a KG initialized with LLM-derived item embeddings. In the personalized information utilization stage, the learned persona representations together with LLM-derived item embeddings are incorporated into a modified architecture of data-driven SBRS to generate a candidate set of relevant items, followed by reranking using the base sequential model to emphasize short-term session intent. Unlike prior approaches that rely solely on sequence modeling or text-based user representations, our method grounds user persona modeling in structured relational signals derived from a heterogeneous KG. Experiments on Amazon Books and Amazon Movies & TV demonstrate that our approach consistently improves over sequential models with user embeddings derived using session history.

---

## 论文详细总结（自动生成）

这是一份关于论文《Leveraging LLMs and Heterogeneous Knowledge Graphs for Persona-Driven Session-Based Recommendation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的会话式推荐系统（SBRS）通常假设会话是匿名的，仅关注短期交互序列。这导致系统难以捕捉用户长期稳定的偏好（即“画像”），在数据稀疏或冷启动场景下表现较差。
*   **研究背景**：虽然大语言模型（LLM）在理解物品语义方面表现出色，但直接用 LLM 建模用户画像存在计算成本高、缺乏结构化约束以及在匿名会话下难以推断等问题。
*   **整体含义**：本文旨在结合**异构知识图谱（HKG）的结构化关系**与**LLM 的语义表征能力**，通过无监督学习提取用户画像，从而在匿名会话中实现更精准的个性化推荐。

### 2. 方法论：核心思想与技术细节
该框架分为两个主要阶段：
*   **阶段一：个性化信息提取（Personalized Information Extraction）**
    *   **异构 KG 构建**：整合了用户-物品交互、物品-物品关系（如“常一起购买”）、物品-特征关联（品牌、类别）以及来自 DBpedia 的外部知识。
    *   **LLM 初始化**：利用 LLM（如 Qwen3-8B）对物品标题、描述和属性进行编码，生成初始语义嵌入，注入到 KG 节点中。
    *   **无监督画像学习**：采用**异构深度图信息最大化（HDGI）**算法。通过最大化局部节点表示与全局图摘要之间的互信息，在不依赖标签的情况下学习用户节点的潜在表示（Persona Embedding）。
*   **阶段二：个性化信息利用（Personalized Information Utilization）**
    *   **检索模块**：将学习到的用户画像嵌入与 LLM 衍生的物品嵌入集成到数据驱动的推荐模型中，生成候选物品集。
    *   **重排序模块**：使用基础序列模型（如 SASRec）对候选集进行重排序，以确保推荐结果既符合长期画像，又兼顾当前的短期会话意图。

### 3. 实验设计
*   **数据集**：使用了两个 Amazon 公开数据集：**Amazon Books** 和 **Amazon Movies & TV**。这些数据集包含丰富的元数据和复杂的实体关系。
*   **Benchmark 与对比方法**：
    *   **基础模型**：SASRec（自注意力序列推荐）。
    *   **对比策略**：
        1.  **None**：无用户偏好表示。
        2.  **Random**：随机初始化并训练用户嵌入。
        3.  **LLM**：直接利用 LLM 根据历史交互生成的文本关键词作为用户表示。
        4.  **KG-LLM（本文方法）**：基于异构 KG 和 HDGI 学习的画像。
*   **评价指标**：Hit Rate (HR@K), NDCG@K, MRR。

### 4. 资源与算力
*   **模型使用**：文本生成使用 **Qwen-3-8B**，文本嵌入使用 **Qwen3-Embedding-8B**。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到在超参数调优中探索了不同的层数（2-4层）、隐藏维度（64-256）和批大小（128-1024）。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域的标准数据集上进行了测试，涵盖了从检索到重排序的完整流程。
*   **充分性评价**：
    *   **对比充分**：对比了纯序列模型、随机初始化嵌入以及纯 LLM 生成的偏好表示，证明了 KG 引入的结构化信息的必要性。
    *   **客观性**：通过设置不同的 K 值（10 和 100）观察召回率和排序质量的变化，实验设计较为客观。
    *   **局限**：缺乏针对不同稀疏程度或冷启动比例的专项消融实验，且未对比其他先进的图神经网络推荐模型（如 KGAT）。

### 6. 主要结论与发现
*   **画像有效性**：引入基于 KG 的用户画像显著提升了 HR@100，表明该方法能有效扩大候选集的覆盖面，捕捉到长尾物品。
*   **协同效应**：LLM 提供的语义初始化与 KG 提供的结构化关系具有互补性，比单一使用 LLM 或序列模型效果更好。
*   **重排序的重要性**：在检索阶段引入画像后，再通过序列模型重排序，可以显著提升 NDCG 和 MRR，平衡了长期偏好与短期意图。

### 7. 优点与亮点
*   **结构化接地（Grounding）**：不同于纯文本 LLM 推荐，该方法将用户建模建立在结构化的知识图谱上，增强了模型的可解释性和鲁棒性。
*   **无监督学习**：利用 HDGI 进行画像提取，解决了匿名会话中缺乏显式用户标签的问题。
*   **两阶段解耦**：将长期画像提取与短期意图建模解耦，既保留了 LLM 的语义优势，又通过重排序降低了计算开销。

### 8. 不足与局限
*   **计算复杂度**：虽然比全 LLM 推理快，但构建大规模异构 KG 并进行 HDGI 预训练仍需较高的离线计算资源。
*   **实时性挑战**：对于完全首次访问的真·匿名用户（KG 中无节点），如何实时生成画像节点并更新图结构，文中未深入讨论。
*   **实验覆盖**：仅在 Amazon 两个类目下验证，对于社交媒体或新闻等实时性极强、实体关系较弱的场景，其泛化能力有待验证。

（完）
