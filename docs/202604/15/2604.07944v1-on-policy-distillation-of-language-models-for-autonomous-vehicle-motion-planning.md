---
title: On-Policy Distillation of Language Models for Autonomous Vehicle Motion Planning
title_zh: 用于自动驾驶汽车运动规划的语言模型同策略蒸馏
authors: "Amirhossein Afsharrad, Amirhesam Abedsoltan, Ahmadreza Moradipari, Sanjay Lall"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07944v1"
tags: ["query:slm-rl"]
score: 10.0
evidence: 大语言模型在自动驾驶运动规划中的蒸馏以及可部署的小模型
tldr: 本研究探讨了如何将大型语言模型（LLM）的自动驾驶运动规划能力迁移到小型模型中。基于GPT-Driver框架，研究者对比了在线广义知识蒸馏（GKD）与强化学习（RL）两种方案。通过让学生模型在自身生成的输出上接受教师模型的密集Token级反馈，成功在模型参数缩小5倍的情况下，使学生模型在nuScenes基准测试中达到了接近教师模型的规划性能，为LLM在车载受限系统中的部署提供了有效路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07944v1/fig-001.webp\", \"caption\": \"Fig. 1. Example input prompt and expected model output. The model generates a chain-of-thought reasoning trace before producing the final trajectory coordinates.\", \"page\": 3, \"index\": 1, \"width\": 504, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.07944v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative trajectory comparison on a scenario where the ego vehicle executes a left turn. The ego vehicle is represented by the black box at the origin, and the other black boxes are the bounding boxes of surrounding agents detected by the perception system. The teacher (blue) correctly follows the ground-truth trajectory (black). The GKD student (green) misses the turn but stays closer to the ground truth (ADE 3.09 m) than the RL student (red), which deviates most severely (ADE 6.29 m vs. teacher ADE 1.51 m). No collision occurs in any prediction.\", \"page\": 6, \"index\": 2, \"width\": 922, \"height\": 312}]"
motivation: 大型语言模型在自动驾驶规划中潜力巨大，但其庞大的计算开销使得在资源受限的车载系统中进行实时部署面临挑战。
method: 采用在线广义知识蒸馏（GKD）方法，让学生模型在自身生成的轨迹上接受教师模型的密集Token级概率反馈，并与强化学习基准进行对比。
result: 实验证明GKD显著优于强化学习方法，在模型规模缩小5倍的情况下，其规划性能已非常接近教师模型的水平。
conclusion: 在线蒸馏是实现基于LLM的自动驾驶规划器在实际系统中高效、轻量化部署的一种原则性且有效的方法。
---

## 摘要
大语言模型（LLMs）最近通过将轨迹预测重新表述为语言生成问题，在自动驾驶汽车运动规划方面展现出强大的潜力。然而，在资源受限的车载系统中部署高性能的 LLM 仍然是一个根本性的挑战。在本文中，我们研究了如何有效地将运动规划知识从大型教师 LLM 迁移到更小、更易于部署的学生模型中。我们基于 GPT-Driver 框架，该框架将驾驶场景表示为语言提示词，并通过思维链（chain-of-thought）推理生成路径点轨迹。我们研究了两种学生模型训练范式：(i) 同策略广义知识蒸馏（GKD），它利用来自教师模型的密集 Token 级反馈，在学生模型自身生成的输出上进行训练；(ii) 密集反馈强化学习（RL）基准，它在策略梯度框架中将教师模型的对数概率作为逐 Token 的奖励信号。在 nuScenes 基准测试上的实验表明，尽管模型参数量减少了 5 倍，但 GKD 的表现显著优于 RL 基准，并接近教师模型的性能水平。这些结果突显了同策略蒸馏作为一种规范且有效的方法，在自动驾驶系统中部署基于 LLM 的规划器方面具有重要的实用价值。

## Abstract
Large language models (LLMs) have recently demonstrated strong potential for autonomous vehicle motion planning by reformulating trajectory prediction as a language generation problem. However, deploying capable LLMs in resource-constrained onboard systems remains a fundamental challenge. In this paper, we study how to effectively transfer motion planning knowledge from a large teacher LLM to a smaller, more deployable student model. We build on the GPT-Driver framework, which represents driving scenes as language prompts and generates waypoint trajectories with chain-of-thought reasoning, and investigate two student training paradigms: (i) on-policy generalized knowledge distillation (GKD), which trains the student on its own self-generated outputs using dense token-level feedback from the teacher, and (ii) a dense-feedback reinforcement learning (RL) baseline that uses the teacher's log-probabilities as per-token reward signals in a policy gradient framework. Experiments on the nuScenes benchmark show that GKD substantially outperforms the RL baseline and closely approaches teacher-level performance despite a 5$\times$ reduction in model size. These results highlight the practical value of on-policy distillation as a principled and effective approach to deploying LLM-based planners in autonomous driving systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《On-Policy Distillation of Language Models for Autonomous Vehicle Motion Planning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何在**资源受限的车载硬件**上部署基于大语言模型（LLM）的自动驾驶运动规划器。
*   **背景**：LLM（如 GPT-Driver）通过将驾驶场景转化为文本提示词并生成路径点（Waypoints），展现了强大的推理和泛化能力。
*   **动机**：高性能 LLM（如 8B 参数以上）推理成本高、速度慢，难以实时运行。传统的知识蒸馏（离线蒸馏）存在“训练-推理分布不一致”问题，即学生模型在推理时产生的微小误差会随时间累积，导致轨迹漂移。
*   **意义**：通过“同策略（On-Policy）”蒸馏，让小模型学习如何纠正自己生成的错误，从而在大幅压缩模型体积的同时保持高精度的规划能力。

### 2. 方法论：核心思想与关键技术
论文对比并应用了两种主要的在线训练范式，核心思想是让学生模型在**自己生成的轨迹（On-Policy Rollouts）**上接受教师模型的指导。

*   **核心框架**：基于 GPT-Driver，输入包括感知预测信息、自车状态和任务目标，输出包含思维链（CoT）推理和 6 个路径点坐标。
*   **同策略广义知识蒸馏 (GKD)**：
    *   **流程**：学生模型针对提示词生成完整响应；教师模型对该响应中的每个 Token 提供完整的概率分布反馈。
    *   **损失函数**：使用广义 Jensen-Shannon 散度（JSD）来衡量教师和学生分布的差异。JSD 平衡了前向 KL（覆盖模式）和逆向 KL（寻找模式）的特性。
    *   **优势**：提供全词表（Full-vocabulary）的密集监督信号，让学生模型理解坐标生成的数值逻辑和物理约束。
*   **密集反馈强化学习 (RL) 基准**：
    *   **流程**：将教师模型对学生采样 Token 的对数概率作为奖励信号。
    *   **算法**：采用策略梯度（REINFORCE）框架，根据教师的偏好上调或下调学生采样 Token 的概率。
    *   **区别**：RL 仅提供标量反馈（采样到的那个 Token 好不好），而 GKD 提供分布反馈（所有可能的 Token 应该是什么分布）。

### 3. 实验设计
*   **数据集**：使用自动驾驶权威基准 **nuScenes**。包含 1000 个复杂驾驶场景，评估集包含 5119 帧数据。
*   **模型设置**：
    *   **教师模型**：Qwen3-8B（经过指令微调）。
    *   **学生模型**：Qwen3-1.7B（参数量缩小约 5 倍）。
*   **对比方法**：
    1.  **Teacher (8B)**：性能上限。
    2.  **GKD Student (1.7B)**：本文提出的核心方法。
    3.  **RL Student (1.7B)**：作为对比的强化学习基准。
*   **评估指标**：
    *   **L2 位移误差**：1s、2s、3s 处的欧氏距离（遵循 STP-3 和 UniAD 协议）。
    *   **碰撞率 (Collision Rate)**：评估轨迹的安全性。
    *   **格式错误率 (Format Error)**：评估模型输出结构化文本的稳定性。

### 4. 资源与算力
*   **硬件**：使用了 1 个节点，配备 **8 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   教师模型微调：DeepSpeed ZeRO-3 优化。
    *   学生模型训练：使用 TRL 库的 `GKDTrainer`。
    *   训练时长：每个模型训练 5 个 Epoch，通过验证集选择最佳 Checkpoint（通常在第 1 或第 3 个 Epoch）。
*   **推理**：采用贪婪解码（Greedy Decoding），最大生成长度 512 Token。

### 5. 实验数量与充分性
*   **实验规模**：论文在标准的 nuScenes 验证集（5119 帧）上进行了全面测试，涵盖了多种天气和路况。
*   **充分性**：
    *   对比了轨迹精度（L2）和安全性（碰撞率）两个关键维度。
    *   通过消融式对比（GKD vs RL），在相同模型架构和数据下隔离了算法影响。
    *   **局限性**：实验主要集中在 Qwen3 系列模型上，未测试其他架构（如 Llama 或 Mistral）的泛化表现；实验属于开环评估（Open-loop），未在闭环仿真环境中测试实时交互。

### 6. 主要结论与发现
*   **性能接近**：1.7B 的 GKD 学生模型在 L2 误差上仅比 8B 教师模型高出约 **5-6%**，表现极其接近。
*   **GKD 优于 RL**：在所有指标上，GKD 均显著超过 RL 基准。RL 产生的 L2 误差比 GKD 高出约 40-55%，碰撞率高出 2.6 倍。
*   **稳定性**：GKD 训练过程比 RL 更稳定，RL 容易出现过拟合或训练波动（在第 1 个 Epoch 后性能就开始下降）。
*   **压缩可行性**：证明了通过同策略蒸馏，可以将 LLM 规划器压缩 5 倍而几乎不损失安全性。

### 7. 优点与亮点
*   **解决分布偏移**：针对自回归模型在轨迹预测中的“一步错步步错”问题，采用了针对性的同策略训练方案。
*   **密集监督**：强调了全词表分布反馈（GKD）在处理坐标数值生成任务时比简单标量奖励（RL）更有效。
*   **可解释性**：继承了 GPT-Driver 的优点，模型在输出坐标前先输出思维链推理，增强了决策的透明度。

### 8. 不足与局限
*   **开环评估风险**：论文仅在离线数据集上测试，无法反映在真实驾驶中学生模型对环境反馈的实时反应（闭环表现）。
*   **坐标表示局限**：依赖于文本形式的坐标生成，虽然通过分词器处理了数值，但相比于直接回归坐标，Token 生成的效率和精度上限仍受限于词表。
*   **教师依赖性**：学生模型的上限被教师模型锁死，若教师模型在长尾场景（如极端天气）中有偏见，学生模型会继承这些错误。

（完）
