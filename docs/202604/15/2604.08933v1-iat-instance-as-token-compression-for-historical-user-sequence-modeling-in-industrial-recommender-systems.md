---
title: "IAT: Instance-As-Token Compression for Historical User Sequence Modeling in Industrial Recommender Systems"
title_zh: IAT：工业级推荐系统中历史用户序列建模的“实例即 Token”压缩方法
authors: "Xinchun Li, Ning Zhang, Qianqian Yang, Fei Teng, Wenlin Zhao, Huizhi Yang, Heng Shi, Linlan Chen, Yixin Wu, Zhen Wang, Daiye Hou, Fei Qin, Lele Yu, Yaocheng Tan"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08933v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 推荐系统中的历史用户行为序列建模
tldr: 针对工业推荐系统中手工序列特征限制模型性能的问题，本文提出IAT（Instance-As-Token）两阶段框架。该框架首先将用户历史交互的所有特征压缩为统一的实例Token，随后利用这些Token进行长程偏好建模。IAT支持时间维度和用户维度的压缩方案，在多项业务场景中显著优于现有方法，并具备优秀的跨域迁移能力，已在电商广告等实际场景成功部署。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08933v1/fig-001.webp\", \"caption\": \"Table 1: Performance and efficiency comparisons of models without or with IAT. Base models with three distinct sequence modeling architectures for traditional behavior sequences are verified (the leftmost column). Both temporal-order IAT and user-order IAT are compared, and only the results of the downstream models are reported. The downstream models adopt the transformer architecture for the IAT sequence.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.08933v1/fig-002.webp\", \"caption\": \"Figure 9: An enhancement training approach for the userorder source model.\", \"page\": 11, \"index\": 2, \"width\": 506, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.08933v1/fig-003.webp\", \"caption\": \"Figure 1: The motivation of IAT. Hand-crafted sequence features limit the further scaling of advanced ranking architectures. Instead, IAT proposes compressing all features within training instances that represent users’ historical interactive behaviors into unified tokens for sequence modeling.\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.08933v1/fig-004.webp\", \"caption\": \"Figure 2: The overall two-stage framework of IAT. The IAT Compression stage optionally builds the designed temporalorder or user-order source model, which generates and stores compact and informative InsEmb. The second stage trains a downstream model that retrieves these InsEmb for sequence modeling.\", \"page\": 3, \"index\": 4, \"width\": 506, \"height\": 382}]"
motivation: 传统推荐系统依赖手工序列特征，其有限的信息容量限制了模型捕捉复杂历史交互模式的能力。
method: 提出两阶段框架，先将单次交互的所有特征压缩为紧凑的实例Token，再由下游任务调用这些Token进行序列建模。
result: 实验证明IAT在多个领域显著超越SOTA方法，并展现出极强的域内和跨域迁移性能。
conclusion: IAT通过实例级压缩有效提升了长序列建模的效率与效果，为工业级推荐系统处理海量历史数据提供了新方案。
---

## 摘要
尽管复杂的序列建模范式在推荐系统中取得了显著成功，但手工设计的序列特征的信息容量限制了性能上限。为了通过编码历史交互模式来更好地提升用户体验，本文提出了一种名为 Instance-As-Token (IAT) 的新型两阶段序列建模框架。IAT 的第一阶段将每个历史交互实例的所有特征压缩为一个统一的实例嵌入（instance embedding），该嵌入将交互特征编码为一个紧凑且信息丰富的 token。本文提出了时间顺序（temporal-order）和用户顺序（user-order）两种压缩方案，其中后者能更好地契合下游序列建模的需求。第二阶段涉及下游任务通过时间戳获取固定长度的压缩实例 token，并采用标准的序列建模方法来学习长程偏好模式。大量实验表明，IAT 显著优于现有最先进的方法，并表现出卓越的域内和跨域迁移能力。IAT 已成功部署于真实的工业级推荐系统，包括电商广告、商城营销和直播电商，在关键业务指标上带来了实质性的提升。

## Abstract
Although sophisticated sequence modeling paradigms have achieved remarkable success in recommender systems, the information capacity of hand-crafted sequential features constrains the performance upper bound. To better enhance user experience by encoding historical interaction patterns, this paper presents a novel two-stage sequence modeling framework termed Instance-As-Token (IAT). The first stage of IAT compresses all features of each historical interaction instance into a unified instance embedding, which encodes the interaction characteristics in a compact yet informative token. Both temporal-order and user-order compression schemes are proposed, with the latter better aligning with the demands of downstream sequence modeling. The second stage involves the downstream task fetching fixed-length compressed instance tokens via timestamps and adopting standard sequence modeling approaches to learn long-range preferences patterns. Extensive experiments demonstrate that IAT significantly outperforms state-of-the-art methods and exhibits superior in-domain and cross-domain transferability. IAT has been successfully deployed in real-world industrial recommender systems, including e-commerce advertising, shopping mall marketing, and live-streaming e-commerce, delivering substantial improvements in key business metrics.

---

## 论文详细总结（自动生成）

这是一份关于论文《IAT: Instance-As-Token Compression for Historical User Sequence Modeling in Industrial Recommender Systems》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现代推荐系统（如广告、电商）高度依赖用户历史行为序列建模。然而，现有的序列建模主要受限于**手工特征工程**（如仅提取商品ID、类别、动作类型等）。这种方式存在两个瓶颈：一是信息容量低，无法捕捉单次交互中成千上万维特征的细粒度信息；二是特征扩展成本高，开发周期长。
*   **核心目标**：打破手工特征的限制，将用户过去的每一次“训练实例”（包含数千维特征的完整上下文）压缩为一个稠密的 Token，从而提升序列建模的信息密度和性能上限。

### 2. 方法论：IAT 框架
IAT 采用两阶段架构，将“实例”视为序列建模的基本单元：

*   **第一阶段：实例压缩（IAT Compression）**
    *   **时间顺序压缩（Temporal-Order）**：在基础模型中加入压缩层（MLP）和解压层。将高维特征表示 $h$ 压缩为低维的 `InsEmb`（实例嵌入），通过重构原始任务目标来保证压缩后的嵌入保留了关键信息。
    *   **用户顺序压缩（User-Order）**：这是本文的核心创新。将训练实例按用户维度重新组织，引入 **Source Instance Transformer (SIT)**。SIT 让当前实例能够感知该用户过去的实例信息，不仅提升了源模型的性能，还赋予了生成的 `InsEmb` 序列建模的感知力。
*   **第二阶段：序列建模（IAT Sequence Modeling）**
    *   **Token 构建**：从存储系统中检索历史 `InsEmb`，并结合核心侧信息（如任务标签、时间戳）构建 `InsToken`。
    *   **下游建模**：使用 Transformer 或 LONGER 等架构对 `InsToken` 序列进行建模。为了匹配，Query Token 也是由当前候选实例的所有特征压缩而成。
*   **工程实现**：设计了专门的存储架构，利用 Embedding PS 存储 `InsEmb`，并使用高性能 KV 存储维护用户与实例 ID 的映射关系，支持流式训练和实时推理。

### 3. 实验设计
*   **数据集与场景**：
    *   **离线实验**：使用某真实工业级广告系统的 CVR（转化率）预测数据集，包含连续 4 个月、**数百亿级**的训练实例。
    *   **在线实验**：部署于字节跳动旗下的电商广告、商城营销、直播电商等多个真实业务场景。
*   **Benchmark 与对比方法**：
    *   **基础模型**：DIN、LONGER、Transformer（基于手工序列特征）。
    *   **对比维度**：对比了无 IAT、有时间顺序 IAT 和有用户顺序 IAT 的表现。
    *   **评估指标**：AUC（曲线下面积）和 LogLoss。

### 4. 资源与算力
*   **算力规模**：论文明确提到实验是在一个**拥有数百个节点的分布式 GPU 集群**上进行的。
*   **训练细节**：Batch Size 设为 1024。
*   **存储开销**：以 64 维 `InsEmb` 为例，存储两年的历史数据（每日数亿样本）大约需要数十 TB 的存储空间。
*   **具体型号**：文中未明确给出具体的 GPU 型号（如 A100 或 H100），但强调了其工业级的计算规模。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了非常充分的实验，包括：
    *   **整体性能对比**：在三种主流序列架构上验证了 IAT 的普适性。
    *   **Scaling Law 研究**：探索了模型参数量（从 50M 到 1B）、序列长度、SIT 层数对性能的影响。
    *   **消融实验**：验证了侧信息、Query 构建方式、存储位置等组件的必要性。
    *   **跨域验证**：在 5 个不同的业务场景进行了在线 A/B 测试。
*   **客观性**：实验基于真实百亿级工业数据，且在多个不同性质的业务（广告 vs 直播）中均取得正向收益，具有极高的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升显著**：在百亿级基准上，IAT 带来的 AUC 提升最高达 **0.31%**，这在成熟的工业推荐系统中属于巨大突破（通常 0.1% 即为显著）。
*   **用户顺序优于时间顺序**：引入 SIT 的用户顺序压缩方案效果最好，因为它在压缩阶段就引入了序列依赖。
*   **符合 Scaling Law**：随着 IAT 序列长度和模型深度的增加，性能呈幂律增长，证明了该方法具备良好的扩展性。
*   **跨域迁移能力**：在一个域（如通用广告）训练的压缩模型，其生成的 Token 可以直接用于其他域（如直播电商），并带来显著收益。

### 7. 优点与亮点
*   **范式创新**：从“手工特征工程”转向“实例级自动压缩”，极大地提高了序列建模的信息密度。
*   **工业级落地**：不仅有理论创新，还详细给出了在大规模分布式系统中的存储、流式训练和部署方案。
*   **通用性强**：不依赖特定的模型架构，可以作为插件集成到现有的各类排序模型中。

### 8. 不足与局限
*   **存储成本**：虽然压缩了特征，但存储数年的实例级 Embedding 仍需巨大的 PS 存储资源，对中小规模公司可能有门槛。
*   **训练复杂性**：两阶段框架（源模型 + 下游模型）增加了维护成本和训练流水线的复杂度。
*   **冷启动依赖**：对于没有任何历史交互实例的新用户，IAT 无法发挥作用，仍需依赖传统特征。

（完）
