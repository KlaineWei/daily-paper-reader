---
title: Advantage-Guided Diffusion for Model-Based Reinforcement Learning
title_zh: 基于优势引导扩散模型的基于模型强化学习
authors: "Daniele Foffano, Arvid Eriksson, David Broman, Karl H. Johansson, Alexandre Proutiere"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09035v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 使用优势引导扩散进行规划的模型强化学习
tldr: 本研究针对扩散世界模型在强化学习中存在的短视问题，提出了优势引导扩散模型（AGD-MBRL）。该方法利用优势函数估计来引导逆向扩散过程，使生成的轨迹集中于具有更高长期回报的区域。通过引入SAG和EAG两种引导机制，AGD-MBRL在MuJoCo任务中显著提升了样本效率和最终回报，相比现有基准提升可达两倍，为扩散模型MBRL提供了一种简单有效的改进方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09035v1/fig-001.webp\", \"caption\": \"Fig. 2: Training curves for the Mujoco environments: HalfCheetah, Hopper, Walker and Reacher. Shaded areas indicate the standard error. The curves have been smoothed by a running average for better readability.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 526}]"
motivation: 现有的扩散模型引导方式要么忽略价值信息，要么因仅依赖即时奖励引导而导致在短预测时域内表现短视。
method: 提出利用优势函数估计来引导逆向扩散过程的SAG和EAG机制，使采样轨迹向高长期价值区域偏移且无需改变训练目标。
result: 在MuJoCo多项控制任务中，AGD-MBRL的样本效率和最终回报显著优于PolyGRAD、奖励引导模型及PPO等传统强化学习基准。
conclusion: 优势感知引导能有效克服扩散模型在MBRL中的短视局限，是实现高效策略改进和提升样本效率的简单且手段。
---

## 摘要
使用自回归世界模型的基于模型强化学习（MBRL）面临复合误差问题，而扩散世界模型通过联合生成轨迹段来缓解这一问题。然而，现有的扩散引导机制要么仅基于策略（丢弃了价值信息），要么基于奖励（在扩散时界较短时会变得短视）。我们提出了用于 MBRL 的优势引导扩散模型（AGD-MBRL），它利用智能体的优势估计来引导反向扩散过程，使采样集中在预期能产生超出生成窗口更高长期回报的轨迹上。我们开发了两种引导方式：(i) Sigmoid 优势引导 (SAG) 和 (ii) 指数优势引导 (EAG)。我们证明了通过 SAG 或 EAG 引导的扩散模型允许我们对轨迹进行重加权采样，其权重随状态-动作优势的增加而增加——在标准假设下，这意味着策略的改进。此外，我们表明，与无引导的扩散模型相比，AGD-MBRL 生成的轨迹遵循改进后的策略（即具有更高的价值）。AGD 通过引导状态分量同时保持动作生成受策略约束，与 PolyGRAD 风格的架构无缝集成，且无需更改扩散训练目标。在 MuJoCo 控制任务（HalfCheetah、Hopper、Walker2D 和 Reacher）上，AGD-MBRL 在样本效率和最终回报方面优于 PolyGRAD、在线 Diffuser 风格的奖励引导以及无模型基准（PPO/TRPO），在某些情况下提升幅度达到 2 倍。这些结果表明，优势感知引导是解决扩散模型 MBRL 中短时界短视问题的一种简单且有效的补救措施。

## Abstract
Model-based reinforcement learning (MBRL) with autoregressive world models suffers from compounding errors, whereas diffusion world models mitigate this by generating trajectory segments jointly. However, existing diffusion guides are either policy-only, discarding value information, or reward-based, which becomes myopic when the diffusion horizon is short. We introduce Advantage-Guided Diffusion for MBRL (AGD-MBRL), which steers the reverse diffusion process using the agent's advantage estimates so that sampling concentrates on trajectories expected to yield higher long-term return beyond the generated window. We develop two guides: (i) Sigmoid Advantage Guidance (SAG) and (ii) Exponential Advantage Guidance (EAG). We prove that a diffusion model guided through SAG or EAG allows us to perform reweighted sampling of trajectories with weights increasing in state-action advantage-implying policy improvement under standard assumptions. Additionally, we show that the trajectories generated from AGD-MBRL follow an improved policy (that is, with higher value) compared to an unguided diffusion model. AGD integrates seamlessly with PolyGRAD-style architectures by guiding the state components while leaving action generation policy-conditioned, and requires no change to the diffusion training objective. On MuJoCo control tasks (HalfCheetah, Hopper, Walker2D and Reacher), AGD-MBRL improves sample efficiency and final return over PolyGRAD, an online Diffuser-style reward guide, and model-free baselines (PPO/TRPO), in some cases by a margin of 2x. These results show that advantage-aware guidance is a simple, effective remedy for short-horizon myopia in diffusion-model MBRL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AGD-MBRL**（优势引导扩散的基于模型强化学习）的新框架，旨在解决扩散世界模型在强化学习任务中的“短视”问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：在基于模型的强化学习（MBRL）中，传统的自回归模型（一步步预测未来）容易产生**复合误差**。扩散模型（Diffusion Models）通过联合生成整个轨迹段，有效缓解了这一问题。
*   **核心问题**：现有的扩散模型引导机制存在缺陷：
    1.  **仅策略引导**（如 PolyGRAD）：只模仿当前策略，忽略了价值信息。
    2.  **基于奖励引导**（如 Diffuser）：仅根据生成窗口内的累积奖励进行引导。由于计算成本限制，扩散时界（Horizon）通常很短，这导致引导过程变得**短视**，忽略了窗口之外的长期回报。
*   **研究动机**：利用强化学习中的**优势函数（Advantage Function）**来引导扩散过程，因为优势函数本身包含了对未来长期价值的估计，从而克服短视问题。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在扩散模型的反向去噪过程中，利用优势函数的梯度来调整采样方向，使生成的轨迹向“比当前策略表现更好”的区域集中。
*   **关键技术细节**：
    *   **两种引导机制**：
        1.  **SAG (Sigmoid Advantage Guidance)**：将步进最优概率定义为优势函数的 Sigmoid 变换。这种方式比较保守，能处理优势函数的估计偏差。
        2.  **EAG (Exponential Advantage Guidance)**：采用指数能量函数引导。这种方式引导强度更大，在价值函数估计准确时收敛更快。
    *   **理论证明**：作者从数学上证明了使用 SAG 或 EAG 引导的采样等同于对一种“改进策略”下的轨迹进行重加权采样，且该改进策略的期望价值高于原策略。
    *   **算法流程**：
        1.  在真实环境收集数据并更新扩散模型（学习环境动力学）。
        2.  在反向扩散采样时，计算优势函数相对于轨迹的梯度。
        3.  利用该梯度修正去噪步骤的均值，生成高优势轨迹。
        4.  在生成的虚拟轨迹上使用 Actor-Critic 算法改进智能体策略。

### 3. 实验设计
*   **实验场景**：使用了 MuJoCo 连续控制基准测试中的四个典型任务：`HalfCheetah`、`Hopper`、`Walker2D` 和 `Reacher`。
*   **对比基准（Benchmarks）**：
    *   **PolyGRAD**：基于策略引导的扩散 MBRL 基准。
    *   **Online Diffuser**：作者实现的在线版 Diffuser，使用累积奖励引导。
    *   **PPO / TRPO**：经典的无模型（Model-free）强化学习算法。
*   **实验设置**：所有方法均在 150 万（1.5M）环境交互步数下进行训练。

### 4. 资源与算力
*   **算力说明**：论文提到计算资源由瑞典国家超级计算学术基础设施（NAISS）提供。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量以及具体的训练时长。但考虑到扩散模型涉及频繁的迭代去噪，其计算开销通常显著高于传统的自回归世界模型。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流 MuJoCo 环境上进行了测试，每个环境都展示了完整的训练曲线和标准差（阴影区域）。
*   **充分性评价**：
    *   **对比全面**：涵盖了模型化（扩散模型）和非模型化（PPO/TRPO）的最强基准。
    *   **消融/变体对比**：对比了 EAG 和 SAG 两种不同的引导函数，展示了它们在不同复杂度环境下的表现差异。
    *   **客观性**：实验结果显示在简单环境（如 Hopper）中提升不明显，但在复杂环境（如 HalfCheetah）中提升巨大（达 2 倍），这种差异化的结果体现了实验的真实性。

### 6. 主要结论与发现
*   **性能提升**：AGD-MBRL 在大多数任务中显著优于 PolyGRAD 和奖励引导的 Diffuser，证明了优势引导的有效性。
*   **克服短视**：实验证实，相比于只看当前窗口奖励的引导，利用优势函数（包含未来价值信息）能生成更高质量的训练数据。
*   **稳定性**：优势引导不仅提升了最终回报，还使训练曲线更加平滑，减少了性能回退。

### 7. 优点（亮点）
*   **理论支撑**：不仅提出了算法，还给出了关于策略改进的数学证明，增强了方法的说服力。
*   **即插即用**：该引导机制不需要改变扩散模型的训练目标，只需在推理（采样）阶段加入梯度引导，易于集成到现有框架。
*   **解决痛点**：直接针对扩散模型在 MBRL 中因计算压力导致的“短时界”局限性提出了有效的补救措施。

### 8. 不足与局限
*   **计算效率**：扩散模型本身采样速度慢是固有缺陷，论文虽提升了样本效率，但未解决生成轨迹时的计算耗时问题。
*   **超参数敏感性**：引导强度（Scale $\alpha$）和引导函数的选择（EAG vs SAG）在不同环境下表现不同，可能需要针对具体任务调优。
*   **环境覆盖**：实验仅限于 MuJoCo 模拟环境，尚未在更复杂的视觉输入任务或真实机器人硬件上进行验证。

（完）
