---
title: "Three Roles, One Model: Role Orchestration at Inference Time to Close the Performance Gap Between Small and Large Agents"
title_zh: 三种角色，一个模型：通过推理阶段的角色编排缩小小型与大型智能体之间的性能差距
authors: "S. Aaron McClendon, Jorge Gallego-Feliciano, Stavros Zervoudakis, Antonios Saravanos"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11465v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 通过推理脚手架提升小模型在复杂任务中的表现
tldr: 本研究针对小参数量模型在复杂多步工具使用任务中表现不佳的问题，提出了一种无需额外训练的推理时脚手架方法。通过将同一个冻结的8B模型编排为总结者、主代理和代码修正者三个角色，有效解决了上下文冗余和重复失败循环。实验表明，该方法使模型性能翻倍，在AppWorld基准测试中甚至超越了参数量大四倍的33B模型，证明了推理时干预在提升小模型竞争力方面的巨大潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11465v1/fig-001.webp\", \"caption\": \"Table 1. Primary failure mode distribution for FP16 baseline on AppWorld test_normal (163 failed tasks). Categories are ranked by frequency. Confidence-weighted scores incorporate the evaluator’s classification confidence.\", \"page\": 6, \"index\": 1, \"width\": 598, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.11465v1/fig-002.webp\", \"caption\": \"Table 7. Top failure modes by difficulty after full scaffolding (AWQ, 158 failed tasks).\", \"page\": 10, \"index\": 2, \"width\": 444, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2604.11465v1/fig-003.webp\", \"caption\": \"Table 6. Failure mode comparison before and after scaffolding (AWQ, test_normal). Counts reflect primary failure classification across all difficulty levels. The scaffolded model has fewer total failures (158 vs. 163) due to improved task goal completion.\", \"page\": 10, \"index\": 3, \"width\": 610, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2604.11465v1/fig-004.webp\", \"caption\": \"Table 3. Task goal completion (%) on AppWorld test_normal (168 tasks). FP16 = full precision with 12K context; AWQ = 4-bit quantized with 32K context. “Scaffold” denotes our three-tier inference scaffolding method. We report 95% Wilson confidence intervals for aggregate task goal completion rates.\", \"page\": 8, \"index\": 4, \"width\": 672, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2604.11465v1/fig-005.webp\", \"caption\": \"Figure 2. Comparison of failure modes for tasks of difficulty 1 for baseline vs full scaffold for Qwen 3 8B AWQ, which is a quantized version of Qwen.\", \"page\": 8, \"index\": 5, \"width\": 1044, \"height\": 518}, {\"url\": \"assets/figures/arxiv/2604.11465v1/fig-006.webp\", \"caption\": \"Table 4. Comparison with published AppWorld results (task goal completion, test_normal). Our results use Qwen3-8B on a single 24 GB GPU. For our models, and for DeepSeek-Coder 33B Instruct assuming the same 168-task evaluation set, we report 95% Wilson confidence intervals.\", \"page\": 9, \"index\": 6, \"width\": 768, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.11465v1/fig-007.webp\", \"caption\": \"Figure 1. Our proposed modular architecture. Each module targets an important failure mode for small language models to increase performance.\", \"page\": 5, \"index\": 7, \"width\": 522, \"height\": 492}]"
motivation: 旨在解决小参数量模型在复杂多步工具使用任务中性能不足，且难以在有限硬件资源下部署高性能智能体的问题。
method: 提出一种三层推理脚手架，将同一个冻结模型分别担任总结者（压缩上下文）、主代理（推理）和修正者（审查代码）三个角色进行协同。
result: 在AppWorld评估中，该方法使8B模型的任务完成率翻倍，且在全精度推理下超越了参数量大4倍的DeepSeek-Coder 33B模型。
conclusion: 通过结构化的推理时角色编排干预，可以在不增加训练成本的前提下显著提升小模型的复杂任务处理能力，缩小其与大模型之间的差距。
---

## 摘要
大语言模型（LLM）智能体在现实的工具使用任务中展现出潜力，但在中低端硬件上部署高性能智能体仍具挑战。我们研究了在不增加额外训练计算量的情况下，仅通过推理阶段的脚手架（scaffolding）能否提升小型模型在复杂多步环境中的性能。在单张 24 GB GPU 上，我们评估了 Qwen3-8B 在全精度（FP16，12K 上下文）和 4 位量化（AWQ，32K 上下文）配置下的表现。在没有任何干预的情况下，原始模型仅实现了 5.4% (FP16) 和 3.0% (AWQ) 的任务目标完成率。在系统性故障模式分析的指导下，我们引入了一个三层推理脚手架流水线，将同一个冻结模型部署为三种不同的角色：(1) 摘要模型：在压缩对话历史的同时保留关键工件（令牌、凭据、API 响应）；(2) 主智能体模型：基于压缩后的上下文进行推理；(3) 独立纠错模型：在不访问对话历史的情况下审查并修正智能体的代码输出，从而打破重复的失败循环。将该脚手架应用于同一个未经修改的模型，任务目标完成率分别提升至 8.9% (FP16) 和 5.9% (AWQ)，在两种设置下性能几乎翻倍，尤其在难度 1 的任务中增益显著（FP16 从 15.8% 提升至 26.3%；AWQ 从 5.3% 提升至 14.0%）。在全精度推理中，我们带有脚手架的 8B 模型超过了原始 AppWorld 评估中的 DeepSeek-Coder 33B Instruct (7.1%)，证明了结构化的推理阶段干预可以使小型模型具备与 4 倍于其规模的系统竞争的能力。我们将该方法形式化为基于冻结基座模型的脚手架策略，即通过不同的条件设置对同一权重进行三次调用，并将其与强化学习中的测试时计算缩放（test-time compute scaling）和动作空间塑造（action-space shaping）联系起来。

## Abstract
Large language model (LLM) agents show promise on realistic tool-use tasks, but deploying capable agents on modest hardware remains challenging. We study whether inference-time scaffolding alone, without any additional training compute, can improve the performance of a small model in complex multi-step environments. Operating on a single 24\,GB GPU, we evaluate Qwen3-8B under both full-precision (FP16, 12K context) and 4-bit quantized (AWQ, 32K context) configurations. Without any intervention, the raw model achieves just 5.4\% (FP16) and 3.0\% (AWQ) task goal completion. Guided by a systematic failure mode analysis, we introduce a three-tier inference scaffolding pipeline that deploys the same frozen model in three distinct roles: (1) a summarization model that preserves critical artifacts (tokens, credentials, API responses) while compressing dialogue history; (2) the main agent model that reasons over the compressed context; and (3) an isolated correction model that reviews and revises the agent's code output without access to conversation history, breaking repetitive failure loops. Applied to the same unmodified model, this scaffolding yields 8.9\% (FP16) and 5.9\% (AWQ) task goal completion, roughly doubling performance in both settings, with particularly strong gains on difficulty-1 tasks (15.8\%$\to$26.3\% FP16; 5.3\%$\to$14.0\% AWQ). On full-precision inference, our scaffolded 8B model surpasses DeepSeek-Coder 33B Instruct (7.1\%) from the original AppWorld evaluation, demonstrating that structured inference-time interventions can make small models competitive with systems 4$\times$ their size. We formalize the approach as a scaffolded policy over a frozen base model, three invocations of the same weights with different conditioning, drawing connections to test-time compute scaling and action-space shaping in reinforcement learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Three Roles, One Model: Role Orchestration at Inference Time to Close the Performance Gap Between Small and Large Agents》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大型语言模型（LLM）在工具使用任务中表现出色，但在受限硬件（如单张消费级 GPU）上部署高性能智能体依然困难。
*   **核心问题**：在不进行任何额外训练或微调的前提下，能否仅通过**推理阶段的脚手架（Inference-time Scaffolding）**结构化干预，显著提升 8B 规模小模型的复杂任务处理能力？
*   **背景**：现有研究多关注通过强化学习（RL）或大规模微调提升性能，但这些方法对算力要求极高。作者希望探索“推理时计算缩放”在资源受限场景下的潜力。

### 2. 论文提出的方法论
核心思想是将同一个冻结（Frozen）的基座模型在推理时通过不同的 Prompt 和上下文约束，编排为三个互补的角色：

*   **角色一：摘要模型（Summarization Model, $\pi_{sum}$）**
    *   **功能**：解决长对话导致的上下文丢失问题。
    *   **细节**：当对话历史超过阈值（如 6000 tokens）时触发。它不仅是简单的压缩，而是强制保留关键工件（如 API 凭据、Token、已解决的错误模式、分页状态），确保主智能体始终能看到核心状态。
*   **角色二：主智能体模型（Main Agent Model, $\pi_{agent}$）**
    *   **功能**：负责核心推理和动作生成。
    *   **细节**：采用 ReAct 模式，基于压缩后的历史和当前观察生成 Python 代码块。
*   **角色三：纠错模型（Correction Model, $\pi_{correct}$）**
    *   **功能**：打破重复失败的循环。
    *   **关键技术（历史隔离）**：该模型**无法访问**完整的对话历史，仅接收当前生成的代码、API 文档和最近的执行错误。这种“隔离”作为一种隐式正则化，防止纠错模型被主智能体之前的错误推理逻辑所误导，强制其根据 API 文档进行客观修正。

### 3. 实验设计
*   **数据集/场景**：**AppWorld**。这是一个极具挑战性的基准测试，包含 9 个互联应用、457 个 API 端点，要求智能体处理身份验证、状态持久化和多步复杂任务。
*   **Benchmark 协议**：使用 `test_normal` 分割集（168 个任务），采用贪婪解码（Temperature=0）和单次尝试（Pass@1）协议。
*   **对比方法**：
    *   **基准线**：原始 Qwen3-8B（FP16 和 AWQ 量化版）。
    *   **外部对比**：DeepSeek-Coder 33B Instruct、LLaMA-3-70B-Instruct、GPT-4o 等在原始 AppWorld 论文中报告的数据。
    *   **消融实验**：对比了“仅纠错”与“全脚手架”配置。

### 4. 资源与算力
*   **硬件环境**：所有实验均在**单张 NVIDIA GPU（24 GB VRAM）**上完成。
*   **模型配置**：
    *   **FP16（全精度）**：使用 vLLM 部署，上下文窗口限制为 12K。
    *   **AWQ（4位量化）**：支持完整的 32K 上下文窗口。
*   **训练时长**：由于是纯推理阶段方法，**无需训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：针对 FP16 和 AWQ 两种配置分别进行了完整测试，并按任务难度（1-3 级）进行了细分统计。
*   **消融实验**：通过“仅纠错”实验验证了各模块的贡献度。
*   **客观性与公平性**：作者使用了 95% Wilson 置信区间来评估结果的统计显著性。虽然样本量（N=168）导致置信区间较宽，但其对比 DeepSeek-Coder 33B 的过程遵循了完全相同的评估协议，具有较高的参考价值。

### 6. 论文的主要结论与发现
*   **性能翻倍**：脚手架干预使 8B 模型的任务完成率从 5.4% 提升至 8.9%（FP16），在难度 1 的任务中表现尤为突出（15.8% → 26.3%）。
*   **以小博大**：带有脚手架的 8B 模型在数值上超过了参数量大 4 倍的 DeepSeek-Coder 33B（7.1%）。
*   **精度优于长度**：实验发现 FP16（12K 窗口）的表现始终优于 AWQ（32K 窗口），说明对于智能体任务，数值精度比极长的上下文更重要。
*   **故障模式转移**：脚手架显著减少了“机械性错误”（如 API 参数错误、重复循环），使得模型真正的“推理/规划局限”暴露出来，为后续改进指明了方向。

### 7. 优点
*   **部署友好**：无需微调，不改变模型权重，极大地降低了小模型在实际生产环境中的应用门槛。
*   **诊断驱动设计**：脚手架的设计并非盲目，而是基于详尽的失败模式分析（如身份验证丢失、不看文档等），针对性极强。
*   **角色编排创新**：提出“历史隔离”作为纠错模型的正则化手段，有效解决了智能体常见的“自我强化错误”问题。

### 8. 不足与局限
*   **高难度任务瓶颈**：对于难度 3 的任务，脚手架提升有限。这说明推理时干预无法完全弥补小模型在长程逻辑规划能力上的根本缺失。
*   **纠错模型的副作用**：在极复杂任务中，由于纠错模型无法访问历史，可能会因缺乏上下文而幻化出错误的变量名或 Token。
*   **统计显著性**：由于 AppWorld 测试集规模限制，8B 模型超越 33B 模型的结论在 95% 置信水平下尚不具备绝对的统计显著性，更多体现为一种趋势。
*   **计算开销**：虽然不增加训练成本，但每步推理需要最多三次模型调用，增加了推理延迟。

（完）
