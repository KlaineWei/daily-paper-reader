---
title: Advantage-Guided Diffusion for Model-Based Reinforcement Learning
title_zh: 优势引导扩散的模型强化学习
authors: "Daniele Foffano, Arvid Eriksson, David Broman, Karl H. Johansson, Alexandre Proutiere"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09035v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 基于优势引导扩散的模型强化学习，旨在提高长期回报
tldr: 针对基于扩散模型的世界模型在强化学习中存在的短视问题，本文提出优势引导扩散（AGD-MBRL）。该方法利用优势函数引导逆向扩散过程，使生成的轨迹集中在具有更高长期回报的区域。通过引入SAG和EAG两种引导机制，AGD在不改变训练目标的前提下，显著提升了MuJoCo任务中的采样效率和最终回报，有效解决了短预测步长下的性能瓶颈。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09035v1/fig-001.webp\", \"caption\": \"Fig. 2: Training curves for the Mujoco environments: HalfCheetah, Hopper, Walker and Reacher. Shaded areas indicate the standard error. The curves have been smoothed by a running average for better readability.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 526}]"
motivation: 现有的扩散世界模型引导机制要么忽略价值信息，要么因仅依赖短期奖励而导致在有限预测步长内表现短视。
method: 提出AGD-MBRL框架，利用优势函数（Advantage）通过Sigmoid或指数引导机制来调整逆向扩散采样，使轨迹向高长期价值区域偏移。
result: 在MuJoCo基准测试中，AGD-MBRL的采样效率和最终回报显著优于PolyGRAD、奖励引导模型及PPO等基准，部分任务性能提升达2倍。
conclusion: 优势感知引导是解决扩散模型在基于模型的强化学习中短视问题的简单且有效的方案，能显著增强轨迹生成的质量。
---

## 摘要
采用自回归世界模型的模型强化学习（MBRL）面临复合误差问题，而扩散世界模型通过联合生成轨迹段来缓解这一问题。然而，现有的扩散引导机制要么仅基于策略（丢弃了价值信息），要么基于奖励（在扩散时界较短时会变得短视）。我们提出了优势引导扩散的模型强化学习（AGD-MBRL），它利用智能体的优势估计来引导反向扩散过程，使采样集中在预期能产生超出生成窗口更高长期回报的轨迹上。我们开发了两种引导方式：(i) Sigmoid 优势引导 (SAG) 和 (ii) 指数优势引导 (EAG)。我们证明，通过 SAG 或 EAG 引导的扩散模型允许我们对轨迹进行重加权采样，其权重随状态-动作优势的增加而增加——这在标准假设下意味着策略改进。此外，我们表明，与无引导的扩散模型相比，AGD-MBRL 生成的轨迹遵循改进的策略（即具有更高的价值）。AGD 与 PolyGRAD 风格的架构无缝集成，通过引导状态分量同时保持动作生成受策略约束，且无需更改扩散训练目标。在 MuJoCo 控制任务（HalfCheetah、Hopper、Walker2D 和 Reacher）上，AGD-MBRL 在样本效率和最终回报方面优于 PolyGRAD、在线 Diffuser 风格的奖励引导以及无模型基准（PPO/TRPO），在某些情况下提升幅度达到 2 倍。这些结果表明，优势感知引导是解决扩散模型 MBRL 中短时界短视问题的一种简单且有效的补救措施。

## Abstract
Model-based reinforcement learning (MBRL) with autoregressive world models suffers from compounding errors, whereas diffusion world models mitigate this by generating trajectory segments jointly. However, existing diffusion guides are either policy-only, discarding value information, or reward-based, which becomes myopic when the diffusion horizon is short. We introduce Advantage-Guided Diffusion for MBRL (AGD-MBRL), which steers the reverse diffusion process using the agent's advantage estimates so that sampling concentrates on trajectories expected to yield higher long-term return beyond the generated window. We develop two guides: (i) Sigmoid Advantage Guidance (SAG) and (ii) Exponential Advantage Guidance (EAG). We prove that a diffusion model guided through SAG or EAG allows us to perform reweighted sampling of trajectories with weights increasing in state-action advantage-implying policy improvement under standard assumptions. Additionally, we show that the trajectories generated from AGD-MBRL follow an improved policy (that is, with higher value) compared to an unguided diffusion model. AGD integrates seamlessly with PolyGRAD-style architectures by guiding the state components while leaving action generation policy-conditioned, and requires no change to the diffusion training objective. On MuJoCo control tasks (HalfCheetah, Hopper, Walker2D and Reacher), AGD-MBRL improves sample efficiency and final return over PolyGRAD, an online Diffuser-style reward guide, and model-free baselines (PPO/TRPO), in some cases by a margin of 2x. These results show that advantage-aware guidance is a simple, effective remedy for short-horizon myopia in diffusion-model MBRL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Advantage-Guided Diffusion for Model-Based Reinforcement Learning》的结构化深入分析总结：

### 1. 核心问题与研究背景
*   **研究动机**：在模型强化学习（MBRL）中，传统的自回归世界模型（一步步预测未来）容易产生“复合误差”。扩散模型（Diffusion Models）通过联合生成整个轨迹段缓解了这一问题。
*   **核心问题**：现有的扩散模型引导机制存在缺陷：
    *   **策略引导（如 PolyGRAD）**：仅模仿当前策略，忽略了价值信息。
    *   **奖励引导（如 Diffuser）**：仅基于生成窗口内的累积奖励，在扩散时界（Horizon）较短时表现“短视”，无法感知窗口之外的长期回报。
*   **研究目标**：利用智能体学习到的**优势函数（Advantage Function）**来引导扩散采样，使生成的轨迹既符合动力学，又指向长期高回报区域。

### 2. 方法论：优势引导扩散（AGD-MBRL）
*   **核心思想**：将强化学习中的优势函数 $A^\pi(s, a)$ 引入扩散模型的反向去噪过程。优势函数衡量了一个动作相对于平均水平的好坏，且包含了对未来长期价值的估计，从而克服了短视问题。
*   **关键技术细节**：
    *   **Sigmoid 优势引导 (SAG)**：将步进最优概率定义为优势函数的 Sigmoid 变换 $p(O_t=1|s_t, a_t) = \sigma(A_t)$。这种方式较为保守，能抑制优势函数估计过高带来的风险。
    *   **指数优势引导 (EAG)**：采用能量模型形式，权重为 $\exp(A_t)$。这种方式引导力度更强，能更激进地向高优势区域偏移。
    *   **引导公式**：在去噪每一步，利用优势函数关于状态-动作的梯度 $\nabla_{\tau} \log f(\tau)$ 来修正均值，其中 $f(\tau)$ 对应 SAG 或 EAG 的目标函数。
*   **算法流程**：
    1.  在真实环境收集数据并更新扩散模型（模拟动力学）。
    2.  使用 AGD 采样生成“想象”轨迹（利用优势梯度引导）。
    3.  在生成的轨迹上使用 Actor-Critic 算法改进策略。
    4.  循环往复。

### 3. 实验设计
*   **实验场景**：使用了 MuJoCo 连续控制基准测试中的四个典型任务：`HalfCheetah`、`Hopper`、`Walker2D` 和 `Reacher`。
*   **对比基准（Benchmarks）**：
    *   **PolyGRAD**：基于策略引导的扩散 MBRL（本文的基础架构）。
    *   **Online Diffuser**：将原始 Diffuser 的奖励引导机制移植到在线设置中（代表短视的奖励引导）。
    *   **无模型基准**：PPO 和 TRPO（经典的深度强化学习算法）。
*   **实验设置**：所有方法均在 150 万（1.5M）环境交互步数下进行训练。

### 4. 资源与算力
*   **算力说明**：论文提到计算资源由瑞典国家超级计算学术基础设施（NAISS）提供。
*   **具体细节**：文中**未明确指出**具体的 GPU 型号、数量或具体的训练时长。但考虑到扩散模型涉及频繁的反向去噪迭代，其计算开销通常显著高于传统的基于 MLP 的世界模型。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流环境上进行了对比，每个环境都展示了完整的训练曲线（均值与标准差）。
*   **充分性评价**：
    *   **客观性**：对比了同类最先进的扩散 MBRL 方法以及经典的无模型方法，实验设计较为公平。
    *   **深度**：论文不仅提供了实验结果，还提供了数学证明（Proposition V.1 & V.2），证明了 AGD 引导等同于在改进后的策略下进行重加权采样，这增强了实验结果的说服力。
    *   **局限**：缺乏针对不同扩散时界（Horizon）长度的消融实验，以更直观地展示 AGD 在极短时界下相对于奖励引导的优势。

### 6. 主要结论与发现
*   **性能提升**：AGD-MBRL 在多数任务中显著优于 PolyGRAD 和奖励引导的 Diffuser。在 `HalfCheetah` 任务中，性能提升甚至达到了 2 倍。
*   **解决短视**：实验证实，相比于只看当前窗口奖励的引导，利用包含未来价值信息的优势函数引导，能生成更高质量的训练数据。
*   **稳定性**：AGD 在训练过程中表现出更好的稳定性，减少了性能回退的频率。
*   **引导函数差异**：EAG 在价值函数易于估计的环境（如 `HalfCheetah`）中表现更强；而 SAG 在复杂环境（如 `Walker2D`）中因其保守性而更稳健。

### 7. 优点
*   **理论支撑扎实**：通过数学证明将扩散引导与策略改进理论联系起来。
*   **即插即用**：无需改变扩散模型的训练目标，仅在推理（采样）阶段通过梯度引导即可实现，兼容性强。
*   **有效解决痛点**：直接针对扩散模型在 MBRL 中因计算成本限制而不得不采用短时界导致的“短视”问题。

### 8. 不足与局限
*   **计算开销**：扩散模型共同的弱点是采样速度慢。尽管 AGD 提高了样本效率，但其壁挂时间（Wall-clock time）可能远高于无模型方法或简单的神经网络世界模型。
*   **依赖优势估计**：AGD 的效果高度依赖于 RL 智能体学习到的优势函数的准确性。如果优势函数估计存在严重偏差，引导过程可能会将模型带偏。
*   **环境覆盖面**：实验仅限于 MuJoCo 任务，尚未在视觉输入或更复杂的稀疏奖励任务中验证其泛化能力。

（完）
