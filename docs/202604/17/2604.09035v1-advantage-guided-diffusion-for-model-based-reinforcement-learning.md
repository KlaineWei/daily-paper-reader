---
title: Advantage-Guided Diffusion for Model-Based Reinforcement Learning
title_zh: 基于优势引导扩散的模型强化学习
authors: "Daniele Foffano, Arvid Eriksson, David Broman, Karl H. Johansson, Alexandre Proutiere"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09035v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 基于优势引导的扩散模型强化学习，旨在提高长程回报
tldr: 针对基于模型的强化学习中自回归模型误差累积及现有扩散引导方式（仅策略或短视奖励）的局限性，本文提出优势引导扩散（AGD-MBRL）。该方法利用优势函数估计引导逆向扩散过程，使采样集中于高长期回报轨迹。通过引入Sigmoid和指数优势引导，在不改变训练目标的前提下，显著提升了MuJoCo任务中的采样效率和最终回报。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09035v1/fig-001.webp\", \"caption\": \"Fig. 2: Training curves for the Mujoco environments: HalfCheetah, Hopper, Walker and Reacher. Shaded areas indicate the standard error. The curves have been smoothed by a running average for better readability.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 526}]"
motivation: 现有的扩散世界模型引导方式要么忽略价值信息，要么因仅关注即时奖励而导致在短时域生成中出现短视问题。
method: 提出AGD-MBRL框架，利用Sigmoid优势引导（SAG）和指数优势引导（EAG）来调整扩散采样过程，使其向高优势值轨迹集中。
result: 在MuJoCo控制任务中，该方法在采样效率和最终回报上显著优于PolyGRAD、奖励引导模型及PPO/TRPO等基准，部分任务性能提升达两倍。
conclusion: 优势感知引导是解决扩散模型MBRL中短视问题的简单且有效的方案，能显著增强生成轨迹的质量并实现策略改进。
---

## 摘要
基于自回归世界模型的模型强化学习（MBRL）面临复合误差问题，而扩散世界模型通过联合生成轨迹段来缓解这一问题。然而，现有的扩散引导机制要么仅基于策略（丢弃了价值信息），要么基于奖励（在扩散时界较短时会产生短视问题）。我们提出了用于 MBRL 的优势引导扩散（AGD-MBRL），它利用智能体的优势估计来引导反向扩散过程，使采样集中在预期在生成窗口之外能产生更高长期回报的轨迹上。我们开发了两种引导方式：(i) Sigmoid 优势引导 (SAG) 和 (ii) 指数优势引导 (EAG)。我们证明了通过 SAG 或 EAG 引导的扩散模型允许我们对轨迹进行重加权采样，其权重随状态-动作优势的增加而增加，这在标准假设下意味着策略改进。此外，我们表明与无引导的扩散模型相比，AGD-MBRL 生成的轨迹遵循改进后的策略（即具有更高的价值）。AGD 与 PolyGRAD 风格的架构无缝集成，通过引导状态分量同时保持动作生成受策略约束，且无需更改扩散训练目标。在 MuJoCo 控制任务（HalfCheetah、Hopper、Walker2D 和 Reacher）上，AGD-MBRL 在样本效率和最终回报方面优于 PolyGRAD、在线 Diffuser 风格的奖励引导以及无模型基准（PPO/TRPO），在某些情况下提升幅度达 2 倍。这些结果表明，优势感知引导是解决扩散模型 MBRL 中短时界短视问题的一种简单且有效的补救措施。

## Abstract
Model-based reinforcement learning (MBRL) with autoregressive world models suffers from compounding errors, whereas diffusion world models mitigate this by generating trajectory segments jointly. However, existing diffusion guides are either policy-only, discarding value information, or reward-based, which becomes myopic when the diffusion horizon is short. We introduce Advantage-Guided Diffusion for MBRL (AGD-MBRL), which steers the reverse diffusion process using the agent's advantage estimates so that sampling concentrates on trajectories expected to yield higher long-term return beyond the generated window. We develop two guides: (i) Sigmoid Advantage Guidance (SAG) and (ii) Exponential Advantage Guidance (EAG). We prove that a diffusion model guided through SAG or EAG allows us to perform reweighted sampling of trajectories with weights increasing in state-action advantage-implying policy improvement under standard assumptions. Additionally, we show that the trajectories generated from AGD-MBRL follow an improved policy (that is, with higher value) compared to an unguided diffusion model. AGD integrates seamlessly with PolyGRAD-style architectures by guiding the state components while leaving action generation policy-conditioned, and requires no change to the diffusion training objective. On MuJoCo control tasks (HalfCheetah, Hopper, Walker2D and Reacher), AGD-MBRL improves sample efficiency and final return over PolyGRAD, an online Diffuser-style reward guide, and model-free baselines (PPO/TRPO), in some cases by a margin of 2x. These results show that advantage-aware guidance is a simple, effective remedy for short-horizon myopia in diffusion-model MBRL.

---

## 论文详细总结（自动生成）

### 论文总结：基于优势引导扩散的模型强化学习 (AGD-MBRL)

#### 1. 核心问题与整体含义（研究动机和背景）
在模型强化学习（MBRL）中，传统的自回归世界模型（一步步预测未来）容易产生**复合误差（Compounding Errors）**，即微小的单步预测偏差会随时间累积导致轨迹偏离。扩散模型（Diffusion Models）通过联合生成整个轨迹段缓解了这一问题。
然而，现有的扩散引导机制存在局限：
*   **仅策略引导（如 PolyGRAD）：** 忽略了价值函数信息，无法主动探索高回报区域。
*   **基于奖励的引导（如 Diffuser）：** 存在**短视（Myopia）**问题。由于计算成本限制，扩散生成的轨迹时界（Horizon）通常较短，仅靠局部奖励总和无法反映超出生成窗口后的长期价值。

#### 2. 方法论：核心思想与关键技术
论文提出了 **AGD-MBRL**，其核心思想是利用智能体学习到的**优势函数（Advantage Function）** $A^\pi(s, a)$ 来引导扩散过程。优势函数包含了对未来长期回报的估计，从而克服了短视问题。

**关键技术细节：**
*   **两种引导机制：**
    1.  **指数优势引导 (EAG)：** 使用能量函数 $E(\tau) = \sum A_t$，通过 $\exp(E(\tau))$ 强烈地将采样过程推向高优势区域。
    2.  **Sigmoid 优势引导 (SAG)：** 将步进最优概率定义为 $\sigma(A_t)$。相比 EAG，SAG 更加保守，能有效抑制优势函数估计过高带来的偏差。
*   **理论证明：** 论文从数学上证明了使用 SAG 或 EAG 引导的扩散采样，等同于在一种“改进策略”下进行轨迹重采样，且该改进策略的期望价值不低于原策略。
*   **算法流程：**
    1.  在真实环境收集数据并更新扩散模型（预测噪声）。
    2.  在逆向扩散（去噪）过程中，计算优势函数关于轨迹的梯度 $g_i$。
    3.  利用梯度修正去噪均值，生成高优势轨迹。
    4.  在生成的虚拟轨迹上使用 Actor-Critic 算法改进策略。

#### 3. 实验设计
*   **实验场景：** 使用了 MuJoCo 连续控制套件中的四个经典任务：`HalfCheetah`、`Hopper`、`Walker2D` 和 `Reacher`。
*   **基准方法 (Benchmarks)：**
    *   **模型化方法：** PolyGRAD（仅策略引导）、Online Diffuser（本文实现的在线版奖励引导扩散模型）。
    *   **无模型方法：** PPO、TRPO。
*   **实验设置：** 所有方法均在 1.5M 环境步数下进行训练，评估最终回报和样本效率。

#### 4. 资源与算力
*   **算力来源：** 论文提到使用了瑞典国家超级计算基础设施 (NAISS) 的资源。
*   **具体细节：** 文中**未明确列出**具体的 GPU 型号、数量或确切的训练时长。但提到扩散模型使用了基于 MLP 的架构，并使用 Adam 优化器。由于扩散模型涉及多步迭代去噪，其计算开销通常显著高于传统的自回归模型。

#### 5. 实验数量与充分性
*   **实验规模：** 在 4 个不同的动力学环境下进行了对比，每组实验均包含均值和标准差（Shaded areas）。
*   **充分性：** 实验涵盖了从简单（Hopper）到复杂（HalfCheetah/Walker）的任务。通过对比“奖励引导”和“优势引导”，直接验证了解决“短视问题”的有效性。
*   **客观性：** 实验对比了主流的无模型算法和最先进的扩散 MBRL 算法，结果显示在多数任务中 AGD 具有明显优势，实验设计较为公正。

#### 6. 主要结论与发现
*   **性能提升：** AGD-MBRL 在样本效率和最终回报上显著优于所有基准。在 `HalfCheetah` 任务中，性能提升接近 2 倍。
*   **解决短视：** 实验证实，相比于仅看局部奖励的 Diffuser 引导，利用优势函数引导能让模型生成更具前瞻性的轨迹。
*   **稳定性：** SAG 和 EAG 在训练过程中表现出更好的稳定性，减少了性能回退的频率。其中 EAG 在简单任务中收敛更快，而 SAG 在复杂任务中更稳健。

#### 7. 优点
*   **理论完备：** 提供了关于策略改进的数学证明，为引导机制提供了理论支撑。
*   **即插即用：** 无需改变扩散模型的训练目标，仅在推理（采样）阶段通过梯度引导即可实现，易于集成到现有架构。
*   **长效性：** 成功解决了扩散 MBRL 中因生成时界短导致的规划短视问题。

#### 8. 不足与局限
*   **计算成本：** 扩散模型固有的多步迭代采样导致生成速度慢，这限制了其在对实时性要求极高的场景中的应用。
*   **函数近似偏差：** 引导的效果高度依赖于优势函数 $A$ 的估计精度。如果 RL 智能体的价值函数估计出现严重偏差，引导可能会将模型带向错误的分布。
*   **实验覆盖面：** 虽然在 MuJoCo 上表现良好，但尚未在更复杂的视觉输入任务或超长程规划任务中进行验证。

（完）
