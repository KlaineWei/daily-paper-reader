---
title: "RoTE: Coarse-to-Fine Multi-Level Rotary Time Embedding for Sequential Recommendation"
title_zh: RoTE：用于序列推荐的由粗到细多级旋转时间嵌入
authors: "Haolin Zhang, Longtao Xiao, Guohao Cai, Ruixuan Li, Xiu Li"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13389v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 用于序列推荐的多级时间嵌入以捕捉兴趣演化
tldr: "针对序列推荐中仅考虑交互顺序而忽略具体时间间隔的问题，本文提出RoTE模型。RoTE通过多级旋转时间嵌入，将时间戳分解为从粗到细的多种粒度，并将其融入项目嵌入中。该模块轻量且即插即用，能有效捕捉异构时间模式和交互间的时间距离。实验证明，RoTE在多个基准数据集上显著提升了Transformer类模型的性能，NDCG@5最高提升达20.11%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13389v1/fig-001.webp\", \"caption\": \"Table 2: Overall performance comparison of state-of-the-art sequential recommendation models and their RoTE-enhanced counterparts on three benchmark datasets. RoTE is a plug-and-play module that improves model performance without modifying the backbone architectures. Performance improvements of RoTE-based models over their corresponding backbones are statistically significant according to a paired t-test with 𝑝 < 0.05.\", \"page\": 4, \"index\": 1, \"width\": 1038, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2604.13389v1/fig-002.webp\", \"caption\": \"Figure 1: Illustration of the proposed RoTE module.\", \"page\": 2, \"index\": 2, \"width\": 446, \"height\": 340}]"
motivation: 现有序列推荐模型通常只关注交互顺序，忽略了交互间的实际时间跨度，导致无法精准捕捉用户的长短期兴趣演化。
method: 提出一种多级旋转时间嵌入模块，将时间戳分解为不同粒度并融入项目嵌入，以显式建模时间跨度信息。
result: "在三个公开数据集上的实验显示，RoTE能显著增强多种主流Transformer推荐模型，NDCG@5指标最高提升了20.11%。"
conclusion: RoTE是一个通用且高效的即插即用模块，通过多粒度时间建模显著提升了序列推荐系统的感知能力和预测精度。
---

## 摘要
序列推荐模型已被广泛用于建模用户行为。现有方法通常通过按时间戳对物品进行排序来构建用户交互序列，并从历史行为中建模用户偏好。尽管这种方法有效，但该过程仅考虑了时间信息的先后顺序，而忽略了交互之间的实际时间间隔，导致对用户时间动态的表示较为粗糙，并限制了模型捕捉长期和短期兴趣演变的能力。为了解决这一局限性，我们提出了 RoTE，这是一种新颖的多级时间嵌入模块，可在序列推荐中显式建模时间间隔信息。RoTE 将每个交互时间戳分解为从粗到细的多个时间粒度，并将生成的时间表示整合到物品嵌入中。这种设计使模型能够捕捉异构的时间模式，并在序列建模过程中更好地感知用户交互之间的时间距离。RoTE 是一个轻量级、即插即用的模块，可以无缝集成到现有的基于 Transformer 的序列推荐模型中，而无需修改其骨干架构。我们将 RoTE 应用于多个代表性模型，并在三个公开基准数据集上进行了广泛的实验。实验结果表明，RoTE 持续增强了相应的骨干模型，在 NDCG@5 指标上实现了高达 20.11% 的提升，证实了所提方法的有效性和通用性。我们的代码可在 https://github.com/XiaoLongtaoo/RoTE 获取。

## Abstract
Sequential recommendation models have been widely adopted for modeling user behavior. Existing approaches typically construct user interaction sequences by sorting items according to timestamps and then model user preferences from historical behaviors. While effective, such a process only considers the order of temporal information but overlooks the actual time spans between interactions, resulting in a coarse representation of users' temporal dynamics and limiting the model's ability to capture long-term and short-term interest evolution. To address this limitation, we propose RoTE, a novel multi-level temporal embedding module that explicitly models time span information in sequential recommendation. RoTE decomposes each interaction timestamp into multiple temporal granularities, ranging from coarse to fine, and incorporates the resulting temporal representations into item embeddings. This design enables models to capture heterogeneous temporal patterns and better perceive temporal distances among user interactions during sequence modeling. RoTE is a lightweight, plug-and-play module that can be seamlessly integrated into existing Transformer-based sequential recommendation models without modifying their backbone architectures. We apply RoTE to several representative models and conduct extensive experiments on three public benchmarks. Experimental results demonstrate that RoTE consistently enhances the corresponding backbone models, achieving up to a 20.11% improvement in NDCG@5, which confirms the effectiveness and generality of the proposed approach. Our code is available at https://github.com/XiaoLongtaoo/RoTE.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《RoTE: Coarse-to-Fine Multi-Level Rotary Time Embedding for Sequential Recommendation》** 的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的序列推荐模型（无论是判别式还是生成式）主要依赖物品的先后顺序（通过位置编码实现），但往往忽略了交互之间**实际的时间跨度（Time Spans）**。
*   **研究背景**：时间间隔在用户行为建模中至关重要。例如，两次购买间隔一天和间隔一年代表了完全不同的兴趣演化。传统的 Unix 时间戳是连续数值，难以直接反映人类感知的层级化时间（如年、月、日）。
*   **整体含义**：论文旨在通过一种轻量级、即插即用的模块，将多粒度的时间信息显式注入到 Transformer 的注意力机制中，从而提升模型对长短期兴趣演化的感知能力。

### 2. 方法论：RoTE 核心思想与技术细节
RoTE（Rotary Time Embedding）的核心是将时间戳分解并利用旋转位置嵌入（RoPE）的思想进行编码：
*   **时间特征构建**：将原始 Unix 时间戳分解为层级化的三元组 $(y_k, m_k, d_k)$，分别代表自 1970 年 1 月 1 日以来累计的年、月、日数。这种序数表示保留了时间顺序，同时提供了多粒度的语义。
*   **旋转时间嵌入**：
    *   借鉴 RoPE 的机制，为年、月、日三个层级分别定义不同的逆频率谱（Base Scalar）。
    *   对 Query ($q$) 和 Key ($k$) 向量应用旋转变换。旋转角度由时间特征值与频率向量相乘得到。
    *   这种变换通过旋转矩阵保持了向量范数，同时将时间差异编码为向量间的夹角关系，使注意力分数对相对时间距离敏感。
*   **多级融合策略**：
    *   独立计算年、月、日三个层级的旋转表示。
    *   使用加权融合策略（$\alpha_y, \alpha_m, \alpha_d$）将三个层级的表示聚合。通常给粗粒度（年）分配更高权重以捕捉长期偏好，细粒度（日）捕捉短期动态。
*   **即插即用**：该模块直接作用于自注意力层的 $q$ 和 $k$，不改变 Value ($v$) 向量，也不需要修改骨干网络架构。

### 3. 实验设计
*   **数据集**：使用了 Amazon Reviews 的三个公开基准数据集：`Sports and Outdoors`、`Beauty`、`Toys and Games`。采用 5-core 预处理（过滤交互少于 5 次的用户和物品）。
*   **Benchmark 与对比方法**：
    *   **传统判别式模型**：GRU4Rec, Caser, BERT4Rec, SASRec。
    *   **生成式模型**：VQ-Rec, TIGER, HSTU, RPG。
*   **评估指标**：Recall@K 和 NDCG@K (K=5, 10)，采用留一法（Leave-one-out）评估。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **效率分析**：文中提供了一张效率对比表，指出 RoTE 引入的 FLOPs 增加极小（例如在 RPG 模型上，FLOPs 从 994.94M 仅微增至 994.97M），推理延迟增加在毫秒级。由于取代了传统的查找式位置编码表，参数量甚至略有下降。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上对 2 种代表性骨干网络（SASRec 和 RPG）进行了 RoTE 增强对比。
    *   进行了消融实验，验证了从“纯时间戳”到“年+月+日”多级建模的增量效果。
    *   进行了效率分析实验。
*   **充分性评价**：实验设计较为客观，涵盖了当前主流的生成式和判别式推荐范式。通过 5 个随机种子取平均值并进行了显著性检验（p < 0.05），结果具有统计学意义。

### 6. 主要结论与发现
*   **性能提升显著**：RoTE 在所有数据集和指标上均一致优于骨干模型。在 `Toys and Games` 数据集上，相比 RPG 模型，NDCG@5 提升了 **20.11%**。
*   **多级建模的必要性**：消融实验证明，将时间分解为年、月、日比直接使用原始 Unix 时间戳效果更好，说明层级化的时间语义更符合用户行为模式。
*   **通用性强**：RoTE 作为一个轻量级模块，能够无缝集成到各种基于 Transformer 的推荐架构中，且不增加显著计算负担。

### 7. 优点
*   **即插即用**：无需修改复杂的模型结构，易于在现有工业系统中部署。
*   **语义对齐**：将抽象的数字时间戳转化为符合人类感知的年/月/日层级，捕捉了异构的时间动态。
*   **高效性**：利用旋转嵌入的数学特性，避免了大规模嵌入表的维护，计算开销极低。

### 8. 不足与局限
*   **权重超参数**：融合权重 ($\alpha$) 和基数频率 (base) 需要手动预设或调优，文中未讨论这些超参数在不同场景下的自适应学习方法。
*   **时间特征单一**：目前仅考虑了绝对日历时间，未考虑周期性特征（如星期几、节假日）或用户特定的相对时间（如注册后的第几天）。
*   **实验覆盖面**：虽然涵盖了 Amazon 数据集，但未在交互更密集、时间敏感度更高的场景（如新闻推荐或短视频推荐）中进行验证。

（完）
