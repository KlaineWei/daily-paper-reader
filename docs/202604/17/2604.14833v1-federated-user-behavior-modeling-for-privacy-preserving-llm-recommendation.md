---
title: Federated User Behavior Modeling for Privacy-Preserving LLM Recommendation
title_zh: 面向隐私保护大语言模型推荐的联邦用户行为建模
authors: "Lei Guo, Hongyun Yang, Pengjie Ren, Tong Chen, Hui Liu, Zhumin Chen"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14833v1"
tags: ["query:llmrec"]
score: 9.0
evidence: 使用大模型和联邦学习的隐私保护跨域推荐
tldr: 针对大语言模型在推荐系统中面临的数据稀疏与隐私泄露挑战，本文提出SF-UBM框架。该方法通过联邦学习架构在不共享原始数据的前提下，利用自然语言作为跨域桥梁，结合知识蒸馏与软提示投影技术，实现了非重叠域下的隐私保护跨域推荐，显著提升了模型对用户行为的建模能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14833v1/fig-001.webp\", \"caption\": \"Figure 2 Parameter sensitivity analysis on three datasets. These figures show the effect of parameter α on Hit@1 for Health-Beauty, FoodKitchen, and Books-Movielens.\", \"page\": 15, \"index\": 1, \"width\": 1043, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.14833v1/fig-002.webp\", \"caption\": \"Figure 3 Parameter sensitivity analysis on three datasets. These figures show the effect of parameter β on Hit@1 for Health-Beauty, FoodKitchen, and Books-Movielens.\", \"page\": 15, \"index\": 2, \"width\": 1043, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.14833v1/fig-003.webp\", \"caption\": \"Table 4 Ablation studies on Health-Beauty, Food-Kitchen, and Books-Movielens.\", \"page\": 14, \"index\": 3, \"width\": 1037, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.14833v1/fig-004.webp\", \"caption\": \"Figure 1 The system architecture of SF-UBM, comprising two stages: 1) federated cross-domain semantic learning, where item semantics are aggregated with privacy protection and transferred to ID embeddings via FKD; 2) LLM-based behavior prediction, where pre-learned knowledge is projected into LLM prompts for fine-tuning.\", \"page\": 5, \"index\": 4, \"width\": 985, \"height\": 813}, {\"url\": \"assets/figures/arxiv/2604.14833v1/fig-005.webp\", \"caption\": \"Figure 4 Parameter sensitivity analysis on three datasets. These figures show the effect of parameter k on Hit@1 for Health-Beauty, FoodKitchen, and Books-Movielens.\", \"page\": 16, \"index\": 5, \"width\": 1043, \"height\": 262}]"
motivation: 现有LLM推荐方法难以在保护隐私的前提下，有效整合非重叠跨域数据中的异构知识以及协同过滤信号。
method: 提出SF-UBM方法，利用联邦架构共享加密文本表示，通过知识蒸馏融合跨域知识，并将用户偏好投影至软提示空间以对齐语义与行为特征。
result: 在三组真实世界跨域数据集上的实验结果证明，SF-UBM在推荐性能上显著优于现有的最先进基准方法。
conclusion: 该研究通过结合联邦学习与语义增强技术，为LLM在隐私保护场景下的跨域行为建模提供了有效的解决方案。
---

## 摘要
大语言模型（LLM）在推荐系统中取得了巨大成功。然而，用户数据的有限性和稀疏性往往限制了 LLM 有效建模行为模式的能力。为了解决这一问题，现有研究通过执行跨域推荐（CDR）任务探索了跨域解决方案。但以往的方法通常假设领域是重叠的且可以被直接访问。目前尚无 LLM 方法解决 CDR 场景下的隐私保护问题，即隐私保护跨域推荐（PPCDR）。利用 LLM 进行非重叠 PPCDR 具有挑战性，原因在于：1）无法跨域共享用户身份或行为数据，阻碍了有效的跨域对齐；2）不同领域间数据模态的异构性使知识整合变得复杂；3）将传统推荐模型的协同过滤信号与 LLM 融合较为困难，因为它们运行在不同的特征空间中。为了解决上述问题，我们提出了 SF-UBM，一种语义增强的联邦用户行为建模方法。具体而言，针对挑战 1，我们利用自然语言作为通用桥梁，通过语义增强的联邦架构连接不相交的领域。在此架构中，基于文本的物品表示被加密并共享，而用户特定数据保留在本地。针对挑战 2，我们设计了一个事实对事实（Fact-counter）知识蒸馏模块，以整合跨不同数据模态的领域无关知识与领域特定知识。针对挑战 3，我们将预先学习的用户偏好和跨域物品表示投影到软提示（soft prompt）空间中，对齐行为空间与语义空间，以实现有效的 LLM 学习。我们在三组真实世界领域上进行了广泛实验，实验结果证明了 SF-UBM 与近期最先进（SOTA）方法相比的有效性。

## Abstract
Large Language Models have shown great success in recommender systems. However, the limited and sparse nature of user data often restricts the LLM's ability to effectively model behavior patterns. To address this, existing studies have explored cross-domain solutions by conducting Cross-Domain Recommendation tasks. But previous methods typically assume domains are overlapped and can be accessed readily. None of the LLM methods address the privacy-preserving issues in the CDR settings, that is, Privacy-Preserving Cross-Domain Recommendation. Conducting non-overlapping PPCDR with LLM is challenging since: 1)The inability to share user identity or behavioral data across domains impedes effective cross-domain alignment. 2)The heterogeneity of data modalities across domains complicates knowledge integration. 3)Fusing collaborative filtering signals from traditional recommendation models with LLMs is difficult, as they operate within distinct feature spaces. To address the above issues, we propose SF-UBM, a Semantic-enhanced Federated User Behavior Modeling method. Specifically, to deal with Challenge 1, we leverage natural language as a universal bridge to connect disjoint domains via a semantic-enhanced federated architecture. Here, text-based item representations are encrypted and shared, while user-specific data remains local. To handle Challenge 2, we design a Fact-counter Knowledge Distillation module to integrate domain-agnostic knowledge with domain-specific knowledge, across different data modalities. To tackle Challenge 3, we project pre-learned user preferences and cross-domain item representations into the soft prompt space, aligning behavioral and semantic spaces for effective LLM learning. We conduct extensive experiments on three pairs of real-world domains, and the experimental results demonstrate the effectiveness of SF-UBM compared to the recent SOTA methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SF-UBM**（Semantic-enhanced Federated User Behavior Modeling）的新框架，旨在解决大语言模型（LLM）在推荐系统中面临的数据稀疏与隐私保护跨域推荐（PPCDR）难题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 LLM 在推荐领域表现出色，但单领域用户行为数据通常非常稀疏。跨域推荐（CDR）虽能缓解此问题，但现有方法大多假设领域间存在重叠用户/物品，且忽略了数据隐私。
*   **核心挑战**：在**非重叠（Non-overlapping）**且**隐私受限**的场景下，如何实现有效的跨域知识迁移？
    1.  **对齐难**：无法共享用户 ID 或原始行为数据。
    2.  **模态异构**：文本语义与 ID 特征难以整合。
    3.  **空间不匹配**：传统协同过滤（CF）信号与 LLM 的语义特征空间存在鸿沟。

### 2. 方法论：核心思想与关键技术
SF-UBM 框架分为两个主要阶段：

*   **阶段 1：联邦跨域语义学习（Federated Semantic Learning）**
    *   **语义提取与加密**：利用 SBERT 提取物品文本语义，并采用“噪声扰动 + 相似性替换（SimReplace）”机制对嵌入进行加密，确保无法逆向还原原始文本。
    *   **领域无关语义挖掘**：各客户端上传加密后的文本嵌入，服务器通过 K-means++ 聚类识别跨域共享的语义中心，并下发给客户端作为全局知识。
    *   **事实对事实知识蒸馏（FKD）**：在本地客户端，通过蒸馏损失对齐 ID 模态（领域特定）与文本模态（领域无关）的表示，利用“事实”与“反事实”偏好的一致性增强行为建模。
*   **阶段 2：基于 LLM 的行为预测**
    *   **知识投影**：设计两个投影层（Projector），将预训练的协同过滤用户表示和增强后的物品表示映射到 LLM 的软提示（Soft Prompt）空间。
    *   **混合提示微调**：将投影后的连续向量与静态文本指令结合，输入到 OPT-6.7B 模型中进行微调，使 LLM 能够理解跨域语义和序列模式。

### 3. 实验设计
*   **数据集与场景**：使用了三对真实世界的非重叠领域数据集：
    *   Amazon: **Health-Beauty**（健康-美容）
    *   Amazon: **Food-Kitchen**（食品-厨房）
    *   Amazon/Movielens: **Books-Movielens**（图书-电影，跨平台场景）
*   **Benchmark 与对比方法**：
    *   **ID-only**：SASRec, GRU4Rec。
    *   **跨域推荐（CDR）**：PTUPCDR, RecGURU。
    *   **ID-Text 混合**：UniSRec, PFCR, FFMSR（后两者为 PPCDR 的 SOTA）。
    *   **LLM 推荐**：TALLRec, A-LLMRec, MLP-LLM。
*   **评价指标**：Hit Ratio@1（命中率）和 Valid Ratio（输出格式有效率）。

### 4. 资源与算力
*   **硬件设备**：实验主要在 **NVIDIA RTX 3090 GPU** 上运行；其中 TALLRec 基准测试在 **NVIDIA RTX 5090 GPU** 上运行。
*   **模型规模**：采用 **OPT-6.7B** 作为 LLM 主干网络。
*   **训练细节**：阶段 1 训练 10 个 epoch，阶段 2 微调 5 个 epoch，学习率设为 0.0001。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个不同性质的领域对上进行了主实验，并进行了详尽的**消融实验**（验证文本、ID、蒸馏、投影各模块的作用）。
*   **深度分析**：包括超参数敏感性分析（聚类数 $k$、损失权重 $\alpha, \beta$）以及**隐私保护强度分析**（噪声水平 $\sigma$ 对性能的影响）。
*   **客观性**：对比了从传统模型到最新 LLM 模型的 11 种基准方法，实验设计较为全面、公平，涵盖了单域、跨域及隐私保护等多个维度。

### 6. 主要结论与发现
*   **性能领先**：SF-UBM 在所有数据集上的 Hit@1 指标均显著优于现有 SOTA 方法，尤其在跨平台（Books-Movielens）场景下提升巨大。
*   **语义桥梁作用**：自然语言作为“通用语言”，在不暴露用户隐私的前提下，能有效连接完全不相交的领域。
*   **模态融合有效性**：通过 FKD 蒸馏技术，ID 模态能够吸收文本模态的全局语义，从而在数据稀疏时提供更稳健的表示。
*   **隐私与性能平衡**：增加扰动噪声会降低推荐精度，但在合理的噪声范围内（如 $\sigma=0.05$），模型仍能保持较高的实用性。

### 7. 优点与亮点
*   **隐私保护机制新颖**：结合了联邦学习与“相似性替换”扰动，比传统的差异隐私或重型加密更具计算效率。
*   **非重叠场景突破**：针对现实中更常见的“无重叠用户”PPCDR 场景提供了切实可行的 LLM 解决方案。
*   **双空间对齐**：通过软提示投影，成功桥接了离散的行为 ID 空间与连续的 LLM 语义空间。

### 8. 不足与局限
*   **模态局限性**：目前主要侧重于文本语义，尚未涉及图像、视频等多模态信息的跨域对齐。
*   **算力开销**：虽然使用了投影层，但微调 6.7B 规模的 LLM 对于大规模工业级实时推荐系统来说，推理延迟和计算成本仍是挑战。
*   **冷启动深度**：虽然缓解了稀疏性，但对于完全没有任何历史记录的“纯冷启动”用户，其表现仍有待进一步探索。

（完）
