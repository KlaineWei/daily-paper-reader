---
title: "GenRec: A Preference-Oriented Generative Framework for Large-Scale Recommendation"
title_zh: GenRec：一种面向大规模推荐的偏好导向生成式框架
authors: "Yanyan Zou, Junbo Qi, Lunsong Huang, Yu Li, Kewei Xu, Jiabao Gao, Binglei Zhao, Xuanhua Yang, Sulong Xu, Shengjie Li"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14878v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 基于Decoder-only架构的大规模推荐生成式检索框架
tldr: "GenRec是一个面向大规模推荐的生成式框架，已在京东App部署。它通过Page-wise NTP任务解决分页请求中的输出不一致问题，利用Token Merger压缩语义ID以降低长序列编码成本，并引入结合强化学习（GRPO-SR）与混合奖励机制的对齐策略。该框架显著提升了推荐的准确性与用户满意度，在线测试中点击和交易量分别增长了9.5%和8.7%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14878v1/fig-001.webp\", \"caption\": \"Table 3: RL performance. We analyze the impact of different method and the gating mechanism (G). Best results are in bold, and the second best are underlined.\", \"page\": 5, \"index\": 1, \"width\": 504, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2604.14878v1/fig-002.webp\", \"caption\": \"Figure 2: SFT loss curves. (a) Page-wise NTP converges faster than NTP. (b) Larger models achieve lower loss with diminishing returns beyond 3B.\", \"page\": 5, \"index\": 2, \"width\": 1036, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2604.14878v1/fig-003.webp\", \"caption\": \"Figure 1: Model architecture of GenRec. High-dimensional items are quantized into Semantic IDs. To enhance efficiency, an Linear token Merger projects the concatenated embeddings of an item’s SIDs into a unified latent vector on the prefilling side. Other tokens (e.g. <sep>) remain uncompressed.\", \"page\": 3, \"index\": 3, \"width\": 819, \"height\": 513}]"
motivation: 旨在解决生成式推荐在大规模工业应用中面临的分页输出不一致、长序列编码成本高以及生成策略与用户偏好对齐难的问题。
method: 提出Page-wise NTP训练任务、非对称线性Token Merger压缩技术，以及结合NLL正则化和混合奖励的GRPO-SR强化学习对齐方法。
result: "在京东App的月度在线A/B测试中，该框架相比现有流水线实现了点击数提升9.5%和交易数提升8.7%的显著增长。"
conclusion: GenRec证明了通过优化训练目标、压缩输入表征和强化学习对齐，生成式检索能够有效应用于大规模工业级推荐系统并取得显著收益。
---

## 摘要
生成式检索（GR）通过下个标记预测（NTP）为推荐系统提供了一种极具前景的范式。然而，将其扩展到大规模工业系统面临三个挑战：(i) 在单次请求中，由于分页请求机制，相同的模型输入可能会产生不一致的输出；(ii) 使用基于语义 ID 的多标记物品表示来编码长用户行为序列的成本过高；(iii) 使生成策略与细微的用户偏好信号保持一致。我们提出了 GenRec，这是一种部署在京东 App 上的偏好导向生成式框架，在单一的纯解码器（decoder-only）架构中解决了上述挑战。在训练目标方面，我们提出了页面级 NTP（Page-wise NTP）任务，该任务对整个交互页面而非单个交互物品进行监督，从而提供更密集的梯度信号，并解决了逐点（point-wise）训练中的一对多歧义问题。在预填充（prefilling）端，一种非对称线性标记合并器（Token Merger）在保留全分辨率解码的同时，压缩了提示词（prompt）中的多标记语义 ID，在精度损失微乎其微的情况下将输入长度缩短了约 2 倍。为了进一步使输出符合用户满意度，我们引入了 GRPO-SR，这是一种强化学习方法，它将组相对策略优化（GRPO）与负对数似然（NLL）正则化相结合以保证训练稳定性，并采用结合了密集奖励模型与相关性门控的混合奖励（Hybrid Rewards）来缓解奖励作弊（reward hacking）问题。在为期一个月的生产流量在线 A/B 测试中，GenRec 相比现有流水线在点击量和交易量上分别实现了 9.5% 和 8.7% 的提升。

## Abstract
Generative Retrieval (GR) offers a promising paradigm for recommendation through next-token prediction (NTP). However, scaling it to large-scale industrial systems introduces three challenges: (i) within a single request, the identical model inputs may produce inconsistent outputs due to the pagination request mechanism; (ii) the prohibitive cost of encoding long user behavior sequences with multi-token item representations based on semantic IDs, and (iii) aligning the generative policy with nuanced user preference signals. We present GenRec, a preference-oriented generative framework deployed on the JD App that addresses above challenges within a single decoder-only architecture. For training objective, we propose Page-wise NTP task, which supervises over an entire interaction page rather than each interacted item individually, providing denser gradient signal and resolving the one-to-many ambiguity of point-wise training. On the prefilling side, an asymmetric linear Token Merger compresses multi-token Semantic IDs in the prompt while preserving full-resolution decoding, reducing input length by ~2X with negligible accuracy loss. To further align outputs with user satisfaction, we introduce GRPO-SR, a reinforcement learning method that pairs Group Relative Policy Optimization with NLL regularization for training stability, and employs Hybrid Rewards combining a dense reward model with a relevance gate to mitigate reward hacking. In month-long online A/B tests serving production traffic, GenRec achieves 9.5% improvement in click count and 8.7% in transaction count over the existing pipeline.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GenRec** 的生成式推荐框架，旨在解决生成式检索（Generative Retrieval, GR）在大型工业级推荐系统（如京东 App）中落地时面临的实际挑战。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成式推荐通过将推荐任务建模为“下个标记预测（NTP）”展现了巨大潜力，但在工业大规模应用中存在三大痛点：
*   **一对多歧义性**：工业系统采用分页请求机制，同一用户历史（输入）在同一页面可能对应多个正向反馈物品（输出），导致标准 NTP 训练目标出现标签模糊，梯度方差大。
*   **计算开销巨大**：使用语义 ID（Semantic IDs, SIDs）表示物品会导致输入序列长度成倍增加（如一个物品对应 3 个 token），在处理长用户行为序列时推理延迟难以接受。
*   **偏好对齐困难**：简单的生成训练难以捕捉用户细微的满意度信号，且直接应用强化学习容易陷入“奖励作弊（Reward Hacking）”，生成语义无关但高分的无效 ID。

### 2. 方法论：核心思想与关键技术
GenRec 采用 **Decoder-only** 架构（基于 Qwen2.5），核心技术包括：
*   **页面级 NTP（Page-wise NTP）**：改变传统的“预测下一个物品”模式，将整个交互页面（下单、点击、曝光物品）按强度排序后作为目标序列进行监督训练。这提供了更密集的梯度信号，消除了输入相同而输出不同的歧义。
*   **非对称线性标记合并器（Token Merger）**：在预填充（Prefilling）阶段，通过线性层将物品的多个 SID token 嵌入合并为一个向量，使输入长度缩短约 2 倍；而在解码阶段保持原始 SID 序列以确保检索精度。
*   **GRPO-SR 强化学习对齐**：
    *   **算法**：改进组相对策略优化（GRPO），引入 **NLL 正则化（Supervised Regularization）**，将策略锚定在真实用户行为上，防止模型在强化学习过程中“跑偏”。
    *   **混合奖励机制**：结合密集偏好模型（SIM）和相关性门控（Gating Mechanism）。门控机制通过过滤无效或无关的 SID 组合来抑制奖励作弊。

### 3. 实验设计
*   **数据集与场景**：使用来自京东 App 的真实大规模数据，包含约 **5.6 亿条**用户交互序列，时间跨度为一个月。
*   **Benchmark（基准方法）**：
    *   **传统方法**：BERT4Rec, SASRec。
    *   **生成式方法**：TIGER, LC-Rec（均基于 NTP 训练）。
*   **对比指标**：HitRate (HR@K), NDCG (N@K), 幻觉率 (HaR, 无效 ID 占比) 以及在线业务指标（点击数、交易数）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU** 进行分布式训练。
*   **模型规模**：在 Qwen2.5 的 1.5B、3B 和 7B 版本上进行了实验。
*   **训练细节**：采用 AdamW 优化器，配合线性预热和余弦学习率衰减。

### 5. 实验数量与充分性
论文进行了多维度的实验验证，具有较高的客观性和说服力：
*   **离线对比实验**：在 5.6 亿规模的数据集上对比了 4 种基准模型。
*   **消融实验**：验证了 Token Merger 的有效性（证明其在减半长度的同时几乎不损精度）以及 GRPO-SR 中各组件（如门控机制 G）的作用。
*   **Scaling Law 研究**：分析了模型参数量（1.5B 到 7B）对损失函数和推荐性能的影响。
*   **在线 A/B 测试**：在京东首页信息流进行了为期一个月的实测，覆盖 10% 的真实生产流量。

### 6. 主要结论与发现
*   **性能领先**：GenRec 在离线指标上显著优于传统和现有的生成式方法，且幻觉率（HaR）降低了 50% 以上。
*   **业务收益**：在线 A/B 测试显示，GenRec 相比现有流水线提升了 **9.5% 的点击量**和 **8.7% 的交易量**。
*   **模型规模效应**：发现 3B 模型在深度和宽度的平衡上表现优异，从 3B 扩展到 7B 的收益边际递减，提示在推荐领域“深度”可能比“宽度”更重要。
*   **对齐的重要性**：GRPO-SR 有效解决了点击与转化之间的失配问题，显著提升了长尾物品的曝光和转化。

### 7. 优点（亮点）
*   **工业实战导向**：针对分页请求和推理延迟等真实工业痛点提出了切实可行的解决方案（Page-wise NTP 和 Token Merger）。
*   **创新的对齐策略**：将大语言模型中先进的 GRPO 算法引入推荐系统，并通过 NLL 正则化和门控奖励解决了推荐场景下的奖励作弊问题。
*   **高效性**：Token Merger 的设计巧妙地平衡了长序列建模能力与在线推理延迟。

### 8. 不足与局限
*   **场景单一性**：实验主要在京东单一电商平台进行，虽然数据量巨大，但在其他类型的推荐场景（如短视频、社交）中的泛化能力有待验证。
*   **推理能力探索不足**：论文末尾提到未来将研究该框架的推理（Reasoning）能力，目前版本仍主要侧重于检索和排序的性能提升。
*   **冷启动细节**：虽然提到了使用多模态信息生成 SID，但对于极度稀疏或全新物品的冷启动表现缺乏详尽的专项分析。

（完）
