---
title: Advantage-Guided Diffusion for Model-Based Reinforcement Learning
title_zh: 优势引导的扩散模型在基于模型的强化学习中的应用
authors: "Daniele Foffano, Arvid Eriksson, David Broman, Karl H. Johansson, Alexandre Proutiere"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09035v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于模型强化学习的优势引导扩散，旨在提高长期回报
tldr: 针对基于模型的强化学习（MBRL）中自回归模型误差累积及现有扩散模型引导机制（如仅策略或基于奖励）存在的短视问题，本文提出优势引导扩散（AGD-MBRL）。该方法利用优势函数估计引导扩散过程，使采样集中于高长期回报的轨迹。通过引入Sigmoid和指数优势引导，在MuJoCo任务中显著提升了采样效率和最终回报，证明了优势感知引导在解决短视问题上的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09035v1/fig-001.webp\", \"caption\": \"Fig. 2: Training curves for the Mujoco environments: HalfCheetah, Hopper, Walker and Reacher. Shaded areas indicate the standard error. The curves have been smoothed by a running average for better readability.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 526}]"
motivation: 现有的扩散世界模型引导机制要么忽略价值信息，要么因仅关注短期奖励而导致在短时域内表现出短视性。
method: 提出AGD-MBRL，通过Sigmoid优势引导（SAG）和指数优势引导（EAG）来调整逆向扩散过程，使生成的轨迹向高优势值区域集中。
result: 在MuJoCo控制任务中，AGD-MBRL在采样效率和最终回报上显著优于PolyGRAD、在线奖励引导及PPO/TRPO等基准模型，部分任务提升达2倍。
conclusion: 优势感知引导是解决扩散模型MBRL中短视问题的简单且有效的方案，能显著提升策略改进的效果。
---

## 摘要
采用自回归世界模型的基于模型的强化学习（MBRL）面临复合误差问题，而扩散世界模型通过联合生成轨迹段来缓解这一问题。然而，现有的扩散引导机制要么仅基于策略（丢弃了价值信息），要么基于奖励（当扩散时界较短时会变得短视）。我们提出了用于 MBRL 的优势引导扩散（AGD-MBRL），它利用智能体的优势估计来引导反向扩散过程，使采样集中在预期在生成窗口之外能产生更高长期回报的轨迹上。我们开发了两种引导方式：(i) Sigmoid 优势引导 (SAG) 和 (ii) 指数优势引导 (EAG)。我们证明，通过 SAG 或 EAG 引导的扩散模型允许我们对轨迹进行重加权采样，其权重随状态-动作优势的增加而增加——在标准假设下，这意味着策略的改进。此外，我们表明，与无引导的扩散模型相比，AGD-MBRL 生成的轨迹遵循改进的策略（即具有更高的价值）。AGD 与 PolyGRAD 风格的架构无缝集成，通过引导状态分量同时保持动作生成受策略约束，且无需更改扩散训练目标。在 MuJoCo 控制任务（HalfCheetah、Hopper、Walker2D 和 Reacher）上，AGD-MBRL 在样本效率和最终回报方面优于 PolyGRAD、在线 Diffuser 风格的奖励引导以及无模型基准（PPO/TRPO），在某些情况下提升幅度达到 2 倍。这些结果表明，感知优势的引导是解决扩散模型 MBRL 中短时界短视问题的一种简单且有效的补救措施。

## Abstract
Model-based reinforcement learning (MBRL) with autoregressive world models suffers from compounding errors, whereas diffusion world models mitigate this by generating trajectory segments jointly. However, existing diffusion guides are either policy-only, discarding value information, or reward-based, which becomes myopic when the diffusion horizon is short. We introduce Advantage-Guided Diffusion for MBRL (AGD-MBRL), which steers the reverse diffusion process using the agent's advantage estimates so that sampling concentrates on trajectories expected to yield higher long-term return beyond the generated window. We develop two guides: (i) Sigmoid Advantage Guidance (SAG) and (ii) Exponential Advantage Guidance (EAG). We prove that a diffusion model guided through SAG or EAG allows us to perform reweighted sampling of trajectories with weights increasing in state-action advantage-implying policy improvement under standard assumptions. Additionally, we show that the trajectories generated from AGD-MBRL follow an improved policy (that is, with higher value) compared to an unguided diffusion model. AGD integrates seamlessly with PolyGRAD-style architectures by guiding the state components while leaving action generation policy-conditioned, and requires no change to the diffusion training objective. On MuJoCo control tasks (HalfCheetah, Hopper, Walker2D and Reacher), AGD-MBRL improves sample efficiency and final return over PolyGRAD, an online Diffuser-style reward guide, and model-free baselines (PPO/TRPO), in some cases by a margin of 2x. These results show that advantage-aware guidance is a simple, effective remedy for short-horizon myopia in diffusion-model MBRL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Advantage-Guided Diffusion for Model-Based Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在基于模型的强化学习（MBRL）中，传统的自回归世界模型（一步步预测未来）存在**复合误差（Compounding Errors）**问题。虽然扩散模型（Diffusion Models）通过联合生成整个轨迹段缓解了这一问题，但现有的扩散引导机制存在缺陷：
    *   **策略引导（如 PolyGRAD）**：仅模仿当前策略，忽略了价值函数信息。
    *   **奖励引导（如 Diffuser）**：仅关注生成窗口内的即时奖励。由于计算成本限制，扩散时界（Horizon）通常很短，这导致模型在规划时表现出**短视（Myopia）**，忽略了窗口之外的长期回报。
*   **研究动机**：利用强化学习中的**优势函数（Advantage Function）**来引导扩散过程，使生成的轨迹不仅符合当前策略，还能向预期长期回报更高的区域偏移。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **AGD-MBRL** 框架，将智能体学习到的优势估计 $A^\pi(s, a)$ 引入扩散模型的反向去噪过程。优势函数包含了对未来长期价值的评估，从而克服了短时界带来的短视问题。
*   **关键技术细节**：
    *   **两种引导机制**：
        1.  **Sigmoid 优势引导 (SAG)**：将步进最优概率定义为优势函数的 Sigmoid 变换。这是一种保守的加权方式，能有效处理优势估计中的噪声和过估计。
        2.  **指数优势引导 (EAG)**：采用能量模型（Energy-based）方法，以优势函数的指数形式进行引导。这种方式对高优势区域的偏移更强烈，收敛可能更快，但对估计误差更敏感。
    *   **理论证明**：论文证明了使用 SAG 或 EAG 引导的采样等同于对轨迹进行重加权，且生成的轨迹分布对应于一个性能优于原策略的新策略（Policy Improvement）。
    *   **算法流程**：在 Dyna 风格的循环中，首先用真实数据训练扩散模型；然后在采样时，利用优势函数的梯度 $\nabla A$ 修正去噪步骤的状态分量，最后在生成的“想象”轨迹上使用 Actor-Critic 算法改进策略。

### 3. 实验设计
*   **实验场景**：使用了 MuJoCo 连续控制基准测试集中的四个典型任务：**HalfCheetah、Hopper、Walker2D 和 Reacher**。
*   **Benchmark 与对比方法**：
    *   **扩散模型基准**：**PolyGRAD**（仅策略引导）、**Online Diffuser**（作者改编的在线奖励引导版本）。
    *   **无模型（Model-free）基准**：**PPO** 和 **TRPO**。
*   **实验设置**：所有方法均在 150 万（1.5M）环境交互步数下进行训练，评估最终回报和采样效率。

### 4. 资源与算力
*   **算力支持**：论文提到计算资源由瑞典国家超级计算基础设施（NAISS）提供。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或确切的训练时长。但提到扩散模型由于需要多次迭代去噪，其生成速度比传统模型慢，这是该类方法的共同特征。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流 MuJoCo 环境上进行了对比，每个环境都展示了完整的训练曲线和最终性能均值/标准差。
*   **充分性评价**：
    *   **对比全面**：涵盖了从无模型到最先进的扩散 MBRL 方法。
    *   **客观性**：通过展示 SAG 和 EAG 在不同环境下的表现差异（如 HalfCheetah 适合 EAG，而 Walker 适合 SAG），体现了实验的客观性。
    *   **局限性**：实验主要集中在连续控制任务上，未涉及视觉输入或更复杂的长程逻辑任务。

### 6. 主要结论与发现
*   **性能提升**：AGD-MBRL 在多数任务中显著优于 PolyGRAD 和奖励引导的 Diffuser，在 HalfCheetah 任务中性能提升甚至达到 2 倍。
*   **克服短视**：实验结果证实，相比于仅看即时奖励的引导，优势引导能让智能体在短生成窗口下依然能感知到长期的价值方向。
*   **学习稳定性**：AGD-MBRL 的训练曲线更加平滑，减少了性能回退的频率，表现出更好的鲁棒性。

### 7. 优点（亮点）
*   **理论支撑**：不仅提出了算法，还给出了关于策略改进的数学证明，解释了引导机制为何有效。
*   **即插即用**：AGD 机制不需要改变扩散模型的训练目标，只需在推理（采样）阶段加入梯度引导，易于集成到现有架构中。
*   **解决痛点**：直接针对扩散模型在 MBRL 中因计算压力导致的“短视”痛点，提出了简单且物理意义明确的解决方案。

### 8. 不足与局限
*   **计算开销**：扩散模型固有的多步迭代去噪导致轨迹生成速度慢，这限制了其在实时性要求极高场景下的应用。
*   **超参数敏感性**：引导强度 $\alpha$ 和选择 SAG 还是 EAG 对结果有显著影响，可能需要针对不同环境进行调优。
*   **依赖价值估计**：如果底层的优势函数（价值网络）估计极度不准确，引导过程可能会将模型带偏（虽然 SAG 缓解了这一点，但风险依然存在）。

（完）
