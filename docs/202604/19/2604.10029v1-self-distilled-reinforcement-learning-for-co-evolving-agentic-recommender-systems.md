---
title: Self-Distilled Reinforcement Learning for Co-Evolving Agentic Recommender Systems
title_zh: 面向协同演化智能体推荐系统的自蒸馏强化学习
authors: "Zongwei Wang, Min Gao, Hongzhi Yin, Junliang Yu, Tong Chen, Shazia Sadiq, Tianrui Li"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10029v1"
tags: ["query:llmrec"]
score: 10.0
evidence: 基于强化学习的大语言模型智能体推荐系统
tldr: 本文针对智能体推荐系统（ARS）过度依赖外部记忆而缺乏参数化学习的问题，提出了CoARS框架。该框架利用自蒸馏强化学习实现推荐智能体与用户智能体的协同演化。通过引入交互奖励和Token级自蒸馏信用分配机制，CoARS能从多轮交互中提取密集监督信号，将交互经验内化为模型能力，显著提升了推荐性能和用户对齐度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10029v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of evolution paradigms in agentic recommender systems. The left part shows the standard ARS pipeline with the Reflexion paradigm. The right part compares three RL paradigms.\", \"page\": 2, \"index\": 1, \"width\": 1022, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.10029v1/fig-002.webp\", \"caption\": \"TABLE III: Recommendation performance comparison across datasets. The best result in each dataset column is highlighted in bold, and the second-best result is underlined.\", \"page\": 7, \"index\": 2, \"width\": 496, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.10029v1/fig-003.webp\", \"caption\": \"TABLE IV: User simulation performance comparison across datasets. The best result in each dataset column is highlighted in bold, and the second-best result is underlined.\", \"page\": 7, \"index\": 3, \"width\": 496, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.10029v1/fig-004.webp\", \"caption\": \"Fig. 2: The framework of our CoARS.\", \"page\": 4, \"index\": 4, \"width\": 1022, \"height\": 481}]"
motivation: 现有ARS主要依赖外部文本记忆而非参数化学习，且未能充分利用推荐与用户智能体间的协同演化及交互过程中的细粒度监督。
method: 提出CoARS框架，结合交互奖励机制实现双智能体协同进化，并利用自蒸馏信用分配将历史轨迹转化为Token级的密集信用信号。
result: 实验结果显示，CoARS在多个数据集上的推荐效果和用户对齐表现均显著优于现有的代表性ARS基准方法。
conclusion: 通过强化学习内化交互经验并引入协同演化机制，可以有效解决ARS中监督信号稀疏的问题，提升系统的决策能力。
---

## 摘要
大语言模型赋能的智能体推荐系统（ARS）将推荐重新定义为推荐智能体与用户智能体之间的多轮交互，从而实现了超越传统单次预测的迭代式偏好启发与细化。然而，现有的 ARS 主要在 Reflexion 风格的范式下进行优化，即过去的交互轨迹被存储为文本记忆，并作为提示词上下文被检索以用于后续推理。尽管这种设计允许智能体召回先前的反馈和观察，但积累的经验仍处于模型参数之外，导致智能体依赖于通用推理，而无法通过学习逐步获得特定于推荐的决策能力。因此，强化学习（RL）提供了一种将此类交互经验内化到参数中的自然方式。然而，现有的面向 ARS 的强化学习方法仍面临两个关键局限：首先，它们未能捕捉到 ARS 的交互本质，即推荐智能体和用户智能体持续相互影响，并能通过交互反馈自然地产生内源性监督；其次，它们将丰富的多轮交互过程简化为最终结果，忽略了嵌入在整个轨迹中的密集监督。为此，我们提出了 CoARS，一种用于协同演化智能体推荐系统的自蒸馏强化学习框架。CoARS 引入了两种互补的学习方案：交互奖励，它从同一交互轨迹中为推荐智能体和用户智能体推导出耦合的任务级监督；以及自蒸馏信用分配，它在教师-学生约束下将历史轨迹转化为 Token 级的信用信号。在多个数据集上的实验表明，CoARS 在推荐性能和用户对齐方面优于代表性的 ARS 基准模型。

## Abstract
Large language model-empowered agentic recommender systems (ARS) reformulate recommendation as a multi-turn interaction between a recommender agent and a user agent, enabling iterative preference elicitation and refinement beyond conventional one-shot prediction. However, existing ARS are mainly optimized in a Reflexion-style paradigm, where past interaction trajectories are stored as textual memory and retrieved as prompt context for later reasoning. Although this design allows agents to recall prior feedback and observations, the accumulated experience remains external to model parameters, leaving agents reliant on generic reasoning rather than progressively acquiring recommendation-specific decision-making ability through learning. Reinforcement learning (RL) therefore provides a natural way to internalize such interaction experience into parameters. Yet existing RL methods for ARS still suffer from two key limitations. First, they fail to capture the interactive nature of ARS, in which the recommender agent and the user agent continuously influence each other and can naturally generate endogenous supervision through interaction feedback. Second, they reduce a rich multi-turn interaction process to final outcomes, overlooking the dense supervision embedded throughout the trajectory. To this end, we propose CoARS, a self-distilled reinforcement learning framework for co-evolving agentic recommender systems. CoARS introduces two complementary learning schemes: interaction reward, which derives coupled task-level supervision for the recommender agent and the user agent from the same interaction trajectory, and self-distilled credit assignment, which converts historical trajectories into token-level credit signals under teacher-student conditioning. Experiments on multiple datasets show that CoARS outperforms representative ARS baselines in recommendation performance and user alignment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CoARS** 的新型强化学习框架，旨在解决大语言模型（LLM）驱动的智能体推荐系统（ARS）在经验内化和协同演化方面的不足。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：传统的推荐系统是单次预测，而智能体推荐系统（ARS）通过推荐智能体（RecAgent）与用户智能体（UserAgent）的多轮对话，实现了偏好的动态启发。
*   **核心问题**：
    *   **经验外部化**：现有 ARS 多采用 Reflexion 范式，将交互历史存为外部文本记忆，模型参数本身并未进化，导致其依赖通用推理而非推荐专业决策。
    *   **监督信号稀疏且片面**：现有强化学习（RL）方法多依赖预定义规则或单一的“智能体裁判”，忽略了推荐与用户双向互动的本质，且将丰富的交互轨迹简化为单一的最终奖励（Outcome-level Reward），丢失了过程中的密集监督信号。
*   **目标**：通过强化学习将交互经验内化到模型参数中，实现推荐智能体与用户智能体的协同演化（Co-evolution）。

### 2. 方法论：核心思想与关键技术
CoARS 框架包含两个互补的核心组件，旨在提供任务级和 Token 级的双重监督：

*   **交互奖励（Interaction Reward）—— 任务级监督**：
    *   **RecAgent 奖励**：结合了推荐正确性（是否命中真值）、用户接受强度（点击/点赞等分值）和交互阶段敏感度。公式设计上，越往后的轮次犯错惩罚越大，以鼓励快速准确推荐。
    *   **UserAgent 奖励**：不仅考虑对正确推荐的接受度，还引入了“同伴相似度”（Peer Similarity）。如果用户智能体的决策与具有相似历史的真实用户一致，则给予正向激励，从而提升用户模拟的真实性。
*   **自蒸馏信用分配（Self-Distilled Credit Assignment）—— Token 级监督**：
    *   **核心思想**：利用“事后诸葛亮”的逻辑。将同一智能体分为**教师模式**和**学生模式**。
    *   **技术细节**：教师模式拥有“特权信息”，即能够看到经过诊断和修正后的历史轨迹（包含真值和改进建议）；学生模式仅看到原始上下文。
    *   **信用分配**：通过对比教师和学生在每个 Token 上的对数概率差异，计算 Token 级的诊断优势（Diagnostic Advantage），引导学生模型吸收历史教训，优化推理路径。

### 3. 实验设计
*   **数据集**：使用了三个主流的序列推荐基准：**LastFM**、**MovieLens** 和 **Instruments**（亚马逊乐器类别）。
*   **基准方法（Benchmarks）**：
    *   **记忆类**：Reflexion（单智能体）、AFL（双智能体反馈循环）、iAgent（指令感知智能体）。
    *   **RL 类**：RecoWorld（基于点击反馈作为裁判信号的 RL 框架）。
*   **评估指标**：推荐准确性（Hit@1）和用户模拟保真度（F1 分数）。
*   **模型骨干**：采用了 Qwen3-8B、Qwen3-4B 以及 GPT-4o-mini（仅作为非微调基准）。

### 4. 资源与算力
*   **算力说明**：论文提到采用了基于 **LoRA** 的强化学习微调（Reinforcement Fine-tuning）。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量以及总训练时长。但从其使用的模型规模（4B/8B）和数据集规模（1000 个测试样本）来看，属于中等规模的学术实验配置。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上进行了全量对比实验。
    *   针对 RecAgent 和 UserAgent 分别进行了详尽的**消融实验**（验证协同训练、交互奖励、自蒸馏各自的贡献）。
    *   提供了详细的**案例研究**，展示了教师模式提示词的构建过程。
*   **充分性评价**：实验设计较为全面，涵盖了推荐效果和用户对齐两个维度。通过跨不同规模的开源模型（Qwen 系列）对比，证明了方法的普适性。对比基准包含了最新的智能体推荐框架，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **参数化学习优于记忆检索**：CoARS 显著优于仅依赖文本记忆的方法，证明了将经验内化到参数中的必要性。
*   **协同演化的重要性**：消融实验显示，如果只训练推荐智能体而固定用户智能体（或反之），性能都会下降。双向优化能产生更强的协同效应。
*   **密集监督的优势**：自蒸馏机制提供的 Token 级信用分配比单纯的标量奖励更能有效引导模型改进推理逻辑。

### 7. 优点与亮点
*   **内源性监督**：不需要外部昂贵的标注或额外的裁判模型，直接从交互反馈中提取监督信号。
*   **双智能体对齐**：通过引入同伴相似度，解决了用户智能体在强化学习中容易偏离真实人类行为的问题。
*   **诊断驱动**：自蒸馏机制不仅是简单的模仿，而是基于“诊断”的修正，能够精准识别是推荐错了还是用户反馈错了。

### 8. 不足与局限
*   **计算开销**：虽然使用了 LoRA，但双智能体的强化学习训练（尤其是涉及多轮交互的 Rollout）通常比监督微调（SFT）耗时更长。
*   **冷启动限制**：该方法依赖于一定的初始交互轨迹来构建自蒸馏信号，对于完全没有历史数据的场景可能受限。
*   **评估闭环**：用户模拟的准确性（F1）虽然有所提升，但最终仍是在模拟环境中的评估，与真实线上环境的差距仍需进一步验证。

（完）
