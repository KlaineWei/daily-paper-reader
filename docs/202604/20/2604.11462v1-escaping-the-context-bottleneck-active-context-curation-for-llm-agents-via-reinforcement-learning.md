---
title: "Escaping the Context Bottleneck: Active Context Curation for LLM Agents via Reinforcement Learning"
title_zh: 摆脱上下文瓶颈：通过强化学习为大语言模型智能体实现主动上下文整理
authors: "Xiaozhe Li, Tianyi Lyu, Yizhao Yang, Liang Shan, Siyi Yang, Ligao Zhang, Zhuoyi Huang, Qingwen Liu, Yang Li"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11462v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 通过强化学习训练轻量级策略模型以处理LLM长程任务
tldr: 针对大语言模型在长程任务中面临的“上下文瓶颈”和“信息迷失”问题，本文提出一种共生框架，将上下文管理与任务执行解耦。通过强化学习训练轻量化的ContextCurator模型，主动精简环境噪声并保留关键推理锚点。实验证明该方法在提升WebArena和DeepSearch任务成功率的同时，显著降低了Token消耗，为长程自主智能体提供了高效可扩展的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11462v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of ActiveContext. Top/Center: Standard LLM agent interactions in web and search domains, illustrating the context bottleneck and ”lost-in-the-middle” phenomenon during multi-turn reasoning. Top-Right & Bottom-Left: Our symbiotic architecture, where the ContextCurator actively refines verbose observations into a highfidelity working memory for the TaskExecutor. Bottom-Right: The Multi-turn GRPO pipeline, which rolls out trajectories to collect distal rewards and optimize the context curation policy.\", \"page\": 3, \"index\": 1, \"width\": 819, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2604.11462v1/fig-002.webp\", \"caption\": \"Table 1: Experimental results on the WebArena benchmark. Success rate (SR ↑) and context token consumption (Tokens (K) ↓)1 are reported across 5 domain subsets. To prevent extreme context bloat, the Full Context baseline replaces historical webpage observations with placeholders. The bottom section extensively ablates different context curation methods using gpt-4o-mini as the fixed TaskExecutor. The final average (Overall) is weighted by the number of instances in each subset.\", \"page\": 6, \"index\": 2, \"width\": 818, \"height\": 768}, {\"url\": \"assets/figures/arxiv/2604.11462v1/fig-003.webp\", \"caption\": \"Table 2: Experimental results of ActiveContext on the DeepSearch benchmark. Success rate (SR ↑) and context token consumption (Tokens (K) ↓) are reported across 7 domain subsets for 4 different backbone LLMs. The bottom section extensively ablates different context curation methods using gpt-4o-mini as the fixed TaskExecutor.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 596}]"
motivation: 解决大模型在多轮交互中因冗余信息积累导致的推理能力下降及上下文窗口限制问题。
method: 采用解耦架构，利用强化学习训练轻量级模型ContextCurator来主动筛选和压缩工作记忆，配合冻结的基础模型执行任务。
result: "在WebArena和DeepSearch基准测试中，该方法在提升任务成功率的同时，分别减少了8.8%和高达8倍的Token消耗。"
conclusion: 主动上下文管理是提升长程智能体性能和计算效率的有效范式，且小规模专用模型可达到顶尖模型的管理水平。
---

## 摘要
大语言模型（LLMs）在处理长程任务时面临挑战，这主要归因于“上下文瓶颈”和“迷失在中间（lost-in-the-middle）”现象，即来自冗长环境的累积噪声会损害多轮交互中的推理能力。为了解决这一问题，我们提出了一种将上下文管理与任务执行解耦的共生框架。该架构将一个轻量化的专门策略模型 ContextCurator 与一个强大的冻结基础模型 TaskExecutor 配对。通过强化学习训练，ContextCurator 能够主动降低工作记忆中的信息熵。它在积极剪枝环境噪声的同时，保留了对未来推导至关重要的稀疏数据点，即“推理锚点”。在 WebArena 基准测试中，我们的框架将 Gemini-3.0-flash 的成功率从 36.4% 提升至 41.2%，并将 Token 消耗降低了 8.8%（从 47.4K 降至 43.3K）。在 DeepSearch 上，该框架实现了 57.1% 的成功率（对比 53.9%），同时将 Token 消耗降低了 8 倍。值得注意的是，一个 7B 参数的 ContextCurator 即可匹配 GPT-4o 的上下文管理性能，为自主长程智能体提供了一种可扩展且计算高效的范式。

## Abstract
Large Language Models (LLMs) struggle with long-horizon tasks due to the "context bottleneck" and the "lost-in-the-middle" phenomenon, where accumulated noise from verbose environments degrades reasoning over multi-turn interactions. To address this issue, we introduce a symbiotic framework that decouples context management from task execution. Our architecture pairs a lightweight, specialized policy model, ContextCurator, with a powerful frozen foundation model, TaskExecutor. Trained via reinforcement learning, ContextCurator actively reduces information entropy in the working memory. It aggressively prunes environmental noise while preserving reasoning anchors, that is, sparse data points that are critical for future deductions. On WebArena, our framework improves the success rate of Gemini-3.0-flash from 36.4% to 41.2% while reducing token consumption by 8.8% (from 47.4K to 43.3K). On DeepSearch, it achieves a 57.1% success rate, compared with 53.9%, while reducing token consumption by a factor of 8. Remarkably, a 7B ContextCurator matches the context management performance of GPT-4o, providing a scalable and computationally efficient paradigm for autonomous long-horizon agents.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ActiveContext** 的框架，旨在解决大语言模型（LLM）智能体在处理长程任务时遇到的性能瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管当前 LLM 的上下文窗口不断扩大，但在长程任务（Long-horizon tasks）中，智能体仍面临“上下文瓶颈”和“迷失在中间（Lost-in-the-Middle）”的问题。
*   **核心挑战**：真实环境（如网页 DOM 树或搜索结果）包含超过 90% 的噪声。直接将原始观测数据堆叠到上下文中会稀释模型的注意力，导致信噪比（SNR）骤降，进而引发幻觉和推理失败。
*   **整体含义**：论文认为上下文管理不应是静态的存储或简单的检索，而应是一个**主动的序列决策过程**。

### 2. 方法论
*   **核心思想**：采用“共生解耦架构”，将系统分为两个专门组件：
    1.  **ContextCurator（上下文整理者）**：一个轻量级的策略模型（如 Qwen-7B），负责主动精简、压缩和重写工作记忆。
    2.  **TaskExecutor（任务执行者）**：一个强大的、冻结的基础模型（如 GPT-4o 或 Gemini），负责基于整理后的记忆执行具体任务。
*   **关键技术细节**：
    *   **主动信息熵还原**：Curator 不只是摘要，而是识别并保留“推理锚点（Reasoning Anchors）”——即那些对未来决策至关重要但当前可能看似无关的稀疏数据点。
    *   **Multi-turn GRPO（多轮群组相对策略优化）**：使用强化学习训练 Curator。其核心是将冻结的 TaskExecutor 视为环境的一部分，通过任务最终的成败（稀疏奖励）来指导 Curator 学习什么样的记忆对 Executor 最有效。
    *   **因果依赖链**：Curator 生成记忆 $m_t$ $\rightarrow$ Executor 基于 $m_t$ 执行动作 $a_t$ $\rightarrow$ 环境反馈奖励 $R$。

### 3. 实验设计
*   **数据集/场景**：
    1.  **WebArena**：模拟真实网页环境（购物、论坛、Gitlab 等），侧重于处理复杂的结构化噪声（DOM 树）。
    2.  **DeepSearch**：复杂的 RAG 环境，要求多轮检索和推理，侧重于处理语义噪声（冗余的搜索段落）。
*   **对比方法（Baselines）**：
    *   **基础方案**：No Memory（无记忆）、Full Context（全上下文）。
    *   **先进架构**：Synapse（轨迹示例）、AWM（工作流记忆）、Mem0（被动存储）、Training-Free GRPO 等。
    *   **Curator 消融**：对比了使用不同模型（GPT-4o, Gemini 等）作为 Zero-shot 整理者的表现。

### 4. 资源与算力
*   **算力配置**：使用了 **1 个节点，配备 8 张 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：采用全参数微调（Full-parameter fine-tuning），学习率为 1e-6，Batch Size 为 8。
*   **推理加速**：使用 vLLM 框架进行高效的 RL 轨迹采样（Rollout）。

### 5. 实验数量与充分性
*   **实验规模**：在 WebArena 的 5 个子域和 DeepSearch 的 7 个子域上进行了广泛测试。
*   **充分性评价**：
    *   **跨模型验证**：测试了 4 种不同的 TaskExecutor（Gemini-3.0/2.5, GPT-4o/mini），证明了框架的通用性。
    *   **消融实验**：详细对比了 RL 训练前后的性能差异，以及 7B 小模型与 4o 大模型在管理能力上的对比。
    *   **客观性**：实验不仅关注成功率（SR），还严格统计了 Token 消耗量，从性能和效率两个维度进行了公平对比。

### 6. 主要结论与发现
*   **性能提升**：在 WebArena 上将 Gemini-3.0-flash 的成功率从 36.4% 提升至 41.2%。
*   **效率惊人**：在 DeepSearch 任务中，在提升成功率的同时，实现了 **8 倍的 Token 消耗削减**（从 46.7K 降至 6.6K）。
*   **以小博大**：经过 RL 训练的 **7B 参数 ContextCurator** 在上下文管理任务上的表现，能够匹配甚至超越未经针对性训练的 **GPT-4o**。
*   **关键发现**：智能体的瓶颈不在于上下文窗口的大小，而在于**信噪比**。

### 7. 优点
*   **解耦思想**：将昂贵的推理与低成本的记忆管理分离，既保护了闭源模型的推理能力，又利用了开源模型的可微调性。
*   **RL 驱动的对齐**：通过最终任务奖励来训练记忆管理，避免了人工编写摘要规则的局限性。
*   **显著降本**：大幅减少了发送给闭源 API 的 Token 数量，具有极高的商业实用价值。

### 8. 不足与局限
*   **训练成本**：虽然推理成本降低，但 RL 训练过程（尤其是多轮交互采样）仍需要可观的算力支持。
*   **任务依赖性**：Curator 的策略可能在不同类型的任务间迁移能力有限（例如从网页导航到代码生成可能需要重新训练）。
*   **黑盒依赖**：框架的效果高度依赖于 TaskExecutor 的稳定性，如果底层 API 模型发生剧烈变动，Curator 可能需要重新对齐。

（完）
