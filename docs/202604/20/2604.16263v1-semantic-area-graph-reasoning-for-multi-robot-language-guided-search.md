---
title: Semantic Area Graph Reasoning for Multi-Robot Language-Guided Search
title_zh: 语义区域图推理用于多机器人语言引导搜索
authors: "Ruiyang Wang, Hao-Lun Hsu, Jiwoo Kim, Miroslav Pajic"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16263v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 利用LLM进行多机器人语义搜索的分层框架
tldr: 针对多机器人系统在未知环境中的语义搜索难题，本文提出SAGR框架。该框架通过构建语义区域图，将环境的拓扑结构和语义信息抽象化，使大语言模型能够进行高层级的语义推理和任务分配。实验表明，SAGR在保持探索效率的同时，显著提升了语义目标搜索的成功率和效率，为复杂室内环境下的机器人协作提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16263v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the SAGR framework. The semantic reasoning layer assigns robots to rooms using an LLM operating on the semantic area graph. The frontier selection layer allocates frontier clusters and optimizes waypoint order, while the local execution layer performs navigation and sensing.\", \"page\": 3, \"index\": 1, \"width\": 529, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2604.16263v1/fig-002.webp\", \"caption\": \"Fig. 2. Example semantic area graph constructed from the observed semantic map. Nodes represent discovered room instances and edges represent spatial adjacency. Each node stores room attributes including frontier clusters, robots, and neighboring rooms.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2604.16263v1/fig-003.webp\", \"caption\": \"Fig. 3. Example environment from the HM3D dataset, showing an indoor apartment scene with semantic room layouts used for our search experiments.\", \"page\": 5, \"index\": 3, \"width\": 484, \"height\": 250}]"
motivation: 传统的机器人协作策略侧重于几何覆盖，难以处理需要结合高层语义意图（如根据房间类型寻找特定物体）的复杂搜索任务。
method: 提出一种分层框架，利用大语言模型在增量构建的语义区域图上进行高层房间分配，并结合确定性前沿规划进行局部导航。
result: "在Habitat-Matterport3D数据集的实验中，SAGR在大型环境下的语义目标搜索效率比现有方法提升了高达18.8%。"
conclusion: 结构化的语义抽象是连接大语言模型推理与多机器人协作的有效接口，能显著增强复杂室内环境下的语义搜索能力。
---

## 摘要
协调多机器人系统（MRS）在未知环境中进行搜索，对于需要超越几何探索的语义推理任务而言尤其具有挑战性。经典的协调策略依赖于边界覆盖或信息增益，无法融入高层任务意图，例如搜索与特定房间类型相关的物体。我们提出了“语义区域图推理”（SAGR），这是一个分层框架，通过环境的结构化语义拓扑抽象，使大语言模型（LLM）能够协调多机器人探索和语义搜索。SAGR 从语义占据地图中增量构建语义区域图，将房间实例、连通性、边界可用性以及机器人状态编码为紧凑的任务相关表示，供 LLM 推理使用。LLM 根据空间结构和任务上下文执行高层语义房间分配，而确定性边界规划和局部导航则负责处理分配房间内的几何执行。在 Habitat-Matterport3D 数据集上进行的 100 个场景实验表明，SAGR 在保持与最先进探索方法竞争力的同时，持续提高了语义目标搜索效率，在大型环境中提升高达 18.8%。这些结果突显了结构化语义抽象作为基于 LLM 的推理与复杂室内环境下多机器人协调之间有效接口的价值。

## Abstract
Coordinating multi-robot systems (MRS) to search in unknown environments is particularly challenging for tasks that require semantic reasoning beyond geometric exploration. Classical coordination strategies rely on frontier coverage or information gain and cannot incorporate high-level task intent, such as searching for objects associated with specific room types. We propose \textit{Semantic Area Graph Reasoning} (SAGR), a hierarchical framework that enables Large Language Models (LLMs) to coordinate multi-robot exploration and semantic search through a structured semantic-topological abstraction of the environment. SAGR incrementally constructs a semantic area graph from a semantic occupancy map, encoding room instances, connectivity, frontier availability, and robot states into a compact task-relevant representation for LLM reasoning. The LLM performs high-level semantic room assignment based on spatial structure and task context, while deterministic frontier planning and local navigation handle geometric execution within assigned rooms. Experiments on the Habitat-Matterport3D dataset across 100 scenarios show that SAGR remains competitive with state-of-the-art exploration methods while consistently improving semantic target search efficiency, with up to 18.8\% in large environments. These results highlight the value of structured semantic abstractions as an effective interface between LLM-based reasoning and multi-robot coordination in complex indoor environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Area Graph Reasoning for Multi-Robot Language-Guided Search》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对的是**多机器人系统（MRS）在未知环境中进行语义搜索**的挑战。
*   **研究动机**：传统的机器人协作策略（如基于前沿覆盖或信息增益的方法）主要依赖几何信息，无法理解和利用高层语义意图（例如“去卧室寻找夹克”）。
*   **现有局限**：虽然大语言模型（LLM）具备强大的语义推理能力，但现有方法往往直接将高维视觉观测或原始地图图像输入模型，导致 Prompt 过长、计算成本高昂，难以满足实时协调的需求。
*   **核心目标**：提出一种紧凑的结构化抽象表示，作为 LLM 推理与底层几何执行之间的接口，实现高效、具备任务感知能力的多机器人协作。

### 2. 方法论：SAGR 框架
SAGR（Semantic Area Graph Reasoning）采用分层架构，核心思想是将环境抽象为**语义区域图（Semantic Area Graph）**。

*   **语义区域图构建**：
    *   从语义占据地图中提取“房间实例”作为图节点。
    *   节点属性包含：房间 ID、语义类型（如厨房、卧室）、前沿点（Frontier）统计数据、当前驻守的机器人状态。
    *   边表示房间之间的空间邻接关系。
    *   该图会随着探索过程增量更新，并剔除已完全探索的节点以保持紧凑性。
*   **分层决策流程**：
    1.  **语义推理层（LLM）**：将序列化后的语义区域图、任务说明和历史计划摘要输入 LLM。LLM 负责高层的“房间分配”，决定哪些机器人去哪些房间。
    2.  **前沿选择层（确定性算法）**：在 LLM 指定的房间内，利用匈牙利算法（Hungarian Algorithm）将具体的前沿点分配给机器人，并使用 TSP 求解器优化遍历顺序。
    3.  **局部执行层**：机器人根据分配的路径点进行运动规划和感知。

### 3. 实验设计
*   **数据集与场景**：使用 **Habitat-Matterport3D (HM3D)** 数据集，包含真实的室内公寓布局。
*   **实验规模**：选择了 10 种不同的公寓布局，每种布局生成 10 个随机场景（初始位姿和目标位置随机），共计 **100 个测试场景**。
*   **环境分级**：分为小型（2 台机器人）、中型（3 台机器人）和大型（4 台机器人）环境。
*   **Benchmark（对比方法）**：
    *   **Hungarian Frontier Assignment**：经典的基于欧几里得距离的前沿分配。
    *   **RACER**：基于分层网格和 CVRP 建模的先进协作探索方法。
    *   **AEP + DVC**：结合信息增益采样与动态 Voronoi 分区的策略。

### 4. 资源与算力
*   **模型支持**：实验对比了云端模型（GPT-4o, Claude-Haiku-4.5, Gemini-2.5-Flash-Lite）和本地部署模型。
*   **硬件配置**：本地模型运行在配备 **4 张 NVIDIA A5000 GPU** 的服务器上。
*   **推理效率**：使用语义区域图后，每次协调查询的 Prompt 通常少于 500 tokens。GPT-4o 的平均推理时间约为 **2.5 秒**，能够满足近实时的协调需求。

### 5. 实验数量与充分性
*   **实验组数**：100 组独立场景测试，涵盖了不同规模的环境和机器人数量，具有较好的统计意义。
*   **消融实验**：
    *   **上下文消融**：分别移除邻接信息、计划摘要、目标房间类型，验证了各组件对搜索效率的贡献（移除目标类型导致效率下降 16%）。
    *   **模型消融**：对比了 6 种不同的 LLM（云端 vs 本地），验证了框架的通用性。
*   **客观性**：所有方法在相同的地图表示、感知配置和运动控制下运行，确保了对比的公平性。

### 6. 主要结论与发现
*   **语义搜索效率显著提升**：在大型环境中，SAGR 的搜索效率比 AEP+DVC 提升了 **18.8%**，比 Hungarian 方法提升了 **19.2%**。
*   **探索能力保持竞争力**：在不含语义目标的纯探索任务中，SAGR 的表现与专门优化的几何方法（如 AEP+DVC）相当，证明其高层抽象并未牺牲底层覆盖效率。
*   **动态适应性**：LLM 能够根据新发现的房间类型动态调整策略，从全局探索平滑过渡到针对性搜索。

### 7. 优点
*   **高效的抽象接口**：通过语义区域图将复杂的地图压缩为拓扑节点，极大地降低了 LLM 的推理负担和 Token 消耗。
*   **分层解耦**：将“语义推理”与“几何执行”分离，既发挥了 LLM 的常识推理能力，又保留了传统算法在路径规划上的确定性和高效性。
*   **良好的鲁棒性**：引入“计划摘要”机制，减少了 LLM 在重新规划时的决策震荡，保证了多机器人协作的稳定性。

### 8. 不足与局限
*   **依赖上游感知**：框架假设语义标签（房间类型）由上游系统准确提供，若感知模型出现严重误判，会直接影响 LLM 的推理结果。
*   **环境局限性**：目前主要在室内公寓环境验证，对于语义边界不明显的室外环境或超大型工业场景的适用性有待进一步研究。
*   **通信假设**：实验隐含了机器人之间及与中心化推理单元之间的可靠通信，未深入探讨通信受限环境下的去中心化推理。

（完）
