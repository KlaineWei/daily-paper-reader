---
title: "Escaping the Context Bottleneck: Active Context Curation for LLM Agents via Reinforcement Learning"
title_zh: 摆脱上下文瓶颈：通过强化学习为 LLM 智能体进行主动上下文管理
authors: "Xiaozhe Li, Tianyi Lyu, Yizhao Yang, Liang Shan, Siyi Yang, Ligao Zhang, Zhuoyi Huang, Qingwen Liu, Yang Li"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11462v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 通过强化学习训练轻量化策略模型进行长程上下文管理
tldr: 针对大语言模型在长程任务中面临的上下文瓶颈和信息冗余问题，本文提出了一种将上下文管理与任务执行解耦的共生框架。通过强化学习训练轻量级的 ContextCurator 模型，主动修剪环境噪声并保留关键推理锚点。实验表明，该方法在 WebArena 和 DeepSearch 任务中显著提升了成功率并大幅降低了 Token 消耗，为长程自主智能体提供了一种高效且可扩展的范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11462v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of ActiveContext. Top/Center: Standard LLM agent interactions in web and search domains, illustrating the context bottleneck and ”lost-in-the-middle” phenomenon during multi-turn reasoning. Top-Right & Bottom-Left: Our symbiotic architecture, where the ContextCurator actively refines verbose observations into a highfidelity working memory for the TaskExecutor. Bottom-Right: The Multi-turn GRPO pipeline, which rolls out trajectories to collect distal rewards and optimize the context curation policy.\", \"page\": 3, \"index\": 1, \"width\": 819, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2604.11462v1/fig-002.webp\", \"caption\": \"Table 1: Experimental results on the WebArena benchmark. Success rate (SR ↑) and context token consumption (Tokens (K) ↓)1 are reported across 5 domain subsets. To prevent extreme context bloat, the Full Context baseline replaces historical webpage observations with placeholders. The bottom section extensively ablates different context curation methods using gpt-4o-mini as the fixed TaskExecutor. The final average (Overall) is weighted by the number of instances in each subset.\", \"page\": 6, \"index\": 2, \"width\": 818, \"height\": 768}, {\"url\": \"assets/figures/arxiv/2604.11462v1/fig-003.webp\", \"caption\": \"Table 2: Experimental results of ActiveContext on the DeepSearch benchmark. Success rate (SR ↑) and context token consumption (Tokens (K) ↓) are reported across 7 domain subsets for 4 different backbone LLMs. The bottom section extensively ablates different context curation methods using gpt-4o-mini as the fixed TaskExecutor.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 596}]"
motivation: 解决大模型在多轮交互中因上下文冗余导致的“中间丢失”现象及推理性能下降问题。
method: 采用强化学习训练轻量级模型 ContextCurator，专门负责主动筛选和精简工作记忆中的信息。
result: "在 WebArena 上将成功率提升至 41.2% 并减少 8.8% 的 Token，在 DeepSearch 上成功率达 57.1% 且 Token 消耗降低 8 倍。"
conclusion: 该框架证明了通过专门的上下文管理模型可以有效克服长程任务中的上下文瓶颈，实现性能与效率的双重提升。
---

## 摘要
大语言模型（LLMs）在处理长程任务时面临挑战，这主要归因于“上下文瓶颈”和“迷失在中间”（lost-in-the-middle）现象，即来自冗长环境的累积噪声会降低多轮交互中的推理能力。为了解决这一问题，我们引入了一种共生框架，将上下文管理与任务执行解耦。我们的架构将一个轻量级的专用策略模型 ContextCurator 与一个强大的冻结基础模型 TaskExecutor 配对。通过强化学习训练，ContextCurator 主动降低工作记忆中的信息熵。它在保留推理锚点（即对未来推导至关重要的稀疏数据点）的同时，积极剪枝环境噪声。在 WebArena 上，我们的框架将 Gemini-3.0-flash 的成功率从 36.4% 提高到 41.2%，同时将 Token 消耗降低了 8.8%（从 47.4K 降至 43.3K）。在 DeepSearch 上，它实现了 57.1% 的成功率（对比组为 53.9%），同时将 Token 消耗降低了 8 倍。值得注意的是，一个 7B 参数规模的 ContextCurator 达到了与 GPT-4o 相当的上下文管理性能，为自主长程智能体提供了一种可扩展且计算高效的范式。

## Abstract
Large Language Models (LLMs) struggle with long-horizon tasks due to the "context bottleneck" and the "lost-in-the-middle" phenomenon, where accumulated noise from verbose environments degrades reasoning over multi-turn interactions. To address this issue, we introduce a symbiotic framework that decouples context management from task execution. Our architecture pairs a lightweight, specialized policy model, ContextCurator, with a powerful frozen foundation model, TaskExecutor. Trained via reinforcement learning, ContextCurator actively reduces information entropy in the working memory. It aggressively prunes environmental noise while preserving reasoning anchors, that is, sparse data points that are critical for future deductions. On WebArena, our framework improves the success rate of Gemini-3.0-flash from 36.4% to 41.2% while reducing token consumption by 8.8% (from 47.4K to 43.3K). On DeepSearch, it achieves a 57.1% success rate, compared with 53.9%, while reducing token consumption by a factor of 8. Remarkably, a 7B ContextCurator matches the context management performance of GPT-4o, providing a scalable and computationally efficient paradigm for autonomous long-horizon agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Escaping the Context Bottleneck: Active Context Curation for LLM Agents via Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义
论文针对大语言模型（LLM）智能体在处理**长程任务（Long-horizon tasks）**时面临的“上下文瓶颈”和“迷失在中间（Lost-in-the-Middle）”现象提出了解决方案。
*   **研究动机**：在复杂的网页导航或多轮搜索任务中，环境观察（如冗长的 DOM 树）包含超过 90% 的噪声。直接将这些原始数据喂给模型会导致注意力稀释和推理链条断裂。
*   **核心目标**：通过主动的上下文管理，将环境中的高熵噪声剔除，同时保留对未来决策至关重要的“推理锚点（Reasoning Anchors）”，从而提升智能体的成功率并降低计算成本。

### 2. 方法论：ActiveContext 框架
论文提出了一种**共生架构（Symbiotic Architecture）**，将系统解耦为两个专门的组件：
*   **ContextCurator（上下文策展人）**：一个轻量级的策略模型（如 Qwen-2.5-7B），负责主动生成和更新压缩后的“工作记忆”。它通过重写历史记录来减少信息熵。
*   **TaskExecutor（任务执行者）**：一个强大的、处于冻结状态的基础模型（如 GPT-4o 或 Gemini-3.0-flash），负责基于策展后的记忆执行具体动作。
*   **关键技术细节**：
    *   **多轮 GRPO（Multi-turn Group Relative Policy Optimization）**：使用强化学习训练 ContextCurator。通过采样多条轨迹，根据 TaskExecutor 最终是否成功完成任务（远端奖励）来优化 Curator 的压缩策略。
    *   **因果依赖链**：Curator 的决策直接影响 Executor 的动作，进而决定最终奖励。这种设计让 Curator 学会识别哪些看似无关的信息实际上是后续步骤的关键锚点。

### 3. 实验设计
*   **数据集/场景**：
    1.  **WebArena**：模拟真实网页环境（购物、论坛、Gitlab 等），具有极高的结构噪声。
    2.  **DeepSearch**：复杂的 RAG 环境，涉及多跳检索和推理（如 HotpotQA、2WikiMultiHop）。
*   **Benchmark 与对比方法**：
    *   **基准**：No Memory（无记忆）、Full Context（全上下文拼接）。
    *   **对比方法**：包括被动存储系统（Mem0）、基于工作流的方法（AWM）、轨迹示例方法（Synapse）以及 Training-Free GRPO 等。
    *   **Executor 变量**：测试了 Gemini-3.0-flash、Gemini-2.5-flash、GPT-4o 和 GPT-4o-mini。

### 4. 资源与算力
*   **硬件**：使用了单节点，配备 **8 张 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：采用全参数微调（Full-parameter fine-tuning）。
*   **训练时长**：文中未明确说明具体的训练总时长，但提到了学习率为 1e-6，Batch Size 为 8，且在 RL 阶段使用了 vLLM 加速推理。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 WebArena 的 5 个领域子集和 DeepSearch 的 7 个领域子集。
*   **消融实验**：非常充分。研究者不仅测试了不同规模的后端模型，还专门对比了“提示词驱动（Zero-shot）”与“RL 训练后”的 Curator 性能差异。
*   **客观性**：通过在多个闭源 SOTA 模型上进行交叉验证，证明了该框架的通用性。实验对比了 Token 消耗与成功率两个维度，展示了明显的 Pareto 前沿改进。

### 6. 主要结论与发现
*   **性能提升**：在 WebArena 上，将 Gemini-3.0-flash 的成功率从 36.4% 提升至 41.2%。
*   **效率惊人**：在 DeepSearch 任务中，成功率提升的同时，Token 消耗降低了 **8 倍**（从 46.7K 降至 6.6K）。
*   **以小博大**：经过 RL 训练的 **7B 参数模型**，在上下文管理能力上可以匹配甚至超越 **GPT-4o** 这种千亿级参数模型。
*   **信噪比（SNR）是关键**：实验证明，单纯增加上下文窗口并不能解决问题，提高信息的信噪比才是提升长程推理能力的核心。

### 7. 优点
*   **解耦思想**：将昂贵的推理与低成本的记忆管理分开，既保护了闭源模型的推理能力，又利用了开源模型的可微调性。
*   **RL 驱动的策展**：不同于传统的启发式摘要，RL 让模型学会了保留“隐含的推理锚点”，这些信息在文本相似度上可能不高，但在逻辑链条上不可或缺。
*   **显著的成本效益**：大幅减少了长程任务中的 API 调用成本。

### 8. 不足与局限
*   **领域局限性**：目前主要在网页导航和搜索任务中验证，对于代码生成或长篇创意写作等其他长程任务的适用性尚待观察。
*   **分布偏移风险**：Curator 是针对特定的 Executor 训练的，如果更换 Executor，可能需要重新微调以达到最佳匹配效果。
*   **训练复杂性**：多轮 RL 训练（尤其是涉及环境交互时）通常比监督微调更不稳定且耗时。

（完）
