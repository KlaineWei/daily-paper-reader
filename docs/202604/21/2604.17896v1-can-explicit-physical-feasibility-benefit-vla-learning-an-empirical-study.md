---
title: Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study
title_zh: 显式物理可行性是否能使 VLA 学习受益？一项实证研究
authors: "Yubai Wei, Chen Wu, Hashem Haghbayan"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17896v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 将物理可行性监督集成到VLA策略训练中
tldr: 本研究探讨了显式物理可行性监督对视觉-语言-动作（VLA）模型学习的影响。针对现有VLA模型仅通过模仿学习隐式推断物理约束的问题，本文提出了一种基于几何的可行性目标，并将其集成到扩散式VLA策略的训练中。通过避障操作任务的系统评估，证明了引入显式可行性信号能显著提升模型的物理可靠性、任务成功率及低数据量下的学习效率，为构建更可靠的机器人策略提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17896v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration on learning VLA with explicit physical feasibility supervision. The policy is trained to match demonstrated action chunks via LMSE. We additionally map predicted actions through forward kinematics and compute signed distances to the obstacle, producing a feasibility loss Lgeo. The combined objective is Ltotal = LMSE+λLgeo. Obstacle geometry and kinematic computations are used only during training; at inference the policy acts solely on RGB observations and language.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-002.webp\", \"caption\": \"TABLE III EFFECT OF TRAINING DATA SIZE UNDER LARGE PERTURBATIONS (δ=0.10, λ=1.0). THE BENEFIT OF FEASIBILITY SUPERVISION IS LARGEST IN THE LOW-DATA REGIME AND DIMINISHES AS DATA INCREASES.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-003.webp\", \"caption\": \"TABLE IV ABLATION OF SUPERVISION STRENGTH (δ, λ) UNDER LARGE PERTURBATIONS (40 TRAINING EPISODES). MODERATE SUPERVISION ACHIEVES THE BEST BALANCE IN SSR, WHILE OVERLY STRONG SUPERVISION INTRODUCES A SAFETY–ACCURACY TRADE-OFF.\", \"page\": 7, \"index\": 3, \"width\": 529, \"height\": 271}]"
motivation: 现有的VLA模型主要依赖模仿学习，缺乏对避障和运动学可行性等硬性物理约束的显式监督，导致其难以理解底层几何结构。
method: 在基于扩散模型的VLA策略训练阶段，引入了一个简单的基于几何结构的显式物理可行性监督目标，为策略提供结构化引导。
result: 实验结果显示，增加可行性监督不仅提高了机器人在避障任务中的物理可靠性和整体表现，还显著提升了在数据稀缺情况下的学习效率。
conclusion: 显式物理可行性信号可以有效补充传统的模仿学习，是提升VLA模型可靠性和学习效率的有效途径。
---

## 摘要
视觉-语言-动作（VLA）模型将多模态输入直接映射到机器人动作，通常通过大规模模仿学习进行训练。尽管这一范式表现出了强大的性能，但现有的 VLA 训练过程并未显式地监督硬性物理约束，如避障或运动学可行性。因此，物理可行行为背后的几何结构只能从演示中隐式地推断出来。在本文中，我们研究了引入显式可行性监督是否能为 VLA 策略提供有效的结构化引导。我们制定了一个简单的基于几何的可行性目标，并将其集成到基于扩散的 VLA 策略的训练阶段。为了系统地评估这一想法，我们使用障碍物感知操纵作为依赖几何的物理可行性的受控探针。实证结果表明，通过可行性监督增强 VLA 训练不仅提高了物理可靠性和整体任务性能，还提升了低数据量情况下的学习效率。这些发现表明，显式可行性信号可以有效地补充基于模仿的 VLA 学习，突显了其在开发更可靠的 VLA 策略方面的潜力。

## Abstract
Vision-Language-Action (VLA) models map multimodal inputs directly to robot actions and are typically trained through large-scale imitation learning. While this paradigm has shown strong performance, prevailing VLA training procedures do not explicitly supervise hard physical constraints such as obstacle avoidance or kinematic feasibility. As a result, the geometric structure underlying physically feasible behavior must be inferred only implicitly from demonstrations. In this paper, we study whether introducing explicit feasibility supervision can provide effective structured guidance for VLA policies. We formulate a simple geometry-grounded feasibility objective and integrate it into the training stage of a diffusion-based VLA policy. To evaluate this idea systematically, we use obstacle-aware manipulation as a controlled probe of geometry-dependent physical feasibility. Empirical results show that augmenting VLA training with feasibility supervision improves both physical reliability and overall task performance, while also enhancing learning efficiency in the low-data regime. These findings indicate that explicit feasibility signals can effectively complement imitation-based VLA learning, highlighting their potential for developing more reliable VLA policies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型主要依赖大规模模仿学习（Imitation Learning），将多模态输入直接映射为动作。然而，这种范式缺乏对物理约束（如避障、运动学可行性）的显式监督，模型必须从演示数据中隐式推断几何结构。
*   **核心问题**：在 VLA 训练中引入**显式的物理可行性监督信号**，是否能提供有效的结构化引导，从而提升模型的可靠性和学习效率？

### 2. 方法论
*   **核心思想**：在标准的扩散策略（Diffusion Policy）训练目标中，增加一个基于几何的辅助损失函数（$L_{geo}$），利用训练时可获得的机器人运动学和障碍物几何信息来引导模型。
*   **关键技术细节**：
    *   **前向运动学映射**：将 VLA 预测的动作序列（关节空间）通过前向运动学（FK）映射到工作空间的机器人连杆位姿。
    *   **有向距离场（SDF）计算**：将障碍物建模为导向包围盒（OBB），计算机器人关键点到 OBB 表面的有向距离。
    *   **几何可行性损失（$L_{geo}$）**：采用平方铰链损失（Squared Hinge Loss）。当机器人与障碍物的距离小于预设的安全裕度 $\delta$ 时，激活该损失。
    *   **总目标函数**：$L_{total} = L_{MSE} + \lambda L_{geo}$。其中 $L_{MSE}$ 负责模仿专家动作，$\lambda$ 控制物理监督的强度。
*   **推理阶段**：该方法具有**非对称性**，即物理监督仅在训练时使用。推理时，模型仅依赖 RGB 图像和语言指令，无需障碍物几何信息或 FK 计算。

### 3. 实验设计
*   **场景与任务**：在 NVIDIA Isaac Sim 中构建了一个“近距离避障到达”任务。机器人（Franka 机械臂）需根据指令到达目标物体，同时避开近距离的障碍物。
*   **数据集生成**：通过反事实轨迹生成（Counterfactual Trajectory Generation）构建了 120 个高质量避障场景，包含 3 视角 RGB 图像。
*   **Benchmark 与对比方法**：
    *   **Baseline**：标准的 MSE 模仿学习（基于 RDT-1B 基础模型）。
    *   **对比项**：增加可行性监督的 RDT-1B。
    *   **评估维度**：在“小扰动”（分布内）和“大扰动”（分布外/局部泛化）两种障碍物配置下进行测试。
    *   **核心指标**：安全成功率（SSR），综合考虑了最小间隙（$d_{min}$）和目标距离（$d_{tgt}$）。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A100 GPU**。
*   **训练时长**：全参数微调 RDT-1B（1.2B 参数）。40 个回合（episodes）训练约 8 小时，80 个回合约 15 小时，120 个回合约 23 小时。
*   **模型规模**：基于 12 亿参数的扩散基础模型 RDT-1B。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **数据效率实验**：对比了 40、80、120 个训练回合下的表现。
    *   **消融实验**：针对监督强度（安全裕度 $\delta$ 和损失权重 $\lambda$）进行了多组组合测试。
    *   **鲁棒性测试**：对每个基础场景进行了多次随机扰动和多次 Rollout。
*   **充分性评价**：实验设计较为客观，通过引入 SSR 指标和分布图（KDE）深入分析了行为变化。虽然任务场景相对单一（仅限避障到达），但在该受控变量（Controlled Probe）下，实验论证是充分且公平的。

### 6. 主要结论与发现
*   **性能提升**：显式可行性监督显著提高了安全成功率，且这种提升是双重的——既增加了避障间隙，也提高了到达目标的精度。
*   **低数据量优势**：在数据稀缺（如仅 40 个回合）时，该方法的提升最为显著，甚至能超过使用 120 个回合训练的基准模型，证明了其极高的**数据效率**。
*   **强度平衡**：物理监督并非越强越好。适度的监督能引导学习，而过强的监督（过大的 $\delta$ 或 $\lambda$）会导致模型过于保守，从而损害任务完成度。

### 7. 优点（亮点）
*   **训练-推理非对称设计**：在不增加推理成本和感知需求（如无需在部署时进行复杂的几何标定）的前提下，通过训练时的归纳偏置提升了模型性能。
*   **结构化引导**：证明了物理先验可以作为模仿学习的有效补充，帮助模型建立视觉观察与 3D 物理约束之间的内在联系。
*   **开源基础模型验证**：在 1.2B 参数的大规模 VLA 模型上验证了有效性，具有较强的现实参考价值。

### 8. 不足与局限
*   **任务多样性有限**：实验仅集中在避障到达任务上，尚未在更复杂的长程任务或多物体交互任务中验证。
*   **可行性定义较窄**：目前仅将“可行性”等同于“避障间隙”，未涉及更复杂的动力学约束或自碰撞规避。
*   **几何依赖**：训练阶段仍需已知的障碍物 OBB 信息，这在完全无监督或真实世界杂乱场景的数据收集阶段可能存在挑战。

（完）
