---
title: "SYMBOLIZER: Symbolic Model-free Task Planning with VLMs"
title_zh: SYMBOLIZER：基于视觉语言模型（VLM）的无符号模型任务规划
authors: "Sami Azirar, Zlatan Ajanovic, Hermann Blum"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17830v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 将VLM与经典规划结合用于长程推理
tldr: 针对传统任务规划中符号模型构建困难及大视觉语言模型（VLM）规划能力不足的问题，本文提出SYMBOLIZER框架。该方法仅需谓词描述，利用VLM从图像中提取符号状态，并采用领域无关的启发式搜索进行规划，无需预定义的动作模型。实验证明，该方法在ProDG和ViPlan基准上取得了SOTA结果，显著提升了机器人在复杂环境下的长程推理与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17830v1/fig-001.webp\", \"caption\": \"Fig. 1. SYMBOLIZER grounds visual observations into well-typed symbolic states using structured VLM outputs, enabling domain-independent planning without handcrafted action models.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-002.webp\", \"caption\": \"Fig. 6. Example observations and goals from the VIPLAN benchmark. BLOCKSWORLD uses photorealistic rendering, HOUSEHOLD provides goals as natural language.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-003.webp\", \"caption\": \"Fig. 2. Example of planning with a simulator. From an initial state, the planner expands successor states generated by the simulator, without requiring explicit knowledge of the underlying actions.\", \"page\": 3, \"index\": 3, \"width\": 526, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-004.webp\", \"caption\": \"Fig. 3. Flowchart of grounding from images or text to symbolic states.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 381}]"
motivation: 传统任务规划依赖难以手工构建的符号模型，而现有大视觉语言模型虽具备视觉理解力但规划能力有限。
method: 提出一种无需动作模型的框架，利用VLM将谓词关系从图像映射为符号状态，并结合领域无关的启发式搜索进行长程推理。
result: 在ProDG和ViPlan基准测试中达到了最先进的性能，优于直接使用VLM规划或仅将VLM作为启发式的方法。
conclusion: 结合VLM的视觉感知与经典启发式搜索，可以在无需预定义动作模型的情况下有效解决跨领域的复杂组合规划问题。
---

## 摘要
传统的任务与运动规划（TAMP）系统依赖于用于运动规划的物理模型和用于任务规划的离散符号模型。尽管物理模型通常是现成的，但符号模型（由符号状态解释和动作模型组成）必须经过精心的人工设计或从标注数据中学习。这一过程既耗费资源，又将解决方案限制在特定领域，从而限制了可扩展性和适应性。另一方面，视觉语言模型（VLM）由于在异构数据上进行了广泛训练，展现出了理想的零样本视觉理解能力，但其规划能力仍然有限。因此，将 VLM 与经典规划相结合，用于 TAMP 问题中的长程推理，具有巨大的潜力。该方向的近期工作仍缺乏通用性，且依赖于手工设计的、特定于任务的解决方案，例如预先描述所有可能的物体，或使用符号动作模型。我们提出了一个能够很好地泛化到未见问题实例的框架。该方法仅需要描述物体间关系的提升谓词（lifted predicates），并利用 VLM 将其从图像中锚定（grounding）以获取符号状态。规划是通过使用目标计数（goal-count）和基于宽度的启发式算法（width-based heuristics）的领域无关启发式搜索完成的，无需动作模型。在 VLM 锚定的状态空间上进行的符号搜索优于直接基于 VLM 的规划，且与使用 VLM 衍生启发式的方法表现相当。这表明领域无关搜索可以有效解决具有大规模组合状态空间的跨领域问题。我们对该方法进行了广泛评估，并在 ProDG 和 ViPlan 基准测试中取得了最先进的结果。

## Abstract
Traditional Task and Motion Planning (TAMP) systems depend on physics models for motion planning and discrete symbolic models for task planning. Although physics model are often available, symbolic models (consisting of symbolic state interpretation and action models) must be meticulously handcrafted or learned from labeled data. This process is both resource-intensive and constrains the solution to the specific domain, limiting scalability and adaptability. On the other hand, Visual Language Models (VLMs) show desirable zero-shot visual understanding (due to their extensive training on heterogeneous data), but still achieve limited planning capabilities. Therefore, integrating VLMs with classical planning for long-horizon reasoning in TAMP problems offers high potential. Recent works in this direction still lack generality and depend on handcrafted, task-specific solutions, e.g. describing all possible objects in advance, or using symbolic action models. We propose a framework that generalizes well to unseen problem instances. The method requires only lifted predicates describing relations among objects and uses VLMs to ground them from images to obtain the symbolic state. Planning is performed with domain-independent heuristic search using goal-count and width-based heuristics, without need for action models. Symbolic search over VLM-grounded state-space outperforms direct VLM-based planning and performs on par with approaches that use a VLM-derived heuristic. This shows that domain-independent search can effectively solve problems across domains with large combinatorial state spaces. We extensively evaluate on extensively evaluate our method and achieve state-of-the-art results on the ProDG and ViPlan benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《SYMBOLIZER: Symbolic Model-free Task Planning with VLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了机器人**任务与运动规划（TAMP）**中的一个核心矛盾：
*   **传统方法局限性**：依赖于手工设计的符号模型（如 PDDL 动作模型和谓词定义），这需要极高的领域专家知识，且难以泛化到新环境。
*   **VLM 的局限性**：虽然视觉语言模型（VLM）具备强大的零样本感知能力，但在处理长程（Long-horizon）推理和复杂组合规划时，容易产生幻觉且缺乏逻辑严密性。
*   **核心目标**：提出一种名为 **SYMBOLIZER** 的框架，旨在利用 VLM 的感知能力将视觉观测转化为符号状态，同时利用经典规划算法的搜索能力解决复杂任务，且**无需预先定义动作模型**。

### 2. 方法论：核心思想与关键技术
SYMBOLIZER 的核心思想是“感知归 VLM，推理归搜索”，具体流程如下：
*   **结构化符号锚定（Structured Grounding）**：
    *   不直接让 VLM 输出自由文本，而是通过 Python 类定义（Class Definitions）约束 VLM 的输出格式。
    *   **对象提取**：首先从图像或文本中识别物体及其类型。
    *   **谓词推理**：基于提取的对象，利用 VLM 推理物体间的关系（如 `on(red_block, blue_block)`）。通过类型检查和受限解码，确保生成的符号状态在逻辑上是“良构”的（Well-formed）。
*   **无模型规划（Model-free Planning）**：
    *   **黑盒转换**：将物理模拟器（Simulator）视为状态转移函数。给定当前状态和动作，模拟器返回下一个状态。
    *   **启发式搜索**：使用领域无关的搜索算法（如 A* 或宽度优先搜索 WBS）。主要启发式函数是**目标计数（Goal-count）**，即当前状态与目标状态之间未满足的谓词数量。
    *   **宽度搜索（Width-based Search）**：在目标计数无法提供足够引导时，通过探索具有新颖谓词组合的状态来打破僵局。

### 3. 实验设计
*   **数据集与场景**：
    *   **自定义域**：包括 Blocksworld（积木世界）、Hanoi（汉诺塔）、Hanoi Color、Kitchen-Worlds。涵盖了 2D 渲染（PDDLGym）、3D 物理渲染（PyBullet）以及真实图像。
    *   **外部基准**：**ProDG**（用于评估符号锚定质量）和 **ViPlan**（用于评估端到端规划性能）。
*   **对比方法**：
    *   **ViLaIn**：现有的基于 VLM 的 PDDL 生成框架。
    *   **Direct VLM Planning**：直接让 VLM 输出动作序列。
    *   **VLM-guided Search**：将 VLM 作为搜索过程中的启发式评估器。
    *   **Dream-VL**：结合 VLM 锚定与学习型世界模型的规划方法。

### 4. 资源与算力
*   **模型使用**：实验使用了多种 VLM，包括 **Gemini 1.5 Pro**、**Gemini 1.5 Flash Lite** 和开源模型 **Mistral Small 2503**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法主要依赖于预训练模型的推理（In-context Learning）而非重新训练，其算力消耗主要集中在 API 调用或本地模型推理上。

### 5. 实验数量与充分性
*   **实验规模**：作者在 7 个不同的领域（跨越 2D、3D 和真实场景）进行了广泛测试。
*   **消融实验**：对比了不同规模的模型（Pro vs. Flash vs. Small）、不同规划策略（直接规划 vs. 符号搜索）以及有无 VLM 启发式的效果。
*   **充分性评价**：实验设计非常充分且客观。通过在外部基准（ProDG, ViPlan）上进行对比，证明了该方法在不同视觉复杂度和逻辑难度下的鲁棒性。特别是对“数据泄露”风险进行了说明（Gemini 的知识截止日期早于基准发布）。

### 6. 主要结论与发现
*   **搜索胜过直觉**：在 VLM 锚定的状态空间上进行系统搜索，其成功率远高于 VLM 直接生成计划。
*   **结构化约束是关键**：通过 Python 类约束 VLM 输出，可以有效消除幻觉和格式错误，这是实现长程规划的前提。
*   **领域无关性**：SYMBOLIZER 无需针对特定任务进行 Prompt 工程或动作模型建模，展现了极强的泛化能力。
*   **SOTA 性能**：在 ViPlan 基准上，即使使用轻量级的 Gemini Flash 模型，其表现也优于使用 GPT-4 的现有最强基准。

### 7. 优点
*   **通用性强**：不需要手工编写 PDDL 动作模型，仅需谓词定义即可跨领域运行。
*   **鲁棒性高**：通过结构化输出解决了 VLM 常见的解析错误问题。
*   **效率平衡**：将复杂的逻辑推理交给成熟的搜索算法，而将视觉理解交给 VLM，充分发挥了各自的长处。

### 8. 不足与局限
*   **静态谓词瓶颈**：在某些领域（如汉诺塔），某些不随动作变化的静态谓词（如盘子大小关系）较难仅通过少量示例让 VLM 准确捕获。
*   **感知误差传递**：虽然搜索可以容忍部分错误，但如果初始状态或目标的视觉锚定出现根本性错误，规划仍会失败。
*   **依赖模拟器**：该方法目前依赖于一个可以作为“黑盒”调用的模拟器来进行状态转移预测，在纯现实世界（无模拟器）中的应用仍需进一步探索（如学习动作模型）。

（完）
