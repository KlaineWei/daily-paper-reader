---
title: "SYMBOLIZER: Symbolic Model-free Task Planning with VLMs"
title_zh: SYMBOLIZER：基于视觉语言模型（VLM）的符号化无模型任务规划
authors: "Sami Azirar, Zlatan Ajanovic, Hermann Blum"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17830v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 将VLM与经典规划结合，用于TAMP中的长程推理
tldr: 针对传统任务规划依赖手工符号模型且VLM规划能力有限的问题，本文提出SYMBOLIZER框架。该方法利用VLM将图像转化为基于谓词的符号状态，并结合领域无关的启发式搜索进行长程推理，无需预定义的动作模型。实验表明，该方法在ProDG和ViPlan基准测试中达到了SOTA水平，展现了极强的泛化能力和在复杂状态空间下的搜索效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17830v1/fig-001.webp\", \"caption\": \"Fig. 1. SYMBOLIZER grounds visual observations into well-typed symbolic states using structured VLM outputs, enabling domain-independent planning without handcrafted action models.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-002.webp\", \"caption\": \"Fig. 6. Example observations and goals from the VIPLAN benchmark. BLOCKSWORLD uses photorealistic rendering, HOUSEHOLD provides goals as natural language.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-003.webp\", \"caption\": \"Fig. 2. Example of planning with a simulator. From an initial state, the planner expands successor states generated by the simulator, without requiring explicit knowledge of the underlying actions.\", \"page\": 3, \"index\": 3, \"width\": 526, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-004.webp\", \"caption\": \"Fig. 3. Flowchart of grounding from images or text to symbolic states.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 381}]"
motivation: 传统任务规划依赖难以获取的手工符号模型，而现有结合VLM的方法往往缺乏通用性且依赖特定任务的动作定义。
method: 利用VLM将视觉信息映射为基于提升谓词的符号状态，并采用领域无关的启发式搜索算法在无动作模型的情况下进行任务规划。
result: 在ProDG和ViPlan基准测试中取得了最先进的性能，证明了符号搜索在处理大规模组合状态空间时优于直接使用VLM规划。
conclusion: 结合VLM的视觉理解能力与经典启发式搜索，可以在无需手工动作模型的情况下实现高效且通用的长程任务规划。
---

## 摘要
传统的任务与运动规划（TAMP）系统依赖于用于运动规划的物理模型和用于任务规划的离散符号模型。尽管物理模型通常是现成的，但符号模型（由符号状态解释和动作模型组成）必须经过精心的人工设计或从标注数据中学习。这一过程既耗费资源，又将解决方案限制在特定领域，从而限制了可扩展性和适应性。另一方面，视觉语言模型（VLM）由于在异构数据上进行了广泛训练，展现出理想的零样本视觉理解能力，但在规划能力方面仍然有限。因此，将 VLM 与经典规划相结合，用于 TAMP 问题中的长程推理，具有巨大的潜力。该方向近期的研究仍缺乏通用性，且依赖于手工设计的特定任务解决方案，例如预先描述所有可能的物体，或使用符号动作模型。我们提出了一个能够很好地泛化到未知问题实例的框架。该方法仅需要描述物体间关系的提升谓词（lifted predicates），并利用 VLM 将其从图像中锚定（grounding）以获取符号状态。规划是通过使用目标计数（goal-count）和基于宽度的启发式搜索（width-based heuristics）的领域无关启发式搜索完成的，无需动作模型。在基于 VLM 锚定的状态空间上进行的符号搜索优于直接基于 VLM 的规划，且与使用 VLM 衍生启发式的方法表现相当。这表明领域无关搜索可以有效解决具有大规模组合状态空间的跨领域问题。我们对该方法进行了广泛评估，并在 ProDG 和 ViPlan 基准测试中取得了最先进的结果。

## Abstract
Traditional Task and Motion Planning (TAMP) systems depend on physics models for motion planning and discrete symbolic models for task planning. Although physics model are often available, symbolic models (consisting of symbolic state interpretation and action models) must be meticulously handcrafted or learned from labeled data. This process is both resource-intensive and constrains the solution to the specific domain, limiting scalability and adaptability. On the other hand, Visual Language Models (VLMs) show desirable zero-shot visual understanding (due to their extensive training on heterogeneous data), but still achieve limited planning capabilities. Therefore, integrating VLMs with classical planning for long-horizon reasoning in TAMP problems offers high potential. Recent works in this direction still lack generality and depend on handcrafted, task-specific solutions, e.g. describing all possible objects in advance, or using symbolic action models. We propose a framework that generalizes well to unseen problem instances. The method requires only lifted predicates describing relations among objects and uses VLMs to ground them from images to obtain the symbolic state. Planning is performed with domain-independent heuristic search using goal-count and width-based heuristics, without need for action models. Symbolic search over VLM-grounded state-space outperforms direct VLM-based planning and performs on par with approaches that use a VLM-derived heuristic. This shows that domain-independent search can effectively solve problems across domains with large combinatorial state spaces. We extensively evaluate on extensively evaluate our method and achieve state-of-the-art results on the ProDG and ViPlan benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《SYMBOLIZER: Symbolic Model-free Task Planning with VLMs》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了机器人**任务与运动规划（TAMP）**中的核心矛盾：
*   **传统 TAMP 的局限性**：高度依赖手工设计的符号模型（如 PDDL 动作定义和谓词解释），这需要大量专家知识，且难以泛化到新环境。
*   **视觉语言模型（VLM）的现状**：VLM 具备极强的零样本视觉理解能力，但在处理长程逻辑推理和复杂组合规划时表现欠缺，且容易产生幻觉或格式错误。
*   **研究动机**：开发一个通用的框架，利用 VLM 的感知能力将原始观测转化为符号状态，并结合经典的启发式搜索算法进行规划，从而**摆脱对预定义符号动作模型的依赖**。

### 2. 方法论：核心思想与关键技术
**SYMBOLIZER** 框架的核心思想是将感知（VLM）与推理（经典搜索）解耦。

*   **结构化符号锚定（Structured Grounding）**：
    *   为了避免 VLM 输出的随意性，研究者定义了基于 Python 类的**结构化输出约束**。
    *   流程：首先从图像/文本中提取物体（Object），然后根据预定义的“提升谓词”（Lifted Predicates）类型签名，约束 VLM 仅能生成合法的谓词实例。
    *   这种方法确保了生成的符号状态在语法和逻辑类型上是完全正确的，消除了幻觉导致的解析错误。
*   **无模型规划（Model-free Planning）**：
    *   该框架不要求提供 PDDL 动作模型（即不需要知道每个动作的前置条件和效果）。
    *   它将**物理模拟器视为黑盒转移模型**：给定当前状态和候选动作，由模拟器计算后继状态。
*   **启发式搜索算法**：
    *   采用领域无关的搜索策略。主要启发式函数是**目标计数（Goal-count）**，即当前状态与目标状态之间未满足的谓词数量。
    *   引入**基于宽度的搜索（Width-based Search）**作为平局打破机制，通过探索具有新颖谓词组合的状态来增强搜索效率。

### 3. 实验设计
研究者在多个维度上进行了详尽的评估：
*   **数据集与场景**：
    *   **自定义领域**：包括 Blocksworld（积木世界）、Hanoi（汉诺塔）、Kitchen-Worlds（厨房世界）。涵盖了 2D 渲染（PDDLGym）、3D 物理渲染（PyBullet）以及真实图像。
    *   **外部基准测试**：**ProDG**（包含烹饪和汉诺塔的真实图像场景）和 **ViPlan**（光照真实的积木世界和具有部分可观测性的家庭任务）。
*   **对比方法**：
    *   **ViLaIn**：现有的基于 VLM 生成 PDDL 问题的 SOTA 方法。
    *   **ViPlan-Planner/Grounder**：直接使用 VLM 作为规划器或验证器的基准。
    *   **Dream-VL**：结合 VLM 锚定与学习型世界模型的规划方法。
    *   **消融实验**：对比了“直接 VLM 规划”和“VLM 引导的启发式搜索”。

### 4. 资源与算力
*   **模型使用**：实验使用了多种 VLM 骨干网络，包括 **Gemini 3.1 Pro**、**Gemini 3.1 Flash Lite**（轻量化模型）和 **Mistral Small 2503**（开源模型）。
*   **算力说明**：文中**未明确提及**具体的 GPU 硬件型号、数量或训练时长。由于该方法主要依赖于预训练模型的推理（In-context Learning）和模拟器搜索，而非大规模端到端训练，其算力消耗主要集中在 VLM 的 API 调用和模拟器的并行步进上。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了从简单 2D 到复杂 3D 乃至真实图像的 7 个以上不同领域，每个领域包含数十至上百个测试实例。
*   **充分性**：实验设计非常充分。不仅评估了最终的规划成功率，还深入分析了物体提取、谓词识别和目标识别的 F1 分数。
*   **客观性**：通过引入不同参数规模的模型（从 Pro 到 Flash）以及开源模型作为对比，证明了该框架的有效性并非仅依赖于最强的闭源模型。同时，在 ViPlan 等第三方基准上的对比遵循了严格的测试协议。

### 6. 主要结论与发现
*   **符号搜索的优越性**：在 VLM 锚定的状态空间上进行符号搜索，其表现远优于直接让 VLM 输出规划序列。
*   **结构化约束的重要性**：通过 Python 类约束 VLM 输出，可以显著提升感知准确度，甚至使轻量级模型（如 Flash）在某些任务上超越未受约束的强力模型（如 GPT-4）。
*   **领域无关性**：SYMBOLIZER 在未经过任何领域特定调优的情况下，在 ProDG 和 ViPlan 上均达到了 SOTA 水平，证明了其极强的泛化能力。
*   **瓶颈所在**：实验发现“目标锚定”（从文本指令理解目标符号状态）目前是系统的主要瓶颈，尤其是在抽象逻辑较强的任务中。

### 7. 优点（亮点）
*   **无需动作模型**：打破了传统 TAMP 对精确符号动力学模型的依赖，极大降低了部署成本。
*   **高可靠性**：利用结构化输出技术解决了 VLM 在符号推理中的“格式崩溃”和“幻觉”问题。
*   **即插即用**：能够直接应用于不同的模拟器和视觉环境，展现了优秀的零样本迁移能力。

### 8. 不足与局限
*   **依赖模拟器**：虽然不需要符号动作模型，但仍需要一个可用的物理模拟器作为转移模型，这在某些复杂的现实场景中可能难以获取。
*   **静态谓词挑战**：对于一些无法通过单张图像直观判断的静态属性（如汉诺塔中盘子的大小关系），VLM 的推理仍存在不稳定性。
*   **计算开销**：在搜索过程中，如果状态空间极大，频繁调用 VLM 进行状态验证或模拟器步进可能会产生较高的计算延迟或 API 成本。

（完）
