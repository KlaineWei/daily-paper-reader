---
title: Self-Guided Plan Extraction for Instruction-Following Tasks with Goal-Conditional Reinforcement Learning
title_zh: 基于目标条件强化学习的指令遵循任务自引导计划提取
authors: "Zoya Volovikova, Nikita Sorokin, Dmitriy Lukashevskiy, Aleksandr Panov, Alexey Skrynnik"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20601v1"
tags: ["query:slm-rl"]
score: 9.0
evidence: 用于长程指令遵循任务的目标条件强化学习
tldr: 本文提出SuperIgor框架，旨在解决指令遵循任务中依赖预定义子任务的问题。该框架利用大语言模型自主生成并优化高层规划，通过目标条件强化学习与语言模型的迭代协同训练，使智能体能根据执行反馈动态调整策略。实验表明，该方法在复杂随机环境中显著提升了指令遵循的准确性与对新指令的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20601v1/fig-001.webp\", \"caption\": \"Figure 1: Conceptual diagram of the SuperIgor framework designed for Instruction Following\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2604.20601v1/fig-002.webp\", \"caption\": \"Figure 8: Example of instructions and corresponding plans\", \"page\": 15, \"index\": 2, \"width\": 456, \"height\": 748}, {\"url\": \"assets/figures/arxiv/2604.20601v1/fig-003.webp\", \"caption\": \"Figure 7: Coverage of reference subtasks (out of 23) across different LLMs. Higher is better.\", \"page\": 15, \"index\": 3, \"width\": 461, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2604.20601v1/fig-004.webp\", \"caption\": \"Figure 3: Comparison of SuperIgor and baseline performance on CrafText tasks (Atomic / Combo / New Objects / Paraphrases). All agents were evaluated at 2.5 billion steps (corresponding to the first cycle in the SuperIgor approach) and 5 billion steps (corresponding to the second cycle).\", \"page\": 7, \"index\": 4, \"width\": 941, \"height\": 568}, {\"url\": \"assets/figures/arxiv/2604.20601v1/fig-005.webp\", \"caption\": \"Figure 5: Training plans generation\", \"page\": 13, \"index\": 5, \"width\": 854, \"height\": 835}, {\"url\": \"assets/figures/arxiv/2604.20601v1/fig-006.webp\", \"caption\": \"Figure 2: Super Igor Pipeline: The pipeline consists of four stages: (1) a language model generates multiple plan options for a given instruction; (2) a policy model is trained via PPO to execute each plan in the environment; (3) each plan is validated by measuring its execution success rate; (4) the language model is optimized using Direct Preference Optimization (DPO)(Rafailov et al., 2023) based on plan performance scores. This iterative loop refines both plan generation and execution.\", \"page\": 4, \"index\": 6, \"width\": 854, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2604.20601v1/fig-007.webp\", \"caption\": \"Figure 10: Example of how the agent follows the plan and chooses actions.\", \"page\": 19, \"index\": 7, \"width\": 854, \"height\": 619}, {\"url\": \"assets/figures/arxiv/2604.20601v1/fig-008.webp\", \"caption\": \"Figure 9: Example of DPO plan reprioritization for the instruction:\\\"Forge a stone pickaxe and mine coal\\\"\", \"page\": 17, \"index\": 8, \"width\": 461, \"height\": 366}]"
motivation: 传统方法依赖人工预定义的子任务和大量标注数据，难以适应具有复杂动态和随机性的实际任务环境。
method: 通过迭代协同训练机制，让强化学习智能体学习执行规划，同时语言模型根据执行反馈和偏好不断精炼高层规划。
result: SuperIgor在动态环境中表现出比基准方法更严格的指令遵循能力，并对未见过的指令展现出强大的泛化性能。
conclusion: 结合语言模型的自我引导规划与强化学习的闭环反馈，是提升智能体自主处理复杂指令遵循任务的有效途径。
---

## 摘要
我们介绍了 SuperIgor，这是一个用于指令遵循任务的框架。与以往依赖预定义子任务的方法不同，SuperIgor 使语言模型能够通过自学习机制生成并优化高层计划，从而减少了对人工数据集标注的需求。我们的方法涉及迭代协同训练：训练强化学习（RL）智能体以遵循生成的计划，同时语言模型根据 RL 的反馈和偏好调整并修改这些计划。这形成了一个反馈循环，使得智能体和规划器能够共同提升。我们在具有丰富动力学特性和随机性的环境中验证了我们的框架。结果表明，SuperIgor 智能体比基准方法更严格地遵循指令，同时也展现出对先前未见指令的强大泛化能力。

## Abstract
We introduce SuperIgor, a framework for instruction-following tasks. Unlike prior methods that rely on predefined subtasks, SuperIgor enables a language model to generate and refine high-level plans through a self-learning mechanism, reducing the need for manual dataset annotation. Our approach involves iterative co-training: an RL agent is trained to follow the generated plans, while the language model adapts and modifies these plans based on RL feedback and preferences. This creates a feedback loop where both the agent and the planner improve jointly. We validate our framework in environments with rich dynamics and stochasticity. Results show that SuperIgor agents adhere to instructions more strictly than baseline methods, while also demonstrating strong generalization to previously unseen instructions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Self-Guided Plan Extraction for Instruction-Following Tasks with Goal-Conditional Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了 AI 智能体如何理解并执行自然语言指令。**核心挑战**在于：
*   **稀疏奖励与长程任务**：在复杂环境中，智能体往往只能在完成整个任务后获得奖励，导致学习效率极低。
*   **对预定义技能的依赖**：现有方法通常假设存在一个预定义的子任务库（Subgoal Library），但在开放世界或新环境中，这些子任务往往是未知的。
*   **标注成本**：依赖专家演示（Demonstrations）的方法难以大规模扩展。
**研究动机**：提出一种无需人工预定义子任务、无需专家演示，通过大语言模型（LLM）与强化学习（RL）智能体“自我引导”迭代进化的框架。

### 2. 方法论：SuperIgor 框架
SuperIgor 的核心思想是**迭代协同训练（Co-training）**，主要包含四个阶段：
*   **自引导计划提取（Plan Generation）**：
    *   利用 LLM 从指令集中提取潜在子任务，构建“子任务库”。
    *   通过 LLM 诱导子任务之间的依赖关系，生成**本体图（Ontology Graph）**。
    *   基于本体图将复杂指令展开为多个候选的线性计划。
*   **技能课程学习（Skill Curriculum Learning）**：
    *   为了解决稀疏奖励问题，算法从单步子任务开始训练，只有当智能体对某子任务的成功率达到阈值（如 0.7）时，才将其标记为“掌握”，并逐步引入包含该子任务的更复杂计划。
*   **计划验证与偏好收集**：
    *   RL 智能体在环境中执行生成的计划，记录每个计划的成功率。
*   **LLM 迭代优化（DPO Fine-tuning）**：
    *   使用**直接偏好优化（DPO）**对 LLM 进行微调。将执行成功率高的计划作为正样本，成功率低的作为负样本，使 LLM 学习生成更符合环境动力学和智能体能力的计划。

### 3. 实验设计
*   **实验环境**：**CrafText** 平台（一个类似 Minecraft 的多模态、动态、部分可观测的开放世界环境）。
*   **数据集**：**FOCUS** 数据集（包含 900+ 指令，1500+ 词汇），分为 Atomic（原子任务）、Combo（组合任务）、Paraphrases（同义改写）和 New Objects（新对象组合）。
*   **对比基准（Baselines）**：
    *   **PPO-T**：文本增强的 PPO（直接输入指令）。
    *   **PPO-T+**：使用 GPT-4 预生成计划的 PPO。
    *   **FiLM**：使用特征线性调制层融合视听特征的方法。
    *   **Oracle**：使用人工编写的完美计划进行训练的上限模型。

### 4. 资源与算力
*   **硬件**：使用配备 **NVIDIA A100 (80GB)** GPU 的高性能计算集群。
*   **算力消耗**：
    *   **RL 训练**：每个配置训练 50 亿步（5B steps），耗时约 **120 GPU 小时**。
    *   **LLM 微调**：使用 Qwen2.5-14B 模型，一轮 DPO 微调约需 **15 GPU 小时**。
    *   **总计**：SuperIgor 完整流程（2 个周期）约需 **152 GPU 小时**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括 10 组完整的训练运行。
*   **消融实验**：针对本体图生成、课程学习、DPO 优化、SFT 预训练等关键组件进行了详细的消融分析。
*   **模型对比**：对比了不同规模和家族的 LLM（Qwen, Gemma, Phi-4, NextCoder 等）作为规划器的表现。
*   **充分性评价**：实验设计较为**充分且客观**。通过设置 Oracle（先验知识上限）和多种 Baseline，并引入 OOD（分布外）测试集（新对象组合），有效地验证了系统的泛化能力和鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：SuperIgor 在原子任务和组合任务上的成功率远超 PPO-T 等基准方法（如原子任务从 0.19 提升至 0.44）。
*   **泛化性强**：在面对未见过的对象组合（New Objects）时，SuperIgor 的表现比 Oracle 模型更稳定，因为它在训练中接触了更多样化的候选计划。
*   **协同效应**：课程学习必须配合高质量的本体计划才能生效；DPO 微调能显著加速第二阶段的策略提升。
*   **LLM 差异**：并非越大的 LLM 效果越好，Qwen 系列在生成结构化计划方面表现出更好的稳定性。

### 7. 优点
*   **闭环自进化**：实现了“LLM 规划 -> RL 执行 -> 反馈回传 -> LLM 优化”的完整闭环，减少了人工干预。
*   **无需预定义技能**：通过本体诱导技术，自动从文本中发现环境的逻辑结构。
*   **课程学习机制**：有效地缓解了长程任务中的稀疏奖励和信用分配问题。

### 8. 不足与局限
*   **初始计划依赖**：系统高度依赖 LLM 初始生成的本体质量。如果 LLM 初始诱导的逻辑存在循环依赖或严重错误，后续训练可能陷入局部最优。
*   **计算成本**：尽管比纯演示方法更具扩展性，但 50 亿步的 RL 训练和多轮 LLM 微调仍需要巨大的算力支持。
*   **环境限制**：目前仅在 CrafText（类网格世界）中验证，在更复杂的 3D 物理仿真环境（如真实机器人环境）中的表现尚待观察。
*   **可解释性**：当训练停滞时，难以精确区分是 RL 探索不足还是 LLM 计划空间设计不合理导致的。

（完）
