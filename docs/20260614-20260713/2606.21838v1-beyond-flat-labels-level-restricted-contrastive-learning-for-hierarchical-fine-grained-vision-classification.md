---
title: "Beyond Flat Labels: Level-Restricted Contrastive Learning for Hierarchical Fine-Grained Vision Classification"
title_zh: 超越扁平标签：用于分层细粒度视觉分类的层级受限对比学习
authors: "Zhiyuan Tao, Srikumar Sastry, Matthew J Thompson, Elizabeth G Campolongo, Net Zhang, Ziheng Zhang, Hilmar Lapp, Yu Su, Tanya Berger-Wolf, Nathan Jacobs, Wei-Lun Chao, Jianyang Gu"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21838v1"
tags: ["query:contrastive"]
score: 9.0
evidence: 层次限制对比学习解决层次分类中的假阴性问题
tldr: "在多模态对比学习中，层次化标签空间常导致预测在不同粒度上不一致，如细粒度预测与粗粒度标签矛盾。本文提出级别限制对比学习，将对比限制在同一分类级别内，并采用组平衡设计确保各层次充分优化。基于BioCLIP在TreeOfLife-10M训练，在iNat21上平均准确率提升30.47%，显著改善层次一致性。该方法有效解决了层次零样本分类中的跨级别预测矛盾，提升了从粗到细的分类准确性与一致性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21838v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 626, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21838v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 822, \"height\": 448, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21838v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1456, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21838v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 338, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21838v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1296, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21838v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 866, \"height\": 284, \"label\": \"Table\"}]"
motivation: 现有方法因跨级别误标负样本导致预测层次不一致。
method: 将对比限制在同一分类级别内，并采用组平衡设计确保各层次优化充分。
result: "在iNat21上平均准确率提升30.47%，层次一致性显著改善。"
conclusion: 提出级别限制对比学习，有效提升层次零样本分类的准确性与一致性。
---

## 摘要
多模态对比学习通过将图像与文本类别对齐，实现了零样本视觉分类。然而，在分层结构的标签空间中，现有方法经常产生在分类层级间不一致的预测。例如，模型可能预测一个细粒度类别，但其父类别与同时预测的更高级别标签相矛盾。分析表明，问题源于对比比较涉及多个分类层级时出现的假负标签。为此，我们提出将对比比较限制在同一分类层级内的类别。此外，我们采用组平衡设计，确保每个分类层级得到充分的优化。因此，所提出的框架从粗粒度到细粒度同时提高了层级一致性和分类准确性。我们基于BioCLIP使用TreeOfLife-10M训练模型，并在多个分层分类基准上评估，模型在欧几里得空间和双曲空间均表现出显著提升的层级一致性。值得注意的是，在iNaturalist 2021（iNat21）上，我们的方法相比基线将各层级的平均准确率提高了30.47%，突显了其在分层零样本分类中的有效性。

## Abstract
Multimodal contrastive learning has enabled zero-shot visual classification by aligning images with textual categories. However, in hierarchically structured label spaces, existing methods often produce predictions that are inconsistent across taxonomic levels. For example, a model may predict a fine-grained category whose parent category contradicts its simultaneously predicted higher-level label. By analysis, the issue originates from false negative labels when contrastive comparison involves multiple taxonomic levels. To this end, we propose to restrict contrastive comparisons to categories within the same taxonomic level. In addition, we adopt a group-balanced design, ensuring each taxonomic level receives adequate optimization. As a result, the proposed framework improves both hierarchical consistency and classification accuracy from coarse to fine granularity. We train our model with TreeOfLife-10M based on BioCLIP and evaluate it across multiple hierarchical classification benchmarks, where the model demonstrates significantly improved hierarchical consistency in both Euclidean and hyperbolic spaces. Notably, on iNaturalist 2021 (iNat21), our method improves average accuracy across levels by 30.47% over the baseline, highlighting its effectiveness for hierarchical zero-shot classification.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：多模态对比学习（如CLIP）通过图像与文本对齐实现了零样本分类，但在具有层次结构（如生物分类学：界、门、纲、目、科、属、种）的标签空间中，现有方法经常产生**层级不一致**的预测。例如，模型可能预测一张图片为“狮子”（物种），同时预测其高层级标签为“植物门”，这显然矛盾。
- **核心问题**：标准对比学习采用“扁平标签”假设，将所有非匹配文本标签视为同等负样本。但在分层标签中，不同层级的类别之间存在语义兼容性（如狮子和狼在“目”层级共享祖先），导致**跨层级假负样本**，从而产生冲突的监督信号，破坏层级一致性。
- **整体含义**：本文指出，解决层级不一致的关键在于重新设计对比学习目标，避免跨层级比较，并平衡各层级的监督信号，从而在保持层级结构的前提下提升分类准确性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：**层级受限对比学习（Level-Restricted Contrastive Learning）**——将对比比较限制在同一分类层级内，避免跨层级假负样本；同时采用**组平衡设计**，确保每个层级得到充分优化。
- **关键技术细节**：
    1. **标签构造**：对每个图像，构建所有层级（界→种）的文本标签，每个层级的标签包含到该层级为止的分类学前缀（如“动物界 脊索动物门 哺乳纲 食肉目 猫科 豹属 狮”）。
    2. **层级内对比**：在每个层级ℓ内，仅对该层级的所有标签计算对比损失。由于同一层级内标签互斥，对比公式无歧义。
    3. **组平衡处理**：对于每个层级，将同一样本共享相同标签的图像–文本对视为多个正对，避免粗层级中同一标签被同时作为正负样本的矛盾。具体地，对每个文本锚点，对应的正图像集为其所有匹配图像，计算文本到图像损失时平均所有正样本。
    4. **损失函数**：每个层级ℓ的损失L<sub>ℓ</sub> = L<sub>I→T</sub> + L<sub>T→I</sub>，总损失为各层级损失的平均值 L = (1/L)Σ<sub>ℓ</sub>L<sub>ℓ</sub>，赋予各层级**等权重**，防止粗层级主导。
    5. **支持双曲空间**：可扩展至洛伦兹双曲空间，使用负洛伦兹距离作为相似度度量。
- **算法流程（文字描述）**：
    - 输入：图像及多层级标签（界→种），编码器f（图像）、g（文本）。
    - 对每个层级ℓ，为该层级内所有文本标签编码，并计算图像到文本损失（公式1，分母仅包含该层级标签）和文本到图像损失（公式2，对每个文本锚点平均其多个正图像）。
    - 将所有层级损失等权重求和，反向传播优化。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **训练数据集**：TreeOfLife-10M（生物分类学的大规模图像-文本数据集，涵盖10M样本）。
- **测试基准**：
    - **iNaturalist 2021 (iNat21)**：广泛覆盖不同分类层级，共约2.6万物种。
    - **Rare Species**：专注于稀有和长尾物种，不包含在TreeOfLife-10M训练集中。
    - **CrypticBio**：强调视觉相似（易混淆）的物种。
- **对比方法**：
    - **OpenCLIP**：原始CLIP（基于OpenCLIP预训练权重）。
    - **BioCLIP**：在TreeOfLife-10M上微调的基础模型。
    - **RCME**：同样基于BioCLIP微调，但采用全局和局部蕴含学习（2025 ICCV方法）。
    - **本文方法（Ours）**：在欧几里得空间和双曲空间分别实现。
- **评估指标**：
    - 各层级top-1准确率和所有层级平均准确率。
    - **层级一致性**：采用**自上而下约束推理（top-down constrained inference）**，即先从粗层级预测，后续层级候选仅限父类有效后代，并报告**归一化最低公共祖先（nLCA）深度**（衡量预测路径与真实路径的匹配深度）。

### 4. 资源与算力

- 论文**未明确说明使用的GPU型号、数量和总训练时长**。
- 仅提及：使用AdamW优化器，batch size 4096，学习率10⁻⁴，权重衰减0.2，训练30 epochs，基于BioCLIP ViT-B/16初始化。温度参数共享于所有层级。
- 双曲空间实验中，固定曲率c=1，采用洛伦兹模型。
- 由于未提供具体算力资源，无法评估训练成本或可复现性。建议作者补充硬件细节。

### 5. 实验数量与充分性

- **主要实验**：在3个基准（iNat21、Rare Species、CrypticBio）上对比了4种方法（OpenCLIP、BioCLIP、RCME、本文方法），报告了各层级准确率和平均准确率（共4张主表）。
- **层级一致性评估**：在iNat21上进行了自上而下约束推理实验（表2），并计算nLCA。
- **可视化分析**：t-SNE展示文本嵌入的层级结构（图2），定性验证方法有效性。
- **消融实验**：**缺乏**。论文没有单独验证“层级内限制”和“组平衡”两个组件各自贡献，也没有分析不同层级权重的影响。这在一定程度上削弱了结论的严谨性。
- **充分性评价**：实验覆盖了多个具有挑战性的场景（覆盖广、稀有、视觉易混淆），对比了基线方法，但缺乏消融和超参数敏感度分析。由于论文为8页短稿，可能受篇幅限制。总体实验设计相对客观公平，但未充分剖析方法内部机制。

### 6. 论文的主要结论与发现

- **层级受限对比学习显著提升了层级一致性**：在iNat21上，本文欧几里得变体的平均准确率较基线BioCLIP提升30.47%（78.96% vs 48.49%），双曲变体也达到78.95%。
- **自上而下推理中优势尤其突出**：在限制候选集的情况下，模型在家族、属、种层级的准确率分别比RCME提高44.54%、39.64%、27.97%，nLCA从0.43提升至0.71。
- **层级结构在嵌入空间自然涌现**：t-SNE可视化显示，本文方法学到的文本嵌入形成清晰的层级团簇，而BioCLIP则较混乱。
- **双曲空间在稀有/混淆场景上有额外优势**：在Rare Species和CrypticBio上，双曲变体在深层级上表现更好，表明双曲空间更擅长表征层级结构。

### 7. 优点：方法或实验设计上的亮点有哪些

- **问题洞察深刻**：精准识别出标准对比学习在分层标签中的假负样本问题，并给出简洁的解决方案（层级内对比 + 组平衡）。
- **方法简洁有效**：无需额外架构或几何损失，仅修改对比目标本身，即可大幅提升层级一致性和准确率，且可同时适用于欧几里得和双曲空间。
- **实验设计合理**：不仅报告标准准确率，还采用自上而下约束推理和nLCA指标，直接衡量层级一致性，评价维度全面。
- **定性分析充分**：通过t-SNE可视化直观展示了嵌入空间的层级结构，增强了方法可信度。
- **开源社区贡献**：已发布欧几里得和双曲模型至HuggingFace，促进复现和应用。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **消融实验缺失**：未对层级内限制和组平衡进行剥离实验，无法量化每个组件的贡献。
- **算力资源未公开**：缺少GPU型号、数量、训练时长等细节，影响方法可复现性和成本评估。
- **仅针对生物分类学领域**：方法虽通用，但实验仅在TreeOfLife数据集及相关基准上验证，未在其他层次结构（如WordNet、医疗诊断层次标签）上测试，泛化性存疑。
- **未讨论跨层级信息交互**：当前完全隔离各层级对比，可能忽略了层级间潜在的语义关联（如父类和子类间的包含关系）。未来可能需在严格对比和语义保持间寻找平衡。
- **未见超参数敏感度分析**：如层级权重是否最优、温度参数是否应分层设定等问题未涉及。

（完）
