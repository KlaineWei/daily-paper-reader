---
title: Geometry-Informed Maritime Anomaly Detection Using Probabilistic Roadmaps
title_zh: 使用概率路线图的几何信息海事异常检测
authors: "Gabriele Oliva, Andrea Tomei, Roberto Setola"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08100v1"
tags: ["query:xai-objdet"]
score: 8.0
evidence: 几何信息引导的异常检测，提取可解释航程特征
tldr: 海上异常检测对航行安全和水下基础设施保护至关重要，但标记异常数据稀缺。本文利用AIS数据构建概率路线图(PRM)作为导航可行域先验，提取路径效率、几何偏差等可解释特征，并通过合成异常生成平衡数据集。随机森林分类器在波罗的海数据集上达到0.837的测试ROC AUC，验证了将航行可行性约束融入检测的有效性。该框架为复杂几何环境下的基础设施感知监测提供了可解释且操作相关的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08100v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 769, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08100v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1377, \"height\": 913, \"label\": \"Figure\"}]"
motivation: 解决标记异常数据稀缺问题，利用几何先验提升海上轨迹异常检测的准确性与可解释性。
method: 构建PRM投影轨迹，提取航次级特征，合成异常生成平衡数据集，训练随机森林分类器。
result: 测试ROC AUC为0.837，交叉验证与留出测试表现稳定，验证方法有效性。
conclusion: 嵌入航行可行性约束的几何感知框架能有效检测异常，具备可解释性与操作相关性。
---

## 摘要
海事异常检测对于航行安全及关键水下基础设施的保护至关重要。本文提出了一种基于几何信息的监督学习框架，利用自动识别系统（AIS）数据检测波罗的海中的异常船舶轨迹。首先在可航行海域上构建概率路线图（PRM），并将其作为结构先验，将轨迹投影到可行走廊上。这种表示能够提取可解释的航次级特征，涵盖航次效率、与名义路径的几何偏差、运动学变异性以及靠近海底电缆的程度。为解决标记异常事件稀缺的问题，通过受控轨迹扰动和基础设施感知扭曲生成合成异常，从而为监督训练创建平衡数据集。基于所得特征集训练随机森林分类器，并通过交叉验证和保留测试集进行评价。实验结果表明，泛化性能稳定，测试ROC AUC达到0.837，说明将航行可行性约束嵌入异常检测过程的有效性。所提方法为几何复杂环境下的基础设施感知海事监测提供了可解释且具有操作相关性的框架。

## Abstract
Maritime anomaly detection is essential for navigational safety and for the protection of critical underwater infrastructure. This paper proposes a geometry-informed supervised framework for detecting anomalous vessel trajectories in the Baltic Sea using Automatic Identification System (AIS) data. A Probabilistic Roadmap (PRM) is constructed over the navigable maritime domain and used as a structural prior to project trajectories onto feasible corridors. This representation enables the extraction of interpretable voyage-level features capturing route efficiency, geometric deviation from nominal paths, kinematic variability, and proximity to submarine cables. To address the scarcity of labeled anomalous events, synthetic anomalies are generated through controlled trajectory perturbations and infrastructure-aware distortions, producing a balanced dataset for supervised training. A Random Forest classifier is trained on the resulting feature set and evaluated under cross-validation and a held-out test split. Experimental results show stable generalization performance, achieving a test ROC AUC of 0.837, indicating the effectiveness of embedding navigational feasibility constraints into the anomaly detection process. The proposed approach provides an interpretable and operationally relevant framework for infrastructure-aware maritime monitoring in geometrically complex environments.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：海事异常检测对于航行安全和关键水下基础设施（如海底通信/能源电缆）保护至关重要。现有方法多依赖隐式行为建模或学习特征嵌入，未能显式编码由地理、海岸线和禁航区产生的航行可行性约束。
- **背景**：波罗的海水域密集、航道狭窄，且集中了大量战略级海底电缆。近期基础设施破坏事件凸显了检测细微几何偏差的必要性。
- **动机**：提出一种几何信息引导的监督框架，通过概率路线图（PRM）将空间可行性先验结构性地嵌入异常定义，弥补纯隐式轨迹表示的不足。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
将机器人运动规划中的概率路线图（PRM）重新解释为海事可航行走廊的结构先验，通过AIS轨迹在PRM上的投影提取可解释航次级特征，结合合成异常生成与随机森林分类器实现监督检测。

### 关键技术细节
- **PRM构建**：在波罗的海可航行水域采样节点（4000个候选节点，经Sentinel-2水陆掩膜过滤后保留2284个），使用k=25的近邻连接，边权重为欧氏距离。分别连接已过滤节点，丢弃与OpenStreetMap海岸线相交的边。
- **轨迹投影**：将AIS轨迹点投影到最近的PRM节点，计算几何长度 \(L_{AIS}\) 和PRM诱导长度 \(L_{PRM} = \sum d_G(\hat{v}_i, \hat{v}_{i+1})\)。
- **特征提取**（三类共12个特征）：
    - **PRM合规性**：长度比、长度差、最大/平均点向偏差、PRM覆盖度、最大滞留时间。
    - **运动学**：速度方差、最大加速度、航向方差。
    - **上下文交互**：最小海岸线距离、靠近电缆总时长、靠近电缆平均速度。
- **合成异常生成**（858条，平衡数据集）：
    1. 正弦扰动（增加周期性偏差+高斯噪声）。
    2. 马尔可夫方向扰动（3×3方向概率核，偏向当前航向）。
    3. 自编码器重构扭曲（注入噪声后重构）。
    4. 电缆附近速度异常（1.8~2.2倍速度变化）。
- **分类器**：随机森林（B棵树，Gini不纯度，多数投票），超参数经两阶段搜索：宽范围随机搜索 + 局部网格细化，10折交叉验证调优。

## 3. 实验设计

- **数据集**：
    - 正常轨迹：858条波罗的海通用货船轨迹（2017.01-2019.01），芬兰海事数据库，5分钟重采样，EPSG:3035投影。
    - 异常轨迹：858条合成生成，按四种方法均匀分布。
    - 总样本：1716条，70/10/20分割为训练、验证、测试集。
- **基准与对比方法**：
    - **未与其他现有方法对比**（如VRNN、Geo-TrackNet、基于聚类的基线），仅采用随机森林自身训练/验证/测试指标进行内部评估。
    - 作者明确此为局限性，需未来进行消融实验（如剔除PRM特征、仅用运动学特征）和与基线对比。
- **评估指标**：准确率、精确率、召回率、F1分数、ROC AUC。

## 4. 资源与算力

- **未明确说明**：文中没有提及GPU型号、数量、训练时长等算力信息。PRM构建和特征提取计算量中等，随机森林训练对算力要求较低，估计可在普通CPU上完成。整体实验规模较小，无需大量HPC资源。

## 5. 实验数量与充分性

- **实验组数**：单个数据集上的完整训练/验证/测试流程，含10折交叉验证和最终测试集评估。无多数据集对比，无消融实验。
- **充分性评价**：
    - **优点**：交叉验证+留出测试结构合理，合成异常覆盖多种扰动类型，概率分布和ROC曲线展示完整。
    - **不足**：缺乏与其他现有方法的对比实验；未进行特征消融（如移除PRM特征）；仅使用单一船型（通用货船）和单一海域（波罗的海），泛化性未知；合成异常可能无法完全代表真实异常模式。

## 6. 主要结论与发现

- 嵌入航行可行性约束的PRM表示能稳定提升异常检测性能，测试ROC AUC达0.837，验证集AUC 0.813，训练集AUC 0.949（过拟合程度可控）。
- 合成异常生成方法有效弥补了标记数据稀缺问题，且四种扰动机制覆盖了不同异常类型（几何偏离、随机摆动、非线性扭曲、基础设施相关）。
- 特征空间具有可解释性，长度比、点向偏差、电缆附近速度等可操作地解释检测结果。

## 7. 优点

- **几何先验显式嵌入**：PRM将地理约束（海岸线、禁航区）直接编码为结构先验，避免纯数据驱动方法无法感知可行域的问题。
- **可解释性好**：提取的特征（长度比、平均偏差、电缆附近时长等）可直接解读异常成因，适合操作人员使用。
- **合成异常设计多样**：四种机制覆盖确定性、随机、生成式和上下文相关异常，增加了数据多样性，降低过拟合风险。
- **框架端到端且轻量**：从PRM构建到特征提取到随机森林分类流程清晰，计算开销低，易于部署。

## 8. 不足与局限

- **缺乏基准对比**：未与其他SOTA方法（如Geo-TrackNet、基于LSTM的检测器、图神经网络方法）比较，无法量化相对增益。
- **仅考虑单一船型与海域**：通用货船轨迹，波罗的海特殊性（多岛屿、窄水道）可能限制向其他海域（如开阔大洋）的迁移性。
- **合成异常与真实异常差距**：虽然设计了四种扰动，但可能无法模拟真正的恶意行为（如故意绕行、关闭AIS、停靠等）。真实异常标签缺失导致无法验证外推能力。
- **无消融分析**：未分离PRM特征、运动学特征、上下文特征的各自贡献，也不清楚k、节点密度、水陆掩膜阈值等超参数敏感性。
- **PRM静态假设**：未考虑季节、天气、交通密度等时变因素，无法反映动态航道变化。
- **计算/资源信息缺失**：缺少训练耗时、硬件配置等可复现性细节。

（完）
