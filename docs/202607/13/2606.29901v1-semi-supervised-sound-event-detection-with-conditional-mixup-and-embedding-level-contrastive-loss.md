---
title: Semi-Supervised Sound Event Detection with Conditional Mixup and Embedding-Level Contrastive Loss
title_zh: 使用条件混合和嵌入级对比损失的半监督声音事件检测
authors: "Nian Shao, Xian Li, Xiaofei Li"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29901v1"
tags: ["query:contrastive"]
score: 8.0
evidence: 嵌入级自监督对比损失用于声音事件检测
tldr: 声音事件检测受限于标注数据稀缺。基于预训练音频模型，本文在伪标签半监督框架中引入嵌入级对比损失，更好利用未标注数据。针对mixup在伪标签与对比学习中的角色冲突，提出条件mixup统一两种增强。在DESED验证集上达到0.645 PSDS1和0.822 PSDS2，创下新纪录。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29901v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 721, \"height\": 228, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29901v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1178, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29901v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 872, \"height\": 595, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29901v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29901v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 837, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29901v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 780, \"height\": 296, \"label\": \"Table\"}]"
motivation: 标注数据稀缺限制SED性能，伪标签半监督框架对未标注数据利用不足，且mixup在监督与对比目标中角色冲突。
method: 提出条件mixup，将组合mixup和扰动mixup统一在伪标签与对比损失框架中，并定义对应的嵌入级对比损失。
result: 在DESED验证集上PSDS1和PSDS2分别达到0.645和0.822，超过先前最优。
conclusion: 嵌入级对比损失和条件mixup有效提升了半监督SED性能，验证了对比学习在微调阶段的潜力。
---

## 摘要
声音事件检测（SED）是声学环境分析的核心模块，但其性能常受限于稀缺的标注数据。近期系统利用大型预训练音频基础模型，但由于标注数据有限而未标注数据丰富，有效微调仍具挑战。先前工作ATST-SED通过基于伪标签的半监督微调框架解决了该问题。在本工作中，我们进一步改进框架，采用受ATST-Frame预训练启发的嵌入级自监督对比损失。该对比目标在微调期间更好地利用了未标注数据。一个挑战是混合（mixup）在两个目标中扮演不同角色：伪标签学习使用组合混合，而对比学习将混合视为扰动。为解决此不匹配，我们提出条件混合，它在同一半监督框架中结合组合混合和扰动混合，并定义了相应的嵌入级对比损失。最终模型在DESED验证集上达到0.645 PSDS1和0.822 PSDS2，创下新的最优性能。

## Abstract
Sound event detection (SED) is a core module for acoustic environmental analysis, yet its performance is often limited by scarce labeled data. Recent systems leverage large pretrained audio foundation models, but effective fine-tuning remains challenging because labeled data are limited while unlabeled data are abundant. A previous work, ATST-SED, addressed this problem with a pseudo-label based semi-supervised fine-tuning framework. In this work, we further improve the framework by adopting an embedding-level self-supervised contrastive loss inspired by ATST-Frame pretraining. This contrastive objective better exploits unlabeled data during fine-tuning. One challenge is that mixup serves different roles in the two objectives: pseudo-label learning uses composition mixup, while contrastive learning treats mixup as a perturbation. To resolve this mismatch, we propose conditional mixup, which combines composition mixup and perturbation mixup in one semi-supervised framework and defines the corresponding embedding-level contrastive losses. The resulting model achieves 0.645 PSDS1 and 0.822 PSDS2 on the DESED validation set, establishing a new state of the art.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- 声音事件检测（SED）旨在识别音频中声音事件并定位其时间边界，是声学环境分析的关键模块。
- 主要挑战：帧级别的强标注成本极高，导致标注数据稀缺；而大规模无标注数据却很丰富。
- 现有方法利用预训练的音频基础模型（如 ATST-Frame），但在微调阶段，由于标注数据有限，如何有效利用无标注数据仍是难题。
- 先前工作 ATST-SED 提出了基于伪标签的两阶段半监督微调框架，但半监督目标中的一致性正则化（如 Mean Teacher）对于大型预训练模型效果较弱，因为其难度远低于预训练任务。
- 本文核心动机：将自监督对比学习引入半监督微调，以更充分地利用无标注数据。同时解决 mixup 在伪标签学习和对比学习中角色冲突的问题。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在 ATST-SED 的第二阶段微调中，除了原有的伪标签损失，引入嵌入级（embedding-level）的自监督对比损失，该损失受 ATST-Frame 预训练启发，能在微调中更好地利用无标注数据。
- **关键技术细节**：
  - **条件 mixup（Conditional Mixup）**：统一两种 mixup 模式：
    - 组合 mixup（Composition mixup）：当插值系数 λ ∈ [0.5, τ] 时，视辅助样本与输入样本的能量相近，保留两者事件，目标取逻辑或（OR），伪标签取两者之和（clamped to 1）。
    - 扰动 mixup（Perturbation mixup）：当 λ ∈ (τ, 1] 时，视辅助样本仅作为噪声扰动，仅保留输入样本的目标。
    - 阈值 τ 设为 0.55。
  - **伪标签损失**：对应两种 mixup 模式分别定义 Lc-psd 和 Lp-psd，最终 Lcp-psd 为两者平均。
  - **嵌入级对比损失**：
    - 在 ATST-SED 编码器后添加投影器（projector）和预测器（predictor），用于对比学习（防止坍塌）。
    - 扰动情况下：对比损失最大化混合样本与学生预测之间的相似度（与教师嵌入对比），包括对称形式。
    - 组合情况下：对比损失最大化混合样本与学生预测与两个源样本教师嵌入的平均值之间的相似度。
    - 总对比损失 Lcon 为两种情况的平均。
  - **整体训练目标**：Ltotal = LBCE + rMT * LMT + rcp-psd * Lcp-psd + rcon * Lcon。保持原有的 Mean Teacher 损失和频率扭曲增强。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：DESED（Domestic Environment Sound Event Detection）。
  - 训练集：1578 段弱标注、10000 段合成强标注、14412 段无标注、3470 段额外真实强标注（来自 AudioSet）。
  - 验证集：1168 段强标注音频片段（10秒，16kHz 重采样）。
- **基准（Baseline）**：ATST-SED [13]，采用两阶段半监督微调，第一阶段冻结 ATST-Frame 并优化 CRNN+SED分类器，第二阶段全参数微调并加入 mixup 和 ICT 损失。
- **对比方法**：
  - CRNN 模型（DCASE 2023 baseline）。
  - MAT-SED [28] 和 PMAM iter2 [29]：两个近期基于 AST 的自监督+SED 方法。
- **评估指标**：PSDS1（强调时间稳定性）、PSDS2（强调音频标签准确率），使用中值滤波后处理或 cSEBB 后处理。

## 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量或训练时长。
- 仅提到优化器为 Adam，学习率设置：层间衰减学习率（ATST 块初始 2e-4，每层衰减 0.5），CNN/投影器/预测器初始 2e-4，RNN/分类器初始 2e-3。训练步数 20000 步，指数 ramp-up 290 步后余弦衰减。

## 5. 实验数量与充分性
- 实验共包含**四组**主要对比/消融：
  - **表Ⅰ**：将提出方法应用于 CRNN 和 ATST-SED 基线，显示提升。
  - **表Ⅱ**：消融研究，逐步添加不同组件（仅 Lcon、仅伪标签、组合分支、扰动分支、全部组件），验证各组件有效性。
  - **图3**：逐类 PSDS1 对比，分析改进分布。
  - **表Ⅲ**：与 SOTA（MAT-SED, PMAM iter2）对比，使用中值滤波和 cSEBB 后处理。
- **充分性评价**：
  - 消融实验覆盖了条件 mixup 的两个分支、对比损失单独使用、伪标签损失单独使用等关键设计，较全面。
  - 对比模型均为近期代表性工作，结果客观。
  - 但仅在一个数据集（DESED）上验证，未提供其他场景或数据集的结果，泛化性存疑。
  - 未报告多次实验的方差或统计显著性，结论稳健性有待确认。

## 6. 论文的主要结论与发现
- 提出的方法（ATST-SEDv2）在 DESED 验证集上达到 **PSDS1 0.645** 和 **PSDS2 0.822**（cSEBB 后处理），创下新纪录。
- 对比损失和伪标签损失互补，单独使用对比损失效果弱于伪标签损失，但两者联合最佳。
- 条件 mixup 中的组合分支和扰动分支各自有效，且兼容。
- 方法对轻量模型（CRNN）也有显著提升（PSDS1 0.384→0.403），显示通用性。
- 逐类分析表明改进分布广泛，尤其对短时、高变异性事件（如“狗”、“猫”、“餐具”）提升明显。

## 7. 优点
- **方法创新**：提出条件 mixup 统一两种语义不同的 mixup 模式，并设计对应的伪标签和对比损失，思路清晰。
- **实用性**：仅在训练阶段增加投影器/预测器，推理架构不变，无额外推理开销。
- **实验设计**：消融实验充分验证各组件贡献，对比 SOTA 方法时统一后处理（cSEBB），公平性较好。
- **性能提升**：在强基线 ATST-SED 上仍有明显提升，且跨模型有效。

## 8. 不足与局限
- **仅限于单一数据集**：所有实验仅在 DESED 上验证，未在如 AudioSet、DCASE 其他任务等更大规模或不同领域数据集上评估，泛化能力未知。
- **缺乏算力/耗时报告**：未说明训练所需 GPU 资源，使得可复现性和资源需求评估困难。
- **未报告统计显著性**：没有重复实验的均值、标准差或显著性检验，单次结果可能存在偶然性。
- **对比损失权重设定**：超参数 rcon=3 等为经验值，未做敏感性分析，可能存在过拟合特定设定。
- **模型复杂度**：未对比计算开销（参数量、FLOPs），与轻量模型相比可能消耗更多资源。
- **仅使用一种预训练模型（ATST-Frame）**：未验证方法是否适用于其他预训练音频模型（如 BEATs, AST）。

（完）
