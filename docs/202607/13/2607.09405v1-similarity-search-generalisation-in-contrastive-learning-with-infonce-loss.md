---
title: Similarity search generalisation in contrastive learning with InfoNCE loss
title_zh: 对比学习中InfoNCE损失的相似性搜索泛化
authors: Nick Whiteley
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09405v1"
tags: ["query:contrastive"]
score: 10.0
evidence: InfoNCE损失函数在对比表示学习中的应用
tldr: 对比学习常使用InfoNCE损失训练嵌入模型进行相似性搜索，但有限负样本下的泛化理解不足。本文证明InfoNCE的总体风险与k个负样本的期望交叉熵之差为O(1/k)，并利用Gâteaux微分导出新的连续性界，该界保留了负样本平均结构并引入逆温度参数。对Lipschitz嵌入函数，负样本平均效应随k增长而稳定泛化误差。此工作补充了互信息与对齐均匀性解释，提供了量化泛化的工具。
source: arxiv
selection_source: fresh_fetch
motivation: 揭示InfoNCE损失在有限负样本下的泛化行为，弥补现有理论空白。
method: 通过风险分析关联InfoNCE与期望交叉熵，并用Gâteaux微分推导连续性界。
result: 风险差为O(1/k)，导出含逆温度参数的泛化界，负样本平均效应稳定误差。
conclusion: 丰富了InfoNCE理解，为对比学习泛化提供新视角和量化工具。
---

## 摘要
相似性搜索是通过对比学习训练的嵌入模型的主要应用。对于最流行的对比学习损失函数之一InfoNCE，我们展示了具有k个负样本的总体风险与期望交叉熵相差O(1/k)，该交叉熵量化了以下两者之间的偏差：i) 使用学习到的嵌入函数对未见数据进行softmax相似性搜索，以及ii) 对相同数据使用正样本生成器中隐含表示的相似性进行理想化的softmax搜索。这补充了InfoNCE在k→∞极限下的现有解释，这些解释以互信息以及嵌入的对齐与均匀性来表达。为了量化泛化性能，我们通过Gateaux微分引入了一个新的InfoNCE损失连续性界。该界保留了损失函数中负样本平均的结构，并具有一个“逆温度”参数，可以调整以考虑算法温度。对于参数为Lipschitz的嵌入函数，这简单地证明了InfoNCE损失中k个负样本的平均效应会随着k的增长而传递到泛化误差的稳定化。

## Abstract
Similarity search is a primary application of embedding models trained by contrastive learning. For one of the most popular contrastive learning loss functions, InfoNCE, we show that the population risk with $k$ negative samples is $O(1/k)$ close to an expected cross-entropy which quantifies deviation between i) a softmax similarity search over unseen data using the learned embedding function, and ii) an idealised softmax search over the same data but using similarity implicitly represented in the positive sample generator. This complements existing interpretations of InfoNCE in the $k\to\infty$ limit which are phrased in terms of mutual information, and alignment versus uniformity in embeddings. To quantify generalisation performance, we introduce a new continuity bound for the InfoNCE loss, obtained via Gâteaux differentiation. The bound preserves the structure of averaging over negative samples present in the loss function and features an ``inverse temperature'' parameter which can be tuned to account for the algorithmic temperature. For embedding functions which are Lipschitz in a parameter, this yields a simple demonstration that the averaging effect of $k$ negative samples in the InfoNCE loss carries over to stabilisation of the generalisation error as $k$ grows.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：对比学习（如SimCLR、MoCo、CLIP）使用InfoNCE损失训练嵌入模型，其核心应用是相似性搜索。然而，关于有限负样本数k下InfoNCE损失的泛化行为，尤其是与相似性搜索性能的直接关联，现有理论尚不充分。已有解释多集中于k→∞极限下的互信息或对齐/均匀性，缺少对有限k下泛化误差的明确刻画。
- **整体含义**：本文旨在从相似性搜索泛化的角度重新解释InfoNCE损失，证明其总体风险与理想软搜索的交叉熵之差为O(1/k)，并利用新的连续性界揭示负样本平均效应对泛化误差的稳定作用，从而补充现有理论。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  - 将InfoNCE的总体风险与两个交叉熵量关联：一是与理想马尔可夫核M的交叉熵（k→∞时），二是与基于k个未见数据点的经验马尔可夫核的期望交叉熵（有限k时）。后者直接对应实际软相似性搜索。
  - 利用Gâteaux微分导出InfoNCE损失关于嵌入函数的连续性界，该界保留了负样本平均结构，并引入可调逆温度参数β，以匹配算法温度τ。
- **关键技术细节**：
  - **命题1**：证明对于任何嵌入函数ϕ∈B(Z, S^(d-1))，k≥1，τ>0，有
    -log(1+e^(2/τ)/k) ≤ ∫CrossEnt[M(z,·)‖Q_ϕ^τ(z,·)] π_data(dz) - R(ϕ;k,τ) ≤ 18/k (e^(2/τ)-1)^2
    其中R(ϕ;k,τ)为InfoNCE总体风险，Q_ϕ^τ为基于ϕ的软相似性核。
  - **命题2**：在M(z,·)≪π_data假设下，InfoNCE总体风险与基于k个未见样本的经验交叉熵之差被界为4/(τk)·E[(dM/dπ_data)^2] + log(1+e^(2/τ)/k)。
  - **命题4**（连续性界）：对于任何k≥1，τ>0，β≥1，ϕ,ϕ'∈B(Z,S^(d-1))，有
    |ℓ(ϕ,z,k,τ)-ℓ(ϕ',z,k,τ)| ≤ (4/τ)‖ϕ(za)-ϕ'(za)‖₂ + (1/τ)‖ϕ(z⁺)-ϕ'(z⁺)‖₂
    + (3/τ)e^(2/βτ) ( (1/k)∑_{j=1}^k ‖ϕ(z⁻_j)-ϕ'(z⁻_j)‖₂^β )^(1/β)
    该界保留了对负样本差分的幂平均结构，β可调以匹配τ。
  - **命题5**：在嵌入函数关于参数θ L-Lipschitz（∂ϕ/∂θ有界）条件下，利用Dudley积分和准则，给出经验Rademacher复杂度上界为O( (√(d_Θ)/τ) · min{a√(log3+1), √(log(1+2a)+1)} / √n )，其中a∝RB_τ，B_τ包含数据范数的平均项（含负样本的幂平均）。
- **算法流程**：论文并未提出新算法，而是以算法1形式描述标准的InfoNCE经验风险最小化过程（包括锚点、正样本、k个负样本的采样及对数损失）。本文的理论分析适用于任何通过该过程（或其近似）得到的嵌入函数。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **实验情况**：本文为纯理论论文，**未涉及任何实际数据集实验、基准对比或实验验证**。所有结果均为数学证明和理论推导。（注：论文在附录A.1中构造了一个理论例子以说明界限的紧致性，但该例子并非基于真实数据，而是人为设定概率分布和嵌入向量，用于展示下界行为。）

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。
- **资源与算力**：论文**未提及任何实验资源、GPU型号、训练时长等信息**。作为理论工作，不涉及计算实验。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。
- **实验数量**：**无实验**。论文的全部结论基于数学推导，不涉及实证验证。因此无法评估实验的充分性、客观性或公平性。

### 6. 论文的主要结论与发现
- **主要结论**：
  1. InfoNCE总体风险与k个负样本的期望交叉熵之差为O(1/k)，揭示了有限负样本下InfoNCE损失与相似性搜索的精确关系。
  2. 新连续性界保留了负样本平均结构，并引入逆温度参数β，允许将算法温度τ的影响纳入泛化分析。
  3. 对于Lipschitz嵌入函数类，经验Rademacher复杂度随k增长而稳定（因负样本范数的平均项收敛），且与嵌入维度d无关。
  4. 结合所有结果，得到定理1：在特定假设下，相似性搜索期望交叉熵（与理想搜索的偏差）可以被训练经验风险、复杂度惩罚、样本变异性、有限k偏差四部分所界定，其中有限k偏差为O(1/k)。
- **发现**：负样本的平均效应（而非最大化）是泛化误差稳定的关键；温度参数τ既影响经验风险中的相似性度量，也通过逆温度β影响复杂度界。

### 7. 优点：方法或实验设计上有哪些亮点
- **理论贡献突出**：
  - 提供了InfoNCE损失与相似性搜索泛化之间的直接联系（交叉熵形式），补充了互信息和对齐/均匀性解释。
  - 利用Gâteaux微分推导的连续性界是新颖的，保留了损失函数的平均结构，适用于后续的Rademacher复杂度分析。
  - 引入逆温度参数β，使理论界能够灵活匹配算法温度，增强了实用性。
  - 所得泛化界不随负样本数k恶化（稳定化），解决了此前文献（如Saunshi等）中复杂度随k增长的问题。
- **框架清晰**：从总体风险解释到泛化分析，逻辑链条完整，且对DCL损失的扩展讨论增加了普遍性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **缺乏实验验证**：全部结论均为理论推导，未在真实数据集（如ImageNet、CIFAR等）上进行验证，也未与现有泛化界进行量化比较。因此，理论界的紧致性、实际可解释性有待实证检验。
- **假设较强**：
  - 假设训练元组i.i.d.，而实践中负样本常来自同一mini-batch，存在依赖性。
  - 嵌入函数必须取值于单位球面S^(d-1)，且满足Lipschitz条件（如全连接网络），限制了网络架构的多样性。
  - 正样本生成核M需受π_data控制（假设(B)），且需知密度比dM/dπ_data，这在实际中难以验证。
- **仅针对InfoNCE及DCL**：未考虑其他对比损失（如NCE、triplet loss等），适用范围有限。
- **计算复杂度**：虽然泛化界与维度无关，但Lipschitz常数C_Φ可能随网络深度/宽度增长，导致界很大。
- **实际应用中的偏差风险**：有限k偏差项（如log(1+e^(2/τ)/k)）在τ很小时会很大，而论文建议的β选择（β=2/τ）可能使界随τ减小而急剧增大，影响实际使用。

（完）
