---
title: What Do They See? Interpreting Complex Road Scenarios Through the Eyes of Vision-Language-Action Models for Safe and Trustworthy Autonomous Vehicle Learning
title_zh: 它们看到了什么？通过视觉-语言-行动模型的眼睛解读复杂道路场景以实现安全可信的自动驾驶学习
authors: "Kalpana Panda, Wesley Maia, Vinti Agarwal, Ross Greer"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16938v1"
tags: ["query:xai-objdet"]
score: 9.0
evidence: 通过反事实消融解释自动驾驶中的VLA模型目标检测
tldr: 端到端自动驾驶模型在复杂场景中表现良好但内部逻辑不透明。提出Counterfactual Vision Action Analysis (CVAA)反事实消融框架，利用生成式修复移除前视相机图像中的单个检测对象，通过对比响应差异量化各对象的因果影响。在Alpamayo 1模型和210个nuScenes场景上创建Counter-nuScenes数据集，发现车辆和行人因果影响最强，交通灯影响与其面积不成比例，也存在模型对无关对象的异常响应。进一步通过机制可解释性比较原图与修复图的中间表示，从行为审计深入到表征理解，为实现可解释自动驾驶奠定基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16938v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1353, \"height\": 345}, {\"url\": \"assets/figures/arxiv/2607.16938v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 703}, {\"url\": \"assets/figures/arxiv/2607.16938v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1441, \"height\": 906}, {\"url\": \"assets/figures/arxiv/2607.16938v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1435, \"height\": 621}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16938v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 398, \"height\": 232}, {\"url\": \"assets/tables/arxiv/2607.16938v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 393, \"height\": 231}, {\"url\": \"assets/tables/arxiv/2607.16938v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 711, \"height\": 319}, {\"url\": \"assets/tables/arxiv/2607.16938v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1179, \"height\": 441}]"
motivation: 自动驾驶模型的黑箱特性导致安全风险，需解释其内部决策逻辑。
method: 提出CVAA框架，用生成式物体移除构建反事实图像，通过分析模型输出变化归因因果影响。
result: 在Alpamayo 1模型中，路径内车辆行人因果影响最强，交通灯影响显著，且存在对无关对象的异常响应。
conclusion: 结合行为审计与机制可解释性，从行为层面到表征层面理解模型，为实现可解释自动驾驶提供新途径。
---

## 摘要
端到端自动驾驶模型现已能够应对复杂的道路场景，将原始传感器观测直接映射到观测路径用于开环评估，并在闭环评估中通常实现有效驾驶。然而，由于交通场景的复杂性，这些安全关键系统的内部逻辑在很大程度上仍不透明。我们提出了一种反事实消融框架，称为反事实视觉行动分析（CVAA），该系统使用逼真的生成式修复从前置摄像头图像中系统地移除单个检测到的物体，以准备反事实集来评估模型响应的差异。这隔离了每个物体存在对模型规划行为的因果影响。应用于跨越210个nuScenes驾驶场景的Alpamayo 1轨迹预测器，我们创建了数据集Counter-nuScenes，通过该数据集我们看到，模型“路径”内的车辆和行人如预期般主导因果影响，而交通灯则如预期般相对于其图像足迹产生不成比例的影响。然而，我们也发现模型对人类驾驶员认为无关的物体做出强烈反应的案例。这引出了一个更深层次的问题：模型本身是将场景视为影响结果的单个物体的总和，还是编码了一组完全不同的内部特征，这些特征不对应于人类可读的场景元素？为了进一步理解这一点，我们使用机械可解释性技术比较原始图像和修复图像对的中间表示，并检查通过不同模型层移除的效果。这两个阶段共同提供了一条从行为审计到表示理解的路径，创建可解释的驾驶系统并巩固人机信任。

## Abstract
End-to-end autonomous driving models are now able to navigate complex road scenarios, mapping raw sensor observations directly to observed paths for open-loop evaluation and often effective driving in closed-loop evaluation. Yet the internal logic of these safety-critical systems remains largely opaque, due to the complexity of traffic scenes. We propose a counterfactual ablation framework called Counterfactual Vision Action Analysis (CVAA) that systematically removes individual detected objects from front-camera images using photorealistic generative inpainting to prepare counterfactual sets to evaluate the difference in the model's response. This isolates the causal effect of each object's presence on the model's planning behaviour. Applied to the Alpamayo 1 trajectory predictor across 210 nuScenes driving scenes, we create a dataset Counter -nuScenes, using which we see that vehicles and pedestrians within the model's 'path' dominate causal influence as expected, while traffic lights, as expected, exert disproportionate effect relative to their image footprint. However, we also find cases where the model responds strongly to objects a human driver would consider irrelevant. This brings forth a deeper question: does the model itself view the scene as a sum of individual objects influencing the outcome, or does it encode an entirely different set of internal features that do not correspond to human-legible scene elements? To further understand this, we compare intermediate representations of original and inpainted image pairs using mechanistic interpretability techniques and examine the effect of the removal through the various model layers. Together, these two stages offer a path from behavioral auditing to representational understanding, creating explainable driving systems and solidifying human-AI trust.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：端到端自动驾驶模型（如VLA模型）能够从原始传感器观测直接输出轨迹，但其内部决策逻辑高度不透明，尤其在复杂交通场景中难以判断模型究竟依赖哪些场景物体做出驾驶决策。现有视觉归因方法（如热力图）在密集场景中难以审计，且可能混淆物体重要性与伪影。
- **核心问题**：哪些可见物体因果性地驱动了视觉模型的轨迹预测？模型是否真正“看到”了安全关键物体？模型内部表征是否对应人类可理解的场景元素？
- **整体含义**：通过提出反事实物体移除框架（CVAA）并结合机制可解释性，建立从行为审计到内部表征理解的桥梁，以提升自动驾驶系统的可解释性和人机信任。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：通过**反事实消融**——在原始图像中使用逼真生成式修复移除单个物体，对比模型在原始图像和去除物体后的图像上的轨迹预测分布偏移，来量化每个物体的因果影响。进一步通过**机制可解释性**技术，追踪物体移除在模型各层隐状态中的传播路径。
- **关键技术细节**：
  - **数据集构建**（Counter-nuScenes）：从nuScenes数据集（700个场景）中，选取210个复杂场景的前视相机关键帧（每帧用YOLOv8+ SAM2检测并分割物体），对每个物体独立使用Gemini imgen或LaMa+FLUX Fill进行光照一致的修复，生成原图-修复图对。共生成3,062对反事实图像。
  - **归因度量**：定义**平均偏差（AD）** 和**最终偏差（FD）**：
    - 模型输出K个随机轨迹样本，取其均值轨迹 \(\bar{p}\)（XY平面）。
    - \(AD = \frac{1}{T} \sum_{t=1}^T \|\bar{p}_{\text{var}}^{(t)} - \bar{p}_{\text{orig}}^{(t)}\|_2\)，衡量全预测时域的平均位置偏差。
    - \(FD = \|\bar{p}_{\text{var}}^{(T)} - \bar{p}_{\text{orig}}^{(T)}\|_2\)，衡量终点位置偏差。
    - 与minADE/minFDE不同（衡量与真实轨迹的精度），AD/FD衡量模型自身输出的分布位移，不依赖真实轨迹。
  - **白盒分析**：计算原图和修复图在视觉编码器、VLM、轨迹专家各层的隐状态余弦delta；使用Logit Lens追踪物体类概率变化；分析注意力权重，识别四种传播模式（耦合、透明、解耦、沉默）。

#### 3. 实验设计
- **数据集与场景**：使用**nuScenes v1.0-trainval**，从中选取210个前视相机关键帧（每帧包含6-32个检测物体），覆盖波士顿和新加坡的多样化城市交通状况。构建**Counter-nuScenes**基准（共3,062个反事实图像对）。
- **模型**：仅使用**Alpamayo 1**轨迹预测器（NVIDIA的VLA模型，包含视觉编码器、VLM、轨迹专家）。
- **对比方法**：论文未直接对比其他归因方法，而是自我评估。仅在讨论中与minADE/minFDE、Grad-CAM等进行了概念对比。实验是框架的首次应用和验证。
- **黑盒评估设置**：使用三个随机种子（42,7,123）重复运行，仅保留前视相机图像（其他摄像头置零），输入单张静态图像。

#### 4. 资源与算力
- **未明确说明**：论文正文及附录中**未提及**使用的GPU型号、数量、训练或推理时长、算力预算等。仅提到使用了YOLOv8、SAM2、Gemini imgen或LaMa+FLUX Fill等工具，但未量化计算资源。需要指出这一信息缺失。

#### 5. 实验数量与充分性
- **实验数量**：
  - 黑盒评估：210个场景 × 每个场景平均约14.6个物体（总2,975个物体），3个种子重复，共约8,925次推理。
  - 白盒评估：对每个物体计算全模型层间delta，覆盖27个视觉块、36个LM层、36个去噪层。
  - 额外分析：掩码大小相关性、交通灯颜色影响、rank稳定性（3个种子）、z-score场景分布等。
- **充分性与公平性**：
  - **优点**：分析维度丰富（类级别、场景级、种子间稳定性、与真实轨迹精度对比）。使用逼真修复避免了分布偏移，比黑色遮挡更合理。统计检验充分（Spearman相关系数、p值等）。
  - **局限**：仅评估了一个模型（Alpamayo 1）和一个数据集（nuScenes前视相机），泛化性未知。仅使用单帧图像（无时间上下文），可能遗漏动态物体影响。白盒分析依赖余弦相似度和Logit Lens，这些工具在VLA架构上的有效性未经验证。实验设计整体客观，但无法证明在其他模型上同样有效。

#### 6. 论文的主要结论与发现
- **黑盒发现**：
  - **类级别**：公交车、卡车、交通灯的因果影响最大，交通灯相对于其图像面积影响不成比例。汽车和行人虽然出现频率高，但归一化后弱于大型车辆。
  - **排名不稳定性**：物体重要性排名在种子间变动大，仅5.6%的物体保持精确排名；说明模型**不依赖单一物体**，而是整合多个线索。
  - **场景级**：多数场景存在一个显著高影响物体（z-score>2占70.5%），但该物体可能并非人眼中最安全关键的物体（例如：已过马路的行人排名高于即将横穿的行人；网球拍、停车标志等偶尔排名第一）。
  - **掩码大小**：物体物理尺寸与AD正相关，但并非唯一决定因素（大物体更易引起偏差）。
  - **交通灯颜色**：红色交通灯移除导致的AD远大于绿色交通灯，符合预期（红灯时轨迹更受约束）。
  - **与真实轨迹精度的关系**：移除高影响物体后，49.3%的情况模型的最佳轨迹更接近真实轨迹（∆minADE为负），说明模型有时过度依赖某些物体。
- **白盒发现**：
  - 视觉编码器不区分物体类别（delta曲线在各类间几乎相同）。
  - VLM中，信号在层0-15平坦，从15层开始按类别分化（公交车信号最强）。
  - 识别出**四种传播模式**：耦合（33%，信号直接传递到输出）、透明（33%，信号几乎消失）、解耦（17%，在轨迹令牌处有信号但输出无差）、沉默（17%，无信号但输出有大幅偏差）。
  - **轨迹专家是非线性放大器**：平均放大倍数115-180×，沉默变种放大超过1000×，说明专家可以放大小到不存在的输入扰动。
  - Logit Lens显示模型内部不编码物体类别词汇级概念，而是编码场景几何和关系上下文。

#### 7. 优点
- **方法创新**：首次将反事实物体移除（逼真修复）与分布偏移度量（AD/FD）结合用于自动驾驶场景自监督因果归因，避免了对真实轨迹的依赖。
- **双管齐下**：黑盒行为审计 + 白盒机制分析，从“什么影响”深入到“如何影响”，提供了系统性的解释流程。
- **高质量基准**：Counter-nuScenes数据集包含3,062个逼真修复后的反事实图像对，可供社区使用。
- **揭示反直觉现象**：发现模型对看似无关物体的敏感性和排名不稳定性，强调了可解释性的必要性。专家非线性放大现象的发现对模型调试有启发。

#### 8. 不足与局限
- **模型与数据泛化性**：仅测试Alpamayo 1和nuScenes前视图，未验证在其他VLA模型（如UniAD、DriveLM）或其他数据集（Waymo、BDD100K）上的表现。单帧输入忽略了时间上下文对物体因果影响的叠加。
- **白盒工具局限性**：余弦delta和Logit Lens是从语言模型借用，可能不适用于VLA的连续几何表征。沉默变种表明存在现有探针无法检测的因果路径。
- **因果推断假设**：逼真修复假设“移除物体”等同于“物体不存在”，但修复可能引入其他伪影（如纹理延续错误），尽管文中声称减少了分布偏移，但仍存在微小偏差。
- **计算与资源信息缺失**：未报告GPU型号、耗时等，削弱了可复现性。
- **解释深度不足**：白盒分析能定位变化区域，但无法解释“为什么”模型对某些物体如此敏感（如网球拍）。

（完）
