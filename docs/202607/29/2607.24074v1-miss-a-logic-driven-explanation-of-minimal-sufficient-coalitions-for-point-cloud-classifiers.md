---
title: "MiSS: A Logic-Driven Explanation of Minimal Sufficient Coalitions for Point Cloud Classifiers"
title_zh: MiSS：点云分类器最小充分联盟的逻辑驱动解释
authors: "Mengda Xing, Jean-Marie Lagniez"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24074v1"
tags: ["query:xai-objdet"]
score: 9.0
evidence: 点云分类器的最小充分联盟逻辑解释方法
tldr: 点云分类器可解释性面临挑战，现有方法需要白盒模型或布尔特征。本文提出MiSS，一种黑盒查询框架，将超点划分作为抽象层，通过加权MaxSAT提出候选几何区域联盟，并由统计预言机验证扰动充分性，返回最小基数充分联盟。在ModelNet40和ShapeNet上，MiSS相比规则基线获得更高精度和覆盖率，且解释时间远低于穷举搜索。其贡献在于首次实现点云分类中基于逻辑驱动的黑盒充分性解释并保证最小基数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24074v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 606}, {\"url\": \"assets/figures/arxiv/2607.24074v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1430, \"height\": 1151}, {\"url\": \"assets/figures/arxiv/2607.24074v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1495, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2607.24074v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1497, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2607.24074v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1488, \"height\": 671}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24074v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1505, \"height\": 566}, {\"url\": \"assets/tables/arxiv/2607.24074v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1421, \"height\": 633}, {\"url\": \"assets/tables/arxiv/2607.24074v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 996, \"height\": 597}, {\"url\": \"assets/tables/arxiv/2607.24074v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1449, \"height\": 143}, {\"url\": \"assets/tables/arxiv/2607.24074v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1041, \"height\": 1357}, {\"url\": \"assets/tables/arxiv/2607.24074v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1020, \"height\": 1359}, {\"url\": \"assets/tables/arxiv/2607.24074v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1169, \"height\": 1255}, {\"url\": \"assets/tables/arxiv/2607.24074v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1399, \"height\": 423}]"
motivation: 现有点云分类解释方法受限于白盒或布尔特征空间，缺乏黑盒充分性推理。
method: 采用加权MaxSAT提出候选联盟，黑盒统计预言机验证充分性，结合启发式自适应策略保证最小基数。
result: 在ModelNet40和ShapeNet上，MiSS的精度和覆盖率优于规则基线，解释时间低于穷举搜索。
conclusion: MiSS为点云分类器提供了可证明最小基数的黑盒充分性解释框架。
---

## 摘要
我们提出MiSS，一个基于查询的黑盒框架，通过扰动相对充分性推理来解释3D点云分类器。MiSS将超点分割视为一个可解释的抽象层，并询问在指定扰动分布下，能否从几何区域的最小联盟中认证原始预测。与需要布尔特征空间或白盒逻辑编码的溯因解释器不同，MiSS将候选提议与验证分离：加权MaxSAT过程使用启发式自适应基数下限、认证精确大小回退、安全收紧上界、阻塞子句以及从先前预言机评估中学习的替代获取启发式来提议联盟，而黑盒统计预言机则通过预测查询决定充分性。该模型返回一个统计验证的充分联盟作为二值归因，并在认证搜索完成时保证最小基数。在ModelNet40和ShapeNet上使用PointNet和PointMLP分类器的实验表明，在大多数设置中，该方法比基于规则的基线具有更高的精确度和覆盖率，且解释时间低于穷举搜索。

## Abstract
We present MiSS, a black-box, query-based framework for explaining 3D point cloud classifiers through perturbation-relative sufficiency reasoning. MiSS treats a superpoint partition as an interpretable abstraction layer and asks whether the original prediction can be certified from a minimal coalition of geometric regions under a specified perturbation distribution. Unlike abductive explainers that require Boolean feature spaces or white-box logical encodings of the predictor, MiSS separates candidate proposal from verification: a weighted MaxSAT procedure proposes coalitions using a heuristic adaptive cardinality floor, certified exact-size fallback, a safely tightened upper bound, blocking clauses, and a surrogate acquisition heuristic learned from previous oracle evaluations, while a blackbox statistical oracle decides sufficiency from prediction queries. The system returns a statistically verified sufficient coalition as a binary attribution, with minimum cardinality guaranteed when certified search completes. Experiments on ModelNet40 and ShapeNet with PointNet and PointMLP classifiers show higher precision and coverage than rule-based baselines in most settings, with lower explanation time than exhaustive search.

---

## 论文详细总结（自动生成）

# MiSS: 点云分类器最小充分联盟的逻辑驱动解释 —— 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有点云分类器的解释方法受限于两个瓶颈：(1) 信号级归因（如Grad-CAM）产生离散、不稳定的点重要性图，缺乏对预测是否“充分”的认证；(2) 溯因解释（Abductive Explanation）需要白盒逻辑编码或布尔特征空间，难以扩展到连续输入的深度神经网络。因此，在点云这类稀疏、无序的3D数据上，**缺乏一种黑盒、统计可验证的充分性解释框架**，能返回语义上紧凑、基数最小的几何区域联盟。
- **整体含义**：MiSS（Minimal Sufficient Set）首次将**逻辑驱动的最小充分集概念**引入点云分类器解释，通过**超点（superpoint）划分**作为抽象层，**结合加权MaxSAT逻辑搜索与黑盒统计验证**，在保证解释精度和最小性的同时，规避了对白盒模型依赖。这项工作桥接了“可证明的溯因解释”与“经验性归因图”之间的鸿沟。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 2.1 核心思想
- **抽象层**：将点云分割为若干超点（几何上紧致的点簇），每个超点作为不可分割的解释单元。
- **扰动相对充分性**：定义一个概率阈值τ，若保留某联盟C中的超点、将其他超点替换为区域内分布补丁后，分类器保持原预测的概率 ≥ τ，则称C是τ-充分的。
- **最小联盟**：在充分联盟中寻找最小基数的一个，即MiSS。

### 2.2 关键技术细节
#### 2.2.1 问题形式化
- **定义3.1 (MiSS)**：联盟C ⊆ S是最小充分集，当且仅当：
  1. Pr[f(̃P<sub>C</sub>) = y] ≥ τ （充分性）
  2. ∀C' ⊊ C, Pr[f(̃P<sub>C'</sub>) = y] < τ （最小性）
- **单调性假设**（Assumption 3.2）：若S⊆T且S充分，则T也充分。该假设使得仅需检查一次移除即可认证最小性。

#### 2.2.2 算法流程（Algorithm 1）
- **输入**：点云P、黑盒分类器f、目标类y、超点集S、扰动生成器M、精度阈值τ、搜索预算等。
- **步骤**：
  1. **初始化**：评估所有单超点联盟，记录历史H；拟合初始替代模型（ridge回归），生成每个超点的软权重w<sub>i</sub>。
  2. **迭代搜索**（while循环）：
     - 构建加权CNF (Weighted CNF) Φ<sub>t</sub>：包含硬约束（基数窗口 [ℓ<sub>cur</sub>, L<sub>cur</sub>]、阻塞子句、失败子集剪枝子句）和软子句（(¬x<sub>i</sub>, q<sub>i</sub>)，q<sub>i</sub> ∝ 1‑w<sub>i</sub>，鼓励更短联盟）。
     - 用MaxSAT求解器（RC2+MapleSat）求解，得到候选联盟C。
     - **黑盒充分性验证**（Algorithm 4）：通过顺序KL界限测试（sequential KL‑bounds test），抽取扰动样本，直至以置信度δ<sub>test</sub>判定充分或不足。返回 (b, ˆp, n)。
     - 更新：若b=True，记录C，收紧上界L<sub>cur</sub>←|C|‑1；若False，添加失败子集剪枝子句。
     - 更新基数下限ℓ<sub>cur</sub>（启发式自适应，Algorithm 3：连续η次同尺寸失败后增加下限）。
     - 重新拟合替代模型，更新软子句。
  3. **输出**：从已记录的所有充分联盟中选取最小基数者，若无则返回∅。

#### 2.2.3 关键组件
- **替代引导启发式**：使用所有历史联盟的估计精度作为训练目标，拟合ridge回归，得到每个超点的权重w<sub>i</sub>∈[0,1]，转化为软子句权值。使得历史信息融入逻辑搜索，引导朝向可能包含高重要性超点的超集。
- **自适应基数窗口**：下限ℓ<sub>cur</sub>从单超点最大精度启发式初始化，并在足够多相同尺寸失败后递增；上限L<sub>cur</sub>在发现充分联盟后立即收紧。结合单调性，可成块剪枝所有子集。
- **认证回退**：当启发式下限≥上限时，回退到最小下限，确保窗口覆盖已跳过尺寸。若最终窗口UNSAT，则推出无更小充分联盟（Lemma 3.4），从而保证返回联盟的全局最小性（Theorem 3.5）。

## 3. 实验设计：数据集、基准、对比方法
### 3.1 数据集
- **点云**：
  - ModelNet40：12,311个CAD模型（40类），官方拆分9,843训练/2,468测试。
  - ShapeNet：12,137训练/2,874测试（为避免类别偏置，随机打乱测试列表）。
- **表格数据**：10个UCI/OpenML基准（Bank, Breast, Credit, Heart, Mushroom, Optdigits, Splice, Vowel, Wine, Quality），维度11-64。

### 3.2 目标分类器
- PointNet（训练200 epochs，Adam，lr=1e-3，batch=32）
- PointMLP（训练200 epochs，SGD，lr=0.1→5e-3，cosine decay，momentum=0.9，weight decay=2e-4）
- 表格实验：逻辑回归。

### 3.3 对比基线
- Anchors, LORE, SkopeRules, CORELS（规则/锚点类解释器）
- GradCAM（仅可视化）
- 对每个基线做最小必要适配以支持超点输入。

### 3.4 评估指标
- **Precision（精确率）**：Pr[f(̃P<sub>C</sub>) = y]，反映充分性。
- **Coverage（覆盖率）**：扰动采样中包含C的比例，衡量通用性。
- **Runtime（运行时间）**：解释生成时间（秒）。

## 4. 资源与算力
- **文中未明确说明使用的GPU型号、数量或训练时长**。仅提到目标分类器（PointNet和PointMLP）各训练200个epoch，但具体硬件资源（如单卡/多卡、GPU类型、总GPU小时）未报告。
- 搜索部分在CPU上进行（使用PySAT与MapleSat），也未提供CPU型号、内存等细节。资源信息缺失，因此无法评估算力需求。

## 5. 实验数量与充分性
- **实验数量**：
  - 点云部分：2个数据集 × 2个分类器 = 4组主实验，每组包含MiSS（完整搜索）、MiSS*（第一个充分联盟）及4个基线，共约4×6=24个配置。每个配置报告Precision、Coverage、Runtime。
  - 表格部分：10个数据集 × 6个方法 = 60个配置。
  - 消融研究：4个变体（默认、无替代、无动态边界、两者皆无）在2数据集×2分类器上比较。
  - 搜索预算敏感性：4个时间预算（30-240s）在ModelNet40和ShapeNet上。
  - 求解器敏感性：8个SAT求解器在4个点云配置上。
  - 超参数敏感性：对最大联盟尺寸、扰动强度、邻域搜索阈值、超点数目进行扫参。
  - **总计超过100组实验配置，数量充分**。
- **实验公平性**：基线均按原论文实现并做最小适配，指标一致。但点云数据集仅两类（ModelNet40、ShapeNet），未包含更复杂的数据集（如ScanObjectNN、Semantic3D），且分类器仅PointNet和PointMLP，未测试PointNet++、Point Transformer等现代架构，**泛化性验证有限**。
- **统计验证**：充分性检验使用KL界限，置信度δ<sub>test</sub>=0.05，保证了统计可靠性。

## 6. 论文的主要结论与发现
1. MiSS在点云基准上**显著优于规则基线**：在两个数据集和两个分类器上，MiSS和MiSS*的Precision均最高（如ModelNet40上PointMLP下Prec≈87%，而Anchors仅58.62%），Coverage与LORE等持平或更高。
2. MiSS*（第一个充分联盟）在**精度与完整搜索接近，但运行时间减半**，表明启发式能快速找到高质量候选。
3. 消融实验显示，**替代引导和自适应基数窗口**均是有效组件：移除它们会导致更高的运行时间或更低的精度。
4. 搜索预算增加（30→240s）对精度/覆盖率影响很小，说明算法能迅速找到满意解释。
5. 表格数据上MiSS在精度和覆盖率上整体与规则基线竞争，部分数据集最高精度的同时略慢。

## 7. 优点
- **黑盒特性**：无需白盒逻辑编码或梯度信息，仅需查询分类器预测，适用于任意连续输入模型。
- **可证明最小性**：在UNSAT窗口认证后，返回的联盟保证是最小基数充分联盟（Theorem 3.5）。
- **统计鲁棒性**：使用顺序KL界限进行充分性判断，受控置信度，避免人为阈值一刀切。
- **高效搜索**：替代引导+自适应窗口+阻塞子句，大幅减少需验证的候选数量，实测运行时间远低于穷举（如ShapeNet上MiSS*仅4.08s vs 穷举未报告但推测极长）。
- **可解释性强**：超点作为语义区域单元输出，便于人类理解。

## 8. 不足与局限
- **依赖超点质量和扰动分布**：解释质量高度依赖于初始超点分割的语义合理性和扰动生成器是否保留数据流形。若分割粗糙或扰动产生分布外样本，验证将失真。
- **最小性保证仅在认证完成时成立**：若搜索被时间/查询预算中止，返回的联盟虽统计充分，但不保证全局最小。
- **单调性假设不总是成立**：现实情况可能存在反单调性（移除某超点后精度反而升高），此时一次移除检查只保证单步最小性。
- **实验覆盖不足**：点云数据集仅有两个（ModelNet40、ShapeNet），且分类器仅两种。未测试更复杂的架构（如PointNet++、Point Transformer）或户外点云数据集（如SemanticKITTI），**结果的泛化性存疑**。
- **表格数据表现有起伏**：在部分数据集（如Breast）上Precision低于CORELS，且运行时间在某些情况下高于规则方法。
- **缺乏人类评估**：论文没有做用户研究来验证解释的“实际可理解性”或“信任度”，仅使用自动化指标。
- **未报告完整算力细节**：复现所需算力不透明，不利于成本评估。

（完）
