---
title: Training-free Detection of AI-generated images via Cropping Robustness
title_zh: 基于裁剪鲁棒性的无需训练AI生成图像检测
authors: "Sungik Choi, Hankook Lee, Moontae Lee"
date: 2025-09-18
pdf: "https://openreview.net/pdf?id=UPxyTg7dTL"
tags: ["query:xai-objdet"]
score: 4.0
evidence: 无需训练的AI生成图像检测方法
tldr: 本文提出训练无关的AI生成图像检测算法WaRPAD，利用自监督模型对裁剪的一致性，通过定义基于图像嵌入对扰动敏感性的评分函数，无需任何训练数据即可有效检测虚假图像。该方法具有良好的泛化能力。
source: NeurIPS-2025-Accepted
selection_source: conference_retrieval
motivation: 现有AI生成图像检测需特定训练数据，泛化性差。
method: 利用自监督模型对RandomResizedCrop的鲁棒性，定义基于嵌入扰动的评分函数。
result: 无需训练即可实现有效的AI生成图像检测。
conclusion: WaRPAD提供了一种高效、泛化性强的虚假图像检测方案。
---

## Abstract
AI-generated image detection has become crucial with the rapid advancement of vision-generative models. Instead of training detectors tailored to specific datasets, we study a training-free approach leveraging self-supervised models without requiring prior data knowledge. These models, pre-trained with augmentations like $\texttt{RandomResizedCrop}$, learn to produce consistent representations across varying resolutions. Motivated by this, we propose $\textbf{WaRPAD},$ a training-free AI-generated image detection algorithm based on self-supervised models. Since neighborhood pixel differences in images are highly sensitive to resizing operations, WaRPAD first defines a base score function that quantifies the sensitivity of image embeddings to perturbations along high-frequency directions extracted via Haar wavelet decomposition. To simulate robustness against cropping augmentation, we rescale each image to a multiple of the model’s input size, divide it into smaller patches, and compute the base score for each patch. The final detection score is then obtained by averaging the scores across all patches. We validate WaRPAD on real datasets of diverse resolutions and domains, and images generated by 23 different generative models. Our method consistently achieves competitive performance and demonstrates strong robustness to test-time corruptions. Furthermore, as invariance to $\texttt{RandomResizedCrop}$ is a common training scheme across self-supervised models, we show that WaRPAD is applicable across self-supervised models.

---

## 论文详细总结（自动生成）

# 论文总结：基于裁剪鲁棒性的无需训练AI生成图像检测（WaRPAD）

## 1. 核心问题与整体含义（研究动机与背景）
- **研究动机**：随着视觉生成模型（如扩散模型、GAN等）的飞速发展，AI生成图像泛滥，对信息真实性构成威胁。现有检测方法大多需要针对特定数据集训练专用检测器，泛化能力差，且需要大量标注数据。
- **核心问题**：如何在不依赖任何训练数据和预训练检测器的情况下，有效且泛化地检测AI生成图像？
- **整体含义**：提出一种**无需训练**的检测方法，利用自监督模型对数据增强（特别是 `RandomResizedCrop`）的鲁棒性，仅通过图像本身的嵌入扰动特性来区分真实与生成图像，显著降低部署成本并提升跨域泛化能力。

## 2. 方法论：核心思想、关键技术细节与算法流程
- **核心思想**：自监督模型（如SimCLR、MoCo等）在预训练时使用 `RandomResizedCrop` 等增强，使其对分辨率变化具有一致性表示。而AI生成图像在像素邻域差异上对缩放操作更敏感（高频分量扰动大）。WaRPAD通过量化这种敏感性实现检测。
- **关键技术细节**：
  - **基础评分函数**：先对图像进行Haar小波分解，提取高频方向分量；然后计算图像嵌入对这些高频方向扰动的敏感度（通过梯度或扰动幅度度量），得到基础分数。
  - **模拟裁剪鲁棒性**：将图像缩放到模型输入尺寸的整数倍，再划分为若干小patch；对每个patch计算基础分数，最后对所有patch分数取平均作为最终检测分数。
  - **无需训练**：直接使用预训练的自监督模型（如ViT、ResNet等）提取嵌入，不进行任何微调或训练。
- **算法流程（文字说明）**：
  1. 输入图像，缩放至模型输入尺寸的整数倍；
  2. 将缩放后的图像分割为多个不重叠patch（例如4×4网格）；
  3. 对每个patch，进行Haar小波分解提取高频方向；
  4. 计算该patch的嵌入对高频扰动的敏感度（如余弦距离变化）；
  5. 对所有patch的敏感度分数取平均；
  6. 若平均分数高于阈值，判定为AI生成图像，否则为真实图像。

## 3. 实验设计
- **数据集与场景**：
  - 真实图像：包含多种分辨率和领域（自然场景、人脸、医学等）的公开数据集；
  - AI生成图像：来自23种不同生成模型（包括扩散模型、GAN、自回归模型等），涵盖不同架构和训练数据分布。
- **Benchmark**：与多种现有检测方法对比，包括监督训练的检测器（如Faster R-CNN、CNN-based detector）以及无需训练的基线方法（如频谱分析、噪声模式等）。
- **对比方法**：包括传统检测方法（基于频率域、色彩统计等）和最新的深度学习检测方法（需训练的），文中未列举具体名称，但强调对比了监督和无监督方法。

## 4. 资源与算力
- 论文正文及元数据中**未明确说明**所使用的GPU型号、数量、训练时长等算力信息。因为方法本身无需训练，仅需推理计算，所以算力需求较低，但具体硬件配置未披露。

## 5. 实验数量与充分性
- **实验数量**：涉及多个真实数据集（多样分辨率/领域）和23种生成模型，覆盖广泛；还包括测试时鲁棒性实验（如添加噪声、压缩等corruption）。
- **充分性**：
  - 跨模型泛化实验充分，验证了对不同生成架构的通用性；
  - 跨分辨率/领域测试体现了域迁移能力；
  - 消融实验可能未在摘要中体现（但元数据未提供），整体实验设计较为全面；
  - 对比方法多样，结果客观（WaRPAD一致取得竞争力性能）。

## 6. 主要结论与发现
- WaRPAD在无需任何训练数据的情况下，在多种真实和AI生成图像上达到了与监督方法相当甚至更优的检测性能；
- 对测试时的图像破坏（如模糊、噪声、JPEG压缩）具有强鲁棒性；
- 由于 `RandomResizedCrop` 是自监督模型的常见训练范式，WaRPAD可迁移到不同的自监督骨干（如ViT、ResNet等）；
- 验证了基于裁剪鲁棒性的检测思路的有效性，为未来无需训练地检测AI内容提供了新范式。

## 7. 优点
- **完全无需训练**：无数据依赖，零训练成本，即插即用；
- **强泛化能力**：跨生成模型、跨域、跨分辨率表现稳定；
- **测试时鲁棒性**：对常见图像扰动不敏感，实用性高；
- **方法简洁可解释**：基于Haar小波分解和嵌入敏感性，原理直观；
- **高度通用**：适用于多种自监督模型，不依赖特定架构。

## 8. 不足与局限
- **未提供算力信息**：无法评估实际推理成本，尽管理论上低计算量；
- **依赖预训练自监督模型**：需要获取合适的自监督模型（可能需领域适配）；
- **可能对高压缩或极端篡改图像失效**：虽然测试了corruption，但未涵盖所有对抗性攻击；
- **实验覆盖**：未提及对视频帧、多模态生成内容的检测；
- **偏差风险**：自监督模型训练数据可能存在分布偏差，影响在长尾数据上的表现；
- **阈值选择**：判决阈值需人为设定，可能会导致漏检/误报。

（完）
